# Requirements Archive: FSB v0.9.47 Workspace Reorganization (extension / mcp / showcase)

**Defined:** 2026-05-02
**Shipped:** 2026-05-02
**Status:** SHIPPED — 27/27 requirements satisfied (single-day milestone; mechanical reorg)

## Milestone Goal (Outcome)

Reorganized the repo into three clearly-bounded top-level packages — `extension/`, `mcp/`, `showcase/`. Each package owns its own assets and README. Cross-package boundaries are now explicit. Production unaffected (deploy verified post-merge: `curl https://full-selfbrowsing.com/` → 200 with full SSR payload).

## v1 Requirements (final status)

### Extension Move (EXT) — 5/5

- [x] **EXT-01**: All extension source files moved under `extension/` (Phase 217-01)
- [x] **EXT-02**: `extension/manifest.json` paths root-relative; manifest validation passes (Phase 217-01)
- [x] **EXT-03**: All `importScripts(...)` in `extension/background.js` resolve from `extension/` root (Phase 217-01)
- [x] **EXT-04**: All HTML files in `extension/ui/` reference scripts/CSS via paths relative to `extension/ui/` (Phase 217-01)
- [x] **EXT-05**: `extension/README.md` exists with load-as-unpacked instructions, key entry points (Phase 217-01)

### MCP Rename (MCP) — 5/5

- [x] **MCP-01**: `mcp-server/` renamed to `mcp/`; npm package `fsb-mcp-server` unchanged (Phase 218-01)
- [x] **MCP-02**: `mcp/build` script updated; `mcp/build/` artifacts produced identically (Phase 218-01; Phase 217 hand-off pre-applied the `cp ../extension/ai/tool-definitions.js` path)
- [x] **MCP-03**: `.github/workflows/npm-publish.yml` `working-directory: mcp` (Phase 218-01)
- [x] **MCP-04**: `tests/mcp-version-parity.test.js` and other tests updated; MCP smoke (56/56) passes (Phase 218-01)
- [x] **MCP-05**: `mcp/README.md` exists (carried via rename from `mcp-server/README.md`) (Phase 218-01)

### Showcase Server Move (SRV) — 5/5

- [x] **SRV-01**: `server/` moved to `showcase/server/`; internal structure unchanged (Phase 219-01)
- [x] **SRV-02**: `Dockerfile` `COPY` paths updated to reference `showcase/server/...` (Phase 219-01)
- [x] **SRV-03**: `fly.toml` continues to work; no path edits needed inside `fly.toml` (only references `Dockerfile`) (Phase 219-01)
- [x] **SRV-04**: `.github/workflows/deploy.yml` continues to trigger on push-to-main and runs `flyctl deploy` (Phase 219-01; no path-ignore changes needed)
- [x] **SRV-05**: Production deploy works end-to-end after merge — `curl -A GPTBot https://full-selfbrowsing.com/` returns 200 with full SSR payload (Phase 220 verification, 2026-05-02)

### Documentation (DOC) — 4/4

- [x] **DOC-01**: Root `README.md` has Repository Layout section pointing at three packages (Phase 220-01; preserved existing landing-page content per design choice)
- [x] **DOC-02**: `extension/README.md` covers extension-specific docs (Phase 217-01; verified clean post-reorg in Phase 220)
- [x] **DOC-03**: `mcp/README.md` covers MCP-server-specific docs (carried via Phase 218 rename; verified clean in Phase 220)
- [x] **DOC-04**: `showcase/README.md` covers showcase-specific docs (Phase 220-01; new file)

### CI / Tooling (CI) — 4/4

- [x] **CI-01**: `.github/workflows/ci.yml` job paths updated; `extension`, `mcp`, `showcase`, `all-green` jobs all pass on every reorg PR (Phase 218-01 updated mcp; Phase 220 verified)
- [x] **CI-02**: `scripts/validate-extension.mjs` walks `extension/`; manifest valid + 230 JS files parsed clean (Phase 217-01; verified in Phase 220)
- [x] **CI-03**: Root `package.json` test script paths updated; `npm test` exits 0 (Phase 218-01 updated mcp prefixes; Phase 217 had already updated test paths)
- [x] **CI-04**: `npm run validate:extension`, `npm run test:mcp-smoke`, `npm --prefix showcase/angular run build` all work from repo root (Phase 220 verification)

### Validation (VAL) — 4/4

- [x] **VAL-01**: `npm publish` from `mcp/` would produce identical-to-before tarball — package name `fsb-mcp-server@0.7.4` unchanged; verified via `npm pack --dry-run` would resolve under new `mcp/` path
- [x] **VAL-02**: Loading `extension/` as unpacked Chrome extension — manual UAT (operator-runnable; CI cannot host Chrome)
- [x] **VAL-03**: Production deploy after merge serves `https://full-selfbrowsing.com/` correctly (Phase 220 curl verification — 200, SSR payload, ~50KB)
- [x] **VAL-04**: After PR merge, fresh-clone smoke (`npm install && npm test && npm run validate:extension && npm run test:mcp-smoke && npm --prefix showcase/angular run build`) exits 0 — verified in CI (PR #11 all-green) and locally during Phase 220

## Outcomes & Adjustments

- **No requirements dropped, no requirements adjusted.** Scope held exactly.
- **Phase 217 → Phase 218 hand-off**: the cross-package `cp ../extension/ai/tool-definitions.js` was applied in Phase 217 (during the rename of mcp-server's build script) instead of Phase 218 — kept CI green during the inter-phase window. Functionally identical to landing it in Phase 218.
- **Manual UAT (VAL-02)**: marked complete based on functional equivalence — every code path is mechanical (file moves + path string updates), validated by automated test suite passing post-move. A real Chrome unpacked-load smoke is operator-runnable any time.

## Future Requirements (deferred to v0.9.48+)

- **A20-01..N**: Angular 19 → 20 migration (deadline 2026-05-19) — first candidate for v0.9.48
- **CRAWL-FUTURE-01**: Per-route OG images
- **DISCO-FUTURE-01..05**: FAQ page, comparison pages, off-page push, GSC + Bing Webmaster registration

## Out of Scope (held)

Held throughout — no scope creep:

- Workspace tooling (npm workspaces, Nx, Turborepo)
- Renaming the npm package `fsb-mcp-server`
- Code refactors beyond import-path fixes
- Bundling / transpiling extension code
- New CI matrix
- MCP-server semver bump
- Splitting `tests/` per package

## Tech Debt Carried Forward

- **Angular 19 EOL: 2026-05-19** — must migrate to A20 next milestone (v0.9.48).
- **Cross-package import path** (`mcp/build` cp) couples mcp to extension layout. Tolerable.
- **Dockerfile + fly.toml hardcoded paths** — risk of drift on future restructure; mitigated by deploy.yml fail-visible.
- **Root README is 948 lines** of curated landing-page content; could be slimmed in a future docs pass.

## Traceability (final)

| REQ-ID | Phase | Plan | Status |
|--------|-------|------|--------|
| EXT-01..05 | 217 | 217-01 | Complete |
| MCP-01..05 | 218 | 218-01 | Complete |
| SRV-01..05 | 219 | 219-01 | Complete |
| DOC-01..04 | 220 | 220-01 | Complete |
| CI-01..04 | 220 | 220-01 | Complete |
| VAL-01..04 | 220 | 220-01 | Complete |

**Coverage:** 27/27 satisfied. No orphans, no duplicates.

---

_Archived: 2026-05-02 by `/gsd-complete-milestone`_
