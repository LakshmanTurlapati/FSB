---
phase: 11-control-panel-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - options.js
  - options.html
autonomous: true

must_haves:
  truths:
    - "No JS references to non-existent HTML elements (speedModeNormal, speedModeFast, apiStatusCard, quickDebugMode, quickConfirmSensitive removed from JS)"
    - "DOM Optimization Stats section removed from options page (placeholder with no real data)"
    - "No duplicate DOMContentLoaded listeners in options.js"
    - "Legacy speedMode save logic removed from saveSettings"
  artifacts:
    - path: "options.js"
      provides: "Clean options page JS with no orphaned element references"
      contains: "cacheElements"
    - path: "options.html"
      provides: "Options page HTML with DOM Optimization Stats section removed"
  key_links:
    - from: "options.js"
      to: "options.html"
      via: "getElementById calls"
      pattern: "getElementById"
---

<objective>
Remove dead UI code from the options page: orphaned element references in options.js that point to HTML elements that do not exist, the placeholder DOM Optimization Stats section, and the legacy speedMode save logic.

Purpose: Eliminate dead code and misleading UI in the options/control panel page.
Output: Clean options.js and options.html with no orphaned references or placeholder sections.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-control-panel-refinement/11-RESEARCH.md
@options.js
@options.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove orphaned element references and dead code from options.js</name>
  <files>options.js</files>
  <action>
Remove ALL references to the following orphaned element IDs that exist in JavaScript but NOT in HTML:

1. **speedModeNormal** and **speedModeFast** (line ~161-162 in cacheElements):
   - Remove from cacheElements(): delete `elements.speedModeNormal = document.getElementById('speedModeNormal');` and `elements.speedModeFast = document.getElementById('speedModeFast');`
   - Remove from setupEventListeners() formInputs array (line ~218-219): remove `elements.speedModeNormal` and `elements.speedModeFast` from the array
   - Remove from loadSettings() (lines ~615-619): delete the `if (settings.speedMode === 'fast')` block that sets speedModeFast/speedModeNormal checked
   - Remove from loadSettings() (line ~645): delete `if (elements.quickDebugMode) elements.quickDebugMode.checked = settings.debugMode;` and `if (elements.quickConfirmSensitive) elements.quickConfirmSensitive.checked = settings.confirmSensitive;`
   - Remove from saveSettings() (line ~666): delete `speedMode: elements.speedModeFast?.checked ? 'fast' : 'normal',` line (the legacy speedMode save)
   - Keep `speedMode` in defaultSettings for backward compatibility reading, but do NOT write it on save

2. **apiStatusCard** (line ~184 in cacheElements):
   - Remove `elements.apiStatusCard = document.getElementById('apiStatusCard');` from cacheElements()
   - In updateApiStatusCard() function (lines ~808-823): Add a guard `if (!elements.apiStatusCard) return;` at the top -- actually this already exists at line 809. The function is safe as-is. No changes needed to updateApiStatusCard.

3. **quickDebugMode** and **quickConfirmSensitive** (line ~174-175 in cacheElements):
   - Remove `elements.quickDebugMode = document.getElementById('quickDebugMode');` and `elements.quickConfirmSensitive = document.getElementById('quickConfirmSensitive');` from cacheElements()
   - Remove the quick toggle sync blocks in setupEventListeners() (lines ~292-312): the entire `if (elements.quickDebugMode && elements.debugMode)` block and the `if (elements.quickConfirmSensitive && elements.confirmSensitive)` block

4. **Consolidate DOMContentLoaded listeners**:
   - There are two DOMContentLoaded listeners: line 82 (`initializeDashboard`) and line 2337 (calls `initializeSessionHistory` with 500ms delay). Move the `initializeSessionHistory` call into the `initializeDashboard()` function (at the end, before the final console.log). Use `setTimeout(initializeSessionHistory, 500)` inside initializeDashboard to preserve the delay. Then remove the second DOMContentLoaded listener block (lines 2337-2340).

Do NOT remove the `debugMode` property from defaultSettings or from the loadSettings/saveSettings logic -- that setting is real and will be wired in plan 02.
  </action>
  <verify>
Search options.js for "speedModeNormal", "speedModeFast", "quickDebugMode", "quickConfirmSensitive" -- should find zero matches except potentially in comments.
Search for "apiStatusCard" -- should only appear in cacheElements (the getElementById call is fine to keep for now since updateApiStatusCard already guards with null check), OR remove it entirely.
Count DOMContentLoaded listeners -- should be exactly 1.
  </verify>
  <done>
No orphaned element references remain in options.js. Legacy speedMode write removed from saveSettings. Single DOMContentLoaded entry point.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove DOM Optimization Stats placeholder from options.html</name>
  <files>options.html</files>
  <action>
In options.html, remove the entire "Optimization Stats" form-group within the DOM Optimization section. This is the block at approximately lines 316-322:

```html
<div class="form-group">
  <label>Optimization Stats</label>
  <div class="stats-info" id="domOptimizationStats">
    <i class="fas fa-info-circle"></i>
    <span>Enable optimization to see compression statistics</span>
  </div>
</div>
```

Remove this entire form-group div. Keep all other DOM Optimization controls (the toggle, max elements slider, and viewport prioritize toggle) as they are properly wired.

Also verify there are no JS references to `domOptimizationStats` in options.js (grep confirmed there are none).
  </action>
  <verify>
Search options.html for "domOptimizationStats" -- should find zero matches.
Search options.html for "Optimization Stats" label -- should find zero matches.
The DOM Optimization section should still contain the 3 working controls (domOptimization toggle, maxDOMElements slider, prioritizeViewport toggle).
  </verify>
  <done>
DOM Optimization Stats placeholder removed. Only functional, wired controls remain in the DOM Optimization section.
  </done>
</task>

</tasks>

<verification>
1. Grep options.js for orphaned IDs: speedModeNormal, speedModeFast, quickDebugMode, quickConfirmSensitive -- zero matches
2. Grep options.js for "speedMode:" in saveSettings context -- should not write speedMode to storage
3. Grep options.html for "domOptimizationStats" -- zero matches
4. Count DOMContentLoaded in options.js -- exactly 1
5. The options page loads without console errors (user verifies manually)
</verification>

<success_criteria>
- Zero references to speedModeNormal, speedModeFast, quickDebugMode, quickConfirmSensitive in options.js
- Legacy speedMode write removed from saveSettings
- DOM Optimization Stats placeholder removed from options.html
- Single DOMContentLoaded initialization entry point in options.js
- All remaining options page controls (debugMode, confirmSensitive, DOM optimization toggles, sliders) still functional
</success_criteria>

<output>
After completion, create `.planning/phases/11-control-panel-refinement/11-01-SUMMARY.md`
</output>
