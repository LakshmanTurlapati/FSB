---
phase: 259-test-rewrites-ci-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/mcp-visual-session-contract.test.js
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-05
requirements_addressed:
  - TEST-01
  - TEST-02
  - TEST-05
user_setup: []

must_haves:
  truths:
    - "tests/mcp-visual-session-contract.test.js asserts the v0.9.62 IMPLICIT contract end-to-end: allowlist behaviour unchanged from v0.9.36, persistence replay works against the Phase 256 key namespace, action calls without visual_reason/client are rejected with VISUAL_FIELDS_REQUIRED, action calls with non-allowlisted client are rejected with BADGE_NOT_ALLOWED, cross-agent action on owned tab is rejected with TAB_NOT_OWNED BEFORE lifecycle, removed tool names return TOOL_REMOVED."
    - "Calling either removed tool name ('start_visual_session' / 'end_visual_session') through the dispatch path returns a typed TOOL_REMOVED error with the migration recipe pointer; no bridge.sendAndWait / queue.enqueue invocation happens."
    - "No assertion in the rewritten file depends on the explicit v0.9.0 start_visual_session / end_visual_session flow that was removed in Phase 258."
    - "npm test exits 0 with the rewritten contract test in the chain."
  artifacts:
    - path: "tests/mcp-visual-session-contract.test.js"
      provides: "End-to-end v0.9.62 implicit contract test (allowlist + persistence + dispatch validation + ownership precedence + TOOL_REMOVED) covering TEST-01 and TEST-02."
      contains: "VISUAL_FIELDS_REQUIRED|BADGE_NOT_ALLOWED|TOOL_REMOVED|visual_reason|client|is_final"
      min_lines: 400
---

<objective>
Rewrite `tests/mcp-visual-session-contract.test.js` END-TO-END for the v0.9.62 implicit contract. The file existed pre-v0.9.62 (690 lines, 4 cases) and tested the explicit start_visual_session / end_visual_session flow. Phase 258 removed those tools (converted to TOOL_REMOVED stubs); their tests are obsolete. The rewrite:

1. KEEPS the allowlist normalization + manager lifecycle case (lines 40-153) where it tests `MCP_VISUAL_CLIENT_LABELS` + `normalizeMcpVisualClientLabel` + helpers. That surface is still alive.
2. KEEPS the persistence replay case (lines 154-269) but updates assertions to the Phase 256 key namespace `mcpVisualSession:<tabId>` if the existing case targets `fsbMcpVisualSessions`.
3. REPLACES the dispatcher validation case (lines 270-561) with v0.9.62 implicit-contract dispatcher validation: missing `visual_reason` returns VISUAL_FIELDS_REQUIRED; missing `client` returns VISUAL_FIELDS_REQUIRED; non-allowlisted `client` returns BADGE_NOT_ALLOWED; valid call passes through to the underlying bridge.
4. REPLACES the Phase 240 ownership-aware start_session case (lines 562-end) with v0.9.62 ownership-precedence: cross-agent action on an owned tab rejects with TAB_NOT_OWNED BEFORE the lifecycle hook fires (no recordVisualSessionTick call).
5. ADDS a new TOOL_REMOVED case: calling either removed tool name through the dispatcher returns the typed TOOL_REMOVED error with the migration recipe pointer; no bridge / queue invocation.

Covers TEST-01 (end-to-end rewrite), TEST-02 (TOOL_REMOVED case), TEST-05 (CI lock -- the file already runs as part of npm test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v0.9.62-CONTRACT.md
@.planning/phases/259-test-rewrites-ci-lock/259-CONTEXT.md
@.planning/phases/255-schema-enforcement-on-action-tools/255-01-SUMMARY.md
@.planning/phases/256-sliding-window-lifecycle-implicit-start-60s-death-timer-sw-eviction-replay/256-01-SUMMARY.md
@.planning/phases/258-removal-migration-errors-package-0.9.0/258-01-SUMMARY.md
@tests/mcp-visual-session-contract.test.js
@tests/visual-session-schema-lock.test.js
@tests/mcp-visual-tick-lifecycle.test.js
@extension/utils/mcp-visual-session.js
@extension/utils/mcp-visual-session-lifecycle.js
@extension/ws/mcp-bridge-client.js
@extension/ws/mcp-tool-dispatcher.js
@mcp/src/tools/visual-session.ts
@mcp/src/errors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite tests/mcp-visual-session-contract.test.js for v0.9.62 contract</name>

  <read_first>
    - `tests/mcp-visual-session-contract.test.js` (entire file, 690 lines -- understand existing 4 cases)
    - `tests/visual-session-schema-lock.test.js` (Phase 255 -- understand the recording-mock pattern)
    - `tests/mcp-visual-tick-lifecycle.test.js` (Phase 256/257 -- understand the createChromeMock pattern)
    - `extension/utils/mcp-visual-session.js` (allowlist module -- API surface for the kept allowlist case)
    - `extension/utils/mcp-visual-session-lifecycle.js` (Phase 256 module -- API surface for the persistence replay case if it targets the new namespace)
    - `extension/ws/mcp-bridge-client.js` (the dispatcher containing `_handleExecuteAction` -- the new dispatcher-validation case calls into this path or a mock thereof)
    - `mcp/src/tools/visual-session.ts` (the TOOL_REMOVED stubs -- the new case asserts against the stub behaviour)
    - `mcp/src/errors.ts` (typed-error registry)
    - `.planning/v0.9.62-CONTRACT.md` (canonical lists + typed-error names + body shapes)
    - `.planning/phases/259-test-rewrites-ci-lock/259-CONTEXT.md` (the rewrite decisions)
  </read_first>

  <files>tests/mcp-visual-session-contract.test.js</files>

  <action>
Rewrite the file in place. Keep the existing test scaffolding (`assert`, `assertEqual`, `assertDeepEqual`, the main runner) and the case structure (4-5 named `run<Name>Case` functions called from the main runner).

Structure the rewritten file with these cases (in order):

1. **runAllowlistAndManagerCase** (KEEP from existing lines 40-153, with minor reads if module API drifted):
   - Asserts `MCP_VISUAL_CLIENT_LABELS` length is reasonable, contains canonical labels (Claude, Codex, etc.), and is read-only.
   - Asserts `normalizeMcpVisualClientLabel('claude')` -> 'Claude', `normalizeMcpVisualClientLabel('CLAUDE')` -> 'Claude', `normalizeMcpVisualClientLabel('NotAClient')` -> null.
   - Asserts `isAllowedMcpVisualClientLabel('Claude')` -> true, `isAllowedMcpVisualClientLabel('NotAClient')` -> false.
   - Asserts `getAllowedMcpVisualClientLabels()` returns a copy (mutating the returned array does NOT mutate the module's internal state).

2. **runPersistenceReplayCase** (REWRITE from existing lines 154-269):
   - Targets the v0.9.62 namespace `mcpVisualSession:<tabId>` keys in `chrome.storage.session` (the Phase 256 module), NOT the v0.9.36 `fsbMcpVisualSessions` key.
   - Asserts: populate storage with a live entry (`deadlineAt > now`) and a stale entry (`deadlineAt <= now`); call `restoreVisualSessionLifecyclesFromStorage()`; assert live entry's alarm is recreated with the correct `when`; stale entry triggers immediate clear (storage key removed, no alarm).
   - The case may share fixtures with `tests/mcp-visual-tick-lifecycle.test.js` Case F (SW-eviction restore) but is independent -- this case is run as part of the CONTRACT suite for traceability.

3. **runImplicitDispatcherValidationCase** (NEW; replaces existing lines 270-561 dispatcher validation):
   - Constructs a recording-mock chrome + a fake bridge + agent registry that issues a known agent_id.
   - Dispatches an action tool name (e.g. `click`) with NO `visual_reason`; asserts the response is `{ code: 'VISUAL_FIELDS_REQUIRED', ... }` AND `bridge.sendAndWait` was NOT called.
   - Dispatches `click` with `visual_reason: 'r'` but NO `client`; asserts VISUAL_FIELDS_REQUIRED again.
   - Dispatches `click` with `visual_reason: 'r'` and `client: 'NotARealClient'`; asserts `{ code: 'BADGE_NOT_ALLOWED', ... }` AND bridge NOT called.
   - Dispatches `click` with valid `visual_reason: 'r'` AND valid `client: 'Claude'`; asserts the bridge IS called with the action params (with the visual fields stripped from the action params and forwarded as the sidecar `visualSession`).

4. **runOwnershipPrecedenceCase** (NEW; replaces existing lines 562-end):
   - Constructs a recording mock; registers agent A; agent A starts a session on tab 100 (via action call with valid bundle).
   - Agent B (different agent_id) attempts an action on tab 100 with a valid bundle.
   - Asserts: response is `{ code: 'TAB_NOT_OWNED', ... }`; lifecycle helper `recordVisualSessionTick` was NOT called for agent B's attempt (verifiable via instrumentation hook on the lifecycle module or via storage inspection -- agent A's entry on tab 100 is untouched).
   - Asserts: agent A's existing storage entry still has agentId === 'A', visualReason / client unchanged.

5. **runToolRemovedCase** (NEW):
   - Constructs a recording-mock MCP server with the visual-session.ts stubs loaded.
   - Invokes the dispatcher with tool name `'start_visual_session'`; asserts: response has `code === 'TOOL_REMOVED'`, `removed_tool === 'start_visual_session'`, `removed_in_version === '0.9.0'`, AND `references` array contains substrings 'CHANGELOG' and 'README' (loose match -- the exact anchors may evolve).
   - Asserts: NO call to `bridge.sendAndWait` / `queue.enqueue` (verified via recording-mock counters).
   - Repeats for `'end_visual_session'`.

Main runner at the bottom:
```javascript
(async () => {
  await runAllowlistAndManagerCase();
  await runPersistenceReplayCase();
  await runImplicitDispatcherValidationCase();
  await runOwnershipPrecedenceCase();
  await runToolRemovedCase();
  console.log(`\n=== Results: ${passed} passed, ${failed} failed ===`);
  process.exit(failed === 0 ? 0 : 1);
})();
```

Use the existing assertion harness from the top of the file. Use `createChromeMock` or equivalent recording-mock pattern from `tests/mcp-bridge-client-lifecycle.test.js` for chrome.storage.session + chrome.alarms + chrome.runtime + chrome.tabs.

Use require() (CJS) -- the existing file is CJS.

Run `node tests/mcp-visual-session-contract.test.js` after writing. Confirm exit 0, no FAIL lines.

Then run `npm test` from the repo root. Confirm exit 0.

Commit with message: `test(259-01): rewrite mcp-visual-session-contract for v0.9.62 implicit contract + TOOL_REMOVED (TEST-01, TEST-02, TEST-05)`.
  </action>

  <verify>
    <automated>
      cd /Users/lakshmanturlapati/Desktop/FSB && node tests/mcp-visual-session-contract.test.js && grep -q "VISUAL_FIELDS_REQUIRED" tests/mcp-visual-session-contract.test.js && grep -q "BADGE_NOT_ALLOWED" tests/mcp-visual-session-contract.test.js && grep -q "TOOL_REMOVED" tests/mcp-visual-session-contract.test.js && grep -q "visual_reason" tests/mcp-visual-session-contract.test.js && grep -q "is_final" tests/mcp-visual-session-contract.test.js && ! grep -q "start_visual_session.*returns session" tests/mcp-visual-session-contract.test.js && npm test 2>&1 | grep -c "FAIL:" | grep -q "^0$"
    </automated>
  </verify>

  <acceptance_criteria>
    - `tests/mcp-visual-session-contract.test.js` runs as a Node script, exits 0.
    - File contains literal strings: `VISUAL_FIELDS_REQUIRED`, `BADGE_NOT_ALLOWED`, `TOOL_REMOVED`, `visual_reason`, `client`, `is_final`.
    - File does NOT contain assertions that the explicit `start_visual_session` returns a `session_token` (v0.9.0 flow). The TOOL_REMOVED stub is asserted instead.
    - File has at least 5 distinct case functions (allowlist, persistence, implicit-dispatcher-validation, ownership-precedence, tool-removed).
    - `npm test` exits 0 from repo root.
    - Commit subject starts with `test(259-01): rewrite mcp-visual-session-contract`.
  </acceptance_criteria>

  <done>
    `tests/mcp-visual-session-contract.test.js` rewritten with 5 cases covering the v0.9.62 implicit contract end-to-end, including TOOL_REMOVED and ownership-precedence. `npm test` exits 0. Commit lands on `refinements`.
  </done>
</task>

</tasks>

<threat_model>
Phase 259 is test-only. No new runtime surface. The rewritten tests cover the runtime surface added by Phases 255-258 (validator, lifecycle, removal stubs, typed errors). Threats:

1. **Test escapes (false PASS)** -- mitigated by grep-verifiable acceptance criteria + recording-mock counters that explicitly assert "no bridge call".
2. **Pre-v0.9.62 test residue** -- the rewrite EXPLICITLY removes assertions about the explicit start_visual_session lifecycle. Acceptance criteria includes a NEGATIVE grep to confirm `start_visual_session.*returns session` is absent.
3. **CI gate regression** -- mitigated by running `npm test` from repo root before commit; the chain must exit 0.

Block-on severity: Cleared.

</threat_model>

<verification>
After Task 1 completes:

1. `tests/mcp-visual-session-contract.test.js` exists with 5 case functions.
2. `node tests/mcp-visual-session-contract.test.js` exits 0.
3. `npm test` exits 0.
4. Grep predicates pass: VISUAL_FIELDS_REQUIRED, BADGE_NOT_ALLOWED, TOOL_REMOVED, visual_reason, is_final all present.
5. NEGATIVE grep predicate passes: no remnants of explicit v0.9.0 start_visual_session session-token assertions.

</verification>

<success_criteria>
- [ ] `tests/mcp-visual-session-contract.test.js` rewritten with 5 cases for the v0.9.62 implicit contract.
- [ ] TOOL_REMOVED case asserts both removed tool names return the typed error with migration recipe pointer.
- [ ] Implicit-dispatcher-validation case asserts VISUAL_FIELDS_REQUIRED + BADGE_NOT_ALLOWED rejection paths.
- [ ] Ownership-precedence case asserts TAB_NOT_OWNED beats lifecycle.
- [ ] Allowlist + persistence cases still pass (or are updated to the v0.9.62 namespace).
- [ ] `npm test` exits 0.
- [ ] Commit on `refinements`.
</success_criteria>

<output>
After completion, create `.planning/phases/259-test-rewrites-ci-lock/259-01-SUMMARY.md` summarising:
- Cases kept / rewritten / added.
- Assertion counts before / after.
- Test exit code + npm test exit code.
- Commit hash + message.
</output>
