---
phase: 09-data-pipeline-foundation
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - background.js
  - site-guides/career/generic.js
  - site-guides/career/indeed.js
  - site-guides/career/glassdoor.js
  - site-guides/career/builtin.js
autonomous: true

must_haves:
  truths:
    - "Running the parser produces company guide files AND background.js is updated with importScripts for all new guides + 5 ATS base guides"
    - "generic.js is imported LAST among all career guides in background.js so company-specific guides match first via URL patterns"
    - "ATS base guides are imported BEFORE company-specific guides in background.js so they register first"
    - "The 4 existing career guides (generic.js, indeed.js, glassdoor.js, builtin.js) are audited and improved to match the new quality bar"
    - "generic.js is updated to serve as the true fallback for unrecognized ATS platforms with improved selectors"
    - "indeed.js, glassdoor.js, builtin.js have confidence, ats, and careerUrl fields added to match new guide format"
    - "All new company guides and ATS base guides appear in the site-guides registry (registered via registerSiteGuide on importScripts load)"
  artifacts:
    - path: "background.js"
      provides: "Updated importScripts block with all new career guide imports in correct order"
      contains: "importScripts.*site-guides/career/workday.js"
    - path: "site-guides/career/generic.js"
      provides: "Improved generic ATS fallback guide imported LAST"
      contains: "registerSiteGuide"
    - path: "site-guides/career/indeed.js"
      provides: "Audited Indeed guide with confidence and careerUrl fields"
      contains: "confidence"
    - path: "site-guides/career/glassdoor.js"
      provides: "Audited Glassdoor guide with confidence and careerUrl fields"
      contains: "confidence"
    - path: "site-guides/career/builtin.js"
      provides: "Audited BuiltIn guide with confidence and careerUrl fields"
      contains: "confidence"
  key_links:
    - from: "background.js"
      to: "site-guides/career/*.js"
      via: "importScripts() calls loading all career guides"
      pattern: "importScripts.*site-guides/career/"
    - from: "site-guides/career/generic.js"
      to: "site-guides/index.js"
      via: "registerSiteGuide() -- imported last so it only matches when no company-specific guide does"
      pattern: "registerSiteGuide"
---

<objective>
Execute the parser, register all outputs in background.js, audit existing career guides, and ensure correct import ordering.

Purpose: This plan wires everything together. The parser (Plan 01) generates guide files, the ATS base guides (Plan 02) provide platform-level intelligence, and this plan ensures all guides are loaded in the correct order in background.js, the generic fallback is imported last, and existing guides meet the new quality bar.

Output: Updated background.js with all career guide imports, improved existing 4 career guides, fully functional site guide registry.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-data-pipeline-foundation/09-CONTEXT.md
@.planning/phases/09-data-pipeline-foundation/09-RESEARCH.md
@.planning/phases/09-data-pipeline-foundation/09-01-SUMMARY.md
@.planning/phases/09-data-pipeline-foundation/09-02-SUMMARY.md
@site-guides/career/generic.js
@site-guides/career/indeed.js
@site-guides/career/glassdoor.js
@site-guides/career/builtin.js
@background.js (lines 1-90 for import pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run parser and update background.js imports</name>
  <files>background.js</files>
  <action>
1. Run `node scripts/parse-research-logs.js` to generate all company guide files from the research logs.

2. Collect the full list of .js files now in site-guides/career/ (both generated and pre-existing).

3. Update the career imports section in background.js (currently lines 73-77) to include ALL career guides in the correct order:

**Import order (critical for URL pattern matching priority):**
```
// ATS base guides (load first so company guides can reference)
importScripts('site-guides/career/workday.js');
importScripts('site-guides/career/greenhouse.js');
importScripts('site-guides/career/lever.js');
importScripts('site-guides/career/icims.js');
importScripts('site-guides/career/taleo.js');

// Third-party job board guides
importScripts('site-guides/career/indeed.js');
importScripts('site-guides/career/glassdoor.js');
importScripts('site-guides/career/builtin.js');

// Per-company career guides (generated from research logs)
importScripts('site-guides/career/microsoft.js');
importScripts('site-guides/career/amazon.js');
// ... all generated company guides alphabetically ...

// Generic ATS fallback (MUST be last -- matches broad /careers/ and /jobs/ patterns)
importScripts('site-guides/career/generic.js');
```

The key constraint: generic.js MUST be the LAST career import. Its broad patterns (`/\/careers\/?/i`, `/\/jobs\/?/i`) would shadow company-specific guides if loaded first. By loading it last, company-specific guides register first in SITE_GUIDES_REGISTRY and get matched first by getGuideForUrl().

Sort per-company guides alphabetically for maintainability. Keep the existing comment style (simple `// Section name` headers).

Do NOT modify any non-career import lines in background.js. Only replace the `// Per-site guides: Career` block (lines 73-77).
  </action>
  <verify>
1. `node scripts/parse-research-logs.js` runs without errors
2. background.js contains importScripts for all 5 ATS base guides
3. background.js contains importScripts for all generated company guides
4. generic.js is the LAST career import in background.js
5. ATS base guides are imported BEFORE company guides
6. No duplicate importScripts lines
7. All other import sections in background.js are unchanged
  </verify>
  <done>
Parser executed successfully, background.js updated with all career guide imports in correct order (ATS bases first, then third-party boards, then company guides alphabetically, then generic.js last).
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and improve existing 4 career guides</name>
  <files>site-guides/career/generic.js, site-guides/career/indeed.js, site-guides/career/glassdoor.js, site-guides/career/builtin.js</files>
  <action>
Audit and improve the 4 existing career guides to match the new quality bar established by the generated guides. Per user decision: "No matter what, refine to the best."

**For all 4 guides, ADD these new fields** (which the generated guides have):
- `confidence: 'HIGH'` (all 4 are well-known platforms with reliable selectors)
- `ats: null` (these are job boards, not ATS platforms)
- `careerUrl: '{primary URL}'` (indeed.com, glassdoor.com, builtin.com, null for generic)

**generic.js improvements:**
- Update the guidance text to reference the 5 specific ATS base guides (Workday, Greenhouse, Lever, iCIMS, Taleo) instead of inline Workday-specific text
- Remove the Workday-specific section from guidance (that's now in workday.js)
- Clarify that generic.js is the fallback for when no company-specific or ATS-specific guide matches
- Add `confidence: 'MEDIUM'` (it's a fallback, not a targeted guide)
- Keep existing patterns (broad `/careers/`, `/jobs/`, etc.) -- these are correct for fallback matching
- Keep `ats: null` and `careerUrl: null`
- Add `resultsContainer` selector: `[class*="results"], [class*="openings"], [class*="listings"], [id*="results"]`
- Add `pagination` selector: `[class*="pagination"], a[rel="next"], button[class*="next"], [aria-label="Next"]`
- Convert to machine-optimized minimal format: remove the human-readable JSDoc comment at the top of the file, remove verbose prose from guidance (keep selector references and workflow steps)

**indeed.js improvements:**
- Add `confidence: 'HIGH'`, `ats: null`, `careerUrl: 'https://www.indeed.com'`
- Add `resultsContainer` and `pagination` selectors if missing
- Verify all selector strings are current (Indeed updates their DOM periodically -- keep what's there but ensure no obvious gaps)
- Convert to machine-optimized minimal format: remove JSDoc comment

**glassdoor.js improvements:**
- Add `confidence: 'HIGH'`, `ats: null`, `careerUrl: 'https://www.glassdoor.com'`
- Add `resultsContainer` and `pagination` selectors if missing
- Add `datePosted` selector: `[data-test="job-age"], .listing-age`
- Convert to machine-optimized minimal format: remove JSDoc comment

**builtin.js improvements:**
- Add `confidence: 'HIGH'`, `ats: null`, `careerUrl: 'https://www.builtin.com'`
- Add `resultsContainer` and `pagination` selectors if missing
- Add `datePosted` selector if missing
- Convert to machine-optimized minimal format: remove JSDoc comment

Per user decision: machine-optimized, minimal format (no comments or human-readable explanations). Remove the `/** ... */` JSDoc header comments from all 4 files. Keep the guidance field concise.
  </action>
  <verify>
1. All 4 files have `confidence` field in registerSiteGuide() call
2. All 4 files have `ats` field
3. indeed.js, glassdoor.js, builtin.js have `careerUrl` with valid URLs
4. generic.js no longer contains Workday-specific guidance (moved to workday.js)
5. generic.js has `confidence: 'MEDIUM'`
6. No JSDoc header comments remain in any of the 4 files
7. All 4 files have `pagination` and `resultsContainer` selectors
  </verify>
  <done>
All 4 existing career guides (generic.js, indeed.js, glassdoor.js, builtin.js) updated with confidence scores, ats field, careerUrl, additional selectors (pagination, resultsContainer), and converted to machine-optimized minimal format. generic.js cleaned of Workday-specific content and marked as MEDIUM confidence fallback.
  </done>
</task>

</tasks>

<verification>
1. `node scripts/parse-research-logs.js` completes successfully
2. background.js has correct import ordering: ATS bases -> third-party boards -> company guides (alpha) -> generic.js (last)
3. Total career guide count in site-guides/career/ matches: 5 ATS + 4 existing + ~33 generated = ~42 files
4. All 4 existing guides have confidence, ats, careerUrl fields
5. generic.js imports LAST in background.js
6. No regression in non-career imports in background.js
</verification>

<success_criteria>
- Parser runs and generates all company guide files
- background.js updated with all career imports in correct order (generic.js LAST)
- 4 existing career guides audited, improved, and converted to new format
- All guides register correctly via registerSiteGuide() on load
- Total site guide count reflects all new additions
</success_criteria>

<output>
After completion, create `.planning/phases/09-data-pipeline-foundation/09-03-SUMMARY.md`
</output>
