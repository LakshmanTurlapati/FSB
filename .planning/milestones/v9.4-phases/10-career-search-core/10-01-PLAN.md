---
phase: 10-career-search-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site-guides/index.js
  - site-guides/career/_shared.js
autonomous: true

must_haves:
  truths:
    - "getGuideByCompanyName('Microsoft') returns the Microsoft career site guide with careerUrl"
    - "getGuideByCompanyName('jpmorgan') resolves alias to JPMorgan Chase guide (exact .site match)"
    - "getGuideByCompanyName('nonexistent') returns null"
    - "Category guidance instructs AI to dismiss cookie banners BEFORE interacting with career page"
    - "Category guidance includes structured error message templates for no-results and auth-wall"
  artifacts:
    - path: "site-guides/index.js"
      provides: "getGuideByCompanyName function with alias map"
      contains: "getGuideByCompanyName"
    - path: "site-guides/career/_shared.js"
      provides: "Enhanced category guidance with cookie dismissal and error templates"
      contains: "cookie"
  key_links:
    - from: "site-guides/index.js"
      to: "SITE_GUIDES_REGISTRY"
      via: "Array.find on registered career guides"
      pattern: "SITE_GUIDES_REGISTRY.find"
    - from: "ai/ai-integration.js _buildTaskGuidance"
      to: "site-guides/index.js extractCompanyFromTask"
      via: "Function call to extract company from task text (consumed by Plan 10-03)"
      pattern: "extractCompanyFromTask"
---

<objective>
Add company name-to-guide lookup function and enhance shared career category guidance.

Purpose: Enable resolving "Microsoft" or "jpmorgan" to the correct site guide with careerUrl (required by Plans 10-02 and 10-03 for guide injection). Enhance category guidance to prioritize cookie banner dismissal and provide structured error message templates.

Output: `getGuideByCompanyName()` function in index.js, enhanced `_shared.js` with cookie and error guidance.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-career-search-core/10-RESEARCH.md
@site-guides/index.js
@site-guides/career/_shared.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getGuideByCompanyName() to site-guides/index.js</name>
  <files>site-guides/index.js</files>
  <action>
Add a new exported function `getGuideByCompanyName(companyName)` after the existing `getGuideForTask()` function. This function maps user-provided company names (from task text like "find jobs at Microsoft") to the correct career site guide.

Implementation:
1. Define a `COMPANY_ALIASES` constant (object) mapping lowercase aliases to the canonical `.site` value used in career guides. Build the alias list by scanning actual career guide `.site` values from the registered guides. Key aliases to include:
   - 'jpmorgan', 'jp morgan', 'jpm', 'jp morgan chase' -> 'JPMorgan Chase'  (file: jpmorganchase.js, .site = 'JPMorgan Chase')
   - 'bofa', 'boa', 'bank of america' -> 'Bank of America'
   - 'cap one' -> 'Capital One'
   - 'meta', 'facebook' -> 'Meta'
   - 'cvs' -> 'CVS Health'
   - 'jnj', 'j&j', 'johnson & johnson', 'johnson and johnson' -> 'Johnson & Johnson'  (file: jnj.js, .site = 'Johnson & Johnson')
   - 'lm', 'lockheed' -> 'Lockheed Martin'
   - 'uhg', 'unitedhealth' -> 'UnitedHealth Group'
   - 'amex', 'american express' -> 'American Express'  (file: amex.js, .site = 'American Express')
   - 'ti', 'texas instruments' -> 'Texas Instruments'  (file: ti.js, .site = 'Texas Instruments')
   - 'mr cooper', 'mrcooper' -> 'Mr. Cooper'
   - 'gs', 'goldman' -> 'Goldman Sachs'
   - 'ms', 'morgan stanley' -> 'Morgan Stanley'
   - 'goog', 'google' -> 'Google Careers'
   - 'openai' -> 'OpenAI'
   - 'att', 'at&t' -> 'AT&T'
   - 'homedepot', 'home depot' -> 'Home Depot'

   IMPORTANT: Each alias target MUST exactly match the .site value in the corresponding career guide JS file.
   Verify after building: for each alias, confirm SITE_GUIDES_REGISTRY.find(g => g.site === target) returns a valid guide (not undefined).

2. Function logic:
   - Return null if companyName is falsy
   - Normalize: `companyName.toLowerCase().trim()`
   - Check alias map first -> find matching guide by `.site` value in `SITE_GUIDES_REGISTRY` where `.category === 'Career & Job Search'`
   - If no alias match: direct `.site` case-insensitive match
   - If no direct match: partial match (name contained in guide site or vice versa)
   - Return matched guide object or null

3. Also add a helper function `extractCompanyFromTask(taskString)` that extracts company name from task text:
   - Match patterns: "at [Company]", "jobs at [Company]", "[Company] careers", "[Company] jobs", "for [Company]"
   - Return extracted company name string or null
   - Handle multi-word companies: capture until end of string or common stopwords (and, then, into, to, with)
  </action>
  <verify>
Manually verify the function exists in site-guides/index.js by reading the file. Confirm:
- `getGuideByCompanyName` function is defined
- `extractCompanyFromTask` function is defined
- COMPANY_ALIASES object has at least 15 entries
- Function filters by `category === 'Career & Job Search'`
- CRITICAL: For each alias target value in COMPANY_ALIASES, verify it exactly matches a .site value in an actual career guide file:
  - 'JPMorgan Chase' must match jpmorganchase.js .site value (NOT 'JP Morgan Chase')
  - 'American Express' must match amex.js .site value (NOT 'Amex')
  - 'Johnson & Johnson' must match jnj.js .site value (NOT 'J&J')
  - 'Texas Instruments' must match ti.js .site value (NOT 'TI')
  - Read each target's career/*.js file and confirm .site === alias target string
  </verify>
  <done>
getGuideByCompanyName('microsoft') finds the Microsoft guide, getGuideByCompanyName('jpmorgan') resolves to 'JPMorgan Chase' (exact .site match), getGuideByCompanyName('amex') resolves to 'American Express', getGuideByCompanyName('unknownxyz') returns null. extractCompanyFromTask('find software engineer jobs at Microsoft') returns 'Microsoft'. extractCompanyFromTask('find jobs at JP Morgan Chase') returns 'JP Morgan Chase' (multi-word company extraction).
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance _shared.js with cookie banner dismissal and error templates</name>
  <files>site-guides/career/_shared.js</files>
  <action>
Update the `registerCategoryGuidance` call in `site-guides/career/_shared.js` to add two new sections to the `guidance` string:

1. **Cookie Banner Dismissal** (add BEFORE the existing "STRATEGY PRIORITY" section):
```
COOKIE BANNER DISMISSAL (DO THIS FIRST):
- Before interacting with ANY career page element, check for cookie consent or privacy banners
- Look for buttons with text: "Accept", "Accept All", "I Accept", "Got it", "OK", "Agree", "Close"
- Common cookie banner selectors: #onetrust-accept-btn-handler, [id*="cookie"] button, [class*="cookie-accept"]
- Click the accept/dismiss button. If no banner is visible, proceed immediately
- Do NOT spend more than one action on cookie dismissal. If the first click does not dismiss it, proceed with the task
```

2. **Error Reporting Templates** (add AFTER the existing "RELEVANCE FILTERING" section):
```
ERROR REPORTING (use these exact formats):
- If search returns no results: report "NO RESULTS: [Company] career page returned 0 results for '[search term]'. Try broadening search terms or checking the career page directly at [URL]."
- If login/auth wall blocks access: report "AUTH REQUIRED: [Company] requires login to view job listings. Career page: [URL]"
- If career page is unreachable or broken: report "PAGE ERROR: Could not access [Company] career page at [URL]. The site may be temporarily unavailable."
- If no site guide exists for the company: report "NO GUIDE: No career intelligence for [Company]. Attempting Google search for '[Company] careers'..."
- NEVER silently fail. Always report what happened and suggest next steps.
```

Also add to the `warnings` array:
- 'Cookie consent banners can block interaction with page elements -- dismiss BEFORE searching'
- 'Report all failures explicitly -- never complete silently when no jobs were extracted'
  </action>
  <verify>
Read site-guides/career/_shared.js and confirm:
- Guidance text contains "COOKIE BANNER DISMISSAL"
- Guidance text contains "ERROR REPORTING"
- Warnings array has new cookie and failure reporting entries
  </verify>
  <done>
Category guidance now instructs AI to dismiss cookies as first action and provides structured error message templates for all failure modes. Warnings array includes cookie and silent-failure warnings.
  </done>
</task>

</tasks>

<verification>
1. Read site-guides/index.js and confirm getGuideByCompanyName and extractCompanyFromTask functions exist
2. Read site-guides/career/_shared.js and confirm cookie and error reporting sections exist
3. Verify no syntax errors in either file by checking for balanced braces/brackets
</verification>

<success_criteria>
- getGuideByCompanyName resolves company names and aliases to career site guides
- extractCompanyFromTask extracts company names from natural language task strings
- Category guidance prioritizes cookie banner dismissal before career page interaction
- Error reporting templates cover no-results, auth-wall, page-error, and no-guide scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/10-career-search-core/10-01-SUMMARY.md`
</output>
