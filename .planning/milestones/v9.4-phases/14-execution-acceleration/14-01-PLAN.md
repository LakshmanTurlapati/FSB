---
phase: 14-execution-acceleration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - background.js
  - ai/ai-integration.js
autonomous: true
requirements:
  - ACCEL-03
  - ACCEL-04
must_haves:
  truths:
    - "Every AI system prompt includes the user's timezone, country, and local datetime"
    - "Career searches default to the user's detected country when no explicit location is specified"
    - "Locale detection works automatically with no user configuration"
    - "If timezone detection fails, the system gracefully falls back to 'Unknown' without breaking"
  artifacts:
    - path: "background.js"
      provides: "TIMEZONE_TO_COUNTRY static map (~100 entries) and getUserLocale() function"
      contains: "TIMEZONE_TO_COUNTRY"
    - path: "ai/ai-integration.js"
      provides: "Locale context injection into system prompt via buildPrompt()"
      contains: "USER LOCALE"
  key_links:
    - from: "background.js"
      to: "ai/ai-integration.js"
      via: "getUserLocale() called and result passed through context to buildPrompt()"
      pattern: "getUserLocale|userLocale|locale"
    - from: "ai/ai-integration.js"
      to: "system prompt text"
      via: "locale.promptString injected into system prompt"
      pattern: "USER LOCALE"
---

<objective>
Add timezone and country detection to the automation engine and inject locale context into every AI system prompt so the AI makes location-aware decisions.

Purpose: Career searches were finding jobs worldwide because the AI had no location context. This injects the user's timezone, country, and local datetime so the AI can default to local jobs and make time-aware decisions universally.

Output: getUserLocale() function with TIMEZONE_TO_COUNTRY map in background.js, locale context section in AI system prompts via ai-integration.js.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-execution-acceleration/14-CONTEXT.md
@.planning/phases/14-execution-acceleration/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TIMEZONE_TO_COUNTRY map and getUserLocale() to background.js</name>
  <files>background.js</files>
  <action>
Add two new items near the top of background.js (after the existing constants/config section, before any class definitions):

1. **TIMEZONE_TO_COUNTRY static map** (~90-100 entries) covering all major IANA timezones mapped to country names. Organize by region for maintainability:
   - Americas: US (New_York, Chicago, Denver, Los_Angeles, Anchorage, Honolulu, Phoenix, Indiana/Indianapolis, Detroit, Boise, Adak), Canada (Toronto, Vancouver, Edmonton, Winnipeg, Halifax, St_Johns), Mexico (Mexico_City, Tijuana, Cancun), Brazil (Sao_Paulo, Manaus), Colombia, Peru, Chile, Argentina, Venezuela
   - Europe: UK, France, Germany, Spain, Italy, Netherlands, Belgium, Switzerland, Sweden, Norway, Denmark, Finland, Poland, Czech Republic, Austria, Ireland, Portugal, Greece, Romania, Turkey, Russia, Ukraine
   - Asia: Japan, China, Hong Kong, Taiwan, South Korea, India, Singapore, Thailand, Indonesia, Philippines, Vietnam, Malaysia, UAE, Saudi Arabia, Pakistan, Bangladesh, Sri Lanka, Nepal, Uzbekistan, Kazakhstan, Iran, Iraq, Israel
   - Oceania: Australia (Sydney, Melbourne, Brisbane, Perth, Adelaide), New Zealand, Fiji
   - Africa: Egypt, Nigeria, South Africa, Kenya, Morocco, Ghana

   Use the exact entries from the 14-RESEARCH.md code example.

2. **getUserLocale() function** that:
   - Calls `Intl.DateTimeFormat().resolvedOptions().timeZone` for timezone detection
   - Validates timezone contains "/" (IANA format) -- if not, fall back to "Unknown" country with a warning log (macOS Sonoma bug mitigation)
   - Looks up country from TIMEZONE_TO_COUNTRY map, defaulting to "Unknown" if not found
   - Formats local datetime using `new Date().toLocaleString('en-US', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric', hour: 'numeric', minute: '2-digit', timeZoneName: 'short' })`
   - Returns object: `{ timezone, country, localDateTime, promptString }` where promptString is `"User is in ${country}, local time: ${localDateTime} (${timezone})"`
   - Entire function wrapped in try/catch, returning fallback object on any error

3. **Wire getUserLocale into session context**: In the `startAutomationLoop` function, call `getUserLocale()` once at session start and store the result on the session object (e.g., `session.userLocale = getUserLocale()`). This value gets passed to the AI integration through the existing context parameter that flows to `buildPrompt()`.

Find where context is assembled for the AI call in startAutomationLoop (look for the object that gets passed to `aiIntegration.getNextAction()` or similar). Add `userLocale: session.userLocale` to that context object.
  </action>
  <verify>
Search background.js for "TIMEZONE_TO_COUNTRY" -- map should exist with 80+ entries.
Search background.js for "getUserLocale" -- function should exist and return { timezone, country, localDateTime, promptString }.
Search background.js for "userLocale" in startAutomationLoop -- should be set on session and passed to AI context.
  </verify>
  <done>
TIMEZONE_TO_COUNTRY map has 80+ entries covering all major timezones. getUserLocale() function returns locale object with timezone, country, localDateTime, and promptString. Session context passes userLocale to AI integration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Inject locale context into AI system prompt and add career location default</name>
  <files>ai/ai-integration.js</files>
  <action>
In ai-integration.js, modify the `buildPrompt()` method (or equivalent system prompt construction method) to inject locale context:

1. **Locale injection in system prompt**: After the existing task type and tool documentation sections, add a new section:

```
=== USER LOCALE ===
${context.userLocale?.promptString || 'User timezone could not be detected.'}
Use this information for location-aware decisions (e.g., filtering job searches by country, using local date formats, time-sensitive actions).
For career/job searches: If the user does not specify a location, default to jobs in ${context.userLocale?.country || 'the user\'s country'}.
```

This section should be injected into ALL task types (not just career searches) per CONTEXT.md locked decision. Place it after the existing context sections and before the response format section.

2. **Career prompt location enhancement**: Find the career-specific prompt section (the career task type prompt built by `_buildCareerPrompt` or similar). Add a line that says:
   - If the user mentions a specific country/state/city in their prompt, use that location for filtering
   - If no location is mentioned, default to filtering by the user's detected country: `${context.userLocale?.country || ''}`
   - This is in ADDITION to the universal locale section -- the career prompt gets both the general locale context AND the explicit career-specific instruction

3. **Handle missing locale gracefully**: Use optional chaining (`context.userLocale?.promptString`) so the system prompt does not break if userLocale is undefined (backward compatibility with any code path that does not pass locale).
  </action>
  <verify>
Search ai-integration.js for "USER LOCALE" -- section should exist in system prompt construction.
Search ai-integration.js for "userLocale" -- should be referenced in buildPrompt with optional chaining.
Search ai-integration.js for career prompt section -- should mention defaulting to user's country.
  </verify>
  <done>
Every AI system prompt includes a USER LOCALE section with timezone, country, and local datetime. Career prompts additionally instruct the AI to default to the user's country when no location is specified. Missing locale data is handled gracefully with optional chaining.
  </done>
</task>

</tasks>

<verification>
1. getUserLocale() exists in background.js and returns a valid locale object
2. TIMEZONE_TO_COUNTRY map in background.js contains 80+ entries covering major world timezones
3. Session context in startAutomationLoop includes userLocale
4. ai-integration.js system prompt includes "USER LOCALE" section for all task types
5. Career-specific prompt includes instruction to default to user's country
6. Missing locale handled gracefully (no crashes if timezone detection fails)
</verification>

<success_criteria>
- User's timezone, country, and local datetime appear in every AI system prompt
- Career search prompts explicitly instruct AI to filter by user's country when no location specified
- No new external dependencies -- only built-in browser APIs (Intl.DateTimeFormat)
- Backward compatible -- existing code paths without userLocale continue to work
</success_criteria>

<output>
After completion, create `.planning/phases/14-execution-acceleration/14-01-SUMMARY.md`
</output>
