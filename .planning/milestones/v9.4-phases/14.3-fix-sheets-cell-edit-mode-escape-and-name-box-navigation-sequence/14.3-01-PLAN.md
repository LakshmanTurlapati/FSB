---
phase: 14.3-fix-sheets-cell-edit-mode-escape-and-name-box-navigation-sequence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site-guides/productivity/google-sheets.js
  - ai/ai-integration.js
autonomous: true
requirements:
  - FIX-SHEETS-ESCAPE

must_haves:
  truths:
    - "Every Name Box navigation instruction across site guide, TASK_PROMPTS, and continuation reminder is preceded by a Press Escape step"
    - "The AI receives a 6-step procedure (Step 0-5) for Sheets cell navigation in TASK_PROMPTS multitab section, with Step 0 being Escape"
    - "Continuation iterations on Google Sheets URLs include an Escape-first navigation reminder"
    - "The site guide warnings array contains a CRITICAL warning about pressing Escape before clicking the Name Box"
    - "Workflows that already have Escape (renameSheet, formatHeaderRow) are NOT modified"
    - "The fillSheetData clipboard path and buildSheetsFormattingDirective are NOT modified"
  artifacts:
    - path: "site-guides/productivity/google-sheets.js"
      provides: "Escape-first Name Box navigation across guidance text, 9 workflow arrays, and warnings"
      contains: "Press Escape to exit"
    - path: "ai/ai-integration.js"
      provides: "6-step Sheets procedure in TASK_PROMPTS and Escape-first continuation reminder in buildMinimalUpdate"
      contains: "Step 0"
  key_links:
    - from: "site-guides/productivity/google-sheets.js guidance text"
      to: "AI first-iteration system prompt"
      via: "site guide injection in _buildTaskGuidance"
      pattern: "Press Escape.*Name Box"
    - from: "ai/ai-integration.js TASK_PROMPTS.multitab"
      to: "AI system prompt for multitab tasks"
      via: "task type prompt selection"
      pattern: "Step 0.*Escape"
    - from: "ai/ai-integration.js buildMinimalUpdate"
      to: "AI continuation prompt on iteration 2+"
      via: "URL-conditional Sheets reminder injection"
      pattern: "press Escape.*cell edit mode"
---

<objective>
Add explicit Escape keystroke before every Name Box navigation instruction in the Google Sheets site guide, TASK_PROMPTS multitab section, and buildMinimalUpdate continuation reminder.

Purpose: Eliminate the "random test datab1" corruption pattern where the AI types a cell reference into the Name Box while still in cell edit mode, causing the reference to be appended to the current cell's content instead of triggering navigation. Session log evidence: iterations 4-5 of fsb-session-2026-02-26-1772124534725.json.

Output: Updated google-sheets.js site guide and ai-integration.js with Escape-first Name Box navigation across all 3 prompt locations.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.3-fix-sheets-cell-edit-mode-escape-and-name-box-navigation-sequence/14.3-CONTEXT.md
@.planning/phases/14.3-fix-sheets-cell-edit-mode-escape-and-name-box-navigation-sequence/14.3-RESEARCH.md
@site-guides/productivity/google-sheets.js
@ai/ai-integration.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Escape step to Google Sheets site guide guidance text, workflow arrays, and warnings</name>
  <files>site-guides/productivity/google-sheets.js</files>
  <action>
  Update the Google Sheets site guide in 3 areas:

  **A. Guidance text -- NAME BOX NAVIGATION section (lines 30-36):**
  Insert Step 0 before the existing Step 1. The updated section should read:
  ```
  NAME BOX NAVIGATION (PRIMARY METHOD):
  0. Press Escape to exit cell edit mode (CRITICAL -- if in edit mode, the reference will go into the active cell instead of the Name Box)
  1. Click the Name Box element (#t-name-box) showing current cell reference like "A1"
  2. The Name Box text will be selected/highlighted
  3. Type the target cell reference (e.g., "A1", "B3", "D10")
  4. Press Enter to navigate to that cell
  5. The cell is now selected and ready for input
  6. Type the cell value -- it will go into the formula bar and cell
  ```

  **B. Workflow arrays -- prepend Escape step to 9 workflows:**
  For each of these workflows, add `'Press Escape to exit any cell edit mode'` as the NEW FIRST element in the array:
  1. `setupHeaderRow` (currently starts with "Click the Name Box (#t-name-box)")
  2. `enterRowData` (currently starts with "Click the Name Box (#t-name-box)")
  3. `navigateToCell` (currently starts with "Click the Name Box (#t-name-box) to select it")
  4. `dataEntrySequential` (currently starts with "Click the Name Box (#t-name-box) to select it")
  5. `formulaBarVerification` (currently starts with "Click the Name Box (#t-name-box)")
  6. `appendToExistingSheet` (currently starts with "Click the Name Box, type \"A1\"...")
  7. `applyAlternatingColors` (currently starts with "Click the Name Box, type the full data range...")
  8. `applyLinkColumnBlueText` (currently starts with "Click the Name Box, type the Apply Link column...")

  For `enterHyperlinkFormula`, change step 1 from:
  `'Navigate to the target cell via Name Box'`
  to:
  `'Press Escape to exit any cell edit mode, then navigate to the target cell via Name Box (click Name Box, type cell ref, press Enter)'`

  DO NOT modify these workflows (they already start with "Press Escape"):
  - `renameSheet` (line 152)
  - `formatHeaderRow` (line 169)
  Also DO NOT modify: `createNewSheet`, `navigateToExistingSheet`, `freezeHeaderRow`, `autoSizeColumns` (these do not use Name Box navigation).

  **C. Warnings array -- add new CRITICAL warning:**
  Add this string to the warnings array (position it near the top, after the first warning about canvas):
  `'CRITICAL: Press Escape before clicking the Name Box to exit cell edit mode. If you type a cell reference while still in edit mode, the reference will be appended to the current cell\'s content instead of navigating.'`
  </action>
  <verify>
  1. Read site-guides/productivity/google-sheets.js and verify:
     - The NAME BOX NAVIGATION section has steps 0-6 with Step 0 being "Press Escape to exit cell edit mode"
     - All 8 prepend-target workflows have 'Press Escape to exit any cell edit mode' as their first array element
     - enterHyperlinkFormula step 1 includes Escape instruction
     - renameSheet and formatHeaderRow are unchanged (still have their existing Escape steps)
     - The warnings array contains the new CRITICAL warning about Escape before Name Box
  2. grep for "Press Escape" in the file and confirm it appears in all expected locations (guidance text + 9 workflows + 2 existing workflows + warnings)
  </verify>
  <done>
  The Google Sheets site guide has Escape-before-NameBox instructions in the guidance text (Step 0), all 9 applicable workflow arrays, and a new CRITICAL warning in the warnings array. The 2 workflows that already had Escape (renameSheet, formatHeaderRow) and 4 workflows that do not use Name Box (createNewSheet, navigateToExistingSheet, freezeHeaderRow, autoSizeColumns) are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Escape step to TASK_PROMPTS multitab section and buildMinimalUpdate continuation reminder</name>
  <files>ai/ai-integration.js</files>
  <action>
  Update ai-integration.js in 2 locations:

  **A. TASK_PROMPTS multitab -- "WRITING TO GOOGLE SHEETS" section (approx lines 235-246):**
  The current 5-step procedure (Steps 1-5) becomes a 6-step procedure (Steps 0-5). Insert Step 0 and renumber:
  ```
  WRITING TO GOOGLE SHEETS:
  - Google Sheets uses a CANVAS-BASED GRID. You CANNOT click individual cells.
  - CELL NAVIGATION VS DATA ENTRY -- these are SEPARATE steps:
    Step 0: Press Escape to exit cell edit mode (CRITICAL before every Name Box navigation)
    Step 1: Click the Name Box (top-left input showing current cell ref like "A1")
    Step 2: Type the target cell reference (e.g., "A1", "B3") into the Name Box
    Step 3: Press Enter to navigate to that cell
    Step 4: Type the actual data value (keystrokes go to the now-active cell)
    Step 5: Press Tab to move right to next column, or Enter to move down to next row
  ```
  Verify the rest of the section (WARNING line, sequential row entry line) remains unchanged.

  **B. buildMinimalUpdate -- GOOGLE SHEETS REMINDER (approx lines 794-799):**
  Update the existing 3-line reminder to 4 lines, adding Escape as the first bullet and embedding it in the navigation instruction. Change from:
  ```javascript
  update += `\n\nGOOGLE SHEETS REMINDER:`;
  update += `\n- To navigate to a cell: click Name Box (#t-name-box), type cell reference (e.g., "B1"), press Enter.`;
  update += `\n- To enter data: type the value (keystrokes go to the active cell). Do NOT type cell references as data values.`;
  update += `\n- Tab = move right, Enter = move down. Use Tab between columns in the same row.`;
  ```
  To:
  ```javascript
  update += `\n\nGOOGLE SHEETS REMINDER:`;
  update += `\n- BEFORE navigating to a cell: press Escape to exit cell edit mode.`;
  update += `\n- To navigate: press Escape, click Name Box (#t-name-box), type cell reference (e.g., "B1"), press Enter.`;
  update += `\n- To enter data: type the value (keystrokes go to the active cell). Do NOT type cell references as data values.`;
  update += `\n- Tab = move right, Enter = move down. Use Tab between columns in the same row.`;
  ```

  DO NOT modify:
  - MINIMAL_CONTINUATION_PROMPT constant
  - buildSheetsFormattingDirective function (already has STEP 0 Escape at line ~470)
  - The fillSheetData data entry directive (uses clipboard tool, not Name Box)
  - Other TASK_PROMPTS entries (career, gaming, shopping, general, etc.)
  </action>
  <verify>
  1. Read ai/ai-integration.js and verify:
     - TASK_PROMPTS multitab section has Steps 0-5 with Step 0 being "Press Escape to exit cell edit mode"
     - No duplicate Step numbers exist (grep for "Step 1" in the WRITING TO GOOGLE SHEETS block -- should appear exactly once)
     - buildMinimalUpdate GOOGLE SHEETS REMINDER has 4 bullet lines with Escape as the first bullet
     - buildSheetsFormattingDirective is unchanged (still has its own STEP 0)
  2. grep for "BEFORE navigating" in ai-integration.js to confirm the new first bullet exists
  3. grep for "Step 0" in ai-integration.js to confirm it appears in the TASK_PROMPTS section (and in buildSheetsFormattingDirective as the existing STEP 0)
  </verify>
  <done>
  The TASK_PROMPTS multitab section has a 6-step Sheets procedure with Escape as Step 0, and the buildMinimalUpdate continuation reminder has Escape as the first bullet in the GOOGLE SHEETS REMINDER block. No other sections of ai-integration.js are modified.
  </done>
</task>

</tasks>

<verification>
1. **Site guide completeness**: grep for "Name Box" in google-sheets.js -- every occurrence that represents a navigation instruction should be preceded (in the same array or within the guidance text) by an Escape instruction
2. **Step numbering**: grep for "Step [0-9]" in ai-integration.js TASK_PROMPTS multitab section -- should show exactly Steps 0, 1, 2, 3, 4, 5 with no gaps or duplicates
3. **No unintended changes**: buildSheetsFormattingDirective still has its own "STEP 0 -- EXIT EDIT MODE" (unchanged), fillSheetData directive is unchanged, MINIMAL_CONTINUATION_PROMPT is unchanged
4. **Workflow safety**: renameSheet and formatHeaderRow still have exactly their original first step (Press Escape), not a doubled Escape
5. **Warning presence**: grep for "CRITICAL: Press Escape before clicking" in google-sheets.js -- should appear exactly once in the warnings array
</verification>

<success_criteria>
- The AI receives Escape-before-NameBox instructions in all 3 prompt injection points: site guide (first iteration), TASK_PROMPTS (system prompt), and buildMinimalUpdate (continuation iterations)
- The 6-step procedure (0-5) is consistent between the site guide NAME BOX NAVIGATION section and the TASK_PROMPTS multitab section
- No existing Escape-already-present workflows are double-Escaped
- No non-Name-Box workflows are incorrectly modified
- The fillSheetData clipboard path is completely unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-fix-sheets-cell-edit-mode-escape-and-name-box-navigation-sequence/14.3-01-SUMMARY.md`
</output>
