---
phase: 244
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp/src/tools/agents.ts
  - mcp/src/tools/autopilot.ts
  - mcp/src/tools/visual-session.ts
  - extension/ai/tool-definitions.js
autonomous: true
requirements:
  - MCP-07
must_haves:
  truths:
    - "The `back` tool description in mcp/src/tools/agents.ts (the server.tool block at lines ~22-24) is extended with the multi-agent contract paragraph: agent_id is FSB-issued and required; tab_id is agent-scoped; cap is configurable default 8 range 1-64; cross-agent calls reject with TAB_NOT_OWNED, incognito with TAB_INCOGNITO_NOT_SUPPORTED, cross-window with TAB_OUT_OF_SCOPE; AGENT_CAP_REACHED fires when the (N+1)th agent claims; tool-specific codes (NO_BACK_HISTORY, BF_CACHE, FRAGMENT_ONLY) remain documented."
    - "The `run_task` tool description in mcp/src/tools/autopilot.ts (server.tool at line ~21-23) is extended with the same multi-agent contract paragraph; existing IMPORTANT clause about manual-tools-first preserved verbatim."
    - "The `stop_task` and `get_task_status` descriptions in autopilot.ts (~201-203 and ~229-231) gain a one-sentence agent-scope clause: 'Agent-scoped: only affects the calling agent's task' (so cross-agent stop/status leakage is documented as forbidden)."
    - "The `start_visual_session` and `end_visual_session` descriptions in mcp/src/tools/visual-session.ts (server.tool at ~34 and ~88) include the multi-agent contract paragraph; existing client-allowlist + glow-overlay copy preserved."
    - "Every manual tool description in extension/ai/tool-definitions.js is extended with a single trailing sentence: 'Multi-agent: agent-scoped tabs; cross-agent reject with TAB_NOT_OWNED; cap configurable (default 8, 1-64).' This single-line addition keeps each entry terse (per CONTEXT D-02 'match existing description style') while satisfying MCP-07's enumeration requirement."
    - "Across all four files, the four typed error codes AGENT_CAP_REACHED, TAB_NOT_OWNED, TAB_INCOGNITO_NOT_SUPPORTED, TAB_OUT_OF_SCOPE each appear in at least one tool's description (so MCP-07's 'enumerated' clause is observable via grep)."
    - "mcp/src/tools/manual.ts is NOT modified -- it pulls description text from TOOL_REGISTRY via tool.description; the source of truth is extension/ai/tool-definitions.js (the schema-bridge.ts loads it via createRequire)."
    - "The mcp/ build still compiles cleanly: `npm --prefix mcp run build` exits 0 after edits (no TypeScript regressions; description strings are plain string literals)."
  artifacts:
    - path: mcp/src/tools/agents.ts
      provides: "Extended `back` tool description with multi-agent contract block + preserved status-code enumeration"
      contains: "AGENT_CAP_REACHED, TAB_NOT_OWNED"
    - path: mcp/src/tools/autopilot.ts
      provides: "Extended run_task / stop_task / get_task_status descriptions with multi-agent contract"
      contains: "TAB_NOT_OWNED, AGENT_CAP_REACHED"
    - path: mcp/src/tools/visual-session.ts
      provides: "Extended start/end visual session descriptions with multi-agent contract"
      contains: "TAB_NOT_OWNED, AGENT_CAP_REACHED"
    - path: extension/ai/tool-definitions.js
      provides: "Trailing multi-agent sentence appended to every manual-tool description (the canonical source loaded by mcp's schema-bridge.ts)"
      contains: "TAB_NOT_OWNED"
  key_links:
    - from: mcp/src/tools/manual.ts (NOT modified)
      to: extension/ai/tool-definitions.js
      via: "schema-bridge.ts createRequire() loads tool-definitions.cjs (built copy of tool-definitions.js); manual.ts reads tool.description verbatim"
      pattern: "tool\\.description"
    - from: mcp/src/tools/agents.ts back tool
      to: Phase 240 dispatch gate
      via: "Description documents the gate's typed reject codes (TAB_NOT_OWNED) so MCP clients have a contract"
      pattern: "TAB_NOT_OWNED"
    - from: extension/ai/tool-definitions.js
      to: mcp build pipeline
      via: "mcp/package.json scripts.build copies tool-definitions.js -> ai/tool-definitions.cjs; description edits flow into the published package automatically"
      pattern: "cp \\.\\./extension/ai/tool-definitions\\.js"
---

<objective>
Update MCP tool descriptions across four source files so every tool documents the v0.9.60 multi-agent contract per CONTEXT D-02. This closes MCP-07 and is one of the two preconditions for Plan 03's release engineering (Plan 01 = test green; Plan 02 = descriptions accurate).

Scope is description text only -- no behavior changes, no parameter changes, no new tools. The four files are touched in isolation (no overlap with Plan 01) so this plan runs in Wave 1 in parallel with Plan 01.

Per CONTEXT D-02 "match existing description style (terse, one to two sentences)": the manual-tool entries in extension/ai/tool-definitions.js get a single trailing sentence appended (~70 entries, ~70 line edits). The four high-touch descriptions (back, run_task, start_visual_session, end_visual_session) get a fuller multi-line contract paragraph because they are the agent-aware tools where the contract matters most.

Purpose: MCP clients receive accurate documentation of agent_id requirements, ownership enforcement, configurable cap, and typed error codes -- so a client driving FSB does not have to read the source to learn the contract.
Output: 3 .ts files + 1 .js source-of-truth file edited; no test files; mcp build still clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/244-hardening-regression-mcp-0-8-0-release/244-CONTEXT.md
@.planning/phases/244-hardening-regression-mcp-0-8-0-release/244-VALIDATION.md

@mcp/src/tools/agents.ts
@mcp/src/tools/autopilot.ts
@mcp/src/tools/visual-session.ts
@mcp/src/tools/manual.ts
@extension/ai/tool-definitions.js

<interfaces>
<!-- Existing tool registration shape (per file). Executor edits the `description`
     string literal in each server.tool() call; nothing else. -->

mcp/src/tools/agents.ts:22-24 -- `back` tool already has a long description ending at ".chrome.tabs.update({active: true})." Append the multi-agent contract block before the closing quote.

mcp/src/tools/autopilot.ts:21-23 -- `run_task` description ends at "...action log."
mcp/src/tools/autopilot.ts:201-203 -- `stop_task` description ends at "...the task being cancelled)."
mcp/src/tools/autopilot.ts:229-231 -- `get_task_status` description ends at "...stop_task (cancel if needed)."

mcp/src/tools/visual-session.ts:34-36 -- `start_visual_session` description ends at "...Pair with end_visual_session."
mcp/src/tools/visual-session.ts:88-90 -- `end_visual_session` description ends at "...Pair with start_visual_session."

extension/ai/tool-definitions.js -- ~70 entries shaped:
  { name: 'tool_name', description: 'existing copy.', inputSchema: { ... } }
The description property is a string literal at TOP-level of each entry (line ~66, ~83, ~99, etc.). Editor appends ' Multi-agent: agent-scoped tabs; cross-agent reject with TAB_NOT_OWNED; cap configurable (default 8, 1-64).' to each.

Build pipeline (mcp/package.json scripts.build):
  npm run clean && tsc && cp ../extension/ai/tool-definitions.js ai/tool-definitions.cjs
So edits to extension/ai/tool-definitions.js propagate to mcp/ai/tool-definitions.cjs at build time.
</interfaces>

<contract_block>
<!-- Canonical multi-agent contract paragraph per CONTEXT D-02. Used verbatim
     for the four high-touch descriptions (back, run_task, start_visual_session,
     end_visual_session). -->

" Multi-agent contract: agent_id is FSB-issued and required (the server captures it via agent:register on first tool call -- callers do not provide it). tab_id is agent-scoped: only tabs owned by the calling agent can be addressed. The concurrency cap is configurable (default 8, range 1-64); the (N+1)th agent claim is rejected with AGENT_CAP_REACHED { cap, active }. Ownership enforcement: cross-agent calls reject with TAB_NOT_OWNED; incognito tabs reject with TAB_INCOGNITO_NOT_SUPPORTED; cross-window tabs reject with TAB_OUT_OF_SCOPE."
</contract_block>

<manual_tail_sentence>
<!-- Single-line append for every manual-tool entry in tool-definitions.js -->
" Multi-agent: agent-scoped tabs; cross-agent reject with TAB_NOT_OWNED; cap configurable (default 8, 1-64)."
</manual_tail_sentence>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append contract block to agents.ts (back) + autopilot.ts (run_task, stop_task, get_task_status) + visual-session.ts (start, end)</name>
  <files>mcp/src/tools/agents.ts, mcp/src/tools/autopilot.ts, mcp/src/tools/visual-session.ts</files>
  <action>
    Read each file's existing server.tool() description string and append the canonical multi-agent contract block (verbatim from <contract_block> above) before the closing quote. Six edits total:

    1. mcp/src/tools/agents.ts -- the `back` tool description (currently ends at "...chrome.tabs.update({active: true})."). Append the full contract block. Existing tool-specific status code enumeration (no_history, cross_origin, bf_cache, fragment_only) MUST be preserved verbatim before the new block.

    2. mcp/src/tools/autopilot.ts `run_task` -- append full contract block after "...action log." Existing IMPORTANT-clause about manual-tools-first MUST be preserved verbatim at the start.

    3. mcp/src/tools/autopilot.ts `stop_task` -- append a SHORT clause (not the full block; this is a control tool, not a tab-bound action): " Agent-scoped: only the calling agent's task is cancelled; cross-agent calls reject with TAB_NOT_OWNED."

    4. mcp/src/tools/autopilot.ts `get_task_status` -- append: " Agent-scoped: returns the calling agent's task status only."

    5. mcp/src/tools/visual-session.ts `start_visual_session` -- append full contract block after "Pair with end_visual_session." Note: this tool already references same-agent re-entry behavior implicitly via the Phase 240 OWN-04 path, so the contract block adds the cross-agent reject clarity.

    6. mcp/src/tools/visual-session.ts `end_visual_session` -- append full contract block after "Pair with start_visual_session."

    Each edit is a string-literal extension only -- do NOT touch the zod input schema, the handler body, or the registration call. Preserve all existing escapes (e.g., \' inside the strings).

    NO emojis. NO tone changes to existing copy. Append cleanly with a leading space inside the quoted string.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Desktop/FSB && npm --prefix mcp run build > /tmp/244-02-task1-build.log 2>&1 && grep -E "TAB_NOT_OWNED|AGENT_CAP_REACHED" mcp/src/tools/agents.ts mcp/src/tools/autopilot.ts mcp/src/tools/visual-session.ts | wc -l | awk '$1 >= 6 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>
    `npm --prefix mcp run build` exits 0; the four error-code tokens (AGENT_CAP_REACHED, TAB_NOT_OWNED, TAB_INCOGNITO_NOT_SUPPORTED, TAB_OUT_OF_SCOPE) collectively appear at least 6 times across the three files; existing tool-specific status codes (NO_BACK_HISTORY, BF_CACHE, FRAGMENT_ONLY) are still present in the back description.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append manual-tail sentence to every entry in tool-definitions.js</name>
  <files>extension/ai/tool-definitions.js</files>
  <action>
    Read extension/ai/tool-definitions.js. For each tool entry (each object in TOOL_REGISTRY array) that has a `description: '...'` property, append the canonical manual-tail sentence (verbatim from <manual_tail_sentence>) inside the quotes, immediately before the closing quote, with a single leading space.

    Heuristic: the file follows the shape:
    ```
    {
      name: 'navigate',
      description: 'Open a URL in the active browser tab. ... navigate).',
      inputSchema: { ... },
      _readOnly: ...,
      _contentVerb: ...,
    },
    ```
    Edit ONLY the `description:` line per entry. Skip any nested description fields under inputSchema.properties (those are parameter descriptions, not tool descriptions -- DO NOT modify them).

    There are approximately 60-70 tool entries (run `grep -c "^\s*name:" extension/ai/tool-definitions.js` first to get the actual count for the SUMMARY).

    For tools whose description ALREADY contains "Multi-agent" (e.g., a Phase 243 partial edit, if any), skip them -- do NOT double-append.

    NO emojis. NO behavior changes. NO escape character changes (preserve \' literally where present).

    After edits, run `npm --prefix mcp run build` -- the build's `cp ../extension/ai/tool-definitions.js ai/tool-definitions.cjs` step will propagate the changes to mcp/ai/tool-definitions.cjs automatically.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Desktop/FSB && npm --prefix mcp run build > /tmp/244-02-task2-build.log 2>&1 && grep -c "Multi-agent: agent-scoped" extension/ai/tool-definitions.js | awk '$1 >= 30 { exit 0 } { exit 1 }' && grep -c "Multi-agent: agent-scoped" mcp/ai/tool-definitions.cjs | awk '$1 >= 30 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>
    Every tool entry in tool-definitions.js carries the multi-agent tail sentence (>= 30 occurrences as a sanity floor; actual count should match number of tool entries). Build completes clean. The propagated copy in mcp/ai/tool-definitions.cjs matches.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify cross-file error-code coverage and existing-test parity</name>
  <files>(verification only -- no source edits)</files>
  <action>
    Run the cross-file MCP-07 enumeration check from <deep_work_rules>:
    ```
    grep -c "AGENT_CAP_REACHED\|TAB_NOT_OWNED\|TAB_INCOGNITO_NOT_SUPPORTED\|TAB_OUT_OF_SCOPE" \
      mcp/src/tools/agents.ts \
      mcp/src/tools/autopilot.ts \
      mcp/src/tools/manual.ts \
      mcp/src/tools/visual-session.ts
    ```
    The total count across the four files MUST be >= 4 (each error code appears in at least one tool's description; manual.ts contributes via its dynamic load of tool-definitions.cjs but that does not show up in this grep -- count from agents/autopilot/visual-session is sufficient).

    Then run the existing v0.9.36/v0.9.40/MCP installer/dashboard contract test suite as a sample (per VALIDATION.md "every test from Phases 237-243 must pass UNCHANGED") to confirm description edits did not break compile-time invariants:
    - node tests/agent-cap.test.js
    - node tests/agent-registry.test.js
    - node tests/agent-pooling.test.js
    - node tests/back-tool.test.js
    - node tests/back-tool-ownership.test.js

    If any fails, the description edits introduced a regression (e.g., escaped quote breakage). Fix the affected file's quoting and re-verify.

    NO emojis in any logging.
  </action>
  <verify>
    <automated>node tests/agent-cap.test.js && node tests/agent-registry.test.js && node tests/agent-pooling.test.js && node tests/back-tool.test.js && node tests/back-tool-ownership.test.js && grep -c "AGENT_CAP_REACHED\|TAB_NOT_OWNED\|TAB_INCOGNITO_NOT_SUPPORTED\|TAB_OUT_OF_SCOPE" mcp/src/tools/agents.ts mcp/src/tools/autopilot.ts mcp/src/tools/manual.ts mcp/src/tools/visual-session.ts | awk -F: '{s+=$2} END { exit (s>=4 ? 0 : 1) }'</automated>
  </verify>
  <done>
    All five sampled tests pass; the cross-file error-code grep aggregates to >= 4 occurrences; mcp build clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client <-> tool description | Clients consume tool descriptions to plan calls; description drift = clients send invalid calls = silent or noisy failures |
| extension/ai/tool-definitions.js <-> mcp/ai/tool-definitions.cjs | Build-time copy via `cp` -- if the copy step is skipped, descriptions in the published package are stale |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-244-01 | T (Tampering) | Description string literals | mitigate | Append-only edits inside the closing quote; no template-literal conversions; preserve all backslash escapes |
| T-244-02 | I (Information disclosure) | Tool descriptions | accept | Descriptions reveal the contract -- this is the goal of MCP-07; no PII leakage risk |
| T-244-03 | D (Denial of service) | mcp build pipeline | mitigate | Task 1 + 2 verify with `npm --prefix mcp run build` after every edit; broken quoting fails fast at tsc step |
| T-244-04 | R (Repudiation) | Drift between description and runtime behavior | mitigate | Task 3 grep-verifies that every documented error code (AGENT_CAP_REACHED, TAB_NOT_OWNED, TAB_INCOGNITO_NOT_SUPPORTED, TAB_OUT_OF_SCOPE) is mentioned in at least one description AND the Plan 01 regression suite asserts each code is actually emitted at runtime -- closing the doc/runtime gap |
</threat_model>

<verification>
- `npm --prefix mcp run build` exits 0.
- `grep -c "AGENT_CAP_REACHED\|TAB_NOT_OWNED\|TAB_INCOGNITO_NOT_SUPPORTED\|TAB_OUT_OF_SCOPE" mcp/src/tools/agents.ts mcp/src/tools/autopilot.ts mcp/src/tools/manual.ts mcp/src/tools/visual-session.ts` aggregates to >= 4.
- `grep -c "Multi-agent: agent-scoped" extension/ai/tool-definitions.js` >= 30.
- `grep -c "Multi-agent: agent-scoped" mcp/ai/tool-definitions.cjs` matches the count above (proves the build's cp step propagated edits).
- 5 sampled Phase 237-243 tests still green.
</verification>

<success_criteria>
- MCP-07 closed: every named tool (run_task, every manual tool, back, start/end visual session) documents the contract.
- All four typed error codes enumerated across the .ts files.
- The single source of truth (tool-definitions.js) carries the manual-tool tail sentence and propagates to the .cjs copy.
- mcp build clean; existing test sample green; no behavior changes.
</success_criteria>

<output>
After completion, create `.planning/phases/244-hardening-regression-mcp-0-8-0-release/244-02-SUMMARY.md` with:
- Exact count of tool entries edited in tool-definitions.js (so Plan 03 CHANGELOG can quote it).
- Final grep counts for the four error codes (proof of MCP-07 enumeration).
- Build duration and any TypeScript warnings introduced (should be zero).
</output>
