---
phase: 246-agent-scoped-tab-resolution-background-default
verified: 2026-05-06T23:06:52Z
status: human_needed
score: 15/16 must-haves verified (1 deferred to human verification)
overrides_applied: 0
human_verification:
  - test: "Manual smoke test (must-have 16): open extension popup -> click any tool button"
    expected: "Legacy popup UX is byte-for-byte unchanged from v0.9.50; no extra resolver-driven behavior; no focus stealing; tools dispatch against the user's currently active tab"
    why_human: "Requires loading the unpacked extension in Chrome, opening a real popup, clicking tool buttons, and observing the behavior visually. Cannot be exercised programmatically because the popup runs in a real Chrome runtime context (not the node:test harness). Plan 02 SUMMARY explicitly flagged this as human-verifiable."
---

# Phase 246: Agent-Scoped Tab Resolution + Background-Default open_tab Verification Report

**Phase Goal:** Replace `chrome.tabs.query({active:true})` with agent-registry-driven tab resolution across read tools, visual-session, and action tools, default `open_tab` to background, and ensure MCP-routed surfaces never steal focus from the user or other agents. Closes the multi-agent concurrency gap surfaced in the v0.9.60 smoke test.

**Verified:** 2026-05-06T23:06:52Z
**Status:** human_needed (15/16 must-haves verified at the code level; 1 manual smoke test required)
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths (the 16 enumerated must-haves)

| #   | Must-Have                                                                                                                                                                                | Status         | Evidence                                                                                                                                                                                                                                       |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | extension/utils/agent-tab-resolver.js exists and exports resolveAgentTabOrError(agentId, params, client)                                                                                  | VERIFIED       | File present (4198 bytes); `function resolveAgentTabOrError` defined at line 41; `exports.resolveAgentTabOrError = resolveAgentTabOrError` at line 82; `globalThis.resolveAgentTabOrError = resolveAgentTabOrError` at line 87.               |
| 2   | All 5 _getActiveTab() callers in extension/ws/mcp-bridge-client.js migrated to the resolver                                                                                              | VERIFIED       | grep `resolveAgentTabOrError` returns 5 hits in mcp-bridge-client.js at lines 526, 543, 579, 1234, 1301 -- inside `_handleGetDOM`, `_handleReadPage`, `_handleExecuteAction`, `_handleFillCredential`, `_handleUsePaymentMethod` respectively. The `_getActiveTab` helper itself remains at line 483 only as the legacy:* fall-through for the resolver. |
| 3   | handleGetPageSnapshotRoute in extension/ws/mcp-tool-dispatcher.js migrated to the resolver                                                                                                | VERIFIED       | Line 1473 declares `handleGetPageSnapshotRoute`; lines 1479-1481 call `globalThis.resolveAgentTabOrError(agentId, payload, client)`. The previous `getActiveTabFromClient(client)` is replaced.                                              |
| 4   | handleStartVisualSessionRoute in extension/ws/mcp-tool-dispatcher.js migrated to the resolver                                                                                             | VERIFIED       | Line 1179 declares `handleStartVisualSessionRoute`; lines 1215-1216 call resolver. Pragmatic legacy:* / missing-agentId fallthrough at lines 1205-1213 preserves byte-for-byte UX for `mcp-visual-session-contract.test.js` synthetic callers. |
| 5   | extension/ws/mcp-tool-dispatcher.js handleOpenTabRoute uses `active: params.active === true` (background default)                                                                         | VERIFIED       | Line 921: `chrome.tabs.create({ url: params.url || 'about:blank', active: params.active === true })`. Phase 240 D-08 bindTab + ownershipToken contract preserved at lines 923-940 byte-for-byte.                                              |
| 6   | tab_id?: number added to inputSchema of all 6 read tools, start_visual_session, and ~30 action tools in BOTH extension/ai/tool-definitions.js AND mcp/ai/tool-definitions.cjs (byte-identical) | VERIFIED       | 41 `tab_id: { type: 'number'` schema entries in extension/ai/tool-definitions.js (35 action tools + 6 read tools). MD5 confirms byte-identity (`7bf8ba2640439a4a12756e4148bedb5a` for both files); `diff -q` returns no output. start_visual_session has tab_id in Zod at mcp/src/tools/visual-session.ts:41. |
| 7   | mcp/src/tools/read-only.ts `void agentScope` REMOVED (agentId + optional tab_id now threaded)                                                                                            | VERIFIED       | grep `void agentScope` in read-only.ts returns 0 occurrences. Lines 95-98 contain comment block "Phase 246 D-02: ... OVERTURNED. Read-only tools now thread agentId + optional tab_id ..."; lines 119-145 implement `agentScope.ensure(bridge)` + `payloadWithAgent: { ..., agentId }` + `captureOwnershipToken` flow mirroring manual.ts. |
| 8   | mcp/src/tools/vault.ts `void agentScope` REMOVED (vault tools now agent-scoped)                                                                                                          | VERIFIED       | grep `void agentScope` in vault.ts returns 0 occurrences. Line 27 begins comment block "Phase 246 D-13 vault overturn: ..."; line 66 calls `agentScope.ensure(bridge)` for fill_credential; line 129 calls `agentScope.ensure(bridge)` for use_payment_method. Both now thread agentId/ownershipToken/connectionId/tab_id. |
| 9   | PARAM_TRANSFORMS for press_key, drag_drop, click_at, drag, fill_sheet explicitly forward tab_id (Pitfall 1 closed)                                                                       | VERIFIED       | mcp/src/tools/schema-bridge.ts:144-186 contains exactly 5 PARAM_TRANSFORMS entries each ending with `...(p.tab_id !== undefined ? { tab_id: p.tab_id } : {})` at lines 150, 158, 166, 178, 184. The 5 tools are press_key, drag_drop, click_at, drag, fill_sheet (matches the spec). |
| 10  | Resolver feeds resolved tabId back into routeParams (D-16) so checkOwnershipGate tab-arm fires for non-legacy MCP agents                                                                  | VERIFIED       | extension/ws/mcp-bridge-client.js line 598 in `_handleExecuteAction`: `...(resolved.skipGate ? {} : { tabId })` -- the load-bearing skipGate-aware spread. Non-legacy agents inject tabId into routeParams before `dispatchMcpToolRoute`; the gate's tab-arm now fires. |
| 11  | Legacy:* skipGate:true field on resolver return prevents tab-arm from firing for legacy surfaces                                                                                          | VERIFIED       | extension/utils/agent-tab-resolver.js line 47 first-line branch: `if (typeof agentId === 'string' && agentId.startsWith('legacy:'))` returns `{ tabId: tab.id, ownershipToken: null, skipGate: true }` (line 54). Non-legacy branches return `skipGate: false` (lines 61, 64, 79). |
| 12  | All 5 new test files exist and exit 0                                                                                                                                                    | VERIFIED       | All 5 NEW test files present: tests/agent-tab-resolver.test.js (25 PASS), tests/read-tool-tab-resolution.test.js (20 PASS), tests/open-tab-background-default.test.js (10 PASS), tests/visual-session-agent-scoped.test.js (16 PASS), tests/action-tool-agent-scoped.test.js (21 PASS). All run as part of npm test which exits 0. |
| 13  | All 4 extended test files still exit 0 with the new cases added                                                                                                                          | VERIFIED       | tests/legacy-agent-synthesis.test.js, tests/visual-session-reentry.test.js, tests/ownership-error-codes.test.js, tests/agent-id-threading.test.js all extended; all exit 0 within npm test. agent-id-threading reports 14 PASS (5 manual + 9 read-only/case A+B). |
| 14  | npm test exits 0 across the full suite                                                                                                                                                   | VERIFIED       | `npm test` exits 0; ~60+ test files chained via `&&` all green. Captured exit code 0 in /tmp/npm-test-full.log (last line `EXIT_CODE: 0`). |
| 15  | tests/multi-agent-regression.test.js extended with 2-agent scenarios from VALIDATION.md and exits 0                                                                                       | VERIFIED       | Direct invocation `node tests/multi-agent-regression.test.js` outputs `10/10 passed` -- 6 existing cases + 4 new Phase 246 scenarios (test_case_7..10): two_agents_read_isolation, legacy_popup_skipgate_after_user_switch, open_tab_background_default_with_bindtab, ambiguous_tab_error_then_explicit_recovery. |
| 16  | Manual smoke test (open extension popup -> click any tool button) confirms legacy popup UX is byte-for-byte unchanged                                                                     | NEEDS_HUMAN    | Documented in `human_verification` block above. Cannot be programmatically exercised; requires loading the extension in Chrome and observing popup tool dispatch against the user's active tab. The legacy:* skipGate:true branch (must-have 11) is structurally in place; human verification confirms the runtime UX matches v0.9.50. |

**Score:** 15/16 truths verified at the code level; 1 (must-have 16) routed to human verification per the original plan's design.

### Required Artifacts

| Artifact                                                  | Expected                                                                                                                | Status   | Details                                                                                                                                                              |
| --------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `extension/utils/agent-tab-resolver.js`                   | resolveAgentTabOrError helper with D-01 (3-branch registry) + D-04 (legacy:* fallthrough)                                | VERIFIED | 4198 bytes; 89 lines; D-01 branches at lines 67-79; D-04 branch at lines 47-55; D-12 error codes (NO_OWNED_TAB, AMBIGUOUS_TAB, NO_ACTIVE_TAB, AGENT_REGISTRY_UNAVAILABLE) all returned per envelope shape. |
| `tests/agent-tab-resolver.test.js`                        | Unit tests for D-01 + D-04 cases                                                                                        | VERIFIED | 25 PASS within npm test.                                                                                                                                             |
| `tests/read-tool-tab-resolution.test.js`                  | Integration tests for D-02 read tool path                                                                               | VERIFIED | 20 PASS within npm test.                                                                                                                                             |
| `tests/open-tab-background-default.test.js`               | Integration tests for D-05/D-06/D-08 open_tab default flip                                                              | VERIFIED | 10 PASS within npm test.                                                                                                                                             |
| `tests/visual-session-agent-scoped.test.js`               | Integration tests for D-09/D-10/D-11 visual session resolver migration                                                  | VERIFIED | 16 PASS within npm test.                                                                                                                                             |
| `tests/action-tool-agent-scoped.test.js`                  | Integration tests for D-13/D-14 action tool migration + 5 PARAM_TRANSFORMS forwards + vault overturn                    | VERIFIED | 21 PASS within npm test.                                                                                                                                             |
| `tests/tool-definitions-parity.test.js`                   | Byte-identity check between extension/ai/tool-definitions.js and mcp/ai/tool-definitions.cjs                            | VERIFIED | 130 PASS within npm test (Buffer.compare === 0 + 6 read tool sanity + open_tab.active sanity + 35 action tool sanity).                                                |
| `tests/multi-agent-regression.test.js` (extended)         | 4 new Phase 246 scenarios A/B/C/D                                                                                       | VERIFIED | 10/10 cases GREEN (6 existing + 4 new).                                                                                                                              |
| `tests/agent-id-threading.test.js` (extended)             | 2 read-only.ts cases (D-02 server-side overturn)                                                                        | VERIFIED | 14 PASS (5 manual + read-only Case A + Case B with tab_id forwarding assertion).                                                                                     |
| `tests/legacy-agent-synthesis.test.js` (extended)         | 2 D-15 cases                                                                                                            | VERIFIED | All assertions pass within npm test.                                                                                                                                 |
| `tests/visual-session-reentry.test.js` (extended)         | 1 D-11 same-agent resume case                                                                                           | VERIFIED | All assertions pass within npm test.                                                                                                                                 |
| `tests/ownership-error-codes.test.js` (extended)          | 2 D-16 cases (resolver feeds tabId; gate fires)                                                                         | VERIFIED | 23 PASS within npm test.                                                                                                                                             |

### Key Link Verification

| From                                                                | To                                              | Via                                                                                            | Status   | Details                                                                                                                                                                                                            |
| ------------------------------------------------------------------- | ----------------------------------------------- | ---------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| extension/ws/mcp-bridge-client.js _handleGetDOM/_handleReadPage     | extension/utils/agent-tab-resolver.js           | `globalThis.resolveAgentTabOrError(agentId, params, this)`                                     | WIRED    | grep returns 5 calls inside the relevant handler bodies (lines 526, 543, 579, 1234, 1301).                                                                                                                          |
| extension/ws/mcp-tool-dispatcher.js handleGetPageSnapshotRoute      | extension/utils/agent-tab-resolver.js           | resolver call inside the handler body                                                          | WIRED    | Lines 1479-1481 inside the function body.                                                                                                                                                                          |
| extension/ws/mcp-tool-dispatcher.js handleStartVisualSessionRoute   | extension/utils/agent-tab-resolver.js           | resolver call (with legacy:* fallthrough at lines 1205-1213)                                    | WIRED    | Lines 1215-1216 inside the function body.                                                                                                                                                                          |
| extension/ws/mcp-tool-dispatcher.js handleOpenTabRoute              | chrome.tabs.create                              | `active: params.active === true`                                                               | WIRED    | Line 921; bindTab + ownershipToken preserved at lines 923-940.                                                                                                                                                     |
| extension/ws/mcp-bridge-client.js _handleExecuteAction routeParams  | dispatchMcpToolRoute (Phase 240 gate)           | `...(resolved.skipGate ? {} : { tabId })` injection at line 598                                | WIRED    | Lines 596-602 build skipGate-aware routeParams; line 611 forwards them down to `_handleExecuteBackground` and to `dispatchMcpToolRoute`. Phase 240 gate's `_resolveTabIdForGate` now sees a non-null tabId for non-legacy agents. |
| mcp/src/tools/read-only.ts (registerReadOnlyTools)                  | bridge payload                                  | `agentScope.ensure(bridge)` + payloadWithAgent + captureOwnershipToken                          | WIRED    | Lines 119-144 thread agentId/ownershipToken/connectionId; messageBuilder forwards p.tab_id at lines 31, 35, 39, 43, 51, 55.                                                                                          |
| mcp/src/tools/vault.ts (fill_credential / use_payment_method)       | bridge payload                                  | `agentScope.ensure(bridge)` (lines 66, 129); agentId in payload (lines 73, 136)                | WIRED    | Vault overturn complete; Phase 238 D-06 exemption removed.                                                                                                                                                          |
| mcp/src/tools/schema-bridge.ts PARAM_TRANSFORMS                     | extension-side _handleExecuteAction params      | `...(p.tab_id !== undefined ? { tab_id: p.tab_id } : {})` spread in 5 transforms                | WIRED    | Lines 150, 158, 166, 178, 184 each carry the spread. Pitfall 1 closed.                                                                                                                                              |
| extension/background.js                                              | utils/agent-tab-resolver.js                     | importScripts at line 16                                                                       | WIRED    | Loaded BEFORE mcp-tool-dispatcher.js (line 28) and mcp-bridge-client.js (line 39). Order verified in REVIEW.md cross-cutting check #8.                                                                              |

### Data-Flow Trace (Level 4)

This phase produces no UI components that render dynamic data; the artifacts are dispatch-path code (resolver, bridge handlers, schema transforms). Level 4 (data flow) is therefore N/A for the artifacts. The relevant data-flow assurance is exercised by the integration tests directly (e.g., agent-id-threading.test.js asserts the agentId actually appears in the bridge payload, action-tool-agent-scoped.test.js asserts tab_id reaches the routed params).

### Behavioral Spot-Checks

| Behavior                                                                    | Command                                                                       | Result        | Status |
| --------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | ------------- | ------ |
| Full npm test suite passes (must-have 14)                                   | `npm test`                                                                    | EXIT_CODE: 0  | PASS   |
| MCP TypeScript build succeeds (regression gate; required by Plan 03 Task 5) | `npm --prefix mcp run build`                                                  | exit 0        | PASS   |
| tool-definitions byte-identity (Pitfall 2 canary)                           | `diff -q extension/ai/tool-definitions.js mcp/ai/tool-definitions.cjs`        | no output     | PASS   |
| MD5 confirmation of byte-identity                                           | `md5 extension/ai/tool-definitions.js mcp/ai/tool-definitions.cjs`            | identical hash (7bf8ba2640439a4a12756e4148bedb5a) | PASS   |
| Resolver loaded in extension SW (importScripts order)                       | `grep agent-tab-resolver extension/background.js`                             | line 16 (before dispatcher line 28 and bridge-client line 39) | PASS   |
| 5 _handleExecuteAction-style call sites consume the resolver                | `grep -c resolveAgentTabOrError extension/ws/mcp-bridge-client.js`            | 5 hits        | PASS   |
| 4 Phase 246 multi-agent regression scenarios pass standalone                | `node tests/multi-agent-regression.test.js`                                   | 10/10 passed (test_case_7..10 included) | PASS   |
| autopilot.ts run_task description has no misleading tab_id reference        | `grep -c tab_id mcp/src/tools/autopilot.ts`                                   | 0             | PASS   |

### Requirements Coverage

Phase 246 introduces NO new REQ-IDs (gap-closure phase). Coverage is against the 16 design contracts D-01..D-16 from CONTEXT.md, which the plans use in lieu of REQ-IDs. Cross-reference of every D-XX:

| D-XX  | Decision                                                                                                                  | Source Plan      | Status     | Evidence                                                                                                                                                                                            |
| ----- | ------------------------------------------------------------------------------------------------------------------------- | ---------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-01  | Auto-resolve via registry: 1 owned tab use; 0 -> NO_OWNED_TAB; 2+ -> AMBIGUOUS_TAB                                         | 01 Task 2        | SATISFIED  | extension/utils/agent-tab-resolver.js lines 67-79 contain the 3 branches with correct error envelopes.                                                                                              |
| D-02  | Optional tab_id on read tool MCP schemas + read-only.ts threads agentId                                                   | 01 Tasks 6 & 7   | SATISFIED  | 6 read tools have tab_id schema (extension+mcp byte-identical); read-only.ts thread agentId via agentScope.ensure (lines 119-144); messageBuilder forwards p.tab_id at lines 31, 35, 39, 43, 51, 55. |
| D-03  | Resolution happens extension-side (registry is extension-owned)                                                            | 01 Task 2        | SATISFIED  | Resolver lives in extension/utils/agent-tab-resolver.js consumed via globalThis. MCP server has no resolver code; only forwards agentId+tab_id+ownership_token through the bridge.                  |
| D-04  | Legacy:* prefix branch: first line of resolver returns active-tab via client._getActiveTab(), tagged skipGate:true        | 01 Task 2        | SATISFIED  | Resolver line 47-55: `if (typeof agentId === 'string' && agentId.startsWith('legacy:'))` returns `{ tabId, ownershipToken: null, skipGate: true }`.                                                  |
| D-05  | open_tab defaults to background: `active: params.active === true`                                                          | 01 Task 5        | SATISFIED  | extension/ws/mcp-tool-dispatcher.js line 921 confirmed.                                                                                                                                              |
| D-06  | open_tab schema declares active: { type: boolean, default: false }                                                         | 01 Task 5        | SATISFIED  | tool-definitions has explicit `active` boolean property; tool-definitions-parity.test.js Test 4 (in-test sanity-check) asserts this.                                                                |
| D-07  | Audit scope: MCP-routed paths only (popup/sidepanel/options/ws-client untouched)                                          | 01 + 02 + 03     | SATISFIED  | Only the 6 enumerated MCP-routed sites changed. popup.js, sidepanel.js, options.js, ws-client.js were not modified by phase 246 commits (verified by `git log --name-only` against the 18 phase commits). |
| D-08  | open_tab still calls bindTab and surfaces ownershipToken in response                                                       | 01 Task 5        | SATISFIED  | extension/ws/mcp-tool-dispatcher.js lines 923-940 preserve the bindTab + ownershipToken contract byte-for-byte.                                                                                     |
| D-09  | handleStartVisualSessionRoute uses resolveAgentTabOrError (replaces getActiveTabFromClient for MCP agents)                | 02 Task 2        | SATISFIED  | mcp-tool-dispatcher.js lines 1215-1216 inside `handleStartVisualSessionRoute` body. Pragmatic legacy:* / missing-agentId fallthrough at 1205-1213 preserves contract test compatibility.            |
| D-10  | start_visual_session schema declares optional tab_id; description documents multi-agent semantics                          | 02 Task 2        | SATISFIED  | mcp/src/tools/visual-session.ts:41 declares `tab_id: z.number().optional().describe('Optional. Tab id ...')`; payload forwards it at line 72.                                                       |
| D-11  | Same-agent visual session resume preserved with explicit tab_id                                                            | 02 Task 2 + extension to visual-session-reentry.test.js | SATISFIED | Resolver returns tabId; mcp-visual-session.js startSession's existing same-agent resume branch (Phase 240 D-03) is reachable; visual-session-reentry.test.js asserts the resume contract holds.     |
| D-12  | Shared error code surface (NO_OWNED_TAB, AMBIGUOUS_TAB, NO_ACTIVE_TAB, AGENT_REGISTRY_UNAVAILABLE)                          | 01 Task 2        | SATISFIED  | Resolver returns plain-object error envelope `{ success: false, code, agentId?, tabIds? }` at lines 52, 70, 74, 77 (4 distinct error codes; matches Phase 240 shape).                                |
| D-13  | Action tools use the same resolver (incl. _handleExecuteAction, _handleFillCredential, _handleUsePaymentMethod) + vault overturn | 02 Tasks 3 & 6 | SATISFIED | mcp-bridge-client.js _handleExecuteAction (line 579), _handleFillCredential (line 1234), _handleUsePaymentMethod (line 1301) all use resolver. vault.ts has agentScope.ensure at lines 66, 129. |
| D-14  | Optional tab_id on all 35 action tool MCP schemas + 5 PARAM_TRANSFORMS forwards                                            | 02 Tasks 4 & 5   | SATISFIED  | 35 action tools have tab_id schema (verified by tool-definitions-parity.test.js 105 PASS lines for 35 tools). PARAM_TRANSFORMS at schema-bridge.ts:150,158,166,178,184 forward tab_id (Pitfall 1 closed). |
| D-15  | Single legacy:* branch covers all tool families; legacy:* dispatch UX byte-for-byte unchanged                              | 02 Task 7 + extension to legacy-agent-synthesis.test.js | SATISFIED | Resolver's first-line branch at line 47 returns `skipGate:true` for any `legacy:*` agentId regardless of tool family. _handleExecuteAction routeParams composition at line 598 honors skipGate. |
| D-16  | Resolver feeds resolved tabId back into routeParams.tabId so dispatch gate's tab-arm fires for non-legacy MCP agents       | 02 Task 3        | SATISFIED  | extension/ws/mcp-bridge-client.js line 598 `...(resolved.skipGate ? {} : { tabId })` is the load-bearing line. ownership-error-codes.test.js D-16 cases (test 22-23) assert TAB_NOT_OWNED fires when an agent passes another agent's tab_id. |

**Coverage:** 16/16 design contracts satisfied. Zero orphaned decisions.

### Anti-Patterns Found

Per the REVIEW.md findings (1 warning + 4 info, all categorized as polish/non-blocking) plus my independent re-scan:

| File                                       | Line(s)         | Pattern                                                                                                                                              | Severity | Impact                                                                                                                                                         |
| ------------------------------------------ | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `mcp/src/tools/visual-session.ts`          | 94              | `end_visual_session` description mentions "tab_id is agent-scoped" but the Zod schema at line 96-97 has only `session_token` + `reason` (no tab_id) | Info     | Documentation hygiene; misleads MCP clients into thinking they can pass tab_id (they can't). REVIEW.md WR-01. Does NOT block phase goal.                       |
| `mcp/src/tools/agents.ts`                  | 33              | `back` description references snake-case `tab_id` while its schema (line 34) uses camelCase `tabId`                                                  | Info     | Documentation hygiene; cross-tool MCP boundary is otherwise snake_case, so `back` is the lone outlier. REVIEW.md WR-01. Does NOT block phase goal.             |
| `extension/ws/mcp-bridge-client.js`        | 650-657         | `_handleExecuteBackground` defensive fallback rebuilds routeParams without tabId                                                                     | Info     | Dead code today (only legitimate caller `_handleExecuteAction` always passes routeParams). REVIEW.md IN-01. Does NOT block phase goal.                          |
| `extension/ws/mcp-tool-dispatcher.js`      | 1204, 1226, 1246 | `let resolvedFromRegistry = false` set then `void resolvedFromRegistry` (dead var)                                                                    | Info     | Cosmetic; comment at line 1244-1245 mentions "may be consumed by future diagnostics". REVIEW.md IN-02. Does NOT block phase goal.                              |
| `tests/visual-session-agent-scoped.test.js` | 158-160         | `uninstallCallbackHandler` defined but never called                                                                                                  | Info     | Test hygiene; each test runs in its own Node process so cross-test pollution is impossible in practice. REVIEW.md IN-03. Does NOT block phase goal.            |
| `tests/action-tool-agent-scoped.test.js`, `tests/visual-session-agent-scoped.test.js`, `tests/ownership-error-codes.test.js` | various         | Wave 0 source-detection gates (`TASK3_LANDED_*`, `TASK5_LANDED_*`) are now dead code (all migrations have shipped)                                 | Info     | Verbose but correct. REVIEW.md IN-04. Does NOT block phase goal.                                                                                                |

**Classification summary:** 0 critical / 0 blocker; 1 warning (WR-01, doc hygiene); 4 info (all cosmetic / dead code in test files / defensive fallthroughs that are safe). None of these prevent the phase goal from being achieved. They are tracked for future polish phases.

### Human Verification Required

#### 1. Manual smoke test of legacy popup UX (must-have 16)

**Test:** Load the unpacked extension in Chrome. Open the FSB extension popup (default v0.9.50 popup UI). Click any tool button (e.g., a quick-action button, the "Start" task button, or any popup-driven control that ultimately routes through the manual-mode tool dispatch path). Observe the runtime behavior.

**Expected:**
- The popup dispatches the tool against the user's currently active tab.
- No focus stealing, no extra navigation, no resolver-driven AMBIGUOUS_TAB or NO_OWNED_TAB errors despite popup being a legacy:popup synthesized agent.
- Tool execution completes against the active tab as it did in v0.9.50.
- No new console errors related to "AGENT_REGISTRY_UNAVAILABLE" or "NO_ACTIVE_TAB".
- (Implicit) The legacy:* skipGate:true branch ensures the dispatch gate's tab-arm is skipped, preserving byte-for-byte UX.

**Why human:** The popup runs in a real Chrome runtime (not the node:test harness), exercises real user-driven tab interactions, and is the only surface where the legacy:* fallthrough is meaningfully observable end-to-end. The structural plumbing (skipGate:true on legacy:popup, routeParams skipping tabId injection when skipGate is true, gate's tab-arm skip path) is verified at the code level (must-haves 11, 10) and the unit test (test_case_8_phase246_legacy_popup_skipgate_after_user_switch in multi-agent-regression) covers the resolver branch directly. But the runtime UX of "user clicks popup tool button after switching tabs" requires actual Chrome and real tab focus. The plan's 16th must-have explicitly flags this as human-verifiable.

### Gaps Summary

No gaps. All 16 must-haves either VERIFIED at the code level (15) or routed to documented human verification (1). All 16 D-XX design contracts realized in code with explicit per-line evidence. The REVIEW.md WR-01 warning + 4 info findings are polish/cosmetic items that do not block the phase goal -- they are flagged in the Anti-Patterns table for future cleanup phases but do not warrant a "gaps_found" status.

The phase goal stated in the prompt -- "Replace `chrome.tabs.query({active:true})` with agent-registry-driven tab resolution across read tools, visual-session, and action tools, default `open_tab` to background, and ensure MCP-routed surfaces never steal focus from the user or other agents" -- is achieved:

- 6 read tools, start_visual_session, and ~37 action tools (entry point: _handleExecuteAction) all consume the resolver.
- open_tab defaults to background (`active: params.active === true`).
- Cross-agent calls reject via the Phase 240 dispatch gate now that resolver feeds tabId into routeParams (D-16).
- Legacy popup/sidepanel/autopilot UX is preserved byte-for-byte via the skipGate:true mechanism.

Status is human_needed (not passed) only because must-have 16 explicitly defers to manual smoke testing -- which the plan acknowledged from the outset.

---

_Verified: 2026-05-06T23:06:52Z_
_Verifier: Claude (gsd-verifier)_
