---
phase: 243
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/utils/overlay-state.js
  - extension/content/visual-feedback.js
  - extension/ui/popup.js
  - extension/ui/sidepanel.js
  - tests/badge-agent-id.test.js
  - tests/owner-chip.test.js
autonomous: true
requirements:
  - UI-01
  - UI-02
must_haves:
  truths:
    - "The trusted-client badge renderer in visual-feedback.js displays `<clientLabel> / <agentIdShort>` when both are present (just clientLabel if only that; just agentIdShort if only that; hidden if neither)."
    - "agentIdShort is produced by formatAgentIdForDisplay (6 hex chars per agent-registry.js:42 FSB_AGENT_DISPLAY_HEX_LENGTH); never sliced locally."
    - "overlay-state.js buildOverlayState threads agentIdShort as a SEPARATE field next to clientLabel (not concatenated upstream — preserves dashboard-mirror flexibility)."
    - "Popup and sidepanel show a read-only chip 'owned by <ownerAgentIdShort>' when the active tab's owner is a non-legacy agent AND that agent is not the current surface's own legacy ID."
    - "When the active tab is owned by legacy:popup or legacy:sidepanel and the rendering surface IS that legacy ID, the chip is hidden."
    - "The chip never includes a click handler, action button, or enforcement (read-only per D-05)."
  artifacts:
    - path: "extension/utils/overlay-state.js"
      provides: "agentIdShort field added to buildOverlayState envelope (next to clientLabel)"
      contains: "agentIdShort"
    - path: "extension/content/visual-feedback.js"
      provides: "Badge renderer reads both clientLabel and agentIdShort; combines as 'clientLabel / agentIdShort'"
      contains: "agentIdShort"
    - path: "extension/ui/popup.js"
      provides: "Active-tab owner chip renderer with surface-self exclusion"
      contains: "owned by"
    - path: "extension/ui/sidepanel.js"
      provides: "Active-tab owner chip renderer with surface-self exclusion"
      contains: "owned by"
    - path: "tests/badge-agent-id.test.js"
      provides: "overlay-state shape test (agentIdShort field) + visual-feedback render test (combined string)"
    - path: "tests/owner-chip.test.js"
      provides: "Chip render condition: shows for non-legacy foreign owner; hidden for self-owned legacy:* surface; hidden for unowned tabs"
  key_links:
    - from: "extension/utils/overlay-state.js buildOverlayState"
      to: "session.agentId via formatAgentIdForDisplay"
      via: "registry helper call"
      pattern: "formatAgentIdForDisplay"
    - from: "extension/content/visual-feedback.js badge renderer"
      to: "overlayState.agentIdShort"
      via: "DOM textContent assignment"
      pattern: "agentIdShort"
    - from: "popup.js / sidepanel.js"
      to: "background.js getActiveTabOwner bridge"
      via: "chrome.runtime.sendMessage({ action: 'getActiveTabOwner', tabId })"
      pattern: "getActiveTabOwner"
---

<objective>
Implement UI-01 (badge agentId suffix) and UI-02 (popup/sidepanel "owned by Agent X" read-only chip):

UI-01: Extend `extension/utils/overlay-state.js` `buildOverlayState` to thread `agentIdShort` from `session.agentId` via `formatAgentIdForDisplay` (the 6-hex-char canonical helper at agent-registry.js:184). Render in `extension/content/visual-feedback.js` at the existing badge consumer (lines 697-702): combine `clientLabel + ' / ' + agentIdShort` when both are present.

UI-02: Add a new `getActiveTabOwner` bridge action in `background.js` (handled by Plan 02 if it adds the listener — coordinate, OR handle here in popup/sidepanel directly via `chrome.runtime.sendMessage` to a NEW handler). Render a read-only chip `owned by <ownerAgentIdShort>` in popup and sidepanel when the active tab is owned by a non-legacy agent AND that agent ID is not the surface's own legacy:popup / legacy:sidepanel synthesized ID.

Purpose: Multi-agent observability — the user can identify which of N concurrent agents owns the visible overlay AND can see when a tab is being driven by an MCP agent vs the legacy popup/sidepanel UI.
Output: 4 source files modified + 2 test files; no new chrome permissions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/243-background-tab-audit-ui-badge-integration/243-CONTEXT.md
@.planning/phases/243-background-tab-audit-ui-badge-integration/243-RESEARCH.md
@.planning/phases/243-background-tab-audit-ui-badge-integration/243-VALIDATION.md

@extension/utils/overlay-state.js
@extension/content/visual-feedback.js
@extension/ui/popup.js
@extension/ui/sidepanel.js
@extension/utils/agent-registry.js

<interfaces>
<!-- overlay-state.js producer (lines 331-353 buildOverlayState) -->

Existing envelope spreads `clientLabel` from `session.clientLabel`. Add a parallel field:
  agentIdShort: session && session.agentId
    ? formatAgentIdForDisplay(session.agentId)
    : null

Confirm formatAgentIdForDisplay reachability at this call site:
  - agent-registry.js exports the helper as a top-level function (line 184) — verify the export pattern.
  - If overlay-state.js is loaded via importScripts in background.js, the global is reachable.
  - If overlay-state.js is required as a module elsewhere, import explicitly.

<!-- visual-feedback.js consumer (lines 697-702) -->

Current code (paraphrased):
  var clientLabel = overlayState.clientLabel ? String(overlayState.clientLabel).trim() : '';
  var clientBadgeEl = this.container.querySelector('.fsb-client-badge');
  if (clientBadgeEl) {
    clientBadgeEl.textContent = clientLabel;
    clientBadgeEl.style.display = clientLabel ? 'inline-flex' : 'none';
  }

Replace with:
  var clientLabel = overlayState.clientLabel ? String(overlayState.clientLabel).trim() : '';
  var agentIdShort = overlayState.agentIdShort ? String(overlayState.agentIdShort).trim() : '';
  var combined = '';
  if (clientLabel && agentIdShort) combined = clientLabel + ' / ' + agentIdShort;
  else if (clientLabel) combined = clientLabel;
  else if (agentIdShort) combined = agentIdShort;
  var clientBadgeEl = this.container.querySelector('.fsb-client-badge');
  if (clientBadgeEl) {
    clientBadgeEl.textContent = combined;
    clientBadgeEl.style.display = combined ? 'inline-flex' : 'none';
  }

<!-- popup.js / sidepanel.js chip pattern -->

Active tab query exists at:
  - popup.js:248: chrome.tabs.query({ active: true, currentWindow: true })
  - sidepanel.js:423, 1093, 1208: same pattern (3 sites)

After the active-tab query (or in a small init helper), call:
  chrome.runtime.sendMessage({ action: 'getActiveTabOwner', tabId: tab.id }, (response) => {
    if (response && response.ownerAgentId && response.ownerAgentId !== mySurface) {
      // Render chip
    }
  });
where mySurface = 'legacy:popup' | 'legacy:sidepanel' (constant per file).

<!-- getActiveTabOwner bridge handler (background.js) -->

Add a case to the existing chrome.runtime.onMessage.addListener switch:
  case 'getActiveTabOwner': {
    const reg = globalThis.fsbAgentRegistryInstance;
    if (!reg || typeof reg.getOwner !== 'function') {
      sendResponse({ ownerAgentId: null });
      return true;
    }
    const tabId = Number(request.tabId);
    if (!Number.isFinite(tabId)) {
      sendResponse({ ownerAgentId: null });
      return true;
    }
    const ownerAgentId = reg.getOwner(tabId);
    if (!ownerAgentId) {
      sendResponse({ ownerAgentId: null });
      return true;
    }
    sendResponse({
      ownerAgentId,
      ownerAgentIdShort: ownerAgentId.startsWith('legacy:')
        ? ownerAgentId
        : (typeof formatAgentIdForDisplay === 'function'
            ? formatAgentIdForDisplay(ownerAgentId) : ownerAgentId)
    });
    return true;
  }

Place this case adjacent to existing tab-related message handlers in background.js.
NOTE: Plan 02 modifies background.js for the BG-04 listener. To avoid same-file collision with Plan 02:
  - Plan 02 owns the webNavigation.onCommitted extension.
  - Plan 03 owns the chrome.runtime.onMessage `getActiveTabOwner` case.
  - Both modify background.js but in DIFFERENT regions (listener vs message switch).
  - Same-wave parallelism with same file requires cooperative editing; downgrade Plan 03 wave if needed.

DECISION: To preserve true Wave-1 zero-overlap, this plan does NOT modify background.js. Instead, popup.js and sidepanel.js fetch ownership directly by calling `agentRegistry.getOwner` via a NEW utility module pattern OR by calling an existing message route. Re-examine existing routes in background.js — if a `getRegistryStatus` or similar already exposes ownership data, use it. Otherwise, expose a dedicated route via the existing `mcp_phase199` route table (Plan 02 area) — coordinate and accept Plan 03 -> Wave 2 if necessary.

PRACTICAL FALLBACK (chosen): popup.js and sidepanel.js access the registry via the existing `chrome.storage.session.get('fsbAgentRegistry')` envelope that Phase 237 D-03 write-throughs. Read the envelope, find the record whose `tabIds` includes the active tab.id, expose ownerAgentId. This bypasses background.js entirely — TRUE Wave-1 zero-overlap.

  Pseudocode:
    chrome.storage.session.get('fsbAgentRegistry', (res) => {
      const env = res && res.fsbAgentRegistry;
      const records = (env && env.records) || {};
      let ownerAgentId = null;
      for (const [agentId, rec] of Object.entries(records)) {
        if (rec.tabIds && rec.tabIds.includes(tab.id)) { ownerAgentId = agentId; break; }
        if (rec.tabId === tab.id) { ownerAgentId = agentId; break; }
      }
      // ... render chip
    });

  Verify the storage envelope shape (records keyed by agentId, value contains tabIds array OR tabId scalar) by reading agent-registry.js _persistEnvelope output. The existing Phase 241 cap UI in options.js already does this kind of lookup (line 263 in RESEARCH Pattern 5) — use that pattern as the reference.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UI-01 — overlay-state agentIdShort + badge renderer combine</name>
  <files>extension/utils/overlay-state.js, extension/content/visual-feedback.js, tests/badge-agent-id.test.js</files>
  <read_first>
    - extension/utils/overlay-state.js lines 320-360 (buildOverlayState body) — confirm session.agentId field availability
    - extension/content/visual-feedback.js lines 444-450 (CSS) and 685-720 (renderer body around the badge update)
    - extension/utils/agent-registry.js lines 40-50 (FSB_AGENT_DISPLAY_HEX_LENGTH constant) and lines 175-200 (formatAgentIdForDisplay implementation + export pattern)
  </read_first>
  <behavior>
    - Test 1 (RED): buildOverlayState({ agentId: 'agent_a3f1ab123c45...' }, ...) returns an object with agentIdShort = 6 hex chars (matches /^agent_[0-9a-f]{6}$/).
    - Test 2 (RED): buildOverlayState({}) (no agentId) returns agentIdShort = null (or absent — test for falsy).
    - Test 3 (RED): The visual-feedback consumer logic, given overlayState = { clientLabel: 'Claude', agentIdShort: 'agent_a3f1ab' }, computes the combined badge text 'Claude / agent_a3f1ab'.
    - Test 4 (RED): clientLabel only (no agentIdShort) -> combined = 'Claude'. agentIdShort only (no clientLabel) -> combined = 'agent_a3f1ab'. Neither -> combined = '' and badge hidden.
  </behavior>
  <action>
    1. Create tests/badge-agent-id.test.js using plain-Node `assert`. Strategy:
       - For Tests 1-2: require the registry helper formatAgentIdForDisplay (exported from agent-registry.js — verify the CommonJS export shim at the bottom of the file). Then either:
         a) require overlay-state.js directly if it has a CommonJS export shim, OR
         b) Test the SHAPE contract by extracting the buildOverlayState function into a testable unit. The simplest path: read overlay-state.js as text, assert that line 331-353 contains `agentIdShort` and a call to `formatAgentIdForDisplay(session.agentId)`. Pair with a positive runtime test that imports the function.
       - For Tests 3-4: extract the badge-combine logic from visual-feedback.js into a small pure helper `_combineBadgeText(clientLabel, agentIdShort)` exported at the top of the file (or in a small extension/content/badge-combine.js helper file). Test the helper directly.
    2. Run `node --test tests/badge-agent-id.test.js`; all RED.
    3. Edit extension/utils/overlay-state.js:
       - In buildOverlayState (around line 331-353), add to the returned envelope:
           agentIdShort: session && session.agentId && typeof formatAgentIdForDisplay === 'function'
             ? formatAgentIdForDisplay(session.agentId)
             : null
       - Confirm formatAgentIdForDisplay reachability. If overlay-state.js is loaded via importScripts AFTER agent-registry.js, the global is available. If overlay-state.js is required separately, add `const { formatAgentIdForDisplay } = require('./agent-registry.js');` at the top OR (cleaner) destructure from a passed-in registry instance. Match the existing import pattern in the file.
    4. Edit extension/content/visual-feedback.js:
       - Locate the badge update block (around line 697-702 — `var clientLabel = overlayState.clientLabel ...`).
       - Replace with the combined logic per <interfaces> above.
       - Optionally extract _combineBadgeText into a helper function at the top of the file or in a new extension/content/badge-combine.js (preferred for testability).
    5. Re-run tests; all GREEN.
    6. Sanity grep:
         grep -c "agentIdShort" extension/utils/overlay-state.js   # >= 2 (extraction + assignment)
         grep -c "agentIdShort" extension/content/visual-feedback.js # >= 1
         grep -c "formatAgentIdForDisplay" extension/utils/overlay-state.js # >= 1
    7. Commit: `feat(243-03): UI-01 thread agentIdShort through overlay-state and combine in badge renderer`.
  </action>
  <verify>
    <automated>node --test tests/badge-agent-id.test.js</automated>
  </verify>
  <done>
    All 4 tests GREEN. overlay-state.js produces agentIdShort field via formatAgentIdForDisplay (6 hex chars). visual-feedback.js consumer combines as 'clientLabel / agentIdShort' when both present.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UI-02 — popup.js owner chip with surface-self exclusion</name>
  <files>extension/ui/popup.js, tests/owner-chip.test.js</files>
  <read_first>
    - extension/ui/popup.js full file: identify (a) the active-tab query at line 248, (b) the popup HTML container DOM where the chip will live (header? status row?), (c) the existing chrome.runtime.sendMessage patterns.
    - extension/ui/popup.html (look for header/status DOM where chip can be inserted)
    - 243-RESEARCH.md "Popup/sidepanel owner-chip integration" + Code Example 3
  </read_first>
  <behavior>
    - Test 1 (RED): A pure helper `shouldShowOwnerChip(ownerAgentId, mySurface)` returns true when ownerAgentId is non-null, does NOT start with 'legacy:', and !== mySurface.
    - Test 2 (RED): Returns false when ownerAgentId is null/undefined (unowned tab).
    - Test 3 (RED): Returns false when ownerAgentId === mySurface (popup owns its own tab — no chip).
    - Test 4 (RED): Returns true when ownerAgentId is 'legacy:sidepanel' AND mySurface = 'legacy:popup' (cross-surface ownership IS shown — per CONTEXT D-05 the legacy surface itself doesn't render the chip when it owns the tab; cross-surface display is allowed and informative).
    - Test 5 (RED): The chip text format is exactly 'owned by ' + ownerLabel where ownerLabel is ownerAgentIdShort (formatAgentIdForDisplay output for non-legacy; the literal string for legacy:*).
  </behavior>
  <action>
    1. Create tests/owner-chip.test.js. Extract the chip-decision logic into a small pure helper file `extension/ui/owner-chip.js` that exports:
         function shouldShowOwnerChip(ownerAgentId, mySurface) { ... }
         function buildChipText(ownerAgentIdShort) { return 'owned by ' + ownerAgentIdShort; }
       Test the helpers directly.
    2. Run tests; all RED.
    3. Implement extension/ui/owner-chip.js with the two helpers.
    4. Edit extension/ui/popup.js:
       - At the top, add the constant `const MY_SURFACE = 'legacy:popup';`.
       - Add a new function `refreshOwnerChip()` that:
         a) Queries chrome.tabs.query({ active: true, currentWindow: true }, (tabs) => { ... });
         b) Reads chrome.storage.session.get('fsbAgentRegistry', (res) => { ... }) and finds the agentId whose tabIds (or tabId) includes the active tab.id.
         c) Falls back to chrome.runtime.sendMessage({ action: 'getActiveTabOwner', tabId }) if a route handler exists — but per <interfaces> DECISION, prefer the storage.session direct read to avoid background.js coupling.
         d) Imports/uses the helpers from extension/ui/owner-chip.js (via a script tag in popup.html if popup.js is loaded as a classic script; or via importScripts equivalent — match the existing popup.js loading style).
         e) Updates a DOM element `<span id="fsb-owner-chip" style="display:none">`.
       - Call refreshOwnerChip() on popup load (DOMContentLoaded).
       - Add the `<span id="fsb-owner-chip">` to popup.html in the existing header / status area. Style minimally (small pill, muted background, no click handler).
    5. Re-run tests; all GREEN.
    6. Sanity grep:
         grep -c "owned by" extension/ui/popup.js                  # >= 1 (or in popup.html)
         grep -c "owned by" extension/ui/owner-chip.js              # >= 1
         grep -c "fsb-owner-chip" extension/ui/popup.html           # >= 1
         grep -c "shouldShowOwnerChip" extension/ui/popup.js         # >= 1
    7. Commit: `feat(243-03): UI-02 popup owner-chip render with surface-self exclusion`.
  </action>
  <verify>
    <automated>node --test tests/owner-chip.test.js</automated>
  </verify>
  <done>
    All 5 tests GREEN. popup.js calls refreshOwnerChip on load; chip renders only for foreign owners (per shouldShowOwnerChip); chip text is `owned by <agentIdShort>`. No click handler attached. popup.html contains the chip span.
  </done>
</task>

<task type="auto">
  <name>Task 3: UI-02 — sidepanel.js owner chip with surface-self exclusion</name>
  <files>extension/ui/sidepanel.js</files>
  <read_first>
    - extension/ui/sidepanel.js full file: identify the 3 active-tab query sites (lines 423, 1093, 1208) and the sidepanel header DOM
    - extension/ui/sidepanel.html for chip insertion point
  </read_first>
  <action>
    1. Apply the same pattern as Task 2:
       - At the top of sidepanel.js, add `const MY_SURFACE = 'legacy:sidepanel';`.
       - Reuse extension/ui/owner-chip.js helpers.
       - Add a `refreshOwnerChip()` function that uses chrome.tabs.query + chrome.storage.session.get + the helpers.
       - Wire chrome.tabs.onActivated to call refreshOwnerChip on tab switch (sidepanel is persistent so this matters more than for popup; mitigates Pitfall 5 stale-data flicker for sidepanel specifically).
       - Insert `<span id="fsb-owner-chip">` in sidepanel.html in the header.
    2. Run `node --test tests/owner-chip.test.js`; tests still GREEN (helpers tested in Task 2; this task adds a consumer).
    3. Sanity grep:
         grep -c "MY_SURFACE\|owned by\|shouldShowOwnerChip" extension/ui/sidepanel.js  # >= 2
         grep -c "fsb-owner-chip" extension/ui/sidepanel.html                            # >= 1
    4. Commit: `feat(243-03): UI-02 sidepanel owner-chip render + chrome.tabs.onActivated refresh`.
  </action>
  <verify>
    <automated>node --test tests/owner-chip.test.js</automated>
  </verify>
  <done>
    sidepanel.js calls refreshOwnerChip on load and on chrome.tabs.onActivated. Chip is read-only. UI-02 closed (popup + sidepanel both render).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.session -> popup/sidepanel | Storage is per-extension-origin; only extension code reads. Trusted. |
| Registry -> overlay-state -> visual-feedback | In-process plumbing; no untrusted boundary crossed |
| visual-feedback DOM badge -> page | Page can read the rendered text via DOM but cannot influence the contents |
| getActiveTabOwner request param tabId | If used, must be validated as finite integer |

## STRIDE Threat Register (4 threats)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-03-01 | Information Disclosure | Badge / chip exposes full agent UUID to page DOM or user | mitigate | All renders go through formatAgentIdForDisplay (6 hex chars per agent-registry.js:42 SSOT). RESEARCH Anti-Pattern explicitly forbids local slice. shouldShowOwnerChip and buildChipText only consume the SHORT id; the helpers are test-gated. |
| T-243-03-02 | Stale ownership cache (popup chip says wrong owner) | Information Disclosure | mitigate | popup.js refreshes on load; sidepanel.js refreshes on chrome.tabs.onActivated. The chip ALWAYS reads the storage envelope (or registry) at fetch time — no caching layer. |
| T-243-03-03 | Elevation via chip click-to-switch | Elevation of Privilege | mitigate | Chip is implemented as a `<span>` with no click handler. RESEARCH Anti-Pattern + ASVS V4 audit explicitly forbid action affordance. Test asserts chip element is span (not button/a). Future "click to switch" deferred per CONTEXT. |
| T-243-03-04 | Tampering of overlay-state envelope from page | Tampering | accept | overlayState is built in extension SW; consumed in content script via runtime message. Pages cannot inject into runtime messages. visual-feedback.js attaches the badge in a closed shadow DOM (existing v0.9.36 pattern). Trust boundary already enforced. |
</threat_model>

<verification>
- `node --test tests/badge-agent-id.test.js` -> all GREEN.
- `node --test tests/owner-chip.test.js` -> all GREEN.
- `grep -c "agentIdShort" extension/utils/overlay-state.js` -> >= 1.
- `grep -c "agentIdShort" extension/content/visual-feedback.js` -> >= 1.
- `grep -c "formatAgentIdForDisplay" extension/utils/overlay-state.js` -> >= 1.
- `grep -c "owned by" extension/ui/popup.js extension/ui/sidepanel.js extension/ui/owner-chip.js` -> >= 1 (across files).
- `grep -c "fsb-owner-chip" extension/ui/popup.html extension/ui/sidepanel.html` -> >= 2.
- Manual UAT (deferred per VALIDATION): real-Chrome badge shows agent_id suffix; popup/sidepanel chip renders for foreign owner.
</verification>

<success_criteria>
- UI-01 closed: trusted-client badge appends `<clientLabel> / <agentIdShort>` (6 hex chars). overlay-state.js threads the field.
- UI-02 closed: popup AND sidepanel render `owned by <ownerAgentIdShort>` chip when active tab is owned by a foreign (non-self) agent. Read-only — no click handler. Chip hidden for self-owned legacy surfaces and for unowned tabs.
- agentIdShort uses formatAgentIdForDisplay everywhere — never sliced locally.
- Existing v0.9.36 overlay tests pass unchanged (`npm test`).
</success_criteria>

<output>
After completion, create `.planning/phases/243-background-tab-audit-ui-badge-integration/243-03-SUMMARY.md` documenting:
- Files modified (overlay-state.js + visual-feedback.js + popup.js + sidepanel.js + new owner-chip.js helper) + new tests (2)
- HTML changes (popup.html + sidepanel.html with chip span)
- Test results
- UI-01 + UI-02 closed
</output>
