---
phase: 243
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/ai/tool-definitions.cjs
  - extension/ws/mcp-tool-dispatcher.js
  - extension/ai/tool-executor.js
  - .planning/phases/243-background-tab-audit-ui-badge-integration/243-BACKGROUND-TAB-AUDIT.md
  - tests/foreground-audit.test.js
autonomous: true
requirements:
  - BG-01
  - BG-02
must_haves:
  truths:
    - "switch_tab is the ONLY tool with _forceForeground:true; all 49 other tools default false."
    - "handleSwitchTabRoute (mcp-tool-dispatcher.js) gates chrome.tabs.update({active:true}) AND chrome.windows.update({focused:true}) behind toolDef._forceForeground."
    - "tool-executor.js switch_tab case (autopilot path) gates the same two calls behind the same flag."
    - "243-BACKGROUND-TAB-AUDIT.md documents all 8 chrome.tabs.update({active:true}) sites with classification (in-tool-route vs out-of-scope)."
    - "243-BACKGROUND-TAB-AUDIT.md documents the setTimeout audit verdict: ZERO >=30s waits exist => no chrome.alarms migrations this phase. The 12 entries in the 1-30s band are listed as Phase 244 follow-ups with file:line and rationale."
  artifacts:
    - path: "extension/ai/tool-definitions.cjs"
      provides: "_forceForeground flag on every tool def; switch_tab=true; all others=false"
      contains: "_forceForeground"
    - path: "extension/ws/mcp-tool-dispatcher.js"
      provides: "Per-tool flag-gated foreground transition in handleSwitchTabRoute"
      contains: "_forceForeground"
    - path: "extension/ai/tool-executor.js"
      provides: "Per-tool flag-gated foreground transition in autopilot switch_tab dispatch"
      contains: "_forceForeground"
    - path: ".planning/phases/243-background-tab-audit-ui-badge-integration/243-BACKGROUND-TAB-AUDIT.md"
      provides: "Per-tool foreground audit table + setTimeout audit verdict + Phase 244 follow-up list"
      contains: "Phase 244"
    - path: "tests/foreground-audit.test.js"
      provides: "Grep-based invariant test: no chrome.tabs.update({active:true}) in MCP tool routes outside switch_tab; flag is read at the right sites."
  key_links:
    - from: "extension/ws/mcp-tool-dispatcher.js"
      to: "extension/ai/tool-definitions.cjs"
      via: "_mcp_getToolByName('switch_tab')._forceForeground"
      pattern: "_forceForeground"
    - from: "extension/ai/tool-executor.js"
      to: "extension/ai/tool-definitions.cjs"
      via: "tool def lookup -> _forceForeground"
      pattern: "_forceForeground"
---

<objective>
Audit the foreground-steal surface across MCP tool routes and the autopilot dispatcher; introduce a per-tool `_forceForeground` boolean in `tool-definitions.cjs` (default false; switch_tab=true) and gate the `chrome.tabs.update({active:true})` + `chrome.windows.update({focused:true})` calls in BOTH MCP and autopilot tool dispatch paths behind that flag. Produce `243-BACKGROUND-TAB-AUDIT.md` documenting (a) every chrome.tabs.update({active:true}) call site with classification and (b) the setTimeout >1s audit with the explicit verdict that ZERO >=30s waits exist this phase, listing the 12 entries in the 1-30s band as Phase 244 follow-ups.

Purpose: Background-tab agents must execute without stealing focus from another agent's tab or from the user. The per-tool flag is the documented contract per CONTEXT D-01. The setTimeout audit closes BG-03 with explicit "no migrations needed" rationale per CONTEXT D-02.
Output: tool-definitions.cjs flagged; 4 dispatcher call sites gated; audit doc artifact; foreground-audit test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/243-background-tab-audit-ui-badge-integration/243-CONTEXT.md
@.planning/phases/243-background-tab-audit-ui-badge-integration/243-RESEARCH.md
@.planning/phases/243-background-tab-audit-ui-badge-integration/243-VALIDATION.md

@extension/ai/tool-definitions.cjs
@extension/ws/mcp-tool-dispatcher.js
@extension/ai/tool-executor.js

<interfaces>
<!-- Key contracts the executor needs. Extracted from RESEARCH and direct file reads. -->

Tool registry shape (extension/ai/tool-definitions.cjs):
Each entry currently has: name, description, inputSchema, _route, _readOnly, _contentVerb, _cdpVerb.
This plan adds a fourth boolean discriminator `_forceForeground` (default false; switch_tab=true).

Switch_tab MCP route (mcp-tool-dispatcher.js around line 878-902):
- Line 890: tab = await chrome.tabs.update(params.tabId, { active: true });
- Line 895: await chrome.windows.update(tab.windowId, { focused: true });
Both must be gated behind: const toolDef = _mcp_getToolByName('switch_tab'); if (toolDef && toolDef._forceForeground === true) { ... }

Switch_tab autopilot route (tool-executor.js around line 240-280):
- Line 264: chrome.tabs.update(targetTabId, { active: true })
- Line 269: chrome.windows.update(tab.windowId, { focused: true })
Same gate; the autopilot side imports / requires tool-definitions.cjs already (verify import path).

Helper: _mcp_getToolByName exists in mcp-tool-dispatcher.js (check existing usage near line 100-150). If absent, use the autopilot's getToolDefinitionByName equivalent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 foreground-audit test scaffold + tool-definitions.cjs _forceForeground flag</name>
  <files>tests/foreground-audit.test.js, extension/ai/tool-definitions.cjs</files>
  <read_first>
    - extension/ai/tool-definitions.cjs (read full file; identify every tool def and the existing _route/_readOnly pattern)
    - extension/ws/mcp-tool-dispatcher.js lines 1-150 (look for _mcp_getToolByName helper) and lines 878-902 (handleSwitchTabRoute)
    - extension/ai/tool-executor.js lines 240-280 (switch_tab case)
    - tests/agent-cap-ui.test.js (existing plain-Node assert pattern from Phase 241)
  </read_first>
  <behavior>
    - Test 1 (RED): tool-definitions.cjs exports a tool list where switch_tab has `_forceForeground: true` and at least one other tool exists with `_forceForeground: false` (or undefined treated as false).
    - Test 2 (RED): A grep over extension/ai/tool-definitions.cjs counts exactly 1 occurrence of `_forceForeground:` followed by `true` (whitespace-tolerant).
    - Test 3 (RED): A regex scan over extension/ws/mcp-tool-dispatcher.js confirms `chrome.tabs.update(...{ active: true })` (or equivalent) appears ONLY inside a block guarded by `_forceForeground`. The test reads the file as text, locates each `chrome.tabs.update` line containing `active: true`, and asserts that within the prior 30 lines a `_forceForeground` reference exists.
    - Test 4 (RED): Same regex scan over extension/ai/tool-executor.js (switch_tab case): `chrome.tabs.update(... active: true ...)` must be inside a `_forceForeground` guard.
    - Test 5 (RED): Audit doc 243-BACKGROUND-TAB-AUDIT.md exists and contains the exact phrase "ZERO setTimeout >= 30s" (case-insensitive) and the exact phrase "Phase 244 follow-up".
  </behavior>
  <action>
    1. Create tests/foreground-audit.test.js using the plain-Node `assert` + `node --test` style established by tests/agent-cap-ui.test.js. Use `fs.readFileSync` to load the three target files; do NOT require()/import them (avoids loading Chrome-API dependencies). Use string/regex predicates for the 5 tests above.
    2. Run `node --test tests/foreground-audit.test.js`; tests MUST fail RED (audit doc and flag do not exist yet).
    3. Edit extension/ai/tool-definitions.cjs: walk every tool definition and add `_forceForeground: false` as the LAST property before the closing `}`. Then for the `switch_tab` entry only, set `_forceForeground: true`. Add a one-line JSDoc comment above switch_tab's _forceForeground line: `// Phase 243 BG-02: switch_tab's semantic intent IS focus transfer (D-01).`
    4. Re-run the test; Test 1 and Test 2 should now pass GREEN. Tests 3, 4, and 5 still fail (gating + audit doc come in subsequent tasks).
    5. Commit: `feat(243-01): add _forceForeground flag to tool-definitions.cjs (switch_tab=true, all others=false)`.
  </action>
  <verify>
    <automated>node --test tests/foreground-audit.test.js</automated>
  </verify>
  <done>
    Tests 1+2 GREEN; tests 3+4+5 still RED. tool-definitions.cjs has _forceForeground on every tool. grep `_forceForeground:\s*true` extension/ai/tool-definitions.cjs returns exactly 1 line.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate foreground transitions in MCP dispatcher AND autopilot tool-executor</name>
  <files>extension/ws/mcp-tool-dispatcher.js, extension/ai/tool-executor.js</files>
  <read_first>
    - extension/ws/mcp-tool-dispatcher.js lines 878-902 (handleSwitchTabRoute body) and confirm where _mcp_getToolByName is defined (or how tool-definitions.cjs is imported)
    - extension/ai/tool-executor.js lines 240-280 (the switch_tab case in the dispatcher switch)
    - 243-RESEARCH.md "Code Examples" Example 1 (the gate pattern)
  </read_first>
  <action>
    1. In extension/ws/mcp-tool-dispatcher.js, locate handleSwitchTabRoute (around line 878). Around line 890-895 the code currently performs:
         tab = await chrome.tabs.update(params.tabId, { active: true });
         if (chrome.windows?.update && tab?.windowId) { await chrome.windows.update(tab.windowId, { focused: true }); }
       Replace with:
         const toolDef = _mcp_getToolByName('switch_tab');
         const forceForeground = !!(toolDef && toolDef._forceForeground === true);
         let tab = await chrome.tabs.get(params.tabId);
         if (forceForeground) {
           tab = await chrome.tabs.update(params.tabId, { active: true });
           if (chrome.windows?.update && tab?.windowId) {
             await chrome.windows.update(tab.windowId, { focused: true });
           }
         }
       Preserve the surrounding sanitizeSingleTab return shape exactly (tabId, previousTabId).
       If _mcp_getToolByName is not present, use the existing import of tool-definitions (look for require('../ai/tool-definitions.cjs') or similar near the top of the file) and find by name array-iteration; add a small inline helper `function _mcp_getToolByName(n){return TOOL_DEFS.find(t => t.name === n);}` if needed. The flag SSOT is the tool def — never hardcode a list of names in dispatcher logic.
    2. In extension/ai/tool-executor.js, locate the `case 'switch_tab':` block (around line 254-275). Apply the EXACT same pattern: read tool def, gate the `chrome.tabs.update({ active: true })` + `chrome.windows.update({ focused: true })` calls behind `_forceForeground`. The non-foreground branch should still resolve the tab via `chrome.tabs.get(targetTabId)` and return the same shape.
    3. Re-run `node --test tests/foreground-audit.test.js` — tests 3 and 4 must now pass GREEN.
    4. Sanity grep:
         grep -c "_forceForeground" extension/ai/tool-definitions.cjs   # expect >= 50 (every tool)
         grep -c "_forceForeground" extension/ws/mcp-tool-dispatcher.js # expect >= 1
         grep -c "_forceForeground" extension/ai/tool-executor.js       # expect >= 1
    5. Commit: `feat(243-01): gate switch_tab foreground transition behind _forceForeground in dispatcher + tool-executor`.
  </action>
  <verify>
    <automated>node --test tests/foreground-audit.test.js</automated>
  </verify>
  <done>
    Tests 1-4 GREEN; test 5 still RED (audit doc next task). grep counts above all satisfied. Manual code review confirms NO unguarded chrome.tabs.update({active:true}) in either of the two gated files.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author 243-BACKGROUND-TAB-AUDIT.md and close foreground-audit.test.js</name>
  <files>.planning/phases/243-background-tab-audit-ui-badge-integration/243-BACKGROUND-TAB-AUDIT.md</files>
  <read_first>
    - 243-RESEARCH.md sections "Audit: chrome.tabs.update({active:true}) call sites" (the 11-row table) and "Audit: setTimeout > 1s call sites" (the per-file delay tables) — the audit doc transcribes these verbatim with verdicts.
  </read_first>
  <action>
    Create `.planning/phases/243-background-tab-audit-ui-badge-integration/243-BACKGROUND-TAB-AUDIT.md` with the following sections (no emojis; markdown headings; tables in pipe-syntax):

    ## Section 1: Foreground side-effect audit (BG-01)
    - Header: "Audited 25+ MCP/autopilot tools for chrome.tabs.update({ active: true }) and chrome.windows.update({ focused: true })."
    - Table columns: # | File | Line | Context | MCP tool? | Verdict
    - Transcribe all 11 rows from RESEARCH "Audit: chrome.tabs.update({active:true}) call sites" table.
    - Per-tool verdict block:
      - "switch_tab is the only tool with `_forceForeground: true` (D-01). All other 49 tools default `_forceForeground: false`."
      - "open_tab: already background-safe (honors params.active per tool-executor.js); _forceForeground stays false. The MCP-route open_tab does not call chrome.tabs.update({active:true}) after creation."
      - "Out-of-scope sites (rows 5-11 in the audit table): BFCache recovery, smart-tab routing inside handleStartAutomation, and legacy popup/sidepanel UI handlers. These are NOT MCP tool routes and do not impact background-agent foreground stability."

    ## Section 2: setTimeout audit (BG-03)
    - Header: "Audited setTimeout call sites in mcp-tool-dispatcher.js, agent-loop.js, background.js, content/lifecycle.js."
    - Verdict (verbatim): "ZERO setTimeout >= 30s exist across the audited files. Per CONTEXT D-02 (chrome.alarms 30s floor), this phase performs no chrome.alarms migrations."
    - Table columns: File | Line | Delay | Purpose | Band | Phase 244 follow-up?
    - Transcribe ALL rows from RESEARCH (mcp-tool-dispatcher.js: 547, 570; agent-loop.js: 1357, 1826, 2421, 2493, 2503; background.js: 1658, 1710, 1782, 2786, 2842, 3047, 3137, 3325, 6404, 7221, 7255, 7293, 8494, 8956, 9045, 9844, 9861, 9907, 9923, 11605, 11628, 12352, 12361; content/lifecycle.js: 314, 395, 443, 580, 682, 692). Mark the 12 entries flagged "1-30s" in RESEARCH as `Phase 244 follow-up: yes`. Mark all sub-1s entries as `no`.

    ## Section 3: Phase 244 follow-up list
    Enumerate the 12 entries (file:line + delay + brief reason) explicitly so Phase 244 hardening picks them up directly. Examples: `extension/background.js:6404 (10000ms navigation watchdog) — flag for Phase 244`, etc. Use the explicit list from RESEARCH "Migration verdict per BG-03 + CONTEXT D-02".

    ## Section 4: Verdict
    "BG-01 closed: 100% of 25+ tools audited; only switch_tab opts in to _forceForeground. BG-02 closed: per-tool flag wired in tool-definitions.cjs; gated in mcp-tool-dispatcher.js handleSwitchTabRoute and tool-executor.js switch_tab case. BG-03 closed: ZERO setTimeout >= 30s; no chrome.alarms migrations needed this phase. The 12 1-30s entries are tracked as Phase 244 follow-ups."

    The doc must contain literally the strings "ZERO setTimeout >= 30s" and "Phase 244 follow-up" so foreground-audit.test.js Test 5 passes.

    Run `node --test tests/foreground-audit.test.js`; ALL 5 tests must now pass GREEN.

    Commit: `docs(243-01): author 243-BACKGROUND-TAB-AUDIT.md (BG-01/BG-02/BG-03 closure)`.
  </action>
  <verify>
    <automated>node --test tests/foreground-audit.test.js</automated>
  </verify>
  <done>
    All 5 foreground-audit.test.js tests GREEN. Audit doc contains both literal phrases. Section 3 lists the 12 Phase-244 follow-ups by file:line. BG-01, BG-02, BG-03 closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> dispatcher | tool params arrive untrusted; switch_tab params.tabId already validated as finite by Phase 240 |
| Tool registry -> dispatcher | tool def is source-controlled; flag is read but never written at runtime |
| Autopilot loop -> tool-executor | autopilot dispatches by name; same flag SSOT |
| Audit doc -> reader | doc is plain markdown; no code path reads it |

## STRIDE Threat Register (4 threats)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-01-01 | Tampering | tool-definitions.cjs `_forceForeground` flip | accept | Tool registry is source-controlled JS; no runtime mutation surface exists. The flag is read at dispatch time but never written. Code review on PR is the existing mitigation. |
| T-243-01-02 | Elevation of Privilege | A future tool sets `active: true` outside the flag (foreground bypass) | mitigate | tests/foreground-audit.test.js scans mcp-tool-dispatcher.js and tool-executor.js for any `chrome.tabs.update(...active: true)` call NOT preceded within 30 lines by `_forceForeground`. CI-grade gate: any new tool that needs focus must add the flag. |
| T-243-01-03 | Information Disclosure | Audit doc leaks an internal file:line that hints at exploitable code path | accept | Audit doc lists file:line of pre-existing public-codebase calls; no secrets; same data already in research artifacts. |
| T-243-01-04 | Denial of Service | A flag-flipped switch_tab in autopilot loops infinitely re-stealing focus from a foreign agent | mitigate | switch_tab is the ONLY tool with the flag; the dispatch gate (Phase 240) already rejects cross-agent tab access. The flag does not bypass ownership; it only controls the focus side-effect on tabs the caller already owns. |
</threat_model>

<verification>
- `node --test tests/foreground-audit.test.js` -> all 5 tests GREEN.
- `grep -c "_forceForeground:\s*true" extension/ai/tool-definitions.cjs` -> exactly 1.
- `grep -c "_forceForeground" extension/ai/tool-definitions.cjs` -> >= 50 (every tool flagged).
- `grep -c "_forceForeground" extension/ws/mcp-tool-dispatcher.js` -> >= 1.
- `grep -c "_forceForeground" extension/ai/tool-executor.js` -> >= 1.
- Audit doc grep: `grep -c "ZERO setTimeout >= 30s" .planning/phases/243-background-tab-audit-ui-badge-integration/243-BACKGROUND-TAB-AUDIT.md` -> >= 1.
- Audit doc grep: `grep -c "Phase 244 follow-up" .planning/phases/243-background-tab-audit-ui-badge-integration/243-BACKGROUND-TAB-AUDIT.md` -> >= 1.
</verification>

<success_criteria>
- BG-01 closed: every chrome.tabs.update({active:true}) site documented and classified in 243-BACKGROUND-TAB-AUDIT.md.
- BG-02 closed: _forceForeground flag exists on every tool def in tool-definitions.cjs (switch_tab=true, all others=false). Both MCP dispatcher and autopilot tool-executor honor the flag at the switch_tab call site.
- BG-03 closed: setTimeout audit explicitly records ZERO >=30s waits; the 12 entries in the 1-30s band are listed as Phase 244 follow-ups by file:line. No chrome.alarms migration occurs in this phase (per D-02).
- foreground-audit.test.js GREEN.
- No regression: existing v0.9.36 / 240 / 241 / 242 tests pass unchanged (verify with `npm test`).
</success_criteria>

<output>
After completion, create `.planning/phases/243-background-tab-audit-ui-badge-integration/243-01-SUMMARY.md` documenting:
- Files modified (4) + new test (1) + new audit doc (1)
- Foreground audit verdict (8 sites total; 4 in MCP routes gated; 7 out of scope)
- setTimeout verdict (ZERO >=30s; 12 Phase 244 follow-ups)
- Test results (foreground-audit.test.js 5/5 GREEN)
- BG-01, BG-02, BG-03 closed
</output>
