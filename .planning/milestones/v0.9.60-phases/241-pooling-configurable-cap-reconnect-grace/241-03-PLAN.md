---
phase: 241-pooling-configurable-cap-reconnect-grace
plan: 03
type: execute
wave: 2
depends_on:
  - 241-01-PLAN.md
files_modified:
  - extension/ui/control_panel.html
  - extension/ui/options.js
  - tests/agent-cap-ui.test.js
  - tests/agent-no-idle.test.js
autonomous: true
requirements:
  - POOL-05
  - POOL-06
  - LOCK-04
tags:
  - settings-ui
  - control-panel
  - cap-input
  - no-idle-timeout
must_haves:
  truths:
    - "control_panel.html Advanced Settings section renders an Agent Concurrency card with a numeric input (id=fsbAgentCap, min=1, max=64, step=1, default=8) and a Reset-to-default button (id=fsbAgentCapReset)."
    - "options.js defaultSettings includes fsbAgentCap: 8."
    - "On input change, the value is clamped to integer 1-64; non-numeric reverts to 8; values outside range visually snap back."
    - "On save, the cap value is written to chrome.storage.local under fsbAgentCap (registry's onChanged listener picks it up)."
    - "On load, the cap value is read from chrome.storage.local and reflected in the input + display."
    - "Reset-to-default button restores the input to 8."
    - "No idle timeout exists (LOCK-04 negative): grep across registry + background confirms no setInterval-based agent reaper."
  artifacts:
    - path: "extension/ui/control_panel.html"
      provides: "Agent Concurrency settings card in Advanced Settings section."
      contains: "fsbAgentCap"
    - path: "extension/ui/options.js"
      provides: "defaultSettings.fsbAgentCap=8; cacheElements / setupEventListeners / loadSettings / saveSettings additions; clamping logic."
    - path: "tests/agent-cap-ui.test.js"
      provides: "DOM-stub assertions for the new input + reset button + clamping + chrome.storage.local round-trip."
    - path: "tests/agent-no-idle.test.js"
      provides: "Negative test: no setInterval / setTimeout-based idle reaper exists in registry / background."
  key_links:
    - from: "Agent Concurrency input change"
      to: "_clampCap (1-64 integer)"
      via: "input event listener in options.js"
      pattern: "fsbAgentCap"
    - from: "saveSettings()"
      to: "chrome.storage.local.set({ fsbAgentCap: <value> })"
      via: "existing settings persistence pattern at options.js:806"
      pattern: "fsbAgentCap"
    - from: "chrome.storage.local fsbAgentCap change"
      to: "SW _cachedCap (Plan 01 onChanged listener)"
      via: "chrome.storage.onChanged broadcast"
      pattern: "fsbAgentCap"
---

<objective>
Add the user-facing concurrency cap setting to FSB's options page, and prove the LOCK-04 negative invariant (no idle timeout exists).

This plan ships:
1. **POOL-05 / POOL-06 / UI surface (D-05):** A new "Agent Concurrency" settings card in `extension/ui/control_panel.html` Advanced Settings section, mirroring the existing Element Cache Size pattern. Numeric input (range 1-64, default 8, integer-validated, clamping in real-time), Reset-to-default button, helper text explaining grandfather behavior.
2. **POOL-05 settings persistence (D-05):** `extension/ui/options.js` extensions — `defaultSettings.fsbAgentCap = 8`; `cacheElements()` adds the new DOM refs; `setupEventListeners()` attaches input + reset handlers with clamping; `loadSettings()` populates the input from chrome.storage.local; `saveSettings()` writes the cap to chrome.storage.local.
3. **LOCK-04 negative assertion:** A new `tests/agent-no-idle.test.js` proves no `setInterval`-based idle reaper or scheduled-cleanup timer was added by Phase 241 to the registry or background.js. This is the codified negative invariant per CONTEXT.md D-13.
4. **POOL-06 input validation (D-05/D-06):** Real-time clamping (out-of-range values visually snap back); integer-only enforcement (decimals floored); reset-to-default button restores 8; no inline-error UI required (clamping is the "rejection" mechanism — values outside the range can never enter the saved settings).

Purpose: Wave 2 UI plan. Parallel with Plan 02 — zero file overlap (Plan 02 touches background.js + bridge + dispatcher + agent-scope.ts; Plan 03 touches control_panel.html + options.js). The registry's onChanged listener (Plan 01) automatically picks up the new cap value when this UI saves it; no further wiring needed between Plan 03 and Plan 01 beyond the storage key contract (`fsbAgentCap`).

Output:
- Modified `extension/ui/control_panel.html` (new card insertion in Advanced Settings).
- Modified `extension/ui/options.js` (defaultSettings, cacheElements, setupEventListeners, loadSettings, saveSettings extensions).
- New `tests/agent-cap-ui.test.js` (Wave 0 scaffold filled in with DOM-stub assertions).
- New `tests/agent-no-idle.test.js` (Wave 0 scaffold filled in with negative-grep + behavioral proof).

Decisions implemented (per CONTEXT.md):
- D-05 (configurable cap in Advanced Settings — numeric input, range 1-64, integer-validated, default 8, reset-to-default; chrome.storage.local key `fsbAgentCap`).
- D-06 (lower-cap grandfather behavior — UI helper text explains; no UI eviction logic).
- D-13 (no idle timeout — codified as a negative test).

Open Questions: NONE for this plan. Q1 / Q2 / Q3 are all resolved in Plan 01 / Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/241-pooling-configurable-cap-reconnect-grace/241-CONTEXT.md
@.planning/phases/241-pooling-configurable-cap-reconnect-grace/241-RESEARCH.md
@.planning/phases/241-pooling-configurable-cap-reconnect-grace/241-VALIDATION.md
@extension/ui/control_panel.html
@extension/ui/options.js

<interfaces>
<!-- Existing UI conventions (from RESEARCH §Pattern 6 + reading control_panel.html / options.js) -->

control_panel.html structure:
  - Advanced Settings section starts at line 333 with heading.
  - Each setting is a `<div class="settings-card">` with `.settings-card-header`, `.settings-card-icon`, `.settings-card-title`, `.settings-card-content`.
  - Existing cards: Automation Limits (line 343), DOM Analysis (line 367), Performance (line 410), Element Cache Size (line 388-405) — Element Cache Size is the closest pattern parity (preset dropdown + custom number input + helper text).
  - The new Agent Concurrency card slots between DOM Analysis (line 367-407) and Performance (line 410), mirroring the Element Cache Size pattern.

options.js structure (from RESEARCH §Pattern 6):
  - `defaultSettings` object near top of file (line ~4) — add `fsbAgentCap: 8`.
  - `cacheElements()` (line ~138) — add `elements.fsbAgentCap`, `elements.fsbAgentCapDisplay`, `elements.fsbAgentCapReset`.
  - `setupEventListeners()` (line ~236) — add input change listener with clamping; add reset button click handler.
  - `loadSettings()` (line ~672) — populate input from `data.fsbAgentCap`.
  - `saveSettings()` (line ~806) — include `fsbAgentCap: parseInt(elements.fsbAgentCap.value, 10) || 8` in the saved object.
  - `markUnsavedChanges()` is the existing unsaved-state helper.

chrome.storage contract (cross-plan):
  - Plan 01's _subscribeToCapChanges() listens for changes to `fsbAgentCap` in area `'local'` and updates SW _cachedCap.
  - Plan 03 writes to chrome.storage.local under exact key `fsbAgentCap`.
  - Both plans clamp to 1-64 integer (defense in depth).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agent Concurrency settings card HTML + options.js extensions (defaultSettings, cacheElements, setupEventListeners, loadSettings, saveSettings)</name>
  <files>extension/ui/control_panel.html, extension/ui/options.js, tests/agent-cap-ui.test.js</files>
  <read_first>
    1. Read extension/ui/control_panel.html lines 333-565 IN FULL — find the exact insertion point between DOM Analysis card (ends ~line 407) and Performance card (starts ~line 410). Confirm the `<div class="advanced-settings-grid">` wrapper. Identify the Element Cache Size card markup (lines 388-405 per RESEARCH) and use it as the visual template.
    2. Read extension/ui/options.js IN FULL or in chunks (lines 1-80, 130-300, 670-850). Identify defaultSettings, cacheElements, setupEventListeners, loadSettings, saveSettings, markUnsavedChanges. Confirm RESEARCH §Pattern 6 line-number recommendations match current code; if drift exists, locate the equivalent block by structure.
    3. Read tests/agent-cap-ui.test.js Wave 0 scaffold — identify any DOM-stub helper (likely jsdom or a manual document mock; if neither, build a minimal `globalThis.document` shim).
    4. Confirm chrome-mock helper from existing test files is reusable for chrome.storage.local.
  </read_first>
  <behavior>
    Test 1 (agent-cap-ui.test.js — DOM card present): Load control_panel.html as a string; assert it contains:
      - `id="fsbAgentCap"` (input)
      - `id="fsbAgentCapDisplay"` (value display)
      - `id="fsbAgentCapReset"` (reset button)
      - `min="1"` and `max="64"` and `step="1"` on the input
      - `value="8"` default on the input
      - The text "Agent Concurrency" in a card heading
      - Helper text containing "1-64" or "1 to 64" or similar range descriptor
      - Helper text mentioning the grandfather behavior (e.g., "active agents", "no eviction" or similar wording)
    Test 2 (agent-cap-ui.test.js — defaultSettings): Load options.js (or import defaultSettings if exported); assert `defaultSettings.fsbAgentCap === 8`.
    Test 3 (agent-cap-ui.test.js — clamping on input): Build a DOM stub. Set input.value = '100'; fire 'input' event. Assert input.value === '64' (clamped down).
    Test 4 (agent-cap-ui.test.js — clamping on input low): Set input.value = '0'; fire 'input'. Assert input.value === '1'.
    Test 5 (agent-cap-ui.test.js — non-integer floor): Set input.value = '3.7'; fire 'input'. Assert input.value === '3' (floored).
    Test 6 (agent-cap-ui.test.js — non-numeric default): Set input.value = 'foo'; fire 'input'. Assert input.value === '8' (reverted to default — parseInt(NaN) -> 8).
    Test 7 (agent-cap-ui.test.js — reset button): Set input.value = '20'; click the reset button. Assert input.value === '8'; assert markUnsavedChanges was called (mock spy).
    Test 8 (agent-cap-ui.test.js — saveSettings persists fsbAgentCap): Mock chrome.storage.local.set spy; set input.value='12'; invoke saveSettings(); assert spy received an object containing `fsbAgentCap: 12`.
    Test 9 (agent-cap-ui.test.js — loadSettings populates input): Mock chrome.storage.local.get to return `{ fsbAgentCap: 24 }`; invoke loadSettings(); assert input.value === '24' and display text === '24'.
    Test 10 (agent-cap-ui.test.js — display element updates on input): On input event firing with valid value, the display element's textContent reflects the new value.
  </behavior>
  <action>
    A. In extension/ui/control_panel.html, add a new settings card BETWEEN the existing DOM Analysis card (ends ~line 407) and Performance card (starts ~line 410). Use the Element Cache Size visual template (the closest existing pattern with a numeric input + reset). Verbatim from RESEARCH §Pattern 6:
       ```html
       <!-- Phase 241 D-05 / POOL-05: Agent Concurrency cap (range 1-64, default 8) -->
       <div class="settings-card">
         <div class="settings-card-header">
           <div class="settings-card-icon">
             <i class="fas fa-users"></i>
           </div>
           <div class="settings-card-title">
             <h3>Agent Concurrency</h3>
             <p>Maximum simultaneous agents</p>
           </div>
         </div>
         <div class="settings-card-content">
           <div class="setting-item">
             <div class="setting-label">
               <span>Concurrency Cap</span>
               <span class="setting-value-display" id="fsbAgentCapDisplay">8</span>
             </div>
             <input type="number" id="fsbAgentCap" class="form-input"
                    min="1" max="64" step="1" value="8">
             <button type="button" class="form-secondary-btn" id="fsbAgentCapReset">
               Reset to default (8)
             </button>
             <div class="setting-hint">
               Default 8. Range 1 to 64. Lowering this value while agents are active does not evict them; new claims past the new cap are rejected.
             </div>
           </div>
         </div>
       </div>
       ```
       (NO emojis. NO Font Awesome icon names that include emoji unicode.)

    B. In extension/ui/options.js:

       (i) Find `defaultSettings` (~line 4) and add `fsbAgentCap: 8,` adjacent to other settings. Sort alphabetically or follow existing convention (likely simply appended).

       (ii) Find `cacheElements()` (~line 138) and add:
       ```
       elements.fsbAgentCap = document.getElementById('fsbAgentCap');
       elements.fsbAgentCapDisplay = document.getElementById('fsbAgentCapDisplay');
       elements.fsbAgentCapReset = document.getElementById('fsbAgentCapReset');
       ```

       (iii) Find `setupEventListeners()` (~line 236) and add the input + reset handlers per RESEARCH §Pattern 6:
       ```
       if (elements.fsbAgentCap) {
         elements.fsbAgentCap.addEventListener('input', (e) => {
           var raw = parseInt(e.target.value, 10);
           if (!Number.isFinite(raw)) raw = 8;
           if (raw < 1) raw = 1;
           if (raw > 64) raw = 64;
           if (e.target.value !== String(raw)) e.target.value = String(raw);
           if (elements.fsbAgentCapDisplay) elements.fsbAgentCapDisplay.textContent = String(raw);
           markUnsavedChanges();
         });
       }
       if (elements.fsbAgentCapReset) {
         elements.fsbAgentCapReset.addEventListener('click', () => {
           if (elements.fsbAgentCap) elements.fsbAgentCap.value = '8';
           if (elements.fsbAgentCapDisplay) elements.fsbAgentCapDisplay.textContent = '8';
           markUnsavedChanges();
         });
       }
       ```

       (iv) Find `loadSettings()` (~line 672) and add (in the data-handler region):
       ```
       if (elements.fsbAgentCap) {
         var capValue = (typeof data.fsbAgentCap === 'number' && Number.isFinite(data.fsbAgentCap)) ? data.fsbAgentCap : 8;
         if (capValue < 1) capValue = 1;
         if (capValue > 64) capValue = 64;
         capValue = Math.floor(capValue);
         elements.fsbAgentCap.value = String(capValue);
         if (elements.fsbAgentCapDisplay) elements.fsbAgentCapDisplay.textContent = String(capValue);
       }
       ```

       (v) Find `saveSettings()` (~line 806) and in the settings object literal that gets passed to chrome.storage.local.set, add:
       ```
       fsbAgentCap: parseInt(elements.fsbAgentCap?.value, 10) || 8,
       ```
       (clamping is also applied SW-side via Plan 01's setCap on storage.onChanged — defense in depth).

    C. Implement tests/agent-cap-ui.test.js — 10 tests above. Use a minimal manual DOM stub (no jsdom dependency to avoid new packages). Stub pattern:
       ```
       function makeInputStub(initialValue) {
         var listeners = {};
         var value = initialValue;
         return {
           get value() { return value; },
           set value(v) { value = v; },
           addEventListener: (evt, handler) => { listeners[evt] = (listeners[evt] || []).concat(handler); },
           dispatchEvent: (evt) => { (listeners[evt.type] || []).forEach(h => h({ target: this })); }
         };
       }
       ```
       For the HTML-presence tests (Test 1), use `fs.readFileSync('extension/ui/control_panel.html', 'utf8')` and string-includes assertions.

    D. Per CLAUDE.md: NO EMOJIS in HTML, JS, or test output. Use Font Awesome class `fas fa-users` (no emoji unicode). Comments use plain ASCII.

    Per CONTEXT.md D-05 (range 1-64, default 8, integer-validated, reset-to-default), D-06 (helper text explains grandfather), D-13 (this UI does NOT add any idle timer — the LOCK-04 negative is enforced by Task 2's grep test).
  </action>
  <verify>
    <automated>node tests/agent-cap-ui.test.js</automated>
  </verify>
  <done>
    - tests/agent-cap-ui.test.js exits 0 with PASS (10 cases).
    - `grep -c "fsbAgentCap" extension/ui/control_panel.html` >= 1.
    - `grep -c "fsbAgentCap" extension/ui/options.js` >= 4 (defaultSettings + cacheElements + setupEventListeners input + loadSettings + saveSettings, 5 sites — >= 4 to be conservative).
    - `grep -c "fsbAgentCapReset" extension/ui/options.js` >= 1.
    - `grep -c "Agent Concurrency" extension/ui/control_panel.html` >= 1.
    - `grep -ci "[\xF0-\xFF]" extension/ui/control_panel.html` === 0 (no emoji unicode).
    - `grep -ci "[\xF0-\xFF]" extension/ui/options.js` === 0 (no emoji unicode in additions).
    - No new npm dependencies. No jsdom install.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/agent-no-idle.test.js — LOCK-04 negative invariant (no idle reaper exists in registry or background.js)</name>
  <files>tests/agent-no-idle.test.js</files>
  <read_first>
    1. Read tests/agent-no-idle.test.js Wave 0 scaffold.
    2. Confirm by grep that the registry (post-Plan 01) does NOT contain any setInterval call: search extension/utils/agent-registry.js for `setInterval`.
    3. Confirm background.js does not contain a NEW setInterval added by Phase 241. Existing setInterval calls in background.js (if any from earlier phases — analytics, heartbeats) are NOT idle reapers; we only assert no Phase-241-added one exists. The simplest negative is: agent-registry.js contains zero setInterval references.
    4. Confirm RECONNECT_GRACE_MS is the ONLY setTimeout-driven scheduling in agent-registry.js, and it's keyed by connection_id (LOCK-02), not idle.
  </read_first>
  <behavior>
    Test 1 (agent-no-idle.test.js — registry source has no setInterval): Read agent-registry.js as a string. Assert `source.indexOf('setInterval') === -1`. (Pure source-grep negative; defends against drift.)
    Test 2 (agent-no-idle.test.js — registry source has no chrome.alarms.create): Read agent-registry.js. Assert `source.indexOf('chrome.alarms') === -1`. (RESEARCH-confirmed: chrome.alarms 30s floor blocks the 10s grace; verifies setTimeout-only path.)
    Test 3 (agent-no-idle.test.js — registry source has no `idle` keyword in scheduling context): Read agent-registry.js. Assert there is no string `idleTimeout` or `idleReaper` or `reapIdle` (case-insensitive). The deferred-ideas section of CONTEXT.md explicitly forbids these.
    Test 4 (agent-no-idle.test.js — behavioral: agent persists across simulated quiet): Register agent A; bindTab(A, 100). Wait 200ms (a "quiet" period). Assert A still in _agents. Assert _tabsByAgent.get(A).has(100). This proves no idle reaper kicks in within 200ms; while not exhaustive of all timeouts, combined with Test 1+2+3 it covers LOCK-04.
    Test 5 (agent-no-idle.test.js — only RECONNECT_GRACE_MS scheduling exists): Read agent-registry.js source. Confirm the ONLY setTimeout call site in the registry is inside `stageReleaseByConnectionId` and `hydrate` (both keyed by connection_id, NOT by idle). Use a regex match to count setTimeout occurrences and assert <= 2 (one for stage, one for hydrate-time recovery).
  </behavior>
  <action>
    A. Implement tests/agent-no-idle.test.js per behavior above. Each test uses `fs.readFileSync` to load `extension/utils/agent-registry.js` as a string, then asserts via plain `assert.strictEqual`.

    B. Test 4 (behavioral) uses the AgentRegistry instance:
       ```
       const { AgentRegistry } = require('../extension/utils/agent-registry.js');
       const reg = new AgentRegistry();
       const { agentId } = await reg.registerAgent();
       await reg.bindTab(agentId, 100);
       await new Promise(r => setTimeout(r, 200));
       assert.ok(reg._agents.has(agentId), 'agent still present after quiet period (LOCK-04)');
       ```

    C. Test 5 uses regex:
       ```
       const matches = source.match(/setTimeout\s*\(/g) || [];
       assert.ok(matches.length <= 2, 'expected at most 2 setTimeout call sites (stage + hydrate); found ' + matches.length);
       ```

    D. End with `console.log('PASS no-idle (LOCK-04)');`. Per CLAUDE.md: NO EMOJIS in test output.

    Per CONTEXT.md D-13 (no idle timeout) — this test codifies the negative. Per RESEARCH §Pitfall 1 — chrome.alarms can't be used for sub-30s grace; setTimeout is the only mechanism, and ONLY for connection_id-keyed grace, not for idle reaping.
  </action>
  <verify>
    <automated>node tests/agent-no-idle.test.js</automated>
  </verify>
  <done>
    - tests/agent-no-idle.test.js exits 0 with PASS.
    - Test asserts grep-level negatives that will fail loudly if a future change accidentally re-introduces idle reaping.
    - No emojis in test output.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (cap numeric field) -> chrome.storage.local | User-controlled value enters via the options page |
| chrome.storage.local -> SW _cachedCap | Plan 01's onChanged listener consumes; clamping applied on read AND write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-241-05 | Tampering / Input validation | Cap UI misconfiguration (cap=0 disables service; cap=99999 cripples) | mitigate | type="number" + min=1 + max=64 HTML enforcement; JS clamp on input event; JS clamp on saveSettings; SW clamp on storage.onChanged (Plan 01); no path admits out-of-range value |
| T-241-11 | Tampering | Direct chrome.storage.local edit (DevTools / extension override) | mitigate | Plan 01's setCap re-clamps on every storage.onChanged event; corrupted values normalized to default 8 |
| T-241-12 | XSS via numeric input display | Cap UI display element | mitigate | Display element uses textContent (not innerHTML); type="number" enforces non-script content; values always pass through parseInt |
| T-241-13 | DoS / Resource leak | Future drift adding idle reaper that silently kills agents | mitigate | Test file tests/agent-no-idle.test.js asserts grep-level negatives — any future PR adding setInterval / 'idle' keyword to registry breaks the build |
</threat_model>

<verification>
After both tasks complete:

```
node tests/agent-cap-ui.test.js \
  && node tests/agent-no-idle.test.js \
  && node tests/agent-cap.test.js \
  && node tests/agent-cap-storage.test.js \
  && node tests/agent-pool-shrink.test.js \
  && node tests/agent-grace.test.js \
  && node tests/agent-pooling.test.js \
  && node tests/agent-registry.test.js \
  && node tests/mcp-tool-smoke.test.js
```

Grep gates (acceptance):
- `grep -c "fsbAgentCap" extension/ui/control_panel.html` >= 1
- `grep -c "fsbAgentCap" extension/ui/options.js` >= 4
- `grep -c "Agent Concurrency" extension/ui/control_panel.html` >= 1
- `grep -c "fsbAgentCapReset" extension/ui/options.js` >= 1
- Test file tests/agent-no-idle.test.js asserts `setInterval` count === 0 in agent-registry.js
- Test file tests/agent-no-idle.test.js asserts `chrome.alarms` count === 0 in agent-registry.js

Manual UAT (Phase 241 VALIDATION.md auto-deferred):
- options.html Advanced Settings cap input visible, range enforced, change persists across SW restart.
- Lower-cap-while-active grandfathering observed in real Chrome.
</verification>

<success_criteria>
- POOL-05 closure: cap configurable via UI; persisted to chrome.storage.local; range 1-64; integer-validated; default 8.
- POOL-06 closure: real-time clamping; reset-to-default works; non-integer values floored; non-numeric reverts to 8.
- LOCK-04 codified: negative test prevents future drift introducing an idle reaper.
- No emojis anywhere.
- No new npm dependencies.
- All Plan 01 + Plan 02 + prior-phase contract tests still pass UNCHANGED.
</success_criteria>

<output>
After completion, create `.planning/phases/241-pooling-configurable-cap-reconnect-grace/241-03-SUMMARY.md` documenting:
- New control_panel.html card markup and where it's placed.
- options.js extensions (defaultSettings, cacheElements, setupEventListeners, loadSettings, saveSettings).
- Clamping logic (input + saveSettings + Plan 01's setCap = three layers of defense).
- LOCK-04 negative test coverage (tests/agent-no-idle.test.js).
- Confirmation that Plan 03 has zero file overlap with Plan 02 (parallel-safe).
- Hand-off: end-to-end Phase 241 contract is now complete; Phase 244 hardening will add 5x manual UAT runs.
</output>
