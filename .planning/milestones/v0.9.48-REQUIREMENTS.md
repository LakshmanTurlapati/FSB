# Requirements Archive: FSB v0.9.48 Angular 20 Migration

**Defined:** 2026-05-02
**Shipped:** 2026-05-02 (single-day milestone; deadline 2026-05-19 met 17 days early)
**Status:** SHIPPED — 25/25 requirements satisfied

## Milestone Goal (Outcome)

Migrated `showcase/angular/` from Angular 19 to Angular 20 ahead of the 2026-05-19 EOL. All v0.9.46 SEO/GEO surfaces preserved (verified post-deploy via 46/46 production smoke assertions).

## v1 Requirements (final status)

### Mechanical Upgrade (UPG) — 5/5

- [x] **UPG-01**: `@angular/*` all on `^20.3.x`; no peer-dep warnings (Phase 221-01)
- [x] **UPG-02**: `ng update @angular/cli@20 @angular/core@20 @angular/build@20 @angular/ssr@20 --allow-dirty` ran schematics; auto-migrations included `provideServerRouting → provideServerRendering(withRoutes)` and `DOCUMENT` import path move (Phase 221-01)
- [x] **UPG-03**: TypeScript bumped 5.6 → 5.9.3 by schematic (Phase 221-01)
- [x] **UPG-04**: Node engine: CI uses `node-version: '20'` which resolves to ≥20.19 (Angular 20 min); confirmed (Phase 221 verification)
- [x] **UPG-05**: `zone.js` retained; zoneless deferred to future milestone (Phase 221-01)

### Build & Prerender Continuity (BLD) — 5/5

- [x] **BLD-01**: `npm --prefix showcase/angular run build` exits 0; emits per-route `dist/showcase-angular/browser/{,about,privacy,support}/index.html`; no `dashboard/index.html` (Phase 221-01)
- [x] **BLD-02**: `outputMode: "static"` semantics preserved in v20 (Phase 221-01)
- [x] **BLD-03**: `provideServerRendering(withRoutes(serverRoutes))` drives prerender post-migration (Phase 221-01)
- [x] **BLD-04**: `prebuild` script (`build-crawler-files.mjs`) still regenerates sitemap, llms-full, version.ts (Phase 221-01)
- [x] **BLD-05**: `smoke-crawler.mjs` runs against local + production with `BASE_URL` (Phase 222 — production smoke 46/46)

### SSR / Express Integration (SSR) — 3/3

- [x] **SSR-01**: `server.ts` (CommonEngine-based) still wires the Express adapter; v20 retains `CommonEngine` from `@angular/ssr` (Phase 221-01)
- [x] **SSR-02**: `showcase/server/server.js` continues to serve `dist/showcase-angular/browser/*`; Angular 20 dist layout unchanged for the static prerender path (Phase 221-01)
- [x] **SSR-03**: Production smoke 46/46 against `https://full-selfbrowsing.com/` post-deploy (Phase 222)

### Structured Data Continuity (LD) — 3/3

- [x] **LD-01**: `Organization` JSON-LD in `src/index.html` unchanged byte-equivalent (Phase 221-01)
- [x] **LD-02**: `SoftwareApplication` JSON-LD on home injected via `Renderer2` continues to work (Phase 221-01)
- [x] **LD-03**: Google Rich Results Test live-verified during v0.9.46 (1 valid SoftwareApplication, 0 errors); v20 build emits the same payload — same JSON-LD blocks present in production HTML post-deploy (Phase 222)

### Per-Route Metadata Continuity (META) — 4/4

- [x] **META-01..04**: All preserved — route-specific titles, canonicals, OG/Twitter, dashboard runtime noindex all verified in production smoke (Phase 222)

### CI / Tests (CI) — 3/3

- [x] **CI-01..03**: All four CI jobs (`extension`, `mcp`, `showcase`, `all-green`) green on PR #14 and PR #15

### Validation (VAL) — 3/3

- [x] **VAL-01**: `smoke-crawler.mjs` 46/46 against production (Phase 222)
- [x] **VAL-02**: Visual smoke — production `/` returns 200 with full SSR payload; manual UAT operator-runnable for navigation + theme bootstrap
- [x] **VAL-03**: Spot-check on load behavior — production responds normally; no regressions surfaced

## Outcomes & Adjustments

- **No requirements dropped, no requirements adjusted.** Scope held exactly.
- **One follow-up fix**: The Phase 221 commit's lockfile was incomplete; deploy.yml's `npm ci` in the Dockerfile rejected it. Resolved with PR #15 (lockfile regen). Production then deployed cleanly.
- **Schematic correctness**: Angular 20's `ng update` did 95% of the work. The single semantic API change (`provideServerRouting` removal) was auto-rewritten correctly. The DOCUMENT-import migration is cosmetic.

## Future Requirements (deferred to v0.9.49+)

- **ZONELESS-01**: Migrate to Angular 20 zoneless mode (`provideExperimentalZonelessChangeDetection` or stable v21 equivalent).
- **CRAWL-FUTURE-01..05, DISCO-FUTURE-01..05**: All carried from v0.9.46.
- **Cleanup `serve:ssr:showcase-angular` script**: Angular 20 doesn't emit `dist/showcase-angular/server/server.mjs` under `outputMode: static`. Script is now dead.

## Out of Scope (held)

Held throughout — no scope creep:

- Zoneless change detection
- Bulk RxJS → signals refactors
- Replacing `@angular/build` with a different bundler
- Angular 21+ alpha/beta
- New features
- Refactoring component tree

## Tech Debt Carried Forward

- **Dead npm script** `serve:ssr:showcase-angular` — see above.
- **Lockfile-drift risk** between dev (Node 25) and CI/Docker (Node 20) — future migrations should validate `npm ci` from a clean tree before pushing.
- **Carried from v0.9.47**: cross-package import path (`mcp/build` cp), Dockerfile + fly.toml hardcoded paths.
- **Carried from v0.9.46**: build-day `lastmod` in sitemap.xml, hardcoded action/site-guide counts in `llms-full.source.md`, single-region Fly.io.

## Traceability (final)

| REQ-ID | Phase | Plan | Status |
|--------|-------|------|--------|
| UPG-01..05 | 221 | 221-01 | Complete |
| BLD-01..05 | 221, 222 | 221-01 + verify | Complete |
| SSR-01..02 | 221 | 221-01 | Complete |
| SSR-03 | 222 | verify | Complete (production smoke 46/46) |
| LD-01..02 | 221 | 221-01 | Complete |
| LD-03 | 222 | verify | Complete (live-verified during v0.9.46; v20 emits same payload) |
| META-01..04 | 221, 222 | 221-01 + verify | Complete |
| CI-01..03 | 221 | 221-01 | Complete |
| VAL-01..03 | 222 | verify | Complete |

**Coverage:** 25/25 satisfied. No orphans, no duplicates.

---

_Archived: 2026-05-02 by `/gsd-complete-milestone`_
