---
phase: 35-notion-todo-workflow-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/dom-analysis.js
autonomous: true
requirements:
  - SNAP-01
  - SNAP-02

must_haves:
  truths:
    - "Every DOM snapshot includes scroll position %, hasMoreAbove, hasMoreBelow flags, and content-remaining estimate in the metadata header"
    - "All interactive elements visible in the current viewport are included in the snapshot, regardless of count"
    - "If 120 elements are in view, 120 are included; if 40, 40 are included -- no arbitrary cap on viewport elements"
  artifacts:
    - path: "content/dom-analysis.js"
      provides: "Enhanced scroll metadata and viewport-complete element inclusion"
      contains: "hasMoreAbove"
  key_links:
    - from: "content/dom-analysis.js:buildMarkdownSnapshot"
      to: "metadata header string"
      via: "scroll metadata calculation"
      pattern: "hasMoreAbove|hasMoreBelow|contentRemaining"
    - from: "content/dom-analysis.js:getFilteredElements"
      to: "element collection pipeline"
      via: "viewport-complete inclusion logic"
      pattern: "viewportElements\\.length"
---

<objective>
Add rich scroll metadata to every DOM snapshot and remove the arbitrary element cap so all viewport-visible interactive elements are included.

Purpose: The AI currently gets minimal scroll info (just scroll %) and an arbitrary 80-element cap that may exclude visible interactive elements. With scroll awareness flags, the AI can reason about whether to scroll. With viewport-complete inclusion, the AI sees everything it could actually interact with.

Output: Updated `buildMarkdownSnapshot` with enhanced metadata header and updated `getFilteredElements` with dynamic viewport-complete element inclusion.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@content/dom-analysis.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich scroll metadata header in buildMarkdownSnapshot</name>
  <files>content/dom-analysis.js</files>
  <action>
In `buildMarkdownSnapshot()` (line ~2574-2597), enhance the metadata header section:

1. After computing `scrollPct` (line ~2582), add these calculations:
   - `hasMoreAbove`: `scrollTop > 10` (boolean)
   - `hasMoreBelow`: `scrollTop + viewportHeight < pageHeight - 10` (boolean)
   - `contentAbove`: approximate lines/chars above viewport -- `Math.round(scrollTop / viewportHeight * 100)` as percentage
   - `contentBelow`: approximate content below -- `Math.round((pageHeight - scrollTop - viewportHeight) / viewportHeight * 100)` as percentage
   - `atTop`: `scrollTop <= 10`
   - `atBottom`: `scrollTop + viewportHeight >= pageHeight - 10`

2. Update the `metaHeader` string (line ~2585) to include the new flags. Change from:
   ```
   > URL: {url} | Scroll: {pct}% | Viewport: {w}x{h}
   ```
   To:
   ```
   > URL: {url} | Scroll: {pct}% | Viewport: {w}x{h} | Above: {hasMoreAbove} | Below: {hasMoreBelow}
   ```
   When hasMoreAbove is true, add `({contentAbove}% content above)`. When hasMoreBelow is true, add `({contentBelow}% content below)`. When atTop, show `atTop`. When atBottom, show `atBottom`.

   Example outputs:
   - `| Scroll: 0% | atTop | Below: 340% content below`
   - `| Scroll: 45% | Above: 180% content above | Below: 160% content below`
   - `| Scroll: 100% | Above: 440% content above | atBottom`

3. Keep the existing Focus: ref line intact.

Do NOT change the charBudget or any other buildMarkdownSnapshot logic. Only the metadata header.
  </action>
  <verify>
    <automated>grep -n "hasMoreAbove\|hasMoreBelow\|contentAbove\|contentBelow\|atTop\|atBottom" content/dom-analysis.js | head -20</automated>
  </verify>
  <done>Every snapshot metadata header includes scroll position with hasMoreAbove/hasMoreBelow flags and content-remaining percentages. AI can reason about scrolling based on these signals.</done>
</task>

<task type="auto">
  <name>Task 2: Implement viewport-complete element inclusion</name>
  <files>content/dom-analysis.js</files>
  <action>
In `getFilteredElements()` (line ~1774) and the viewport-first collection logic (line ~3086-3131):

1. **Remove the arbitrary viewport cap in buildMarkdownSnapshot's call to getFilteredElements.** In `buildMarkdownSnapshot()` (line ~2472-2493), the call passes `maxElements: 80`. Change the approach:
   - Remove the `maxElements = 80` default from `buildMarkdownSnapshot`. Instead, pass a new option `viewportComplete: true`.
   - When `viewportComplete: true`, getFilteredElements should include ALL viewport elements without slicing, then add a limited number of important offscreen elements (keep the existing offscreen budget logic, but cap offscreen at 30).

2. **In getFilteredElements (line ~1774)**, add the `viewportComplete` option:
   ```javascript
   const {
     maxElements = 50,
     prioritizeViewport = true,
     taskType = 'general',
     guideSelectors = null,
     viewportComplete = false
   } = options;
   ```

3. **In the viewport-first collection section (line ~3086-3131)**, when `viewportComplete` is true:
   - Include ALL viewport elements (no slicing): `elements = [...viewportElements]`
   - Add important offscreen elements capped at 30: `importantOffscreen = offscreenElements.filter(el => el.isButton || el.isInput || el.isLink || el.attributes?.['data-testid'] || el.attributes?.['aria-label'] || el.isCaptcha).slice(0, 30)`
   - Combine: `elements = [...viewportElements, ...importantOffscreen]`
   - When `viewportComplete` is false, preserve the existing maxElements-based logic exactly as-is.

4. **In buildMarkdownSnapshot** (line ~2488-2493), update the getFilteredElements call:
   ```javascript
   const elements = getFilteredElements({
     viewportComplete: true,
     prioritizeViewport: true,
     taskType: 'general',
     guideSelectors
   });
   ```
   Remove the `maxElements: 80` parameter from this call.

5. The charBudget truncation (line ~2631-2641) already handles long snapshots by truncating at the character budget. This is the safety valve -- even if 200 elements are in view, the snapshot will be truncated to charBudget. Do NOT add any new element cap.

6. Update the logger call in the viewport-first collection (line ~3115) to log `viewportComplete` mode.

IMPORTANT: Do NOT change the `maxElements` default of 50 in `getFilteredElements` -- that's used by other callers (like the legacy getDOMRepresentation). Only `buildMarkdownSnapshot` should use `viewportComplete: true`.
  </action>
  <verify>
    <automated>grep -n "viewportComplete" content/dom-analysis.js | head -10</automated>
  </verify>
  <done>buildMarkdownSnapshot includes all viewport-visible interactive elements without an arbitrary cap. The charBudget truncation serves as the natural limit. Other callers of getFilteredElements are unaffected.</done>
</task>

</tasks>

<verification>
1. Scroll metadata: grep for hasMoreAbove/hasMoreBelow in dom-analysis.js confirms the flags exist in the metadata header builder
2. Viewport-complete: grep for viewportComplete confirms the new option flows through getFilteredElements and buildMarkdownSnapshot
3. No regression: the maxElements=50 default is preserved for legacy callers; only buildMarkdownSnapshot uses viewportComplete
</verification>

<success_criteria>
- Snapshot metadata header contains scroll position %, hasMoreAbove/Below flags with content-remaining percentages
- buildMarkdownSnapshot includes all viewport interactive elements without arbitrary cap
- charBudget truncation is the only size limiter for snapshots
- Existing getFilteredElements callers using maxElements are unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/35-notion-todo-workflow-refinement/35-01-SUMMARY.md`
</output>
