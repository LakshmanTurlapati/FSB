---
phase: 34-memory-tab-theme-fix-export-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/options.css
  - ui/options.js
autonomous: true
requirements:
  - THEME-01

must_haves:
  truths:
    - "All Memory tab elements use CSS variables instead of hardcoded hex colors"
    - "In dark mode, no white/light backgrounds or unreadable text appear in Memory tab"
    - "In light mode, no dark backgrounds or unreadable text appear in Memory tab"
    - "Missing CSS variables (--surface-color, --card-bg, --hover-bg) are defined in both :root and dark theme"
  artifacts:
    - path: "ui/options.css"
      provides: "Theme-aware Memory tab styles"
      contains: "--surface-color"
    - path: "ui/options.js"
      provides: "Theme-aware inline styles in JS"
  key_links:
    - from: "ui/options.css"
      to: ":root and [data-theme=dark] blocks"
      via: "CSS custom properties"
      pattern: "--surface-color|--card-bg|--hover-bg"
---

<objective>
Fix all theme mismatches in Memory tab UI so every element respects the active theme.

Purpose: Phase 33 introduced new Memory tab elements (outcome badges, recon report, collapsible sections, task graph) that use hardcoded colors or undefined CSS variables, breaking dark/light mode.
Output: All Memory tab CSS uses defined CSS variables; inline styles in JS use CSS variables or var() fallbacks.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-memory-tab-theme-fix-export-import/34-CONTEXT.md

@ui/options.css
@ui/options.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define missing CSS variables and fix hardcoded colors in options.css</name>
  <files>ui/options.css</files>
  <action>
1. Add missing CSS variables to `:root` (around line 3-74) and `[data-theme="dark"]` (around line 77-103):
   - `:root` add: `--surface-color: var(--gray-50);` `--card-bg: var(--bg-primary);` `--hover-bg: var(--gray-100);`
   - `[data-theme="dark"]` add: `--surface-color: var(--gray-800);` `--card-bg: var(--gray-800);` `--hover-bg: var(--gray-700);`

2. Fix `.overflow-dropdown` (line ~3745): Change `background: var(--card-bg, #1a1a2e)` to `background: var(--card-bg)` (now defined). Change fallback border `#2a2a3e` to just `var(--border-color)`.

3. Fix `.overflow-item:hover` (line ~3773): Change `background: rgba(255, 255, 255, 0.05)` to `background: var(--hover-bg)`.

4. Fix `.memory-cost-card` (line ~3822): Same pattern as overflow-dropdown -- remove hardcoded fallbacks now that vars are defined.

5. Fix `.cost-label` (line ~3806): Change fallback `#a0a0b0` to just `var(--text-secondary)`.

6. Fix `.cost-value` and `.cost-card-header` (lines ~3814, 3835): Change fallback `#e0e0e0` to just `var(--text-primary)`.

7. Fix `.overflow-item` color (line ~3766): Change fallback `#e0e0e0` to just `var(--text-primary)`.

8. Verify all `.recon-*` and `.task-graph-*` and `.outcome-badge-*` styles use defined CSS variables (they already reference --border-color, --success-color, etc. which are defined -- just confirm no missing ones).

Do NOT add dark-mode overrides for styles that already use CSS variables correctly (like outcome badges using --success-color). Only fix undefined variables and hardcoded hex values.
  </action>
  <verify>
    <automated>grep -n '#[0-9a-fA-F]\{6\}' ui/options.css | grep -E 'memory|overflow|recon|task-graph|outcome|cost-card|cost-label|cost-value' | grep -v 'var(' | grep -v ':root' | grep -v 'data-theme'</automated>
  </verify>
  <done>No hardcoded hex colors remain in Memory tab CSS rules. --surface-color, --card-bg, --hover-bg defined in both :root and [data-theme="dark"]. Overflow dropdown, memory cost card, and all Phase 33 elements use theme-aware CSS variables.</done>
</task>

<task type="auto">
  <name>Task 2: Fix hardcoded inline colors in options.js</name>
  <files>ui/options.js</files>
  <action>
1. Fix guide name label (line ~3187): Change `color: #4fc3f7` to `color: var(--info-color)` — the info-color is defined as #0891b2 (light) / #4dd0e1 (dark), which is cyan-family like #4fc3f7.

2. Fix snapshot unavailable label (line ~3192): Change `color: #ffb74d` to `color: var(--warning-color)` — warning-color is #d97706 (light) / #e6b844 (dark), amber-family like #ffb74d.

3. Fix markdown snapshot pre block (line ~3196): Change `background: #1a1a2e; color: #e0e0e0` to `background: var(--bg-tertiary); color: var(--text-primary)`. This makes code blocks readable in both themes.

4. Fix site graph legend Page dots (lines ~4928-4929): Change `background: #4285f4` to `background: var(--info-color)`. Change the dashed border Discovered dot from `border: 1.5px dashed #4285f4` to `border: 1.5px dashed var(--info-color)`.

These are inline style attributes in template literals, so use `var(--variable-name)` syntax directly in the style strings.
  </action>
  <verify>
    <automated>grep -n '#4fc3f7\|#ffb74d\|#1a1a2e.*#e0e0e0\|#4285f4' ui/options.js</automated>
  </verify>
  <done>Zero hardcoded hex colors remain in Memory tab inline styles in options.js. All use CSS custom properties that adapt to light/dark theme.</done>
</task>

</tasks>

<verification>
1. Open options page in light mode -- Memory tab cards, detail panels, graph, overflow menu all use light backgrounds with dark text
2. Switch to dark mode -- all elements switch to dark backgrounds with light text, no white flashes or unreadable text
3. Specifically verify: overflow dropdown, memory cost card, markdown snapshot blocks, site graph legend dots, guide name labels, snapshot unavailable labels
</verification>

<success_criteria>
- Zero undefined CSS variables (--surface-color, --card-bg, --hover-bg all defined)
- Zero hardcoded hex colors in Memory-tab-related CSS rules
- Zero hardcoded hex colors in Memory-tab-related inline JS styles
- All Memory tab elements are readable in both light and dark themes
</success_criteria>

<output>
After completion, create `.planning/phases/34-memory-tab-theme-fix-export-import/34-01-SUMMARY.md`
</output>
