# Milestone v0.9.63 -- Showcase i18n -- Requirements (Archived)

**Status:** Shipped 2026-05-13
**Branch:** `feat/showcase-i18n`
**Coverage:** 14/14 requirements satisfied; 0 unsatisfied; 0 orphaned.
**Audit:** `passed` -- `.planning/milestones/v0.9.63-MILESTONE-AUDIT.md`

## Requirement IDs

REQ IDs are namespaced by concern: `COPY-*` (string marking), `ROUTE-*` (locale routing), `CI-*` (drift gates), `TRANS-*` (translation fill), `SEO-*` (hreflang / canonical), `BUILD-*` (prerender output).

| ID | Description | Phase | Status |
|----|-------------|-------|--------|
| [x] ROUTE-02 | LocaleService + locale-constants module; supported locales = en, es, de, ja, zh-CN, zh-TW | 261 | DONE |
| [x] CI-05 | `verify-locale-sync` script enforces locale constants <-> angular.json parity | 261 | DONE |
| [x] CI-03 | `@angular-eslint/template/i18n` rule wired in `eslint.config.js`; `lint:i18n` npm script available | 261 | DONE |
| [x] COPY-01 | Every visible marketing string marked with `i18n` / `$localize` across home, about, agents, privacy, support, shell + picker | 262 | DONE |
| [x] COPY-03 | `ng extract-i18n` produces `messages.xlf` (source XLIFF) with 420 trans-units across 7 namespaces | 262 | DONE |
| [x] CI-02 | `extract-i18n-clean` CI gate: `messages.xlf` byte-equal to a fresh extract | 262 | DONE |
| [x] CI-01 | `lint:i18n` promoted to hard-fail CI step (website job) | 262 | DONE |
| [x] COPY-02 | TS-side SEO `Title` + `Meta` strings marked via `$localize` tagged template (5 pages x 2 markers) | 262 | DONE |
| [x] SEO-01 | Per-locale `index.html` bootstrap fan-out with correct `<link rel="alternate" hreflang>` + canonical | 264 | DONE |
| [x] TRANS-01 | AI translator fills target XLIFFs (es, de, ja, zh-CN, zh-TW) from `messages.xlf` | 265 | DONE |
| [x] CI-04 | `i18nMissingTranslation` flipped from `warning` to `error` once target XLIFFs are filled | 265 | DONE |
| [x] BUILD-01 | `ng build` emits 30 prerendered HTMLs (6 marketing routes x 5 locale subpaths + en root) | 266 | DONE |
| [x] VERIFY-01 | Verification baseline: lint:i18n 0 errors, extract-clean 0 diff, build 30 HTMLs, no untranslated trans-units in target XLIFFs | 266 | DONE |
| [x] ROUTE-03 | Server-side Accept-Language redirect on `/` (cookie-respecting, bot-safe, 302) | 267 | DONE |

## Phase 268 Addendum (post-audit cleanup, no new REQ-IDs)

Phase 268 closed two audit warnings without introducing new requirement IDs:

- **WARNING-01 resolved** by Plan 268-01: server.js now derives the supported-locale list from the CJS registry via `LOCALES.filter(l => l !== SOURCE_LOCALE)`. Strengthens ROUTE-03 by inheriting `verify-locale-sync.mjs` CI parity protection.
- **WARNING-03 resolved** by Plan 268-02: six per-phase `VERIFICATION.md` files written; cross-cutting milestone-template gap closed.
- **WARNING-02 deferred:** picker-set `fsb-locale` cookie causes the bare-`/` redirect to short-circuit (returning fresh-tab/shared-link visitors see EN at root). Locked per 267-CONTEXT D-02 / T-267-03; revisit candidate for v0.9.64+.

## Outcomes

- All 14 requirements validated through CI gates + integration check + per-phase VERIFICATION.md (six artifacts, all `status: passed`).
- No requirements were adjusted or dropped during execution.
- Cross-phase integration check: 12/12 connections WIRED, 0 BROKEN, 3/3 E2E flows complete (`.planning/phases/v0.9.63-INTEGRATION-CHECK.md`).

## Out of scope (deferred)

- Dashboard surface (`src/app/pages/dashboard/**`) -- deferred to v0.9.65 per user decision 2026-05-12.
- Extension UI / popup translation -- no current REQ.
- MCP server / skills text -- English-only by design (CLI output).
- Picker-cookie short-circuit (WARNING-02) -- candidate for v0.9.64+ UX revisit.
