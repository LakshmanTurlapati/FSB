---
quick_id: 260508-gu8
title: Add Agents nav + page to showcase
date: 2026-05-08
description: New /agents marketing page for FSB OpenClaw skill
status: in_progress
---

<objective>
Add a new `/agents` showcase page with OpenClaw branding, marketing-style copy about the FSB skill + MCP power, install instructions, try-it prompts, and CTAs. Place "Agents" nav link between Dashboard and Privacy in three locations (desktop nav, mobile menu, footer). Wire up prerender + SEO files.
</objective>

<must_haves>
- `/agents` route registered in `app.routes.ts` (lazy load) and `app.routes.server.ts` (prerender).
- New page component at `pages/agents/agents-page.component.{ts,html,scss}` with hero + 5 marketing sections + CTA, matching showcase visual language (reuse `.section`, `.container`, `.reveal`, `.text-orange` patterns).
- Nav link "Agents" with OpenClaw logo (`/assets/providers/openclaw.svg`) in showcase-shell desktop nav, mobile menu, and footer Pages column. Position: between Dashboard and Privacy.
- Page meta tags + canonical URL + Title (`FSB -- Agents` or similar) following the about-page pattern (Title, Meta, OG tags, JSON-LD if useful).
- `showcase/angular/scripts/build-crawler-files.mjs` ROUTES updated to include `/agents`. Sitemap.xml regenerated. llms-full.txt regenerated from updated source.
- `llms.txt` (short form) updated with one-line `/agents` mention. `llms-full.source.md` augmented with an `/agents` route reference.
- Build passes (`npm --prefix showcase/angular run build`).
- Phase 251 test still passes (no skill artifacts touched).
- ZERO emojis. ASCII only.
</must_haves>

<task n="1" id="quick-260508-gu8-1">
<files>
- showcase/angular/src/app/pages/agents/agents-page.component.ts (NEW)
- showcase/angular/src/app/pages/agents/agents-page.component.html (NEW)
- showcase/angular/src/app/pages/agents/agents-page.component.scss (NEW)
</files>
<read_first>
- showcase/angular/src/app/pages/about/about-page.component.{ts,html,scss} (style + meta pattern reference)
- showcase/angular/src/app/pages/support/support-page.component.html (alt content layout reference)
- skills/FSB Skill/SKILL.md (canonical skill description)
- skills/FSB Skill/USAGE.md (install + try-it copy source)
</read_first>
<action>
Create the agents page component. Sections (in order):
1. Hero -- headline mentioning Agents + OpenClaw badge (logo + "OpenClaw Ready"), 1-line value prop, primary CTA button (ClawHub or GitHub).
2. "Meet the FSB Skill" -- what it is (canonical OpenClaw onboarding path), 3-bullet feature highlights (doctor flow, stdio printer, multi-host installer), small visual.
3. "Why FSB MCP is powerful" -- marketing copy about the FSB MCP surface: 50+ tools, multi-agent identity + tab ownership, vault boundary, real Chrome session, observability. 4-card grid.
4. "Install in 3 Steps" -- copy-paste-friendly numbered list:
   - Step 1: Chrome extension (Web Store URL + GitHub Releases fallback)
   - Step 2: MCP server (npx -y fsb-mcp-server, paste stdio block)
   - Step 3: Verify (`node scripts/doctor.mjs`)
   Each with a code block.
5. "Try It" -- 3 manual-mode prompts (read_page / click / type_text) + 1 explicit autopilot prompt (run_task), with the same "explicit delegation only" framing used in USAGE.md.
6. CTA footer band -- ClawHub install button + GitHub link + "see also" links to USAGE.md, MCP server section.

Style: match about-page visual language (`.section`, `.container`, `.reveal`, `.text-orange`, dark-themed sections, code blocks with proper monospace styling). Use Phosphor icons or Font Awesome (already loaded).

Meta + JSON-LD: follow about-page pattern -- inject Title, description meta, OG tags, canonical.

ZERO emojis.
</action>
<acceptance_criteria>
- Files exist at the three paths above
- Component imports `@angular/core`, `Title`, `Meta` (matches about-page pattern)
- Template references `assets/providers/openclaw.svg` at least once (hero badge)
- Template includes literal `https://chromewebstore.google.com/detail/badgafnfchcihdfnjneklogedcdkmjfk`
- Template includes literal `npx -y fsb-mcp-server`
- Template includes the strict autopilot rule "NEVER ... unless ... explicitly delegate"
- ASCII-only across all 3 files (perl `[^\x00-\x7F]` returns 0)
- No emoji code points (perl regex over emoji ranges returns 0)
</acceptance_criteria>
<verify>
<automated>
test -f "showcase/angular/src/app/pages/agents/agents-page.component.ts" && echo OK_TS
test -f "showcase/angular/src/app/pages/agents/agents-page.component.html" && echo OK_HTML
test -f "showcase/angular/src/app/pages/agents/agents-page.component.scss" && echo OK_SCSS
grep -q 'assets/providers/openclaw.svg' showcase/angular/src/app/pages/agents/agents-page.component.html && echo OK_LOGO
grep -q 'badgafnfchcihdfnjneklogedcdkmjfk' showcase/angular/src/app/pages/agents/agents-page.component.html && echo OK_WEBSTORE
grep -q 'npx -y fsb-mcp-server' showcase/angular/src/app/pages/agents/agents-page.component.html && echo OK_MCP
</automated>
</verify>
<done>Agents page component scaffolded with all 6 sections.</done>
</task>

<task n="2" id="quick-260508-gu8-2">
<files>
- showcase/angular/src/app/app.routes.ts (MODIFY)
- showcase/angular/src/app/app.routes.server.ts (MODIFY)
- showcase/angular/src/app/layout/showcase-shell/showcase-shell.component.html (MODIFY)
</files>
<read_first>
- All 3 above
</read_first>
<action>
1. `app.routes.ts`: add `{ path: 'agents', loadComponent: () => import('./pages/agents/agents-page.component').then(m => m.AgentsPageComponent) }` between dashboard and privacy.
2. `app.routes.server.ts`: add `{ path: 'agents', renderMode: RenderMode.Prerender }` between support and dashboard.
3. `showcase-shell.component.html`: add `Agents` link between Dashboard and Privacy in three places:
   - Line 10-11 (desktop nav)
   - Line 29-30 (mobile menu)
   - Line 51-52 (footer Pages column)
   Desktop nav link includes `<img src="assets/providers/openclaw.svg" alt="OpenClaw" class="nav-icon">` before the "Agents" text.
</action>
<acceptance_criteria>
- `grep -q "agents-page.component" showcase/angular/src/app/app.routes.ts`
- `grep -q "path: 'agents'" showcase/angular/src/app/app.routes.server.ts`
- `grep -c 'routerLink="/agents"' showcase/angular/src/app/layout/showcase-shell/showcase-shell.component.html` returns 3
- `grep -q 'openclaw.svg' showcase/angular/src/app/layout/showcase-shell/showcase-shell.component.html`
</acceptance_criteria>
<verify>
<automated>
grep -q "agents-page.component" showcase/angular/src/app/app.routes.ts && echo OK_ROUTES
grep -q "path: 'agents'" showcase/angular/src/app/app.routes.server.ts && echo OK_SERVER_ROUTES
test "$(grep -c 'routerLink="/agents"' showcase/angular/src/app/layout/showcase-shell/showcase-shell.component.html)" = "3" && echo OK_NAV_LINKS
grep -q 'openclaw.svg' showcase/angular/src/app/layout/showcase-shell/showcase-shell.component.html && echo OK_NAV_LOGO
</automated>
</verify>
<done>Routes wired; nav link present in all 3 locations; OpenClaw logo on desktop nav.</done>
</task>

<task n="3" id="quick-260508-gu8-3">
<files>
- showcase/angular/scripts/build-crawler-files.mjs (MODIFY)
- showcase/angular/scripts/llms-full.source.md (MODIFY)
- showcase/angular/public/llms.txt (MODIFY)
- showcase/angular/public/sitemap.xml (REGENERATED)
- showcase/angular/public/llms-full.txt (REGENERATED)
</files>
<read_first>
- All 4 above
</read_first>
<action>
1. `build-crawler-files.mjs` line 20: add `/agents` to the ROUTES array.
2. `llms-full.source.md`: add a one-paragraph `/agents` reference in the section listing routes (or section 5 Comparison's neighborhood). Mention page covers FSB skill + MCP power + install + try-it.
3. `llms.txt`: add a one-line mention of `/agents` to the Docs list.
4. Run `node showcase/angular/scripts/build-crawler-files.mjs` to regenerate sitemap.xml + llms-full.txt.
</action>
<acceptance_criteria>
- `grep -q "/agents" showcase/angular/scripts/build-crawler-files.mjs`
- `grep -q "/agents" showcase/angular/public/sitemap.xml`
- `grep -q "/agents" showcase/angular/public/llms.txt`
- `grep -q "/agents" showcase/angular/public/llms-full.txt`
</acceptance_criteria>
<verify>
<automated>
grep -q "'/agents'" showcase/angular/scripts/build-crawler-files.mjs && echo OK_CRAWLER_SCRIPT
grep -q "/agents" showcase/angular/public/sitemap.xml && echo OK_SITEMAP
grep -q "/agents" showcase/angular/public/llms.txt && echo OK_LLMS_SHORT
grep -q "/agents" showcase/angular/public/llms-full.txt && echo OK_LLMS_FULL
</automated>
</verify>
<done>SEO files include /agents; sitemap/llms-full regenerated.</done>
</task>

<task n="4" id="quick-260508-gu8-4">
<files>
- (verification only -- no file mutations)
</files>
<read_first>
- showcase/angular/package.json (build script)
</read_first>
<action>
Run the showcase Angular build to confirm the new route, page component, and nav additions compile cleanly without TypeScript errors. If the build fails, surface the error and fix before completing the task.
</action>
<acceptance_criteria>
- `npm --prefix showcase/angular run build` exits 0
- Build artifacts include the agents route output
</acceptance_criteria>
<verify>
<automated>
npm --prefix showcase/angular run build 2>&1 | tail -20
</automated>
</verify>
<done>Build green; the new page is shippable.</done>
</task>
