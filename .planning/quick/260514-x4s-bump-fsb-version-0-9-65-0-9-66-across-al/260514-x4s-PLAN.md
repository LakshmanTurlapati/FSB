---
phase: quick-260514-x4s
plan: 01
type: quick
description: Bump FSB version 0.9.65 → 0.9.66 across all manifests, READMEs, and CWS listing copy
files_modified:
  - package.json
  - extension/manifest.json
  - README.md
  - extension/README.md
  - store-assets/chrome-web-store/listing-copy.md
  - mcp/README.md
  - showcase/angular/package.json
  - showcase/angular/package-lock.json
must_haves:
  truths:
    - "All 8 user-visible version surfaces read 0.9.66"
    - "mcp/src/version.ts FSB_MCP_VERSION stays at 0.9.0 (independent versioning)"
    - "Zero residual 0.9.65 refs in tracked source (verified via grep across *.json *.md *.ts *.js *.html, excluding node_modules + .planning + dist + .git + .angular)"
    - "Historical anchors (v0.9.60, v0.9.61, v0.9.62, v0.9.63, v0.9.64, v0.9.69 milestone refs, Phase XXX / v0.9.X comments) are NOT bumped — they reference prior milestones and stay stable"
    - "showcase/angular/src/app/core/seo/version.ts is NOT manually edited — auto-regenerated from extension/manifest.json by the prebuild script on next build"
  artifacts:
    - "package.json — 3 refs bumped (version, package script zip filename, badge URL)"
    - "extension/manifest.json — 2 refs bumped (name, version)"
    - "README.md — 3 refs bumped (title, badge, status banner)"
    - "extension/README.md — 1 ref bumped"
    - "store-assets/chrome-web-store/listing-copy.md — 1 ref bumped (line 5 ONLY; line 83 v0.9.69 milestone ref left untouched)"
    - "mcp/README.md — 1 ref bumped (line 534 parenthetical cross-reference)"
    - "showcase/angular/package.json — 1 ref bumped"
    - "showcase/angular/package-lock.json — 2 refs bumped (top-level version + packages[''].version)"
  key_links:
    - "Mirrors prior commit 858e692 (0.9.64 → 0.9.65) — same diff shape, same file set"
---

# Plan: Bump FSB version 0.9.65 → 0.9.66

Mechanical version bump. 8 files, 14 line changes total. Same shape as the prior 0.9.64 → 0.9.65 bump (commit 858e692).

## Files NOT touched (deliberate)

- `mcp/src/version.ts` — `FSB_MCP_VERSION = '0.9.0'` is independently versioned per `mcp/README.md` line 534. Never bumps with the extension.
- `showcase/angular/src/app/core/seo/version.ts` — auto-generated from `extension/manifest.json` by the prebuild script. Will regenerate on next `npm --prefix showcase/angular run build`.
- All historical `v0.9.XX` references in source comments (Phase 269 / v0.9.69, v0.9.60 multi-agent, v0.9.62 visual-session, etc.) — they're milestone anchors, not the current release marker.
- `listing-copy.md:83` references v0.9.69 as the milestone of the telemetry feature — left intact.

## Verification (post-commit)

- `jq` parses all 4 modified JSON files without error.
- `grep -rn "0\.9\.65" --include="*.json" --include="*.md" ...` returns zero hits in tracked source.
- `tests/mcp-version-parity.test.js` passes (10/10 sub-asserts).

## Commit

ONE commit:
```
chore(release): bump version 0.9.65 -> 0.9.66
```
