---
phase: 162.1-partial-completion-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 162.1-01
files_modified:
  - background.js
  - utils/automation-logger.js
  - ui/popup.js
  - ui/sidepanel.js
  - ui/options.js
autonomous: true
gap_closure: true
requirements:
  - OUTCOME-02
must_haves:
  truths:
    - "Persisted session history preserves structured partial outcome details instead of only a coarse error status."
    - "Popup and sidepanel render blocked partial completion as a non-crash result using the runtime's explicit outcome contract."
    - "MCP/session history consumers can distinguish partial completion from true failure when reading session index or full session detail."
  artifacts:
    - path: "utils/automation-logger.js"
      provides: "Persisted session index/detail metadata for partial/blocked outcomes"
      contains: "saveSession"
    - path: "ui/popup.js"
      provides: "Live completion rendering that consumes explicit outcome data for partial results"
      contains: "automationComplete"
    - path: "ui/sidepanel.js"
      provides: "Live completion plus history rendering that shows partial outcomes as non-crash state"
      contains: "automationComplete"
    - path: "ui/options.js"
      provides: "Session list/detail badges that remain consistent with persisted partial outcome status"
      contains: "session-status-badge"
  key_links:
    - from: "utils/automation-logger.js"
      to: "background.js"
      via: "mcp:list-sessions and mcp:get-session read the persisted session index/detail"
      pattern: "listSessions"
    - from: "ai/agent-loop.js"
      to: "ui/popup.js"
      via: "automationComplete payload with explicit outcome fields"
      pattern: "automationComplete"
    - from: "ai/agent-loop.js"
      to: "ui/sidepanel.js"
      via: "automationComplete payload with explicit outcome fields"
      pattern: "automationComplete"
---

<objective>
Propagate the new partial-completion lifecycle into persisted history and user-facing surfaces so blocked-but-useful sessions are rendered as non-crash results everywhere the user sees them.

Purpose: Even after the runtime supports a partial outcome, the gap is not closed unless history and UI consumers preserve and display that outcome as a useful terminal state. This plan makes the logger/session index, MCP-visible history, and popup/sidepanel rendering outcome-aware.
Output: Persisted partial outcome metadata in session history plus UI/history surfaces that display partial completion as non-error state with preserved summary/blocker details.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/debug/auth-blocked-partial-outcome-lifecycle.md
@.planning/phases/162.1-partial-completion-lifecycle/162.1-CONTEXT.md
@.planning/phases/162.1-partial-completion-lifecycle/162.1-01-PLAN.md
@.planning/phases/162-event-bus-wiring/162-01-SUMMARY.md

@utils/automation-logger.js
@ui/popup.js
@ui/sidepanel.js
@ui/options.js
@background.js

<interfaces>
From utils/automation-logger.js:
```javascript
const indexEntry = {
  id: sessionId,
  task: savedSession.task,
  status: savedSession.status
};
```

From background.js:
```javascript
case 'mcp:list-sessions': {
  const sessions = await automationLogger.listSessions();
  sendMCPResponse(id, { success: true, sessions });
}
case 'mcp:get-session': {
  const session = await automationLogger.loadSession(sessionId);
  sendMCPResponse(id, { success: true, session });
}
```

From ui/popup.js and ui/sidepanel.js:
```javascript
const isPartial = request.partial === true;
completeStatusMessage(completionMessage, isPartial ? 'partial' : undefined);
```

From ui/options.js:
```javascript
<span class="session-status-badge ${session.status}">${session.status}</span>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist structured partial outcome data in session history and MCP-visible exports</name>
  <files>
utils/automation-logger.js
background.js
  </files>
  <read_first>
utils/automation-logger.js
background.js
.planning/debug/auth-blocked-partial-outcome-lifecycle.md
.planning/phases/162.1-partial-completion-lifecycle/162.1-01-PLAN.md
  </read_first>
  <action>
Implement D-02 and D-04 for persisted history consumers.

In `utils/automation-logger.js`, extend the saved session index/detail shape so partial/blocked outcomes preserve their structured result data rather than only a generic status string. The session index should expose enough outcome metadata for MCP `list_sessions` and history lists to distinguish partial completion from error. Full session detail and human-readable export should preserve the useful completed-work summary and blocker details.

In `background.js`, keep `mcp:list-sessions` and `mcp:get-session` on the existing logger-backed path, but make sure the payload shape exposed to MCP includes the new partial outcome metadata written by the logger. Do not add a parallel MCP-only session model.
  </action>
  <verify>
    <automated>node --check utils/automation-logger.js && node --check background.js && rg -n "partial|blocked|outcome|listSessions|loadSession|Status:" utils/automation-logger.js background.js</automated>
  </verify>
  <acceptance_criteria>
    - Persisted session index entries can distinguish partial/blocked completion from true failure
    - Full saved session detail preserves the useful completion summary and blocker details for partial outcomes
    - Human-readable export includes partial outcome status/details instead of rendering the run as an error-only report
    - MCP `list_sessions` and `get_session` expose the logger-backed partial outcome metadata without a new shadow model
  </acceptance_criteria>
  <done>MCP-visible session history preserves structured partial outcomes instead of collapsing them into generic error state.</done>
</task>

<task type="auto">
  <name>Task 2: Render partial outcomes as non-crash results in popup, sidepanel, and history views</name>
  <files>
ui/popup.js
ui/sidepanel.js
ui/options.js
  </files>
  <read_first>
ui/popup.js
ui/sidepanel.js
ui/options.js
utils/automation-logger.js
.planning/phases/162.1-partial-completion-lifecycle/162.1-CONTEXT.md
  </read_first>
  <action>
Update live and persisted UI consumers to use the explicit partial-outcome contract from Plan 01.

In `ui/popup.js` and `ui/sidepanel.js`, consume the new outcome metadata from `automationComplete` while preserving the existing partial-result bubble affordance. A blocked partial result should display as a non-error completion, while true failures still use the error path. Avoid relying solely on the legacy `request.partial === true` boolean if Plan 01 introduces richer outcome fields.

In `ui/sidepanel.js` history views and `ui/options.js` session list/detail views, render the persisted partial/blocked status with appropriate labels/badges so those surfaces no longer imply that the run crashed. Reuse existing session status badge patterns where possible; do not redesign the UI.
  </action>
  <verify>
    <automated>node --check ui/popup.js && node --check ui/sidepanel.js && node --check ui/options.js && rg -n "automationComplete|partial|blocked|session-status-badge|history-status" ui/popup.js ui/sidepanel.js ui/options.js</automated>
  </verify>
  <acceptance_criteria>
    - Popup live completion renders blocked partial completion as a non-error result
    - Sidepanel live completion and history views distinguish partial completion from error/stopped runs
    - Options/session-log history views render partial/blocked persisted sessions consistently with the logger data
    - Existing partial-result styling is reused; no UI redesign or new view model is introduced
  </acceptance_criteria>
  <done>All required user-facing surfaces show partial completion as a preserved non-crash result rather than generic failure.</done>
</task>

</tasks>

<verification>
Plan 02 closes the persistence/render half of the gap:

1. MCP session history preserves structured partial outcome data from the logger-backed session model.
2. Popup and sidepanel render blocked partial completion through the existing non-error result affordance.
3. History/status surfaces distinguish partial completion from error and stopped sessions.
</verification>

<success_criteria>
1. Persisted session index/detail data preserves partial outcome metadata for MCP history consumers.
2. Popup and sidepanel live completion paths render partial outcomes through non-error UI.
3. History/status badges and session detail views distinguish partial completion from errors and stops.
</success_criteria>

<output>
After completion, create `.planning/phases/162.1-partial-completion-lifecycle/162.1-02-SUMMARY.md`
</output>
