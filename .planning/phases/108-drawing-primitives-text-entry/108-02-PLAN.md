---
phase: 108-drawing-primitives-text-entry
plan: 02
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - site-guides/design/excalidraw.js
autonomous: true
requirements: [TEXT-01, TEXT-02, TEXT-03]

must_haves:
  truths:
    - "Site guide documents standalone text label creation (press T, click canvas, cdpInsertText)"
    - "Site guide documents in-shape text entry (double-click shape center via cdpClickAt, wait, cdpInsertText)"
    - "Site guide documents editing existing text (click shape, press Enter, wait for textarea, cdpInsertText)"
    - "Site guide documents the select+Enter alternative for in-shape text"
  artifacts:
    - path: "site-guides/design/excalidraw.js"
      provides: "Complete text entry workflows for standalone, in-shape, and edit modes"
      contains: "textStandalone"
  key_links:
    - from: "site-guides/design/excalidraw.js"
      to: "ai-integration.js"
      via: "registerSiteGuide injection into AI system prompt"
      pattern: "registerSiteGuide"
---

<objective>
Expand the text entry documentation in the Excalidraw site guide to cover all three text modes: standalone labels, in-shape text, and editing existing text.

Purpose: Phase 107 added a basic textEntry workflow, but it does not distinguish between standalone text (TEXT-01), in-shape text via double-click (TEXT-02), and editing existing text (TEXT-03). The AI needs explicit workflows for each mode so it chooses the correct activation method.

Output: Expanded `site-guides/design/excalidraw.js` with 3 distinct text workflow arrays and an updated TEXT ENTRY WORKFLOW section in the guidance string.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-drawing-primitives-text-entry/108-CONTEXT.md
@.planning/research/STACK.md
@site-guides/design/excalidraw.js

<interfaces>
<!-- After Plan 01 completes, the site guide will have these workflows: -->
<!-- sessionSetup, createFrame, drawRectangle, drawEllipse, drawDiamond, drawLine, drawArrow, drawFreedraw, alignShapes, fullFrameAlignmentWorkflow, textEntry -->
<!-- The textEntry workflow from Phase 107 is a generic 5-step sequence -->
<!-- This plan replaces/expands it into 3 specific workflows -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand TEXT ENTRY WORKFLOW section and add per-mode text workflows</name>
  <files>site-guides/design/excalidraw.js</files>
  <read_first>site-guides/design/excalidraw.js, .planning/research/STACK.md</read_first>
  <action>
Expand the text entry documentation in the Excalidraw site guide to cover all three text entry modes.

1. Update the TEXT ENTRY WORKFLOW section in the guidance string to document three distinct modes:

TEXT ENTRY WORKFLOW (3 modes):

  MODE 1 -- STANDALONE TEXT LABEL (TEXT-01):
    Step 1: press_key T to activate text tool
    Step 2: cdpClickAt(x, y) on the canvas where the text should appear -- this creates a new text element and opens the editor
    Step 3: Wait 300ms for transient textarea.excalidraw-wysiwyg to mount and auto-focus
    Step 4: cdpInsertText "your text here" -- text appears in the textarea
    Step 5: press_key Escape to commit the text and close the textarea
    NOTE: After placing text, tool auto-switches to V. Re-press T before placing the next standalone text.

  MODE 2 -- TEXT INSIDE A SHAPE (TEXT-02):
    Step 1: Double-click the shape center via two rapid cdpClickAt(shapeX, shapeY) calls (50ms apart) -- this opens the in-shape text editor
    Alternative: Click the shape once via cdpClickAt to select it, then press_key Enter to open the text editor
    Step 2: Wait 300ms for transient textarea.excalidraw-wysiwyg to mount and auto-focus
    Step 3: cdpInsertText "your label text" -- text appears inside the shape
    Step 4: press_key Escape to commit the text and close the textarea
    NOTE: The text will be centered within the shape boundary automatically.

  MODE 3 -- EDIT EXISTING TEXT (TEXT-03):
    Step 1: Click the text element or shape with text via cdpClickAt(elementX, elementY) to select it
    Step 2: press_key Enter to re-open the text editor on the selected element
    Step 3: Wait 300ms for transient textarea.excalidraw-wysiwyg to mount -- it will contain the existing text
    Step 4: Select all existing text: press_key a ctrl=true (Ctrl+A inside the textarea selects textarea content, not canvas elements)
    Step 5: cdpInsertText "replacement text" -- overwrites the selected text
    Step 6: press_key Escape to commit the updated text
    NOTE: To append instead of replace, skip Step 4 and just cdpInsertText the additional text.

  COMMON RULES FOR ALL TEXT MODES:
    - ALWAYS use cdpInsertText, NEVER use the type tool -- the textarea is transient and not in DOM snapshots
    - ALWAYS wait 300ms after activating text mode before cdpInsertText
    - ALWAYS press Escape to commit -- clicking elsewhere may lose the text
    - For multi-line text, use press_key Enter (without ctrl) between lines within the textarea
    - The textarea class is excalidraw-wysiwyg -- if you need to verify it mounted, check for this class in DOM

2. Replace the existing textEntry workflow array with three specific workflow arrays:

  textStandalone: [
    "Press T via press_key to activate text tool",
    "Click canvas position via cdpClickAt(x, y) to create text element and open editor",
    "Wait 300ms for transient textarea.excalidraw-wysiwyg to mount and auto-focus",
    "Type text using cdpInsertText (NOT the type tool)",
    "Press Escape via press_key to commit text and close textarea",
    "Tool auto-switches to V after text placement -- re-press T for next standalone text"
  ]

  textInShape: [
    "Double-click shape center via two rapid cdpClickAt(shapeX, shapeY) calls 50ms apart to open in-shape text editor",
    "Alternative: click shape once via cdpClickAt to select, then press_key Enter to open text editor",
    "Wait 300ms for transient textarea.excalidraw-wysiwyg to mount and auto-focus",
    "Type label text using cdpInsertText (NOT the type tool)",
    "Press Escape via press_key to commit text -- text centers within shape boundary automatically"
  ]

  textEdit: [
    "Click text element or shape with text via cdpClickAt to select it",
    "Press Enter via press_key to re-open text editor on selected element",
    "Wait 300ms for transient textarea.excalidraw-wysiwyg to mount with existing text",
    "Select all existing text: press_key(a, ctrl=true) to select textarea content",
    "Type replacement text using cdpInsertText to overwrite selected text",
    "Press Escape via press_key to commit updated text"
  ]

3. Keep the existing textEntry workflow array as-is for backward compatibility (it serves as the generic reference). Add a comment noting that textStandalone, textInShape, and textEdit provide mode-specific workflows.

4. Add a warning to the warnings array:
  "For in-shape text (double-click), ensure the cdpClickAt coordinates target the CENTER of the shape, not an edge -- edge clicks may start a resize or connector instead of opening the text editor"

5. Add the wysiwyg textarea selector to the selectors object if not already present:
  textareaWysiwyg: 'textarea.excalidraw-wysiwyg, textarea[data-type="wysiwyg"]'
  </action>
  <verify>
    <automated>grep -q "textStandalone" site-guides/design/excalidraw.js && grep -q "textInShape" site-guides/design/excalidraw.js && grep -q "textEdit" site-guides/design/excalidraw.js && grep -q "MODE 1" site-guides/design/excalidraw.js && grep -q "MODE 2" site-guides/design/excalidraw.js && grep -q "MODE 3" site-guides/design/excalidraw.js && grep -q "textareaWysiwyg" site-guides/design/excalidraw.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "textStandalone" site-guides/design/excalidraw.js returns a match
    - grep "textInShape" site-guides/design/excalidraw.js returns a match
    - grep "textEdit" site-guides/design/excalidraw.js returns a match
    - grep "MODE 1" site-guides/design/excalidraw.js returns a match (standalone text label)
    - grep "MODE 2" site-guides/design/excalidraw.js returns a match (text inside shape)
    - grep "MODE 3" site-guides/design/excalidraw.js returns a match (edit existing text)
    - grep "COMMON RULES" site-guides/design/excalidraw.js returns a match
    - grep "textareaWysiwyg" site-guides/design/excalidraw.js returns a match (selector added)
    - grep "center of the shape" site-guides/design/excalidraw.js returns a match (warning added, case-insensitive ok)
    - node -c site-guides/design/excalidraw.js exits 0 (valid JS syntax)
  </acceptance_criteria>
  <done>All three text entry modes are documented: standalone text labels (TEXT-01) via press T + cdpClickAt + cdpInsertText, in-shape text (TEXT-02) via double-click or select+Enter, and editing existing text (TEXT-03) via click + Enter + select-all + cdpInsertText. The guidance string has three MODE sections, the workflows object has textStandalone/textInShape/textEdit arrays, and the selectors include textareaWysiwyg.</done>
</task>

</tasks>

<verification>
- `node -c site-guides/design/excalidraw.js` -- syntax check passes
- Three text workflow arrays exist (textStandalone, textInShape, textEdit)
- TEXT ENTRY WORKFLOW section has MODE 1, MODE 2, MODE 3 subsections
- COMMON RULES section documents the universal cdpInsertText/Escape/wait pattern
- textareaWysiwyg selector added to selectors object
- Warning about targeting shape center for double-click added
</verification>

<success_criteria>
The AI can look up any of the 3 text entry scenarios and find the exact step-by-step workflow: which tool/key activates the mode, what to wait for, how to insert text via cdpInsertText, and how to commit via Escape. The guidance string distinguishes standalone text, in-shape text, and editing existing text as three separate documented modes.
</success_criteria>

<output>
After completion, create `.planning/phases/108-drawing-primitives-text-entry/108-02-SUMMARY.md`
</output>
