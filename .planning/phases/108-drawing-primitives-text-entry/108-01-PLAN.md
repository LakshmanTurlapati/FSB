---
phase: 108-drawing-primitives-text-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site-guides/design/excalidraw.js
autonomous: true
requirements: [DRAW-01, DRAW-02, DRAW-03, DRAW-04, DRAW-05, DRAW-06, DRAW-07]

must_haves:
  truths:
    - "Site guide documents how to draw rectangles via press_key R + cdpDrag"
    - "Site guide documents how to draw ellipses via press_key O + cdpDrag"
    - "Site guide documents how to draw diamonds via press_key D + cdpDrag"
    - "Site guide documents how to draw lines via press_key L + cdpDrag"
    - "Site guide documents how to draw arrows via press_key A + cdpDrag"
    - "Site guide documents how to freedraw via press_key P + cdpDrag"
    - "Site guide documents how to draw frames via press_key F + cdpDrag"
    - "Site guide warns about tool auto-switch after each draw and the need to re-press the tool key"
  artifacts:
    - path: "site-guides/design/excalidraw.js"
      provides: "Per-shape drawing workflows for all 7 shape types"
      contains: "drawEllipse"
  key_links:
    - from: "site-guides/design/excalidraw.js"
      to: "ai-integration.js"
      via: "registerSiteGuide injection into AI system prompt"
      pattern: "registerSiteGuide"
---

<objective>
Add drawing primitive workflows for all 7 Excalidraw shape types (rectangle, ellipse, diamond, line, arrow, freedraw, frame) to the site guide.

Purpose: The AI needs explicit per-shape workflows so it knows the keyboard shortcut, cdpDrag pattern, minimum drag distance, and tool-key re-press rule for each shape type. Without these, the AI guesses and often fails to re-press the tool key between consecutive shapes.

Output: Expanded `site-guides/design/excalidraw.js` with 7 new workflow arrays and updated guidance string documenting all drawing primitives.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-drawing-primitives-text-entry/108-CONTEXT.md
@.planning/research/STACK.md
@site-guides/design/excalidraw.js

<interfaces>
<!-- The site guide file uses registerSiteGuide with these top-level keys: -->
<!-- site, category, patterns, guidance (string), selectors (object), workflows (object), warnings (array), toolPreferences (array) -->
<!-- Existing workflows: sessionSetup, createFrame, drawRectangle, alignShapes, fullFrameAlignmentWorkflow, textEntry -->
<!-- The guidance string already has: SESSION SETUP, KEYBOARD SHORTCUTS, CANVAS ELEMENT, TOOLBAR STRUCTURE, FRAME TOOL, DRAWING SHAPES ON CANVAS, TEXT ENTRY WORKFLOW, MULTI-SELECT SHAPES, ALIGNMENT TOOLBAR, WELCOME DIALOG, PROPERTY PANELS -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DRAWING PRIMITIVES section to guidance string and per-shape workflows</name>
  <files>site-guides/design/excalidraw.js</files>
  <read_first>site-guides/design/excalidraw.js, .planning/research/STACK.md</read_first>
  <action>
Expand the Excalidraw site guide with comprehensive drawing primitive documentation.

1. Add a DRAWING PRIMITIVES section to the guidance string (insert after the existing DRAWING SHAPES ON CANVAS section). This section must document each shape type with its keyboard shortcut, the cdpDrag pattern, and the mandatory tool-key re-press rule. Content:

DRAWING PRIMITIVES (per-shape workflows):
  RECTANGLE (DRAW-01): press_key R, then cdpDrag(startX, startY, endX, endY, steps=15, stepDelayMs=15). Min 50px drag in both axes. Tool auto-switches to V after draw -- re-press R before next rectangle.
  ELLIPSE (DRAW-02): press_key O, then cdpDrag(startX, startY, endX, endY, steps=15, stepDelayMs=15). Min 50px drag in both axes. Tool auto-switches to V after draw -- re-press O before next ellipse.
  DIAMOND (DRAW-03): press_key D, then cdpDrag(startX, startY, endX, endY, steps=15, stepDelayMs=15). Min 50px drag in both axes. Tool auto-switches to V after draw -- re-press D before next diamond.
  LINE (DRAW-04): press_key L, then cdpDrag(startX, startY, endX, endY, steps=15, stepDelayMs=15). Min 50px drag. Tool auto-switches to V after draw -- re-press L before next line.
  ARROW (DRAW-05): press_key A, then cdpDrag(startX, startY, endX, endY, steps=20, stepDelayMs=20). Use 20+ steps for reliable arrow binding to shapes. Min 50px drag. Tool auto-switches to V after draw -- re-press A before next arrow.
  FREEDRAW (DRAW-06): press_key P, then cdpDrag(startX, startY, endX, endY, steps=30, stepDelayMs=10). Use 30+ steps for smooth freehand stroke. Tool auto-switches to V after draw -- re-press P before next stroke.
  FRAME (DRAW-07): press_key F, then cdpDrag(startX, startY, endX, endY, steps=15, stepDelayMs=15). Frames act as named containers for shapes inside them. Double-click frame label to rename. Tool auto-switches to V after draw -- re-press F before next frame.

  CRITICAL RULE: Excalidraw auto-switches to selection tool (V) after EVERY shape draw. You MUST re-press the tool key (R, O, D, L, A, P, F) before each subsequent shape. Without re-pressing, cdpDrag creates a selection box instead of a shape. CDP reports success either way so the error is silent.

  COORDINATE CONVENTION: Use 150px horizontal spacing, 120px vertical spacing, and 150x80px default shape size for consistent diagram layouts. Example: first shape at (200, 200), second at (350, 200), third at (500, 200) for a horizontal row.

2. Add 6 new workflow arrays to the workflows object (drawRectangle already exists -- update it to match the standardized pattern and add the remaining 6):

Update existing drawRectangle workflow to include the 50px minimum drag note and coordinate convention.

Add these new workflow arrays:
  - drawEllipse: [press_key O, cdpDrag with 50px min, tool re-press note]
  - drawDiamond: [press_key D, cdpDrag with 50px min, tool re-press note]
  - drawLine: [press_key L, cdpDrag with 50px min, tool re-press note]
  - drawArrow: [press_key A, cdpDrag with 20+ steps for binding, tool re-press note]
  - drawFreedraw: [press_key P, cdpDrag with 30+ steps, tool re-press note]
  - drawFrame: already exists as createFrame -- update it to match standardized pattern with 50px minimum drag

Each workflow array should have 4 steps:
  Step 1: "Press {KEY} via press_key to activate {shape} tool"
  Step 2: "Draw {shape} by dragging on canvas: cdpDrag(startX, startY, endX, endY, steps={N}, stepDelayMs={M}) -- minimum 50px drag in both axes"
  Step 3: "After drawing, Excalidraw auto-switches to selection tool (V) -- re-press {KEY} before drawing the next {shape}"
  Step 4: "Use 150px horizontal spacing and 120px vertical spacing between shapes for consistent layouts"

3. Add a warning to the warnings array about the 50px minimum drag distance (update existing 30px warning to 50px per research findings):
  "Minimum drag distance of 50px in both axes needed for Excalidraw to register as a shape draw (not a click) -- research verified this threshold"

4. Update the existing minimum drag distance references in the guidance string from 30px to 50px to match research findings.
  </action>
  <verify>
    <automated>grep -c "drawEllipse\|drawDiamond\|drawLine\|drawArrow\|drawFreedraw" site-guides/design/excalidraw.js | grep -q "[5-9]" && grep -q "DRAWING PRIMITIVES" site-guides/design/excalidraw.js && grep -q "CRITICAL RULE" site-guides/design/excalidraw.js && grep -q "50px" site-guides/design/excalidraw.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "drawEllipse" site-guides/design/excalidraw.js returns a match
    - grep "drawDiamond" site-guides/design/excalidraw.js returns a match
    - grep "drawLine" site-guides/design/excalidraw.js returns a match
    - grep "drawArrow" site-guides/design/excalidraw.js returns a match
    - grep "drawFreedraw" site-guides/design/excalidraw.js returns a match
    - grep "DRAWING PRIMITIVES" site-guides/design/excalidraw.js returns a match
    - grep "CRITICAL RULE" site-guides/design/excalidraw.js returns a match
    - grep "COORDINATE CONVENTION" site-guides/design/excalidraw.js returns a match
    - grep "50px" site-guides/design/excalidraw.js returns a match (updated from 30px)
    - grep "re-press" site-guides/design/excalidraw.js returns multiple matches
    - node -c site-guides/design/excalidraw.js exits 0 (valid JS syntax)
  </acceptance_criteria>
  <done>All 7 shape types (rectangle, ellipse, diamond, line, arrow, freedraw, frame) have documented workflows in the site guide with keyboard shortcuts, cdpDrag parameters, 50px minimum drag distance, tool-key re-press rule, and coordinate convention. The guidance string contains a DRAWING PRIMITIVES section with the CRITICAL RULE about tool auto-switch.</done>
</task>

</tasks>

<verification>
- `node -c site-guides/design/excalidraw.js` -- syntax check passes
- `grep -c "draw" site-guides/design/excalidraw.js` -- multiple drawing workflow references
- All 7 shape types have workflow arrays in the workflows object
- DRAWING PRIMITIVES section exists in guidance string
- CRITICAL RULE about tool auto-switch documented
- COORDINATE CONVENTION documented with 150px/120px spacing
- 50px minimum drag distance documented (updated from 30px)
</verification>

<success_criteria>
The AI can look up any of the 7 shape types in the site guide and find: the keyboard shortcut to activate the tool, the cdpDrag call pattern with recommended steps/delay, the minimum drag distance (50px), the tool-key re-press rule, and the coordinate spacing convention.
</success_criteria>

<output>
After completion, create `.planning/phases/108-drawing-primitives-text-entry/108-01-SUMMARY.md`
</output>
