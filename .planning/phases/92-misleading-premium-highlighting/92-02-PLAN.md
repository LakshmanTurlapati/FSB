---
phase: 92-misleading-premium-highlighting
plan: 02
type: execute
wave: 2
depends_on:
  - 92-01
files_modified:
  - .planning/phases/92-misleading-premium-highlighting/92-DIAGNOSTIC.md
autonomous: false
requirements:
  - DARK-06

must_haves:
  truths:
    - "Live MCP test attempted against a flight search results page (Google Flights, Kayak, Southwest, or airline comparison site) using navigate + get_dom_snapshot + get_text to extract ALL flight prices, parse to numeric values, identify the cheapest option by minimum price, and verify the cheapest option is identifiable despite misleading premium highlighting, with documented outcome (pass/partial/fail/skip-auth)"
    - "Diagnostic report exists with step-by-step log, what worked, what failed, tool gaps, and autopilot recommendations specific to misleading premium highlighting dark pattern navigation"
    - "Diagnostic report contains a Dark Pattern Analysis section assessing premium highlighting techniques found (color emphasis, badge labels, size scaling, position manipulation, pre-selection defaults), price extraction accuracy, and whether numeric comparison correctly identified the cheapest flight ignoring visual misdirection"
    - "Human verified the diagnostic report accuracy and outcome classification"
  artifacts:
    - path: ".planning/phases/92-misleading-premium-highlighting/92-DIAGNOSTIC.md"
      provides: "DARK-06 autopilot diagnostic report"
      contains: "DARK-06"
  key_links:
    - from: "92-DIAGNOSTIC.md"
      to: "site-guides/utilities/premium-highlighting.js"
      via: "selector accuracy table references premium-highlighting.js selectors for result containers, price elements, fare class labels, premium indicators"
      pattern: "selectCheapestFlight|resultContainer|priceElement|fareClassLabel|premiumIndicators"
    - from: "92-DIAGNOSTIC.md"
      to: "mcp-server/src/tools/manual.ts"
      via: "tool usage section references navigate, click, read_page, get_dom_snapshot, get_text, get_attribute tools"
      pattern: "navigate|click|read_page|get_dom_snapshot|get_text|get_attribute"
---

<objective>
Execute live MCP test for DARK-06 (select cheapest flight on airline site with misleading premium highlighting) and generate the autopilot diagnostic report.

Purpose: Validate whether the MCP navigate + get_dom_snapshot + get_text chain can extract all flight prices from a results page, parse them to numeric values, identify the cheapest option by minimum price comparison, and determine that the cheapest option is identifiable despite misleading visual highlighting (premium badges, color emphasis, size scaling, position manipulation, pre-selection defaults). The key test: given flight results where expensive options are visually prominent and cheap options are visually subdued, can the AI identify and target the cheapest flight purely through DOM text price comparison? Expected outcome is PARTIAL (HTTP validation of flight result DOM structure, price extraction, and premium highlighting patterns, but live click execution blocked by WebSocket bridge disconnect) or PASS (if MCP bridge is operational). Produces a structured diagnostic report for the autopilot enhancement milestone.

Output: 92-DIAGNOSTIC.md with DARK-06 test results
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-misleading-premium-highlighting/92-CONTEXT.md
@.planning/phases/92-misleading-premium-highlighting/92-01-SUMMARY.md
@site-guides/utilities/premium-highlighting.js
@.planning/phases/91-adblocker-modal-bypass/91-DIAGNOSTIC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute live MCP premium highlighting test, generate DARK-06 diagnostic report</name>
  <files>.planning/phases/92-misleading-premium-highlighting/92-DIAGNOSTIC.md</files>
  <read_first>
    - site-guides/utilities/premium-highlighting.js (full file -- site guide with selectCheapestFlight workflow, DARK-06 guidance, price-based selection strategy, selectors)
    - .planning/phases/91-adblocker-modal-bypass/91-DIAGNOSTIC.md lines 1-15 (diagnostic report template structure -- use exact same format with Dark Pattern Analysis section for premium highlighting)
    - .planning/phases/92-misleading-premium-highlighting/92-01-SUMMARY.md (Plan 01 outcome -- what was added to premium-highlighting.js)
    - site-guides/travel/google-travel.js lines 1-60 (Google Flights selectors for result cards and prices -- used as primary test target)
    - site-guides/travel/kayak.js lines 1-50 (Kayak selectors for result cards and prices -- used as secondary test target)
    - site-guides/travel/southwest.js lines 1-50 (Southwest fare tier structure -- Wanna Get Away / Anytime / Business Select columns as example of multi-tier highlighting)
  </read_first>
  <action>
Execute the DARK-06 edge case test using MCP manual tools following the selectCheapestFlight workflow from the premium-highlighting.js site guide. Test steps:

1. IDENTIFY TEST TARGETS FOR PREMIUM HIGHLIGHTING:
   Use HTTP-based validation to analyze flight result page structures for premium highlighting patterns:
   (a) Google Flights (google.com/travel/flights/search) -- primary target. Search results show multiple flight options with prices. Higher-priced options may appear first or be visually emphasized.
   (b) Kayak (kayak.com/flights) -- secondary target. Aggregator showing results from multiple providers, potentially with "Best" or "Recommended" badges.
   (c) Southwest (southwest.com) -- fare tier target. Shows Wanna Get Away (cheapest), Anytime, and Business Select columns per flight, with Business Select visually prominent.
   (d) Any airline comparison page that shows multiple fare tiers or results with visual emphasis differences.
   (e) HTTP fetch each candidate to analyze their HTML for flight result structures, price elements, and premium highlighting patterns.

2. VALIDATE GOOGLE FLIGHTS RESULT STRUCTURE (primary target):
   (a) Fetch https://www.google.com/travel/flights (or a search results URL with pre-filled route) to analyze the result DOM structure
   (b) Look for result card containers: .pIav2d, [data-resultid], li elements with result classes
   (c) Look for price elements: .YMlIz, aria-labels containing prices, spans with dollar amounts
   (d) Check for visual emphasis patterns: "Best departing flight" label, "Recommended" badges, highlighted borders on certain results
   (e) Check for fare class differentiation: does Google Flights show multiple fare tiers per result?
   (f) Analyze whether prices are in server-rendered HTML or client-rendered only
   (g) Record: URL tested, HTTP status, page size, result containers found, price elements found, highlighting patterns found

3. VALIDATE KAYAK RESULT STRUCTURE (secondary target):
   (a) Fetch https://www.kayak.com/flights (or a search results URL) to analyze result DOM
   (b) Look for result cards: .nrc6-inner, [id^="resultInner"]
   (c) Look for prices: .f8F1-price-text, [class*="price"]
   (d) Check for badges: "Best", "Cheapest", "Fastest" labels on certain results
   (e) Check for visual emphasis: highlighted borders, larger fonts on certain results
   (f) Record: URL tested, HTTP status, page size, result containers found, price patterns found, badge patterns found

4. VALIDATE SOUTHWEST FARE TIER STRUCTURE (fare tier target):
   (a) Fetch https://www.southwest.com/ or a flight search results page to analyze fare tier DOM
   (b) Look for fare column structure: Wanna Get Away, Anytime, Business Select columns
   (c) Check for visual emphasis: which fare tier has the largest card, brightest color, or "Recommended" label
   (d) Check for pre-selection: is a non-cheapest fare tier pre-selected?
   (e) Record: URL tested, HTTP status, fare tier structure found, visual emphasis patterns found

5. PREMIUM HIGHLIGHTING PATTERN ANALYSIS:
   (a) For every page with flight results found, analyze the premium highlighting dark patterns:
       - Color emphasis: Which results use bolder/brighter colors? Which are gray/muted?
       - Size scaling: Which result cards are larger? Which are compact?
       - Badge labels: Which results have "Recommended", "Best Value", "Best", "Popular" labels?
       - Position: Are cheaper flights at the bottom? Are expensive flights at the top?
       - Pre-selection: Is a non-cheapest option pre-selected (checked radio, highlighted border)?
       - Hidden pricing: Are "from" prices displayed that differ from actual total prices?
   (b) Classify each highlighting technique by prevalence: found on how many of the tested sites?
   (c) Record: complete highlighting pattern inventory per target

6. PRICE EXTRACTION VALIDATION:
   (a) For each site, attempt to extract ALL prices visible on the results page
   (b) Test price parsing: "$199", "199 USD", "From $199", "$1,234", "1,234.56"
   (c) Verify that the cheapest price can be identified by numeric comparison
   (d) Check whether the cheapest price element is visually subdued compared to premium options
   (e) Verify that the premium-highlighting.js selectors (priceElement.google, priceElement.kayak, etc.) match actual price elements in the HTML
   (f) Record: number of prices extracted per site, parsed values, minimum identified, selector accuracy

7. CHEAPEST OPTION IDENTIFICATION:
   (a) For each site, identify which result contains the absolute cheapest price
   (b) Analyze that result's visual treatment: is it highlighted or subdued? Does it have a badge? Is it at the top or bottom?
   (c) Compare its visual treatment to the most visually prominent result on the page
   (d) Record: cheapest price, its visual treatment, vs most prominent result's price and treatment

8. OUTCOME CLASSIFICATION:
   - PASS = flight results found with premium highlighting dark patterns, price extraction via DOM correctly identifies cheapest option, cheapest option clicked and verified via MCP tools
   - PARTIAL = HTTP validation confirms flight result DOM structures and premium highlighting patterns, price extraction strategy validated against real HTML, but live click execution blocked by WebSocket bridge disconnect
   - FAIL = could not find any flight results page for analysis, or price extraction from DOM is not feasible
   - SKIP-AUTH = all flight booking sites require authentication with no public search alternative

Record every step outcome for the diagnostic report.

After test execution (regardless of outcome), generate 92-DIAGNOSTIC.md with ALL of these sections:

## Metadata
- Phase: 92, Requirement: DARK-06, Date: today, Outcome: (PASS/PARTIAL/FAIL/SKIP-AUTH), Live MCP Testing: YES/NO

## Prompt Executed
The exact prompt: "Navigate to an airline flight search results page, identify the cheapest available flight by extracting and comparing all visible prices from the DOM (ignoring visual highlighting, badges, and position), select the cheapest option, and verify the selection."

## Result Summary
2-3 sentence summary of what happened.

## Step-by-Step Log
Table with columns: Step | MCP Tool Used | Target | Result | Notes
One row per tool invocation or HTTP fetch attempted.

## What Worked
Bullet list of everything that succeeded.

## What Failed
Bullet list of everything that failed or was not executed, with reasons.

## Tool Gaps Identified
New tool gaps discovered plus reference to known gaps. Consider:
- Did navigate successfully load flight search results?
- Could get_dom_snapshot find result container and price elements?
- Could get_text extract clean price text for numeric parsing?
- Is there a tool gap for parsing and comparing numeric values from price strings?
- Is there a tool gap for identifying the DOM element containing the minimum price?
- Is there a tool gap for clicking a specific fare tier within a multi-column flight row?
- WebSocket bridge availability (persistent gap from phases 55-91)

## Dark Pattern Analysis (DARK PATTERN CATEGORY SECTION)
Specific analysis of misleading premium highlighting dark patterns:
- Premium highlighting techniques found per target (color emphasis, size scaling, badge labels, position manipulation, pre-selection, hidden pricing, fare class obfuscation)
- Visual treatment comparison: cheapest flight's DOM presentation vs most prominent flight's DOM presentation
- Badge prevalence: which badge labels appear, which fare tiers they are applied to, whether any badge appears on the cheapest option
- Pre-selection analysis: which fare tier is pre-selected by default, is it the cheapest?
- Price extraction feasibility: can all prices be reliably extracted from DOM text across tested sites?
- Recommendations for DOM-only price comparison without visual analysis

## Bugs Fixed In-Phase
Any bugs found and fixed during testing.

## Autopilot Recommendations
8-10 specific recommendations for how autopilot should handle misleading premium highlighting. Cover:
- Always use get_dom_snapshot to extract ALL price elements from flight results, not just visually prominent ones
- Parse every price to numeric value: strip $, commas, "From", currency codes before comparing
- Select the minimum numeric price regardless of which result card it appears in
- NEVER use badge text ("Recommended", "Best Value") as a selection criterion -- these are marketing labels on premium fares
- NEVER select the pre-selected/highlighted option without comparing its price to all alternatives
- On airline direct sites, compare prices across fare tier columns (Basic Economy through First Class) not just across different flights
- Check for hidden fees: "from" prices may exclude taxes; look for total price elements
- Verify selection by reading the booking summary after clicking the cheapest option
- If the cheapest option is at the bottom of the page, scroll down before clicking
- Use get_text on individual price elements rather than reading the entire page to avoid context bloat from flight details

## Selector Accuracy
Table: Selector | Expected | Actual | Match -- for each selector from premium-highlighting.js tested against live DOM or HTTP. Test at minimum: resultContainer.google, resultContainer.kayak, priceElement.google, priceElement.kayak, priceElement.generic, premiumIndicators.badge, premiumIndicators.preselected.

## New Tools Added This Phase
Table: Tool Name | File | Purpose | Parameters
Note: No new MCP tools added this phase. List the premium-highlighting.js site guide selectCheapestFlight workflow addition as the new content.

Use the exact same structure and level of detail as 91-DIAGNOSTIC.md, with the Dark Pattern Analysis section covering premium highlighting specifics. Do NOT leave any section as placeholder text like "TBD" or "TODO".
  </action>
  <verify>
    <automated>test -f .planning/phases/92-misleading-premium-highlighting/92-DIAGNOSTIC.md && grep -q "DARK-06" .planning/phases/92-misleading-premium-highlighting/92-DIAGNOSTIC.md && grep -q "Outcome:" .planning/phases/92-misleading-premium-highlighting/92-DIAGNOSTIC.md && grep -q "Autopilot Recommendations" .planning/phases/92-misleading-premium-highlighting/92-DIAGNOSTIC.md && grep -q "Step-by-Step Log" .planning/phases/92-misleading-premium-highlighting/92-DIAGNOSTIC.md && grep -q "Dark Pattern" .planning/phases/92-misleading-premium-highlighting/92-DIAGNOSTIC.md && grep -q "Selector Accuracy" .planning/phases/92-misleading-premium-highlighting/92-DIAGNOSTIC.md && grep -q "New Tools Added" .planning/phases/92-misleading-premium-highlighting/92-DIAGNOSTIC.md && grep -q "price\|Price" .planning/phases/92-misleading-premium-highlighting/92-DIAGNOSTIC.md && grep -q "numeric\|Numeric\|compare\|Compare" .planning/phases/92-misleading-premium-highlighting/92-DIAGNOSTIC.md && grep -q "Recommended\|Best Value\|badge\|Badge" .planning/phases/92-misleading-premium-highlighting/92-DIAGNOSTIC.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/92-misleading-premium-highlighting/92-DIAGNOSTIC.md exists
    - grep "DARK-06" 92-DIAGNOSTIC.md returns at least 1 match
    - grep "Outcome:" 92-DIAGNOSTIC.md returns at least 1 match (PASS, PARTIAL, FAIL, or SKIP-AUTH)
    - grep "Autopilot Recommendations" 92-DIAGNOSTIC.md returns at least 1 match
    - grep "Step-by-Step Log" 92-DIAGNOSTIC.md returns at least 1 match
    - grep "Dark Pattern" 92-DIAGNOSTIC.md returns at least 1 match (category-specific section)
    - grep "Selector Accuracy" 92-DIAGNOSTIC.md returns at least 1 match
    - grep "What Worked" 92-DIAGNOSTIC.md returns at least 1 match
    - grep "What Failed" 92-DIAGNOSTIC.md returns at least 1 match
    - grep "price\|Price" 92-DIAGNOSTIC.md returns at least 5 matches (price extraction analysis)
    - grep "numeric\|Numeric\|compare\|Compare" 92-DIAGNOSTIC.md returns at least 2 matches (numeric comparison strategy)
    - grep "Recommended\|Best Value\|badge\|Badge" 92-DIAGNOSTIC.md returns at least 2 matches (badge dark pattern analysis)
    - grep "pre-select\|preselect\|default.*select\|highlighted" 92-DIAGNOSTIC.md returns at least 1 match (pre-selection analysis)
    - grep "fare.*class\|fare.*tier\|economy\|premium\|Economy\|Premium" 92-DIAGNOSTIC.md returns at least 2 matches (fare tier analysis)
    - grep "Google.*Flight\|Kayak\|Southwest" 92-DIAGNOSTIC.md returns at least 2 matches (test target sites)
    - grep "New Tools Added" 92-DIAGNOSTIC.md returns at least 1 match
    - No section contains only placeholder text like "TBD" or "TODO"
  </acceptance_criteria>
  <done>DARK-06 diagnostic report generated with real test data covering all required sections: metadata, prompt, result summary, step-by-step log (Google Flights/Kayak/Southwest result structure analysis, price extraction validation, premium highlighting pattern inventory, cheapest option identification), what worked, what failed, tool gaps (result loading, price extraction, numeric parsing, minimum identification, fare tier clicking, WebSocket bridge), dark pattern analysis (highlighting techniques per target, visual treatment comparison, badge prevalence, pre-selection analysis, price extraction feasibility, DOM-only comparison recommendations), bugs fixed, autopilot recommendations (extract ALL prices, parse to numeric, select minimum, never trust badges, never trust pre-selection, compare across fare tiers, check hidden fees, verify selection, scroll to bottom results, use get_text per element), selector accuracy (premium-highlighting.js selectors vs live DOM), new tools (selectCheapestFlight workflow addition)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify DARK-06 diagnostic report accuracy</name>
  <files>.planning/phases/92-misleading-premium-highlighting/92-DIAGNOSTIC.md</files>
  <action>
Human verifies the DARK-06 diagnostic report accuracy.

What was built: DARK-06 misleading premium highlighting diagnostic report with MCP test results. The report documents the outcome (PASS/PARTIAL/FAIL/SKIP-AUTH) of attempting to select the cheapest flight on an airline site with misleading premium highlighting. The workflow tests: (a) navigating to flight search results, (b) using get_dom_snapshot and get_text to extract ALL visible prices from result elements, (c) parsing prices to numeric values (stripping currency symbols, commas, formatting), (d) identifying the cheapest option by minimum numeric comparison, (e) verifying the cheapest option is identifiable despite being visually subdued compared to premium-highlighted expensive options. This phase also created a premium-highlighting.js site guide with a selectCheapestFlight workflow (8 steps), 7 premium highlighting technique documentation, price extraction strategy, airline site result DOM patterns, and fare class dark pattern guidance. The diagnostic includes a "Dark Pattern Analysis" section assessing premium highlighting techniques found, visual treatment comparisons, badge prevalence, and price extraction feasibility.

How to verify:
1. Open .planning/phases/92-misleading-premium-highlighting/92-DIAGNOSTIC.md
2. Verify the outcome classification is accurate (PASS/PARTIAL/FAIL/SKIP-AUTH matches what actually happened)
3. Verify step-by-step log has real data (no placeholder rows)
4. Verify price extraction analysis is documented (which sites had prices in server HTML vs client-rendered)
5. Verify premium highlighting patterns are documented (badges, colors, sizes, positions, pre-selections found)
6. Verify cheapest option identification is documented (which result had the minimum price, its visual treatment vs premium options)
7. Verify Dark Pattern Analysis section has specific findings about highlighting techniques per target site
8. Verify fare tier analysis is present (for airline direct sites with multiple fare columns)
9. Verify autopilot recommendations are specific to premium highlighting avoidance (not generic flight search advice)
10. Verify selector accuracy table has entries from premium-highlighting.js tested against live DOM or HTTP
11. Confirm the report is complete and ready for the autopilot enhancement milestone

Resume: Type "approved" to accept the diagnostic report, or describe any issues to fix.
  </action>
  <verify>Human confirms by typing "approved" or describes issues</verify>
  <done>Human has reviewed and approved the DARK-06 edge case results and diagnostic report.</done>
</task>

</tasks>

<verification>
1. 92-DIAGNOSTIC.md exists with DARK-06 reference and all required sections
2. Outcome is one of: PASS, PARTIAL, FAIL, SKIP-AUTH
3. Step-by-step log contains at least 8 rows with real tool invocations or HTTP fetch results
4. Price extraction analysis documented (prices found per site, parsing results, minimum identified)
5. Premium highlighting pattern inventory documented (techniques found per target)
6. Visual treatment comparison documented (cheapest option vs most prominent option presentation)
7. Badge analysis documented (which labels found, which fare tiers they apply to)
8. Pre-selection analysis documented (which option pre-selected by default, whether cheapest)
9. Fare tier analysis documented (multi-column fare structure on airline direct sites)
10. Dark Pattern Analysis section has specific findings (highlighting techniques, price extraction feasibility, DOM-only comparison)
11. Autopilot recommendations section has at least 8 specific items about premium highlighting avoidance
12. Selector accuracy table has entries from premium-highlighting.js tested against live DOM or HTTP
13. New Tools Added section notes selectCheapestFlight workflow addition to premium-highlighting.js
14. Human approved the diagnostic report
</verification>

<success_criteria>
- DARK-06 edge case attempted via MCP manual tools with documented outcome
- Diagnostic report generated with all required sections filled with real data
- Price extraction and numeric comparison validated (or reason for incomplete execution)
- Premium highlighting techniques documented per target site
- Cheapest option identification strategy validated against real flight result DOM structures
- Dark Pattern Analysis section provides insights for premium highlighting automation
- Human verified and approved the report accuracy
- Phase 92 complete, ready to proceed to Phase 93 (DARK-07)
</success_criteria>

<output>
After completion, create `.planning/phases/92-misleading-premium-highlighting/92-02-SUMMARY.md`
</output>
