---
phase: 161-module-adoption
plan: 03
type: execute
wave: 1
depends_on: ["161-01", "161-02"]
files_modified: ["background.js"]
autonomous: true
requirements: ["ADOPT-05"]
gap_closure: true

must_haves:
  truths:
    - "Restored sessions after service worker restart preserve their original mode (autopilot, mcp-manual, mcp-agent, dashboard-remote) instead of defaulting to autopilot"
    - "persistSession() includes session.mode in the persistableSession object written to chrome.storage.session"
  artifacts:
    - path: "background.js"
      provides: "mode field in persistableSession object"
      contains: "mode: session.mode"
  key_links:
    - from: "background.js:persistSession"
      to: "chrome.storage.session"
      via: "persistableSession.mode"
      pattern: "mode:\\s*session\\.mode"
    - from: "background.js:restoreSessionsFromStorage"
      to: "ai/session-schema.js:createSession"
      via: "createSession({...persistedSession}) now includes mode"
      pattern: "createSession"
---

<objective>
Fix session mode loss across service worker restarts by adding the `mode` field to the `persistableSession` object in `persistSession()`.

Purpose: Without this fix, dashboard-remote and mcp-agent sessions silently revert to autopilot mode after a Chrome service worker kill/restart, causing loadSessionConfig to apply wrong per-mode limits.
Output: Single line addition to background.js persistSession() function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161-module-adoption/161-VERIFICATION.md

<interfaces>
<!-- From ai/session-schema.js lines 348-352: mode is declared warm-tier -->
mode: {
  default: 'autopilot',
  tier: 'warm',
  type: 'string (autopilot|mcp-manual|mcp-agent|dashboard-remote)'
}

<!-- From background.js persistSession() lines 2787-2811: persistableSession object -->
<!-- mode field is MISSING from this object -- that is the gap -->

<!-- From background.js lines 6152, 6417: mode is set on new sessions -->
mode: 'autopilot'          // handleStartAutomation (line 6152)
mode: sessionMode           // executeAutomationTask (line 6417)

<!-- From background.js restoreSessionsFromStorage: reads back persisted sessions -->
<!-- createSession({...persistedSession, isRestored: true}) -- if mode is undefined, defaults to 'autopilot' -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mode field to persistableSession in persistSession()</name>
  <read_first>background.js lines 2786-2812 (persistableSession object construction)</read_first>
  <files>background.js</files>
  <action>
In the persistSession() function (background.js ~line 2773), add `mode: session.mode,` to the persistableSession object. Place it after the `status` field (line 2791) since mode is a session identity field alongside sessionId, task, tabId, and status.

The exact insertion point is after line 2791 (`status: session.status,`):
```
      mode: session.mode,
```

This ensures that when chrome.storage.session.set writes the session, the mode value ('autopilot', 'mcp-manual', 'mcp-agent', or 'dashboard-remote') is preserved. On restore, createSession({...persistedSession, isRestored: true}) will receive the correct mode instead of falling back to the SESSION_FIELDS default of 'autopilot'.

Do NOT modify any other part of persistSession(). Do NOT refactor persistSession to use getWarmFields() -- that is a larger change outside this gap closure scope.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Extensions/FSB && grep -n "mode: session.mode" background.js | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep confirms `mode: session.mode` appears inside the persistableSession object (between lines 2787-2811)
    - No other lines in persistSession() are modified
    - The mode field appears alongside session identity fields (sessionId, task, tabId, status)
  </acceptance_criteria>
  <done>persistableSession object includes mode: session.mode, ensuring restored sessions preserve their original execution mode across service worker restarts</done>
</task>

</tasks>

<verification>
1. `grep -n "mode: session.mode" background.js` returns a match inside persistSession()
2. The persistableSession object now includes mode alongside sessionId, task, tabId, status
3. No syntax errors: `node -c background.js` (basic syntax check, may warn about Chrome APIs but should not error on JS syntax)
</verification>

<success_criteria>
- persistSession() writes session.mode to chrome.storage.session
- Restored sessions via createSession({...persistedSession}) receive the persisted mode value
- ADOPT-05 fully satisfied: session.mode set on creation AND preserved across SW restarts
</success_criteria>

<output>
After completion, create `.planning/phases/161-module-adoption/161-03-SUMMARY.md`
</output>
