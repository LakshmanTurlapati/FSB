---
phase: 03-dom-change-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content.js
  - background.js
autonomous: true

must_haves:
  truths:
    - "Scrolling the page does not trigger false 'everything changed' signals -- element fingerprints use structural path, not viewport-relative position"
    - "The multi-signal hash detects content changes (text typed into a field, success message replacing an error) that the old hash missed"
    - "The multi-signal hash detects interaction state changes (button disabled after submit, checkbox toggle) that the old hash missed"
    - "The multi-signal hash detects modal/toast appearance even when element count is identical"
  artifacts:
    - path: "content.js"
      provides: "Structural-path-based hashElement (DOMStateManager class method ~line 222)"
      contains: "parentTag.*parentRole.*formId"
    - path: "content.js"
      provides: "Structural-path-based hashElement (standalone function ~line 8447)"
      contains: "parentTag.*parentRole.*formId"
    - path: "content.js"
      provides: "Position-free hasElementChanged (~line 261)"
      contains: "hasElementChanged"
    - path: "background.js"
      provides: "createDOMSignals() returning multi-channel signal object"
      contains: "createDOMSignals"
    - path: "background.js"
      provides: "quickHash utility function"
      contains: "quickHash"
    - path: "background.js"
      provides: "compareSignals() for channel-wise signal comparison"
      contains: "compareSignals"
  key_links:
    - from: "content.js DOMStateManager.hashElement()"
      to: "content.js DOMStateManager.computeDiff()"
      via: "computeDiff calls hashElement for element map keys"
      pattern: "this\\.hashElement\\(element\\)"
    - from: "content.js standalone hashElement()"
      to: "content.js diffDOM()"
      via: "diffDOM calls standalone hashElement for map keys"
      pattern: "hashElement\\(el\\)"
    - from: "background.js createDOMSignals()"
      to: "background.js automation loop (~line 5055)"
      via: "replaces createDOMHash() call site"
      pattern: "createDOMSignals\\(domResponse\\.structuredDOM\\)"
---

<objective>
Replace position-based element fingerprinting with structural-path fingerprinting in content.js, and replace the single-value DOM hash in background.js with a multi-signal system that detects content, interaction state, and page state changes independently.

Purpose: The current hashElement() uses viewport-relative position (x,y) causing every element hash to invalidate on scroll. The current createDOMHash() only samples element types and counts, missing content changes, interaction state transitions, and modal/toast appearances. This plan fixes both the fingerprinting foundation and the signal generation layer.

Output: Updated content.js with structural-path hashElement (both class method and standalone), updated hasElementChanged without position sensitivity, and updated background.js with createDOMSignals(), compareSignals(), quickHash(), and session property changes from lastDOMHash to lastDOMSignals.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dom-change-detection/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace position-based element fingerprinting with structural-path fingerprinting in content.js</name>
  <files>content.js</files>
  <action>
  Modify three functions in content.js:

  1. **DOMStateManager.hashElement() (~line 222-237):** Replace the current implementation that uses `element.position?.x, element.position?.y` and `element.class` with structural-path-based fingerprinting. The new hash string must use these fields in this order: `type`, `id` (stable ID), `attributes['data-testid']`, `attributes.role`, `attributes.name`, `context.parentContext.tag:context.parentContext.role`, `formId`, `text` (first 30 chars). Keep the DJB2 hash algorithm and `'h' + Math.abs(hash).toString(36)` return format unchanged.

  Exact new hash string: `${type}|${stableId}|${testId}|${role}|${name}|${parentTag}:${parentRole}|${formId}|${text}`

  Do NOT use `position.x`, `position.y`, or `class` in the fingerprint -- these are scroll-dependent and framework-toggled respectively.

  2. **Standalone hashElement() (~line 8447-8449):** Replace the current one-liner that uses `element.type-element.id-element.class-element.text-element.position.x-element.position.y` with structural-path fingerprinting using the same fields as the class method. Note: this standalone version returns the raw string directly (not hashed), because diffDOM() uses it as a Map key. Use the same field composition: `${type}|${stableId}|${testId}|${role}|${name}|${parentTag}:${parentRole}|${formId}|${text}`.

  3. **DOMStateManager.hasElementChanged() (~line 261-280):** Remove the position change check (the `Math.abs((current.position?.x || 0) - (previous.position?.x || 0)) > 10` block, lines 266-268). Keep text change check, visibility change check, interactionState check (via shallowEqual), and attributes check (via shallowEqual). This removes scroll-induced false positives from the element change detection path.

  Safety: These changes affect `computeDiff()` and `diffDOM()` which are used during DOM state tracking. Since both hash functions now use the same structural fields, identity semantics remain consistent across both diff paths.
  </action>
  <verify>
  Search content.js for `position?.x` and `position.x` and `position.y` within the three modified functions -- none should remain in hashElement or hasElementChanged. Verify the standalone hashElement still returns a string (not a number), and the class method still returns `'h' + ...toString(36)`. Grep for `element.class` in hashElement -- should not appear.
  </verify>
  <done>
  Both hashElement functions produce structural-path fingerprints that are identical in field composition. hasElementChanged no longer triggers on position changes. Scrolling will not invalidate element identity maps.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace createDOMHash with multi-signal createDOMSignals in background.js</name>
  <files>background.js</files>
  <action>
  Modify background.js to replace the single-value DOM hash with a multi-signal system:

  1. **Add quickHash() utility** (place just above the current createDOMHash at ~line 4491):
  ```
  function quickHash(str) {
    let hash = 0;
    for (let i = 0; i < str.length; i++) {
      hash = ((hash << 5) - hash) + str.charCodeAt(i);
      hash = hash & hash;
    }
    return hash;
  }
  ```

  2. **Replace createDOMHash() (~line 4494-4526) with createDOMSignals():**
  Return an object with 4 signal channels + raw data for downstream descriptor generation:
  - **structural**: quickHash of top 5 element types by count (same logic as old hash's topTypes)
  - **content**: quickHash of text/value sampled from first 15 interactive elements (filter for isInput, isButton, or elements with role in ['alert','status','dialog','alertdialog'], sort by elementId for stable ordering, take first 15). For inputs: `name||id:value(20chars)`. For others: `text(30chars)`. Exclude elements whose text is purely numeric (timestamps, counters). Join with `|`.
  - **interaction**: quickHash of disabled/checked/readonly state from first 20 input/button elements. Format: `id||type:D/C/R` flags. Explicitly EXCLUDE `focused` state (focus changes every iteration from AI actions -- see Pitfall 3 in research).
  - **pageState**: quickHash of JSON.stringify of `{ urlPath, title, elementCount, hasModal, hasAlert, captchaPresent }` where hasModal = any element with role=dialog/alertdialog, hasAlert = any element with role=alert/status.
  - **_raw**: store `{ topTypes, elementCount, pageStateFlags }` for use by change descriptor generation in Plan 02.

  Keep the old `createDOMHash` function name as a wrapper that calls `createDOMSignals` and returns a combined hash string for backward compatibility with `automationLogger.logIteration()` and `stateHistory.domHash`. The wrapper: `function createDOMHash(domState) { const signals = createDOMSignals(domState); return ''+signals.structural+signals.content+signals.interaction+signals.pageState; }`

  3. **Add compareSignals()** (place after createDOMSignals):
  Compare two signal objects channel by channel. Return `{ changed: boolean, channels: string[] }`. If previous is null/undefined, return `{ changed: true, channels: ['initial'], summary: 'First DOM snapshot' }`.

  4. **Update session initialization (~line 3320):** Add `lastDOMSignals: null` alongside existing `lastDOMHash: null`. Both properties must exist -- `lastDOMHash` for backward compat, `lastDOMSignals` for the new system.

  5. **Update session reset on redirect (~line 5441):** Add `session.lastDOMSignals = null;` alongside the existing `session.lastDOMHash = null;`.

  6. **Update the main automation loop (~line 5055):** After `const currentDOMHash = createDOMHash(...)`, add:
  ```
  const currentDOMSignals = createDOMSignals(domResponse.structuredDOM);
  ```
  Then after the stuck detection block and before `session.lastDOMHash = currentDOMHash;` (~line 5126), add:
  ```
  session.lastDOMSignals = currentDOMSignals;
  ```

  7. **Update progress tracking (~line 6016):** The `iterationStats.domChanged` line calls `createDOMHash()` again. This is fine -- the wrapper still works. No change needed here.

  Do NOT remove any existing properties or break the logging calls that use `currentDOMHash` as a string.
  </action>
  <verify>
  Search background.js for `createDOMSignals` -- should appear (function definition + call site). Search for `compareSignals` -- should appear (function definition). Search for `lastDOMSignals` -- should appear in session init, session reset, and the automation loop assignment. Verify `createDOMHash` still exists as a wrapper returning a string. Verify `quickHash` exists.
  </verify>
  <done>
  background.js has createDOMSignals() producing a 4-channel signal object, compareSignals() for channel-wise comparison, quickHash() for fast string hashing. Session tracks lastDOMSignals alongside lastDOMHash. The old createDOMHash wrapper maintains backward compatibility with logging and stateHistory.
  </done>
</task>

</tasks>

<verification>
1. Grep content.js for `position?.x` and `position.x` in hashElement functions -- zero matches expected within hashElement/hasElementChanged
2. Grep content.js for `element.class` in hashElement functions -- zero matches expected
3. Grep background.js for `createDOMSignals` -- function definition + at least 1 call site
4. Grep background.js for `compareSignals` -- function definition present
5. Grep background.js for `lastDOMSignals` -- at least 3 occurrences (init, reset, assignment)
6. Grep background.js for `createDOMHash` -- still exists as wrapper function
7. Verify no new files were created (all changes in existing files)
</verification>

<success_criteria>
- Both hashElement functions in content.js use structural-path fingerprinting (type, id, testid, role, name, parent context, formId, text) with NO position or class fields
- hasElementChanged in content.js does NOT check position changes
- background.js has createDOMSignals() returning {structural, content, interaction, pageState, _raw}
- background.js has compareSignals() returning {changed, channels}
- Session object tracks lastDOMSignals alongside lastDOMHash
- createDOMHash still exists as a backward-compatible wrapper returning a string
- No new files created, no new dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/03-dom-change-detection/03-01-SUMMARY.md`
</output>
