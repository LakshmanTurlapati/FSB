---
phase: 03-dom-change-detection
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - background.js
  - ai/ai-integration.js
autonomous: true

must_haves:
  truths:
    - "After clicking 'Send' on a compose form, the change detector reports specific signals like 'elements removed, page content changed' -- not just domChanged: true"
    - "When a success toast appears or a modal opens, the change detector flags the specific state change even if element count is identical"
    - "The stuck detector no longer fires false positives when the AI successfully completed actions that changed content but not element types/counts"
    - "The AI prompt shows structured change information: 'DOM changed: Yes -- page content changed; modal/dialog opened' instead of 'DOM changed: Yes/No'"
    - "When the structural channel changes, the descriptor reports WHICH element types appeared or disappeared (e.g., 'dialog elements appeared', '3 input fields removed') by comparing topTypes between snapshots"
  artifacts:
    - path: "background.js"
      provides: "Structured change descriptor generation from signal comparison with topTypes diffing"
      contains: "changeSignals"
    - path: "background.js"
      provides: "Multi-signal stuck detection logic replacing single-hash comparison"
      contains: "substantiveChannels"
    - path: "ai/ai-integration.js"
      provides: "formatChangeInfo helper for AI prompts"
      contains: "formatChangeInfo"
    - path: "ai/ai-integration.js"
      provides: "Updated buildMinimalUpdate using changeSignals"
      contains: "changeSignals"
    - path: "ai/ai-integration.js"
      provides: "Updated buildPrompt using changeSignals"
      contains: "changeSignals"
  key_links:
    - from: "background.js compareSignals()"
      to: "background.js context.changeSignals"
      via: "Signal comparison result feeds change descriptor generation in automation loop"
      pattern: "compareSignals\\(currentDOMSignals"
    - from: "background.js context.changeSignals"
      to: "ai/ai-integration.js buildMinimalUpdate()"
      via: "context object passed to AI integration, changeSignals read in prompt construction"
      pattern: "context\\.changeSignals"
    - from: "background.js context.changeSignals"
      to: "ai/ai-integration.js buildPrompt()"
      via: "context object passed to AI integration, changeSignals read in prompt construction"
      pattern: "context\\.changeSignals"
    - from: "background.js multi-signal stuck detection"
      to: "background.js session.stuckCounter"
      via: "Channel-aware stuck logic replaces single-boolean comparison"
      pattern: "substantiveChannels\\.length"
    - from: "background.js createDOMSignals()._raw.topTypes"
      to: "background.js structural channel descriptor"
      via: "topTypes string parsed and diffed to report which element types appeared/disappeared"
      pattern: "prevTopTypes|currTopTypes"
    - from: "background.js domChanged local variable"
      to: "background.js iterationStats.domChanged (~line 6016)"
      via: "domChanged computed from changeResult.changed is reused for progress tracking without redundant createDOMHash call"
      pattern: "domChanged"
---

<objective>
Wire the multi-signal change detection system into the stuck detector, context assembly, and AI prompts -- replacing the boolean domChanged flag with structured change descriptors that tell the AI what appeared, disappeared, or changed.

Purpose: Plan 01 built the data layer (structural fingerprints + multi-signal hash). This plan builds the consumer layer: generating human-readable change summaries from signal comparison, rewiring stuck detection to use channel-aware logic (eliminating false stuck positives), passing changeSignals through the context object, and updating both AI prompt functions to display structured change information.

Output: Updated background.js with change descriptor generation and multi-signal stuck detection. Updated ai/ai-integration.js with formatChangeInfo() helper and updated buildMinimalUpdate/buildPrompt to show structured change info.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dom-change-detection/03-RESEARCH.md
@.planning/phases/03-dom-change-detection/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate structured change descriptors and rewire stuck detection in background.js</name>
  <files>background.js</files>
  <action>
  Modify the automation loop in background.js to generate structured change descriptors from signal comparison and consume them in stuck detection:

  1. **Generate change descriptors (~after line 5055 where currentDOMSignals is created in Plan 01):**
  After computing `currentDOMSignals`, call `compareSignals(currentDOMSignals, session.lastDOMSignals)` to get `changeResult = { changed, channels }`. Then build a `changeSignals` object with a human-readable `summary` array:

  ```
  const changeSignals = {
    changed: changeResult.changed,
    channels: changeResult.channels,
    summary: []
  };
  ```

  Populate `summary` based on which channels changed:
  - If `structural` changed: Compare `currentDOMSignals._raw.topTypes` vs `session.lastDOMSignals._raw?.topTypes` to report WHICH element types appeared or disappeared, not just counts. Parse both topTypes strings (format: `"button:12,input:8,link:5,div:3,select:2"`) into Maps. Diff the Maps to find:
    - Types present in current but not previous: push `"${type} elements appeared"` (e.g., "dialog elements appeared")
    - Types present in previous but not current: push `"${type} elements removed"` (e.g., "form elements removed")
    - Types with count changes: push `"${countDelta} ${type} elements ${countDelta > 0 ? 'added' : 'removed'}"` (e.g., "3 input elements added")
    - If topTypes are identical but structural hash still changed (unlikely edge case): push `"element structure changed"`

    Also report the overall element count delta for context:
    Compare `currentDOMSignals._raw.elementCount` vs `session.lastDOMSignals._raw?.elementCount`. If different, append `"(${Math.abs(countDelta)} elements net ${countDelta > 0 ? 'added' : 'removed'})"` to the last structural summary item.

    Implementation sketch for topTypes diffing:
    ```
    function parseTopTypes(topTypesStr) {
      const map = new Map();
      if (!topTypesStr) return map;
      for (const entry of topTypesStr.split(',')) {
        const [type, count] = entry.split(':');
        if (type && count) map.set(type.trim(), parseInt(count, 10));
      }
      return map;
    }
    ```
    Place `parseTopTypes` as a local helper near `createDOMSignals`/`compareSignals`.

  - If `content` changed: push "page content changed (text or input values)".
  - If `interaction` changed: push "element states changed (disabled, checked, or readonly)".
  - If `pageState` changed: compare `_raw.pageStateFlags` objects. Report specific changes: "modal/dialog opened", "modal/dialog closed", "alert/status message appeared", "alert/status message removed", `title changed to "..."`.

  If `changeResult.channels` is `['initial']`, set summary to `['First DOM snapshot -- no comparison available']`.

  2. **Derive domChanged from changeResult and store it as a local variable for reuse:**
  Right after building changeSignals, compute:
  ```
  let domChanged = changeResult.changed;
  ```
  This local `domChanged` variable will be reused in THREE places: (a) stuck detection, (b) context assembly, (c) progress tracking at ~line 6016. This eliminates the need for redundant `createDOMHash()` calls or `session._lastChangeResult` workarounds.

  3. **Replace stuck detection block (~lines 5073-5126) with multi-signal logic:**
  Replace the current `if (!domChanged && !urlChanged)` block with channel-aware stuck detection:

  - If `!changeResult.changed && !urlChanged`: genuinely stuck. Keep the existing typing-sequence special-case logic (lines 5078-5113) as a safety net -- it catches edge cases where typing doesn't change any sampled content. Increment `stuckCounter` via the same paths.

  - If `changeResult.changed`: check which channels changed.
    - If substantive channels changed (structural, content, pageState -- anything except interaction alone): `session.stuckCounter = 0` (definite progress, reset counter).
    - If ONLY interaction changed (focus moved, element state toggled): `session.stuckCounter = Math.max(0, session.stuckCounter - 1)` (reduce penalty by 1, don't fully reset).

  - Keep the `else` branch at line 5122-5124 that resets stuckCounter to 0 on URL change -- urlChanged already handles that.

  4. **Update context assembly (~line 5376-5401):**
  Add `changeSignals` to the context object alongside the existing `domChanged`:
  ```
  changeSignals,          // Structured: { changed, channels, summary }
  domChanged,             // Boolean backward compat (derived from changeResult.changed)
  ```
  The `domChanged` property now comes from `changeResult.changed` instead of hash string comparison. Both must be present.

  5. **Update progress tracking (~line 6016):**
  The `iterationStats.domChanged` at line 6016 currently calls `createDOMHash()` redundantly. Replace it with the already-computed `domChanged` local variable:
  ```
  domChanged: domChanged,
  ```
  This is the simplest fix: `domChanged` is already computed earlier in the same iteration from `changeResult.changed` and is in scope at line 6016. No need for `session._lastChangeResult` or re-calling `createDOMSignals()`. The old `createDOMHash()` call at this line is removed.

  Do NOT remove the typing-sequence safety-net code -- it covers edge cases where content sampling misses the change. Do NOT remove `domChanged` from the context object.
  </action>
  <verify>
  Search background.js for `changeSignals` -- should appear in descriptor generation and context assembly. Search for `substantiveChannels` -- should appear in stuck detection. Search for `compareSignals(` -- should have at least 1 call site in the automation loop. Verify `domChanged` still appears in the context object. Verify the typing-sequence block (searching for `isTypingSequence`) still exists as safety net. Search for `parseTopTypes` -- should appear as helper function. Search for `prevTopTypes` or `currTopTypes` -- should appear in structural descriptor logic confirming topTypes consumption. Verify line ~6016 uses `domChanged` variable directly (not `createDOMHash()` or `session._lastChangeResult`).
  </verify>
  <done>
  The automation loop generates structured change descriptors with human-readable summaries that report WHICH element types appeared/disappeared (e.g., "dialog elements appeared", "3 input elements added") by diffing topTypes from _raw, not just element counts. Stuck detection uses channel-aware logic: substantive changes (structural/content/pageState) reset the counter, interaction-only changes reduce it, and no-change increments it (with typing-sequence safety net preserved). The context object carries both changeSignals and backward-compatible domChanged boolean. Progress tracking at ~line 6016 reuses the already-computed `domChanged` local variable with no redundant hash calls.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update AI prompts to display structured change information in ai-integration.js</name>
  <files>ai/ai-integration.js</files>
  <action>
  Modify ai/ai-integration.js to consume the new changeSignals from the context object:

  1. **Add formatChangeInfo() helper** (place near the top of the class or as a standalone function before buildMinimalUpdate):
  ```
  formatChangeInfo(context) {
    const cs = context?.changeSignals;
    if (!cs) {
      // Fallback for backward compatibility if changeSignals not present
      return `DOM changed: ${context?.domChanged ? 'Yes' : 'No'}`;
    }
    if (!cs.changed) {
      return 'DOM changed: No (page appears unchanged since your last action)';
    }
    let info = 'DOM changed: Yes';
    if (cs.summary && cs.summary.length > 0) {
      info += ' -- ' + cs.summary.join('; ');
    }
    return info;
  }
  ```

  If adding as a class method, use `this.formatChangeInfo(context)`. If standalone, just `formatChangeInfo(context)`. Choose class method to keep it co-located with buildMinimalUpdate/buildPrompt.

  2. **Update buildMinimalUpdate() (~line 345):**
  Replace `DOM changed: ${context?.domChanged ? 'Yes' : 'No'}` with a call to formatChangeInfo:
  ```
  ${this.formatChangeInfo(context)}
  ```
  This line is in the template string that builds the `update` variable.

  3. **Update buildPrompt() (~line 1693):**
  Replace `userPrompt += \`\nDOM changed since last action: ${context.domChanged ? 'Yes' : 'No'}\`` with:
  ```
  userPrompt += `\n${this.formatChangeInfo(context)}`;
  ```

  The AI will now see: "DOM changed: Yes -- dialog elements appeared; page content changed; modal/dialog opened" instead of just "DOM changed: Yes". When nothing changed, it sees: "DOM changed: No (page appears unchanged since your last action)" which is more informative than just "No".

  Do NOT change any other part of buildMinimalUpdate or buildPrompt. The budget-partitioned structure from Phase 2 must remain intact. Only the single domChanged line in each function changes.
  </action>
  <verify>
  Search ai-integration.js for `formatChangeInfo` -- should appear as method definition + 2 call sites (buildMinimalUpdate, buildPrompt). Search for the OLD pattern `context?.domChanged ? 'Yes' : 'No'` -- should NOT appear (replaced by formatChangeInfo). Search for `context.domChanged ? 'Yes' : 'No'` -- should NOT appear. Verify `changeSignals` is referenced inside formatChangeInfo.
  </verify>
  <done>
  Both buildMinimalUpdate and buildPrompt display structured change information via formatChangeInfo(). The AI receives descriptive change summaries like "DOM changed: Yes -- dialog elements appeared; 3 input elements added; page content changed" instead of a bare boolean. Backward compatibility maintained via fallback when changeSignals is absent.
  </done>
</task>

</tasks>

<verification>
1. Grep background.js for `changeSignals` -- at least 3 occurrences (generation, context assembly, descriptor population)
2. Grep background.js for `compareSignals(` -- at least 1 call site
3. Grep background.js for `substantiveChannels` -- appears in stuck detection
4. Grep background.js for `isTypingSequence` -- still present (safety net preserved)
5. Grep background.js for `parseTopTypes` -- appears as helper function for topTypes diffing
6. Grep background.js for structural descriptor logic that consumes `_raw.topTypes` -- confirms Plan 01's topTypes data is consumed
7. Grep ai-integration.js for `formatChangeInfo` -- method definition + 2 call sites
8. Grep ai-integration.js for `domChanged ? 'Yes' : 'No'` -- zero matches (all replaced)
9. Grep ai-integration.js for `changeSignals` -- referenced in formatChangeInfo
10. Verify background.js context object has both `changeSignals` and `domChanged` properties
11. Verify progress tracking (~line 6016) uses `domChanged` local variable directly, not `createDOMHash()` or `session._lastChangeResult`
12. Verify no new files were created (all changes in existing files)
</verification>

<success_criteria>
- background.js generates structured change descriptors with human-readable summaries that report WHICH element types appeared/disappeared by diffing topTypes
- The _raw.topTypes stored by Plan 01's createDOMSignals is explicitly consumed by Plan 02's structural channel descriptor via parseTopTypes diffing
- Stuck detection uses channel-aware logic: substantive changes reset counter, interaction-only reduces it, no-change increments it
- Typing-sequence safety-net code is preserved as fallback
- Context object carries both changeSignals (structured) and domChanged (boolean backward compat)
- Progress tracking at ~line 6016 reuses the already-computed domChanged local variable (no redundant createDOMHash call, no session._lastChangeResult indirection)
- ai-integration.js formatChangeInfo() method renders structured change info for AI prompts
- Both buildMinimalUpdate and buildPrompt use formatChangeInfo instead of bare boolean
- Backward compatibility maintained: if changeSignals absent, falls back to domChanged boolean display
- No new files created, no new dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/03-dom-change-detection/03-02-SUMMARY.md`
</output>
