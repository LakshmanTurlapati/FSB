---
phase: 05-dead-code-removal-and-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/accessibility.js
  - content.js.bak
  - utils/dom-state-manager.js
autonomous: true

must_haves:
  truths:
    - "Searching the entire codebase for 'waitForActionable' returns zero results"
    - "No orphaned comments, TODOs, or JSDoc referencing waitForActionable remain"
    - "content.js.bak (Phase 4 backup) no longer exists in the repository"
    - "utils/dom-state-manager.js no longer exists (confirmed dead -- not imported by any JS, HTML, or JSON file)"
    - "Extension loads and runs automation tasks identically to before removal"
  artifacts:
    - path: "content/accessibility.js"
      provides: "Accessibility and readiness check functions without dead waitForActionable code"
      contains: "FSB.smartEnsureReady"
  key_links:
    - from: "content/accessibility.js"
      to: "FSB namespace"
      via: "FSB.smartEnsureReady and FSB.ensureElementReady exports"
      pattern: "FSB\\.smartEnsureReady|FSB\\.ensureElementReady"
---

<objective>
Remove all confirmed dead code from the FSB extension codebase: the waitForActionable function and its references, the content.js.bak backup file, and the orphaned utils/dom-state-manager.js file.

Purpose: Eliminate dead code that adds maintenance burden and confuses future readers. Phase 4 modularization made module boundaries clear, so dead code is now obvious.
Output: A cleaner codebase with zero references to waitForActionable and no orphaned files.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dead-code-removal-and-configuration/05-RESEARCH.md

Key source files:
@content/accessibility.js (lines 1094-1280 contain the dead code)
@content.js.bak (entire file is dead -- Phase 4 backup)
@utils/dom-state-manager.js (entire file is dead -- not in injection pipeline, not imported anywhere)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove waitForActionable and orphaned files</name>
  <files>content/accessibility.js, content.js.bak, utils/dom-state-manager.js</files>
  <action>
  **In content/accessibility.js:**
  1. Delete the entire `waitForActionable` function definition (lines 1094-1251), including:
     - The JSDoc comment block starting at line 1094 (`/** Smart wait for element...`)
     - The `NOTE: This is dead code per Phase 5 plans, but kept for now.` comment on line 1097
     - The async function body through line 1251
  2. Delete the dead-code export comment and assignment (lines 1276-1277):
     - Line 1276: `// Dead code kept for now (Phase 5 will evaluate)`
     - Line 1277: `FSB.waitForActionable = waitForActionable;`
  3. Leave the blank line between the last real export (`FSB.ensureElementReady`) and the `window.FSB._modules['accessibility']` registration line. Do NOT change any other exports.

  **Delete entire files:**
  4. Delete `content.js.bak` (486KB Phase 4 backup -- no longer needed, contains old monolithic content.js with its own copy of waitForActionable)
  5. Delete `utils/dom-state-manager.js` (standalone DOMStateManager file -- confirmed dead: not referenced in any JS, HTML, or JSON file; not in CONTENT_SCRIPT_FILES array; not in background.js importScripts; only mentioned in README.md which is documentation, not code)

  **Sweep for straggler references:**
  6. After the above removals, grep the entire codebase for:
     - `waitForActionable` -- must return zero results
     - `dom-state-manager` in *.js, *.html, *.json files -- must return zero results
     - `content.js.bak` in *.js, *.html, *.json files -- must return zero results
  7. If any references are found in code files (not README.md), remove them. README.md references to dom-state-manager.js are documentation and will be cleaned up separately if needed, but do NOT modify README.md in this task.

  **Preservation rules (per user decision):**
  - Do NOT remove `speedMode` from config.js or options.js -- it has active legacy migration logic
  - Do NOT remove any scaffolding/placeholders related to future background agent functionality
  - Do NOT modify any other functions or exports in accessibility.js
  </action>
  <verify>
  Run these grep commands and confirm zero results for each:
  ```bash
  grep -r "waitForActionable" --include="*.js" --include="*.html" --include="*.json" .
  grep -r "dom-state-manager" --include="*.js" --include="*.html" --include="*.json" .
  ```
  Confirm files are deleted:
  ```bash
  test ! -f content.js.bak && echo "DELETED" || echo "STILL EXISTS"
  test ! -f utils/dom-state-manager.js && echo "DELETED" || echo "STILL EXISTS"
  ```
  Verify accessibility.js still has all valid exports (smartEnsureReady, ensureElementReady, etc.) by grepping:
  ```bash
  grep "FSB\." content/accessibility.js | grep -v "//\|FSB.tools\|FSB.querySelectorWithShadow\|FSB._modules"
  ```
  This should show all the real exports (getInputRole, getImplicitRole, computeAccessibleName, etc.) and NOT show waitForActionable.
  </verify>
  <done>
  - Zero results for `waitForActionable` across the entire codebase (*.js, *.html, *.json)
  - content.js.bak file deleted
  - utils/dom-state-manager.js file deleted
  - All valid accessibility.js exports (smartEnsureReady, ensureElementReady, isElementActionable, etc.) are preserved
  - No orphaned comments or TODOs referencing the removed code remain
  </done>
</task>

<task type="auto">
  <name>Task 2: Broad dead code sweep across all extension files</name>
  <files>content/, background.js, ai/ai-integration.js, ui/popup.js, ui/sidepanel.js, ui/options.js, config/init-config.js, config/secure-config.js, utils/automation-logger.js, utils/analytics.js, content/actions.js</files>
  <action>
  Perform a logical dead code sweep across all extension JavaScript files listed below. For each file:
  1. Look for exported functions/variables that have zero consumers
  2. Look for internal helper functions that are never called
  3. Look for unreachable code paths (dead branches, always-false conditions)
  4. Look for orphaned TODOs, FIXMEs, or comments referencing removed features

  **Files to sweep (research flagged "needs verification sweep"):**
  - `ui/popup.js` -- check for unreferenced functions
  - `ui/sidepanel.js` -- check for unreferenced functions
  - `config/init-config.js` -- check for dead initialization paths
  - `config/secure-config.js` -- check for unreferenced encryption methods
  - `utils/automation-logger.js` -- check for dead log handlers
  - `utils/analytics.js` -- check for dead tracking methods
  - `content/actions.js` -- check for internal dead paths

  **Files already confirmed clean by research (skip deep analysis, just verify):**
  - `background.js` -- research says no dead code found
  - `ai/ai-integration.js` -- speedMode migration is still needed, skip
  - `ai/universal-provider.js` -- research says clean
  - `ai/ai-providers.js` -- research says clean
  - `content/utils.js`, `content/dom-state.js`, `content/selectors.js`, `content/visual-feedback.js`, `content/dom-analysis.js`, `content/messaging.js`, `content/lifecycle.js`, `content/init.js` -- all exports verified by research

  **For each dead code item found:**
  - Verify it truly has zero callers (grep for the function/variable name)
  - If confirmed dead, remove it
  - If uncertain (might be called dynamically or via string reference), leave it with a brief comment

  **Preservation rules:**
  - Do NOT remove `speedMode` anywhere -- active legacy migration
  - Do NOT remove background agent scaffolding/placeholders
  - Do NOT modify README.md

  **Expected outcome:** Research suggests no additional dead code beyond waitForActionable (already handled in Task 1). This task is the verification sweep to confirm that expectation. If nothing is found, that is a valid result -- log that the sweep was clean.
  </action>
  <verify>
  For any dead code removed, grep for the removed function/variable name to confirm zero remaining references:
  ```bash
  grep -r "FUNCTION_NAME" --include="*.js" .
  ```
  If no dead code was found, verify the sweep was thorough by confirming at least the following files were checked:
  ```bash
  for f in ui/popup.js ui/sidepanel.js config/init-config.js config/secure-config.js utils/automation-logger.js utils/analytics.js content/actions.js; do
    test -f "$f" && echo "CHECKED: $f"
  done
  ```
  </verify>
  <done>
  - All files in the sweep list have been analyzed for dead code
  - Any confirmed dead code has been removed (or documented as clean if none found)
  - No functions with zero callers remain (except speedMode migration and background agent scaffolding, which are explicitly preserved)
  - Orphaned TODOs/comments referencing removed features are cleaned up
  </done>
</task>

</tasks>

<verification>
1. `grep -r "waitForActionable" --include="*.js" --include="*.html" --include="*.json" .` returns zero results
2. `content.js.bak` does not exist
3. `utils/dom-state-manager.js` does not exist
4. `content/accessibility.js` still exports: getInputRole, getImplicitRole, computeAccessibleName, getARIARelationships, isElementActionable, checkElementVisibility, checkElementEnabled, checkElementStable, detectCodeEditor, checkElementReceivesEvents, checkElementEditable, scrollIntoViewIfNeeded, performQuickReadinessCheck, smartEnsureReady, ensureElementReady
5. No new console errors when loading the extension
</verification>

<success_criteria>
- Zero results for "waitForActionable" in the entire codebase
- content.js.bak and utils/dom-state-manager.js deleted
- All other extension functionality unchanged
- Dead code sweep completed across all flagged files with results documented
</success_criteria>

<output>
After completion, create `.planning/phases/05-dead-code-removal-and-configuration/05-01-SUMMARY.md`
</output>
