---
phase: 162.2-auth-wall-handoff-with-result-preservation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai/tool-definitions.js
  - ai/agent-loop.js
  - site-guides/social/_shared.js
  - site-guides/career/_shared.js
  - site-guides/productivity/google-docs.js
autonomous: true
gap_closure: true
requirements:
  - AUTH-02
must_haves:
  truths:
    - "The model is explicitly told to end login, credential, skipped-login, and manual-approval blockers with `partial_task` once useful work is complete, not `fail_task`."
    - "`partial_task` for auth blockers is explicitly sequenced after one available saved-credential or operator-prompt attempt is exhausted, skipped, unavailable, or fails."
    - "Auth-wall handoff payloads consistently preserve what was completed, the exact blocker, and the manual next step the user can take."
    - "Existing `AUTH REQUIRED` and `SKIP-AUTH` site-guide language reinforces the same partial/manual-handoff behavior across social, career, and Google Docs flows."
  artifacts:
    - path: "ai/tool-definitions.js"
      provides: "Auth-aware `partial_task` guidance and blocker taxonomy"
      contains: "partial_task"
    - path: "ai/agent-loop.js"
      provides: "System-prompt rules for auth-wall/manual-handoff outcomes"
      contains: "Call partial_task"
    - path: "site-guides/social/_shared.js"
      provides: "Shared social auth-wall guidance aligned to preserved handoff behavior"
      contains: "require login"
    - path: "site-guides/career/_shared.js"
      provides: "Career `AUTH REQUIRED` reporting aligned to resumable manual handoff"
      contains: "AUTH REQUIRED"
    - path: "site-guides/productivity/google-docs.js"
      provides: "Google Docs `SKIP-AUTH` guidance aligned to the same partial contract"
      contains: "SKIP-AUTH"
  key_links:
    - from: "ai/tool-definitions.js"
      to: "ai/agent-loop.js"
      via: "tool description plus system prompt define when auth blockers must terminate with `partial_task`"
      pattern: "partial_task"
    - from: "site-guides/career/_shared.js"
      to: "ai/agent-loop.js"
      via: "site guidance injected into the prompt keeps `AUTH REQUIRED` flows resumable instead of crash-framed"
      pattern: "AUTH REQUIRED"
    - from: "site-guides/productivity/google-docs.js"
      to: "ai/agent-loop.js"
      via: "existing `SKIP-AUTH` guidance feeds the same auth-wall partial outcome semantics"
      pattern: "SKIP-AUTH"
---

<objective>
Define the exact auth-wall partial/manual-handoff contract the model should follow so auth-gated final steps preserve useful work instead of collapsing into generic failure.

Purpose: Phase 162.1 created the generic partial outcome transport, but AUTH-02 remains open until the prompt, tool contract, and shared site guidance explicitly teach the model when and how to use that lifecycle for auth walls.
Output: Tightened `partial_task` auth-wall guidance in `ai/*` plus aligned shared site-guide wording for `AUTH REQUIRED` and `SKIP-AUTH` flows.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v0.9.24-MILESTONE-AUDIT.md
@.planning/debug/auth-blocked-partial-outcome-lifecycle.md
@.planning/phases/162.2-auth-wall-handoff-with-result-preservation/162.2-CONTEXT.md
@.planning/phases/162.1-partial-completion-lifecycle/162.1-01-SUMMARY.md
@.planning/phases/162.1-partial-completion-lifecycle/162.1-02-SUMMARY.md

@ai/tool-definitions.js
@ai/agent-loop.js
@site-guides/social/_shared.js
@site-guides/career/_shared.js
@site-guides/productivity/google-docs.js

<interfaces>
From ai/tool-definitions.js:
```javascript
{
  name: 'partial_task',
  description: 'Signal that the task is partially complete because useful work was completed but an external blocker prevents the final step.',
  inputSchema: {
    properties: {
      summary: { type: 'string' },
      blocker: { type: 'string' },
      next_step: { type: 'string' },
      reason: { type: 'string', description: '... "blocked", "auth_required", "manual_approval"' }
    }
  }
}
```

From ai/agent-loop.js:
```javascript
8. Call complete_task with a summary when the FULL task is done.
9. Call partial_task when useful work is done but an external blocker prevents the final step. Include what you completed and the blocker.
10. Call fail_task with a reason if you cannot complete it and there is no useful partial outcome to deliver.
```

From site-guides/career/_shared.js:
```javascript
- If login/auth wall blocks access: report "AUTH REQUIRED: [Company] requires login to view job listings. Career page: [URL]"
```

From site-guides/productivity/google-docs.js:
```javascript
- Expected outcome: SKIP-AUTH (Google account required for editing) or PARTIAL (navigation and text reading works but editing blocked)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten the auth-wall partial-task contract in tool docs and system prompt</name>
  <files>
ai/tool-definitions.js
ai/agent-loop.js
  </files>
  <read_first>
ai/tool-definitions.js
ai/agent-loop.js
.planning/phases/162.2-auth-wall-handoff-with-result-preservation/162.2-CONTEXT.md
.planning/phases/162.1-partial-completion-lifecycle/162.1-01-SUMMARY.md
  </read_first>
  <action>
Implement D-01, D-02, D-03, D-04, and D-06 in the agent-facing contract layer.

In `ai/tool-definitions.js`, expand `partial_task` so auth-wall/manual-handoff usage is unambiguous. Keep the existing terminal channel from Phase 162.1 per D-01; do not add a new auth-specific completion tool. Make the description and schema explicitly cover the D-02 blocker set: login required, no saved credentials, user skipped login, credentials failed, and manual approval / MFA / external verification. Standardize a small reason taxonomy that later runtime work can reuse, for example `auth_required`, `credentials_missing`, `user_skipped_login`, `credentials_failed`, and `manual_approval` (Claude's discretion on the exact final set, but keep it narrow and stable).

In `ai/agent-loop.js`, strengthen `buildSystemPrompt()` so the model is told exactly when to call `partial_task`: useful work is already complete, the remaining step is auth-gated, and the user can still benefit from a preserved handoff. Make D-04 explicit in the instructions: the payload must name what was completed, what exact auth blocker stopped the last step, and what manual next step the user should take. Add one sequencing rule so this plan stays compatible with Plan 02: if the runtime offers one saved-credential or operator-prompt attempt, the model should preserve the auth blocker details and let that resolution attempt happen first; `partial_task` is the terminal outcome for unresolved auth after that attempt is exhausted, skipped, unavailable, or fails. Also state the D-06 guardrail clearly: do not keep retrying auth, do not invent broad autonomous sign-in flows, and do not treat MFA/manual approval as something to brute-force. Preserve the existing setTimeout-chained agent loop and the Phase 162.1 outcome transport.
  </action>
  <verify>
    <automated>node --check ai/tool-definitions.js && node --check ai/agent-loop.js && rg -n "partial_task|auth_required|credentials_missing|manual_approval|user_skipped_login|credentials_failed|fail_task" ai/tool-definitions.js ai/agent-loop.js</automated>
  </verify>
  <acceptance_criteria>
    - `partial_task` guidance explicitly covers auth walls, missing credentials, skipped login, credential failure, and manual approval / MFA blockers
    - The prompt explicitly sequences auth-wall `partial_task` after one available saved-credential or operator-prompt attempt is exhausted, skipped, unavailable, or fails
    - The system prompt tells the model to preserve D-04 handoff content instead of emitting generic auth failure text
    - No new auth-specific terminal tool or second completion channel is introduced
    - The prompt keeps the existing setTimeout-chained loop and does not broaden scope into general autonomous login
  </acceptance_criteria>
  <done>The agent-facing contract makes auth walls a first-class `partial_task` outcome with precise blocker and handoff expectations.</done>
</task>

<task type="auto">
  <name>Task 2: Align shared AUTH REQUIRED and SKIP-AUTH site guidance to the same handoff contract</name>
  <files>
site-guides/social/_shared.js
site-guides/career/_shared.js
site-guides/productivity/google-docs.js
  </files>
  <read_first>
site-guides/social/_shared.js
site-guides/career/_shared.js
site-guides/productivity/google-docs.js
.planning/phases/162.2-auth-wall-handoff-with-result-preservation/162.2-CONTEXT.md
.planning/debug/auth-blocked-partial-outcome-lifecycle.md
  </read_first>
  <action>
Implement D-02, D-04, D-07, and D-08 in the shared site-guide layer.

Update the shared social guidance so login-gated posting, messaging, and commenting flows tell the model to preserve whatever discovery or drafting work was already completed and then end with the auth-wall partial contract rather than a generic failure. Update the career shared guide so the existing `AUTH REQUIRED:` reporting pattern stays recognizable but now implies a preserved manual handoff when useful research is already done. Update the Google Docs guide so existing `SKIP-AUTH` guidance explicitly maps to the same partial/manual-handoff structure instead of a vague skip classification.

Keep these changes at the shared-guidance level per D-07 so LinkedIn-style messaging, career-page auth walls, and Google Docs editing blockers converge on one behavior without site-specific one-offs. Per D-08, do not add new UI instructions, new credential surfaces, or large per-site special cases. The goal is prompt alignment, not a guide explosion.
  </action>
  <verify>
    <automated>node --check site-guides/social/_shared.js && node --check site-guides/career/_shared.js && node --check site-guides/productivity/google-docs.js && rg -n "AUTH REQUIRED|SKIP-AUTH|partial_task|manual handoff|next step|login" site-guides/social/_shared.js site-guides/career/_shared.js site-guides/productivity/google-docs.js</automated>
  </verify>
  <acceptance_criteria>
    - Shared social guidance frames login-gated interaction flows as preserved handoffs when useful work is already complete
    - Career shared guidance keeps the `AUTH REQUIRED:` pattern but aligns it to partial/manual-handoff semantics
    - Google Docs `SKIP-AUTH` guidance points to the same completed-work/blocker/next-step contract
    - No new UI surface, credential vault concept, or site-specific auth automation scope is introduced
  </acceptance_criteria>
  <done>Prompt and site-guide language now converge on one precise auth-wall manual-handoff contract for the model.</done>
</task>

</tasks>

<verification>
Plan 01 closes the guidance half of AUTH-02:

1. The model is explicitly instructed to use `partial_task` for auth walls after useful work is complete.
2. The auth-wall payload contract preserves completed work, blocker, and manual next step.
3. Existing `AUTH REQUIRED` / `SKIP-AUTH` guide language points to the same outcome semantics instead of vague failure/skip wording.
</verification>

<success_criteria>
1. `ai/tool-definitions.js` and `ai/agent-loop.js` teach a consistent auth-wall `partial_task` contract with a stable blocker taxonomy.
2. Shared social, career, and Google Docs guides reinforce the same manual-handoff semantics.
3. No new terminal channel, UI redesign, or broad autonomous-login scope is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/162.2-auth-wall-handoff-with-result-preservation/162.2-01-SUMMARY.md`
</output>
