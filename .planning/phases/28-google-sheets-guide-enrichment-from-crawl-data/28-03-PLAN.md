---
phase: 28-google-sheets-guide-enrichment-from-crawl-data
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [site-guides/productivity/google-sheets.js]
autonomous: true
gap_closure: true
requirements: [P28-04]

must_haves:
  truths:
    - "The selectors map contains a 'sheetTab' (singular) key targeting '.docs-sheet-active-tab, .docs-sheet-tab'"
    - "buildGuideAnnotations() can produce [hint:sheetTab] annotations for matched sheet tab elements"
  artifacts:
    - path: "site-guides/productivity/google-sheets.js"
      provides: "sheetTab selector key in selectors map"
      contains: "sheetTab"
  key_links:
    - from: "site-guides/productivity/google-sheets.js selectors.sheetTab"
      to: "content/dom-analysis.js buildGuideAnnotations()"
      via: "selectors map iteration"
      pattern: "sheetTab.*docs-sheet-active-tab"
---

<objective>
Add the missing `sheetTab` (singular) selector key to the Google Sheets site guide selectors map to close the P28-04 verification gap.

Purpose: The sheet-tab fsbElement was added but its corresponding annotation key in the selectors map was omitted. Without it, buildGuideAnnotations() cannot produce [hint:sheetTab] annotations for the active sheet tab element.
Output: Updated google-sheets.js with sheetTab selector key
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-google-sheets-guide-enrichment-from-crawl-data/28-VERIFICATION.md

@site-guides/productivity/google-sheets.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sheetTab selector key to selectors map</name>
  <files>site-guides/productivity/google-sheets.js</files>
  <action>
In the `selectors` object of google-sheets.js, add the following key immediately after the existing `sheetTabs` entry (line 417):

```
sheetTab: '.docs-sheet-active-tab, .docs-sheet-tab',
```

This goes between the `sheetTabs: '.docs-sheet-tab',` line and the `addSheet:` line. The key targets both the active tab class and the general tab class, matching the selectors defined in the sheet-tab fsbElement.

Do NOT modify any other lines. This is a single-line addition.
  </action>
  <verify>
    <automated>grep -n "sheetTab:" site-guides/productivity/google-sheets.js | grep -v "sheetTabs" | head -5</automated>
  </verify>
  <done>The selectors map contains `sheetTab: '.docs-sheet-active-tab, .docs-sheet-tab'` as a distinct key from the existing `sheetTabs` key. P28-04 is fully satisfied.</done>
</task>

</tasks>

<verification>
- grep confirms `sheetTab:` (singular) exists as a distinct key from `sheetTabs:` (plural)
- The selector value targets `.docs-sheet-active-tab, .docs-sheet-tab`
- No syntax errors in the JS file (node -c check passes)
</verification>

<success_criteria>
- sheetTab key present in selectors map with value '.docs-sheet-active-tab, .docs-sheet-tab'
- File parses without syntax errors
- P28-04 gap closed: all 18/18 expected new selector keys now present
</success_criteria>

<output>
After completion, create `.planning/phases/28-google-sheets-guide-enrichment-from-crawl-data/28-03-SUMMARY.md`
</output>
