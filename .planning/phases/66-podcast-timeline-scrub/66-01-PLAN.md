---
phase: 66-podcast-timeline-scrub
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site-guides/media/podcast-player.js
  - background.js
autonomous: true
requirements:
  - MICRO-10

must_haves:
  truths:
    - "A podcast-player site guide exists with a scrubTimeline workflow for seeking audio to a specific timestamp via click_at on the progress bar at a calculated pixel position"
    - "The site guide documents timeline position calculation formula: targetX = trackLeft + (targetSeconds / totalSeconds) * trackWidth"
    - "The site guide covers at least 3 podcast platforms with player-specific selectors for progress bar, current time display, and total duration"
    - "The site guide is wired into background.js importScripts in the Media section alongside video-player.js"
  artifacts:
    - path: "site-guides/media/podcast-player.js"
      provides: "Podcast audio timeline scrub site guide with scrubTimeline workflow"
      contains: "scrubTimeline"
    - path: "background.js"
      provides: "importScripts entry for podcast-player.js in Media section"
      contains: "media/podcast-player"
  key_links:
    - from: "background.js"
      to: "site-guides/media/podcast-player.js"
      via: "importScripts loads the site guide module into service worker"
      pattern: "media/podcast-player"
---

<objective>
Create a podcast-player site guide with timeline scrub patterns and wire it into the extension for MICRO-10 edge case validation.

Purpose: MICRO-10 requires scrubbing a podcast audio timeline to exactly the 14:22 mark. The existing video-player.js site guide covers volume sliders but not audio timeline/progress bar seeking. A dedicated podcast-player site guide is needed that documents: podcast platform selectors (progress bar, time display, duration), timeline position calculation (targetSeconds / totalSeconds * trackWidth), and a scrubTimeline workflow using click_at on the progress bar at the calculated position. This uses the same coordinate-based interaction approach as Phase 57 (volume slider) but targets audio timeline progress bars instead of volume controls.

Output: site-guides/media/podcast-player.js, background.js wiring
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-podcast-timeline-scrub/66-CONTEXT.md
@site-guides/media/video-player.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create podcast-player site guide with timeline scrub selectors and workflows</name>
  <files>site-guides/media/podcast-player.js</files>
  <read_first>
    - site-guides/media/video-player.js (use exact same registerSiteGuide structure: site, category, patterns, guidance, selectors, workflows, warnings, toolPreferences)
    - .planning/phases/66-podcast-timeline-scrub/66-CONTEXT.md (target site decisions, timeline position formula, verification method)
  </read_first>
  <action>
Create site-guides/media/podcast-player.js following the exact registerSiteGuide pattern from video-player.js. This site guide covers podcast/audio player timeline scrubbing, not volume control.

registerSiteGuide structure:
- site: 'Podcast Audio Player'
- category: 'Media'
- patterns: regex array matching podcast platforms. Include at minimum:
  * /podbean\.com/i
  * /buzzsprout\.com/i
  * /spreaker\.com/i
  * /anchor\.fm/i (Spotify for Podcasters)
  * /transistor\.fm/i
  * /simplecast\.com/i
  * /podcasts\.apple\.com/i
  * /open\.spotify\.com.*episode/i
  * /soundcloud\.com/i
  * Also match generic HTML5 audio elements: these sites all use custom UI over HTML5 audio

- guidance: comprehensive string covering:

  AUDIO TIMELINE ANATOMY:
  - Podcast players render a horizontal progress bar (timeline/scrubber) showing playback position
  - The track is a div/span representing total episode duration, with a fill child showing elapsed time
  - Some players have a draggable thumb/handle on the timeline; others respond to click on track
  - Key attributes for time state:
    * aria-valuenow: current playback position in seconds
    * aria-valuemin: 0 (start)
    * aria-valuemax: total duration in seconds
    * data-current-time, data-duration: raw values
    * Time display elements showing "MM:SS" or "HH:MM:SS" format
    * HTML5 audio element currentTime and duration properties

  TIMELINE POSITION CALCULATION:
  - Target: 14:22 = 14 * 60 + 22 = 862 seconds
  - Formula: targetX = trackLeft + (862 / totalDurationSeconds) * trackWidth
  - targetY = trackTop + (trackHeight / 2) -- center of the track vertically
  - Need total duration from: aria-valuemax, data-duration, duration text element, or audio.duration
  - If total duration unknown, estimate from common podcast lengths (30min=1800s, 60min=3600s)

  TIMELINE SELECTORS BY PLATFORM:
  - Podbean: .progress-bar, .podcast-player [class*="progress"], [class*="seek"], input[type="range"][class*="progress"]
  - Buzzsprout: .episode-player [class*="progress"], [class*="scrubber"], [role="slider"]
  - Spreaker: .player [class*="progress"], [class*="timeline"], [role="slider"]
  - Apple Podcasts: .podcast-player [class*="progress"], [role="slider"][aria-label*="seek" i]
  - Spotify: [data-testid="progress-bar"], [class*="progress-bar"], [role="slider"]
  - SoundCloud: .playbackTimeline__progressWrapper, .playbackTimeline__bar, [role="progressbar"]
  - Generic HTML5: audio + adjacent [role="slider"], input[type="range"][class*="progress"], [class*="seek-bar"], [class*="timeline"], [class*="progress-bar"], [aria-label*="seek" i], [aria-label*="progress" i], [aria-label*="timeline" i]

  TIME DISPLAY SELECTORS:
  - Current time: [class*="current-time"], [class*="currentTime"], [class*="elapsed"], [class*="time-current"], time[class*="current"]
  - Total duration: [class*="duration"], [class*="total-time"], [class*="totalTime"], [class*="time-total"], [class*="time-remaining"]
  - Combined display: [class*="time-display"], [class*="timeDisplay"], [class*="time-info"]

  METHOD 1 -- CLICK ON PROGRESS BAR (preferred for seeking):
  Steps:
  1. Identify the audio player and ensure playback has started (click play button if paused)
  2. Get total duration from aria-valuemax, data-duration, duration text, or audio.duration
  3. Get progress bar track bounding rect from get_dom_snapshot
  4. Calculate targetX = trackLeft + (862 / totalDurationSeconds) * trackWidth
  5. Calculate targetY = trackTop + (trackHeight / 2)
  6. Execute click_at(targetX, targetY) on the progress bar at the 14:22 position
  7. Verify via current time display showing approximately "14:22" or "14:2" prefix

  METHOD 2 -- DRAG SCRUBBER THUMB (fallback):
  Steps:
  1. Locate the scrubber thumb position on the progress bar
  2. Record thumbX = thumb center X, thumbY = thumb center Y
  3. Calculate targetX = trackLeft + (862 / totalDurationSeconds) * trackWidth
  4. Execute drag(thumbX, thumbY, targetX, targetY, 10, 20)
  5. Verify via current time display

  VERIFICATION:
  - After seeking, verify current time using:
    * Read time display element text (should show "14:22" or within 5 seconds: "14:17" to "14:27")
    * get_attribute on slider for aria-valuenow (should be approximately 862)
    * The progress bar fill width should reflect the correct percentage
  - Accept range of 14:17 to 14:27 (within 5 seconds tolerance per CONTEXT.md)

  PLAY STATE:
  - Most podcast players must be playing (or at least loaded) before seeking works
  - Click the play button first if the player shows a play icon
  - Some players only load the audio on first play click
  - After seeking, player may auto-pause -- this is acceptable, position is what matters

  COOKIE/CONSENT POPUPS:
  - Dismiss cookie banners before interacting with the player
  - Same consent selectors as video-player site guide

- selectors object with keys:
  progressBar: '[role="slider"][aria-label*="seek" i], [role="slider"][aria-label*="progress" i], [role="slider"][aria-label*="timeline" i], input[type="range"][class*="progress"], [class*="progress-bar"][class*="seek"], [class*="seek-bar"], [class*="scrubber"], [class*="timeline-bar"], .playbackTimeline__progressWrapper'
  progressThumb: '[class*="progress"][class*="thumb"], [class*="seek"][class*="thumb"], [class*="scrubber"][class*="handle"], [class*="timeline"][class*="knob"], [class*="progress-handle"]'
  progressTrack: '[class*="progress"][class*="track"], [class*="seek"][class*="track"], [class*="timeline"][class*="track"], [class*="progress-bar"][class*="rail"], .playbackTimeline__bar'
  progressFill: '[class*="progress"][class*="fill"], [class*="progress"][class*="played"], [class*="progress-level"], [class*="elapsed-bar"], .playbackTimeline__timePassed'
  currentTime: '[class*="current-time"], [class*="currentTime"], [class*="elapsed"], [class*="time-current"], [data-testid*="current-time"]'
  totalDuration: '[class*="duration"], [class*="total-time"], [class*="totalTime"], [class*="time-total"], [data-testid*="duration"]'
  playButton: '[aria-label*="Play" i], button[class*="play"], [data-testid*="play"], [class*="play-button"], [class*="playButton"]'
  pauseButton: '[aria-label*="Pause" i], button[class*="pause"], [data-testid*="pause"]'
  playerContainer: '[class*="podcast-player"], [class*="audio-player"], [class*="episode-player"], [class*="player-container"], [class*="player-wrapper"]'
  audioElement: 'audio'
  cookieConsent: '#onetrust-accept-btn-handler, [class*="cookie"] button[class*="accept"], [class*="consent"] button, [id*="cookie"] button'

- workflows object with keys:
  scrubTimelineByClick: 6-step array (navigate, get DOM, get duration, calculate position, click_at, verify time)
  scrubTimelineByDrag: 7-step array (navigate, get DOM, get duration, locate thumb, calculate target, drag, verify time)
  fullTimelineScrubTest: 10-step array (navigate, dismiss cookies, start playback, get DOM, read duration, calculate 14:22 position, click_at on progress bar, verify time display, if click failed try drag, final verification within 5s tolerance)

- warnings array including:
  * "Audio must be loaded (click play first) before seeking works on most players"
  * "Some players use 0-1 range (percentage) while others use seconds for aria-valuenow"
  * "14:22 = 862 seconds -- ensure you convert MM:SS to total seconds for calculation"
  * "Do NOT click the play/pause button when trying to seek -- only interact with the progress bar"
  * "Accept 14:17-14:27 (5 second tolerance) as success due to pixel granularity on narrow progress bars"
  * "Some podcast pages require the audio to fully load before the timeline is interactive"
  * "SoundCloud uses a different progress bar structure (.playbackTimeline__) than most players"
  * "Spotify web player may require authentication -- use public podcast pages without auth"

- toolPreferences: ['click_at', 'drag', 'hover', 'get_attribute', 'click', 'navigate', 'get_dom_snapshot', 'read_page', 'waitForDOMStable', 'press_key']
  </action>
  <verify>
    <automated>test -f site-guides/media/podcast-player.js && grep -q "registerSiteGuide" site-guides/media/podcast-player.js && grep -q "scrubTimeline" site-guides/media/podcast-player.js && grep -q "862" site-guides/media/podcast-player.js && grep -q "14:22\|14.*22" site-guides/media/podcast-player.js && grep -q "progressBar" site-guides/media/podcast-player.js && grep -q "currentTime" site-guides/media/podcast-player.js && grep -q "click_at" site-guides/media/podcast-player.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File site-guides/media/podcast-player.js exists
    - grep "registerSiteGuide" podcast-player.js returns at least 1 match
    - grep "scrubTimeline" podcast-player.js returns at least 1 match (workflow name)
    - grep "862" podcast-player.js returns at least 1 match (14:22 in seconds)
    - grep "progressBar" podcast-player.js returns at least 1 match (selector key)
    - grep "currentTime" podcast-player.js returns at least 1 match (time display selector)
    - grep "totalDuration\|duration" podcast-player.js returns at least 1 match
    - grep "click_at" podcast-player.js returns at least 1 match (preferred tool reference)
    - grep "drag" podcast-player.js returns at least 1 match (fallback tool reference)
    - grep "podbean\|buzzsprout\|spreaker\|soundcloud" podcast-player.js returns at least 1 match (platform patterns)
    - grep "toolPreferences" podcast-player.js returns at least 1 match
    - grep "warnings" podcast-player.js returns at least 1 match
  </acceptance_criteria>
  <done>Podcast-player site guide created with scrubTimeline workflows (click and drag methods), timeline position calculation for 14:22 = 862 seconds, platform-specific selectors for at least 3 podcast services, time display verification selectors, and 5-second tolerance acceptance range</done>
</task>

<task type="auto">
  <name>Task 2: Wire podcast-player site guide into background.js importScripts</name>
  <files>background.js</files>
  <read_first>
    - background.js lines 140-143 (Media section importScripts -- video-player.js and voice-recorder.js entries to see exact pattern)
  </read_first>
  <action>
Add importScripts entry for podcast-player.js in the Media section of background.js. Insert the new line after voice-recorder.js (line 142) to maintain alphabetical grouping within the Media category:

```
importScripts('site-guides/media/podcast-player.js');
```

The Media section currently has:
```
/ Per-site guides: Media
importScripts('site-guides/media/video-player.js');
importScripts('site-guides/media/voice-recorder.js');
```

After this task it should be:
```
/ Per-site guides: Media
importScripts('site-guides/media/podcast-player.js');
importScripts('site-guides/media/video-player.js');
importScripts('site-guides/media/voice-recorder.js');
```

Note: "podcast-player" comes before "video-player" and "voice-recorder" alphabetically.
  </action>
  <verify>
    <automated>grep -q "media/podcast-player" background.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "media/podcast-player" background.js returns at least 1 match
    - The importScripts line appears in the Media section (between "Per-site guides: Media" comment and the next section)
    - No duplicate entries for podcast-player.js
  </acceptance_criteria>
  <done>podcast-player.js wired into background.js importScripts in the Media section, accessible to MCP tool chain</done>
</task>

</tasks>

<verification>
1. site-guides/media/podcast-player.js exists with registerSiteGuide call
2. Site guide contains scrubTimeline workflow (at least scrubTimelineByClick)
3. Site guide contains 862 (14:22 in seconds) in the guidance or formula
4. Site guide contains selectors for progressBar, currentTime, totalDuration
5. Site guide contains platform patterns (podbean, buzzsprout, spreaker, etc.)
6. background.js contains importScripts for media/podcast-player.js
7. Import is in the Media section, not duplicated
</verification>

<success_criteria>
- Podcast-player site guide created with timeline scrub patterns
- scrubTimeline workflow documents click_at on progress bar at calculated position
- Timeline position formula: targetX = trackLeft + (862 / totalSeconds) * trackWidth
- At least 3 podcast platform selector patterns documented
- Site guide wired into background.js and accessible to MCP tool chain
- Ready for Plan 02 live MCP test
</success_criteria>

<output>
After completion, create `.planning/phases/66-podcast-timeline-scrub/66-01-SUMMARY.md`
</output>
