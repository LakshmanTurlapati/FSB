---
phase: 250-usage-md-references-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/FSB Skill/USAGE.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03]

must_haves:
  truths:
    - "USAGE.md framing tells a new user who FSB is for and what counts as a good FSB task vs an ask-another-tool task."
    - "USAGE.md ships a 3-step install: (1) Chrome extension via the canonical Web Store URL plus GitHub Releases fallback, (2) MCP server via npx -y fsb-mcp-server plus paste of the canonical OpenClaw stdio block, (3) verify via doctor."
    - "USAGE.md try-it section demonstrates manual mode with read_page, click, and type_text prompts plus a single explicit run_task autopilot delegation example."
    - "USAGE.md doctor-failure recovery recipe maps each of the six layers (package, bridge, extension, active-tab, content-script, config) to a one-line cause and a copy-pasteable next-step command."
    - "USAGE.md is plain ASCII; no emojis."
  artifacts:
    - path: "skills/FSB Skill/USAGE.md"
      provides: "Human-facing 3-step install + try-it + recovery one-pager."
      contains: "https://chromewebstore.google.com/detail/badgafnfchcihdfnjneklogedcdkmjfk"
  key_links:
    - from: "skills/FSB Skill/USAGE.md"
      to: "skills/FSB Skill/scripts/doctor.mjs"
      via: "Doctor recovery table mirrors the six-layer LAYERS array and NEXT_STEPS map."
      pattern: "package.*bridge.*extension.*active-tab.*content-script.*config"
    - from: "skills/FSB Skill/USAGE.md"
      to: "https://github.com/LakshmanTurlapati/FSB/releases"
      via: "GitHub Releases fallback link in step 1."
      pattern: "github\\.com/LakshmanTurlapati/FSB/releases"
---

<objective>
Replace the Phase 248 placeholder USAGE.md with the human-facing one-pager that walks a new user from a clean machine through install, a try-it pass, and recovery if anything fails.

Purpose: DOCS-01..03 -- this is the surface a new user reads first. It must be self-contained (no clicking through to repo issues to figure out install), copy-paste recoverable, and aligned with the six-layer doctor flow that scripts/doctor.mjs implements.
Output: skills/FSB Skill/USAGE.md (~150-300 lines, plain ASCII, zero emojis).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/250-usage-md-references-policy/250-CONTEXT.md
@skills/FSB Skill/SKILL.md
@skills/FSB Skill/USAGE.md
@skills/FSB Skill/scripts/doctor.mjs

<interfaces>
<!-- Six-layer dispatcher contract from scripts/doctor.mjs (read first). -->
<!-- USAGE.md doctor-failure recovery table MUST mirror this exact set, in this order. -->

LAYERS = ['package', 'bridge', 'extension', 'active-tab', 'content-script', 'config']

NEXT_STEPS (verbatim wording from doctor.mjs -- USAGE.md may rephrase but cause/action must match):
  package:        "Run `npx -y fsb-mcp-server --version` to confirm npm can reach the package."
  bridge:         "Start the FSB extension and run `npx -y fsb-mcp-server status --watch` to confirm ws://localhost:7225 is reachable."
  extension:      "Install the FSB Chrome extension from https://chromewebstore.google.com/detail/badgafnfchcihdfnjneklogedcdkmjfk -- then reopen this session."
  active-tab:     "Open a normal http(s) tab (not chrome://, edge://, or the Web Store) and run `npx -y fsb-mcp-server status --watch` to re-attach."
  content-script: "Reload the active tab. The FSB content script attaches on page load; reloads after extension install are required."
  config:         "Re-run `npx -y fsb-mcp-server install --<host>` for your MCP host (replace <host> with claude-desktop, cursor, etc.)."
  ok:             "All layers green. Run a small read_page or list_tabs call to confirm end-to-end."

Canonical URLs (must appear verbatim somewhere in USAGE.md):
  Chrome Web Store: https://chromewebstore.google.com/detail/badgafnfchcihdfnjneklogedcdkmjfk
  GitHub Releases: https://github.com/LakshmanTurlapati/FSB/releases

OpenClaw stdio block (DO NOT inline -- direct user to run scripts/print-stdio.mjs):
  Phase 249 SKILL-04: scripts/print-stdio.mjs prints the canonical block sourced from mcp/src/install.ts getSetupSections().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author USAGE.md framing + 3-step install (DOCS-01)</name>
  <files>skills/FSB Skill/USAGE.md</files>
  <read_first>
    - skills/FSB Skill/USAGE.md (current placeholder -- about to be overwritten)
    - skills/FSB Skill/SKILL.md (top of file framing -- USAGE.md framing must be consistent)
    - .planning/phases/250-usage-md-references-policy/250-CONTEXT.md decisions block (DOCS-01 wording rules)
    - ./CLAUDE.md (NO EMOJIS rule, ASCII status markers only)
  </read_first>
  <action>
    Overwrite skills/FSB Skill/USAGE.md. Build the file in this order, top to bottom:

    1. H1 title: "FSB Skill -- Usage". One-sentence subtitle naming what FSB does (drives the user's real Chrome via the FSB extension and a local MCP bridge for live web tasks).

    2. "Who FSB is for" framing paragraph (~3-5 lines). State plainly: FSB is for tasks that need clicks, typing, multi-tab, auth, or live page state. Mention the carve-out: public-doc / JSON / RSS reads can stay on WebFetch (per references/default-to-fsb.md). End with one line on the goal: a new user should reach a green doctor in under five minutes.

    3. "## Install (3 steps)" H2 section. Use ordered list, one numbered step per install layer:

       Step 1 -- Install the FSB Chrome extension.
         - Primary: paste the URL `https://chromewebstore.google.com/detail/badgafnfchcihdfnjneklogedcdkmjfk` into Chrome and click Add to Chrome.
         - Fallback: GitHub Releases page `https://github.com/LakshmanTurlapati/FSB/releases` -- download the .zip, unzip, and load unpacked at chrome://extensions with Developer mode on.
         - DO NOT auto-launch the URL from this skill -- print it for the user to click. (Anti-scope from REQUIREMENTS.md / ROADMAP.md.)

       Step 2 -- Install the FSB MCP server config.
         - Run `node scripts/print-stdio.mjs` (or `npx -y fsb-mcp-server install --list` to discover other hosts) and paste the printed stdio block into your OpenClaw MCP config. Restart OpenClaw afterward.
         - Mention: `npx -y fsb-mcp-server` invocations stay unpinned by design (matches existing FSB convention).
         - Mention: zero env vars needed -- vault values resolve inside the extension; see `references/vault-boundary.md`.

       Step 3 -- Verify with the doctor.
         - Run `node scripts/doctor.mjs`. Expect all six layers to print [OK]. If any layer prints [FAIL], jump to the recovery table below (Section "Recover when the doctor fails").

    Plain ASCII only. ASCII status markers in any code blocks: [OK], [FAIL], [WARN]. Zero emojis. Code blocks fenced with triple backticks; commands copy-pasteable.

    Cover for DOCS-01: Chrome Web Store URL appears verbatim, GitHub Releases fallback link appears, three numbered install steps are present.
  </action>
  <verify>
    <automated>grep -F 'https://chromewebstore.google.com/detail/badgafnfchcihdfnjneklogedcdkmjfk' "skills/FSB Skill/USAGE.md" && grep -F 'github.com/LakshmanTurlapati/FSB/releases' "skills/FSB Skill/USAGE.md" && ! grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' "skills/FSB Skill/USAGE.md"</automated>
  </verify>
  <done>
    USAGE.md no longer contains the "Status: scaffolded by Phase 248" placeholder line.
    USAGE.md contains both canonical URLs verbatim.
    USAGE.md has three numbered install steps with copy-pasteable commands.
    Zero emojis (grep returns no Unicode emoji code points).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add try-it prompts and doctor-failure recovery recipe (DOCS-02, DOCS-03)</name>
  <files>skills/FSB Skill/USAGE.md</files>
  <read_first>
    - skills/FSB Skill/USAGE.md (Task 1 output)
    - skills/FSB Skill/scripts/doctor.mjs (LAYERS array + NEXT_STEPS map -- recovery table mirrors these verbatim in cause/action wording)
    - .planning/phases/250-usage-md-references-policy/250-CONTEXT.md (DOCS-02 wording rules: 3 manual prompts + single autopilot example with explicit delegation framing)
  </read_first>
  <action>
    Append two H2 sections to skills/FSB Skill/USAGE.md after the Install section. Order matters -- try-it before recovery.

    Section A: "## Try it" (covers DOCS-02)

    Open with one line: "Once the doctor is green, paste any of these into your OpenClaw chat to confirm the loop works."

    Subsection "### Manual mode (default)": 3 example prompts as a bulleted list. Each prompt is a complete sentence the user can paste verbatim. Make sure all three exercise different tools:
      - One prompt that exercises `read_page` (e.g., "Open https://example.com and read the page.").
      - One prompt that exercises `click` (e.g., "On https://example.com, click the 'More information' link.").
      - One prompt that exercises `type_text` (e.g., "Open https://duckduckgo.com and type 'fsb-mcp-server' into the search box, then press Enter.").

    Subsection "### Autopilot (explicit delegation only)": exactly one example prompt that demonstrates `run_task`. Frame it explicitly: "This delegates the WHOLE task to FSB autopilot. Use only when you are willing to hand off the full plan-and-execute loop." Example: "Use FSB autopilot to find the first GitHub repo for fsb-mcp-server and report its README." Add a one-line warning: "Autopilot is NOT the default entry point. For day-to-day work, use manual mode prompts above."

    Section B: "## Recover when the doctor fails" (covers DOCS-03)

    Open with one line: "Each [FAIL] from `node scripts/doctor.mjs` maps to one of six layers. Find your symptom in the table, run the action, and re-run the doctor."

    Markdown table with columns: | Layer | What it means | What to do |
    Six rows, in this exact order (matches LAYERS array in scripts/doctor.mjs):

      | package        | npm cannot fetch fsb-mcp-server.                        | Run `npx -y fsb-mcp-server --version`. If that fails, check Node 18+ is installed and on PATH.                                |
      | bridge         | The extension is not running, or ws://localhost:7225 is not reachable. | Open Chrome with the FSB extension installed, then run `npx -y fsb-mcp-server status --watch`.                                |
      | extension      | The FSB Chrome extension is not installed.              | Install from `https://chromewebstore.google.com/detail/badgafnfchcihdfnjneklogedcdkmjfk` (fallback: GitHub Releases). Reopen the session. |
      | active-tab     | The active tab is restricted (chrome://, edge://, Web Store) or no tab is owned by the agent. | Open a normal http(s) tab and run `npx -y fsb-mcp-server status --watch`. See `references/restricted-tab-recovery.md` for the recovery toolset. |
      | content-script | The content script did not attach (extension installed after the tab loaded). | Reload the active tab. Reloads after extension install are required.                                                          |
      | config         | The MCP host config does not include the FSB stdio block. | Re-run `npx -y fsb-mcp-server install --<host>` (replace `<host>` with `claude-desktop`, `cursor`, etc.). Or paste the block printed by `node scripts/print-stdio.mjs`. |

    Close the section with a one-line "If still stuck" pointer: "Re-run `node scripts/doctor.mjs` after each fix. If the layer keeps flipping or you see [WARN], capture the raw output and file an issue at https://github.com/LakshmanTurlapati/FSB/issues."

    All ASCII. Zero emojis. Copy-paste recoverable commands. Table cause/action wording must align with scripts/doctor.mjs NEXT_STEPS map (rephrase OK; same cause + same action).
  </action>
  <verify>
    <automated>grep -E 'read_page' "skills/FSB Skill/USAGE.md" && grep -E '\bclick\b' "skills/FSB Skill/USAGE.md" && grep -E 'type_text' "skills/FSB Skill/USAGE.md" && grep -E 'run_task' "skills/FSB Skill/USAGE.md" && grep -E '\bpackage\b.*\|.*\|' "skills/FSB Skill/USAGE.md" && grep -E '\bbridge\b.*\|.*\|' "skills/FSB Skill/USAGE.md" && grep -E '\bextension\b.*\|.*\|' "skills/FSB Skill/USAGE.md" && grep -E 'active-tab.*\|.*\|' "skills/FSB Skill/USAGE.md" && grep -E 'content-script.*\|.*\|' "skills/FSB Skill/USAGE.md" && grep -E '\bconfig\b.*\|.*\|' "skills/FSB Skill/USAGE.md" && ! grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' "skills/FSB Skill/USAGE.md"</automated>
  </verify>
  <done>
    USAGE.md "Try it" section names read_page, click, type_text in three separate manual prompts.
    USAGE.md has exactly one run_task autopilot example with explicit "delegates the WHOLE task" framing.
    USAGE.md "Recover" section has a six-row table with all six layer names from scripts/doctor.mjs (package, bridge, extension, active-tab, content-script, config).
    Zero emojis.
    File ends without trailing placeholder text.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the assembled USAGE.md:

1. `grep -c '^## ' "skills/FSB Skill/USAGE.md"` returns at least 3 (Install, Try it, Recover).
2. `grep -F 'https://chromewebstore.google.com/detail/badgafnfchcihdfnjneklogedcdkmjfk' "skills/FSB Skill/USAGE.md"` returns one or more matches.
3. `grep -F 'github.com/LakshmanTurlapati/FSB/releases' "skills/FSB Skill/USAGE.md"` returns one or more matches.
4. All six doctor layers (package, bridge, extension, active-tab, content-script, config) appear in the recovery table.
5. The placeholder string "scaffolded by Phase 248" is gone from the file.
6. No emojis: `grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]'` returns zero matches.
</verification>

<success_criteria>
- DOCS-01: USAGE.md ships a 3-step install (extension via canonical Web Store URL + GitHub Releases fallback, then MCP server, then verify).
- DOCS-02: USAGE.md try-it section demonstrates read_page, click, type_text manual prompts plus a single explicit run_task autopilot example.
- DOCS-03: USAGE.md doctor-failure recovery table covers all six layers from scripts/doctor.mjs in matching cause/action shape.
- Zero emojis. Plain ASCII. Copy-paste recoverable.
</success_criteria>

<output>
After completion, create `.planning/phases/250-usage-md-references-policy/250-01-SUMMARY.md` documenting:
- Final USAGE.md line count and section structure.
- Confirmation that all three REQ-IDs (DOCS-01..03) are covered with quoted snippets.
- Any wording deviations from CONTEXT.md decisions (should be none -- record any judgment calls under "Claude's Discretion was used for ...").
</output>
