---
phase: 250-usage-md-references-policy
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/FSB Skill/references/visual-session-lifecycle.md
  - skills/FSB Skill/SKILL.md
autonomous: true
requirements: [POLICY-03]

must_haves:
  truths:
    - "A new references/visual-session-lifecycle.md exists at the path SKILL.md's body points to (POLICY-03 says 'references/ link covers the lifecycle in detail')."
    - "visual-session-lifecycle.md documents the open-and-close pairing rule: every external-AI-driven sequence opens with start_visual_session(client=\"OpenClaw\", ...) and closes with end_visual_session(session_token=..., reason=...)."
    - "visual-session-lifecycle.md shows pairing on every error path so the orange glow never gets stuck on a page (try/finally pattern, error-path close, abort/cancel close, completion close)."
    - "SKILL.md body's existing 'Visual session wrapping' paragraph points to references/visual-session-lifecycle.md (the file the body's pointer resolves to)."
  artifacts:
    - path: "skills/FSB Skill/references/visual-session-lifecycle.md"
      provides: "Open/close pairing contract for the visual session API; error-path coverage."
      contains: "start_visual_session"
    - path: "skills/FSB Skill/SKILL.md"
      provides: "Body pointer from the visual-session paragraph to the new lifecycle file (small body edit only -- frontmatter untouched)."
      contains: "references/visual-session-lifecycle.md"
  key_links:
    - from: "skills/FSB Skill/SKILL.md"
      to: "skills/FSB Skill/references/visual-session-lifecycle.md"
      via: "Body paragraph 'Visual session wrapping' adds an explicit pointer to the lifecycle file."
      pattern: "references/visual-session-lifecycle\\.md"
---

<objective>
Cover POLICY-03 by adding the references-side lifecycle file and adjusting the SKILL.md body pointer so the visual-session wrapping rule resolves to a file that explains the lifecycle in detail.

Purpose: POLICY-03 -- "SKILL.md body includes the visual-session wrapping rule + references/ link covers the lifecycle in detail and shows pairing on every error path so the orange glow never gets stuck on a page". SKILL.md already has the body paragraph (Phase 249 SKILL-02). What is missing is the references/ file the paragraph alludes to and an explicit pointer to it.
Output:
  skills/FSB Skill/references/visual-session-lifecycle.md (NEW, ~120-200 lines)
  skills/FSB Skill/SKILL.md (small body edit only -- add explicit pointer line in the "Visual session wrapping" section; do NOT touch frontmatter)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/250-usage-md-references-policy/250-CONTEXT.md
@skills/FSB Skill/SKILL.md

<interfaces>
<!-- Visual session API (sourced from mcp/src/tools/visual-session.ts -- read-only reference for plan authoring): -->

start_visual_session({
  client: string,           // e.g., "OpenClaw" -- caller-provided label
  task: string,             // short human-readable label for the session
  ...                       // additional fields per current FSB build
}) -> { session_token: string, ... }

end_visual_session({
  session_token: string,    // returned by start_visual_session
  reason: string,           // "complete", "error", "aborted", "user_cancelled", etc.
}) -> { ok: boolean, ... }

<!-- Behavior: while a session is open, the FSB extension paints an orange glow on the active tab -->
<!-- so the user can see external AI is driving. If end_visual_session is never called, the glow -->
<!-- persists across reloads until the user manually closes the tab or clears the extension state. -->

<!-- POLICY-03 explicit requirement (verbatim from REQUIREMENTS.md): -->
<!-- "SKILL.md body includes the visual-session wrapping rule: any external-AI-driven sequence opens with -->
<!--  start_visual_session(client=\"OpenClaw\", ...) and closes with end_visual_session(session_token=..., reason=...); -->
<!--  references/ link covers the lifecycle in detail." -->

<!-- Existing SKILL.md body content (Phase 249 SKILL-02), to be augmented with one explicit pointer line: -->
<!--   ## Visual session wrapping -->
<!--   Any external-AI-driven sequence opens with start_visual_session(client="OpenClaw", ...) and closes with -->
<!--   end_visual_session(session_token=..., reason=...). The wrap MUST close on every error path so the orange -->
<!--   glow does not get stuck on the user's tab. Lifecycle details and recovery steps live in references/. -->

<!-- Anti-scope: do NOT change SKILL.md frontmatter (Phase 249 SKILL-01 owned that surface and Phase 251 TEST-02 -->
<!-- verifies its shape). Body edits are limited to the visual-session paragraph; do NOT add or remove other -->
<!-- body sections, do NOT touch the References list at the bottom of SKILL.md (that is a Phase 250 plan-02/03 -->
<!-- concern via the per-file pointer paths, but the visual-session-lifecycle.md addition belongs in the body -->
<!-- paragraph specifically, not the bottom References list -- keep that list scoped to the original five files -->
<!-- the SKILL.md author chose to enumerate in the bottom list). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author references/visual-session-lifecycle.md</name>
  <files>skills/FSB Skill/references/visual-session-lifecycle.md</files>
  <read_first>
    - skills/FSB Skill/SKILL.md (existing "Visual session wrapping" paragraph -- keep wording consistent)
    - .planning/phases/250-usage-md-references-policy/250-CONTEXT.md "POLICY-03" decisions block (recommends adding this file for completeness)
    - mcp/src/tools/visual-session.ts (skim to confirm tool argument shapes -- caller passes client + task to start, session_token + reason to end)
    - ./CLAUDE.md (NO EMOJIS rule)
  </read_first>
  <action>
    Create skills/FSB Skill/references/visual-session-lifecycle.md from scratch. Build the file in this order:

    1. H1: "Visual session lifecycle". One-paragraph framing: "While a visual session is open, the FSB extension paints an orange glow on the active tab so the user can see an external AI is driving. The glow stays on until `end_visual_session` is called. If the session is never closed, the glow persists across reloads -- the user has to close the tab or clear extension state to remove it. This file documents the open/close pairing rule and shows the close path on every termination outcome."

    2. "## The pairing rule" H2 section. One numbered list:
       1. Open the session with `start_visual_session({ client: "OpenClaw", task: <short label> })`. Save the returned `session_token`.
       2. Run the work (any sequence of FSB tools).
       3. Close the session with `end_visual_session({ session_token, reason })` exactly once. The `reason` is one of `complete`, `error`, `aborted`, `user_cancelled`. Use whichever matches the actual termination state.

       Close with a one-line rule: "One open => exactly one close. No exceptions."

    3. "## Termination outcomes (always close)" H2 section. Markdown table with two columns: Outcome | reason value.

       | Outcome                                          | reason value     |
       |--------------------------------------------------|------------------|
       | Task finished successfully.                      | `complete`       |
       | A typed error fired and the loop cannot recover. | `error`          |
       | The model decided to abort mid-sequence.         | `aborted`        |
       | The user cancelled (stop button, escape, etc.).  | `user_cancelled` |

    4. "## Pattern: try/finally close" H2 section. Show the canonical pseudocode pattern in a fenced code block (plain ASCII, no language hint required, but `js`/`pseudo` is fine):

       ```
       const { session_token } = await start_visual_session({ client: "OpenClaw", task: "<short label>" });
       try {
         // ... FSB tool calls ...
         await end_visual_session({ session_token, reason: "complete" });
       } catch (err) {
         await end_visual_session({ session_token, reason: "error" });
         throw err;
       }
       ```

       Add a one-line note: "Even when the model is reasoning across multiple turns, the close MUST run on every termination path. Defer the close into a finally-equivalent or wrap the whole turn-loop so the path is impossible to miss."

    5. "## Error-path close coverage" H2 section. Bulleted list of common error paths and the right close call:
       - Typed multi-agent error (`TAB_NOT_OWNED`, `AGENT_CAP_REACHED`, `TAB_INCOGNITO_NOT_SUPPORTED`, `TAB_OUT_OF_SCOPE`) -- close with `reason: "error"` and report the typed error to the user.
       - Restricted-tab attach failure (`chrome://`, `edge://`, Web Store) -- recover via `references/restricted-tab-recovery.md` first; only close if recovery itself fails.
       - User cancel mid-task -- close with `reason: "user_cancelled"`.
       - Model self-aborts (decides task is impossible) -- close with `reason: "aborted"`.
       - Network / bridge disconnect -- close with `reason: "error"`. The bridge may have already invalidated the session; the close call is idempotent and safe to attempt anyway.

    6. "## Anti-patterns" H2 section. Two fenced [BAD]/[GOOD] callouts:

       ```
       [BAD]  start_visual_session({ client: "OpenClaw", task: "..." })
              // ... work ...
              // never call end_visual_session
              // result: orange glow stuck on the user's tab indefinitely.
       [GOOD] always pair start with end on every termination path (see try/finally pattern above).
       ```

       ```
       [BAD]  end_visual_session({ session_token })   // missing reason
       [GOOD] end_visual_session({ session_token, reason: "complete" })
       ```

    7. "## See also" H2 section:
       - `SKILL.md` -- "Visual session wrapping" body paragraph.
       - `references/multi-agent-contract.md` -- typed errors that trigger error-path closes.
       - `references/restricted-tab-recovery.md` -- recover before closing on attach failures.

    Plain ASCII. Zero emojis. ASCII status markers ([OK], [FAIL], [WARN]) only if needed; not required here.
  </action>
  <verify>
    <automated>test -f "skills/FSB Skill/references/visual-session-lifecycle.md" && grep -q 'start_visual_session' "skills/FSB Skill/references/visual-session-lifecycle.md" && grep -q 'end_visual_session' "skills/FSB Skill/references/visual-session-lifecycle.md" && grep -q 'session_token' "skills/FSB Skill/references/visual-session-lifecycle.md" && grep -qi 'try' "skills/FSB Skill/references/visual-session-lifecycle.md" && grep -qi 'error' "skills/FSB Skill/references/visual-session-lifecycle.md" && grep -qi 'orange' "skills/FSB Skill/references/visual-session-lifecycle.md" && ! grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' "skills/FSB Skill/references/visual-session-lifecycle.md"</automated>
  </verify>
  <done>
    skills/FSB Skill/references/visual-session-lifecycle.md exists.
    start_visual_session and end_visual_session are both named with their full argument shapes (client/task for start, session_token/reason for end).
    Try/finally pattern is shown in a fenced block.
    Error-path close coverage names typed errors, restricted-tab failure, user cancel, model abort, and bridge disconnect.
    Two anti-pattern [BAD]/[GOOD] callouts are present.
    Zero emojis.
  </done>
</task>

<task type="auto">
  <name>Task 2: Adjust SKILL.md body pointer to reference visual-session-lifecycle.md</name>
  <files>skills/FSB Skill/SKILL.md</files>
  <read_first>
    - skills/FSB Skill/SKILL.md (current state -- Phase 249 SKILL-02 output; specifically the "## Visual session wrapping" body section)
    - .planning/phases/250-usage-md-references-policy/250-CONTEXT.md "POLICY-03" decisions block
    - .planning/ROADMAP.md "Anti-Scope" section (frontmatter MUST stay untouched)
  </read_first>
  <action>
    Make the SMALLEST possible body edit to skills/FSB Skill/SKILL.md to add an explicit pointer to the new references/visual-session-lifecycle.md file.

    Constraints (do not deviate):
    - Do NOT touch the frontmatter (lines 1-12 of the current file). Phase 251 TEST-02 verifies the frontmatter shape; any change risks breaking the test.
    - Do NOT add new H2 sections or reorder existing sections.
    - Do NOT touch the bottom "## References (load on demand)" bulleted list. That list enumerates the original five files the SKILL.md author scoped at Phase 249. Adding visual-session-lifecycle.md there would expand the scope of the body's reference list, which is out of scope for this plan -- the pointer belongs in the visual-session-wrapping paragraph itself, not the bottom enumeration.
    - Do NOT touch any other section (When to use FSB, Doctor-first protocol, Multi-agent contract, Vault and credentials, Scripts).

    Make exactly one edit:

    BEFORE (current last sentence of the "## Visual session wrapping" section):
      "Lifecycle details and recovery steps live in `references/`."

    AFTER (replace that sentence with):
      "Lifecycle details, the try/finally close pattern, and error-path close coverage live in `references/visual-session-lifecycle.md`."

    The edit is one sentence rewrite -- it preserves the meaning ("lifecycle details live in references/") while making the pointer explicit and resolvable.

    After the edit, run `node --check` is not applicable (markdown), so verify by inspection: the section starts with `## Visual session wrapping`, contains the start_visual_session/end_visual_session call shapes, and ends with the new pointer sentence naming `references/visual-session-lifecycle.md`.
  </action>
  <verify>
    <automated>grep -F 'references/visual-session-lifecycle.md' "skills/FSB Skill/SKILL.md" && grep -F 'start_visual_session(client="OpenClaw"' "skills/FSB Skill/SKILL.md" && grep -F 'end_visual_session(session_token=' "skills/FSB Skill/SKILL.md" && head -12 "skills/FSB Skill/SKILL.md" | grep -q 'version: 0.9.61' && head -12 "skills/FSB Skill/SKILL.md" | grep -qE 'requires:' && ! grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' "skills/FSB Skill/SKILL.md"</automated>
  </verify>
  <done>
    skills/FSB Skill/SKILL.md "Visual session wrapping" section ends with a sentence that names `references/visual-session-lifecycle.md` explicitly.
    The frontmatter (lines 1-12) is byte-identical to the Phase 249 output (`name: FSB`, `version: 0.9.61`, `requires.bins`, `requires.env: []`, `homepage`, `metadata.openclaw` all unchanged).
    No other body sections were edited.
    The bottom "## References (load on demand)" list is unchanged.
    Zero emojis.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify:

1. `test -f "skills/FSB Skill/references/visual-session-lifecycle.md"` succeeds.
2. SKILL.md body contains the new pointer: `grep -F 'references/visual-session-lifecycle.md' "skills/FSB Skill/SKILL.md"` returns one or more matches.
3. SKILL.md frontmatter is byte-identical to Phase 249 output: `head -12 "skills/FSB Skill/SKILL.md"` shows the same `name`, `description`, `version: 0.9.61`, `user-invocable: true`, `requires.bins`, `requires.env: []`, `homepage`, `metadata.openclaw` lines.
4. visual-session-lifecycle.md grep coverage: start_visual_session, end_visual_session, session_token, try, error, orange.
5. No emojis introduced anywhere.
</verification>

<success_criteria>
- POLICY-03: SKILL.md body's visual-session wrapping rule has an explicit pointer to references/visual-session-lifecycle.md, AND that file exists, AND it documents the open/close pairing rule + try/finally pattern + error-path close coverage so the orange glow never gets stuck.
- Zero emojis. Plain ASCII.
- SKILL.md frontmatter and unrelated body sections are untouched (Phase 251 TEST-02 / TEST-04 pre-pass).
</success_criteria>

<output>
After completion, create `.planning/phases/250-usage-md-references-policy/250-04-SUMMARY.md` documenting:
- Final visual-session-lifecycle.md line count and section structure.
- The exact one-sentence diff applied to SKILL.md (BEFORE / AFTER quoted).
- Confirmation that POLICY-03 is covered with quoted snippets.
- Confirmation that frontmatter and unrelated body sections were not touched.
</output>
