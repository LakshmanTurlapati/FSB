---
phase: 250-usage-md-references-policy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/FSB Skill/references/tool-decision-tree.md
  - skills/FSB Skill/references/multi-agent-contract.md
autonomous: true
requirements: [DOCS-04, DOCS-05]

must_haves:
  truths:
    - "An OpenClaw model loading tool-decision-tree.md can decide between read_page, get_dom_snapshot, get_page_snapshot, and get_site_guide without re-reading the SKILL.md body."
    - "tool-decision-tree.md rules typed events (click, type_text) over execute_js with .value -- the React/Vue change-handler pitfall is named explicitly."
    - "tool-decision-tree.md restricts run_task framing -- run_task is described in narrative prose only as the autopilot delegation surface, NOT listed as an in-tree manual mode tool, so every bare tool name in the tree/table exists in mcp/ai/tool-definitions.cjs."
    - "multi-agent-contract.md states explicitly: callers MUST NOT pass agent_id; the server mints it."
    - "multi-agent-contract.md names all four typed errors (TAB_NOT_OWNED, AGENT_CAP_REACHED, TAB_INCOGNITO_NOT_SUPPORTED, TAB_OUT_OF_SCOPE) with one-line meaning + one-line action each."
    - "multi-agent-contract.md documents back as the typed replacement for execute_js('history.back()')."
  artifacts:
    - path: "skills/FSB Skill/references/tool-decision-tree.md"
      provides: "Read-only-first decision tree for FSB AI manual mode tools."
      contains: "read_page"
    - path: "skills/FSB Skill/references/multi-agent-contract.md"
      provides: "v0.8.0 multi-agent contract: agent_id rule, four typed errors, back tool."
      contains: "TAB_NOT_OWNED"
  key_links:
    - from: "skills/FSB Skill/references/tool-decision-tree.md"
      to: "mcp/ai/tool-definitions.cjs"
      via: "Every bare tool token in tree/table form must match a name field in tool-definitions.cjs."
      pattern: "name:\\s*'(read_page|get_dom_snapshot|get_page_snapshot|get_site_guide)'"
    - from: "skills/FSB Skill/references/multi-agent-contract.md"
      to: "skills/FSB Skill/SKILL.md"
      via: "SKILL.md body references this file by relative path; pointer must remain valid."
      pattern: "references/multi-agent-contract.md"
---

<objective>
Author the two reference files that codify the FSB tool decision tree and the v0.8.0 multi-agent contract -- the priors OpenClaw needs before it picks a tool or sees a typed error.

Purpose: DOCS-04 + DOCS-05. SKILL.md body already points at both filenames; this plan fills the placeholders so the pointers resolve. Phase 251 TEST-04 will grep these files for tool-name validity and typed-error coverage, so the constraints are enforced at CI time.
Output:
  skills/FSB Skill/references/tool-decision-tree.md (~180-300 lines)
  skills/FSB Skill/references/multi-agent-contract.md (~150-250 lines)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/250-usage-md-references-policy/250-CONTEXT.md
@skills/FSB Skill/SKILL.md
@skills/FSB Skill/references/README.md
@mcp/ai/tool-definitions.cjs

<interfaces>
<!-- Tool names that DO exist in mcp/ai/tool-definitions.cjs (use these as bare tokens). -->
<!-- Source of truth: name: '...' fields in mcp/ai/tool-definitions.cjs. -->

Read-only / inspection tools (in tool-definitions.cjs):
  read_page, get_dom_snapshot, get_page_snapshot, get_site_guide, get_text, get_attribute, list_tabs

Action tools (in tool-definitions.cjs):
  click, type_text, press_enter, press_key, select_option, check_box, hover, right_click, double_click,
  select_text_range, drag_drop, drop_file, focus, clear_input, scroll, scroll_to_top, scroll_to_bottom,
  scroll_to_element, wait_for_element, wait_for_stable, set_attribute, fill_sheet, read_sheet,
  click_at, click_and_hold, drag, drag_variable_speed, scroll_at, insert_text, double_click_at

Navigation / tab tools (in tool-definitions.cjs):
  navigate, search, go_back, go_forward, refresh, open_tab, switch_tab, close_tab

Coarse / lifecycle tools (in tool-definitions.cjs):
  execute_js, search_memory, report_progress, complete_task, partial_task, fail_task

<!-- Tool names that DO NOT appear in mcp/ai/tool-definitions.cjs and therefore must NOT appear -->
<!-- as a bare token in the tree/table of tool-decision-tree.md (Phase 251 TEST-04 grep enforces). -->
<!-- They live in mcp/src/tools/*.ts on the MCP server side, not the AI manual-mode registry. -->

NOT in tool-definitions.cjs (do NOT list as tree/table tokens):
  run_task            <- autopilot delegation surface; reference by NARRATIVE prose only.
  back                <- v0.8.0 typed replacement for execute_js('history.back()'); narrative-only here, named in multi-agent-contract.md as a contract item.
  fill_credential, use_payment_method   <- vault-side; lives in vault-boundary.md only.
  start_visual_session, end_visual_session  <- visual session lifecycle; lives in SKILL.md body + visual-session-lifecycle.md.

<!-- Phase 251 TEST-04 enforcement (verbatim from REQUIREMENTS.md TEST-04): -->
<!-- "verifies references/tool-decision-tree.md references only tool names that exist in mcp/ai/tool-definitions.cjs". -->
<!-- "verifies references/multi-agent-contract.md mentions every typed error name (TAB_NOT_OWNED, AGENT_CAP_REACHED, -->
<!--  TAB_INCOGNITO_NOT_SUPPORTED, TAB_OUT_OF_SCOPE) and the back tool". -->

<!-- Existing v0.8.0 contract details extractable from go_back description in tool-definitions.cjs:165: -->
<!-- "Multi-agent: agent-scoped tabs; cross-agent reject with TAB_NOT_OWNED; cap configurable (default 8, 1-64)." -->

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author references/tool-decision-tree.md (DOCS-04)</name>
  <files>skills/FSB Skill/references/tool-decision-tree.md</files>
  <read_first>
    - mcp/ai/tool-definitions.cjs (skim with `grep -nE "name:\\s*'" mcp/ai/tool-definitions.cjs` to confirm the canonical token list before writing -- this is the registry the test greps against)
    - skills/FSB Skill/SKILL.md (the relative pointer path -- file must be createable at the path the body links to: `references/tool-decision-tree.md`)
    - .planning/phases/250-usage-md-references-policy/250-CONTEXT.md "references/tool-decision-tree.md (DOCS-04)" decisions block
    - ./CLAUDE.md (NO EMOJIS rule)
  </read_first>
  <action>
    Create skills/FSB Skill/references/tool-decision-tree.md from scratch. Build the file in this order:

    1. H1: "FSB tool decision tree". One-paragraph framing: "Read-only first; act with typed events; escalate to autopilot only when the user explicitly delegates the whole task." State that the names below match the FSB AI manual-mode tool registry (`mcp/ai/tool-definitions.cjs`).

    2. "## Read-only first (in this order)" H2 section. Bulleted decision tree (top to bottom = preferred order):
       - `read_page` -- the default reader. Use first when you need the user-visible text + interactable elements of the current tab. Cheap; runs on the live DOM.
       - `get_dom_snapshot` -- escalate from read_page when you need the structured DOM (attributes, hidden elements, ARIA roles). More tokens; slower.
       - `get_page_snapshot` -- escalate from get_dom_snapshot when you need the full page including computed visual state (e.g., position-aware layout). Heaviest; use sparingly.
       - `get_site_guide` -- check this BEFORE the three above for known sites. The skill ships ~43 site guides under `site-guides/`; if the active host has a guide, it tells you exactly which selectors are stable.

       Add a one-line rule under the bullets: "If get_site_guide returns a guide, follow the guide's selectors first; only fall back to read_page / snapshots when the guide does not cover the current state."

    3. "## Typed events over .value" H2 section. Plain prose + one short anti-pattern callout:
       - State the rule: "Always use the typed action tools (click, type_text, press_enter, press_key, select_option, check_box, hover, focus, clear_input, etc.) instead of `execute_js` with `.value =` or `.click()`."
       - Explain WHY in one paragraph: React, Vue, Solid, and Angular all install change/input handlers that fire only on real user-style events. Setting `input.value = 'foo'` directly via execute_js does NOT fire those handlers, so the framework's state stays out of sync, and the form does not validate or submit.
       - Anti-pattern callout (one fenced ASCII block):

         ```
         [BAD]  execute_js("document.querySelector('input').value = 'foo'")
         [GOOD] type_text({ selector: "input[name=q]", text: "foo" })
         ```

       - One-line escape hatch: "Reach for `execute_js` only when the typed tools genuinely cannot express the action (rare; e.g., reading a computed style or invoking a non-DOM API)."

    4. "## Tool-by-tool quick reference" H2 section. A markdown table with three columns: Tool | When to use | Common pitfall. Include rows for at least these tools (every name MUST exist in tool-definitions.cjs as a bare token in the Tool column):

       read_page         | Default reader for the active tab text + interactables.            | Stale if a recent action changed the DOM and you have not re-read.
       get_dom_snapshot  | Need attributes, ARIA, hidden nodes that read_page omits.          | Tokens scale with page size; prefer read_page first.
       get_page_snapshot | Need computed layout / position-aware state.                       | Heaviest read; do not call in a tight loop.
       get_site_guide    | Known site -- check first to get stable selectors.                 | Skip if the host has no guide; do not call once per action.
       click             | Fire a real user click on a typed selector.                        | If the click does not register, the element may need scroll_to_element first.
       type_text         | Type into an input/textarea via typed events.                      | Do NOT use execute_js to set .value; React/Vue change handlers will not fire.
       press_enter       | Submit a form / confirm via Enter key.                             | Many sites prefer click on a submit button; fall back to press_enter only if there is no button.
       wait_for_element  | Block until a selector appears.                                    | Always set a timeout; bare waits hang the loop on broken selectors.
       wait_for_stable   | Block until DOM mutations settle.                                  | Pair with wait_for_element when the page renders progressively.
       open_tab          | New tab in agent scope (background by default).                    | Default is background; pass active:true ONLY when the user must see it.
       switch_tab        | Move agent focus to another owned tab.                             | Cross-agent tabs reject with TAB_NOT_OWNED -- see references/multi-agent-contract.md.
       list_tabs         | Enumerate owned tabs (recovery + bootstrap).                       | Recovery-safe even on chrome:// / web-store; see references/restricted-tab-recovery.md.
       navigate          | Change URL in current tab.                                         | Recovery-safe; use to escape restricted tabs.
       go_back           | Step back one history entry.                                       | Use this typed tool, never execute_js("history.back()") -- see references/multi-agent-contract.md.
       go_forward        | Step forward one history entry.                                    | Pair with go_back; same ownership rules.
       refresh           | Reload current tab.                                                | Use after content-script attach failures.
       execute_js        | Last resort for things the typed tools cannot express.             | Bypasses framework change handlers; do not use for input or clicks.

       Notes for the executor: keep the table compact (one line per cell). Every Tool-column token must be a name listed in mcp/ai/tool-definitions.cjs. Do NOT include `run_task`, `back`, `fill_credential`, `use_payment_method`, `start_visual_session`, or `end_visual_session` as a bare token in the Tool column -- those live in narrative prose elsewhere.

    5. "## When to escalate to autopilot" H2 section. Two short paragraphs of NARRATIVE prose (no Tool-column row, no fenced example):
       - First paragraph names the autopilot surface and frames it: "Autopilot (the `run_task` MCP tool, served by mcp/src/tools/autopilot.ts) is a separate surface. It runs FSB's plan-and-execute loop end-to-end. It is NOT in mcp/ai/tool-definitions.cjs because it is not a manual-mode tool -- it is the delegation surface."
       - Second paragraph states the rule: "Use autopilot ONLY when the user explicitly delegates the whole task ('use FSB autopilot to ...', 'run the full task with FSB'). Default to manual mode tools above for everything else. Autopilot is not the default entry point."

    6. "## See also" H2 section. Bulleted cross-links to sibling reference files:
       - `references/multi-agent-contract.md` -- typed errors and the `back` tool.
       - `references/restricted-tab-recovery.md` -- DOM tools fail on `chrome://` / `edge://` / Web Store.
       - `references/default-to-fsb.md` -- when to prefer FSB vs WebFetch.
       - `references/vault-boundary.md` -- credential-routed tools.

    Plain ASCII. Zero emojis. Backtick-quoted tool names everywhere. No emoji status markers; ASCII only ([OK], [FAIL], [WARN]) if used at all.
  </action>
  <verify>
    <automated>test -f "skills/FSB Skill/references/tool-decision-tree.md" && grep -q '\bread_page\b' "skills/FSB Skill/references/tool-decision-tree.md" && grep -q '\bget_dom_snapshot\b' "skills/FSB Skill/references/tool-decision-tree.md" && grep -q '\bget_page_snapshot\b' "skills/FSB Skill/references/tool-decision-tree.md" && grep -q '\bget_site_guide\b' "skills/FSB Skill/references/tool-decision-tree.md" && grep -q '\btype_text\b' "skills/FSB Skill/references/tool-decision-tree.md" && grep -q 'autopilot' "skills/FSB Skill/references/tool-decision-tree.md" && ! grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' "skills/FSB Skill/references/tool-decision-tree.md"</automated>
  </verify>
  <done>
    skills/FSB Skill/references/tool-decision-tree.md exists.
    The four read-only tools (read_page, get_dom_snapshot, get_page_snapshot, get_site_guide) appear in the read-only decision tree.
    The typed-events-over-.value rule appears with a [BAD]/[GOOD] anti-pattern block.
    Every bare tool-name token in the tree/table is also present as a `name:` field in mcp/ai/tool-definitions.cjs (executor must run `grep -nE "name:\\s*'" mcp/ai/tool-definitions.cjs` and cross-check before declaring done).
    run_task is mentioned only in the autopilot escalation prose -- never as a tree bullet or table row.
    Zero emojis.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author references/multi-agent-contract.md (DOCS-05)</name>
  <files>skills/FSB Skill/references/multi-agent-contract.md</files>
  <read_first>
    - skills/FSB Skill/SKILL.md (existing pointer to references/multi-agent-contract.md must resolve to this new file)
    - mcp/ai/tool-definitions.cjs lines 105-150 (go_back / go_forward / refresh descriptions name the typed errors and ownership rule -- mirror that wording rather than inventing new behavior)
    - .planning/phases/250-usage-md-references-policy/250-CONTEXT.md "references/multi-agent-contract.md (DOCS-05)" decisions block (four typed errors + back rule)
    - ./CLAUDE.md (NO EMOJIS rule)
  </read_first>
  <action>
    Create skills/FSB Skill/references/multi-agent-contract.md from scratch. Build the file in this order:

    1. H1: "FSB multi-agent contract (v0.8.0)". One-paragraph framing: this file documents the rules that make FSB's per-agent tab ownership work. Anyone calling FSB tools through MCP MUST follow these rules; breaking them produces typed errors that recover cleanly only when the caller knows the contract.

    2. "## Never pass agent_id" H2 section. Two short paragraphs:
       - State the rule once: "Callers MUST NOT pass `agent_id` on any FSB MCP tool call. The FSB MCP server mints `agent_id` when a session opens and threads it through the bridge automatically."
       - Explain why in one line: "Caller-supplied agent_id breaks ownership accounting -- a fabricated id either steals tabs from another agent or claims tabs the bridge has not handed out."
       - One-line anti-pattern callout in a fenced ASCII block:

         ```
         [BAD]  click({ selector: "#submit", agent_id: "openclaw-1" })
         [GOOD] click({ selector: "#submit" })
         ```

    3. "## Four typed errors and how to recover" H2 section. One subsection per typed error. Each subsection has H3 title (the error name as a bare token), one-line "What it means", one-line "What to do". Use the four error names verbatim:

       ### TAB_NOT_OWNED
       - What it means: The tool targeted a tab that belongs to a different agent (or to `legacy:sidepanel`).
       - What to do: Either call `switch_tab` to a tab this agent owns, or `open_tab` to create a new one in agent scope.

       ### AGENT_CAP_REACHED
       - What it means: The session hit its concurrent-agent cap (default 8, configurable 1-64 in extension settings).
       - What to do: Reduce parallel calls, finish in-flight tasks, or raise the cap from the FSB extension's options page.

       ### TAB_INCOGNITO_NOT_SUPPORTED
       - What it means: The targeted tab is in an Incognito window. FSB rejects Incognito tabs at the dispatcher boundary because the extension's storage and ownership model do not span Incognito.
       - What to do: Move the work to a normal (non-Incognito) Chrome window. Open a new tab there with `open_tab`.

       ### TAB_OUT_OF_SCOPE
       - What it means: The targeted tab is in a different Chrome window than the agent's session window. Each agent only sees tabs in the window where its session began.
       - What to do: Use `list_tabs` to enumerate this agent's owned tabs and pick one; if the work belongs in a different window, open a new tab in the current window with `open_tab`.

    4. "## Use back instead of execute_js('history.back()')" H2 section. Two short paragraphs + a [BAD]/[GOOD] anti-pattern block:
       - Rule paragraph: "Use the typed `back` tool (FSB v0.8.0+) for one-step history navigation. Do NOT call `execute_js(\"history.back()\")`."
       - Why paragraph: "`back` is typed, ownership-gated, BF-cache aware, and history-depth-tracking. It returns a structured result with status fields the caller can branch on. `execute_js(\"history.back()\")` is none of those: it bypasses ownership, lies about success on BF-cached pages, and returns whatever the JS evaluator coerced last."
       - Anti-pattern block:

         ```
         [BAD]  execute_js("history.back()")
         [GOOD] back({})
         ```

       - Note: `go_back` (the existing tool in mcp/ai/tool-definitions.cjs) is the same surface; some FSB versions expose this as `back`, others as `go_back`. Both are typed, ownership-gated, and BF-cache aware. Use whichever is published by your FSB build; never fall back to execute_js.

    5. "## Why the contract matters" H2 section (one short paragraph, optional but recommended): "FSB's per-agent ownership is what lets multiple AI sessions share one Chrome instance without fighting. The contract is small (four error names + agent_id + back), but every rule matters -- bypassing one rule corrupts the ownership graph and the only recovery is closing the affected tab."

    6. "## See also" H2 section. Bulleted cross-links:
       - `references/tool-decision-tree.md` -- which tools to reach for first.
       - `references/restricted-tab-recovery.md` -- recovery on chrome:// / edge:// / Web Store tabs.
       - `references/default-to-fsb.md` -- soft preference and hard escalation rule.
       - `references/vault-boundary.md` -- credential routing rules.

    Plain ASCII. Zero emojis. Every typed error name appears verbatim (TAB_NOT_OWNED, AGENT_CAP_REACHED, TAB_INCOGNITO_NOT_SUPPORTED, TAB_OUT_OF_SCOPE). The `back` tool name appears at least once. The phrase `agent_id` (with underscore) appears at least once.
  </action>
  <verify>
    <automated>test -f "skills/FSB Skill/references/multi-agent-contract.md" && grep -q 'TAB_NOT_OWNED' "skills/FSB Skill/references/multi-agent-contract.md" && grep -q 'AGENT_CAP_REACHED' "skills/FSB Skill/references/multi-agent-contract.md" && grep -q 'TAB_INCOGNITO_NOT_SUPPORTED' "skills/FSB Skill/references/multi-agent-contract.md" && grep -q 'TAB_OUT_OF_SCOPE' "skills/FSB Skill/references/multi-agent-contract.md" && grep -qE '\bback\b' "skills/FSB Skill/references/multi-agent-contract.md" && grep -q 'agent_id' "skills/FSB Skill/references/multi-agent-contract.md" && ! grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' "skills/FSB Skill/references/multi-agent-contract.md"</automated>
  </verify>
  <done>
    skills/FSB Skill/references/multi-agent-contract.md exists.
    All four typed error names (TAB_NOT_OWNED, AGENT_CAP_REACHED, TAB_INCOGNITO_NOT_SUPPORTED, TAB_OUT_OF_SCOPE) appear verbatim.
    The `back` tool is named in its own H2 section as the typed replacement for `execute_js("history.back()")`.
    The agent_id rule (callers MUST NOT pass it) is stated explicitly with an anti-pattern block.
    Zero emojis.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify:

1. `test -f "skills/FSB Skill/references/tool-decision-tree.md"` succeeds.
2. `test -f "skills/FSB Skill/references/multi-agent-contract.md"` succeeds.
3. Cross-check every bare tool token in tool-decision-tree.md against mcp/ai/tool-definitions.cjs:
   `for tok in $(grep -oE '\b[a-z_]+\b' "skills/FSB Skill/references/tool-decision-tree.md" | sort -u); do ...`. Specifically, the executor must verify `read_page`, `get_dom_snapshot`, `get_page_snapshot`, `get_site_guide`, `click`, `type_text`, `wait_for_element`, `open_tab`, `switch_tab`, `list_tabs`, `navigate`, `go_back`, `go_forward`, `refresh`, `execute_js` all appear as `name:` fields in tool-definitions.cjs (they all do; spot-check is sufficient).
4. multi-agent-contract.md contains all four typed errors verbatim AND the `back` token.
5. No emojis in either file (`grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]'` returns zero).
</verification>

<success_criteria>
- DOCS-04: tool-decision-tree.md explains read_page vs get_dom_snapshot vs get_page_snapshot vs get_site_guide; rules typed events over .value; restricts run_task framing to autopilot delegation prose.
- DOCS-05: multi-agent-contract.md documents the agent_id rule, all four typed errors with one-line meaning + one-line action, and the back tool as the typed replacement for execute_js("history.back()").
- Zero emojis. Plain ASCII. Phase 251 TEST-04 grep checks pre-pass against the local files.
</success_criteria>

<output>
After completion, create `.planning/phases/250-usage-md-references-policy/250-02-SUMMARY.md` documenting:
- Final line counts for both files.
- The exact list of tool tokens used in tool-decision-tree.md tree+table, confirmed against tool-definitions.cjs.
- Confirmation that all four typed error names appear in multi-agent-contract.md verbatim.
- Any wording deviations from CONTEXT.md decisions.
</output>
