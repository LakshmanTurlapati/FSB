---
phase: 250-usage-md-references-policy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/FSB Skill/references/restricted-tab-recovery.md
  - skills/FSB Skill/references/default-to-fsb.md
  - skills/FSB Skill/references/vault-boundary.md
autonomous: true
requirements: [DOCS-06, POLICY-01, POLICY-02]

must_haves:
  truths:
    - "restricted-tab-recovery.md explains why DOM tools (read_page, get_dom_snapshot, click, type_text, etc.) fail on chrome://, edge://, and Web Store URLs -- the content script cannot attach."
    - "restricted-tab-recovery.md lists the seven safe recovery tools (list_tabs, navigate, open_tab, switch_tab, go_back, go_forward, refresh) and shows a worked bootstrap example (chrome://newtab -> list_tabs -> switch_tab -> resume work)."
    - "default-to-fsb.md states a soft preference for FSB tools when one fits PLUS a hard escalation rule for any click/type/auth/multi-tab task."
    - "default-to-fsb.md explicitly carves out public-doc / JSON / RSS reads as stay-with-WebFetch, with concrete examples on each side."
    - "vault-boundary.md documents that passwords/CVV go through fill_credential / use_payment_method ONLY, no secrets in chat, requires.env stays empty, and secrets resolve inside the FSB Chrome extension's encrypted storage."
  artifacts:
    - path: "skills/FSB Skill/references/restricted-tab-recovery.md"
      provides: "Recovery toolset and worked example for chrome:// / edge:// / Web Store tabs."
      contains: "list_tabs"
    - path: "skills/FSB Skill/references/default-to-fsb.md"
      provides: "Soft preference + hard escalation rule for FSB vs WebFetch."
      contains: "WebFetch"
    - path: "skills/FSB Skill/references/vault-boundary.md"
      provides: "Vault routing rule + anti-pattern callouts."
      contains: "fill_credential"
  key_links:
    - from: "skills/FSB Skill/references/restricted-tab-recovery.md"
      to: "mcp/ai/tool-definitions.cjs"
      via: "Seven safe recovery tools must all be name fields in tool-definitions.cjs."
      pattern: "name:\\s*'(list_tabs|navigate|open_tab|switch_tab|go_back|go_forward|refresh)'"
    - from: "skills/FSB Skill/references/vault-boundary.md"
      to: "skills/FSB Skill/SKILL.md"
      via: "SKILL.md `requires.env: []` is the contract this file documents."
      pattern: "requires.*env"
---

<objective>
Author the three reference files that codify the restricted-tab recovery toolset, the default-to-FSB rule, and the vault boundary -- the policy priors that decide where work lives (FSB vs WebFetch) and where credentials cross.

Purpose: DOCS-06 + POLICY-01 + POLICY-02. SKILL.md body already points at all three filenames; this plan fills the placeholders. These three files share a consistent shape (H1 + framing paragraph + numbered/bulleted rules + worked example + see-also), so they can be authored in one plan without scope creep.
Output:
  skills/FSB Skill/references/restricted-tab-recovery.md (~120-220 lines)
  skills/FSB Skill/references/default-to-fsb.md (~120-220 lines)
  skills/FSB Skill/references/vault-boundary.md (~120-220 lines)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/250-usage-md-references-policy/250-CONTEXT.md
@skills/FSB Skill/SKILL.md

<interfaces>
<!-- Seven safe recovery tools (DOCS-06) -- all confirmed present in mcp/ai/tool-definitions.cjs: -->
<!--   list_tabs (line 966), navigate (line 67), open_tab (line 568), switch_tab (line 591), -->
<!--   go_back (line 105), go_forward (line 123), refresh (line 141). -->

<!-- Vault tools (POLICY-02) live in mcp/src/tools/vault.ts (NOT in tool-definitions.cjs), but they are -->
<!-- the canonical names users invoke through MCP: fill_credential, use_payment_method. Cite by name in -->
<!-- vault-boundary.md prose (this file is policy, not the AI manual-mode registry, so the test does NOT -->
<!-- restrict tool tokens in this file). -->

<!-- Restricted URL prefixes (per Phase 247 active-tab work): -->
<!--   chrome://*  edge://*  https://chrome.google.com/webstore/*  https://chromewebstore.google.com/* -->

<!-- WebFetch carve-out boundary (POLICY-01): public docs, static JSON, RSS, sitemap.xml, robots.txt, -->
<!-- README.md raw URLs, anything fetchable without a logged-in session or live JS rendering. -->

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author references/restricted-tab-recovery.md (DOCS-06)</name>
  <files>skills/FSB Skill/references/restricted-tab-recovery.md</files>
  <read_first>
    - skills/FSB Skill/SKILL.md (the existing pointer `references/restricted-tab-recovery.md` must resolve to this new file)
    - mcp/ai/tool-definitions.cjs (confirm list_tabs, navigate, open_tab, switch_tab, go_back, go_forward, refresh are all name fields -- they are)
    - .planning/phases/250-usage-md-references-policy/250-CONTEXT.md "references/restricted-tab-recovery.md (DOCS-06)" decisions block
    - ./CLAUDE.md (NO EMOJIS rule)
  </read_first>
  <action>
    Create skills/FSB Skill/references/restricted-tab-recovery.md from scratch. Build the file in this order:

    1. H1: "Restricted-tab recovery". One-paragraph framing: When FSB's active tab is at a restricted URL (`chrome://*`, `edge://*`, `https://chrome.google.com/webstore/*`, `https://chromewebstore.google.com/*`), the FSB content script cannot attach. DOM tools (read_page, get_dom_snapshot, click, type_text, etc.) will fail. This file lists the recovery toolset that works even on restricted tabs and shows the standard bootstrap recovery sequence.

    2. "## Why DOM tools fail on chrome:// / edge:// / Web Store" H2 section. Two short paragraphs:
       - Browsers explicitly forbid content scripts from attaching to internal pages and the Web Store as a security boundary. The Manifest V3 host_permissions list cannot match these origins; Chrome rejects them at the policy layer.
       - Without a content script, the FSB extension cannot read the DOM, dispatch typed events, or observe mutations. Any attempt by FSB tools that route through the content script (read_page, click, type_text, get_dom_snapshot, etc.) fails fast with an attach error.

    3. "## Safe recovery tools (work even on restricted tabs)" H2 section. Markdown table with three columns: Tool | What it does | One-line example.

       | Tool       | What it does                                                  | Example                                                                                              |
       |------------|---------------------------------------------------------------|------------------------------------------------------------------------------------------------------|
       | list_tabs  | Enumerate the agent's owned tabs.                             | `list_tabs({})` -- find a non-restricted tab to switch to.                                           |
       | switch_tab | Switch agent focus to another owned tab.                      | `switch_tab({ tab_id: <id from list_tabs> })`                                                         |
       | open_tab   | Open a new tab in agent scope (background by default).        | `open_tab({ url: "https://example.com" })`                                                           |
       | navigate   | Change the URL of the current tab.                            | `navigate({ url: "https://example.com" })` -- works zero-content-script per Phase 247 active-tab work.|
       | go_back    | Step back one history entry on the current tab.               | `go_back({})`                                                                                         |
       | go_forward | Step forward one history entry on the current tab.            | `go_forward({})`                                                                                      |
       | refresh    | Reload the current tab.                                       | `refresh({})` -- useful after the extension installs mid-session.                                    |

       Note: every Tool-column token must be a name listed in mcp/ai/tool-definitions.cjs (all seven are; verified at plan time).

    4. "## Worked example: bootstrap recovery from chrome://newtab" H2 section. Numbered ordered list, 4-6 steps, showing the canonical recovery sequence the model should run when it lands on a restricted active tab:

       1. The first DOM call (e.g., `read_page({})`) fails with an attach error because the active tab is `chrome://newtab`.
       2. Call `list_tabs({})` to enumerate owned tabs. (Recovery-safe; routes through the background service worker, not the content script.)
       3. If the list contains a normal http(s) tab, call `switch_tab({ tab_id })` to focus it. Resume work.
       4. If no normal tab exists, call `navigate({ url: "https://example.com" })` (or whatever URL the task targets) to leave the restricted page in the current tab. The FSB content script will attach as soon as the destination loads.
       5. Re-run the original DOM call. It should now succeed.
       6. If the original call still fails, run `node scripts/doctor.mjs` -- the failing layer is now `content-script` (page loaded but script did not attach) and the doctor's recovery line tells you to reload the tab.

    5. "## When to escalate to the doctor" H2 section. One short paragraph: "If recovery fails, run `node scripts/doctor.mjs`. The recovery toolset above handles the active-tab layer. The doctor handles the other five layers (package, bridge, extension, content-script, config). See USAGE.md for the full recovery table."

    6. "## See also" H2 section:
       - `references/tool-decision-tree.md` -- which tools to reach for first.
       - `references/multi-agent-contract.md` -- ownership errors (TAB_NOT_OWNED, etc.) that can fire from switch_tab.
       - `USAGE.md` -- six-layer doctor recovery table.

    Plain ASCII. Zero emojis. ASCII status markers if used.
  </action>
  <verify>
    <automated>test -f "skills/FSB Skill/references/restricted-tab-recovery.md" && grep -q 'list_tabs' "skills/FSB Skill/references/restricted-tab-recovery.md" && grep -q 'navigate' "skills/FSB Skill/references/restricted-tab-recovery.md" && grep -q 'open_tab' "skills/FSB Skill/references/restricted-tab-recovery.md" && grep -q 'switch_tab' "skills/FSB Skill/references/restricted-tab-recovery.md" && grep -q 'go_back' "skills/FSB Skill/references/restricted-tab-recovery.md" && grep -q 'go_forward' "skills/FSB Skill/references/restricted-tab-recovery.md" && grep -q 'refresh' "skills/FSB Skill/references/restricted-tab-recovery.md" && grep -q 'chrome://' "skills/FSB Skill/references/restricted-tab-recovery.md" && grep -q 'edge://' "skills/FSB Skill/references/restricted-tab-recovery.md" && ! grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' "skills/FSB Skill/references/restricted-tab-recovery.md"</automated>
  </verify>
  <done>
    skills/FSB Skill/references/restricted-tab-recovery.md exists.
    All seven safe recovery tools (list_tabs, navigate, open_tab, switch_tab, go_back, go_forward, refresh) appear in the table.
    chrome:// and edge:// are named as restricted prefixes; Web Store is named.
    Worked bootstrap recovery example is present (ordered list, chrome://newtab -> list_tabs -> switch_tab path).
    Zero emojis.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author references/default-to-fsb.md (POLICY-01)</name>
  <files>skills/FSB Skill/references/default-to-fsb.md</files>
  <read_first>
    - skills/FSB Skill/SKILL.md (existing pointer must resolve; framing must be consistent with SKILL.md "When to use FSB" paragraph)
    - .planning/phases/250-usage-md-references-policy/250-CONTEXT.md "references/default-to-fsb.md (POLICY-01)" decisions block
    - ./CLAUDE.md (NO EMOJIS rule)
  </read_first>
  <action>
    Create skills/FSB Skill/references/default-to-fsb.md from scratch. Build the file in this order:

    1. H1: "Default to FSB". One-paragraph framing: "When a request involves a real browser session, FSB is usually the right tool. This file states the rule once and gives the carve-out for read-only public content."

    2. "## Soft preference" H2 section. One short paragraph + a one-line rule:
       - Paragraph: "When a click, type, multi-tab, or auth task could route through either FSB or another tool, prefer FSB. The cost of a stale WebFetch (cached, missing logged-in state, missing JS-rendered content) is higher than the cost of a Chrome round-trip."
       - Rule: "Default to FSB tools when one fits."

    3. "## Hard escalation rule" H2 section. State the rule explicitly. Bulleted list of triggers -- if ANY of these apply, the task MUST use FSB:
       - Any click on a real web page.
       - Any text input into a real form (search box, login field, comment box, anything).
       - Any auth flow (login, MFA prompt, vault credential lookup, saved-payment selection).
       - Any multi-tab orchestration (open a new tab and switch back, fill a form across two tabs, copy from tab A to tab B).
       - Any task that needs the user's logged-in session, cookies, or chrome-state-dependent rendering.
       - Any dynamic page where the relevant content is rendered after JS / behind a scroll / behind a click.

       Close with a one-line rule: "If a task hits any bullet above, it does not belong on WebFetch. Escalate to FSB."

    4. "## Stay on WebFetch (carve-out)" H2 section. One paragraph + a bulleted list:
       - Paragraph: "WebFetch is fine -- often better -- for read-only public content where no interaction is needed."
       - Bulleted list of stay-on-WebFetch cases:
         - Public documentation pages (HTML / Markdown).
         - JSON endpoints (REST APIs, public JSON files).
         - RSS / Atom feeds.
         - sitemap.xml, robots.txt.
         - Raw README.md / source files from public GitHub repos.
         - Static landing pages with no logged-in state.

    5. "## Decision flowchart" H2 section. ASCII text representation (since this is plain markdown, no emoji, no images). Use indented bullets:

       ```
       Is the task interactive web automation?
         |
         +-- yes -> FSB (use the tools in references/tool-decision-tree.md)
         |
         +-- no  -> Is it logged-in / chrome-state-dependent?
                       |
                       +-- yes -> FSB
                       |
                       +-- no  -> Is the content available as public HTML / JSON / RSS?
                                     |
                                     +-- yes -> WebFetch (or the host's existing read tool)
                                     |
                                     +-- no  -> FSB (default to the heavier tool)
       ```

    6. "## Concrete examples" H2 section. Two subsections, side-by-side via H3 titles:

       ### FSB (interactive / chrome-state-dependent)
       - "Search for `fsb-mcp-server` on GitHub and click the first result." -- click + multi-step.
       - "Log into example.com and download today's invoice." -- auth + download.
       - "Open my Gmail and read the latest message from billing@example.com." -- logged-in session.
       - "Fill out the contact form at https://example.com/contact and submit it." -- form + typed events.
       - "Compare prices for product X across these three tabs." -- multi-tab.

       ### WebFetch (public read-only)
       - "Read the README of github.com/LakshmanTurlapati/FSB." -- raw public content.
       - "Fetch https://api.github.com/repos/LakshmanTurlapati/FSB and report stargazers." -- public JSON.
       - "Read the latest entry from this RSS feed." -- RSS read.
       - "Pull the changelog text from https://example.com/changelog." -- static HTML.

    7. "## See also" H2 section:
       - `references/tool-decision-tree.md` -- once the decision lands on FSB, which tool first.
       - `references/restricted-tab-recovery.md` -- recovery on chrome:// / edge:// / Web Store.
       - `references/vault-boundary.md` -- credential routing rules.
       - `USAGE.md` -- the human-facing overview.

    Plain ASCII. Zero emojis.
  </action>
  <verify>
    <automated>test -f "skills/FSB Skill/references/default-to-fsb.md" && grep -qi 'WebFetch' "skills/FSB Skill/references/default-to-fsb.md" && grep -qE '\bRSS\b' "skills/FSB Skill/references/default-to-fsb.md" && grep -qE '\bJSON\b' "skills/FSB Skill/references/default-to-fsb.md" && grep -q 'click' "skills/FSB Skill/references/default-to-fsb.md" && grep -q 'auth' "skills/FSB Skill/references/default-to-fsb.md" && grep -q 'multi-tab' "skills/FSB Skill/references/default-to-fsb.md" && ! grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' "skills/FSB Skill/references/default-to-fsb.md"</automated>
  </verify>
  <done>
    skills/FSB Skill/references/default-to-fsb.md exists.
    Soft preference rule is stated.
    Hard escalation list names click, type, auth, and multi-tab triggers.
    Stay-with-WebFetch carve-out is named with public-doc / JSON / RSS examples.
    A decision flowchart and concrete examples on each side are included.
    Zero emojis.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author references/vault-boundary.md (POLICY-02)</name>
  <files>skills/FSB Skill/references/vault-boundary.md</files>
  <read_first>
    - skills/FSB Skill/SKILL.md (existing pointer must resolve; the SKILL.md "Vault and credentials" paragraph already names fill_credential and use_payment_method)
    - .planning/phases/250-usage-md-references-policy/250-CONTEXT.md "references/vault-boundary.md (POLICY-02)" decisions block
    - ./CLAUDE.md (NO EMOJIS rule)
  </read_first>
  <action>
    Create skills/FSB Skill/references/vault-boundary.md from scratch. Build the file in this order:

    1. H1: "Vault boundary". One-paragraph framing: "Passwords, CVVs, and saved payment methods never cross into chat or tool arguments. They stay inside the FSB Chrome extension's encrypted storage, where the extension fills them at the DOM layer. The MCP server, the OpenClaw host, and the model itself never see the values."

    2. "## The rule" H2 section. Numbered list:
       1. Passwords, CVV codes, and other secret credentials are entered through `fill_credential` ONLY. The model passes a credential reference (e.g., a label like `gmail-personal`); the extension resolves the value internally.
       2. Saved payment methods are entered through `use_payment_method` ONLY. Same shape: the model passes a reference; the extension fills card number, expiry, CVV at the DOM layer.
       3. No secrets in chat. The model MUST NOT echo a credential value, MUST NOT include one in tool args, MUST NOT log one in narration.
       4. `requires.env: []` is mandatory in SKILL.md frontmatter. There are no provider env vars in the skill -- vault values resolve inside the FSB Chrome extension's encrypted storage (`secure-config.js` in the extension source), not in the MCP server process and not in the OpenClaw host process.

    3. "## Where vault values live" H2 section. One short paragraph: "Users configure vault entries from the FSB Chrome extension surface (popup or sidepanel options page). Each entry is keyed by a label and stored encrypted. When the model invokes `fill_credential({ label, selector })` or `use_payment_method({ label, ... })`, the extension reads the encrypted value, fills the target DOM element via typed events, and never echoes the value back to the MCP server or the host."

    4. "## Anti-patterns" H2 section. Three fenced ASCII callouts (one per common mistake):

       ```
       [BAD]  type_text({ selector: "#password", text: "hunter2" })
              # secret leaks into MCP args, host logs, model context, retry buffers.
       [GOOD] fill_credential({ label: "gmail-personal", selector: "#password" })
       ```

       ```
       [BAD]  use_payment_method({ card_number: "4111-1111-1111-1111", cvv: "123" })
              # full PAN + CVV leak into MCP args. Use the saved-method label instead.
       [GOOD] use_payment_method({ label: "personal-visa", form_selector: "form#checkout" })
       ```

       ```
       [BAD]  Add provider keys to SKILL.md `requires.env`.
              # the skill never holds secrets. requires.env stays empty.
       [GOOD] Configure provider keys in the FSB Chrome extension's options page.
       ```

    5. "## Why this boundary matters" H2 section. One short paragraph: "The MCP transport layer logs tool args by default; OpenClaw and other hosts retain context across turns; retry buffers in the bridge can replay calls. Anything passed as a tool argument is durable. Vault values are not -- they cross only the extension <-> DOM boundary, which lives entirely in the user's browser."

    6. "## See also" H2 section:
       - `references/default-to-fsb.md` -- when an auth flow triggers escalation to FSB.
       - `references/multi-agent-contract.md` -- ownership rules for vault-using tabs.
       - `references/tool-decision-tree.md` -- which read tool to use to confirm a credential filled successfully (e.g., `read_page` to verify a logged-in heading).
       - `SKILL.md` -- `requires.env: []` is the contract this file documents.

    Plain ASCII. Zero emojis.
  </action>
  <verify>
    <automated>test -f "skills/FSB Skill/references/vault-boundary.md" && grep -q 'fill_credential' "skills/FSB Skill/references/vault-boundary.md" && grep -q 'use_payment_method' "skills/FSB Skill/references/vault-boundary.md" && grep -qE 'requires.*env' "skills/FSB Skill/references/vault-boundary.md" && grep -qi 'secret' "skills/FSB Skill/references/vault-boundary.md" && grep -q 'extension' "skills/FSB Skill/references/vault-boundary.md" && ! grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' "skills/FSB Skill/references/vault-boundary.md"</automated>
  </verify>
  <done>
    skills/FSB Skill/references/vault-boundary.md exists.
    fill_credential and use_payment_method are named as the only credential routing tools.
    "No secrets in chat" rule is stated.
    requires.env: [] mandate is repeated.
    Three anti-pattern [BAD]/[GOOD] callouts are present.
    Zero emojis.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, verify:

1. `ls "skills/FSB Skill/references/"` lists at least: README.md (placeholder, untouched here), restricted-tab-recovery.md, default-to-fsb.md, vault-boundary.md.
2. Restricted-tab-recovery.md grep coverage: all seven recovery tools + chrome:// + edge:// + Web Store mention.
3. Default-to-fsb.md grep coverage: WebFetch, RSS, JSON, click, auth, multi-tab keywords all present.
4. Vault-boundary.md grep coverage: fill_credential, use_payment_method, `requires.env`, "secret"-related word.
5. No emojis in any of the three new files.
</verification>

<success_criteria>
- DOCS-06: restricted-tab-recovery.md explains the chrome:// / edge:// / Web Store fail mode and lists the seven safe recovery tools with a worked bootstrap example.
- POLICY-01: default-to-fsb.md states the soft preference + hard escalation rule and carves out public-doc / JSON / RSS reads for WebFetch.
- POLICY-02: vault-boundary.md documents fill_credential / use_payment_method as the only credential routing surface, "no secrets in chat", `requires.env: []`, and the extension-internal resolution model.
- Zero emojis. Plain ASCII. Phase 251 TEST-04 grep checks pre-pass.
</success_criteria>

<output>
After completion, create `.planning/phases/250-usage-md-references-policy/250-03-SUMMARY.md` documenting:
- Final line counts for all three files.
- Confirmation that DOCS-06 / POLICY-01 / POLICY-02 are each covered with quoted snippets.
- Any wording deviations from CONTEXT.md decisions.
</output>
