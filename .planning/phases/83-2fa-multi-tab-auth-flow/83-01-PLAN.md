---
phase: 83-2fa-multi-tab-auth-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site-guides/utilities/two-factor-auth.js
  - background.js
autonomous: true
requirements:
  - CONTEXT-07

must_haves:
  truths:
    - "The two-factor-auth.js site guide contains a twoFactorMultiTab workflow documenting the full sequence: navigate to a site with 2FA login, enter credentials, trigger 2FA code delivery (email), open new tab to email provider, search for 2FA code email, extract the numeric code, switch back to the original auth tab, enter the code, submit, and verify login succeeded"
    - "The two-factor-auth.js site guide contains a CONTEXT-07 section in its guidance string explaining multi-tab 2FA context retention strategy: remember the auth tab ID before opening email tab, extract only the code (not full email DOM), switch back by stored tab ID (not by guessing)"
    - "The two-factor-auth.js site guide documents 2FA demo site targets for testing: sites with demo/test 2FA flows that do not require real accounts, with fallback to skip-auth if none available"
    - "The two-factor-auth.js site guide documents context bloat mitigation for multi-tab 2FA: extract only the 6-digit code from email tab (not full inbox DOM), store compact {authTabId, emailTabId, code} record"
  artifacts:
    - path: "site-guides/utilities/two-factor-auth.js"
      provides: "twoFactorMultiTab workflow and CONTEXT-07 guidance for 2FA multi-tab authentication flow"
      contains: "twoFactorMultiTab"
    - path: "background.js"
      provides: "Import registration for two-factor-auth.js site guide"
      contains: "two-factor-auth"
  key_links:
    - from: "site-guides/utilities/two-factor-auth.js"
      to: "mcp-server/src/tools/manual.ts"
      via: "toolPreferences referencing open_tab, switch_tab, navigate, click, type_text, read_page for multi-tab 2FA flow"
      pattern: "open_tab|switch_tab|navigate|click|type_text|read_page"
    - from: "site-guides/utilities/two-factor-auth.js"
      to: "mcp-server/src/tools/read-only.ts"
      via: "list_tabs read-only tool for enumerating open tabs with IDs and URLs"
      pattern: "list_tabs"
    - from: "background.js"
      to: "site-guides/utilities/two-factor-auth.js"
      via: "importScripts statement wiring the 2FA site guide into the extension"
      pattern: "two-factor-auth"
---

<objective>
Create a two-factor-auth.js site guide with a twoFactorMultiTab workflow for CONTEXT-07 (handle 2FA flow: trigger email code, open new tab to fetch code, return to complete login) and wire it into background.js.

Purpose: CONTEXT-07 requires executing a 2FA authentication flow that spans multiple tabs: (a) login page triggers 2FA code delivery via email, (b) open a new tab to an email provider to retrieve the code, (c) extract the numeric code from the email, (d) switch back to the auth tab, (e) enter the code and complete login. This is a "Context Bloat & Long-Running Workflows" edge case -- the challenge is multi-tab state management (remembering which tab is the auth tab vs email tab) while keeping context lean (extract only the 6-digit code, not the full email inbox DOM). The site guide documents: (a) 2FA demo site targets for testing without real accounts, (b) twoFactorMultiTab workflow covering the full login-through-2FA-completion sequence, (c) CONTEXT-07 guidance for tab ID retention and code extraction, (d) email provider code extraction patterns (Gmail, Outlook, Yahoo Mail), (e) context bloat mitigation for multi-tab 2FA, and (f) fallback strategies when demo sites are unavailable.

Output: New site-guides/utilities/two-factor-auth.js with CONTEXT-07 workflow and guidance, background.js updated with import
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-2fa-multi-tab-auth-flow/83-CONTEXT.md
@site-guides/utilities/support-chatbot.js
@site-guides/email/gmail.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create two-factor-auth.js site guide with twoFactorMultiTab workflow and CONTEXT-07 guidance</name>
  <files>site-guides/utilities/two-factor-auth.js</files>
  <read_first>
    - site-guides/utilities/support-chatbot.js (reference site guide structure in Utilities category: registerSiteGuide call with site, category, patterns, guidance, selectors, workflows, warnings, toolPreferences -- use as structural template)
    - site-guides/utilities/carousel.js lines 1-40 (another Utilities site guide for structure reference)
    - site-guides/email/gmail.js lines 1-60 (Gmail email provider selectors and patterns for code extraction from email tab)
    - site-guides/email/outlook.js lines 1-40 (Outlook email selectors for fallback email provider)
    - .planning/phases/83-2fa-multi-tab-auth-flow/83-CONTEXT.md (target: 2FA multi-tab auth flow with email code fetch)
    - mcp-server/src/tools/manual.ts lines 277-289 (open_tab, switch_tab tool signatures for multi-tab workflow)
    - mcp-server/src/tools/read-only.ts lines 92-108 (list_tabs tool signature for tab enumeration)
  </read_first>
  <action>
Create a new file site-guides/utilities/two-factor-auth.js with a registerSiteGuide call following the same structure as support-chatbot.js and carousel.js. The site guide targets websites with 2FA login flows that send verification codes via email, focusing on the multi-tab pattern of fetching the code from an email provider tab and returning to the auth tab.

**File structure:**

```javascript
/**
 * Site Guide: Two-Factor Authentication
 * Per-site guide for 2FA login flows that require fetching a verification code
 * from email in a separate tab and returning to complete authentication.
 *
 * Key challenge: maintaining tab state (auth tab vs email tab) while extracting
 * only the verification code from the email provider, not the full inbox DOM.
 * Uses open_tab, switch_tab, list_tabs for multi-tab orchestration.
 *
 * Created for Phase 83, CONTEXT-07 edge case validation.
 * Target: handle 2FA flow -- trigger email code, open new tab to fetch, return to complete login.
 */

registerSiteGuide({
  site: 'Two-Factor Authentication',
  category: 'Utilities',
  patterns: [
    /2fa/i,
    /two.factor/i,
    /verify.*code/i,
    /login.*verification/i,
    /otp/i,
    /one.time.password/i,
    /practiceautomation\.com/i,
    /the-internet\.herokuapp\.com/i,
    /conduit\.realworld\.how/i,
    /demoqa\.com/i,
    /uitestingplayground\.com/i
  ],
  guidance: `...`,
  selectors: {...},
  workflows: {...},
  warnings: [...],
  toolPreferences: [...]
});
```

**Guidance string content (insert as template literal):**

```
TWO-FACTOR AUTHENTICATION INTELLIGENCE:

TARGET SELECTION (CONTEXT-07):
- This edge case tests MULTI-TAB 2FA: trigger email code delivery on auth site, open new tab to email provider, extract code, switch back, enter code, complete login
- The challenge is finding a demo 2FA site that: (a) has a working 2FA login, (b) sends a code to an accessible email, (c) does not require a real paid account
- Primary target: practiceautomation.com/2fa or similar 2FA practice site
  -- Look for sites that simulate 2FA by displaying the code on a separate page or sending to a test email inbox
- Fallback 1: the-internet.herokuapp.com/login (Heroku test app -- basic auth, may not have 2FA but demonstrates multi-step login)
- Fallback 2: conduit.realworld.how (RealWorld demo app -- registration + login, may have 2FA toggle)
- Fallback 3: demoqa.com (has login form but likely no 2FA)
- Fallback 4: Any site with a demo/sandbox 2FA that does not require real email delivery
- ALTERNATIVE APPROACH: If no demo 2FA site is available, simulate the multi-tab pattern:
  (a) Navigate to a login page, (b) Open a new tab to a test email service (e.g., guerrillamail.com, mailinator.com, tempail.com for disposable email), (c) Check for any email, (d) Switch back to original tab
  This demonstrates the multi-tab orchestration pattern even without real 2FA
- SKIP-AUTH: if no demo 2FA site exists and the multi-tab simulation cannot demonstrate the pattern, document as skip-auth

MULTI-TAB 2FA WORKFLOW STRATEGY (CONTEXT-07):
- This workflow tests MULTI-TAB STATE MANAGEMENT: remember the auth tab ID, open email tab, extract code, return to auth tab by ID
- The challenge is maintaining tab identity across switches without losing track of which tab is which

Step-by-step pattern:
  Phase A -- Login and trigger 2FA:
    1. Navigate to target site login page
    2. list_tabs to capture current tab ID -- this is the AUTH TAB (store as authTabId)
    3. Enter username/email credentials
    4. Enter password
    5. Click login/submit button
    6. Wait for 2FA prompt to appear (the site asks for a verification code)
    7. If no 2FA prompt appears: site does not have 2FA, try next target

  Phase B -- Fetch code from email:
    8. open_tab to email provider (Gmail: mail.google.com, Outlook: outlook.live.com, or disposable: guerrillamail.com)
    9. list_tabs to capture email tab ID -- this is the EMAIL TAB (store as emailTabId)
    10. Search for the 2FA email (search for "verification code" or "login code" or sender domain)
    11. Open the most recent matching email
    12. Extract the verification code (typically 4-8 digit number) using read_page
    13. Store the code as a simple string (e.g., "482916") -- do NOT store full email content

  Phase C -- Return and complete login:
    14. switch_tab to authTabId (the original auth tab)
    15. Verify the 2FA input field is still visible
    16. type_text the extracted code into the 2FA input field
    17. Click verify/submit button
    18. Wait for login to complete (redirect to dashboard/homepage)
    19. Verify login succeeded (check for user profile element, logout button, or dashboard content)

CONTEXT RETENTION -- TAB ID MANAGEMENT:
- CRITICAL: Store authTabId BEFORE opening the email tab. This is the key state to retain.
- After open_tab returns the new tab: you are automatically in the new tab (email tab)
- After extracting the code: switch_tab(authTabId) to return to the exact auth tab
- Do NOT use list_tabs to find the auth tab after switching -- rely on the stored authTabId
- Compact state record: {authTabId: N, emailTabId: M, code: "XXXXXX", codeFetched: true}

EMAIL CODE EXTRACTION PATTERNS:
- Gmail: search "is:unread verification code" or "is:unread login code", open first result, read body
- Outlook: search "verification code" in inbox, open first unread, read body
- Disposable email (guerrillamail.com): check inbox for latest email, read body
- Code format: typically 4-8 digits, sometimes with hyphens (e.g., "482916", "48-29-16", "4829-16")
- Extraction: use read_page on the email body, look for a standalone number pattern (\d{4,8})
- IMPORTANT: extract ONLY the code string, not the full email body -- context bloat mitigation

2FA INPUT FIELD PATTERNS:
- Common selectors: input[type="text"][maxlength="6"], input[name*="code"], input[name*="otp"], input[name*="token"], input[autocomplete="one-time-code"]
- Some sites split 2FA into individual digit inputs (6 separate single-digit fields)
  - For split inputs: type one digit per field, or try typing the full code in the first field (some sites auto-advance)
- Label text: "Verification code", "Enter code", "Two-factor code", "OTP", "Authentication code"

VERIFICATION CRITERIA:
- PASS = 2FA code triggered, fetched from email tab, entered on auth tab, login completed
- PARTIAL = multi-tab pattern demonstrated (open_tab, switch_tab, read_page across tabs) but 2FA not fully completed (no demo site has real 2FA, or email delivery not accessible)
- FAIL = could not demonstrate multi-tab orchestration pattern
- SKIP-AUTH = no demo 2FA site available and multi-tab pattern simulation could not run

CONTEXT BLOAT MITIGATION FOR MULTI-TAB 2FA:
- On the auth tab: extract only the 2FA prompt presence (is there a code input field?) -- not the full login page DOM
- On the email tab: extract ONLY the verification code (4-8 digits) -- not the full email body, inbox list, or email headers
- Total context for 2FA flow: under 500 characters (authTabId, emailTabId, code, login status)
- Compare to Phase 80 (multi-tab flights): same open_tab/switch_tab pattern but with code extraction instead of price extraction
- After code entry: do NOT re-read the email tab -- it served its purpose
```

**Selectors object:**

```javascript
selectors: {
    // Login form fields
    usernameInput: 'input[name="username"], input[name="email"], input[type="email"], input[id*="email"], input[id*="username"], input[autocomplete="username"], input[autocomplete="email"]',
    passwordInput: 'input[name="password"], input[type="password"], input[autocomplete="current-password"]',
    loginButton: 'button[type="submit"], input[type="submit"], button:has-text("Log in"), button:has-text("Sign in"), button:has-text("Login"), [data-testid*="login"], [data-testid*="submit"]',
    // 2FA code input (single field)
    codeInput: 'input[name*="code"], input[name*="otp"], input[name*="token"], input[name*="totp"], input[autocomplete="one-time-code"], input[maxlength="6"], input[maxlength="8"], input[placeholder*="code" i], input[placeholder*="digit" i], input[aria-label*="code" i], input[aria-label*="verification" i]',
    // 2FA code input (split digit fields)
    splitDigitInput: 'input[maxlength="1"][type="text"], input[maxlength="1"][type="tel"], input[maxlength="1"][inputmode="numeric"]',
    // 2FA verify/submit button
    verifyButton: 'button[type="submit"], button:has-text("Verify"), button:has-text("Confirm"), button:has-text("Submit"), button:has-text("Continue"), [data-testid*="verify"], [data-testid*="confirm"]',
    // 2FA prompt container (indicates 2FA step is active)
    twoFactorPrompt: '[class*="two-factor"], [class*="2fa"], [class*="otp"], [class*="verification"], [class*="mfa"], [data-testid*="2fa"], [data-testid*="otp"], form[action*="verify"], form[action*="2fa"]',
    // Success indicators (login completed)
    loginSuccess: '[class*="dashboard"], [class*="profile"], [class*="account"], button[aria-label*="logout" i], a[href*="logout"], [data-testid*="avatar"], [data-testid*="user-menu"]',
    // Email code in email body (for read_page extraction)
    emailCodePattern: '[class*="code"], [class*="otp"], [class*="verification"], strong, b, .code, #code',
    // Disposable email inbox
    disposableInbox: '[class*="inbox"], [id*="inbox"], table.email-list, [class*="mail-list"], [data-testid*="inbox"]'
}
```

**Workflows object:**

```javascript
workflows: {
    twoFactorMultiTab: [
      'SETUP: Navigate to target site with 2FA login. Primary: practiceautomation.com/2fa or similar 2FA demo site. If no dedicated 2FA demo available, try simulating multi-tab pattern with disposable email service.',
      'CAPTURE AUTH TAB: Use list_tabs to get the current tab ID. Store as authTabId. This is CRITICAL -- you must remember this ID to return after fetching the code.',
      'ENTER CREDENTIALS: Type username/email into usernameInput field. Type password into passwordInput field.',
      'SUBMIT LOGIN: Click loginButton to submit credentials. Wait for page to respond.',
      'DETECT 2FA PROMPT: Check if a 2FA code input appeared (codeInput or splitDigitInput selector). If yes, 2FA is active. If the site logged in directly (no 2FA), note as "no 2FA on this target" and try next target.',
      'OPEN EMAIL TAB: Use open_tab to open the email provider where the 2FA code was sent. Gmail: https://mail.google.com, Outlook: https://outlook.live.com, Disposable: https://www.guerrillamail.com. Store the new tab ID as emailTabId.',
      'SEARCH FOR CODE EMAIL: On the email tab, search for "verification code" or "login code" or the sender domain. Look for the most recent unread email matching the 2FA pattern.',
      'EXTRACT CODE: Open the 2FA email and use read_page to extract the verification code. Look for a 4-8 digit number in the email body. Store ONLY the code string (e.g., "482916"), not the full email content.',
      'SWITCH BACK TO AUTH TAB: Use switch_tab(authTabId) to return to the original authentication tab. Verify the 2FA input field is still visible.',
      'ENTER CODE: Type the extracted code into the codeInput field. If the site uses split digit inputs (splitDigitInput), type each digit into the corresponding field.',
      'SUBMIT 2FA: Click verifyButton to submit the code. Wait for login to complete.',
      'VERIFY LOGIN: Check for login success indicators (loginSuccess selector): dashboard page, profile menu, logout button, or user avatar. Confirm the user is now authenticated.',
      'REPORT: State outcome (PASS/PARTIAL/FAIL/SKIP-AUTH) with: target site used, 2FA method encountered, code fetched (yes/no), tabs used (authTabId, emailTabId), login completed (yes/no).'
    ],
    simulateMultiTabCodeFetch: [
      'If no real 2FA site is available, demonstrate the multi-tab pattern with a simulated flow:',
      '1. Navigate to any login page (the "auth tab"). Store authTabId via list_tabs.',
      '2. Open a new tab to guerrillamail.com or mailinator.com (the "email tab"). Store emailTabId.',
      '3. Read the disposable email inbox using read_page.',
      '4. Switch back to authTabId using switch_tab.',
      '5. Confirm you are back on the original auth tab.',
      'This validates open_tab, list_tabs, switch_tab, read_page across tabs even without real 2FA.'
    ]
}
```

**Warnings array:**

```javascript
warnings: [
    'Most real 2FA flows require an authenticated account with email access -- demo/test sites may not have working 2FA.',
    'Disposable email services (guerrillamail, mailinator) are public -- anyone can read emails sent to them. Do NOT use for real credentials.',
    'Some 2FA codes expire quickly (60-120 seconds) -- minimize time spent on the email tab.',
    '2FA code input may be a single field (type full code) or split into individual digit inputs (type one digit per field).',
    'After switching tabs with switch_tab, verify the expected page is still loaded -- tabs can refresh or expire.',
    'Email provider may require its own authentication -- if Gmail/Outlook requires login, the 2FA flow adds another auth step.',
    'Some sites use authenticator app TOTP instead of email codes -- this workflow only covers email-based 2FA.',
    'If the 2FA code email has not arrived, wait 10-15 seconds and refresh the inbox before concluding it failed.',
    'For CONTEXT-07: store ONLY authTabId, emailTabId, and the extracted code. Do NOT read full page DOM from either tab.'
]
```

**toolPreferences array:**

```javascript
toolPreferences: ['navigate', 'open_tab', 'switch_tab', 'list_tabs', 'click', 'type_text', 'press_enter', 'read_page', 'wait_for_stable']
```

Ensure the file follows the exact registerSiteGuide structure pattern from support-chatbot.js and carousel.js. The file must be syntactically valid JavaScript with no trailing commas that break parsing.
  </action>
  <verify>
    <automated>grep -q "twoFactorMultiTab" site-guides/utilities/two-factor-auth.js && grep -q "CONTEXT-07" site-guides/utilities/two-factor-auth.js && grep -q "registerSiteGuide" site-guides/utilities/two-factor-auth.js && grep -q "open_tab\|open.tab" site-guides/utilities/two-factor-auth.js && grep -q "switch_tab\|switch.tab" site-guides/utilities/two-factor-auth.js && grep -q "list_tabs\|list.tabs" site-guides/utilities/two-factor-auth.js && grep -q "authTabId" site-guides/utilities/two-factor-auth.js && grep -q "emailTabId" site-guides/utilities/two-factor-auth.js && grep -q "codeInput\|code.*input\|code.*Input" site-guides/utilities/two-factor-auth.js && grep -q "verifyButton\|verify.*button" site-guides/utilities/two-factor-auth.js && grep -q "guerrillamail\|mailinator\|disposable" site-guides/utilities/two-factor-auth.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "twoFactorMultiTab" two-factor-auth.js returns at least 1 match (workflow name)
    - grep "CONTEXT-07" two-factor-auth.js returns at least 2 matches (guidance section and workflow context)
    - grep "registerSiteGuide" two-factor-auth.js returns 1 match (registration call)
    - grep "open_tab" two-factor-auth.js returns at least 3 matches (workflow steps referencing tab opening)
    - grep "switch_tab" two-factor-auth.js returns at least 3 matches (workflow steps referencing tab switching)
    - grep "list_tabs" two-factor-auth.js returns at least 2 matches (tab enumeration for authTabId capture)
    - grep "authTabId" two-factor-auth.js returns at least 3 matches (tab ID storage and retrieval)
    - grep "emailTabId" two-factor-auth.js returns at least 2 matches (email tab ID tracking)
    - grep "codeInput" two-factor-auth.js returns at least 1 match (2FA code input selector)
    - grep "verifyButton" two-factor-auth.js returns at least 1 match (2FA submit button selector)
    - grep "guerrillamail\|mailinator\|disposable" two-factor-auth.js returns at least 1 match (disposable email fallback)
    - grep "toolPreferences" two-factor-auth.js returns 1 match (tool list present)
    - grep "workflows" two-factor-auth.js returns 1 match (workflows object present)
    - grep "context.*bloat\|Context.*Bloat" two-factor-auth.js returns at least 1 match (mitigation section)
    - grep "split.*digit\|splitDigit" two-factor-auth.js returns at least 1 match (split digit input handling)
  </acceptance_criteria>
  <done>two-factor-auth.js created with registerSiteGuide call containing: URL patterns for 2FA-related sites and demo targets, CONTEXT-07 guidance section (target selection with demo/test sites, multi-tab 2FA workflow strategy in 3 phases: login-and-trigger / fetch-code-from-email / return-and-complete, tab ID management with authTabId/emailTabId retention, email code extraction patterns for Gmail/Outlook/disposable, 2FA input field patterns including split digit fields, verification criteria, context bloat mitigation for multi-tab 2FA under 500 chars), twoFactorMultiTab workflow (13 steps: setup, capture auth tab, enter credentials, submit login, detect 2FA prompt, open email tab, search for code email, extract code, switch back to auth tab, enter code, submit 2FA, verify login, report), simulateMultiTabCodeFetch fallback workflow, selectors for login form, 2FA code input, verify button, success indicators, email code patterns, disposable inbox, 9 warnings about demo site limitations and code expiry, 9 tool preferences including multi-tab tools.</done>
</task>

<task type="auto">
  <name>Task 2: Wire two-factor-auth.js import into background.js</name>
  <files>background.js</files>
  <read_first>
    - background.js lines 178-185 (utilities section imports -- find where utilities site guides are imported, specifically after support-chatbot.js to determine exact insertion point)
  </read_first>
  <action>
Add an importScripts line for the new two-factor-auth.js site guide in the Utilities section of background.js. Follow the existing pattern for utilities site guide imports.

Find the block of utilities imports (carousel.js, color-picker.js, file-upload.js, slider-captcha.js, support-chatbot.js) and add:
```javascript
importScripts('site-guides/utilities/two-factor-auth.js');
```

Insert AFTER support-chatbot.js (alphabetically: carousel, color-picker, file-upload, slider-captcha, support-chatbot, two-factor-auth). Maintain the same formatting and no blank lines within the import block.

Do NOT modify any existing import lines. Only ADD the new import for two-factor-auth.js.
  </action>
  <verify>
    <automated>grep -q "two-factor-auth" background.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "two-factor-auth" background.js returns 1 match (importScripts line)
    - The import line reads exactly: importScripts('site-guides/utilities/two-factor-auth.js');
    - The import appears after support-chatbot.js in the utilities section
    - All existing imports are preserved (no lines removed)
  </acceptance_criteria>
  <done>two-factor-auth.js import wired into background.js in the Utilities section after support-chatbot.js. All existing imports preserved.</done>
</task>

</tasks>

<verification>
1. site-guides/utilities/two-factor-auth.js exists with registerSiteGuide call
2. Site guide contains twoFactorMultiTab workflow with at least 10 steps
3. Site guide contains CONTEXT-07 guidance section with multi-tab 2FA strategy
4. Site guide documents tab ID management (authTabId and emailTabId retention)
5. Site guide documents email code extraction patterns (Gmail, Outlook, disposable email)
6. Site guide documents 2FA input field patterns including split digit fields
7. Site guide documents context bloat mitigation (under 500 chars for 2FA flow)
8. Site guide contains selectors for login form, 2FA code input, verify button, success indicators
9. Site guide contains a simulateMultiTabCodeFetch fallback workflow
10. Site guide import wired into background.js in the Utilities section
11. All existing utility site guide imports preserved
12. toolPreferences includes multi-tab tools (open_tab, switch_tab, list_tabs)
</verification>

<success_criteria>
- two-factor-auth.js created with CONTEXT-07 workflow and guidance
- twoFactorMultiTab workflow documents the full multi-tab 2FA sequence (login, trigger code, fetch from email tab, return, enter code, verify)
- Tab ID management documented (store authTabId before opening email tab, switch back by stored ID)
- Email code extraction patterns documented for Gmail, Outlook, and disposable email services
- Context bloat mitigation for multi-tab 2FA documented
- Fallback simulation workflow documented for when no real 2FA demo is available
- Site guide import wired into extension
- Ready for Plan 02 live MCP test
</success_criteria>

<output>
After completion, create `.planning/phases/83-2fa-multi-tab-auth-flow/83-01-SUMMARY.md`
</output>
