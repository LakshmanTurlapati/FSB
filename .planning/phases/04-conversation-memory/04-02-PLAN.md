---
phase: 04-conversation-memory
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - ai/ai-integration.js
autonomous: true

must_haves:
  truths:
    - "When compaction produces a summary shorter than 500 characters, it retries once with a stronger prompt before accepting"
    - "When compaction fails entirely (API timeout or error), a local extractive fallback produces a summary of at least 500 characters from raw conversation turns"
    - "The original task goal is always present in the AI prompt regardless of how many compaction cycles have run"
    - "Critical actions (send/submit/purchase with success) are always present in the AI prompt and survive compaction"
    - "At session start on a previously-visited domain, the AI receives site-specific memories in the first-iteration prompt -- not just after compaction triggers"
  artifacts:
    - path: "ai/ai-integration.js"
      provides: "Hardened triggerCompaction with retry + local fallback; hard facts section in buildMemoryContext; long-term memory injection in buildPrompt"
      contains: "_localExtractiveFallback"
  key_links:
    - from: "ai-integration.js triggerCompaction()"
      to: "ai-integration.js _localExtractiveFallback()"
      via: "Called when API compaction fails or returns <500 chars after retry"
      pattern: "_localExtractiveFallback"
    - from: "ai-integration.js updateSessionMemory()"
      to: "ai-integration.js hardFacts"
      via: "Critical actions detected by lastActionResult.result.elementText matching irrevocable verbs, stored in this.hardFacts"
      pattern: "lastActionResult\\.result\\.elementText"
    - from: "ai-integration.js _fetchLongTermMemories()"
      to: "ai-integration.js buildPrompt()"
      via: "Long-term memories injected as SITE KNOWLEDGE section in first-iteration user prompt"
      pattern: "_longTermMemories"
---

<objective>
Harden compaction so it never produces useless summaries, add a hard facts section that survives all compaction cycles, and inject long-term memories into the first-iteration prompt so the AI benefits from past sessions immediately.

Purpose: MEM-01 prevents the "27-character summary" failure that destroys operational context. MEM-03 ensures the task goal, critical action outcomes, and proven selectors are always visible to the AI. MEM-04 gives the AI site-specific knowledge from previous sessions starting at iteration 1, not iteration 5+.

Output: Resilient compaction with fallback, hard facts section exempt from compaction, and long-term memory in first-iteration prompts.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-conversation-memory/04-RESEARCH.md
@.planning/phases/04-conversation-memory/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Add _localExtractiveFallback method</name>
  <files>ai/ai-integration.js</files>
  <action>
  Add a new `_localExtractiveFallback()` method on the AIIntegration class, after triggerCompaction (~line 824):

  The method accepts `messagesToCompact` (array of conversation messages) and returns a string summary of at least 500 characters built by scanning the raw messages for:
  - URLs: regex `https?://[^\s"'<>]+`, deduplicate, join as "Pages visited: url1 -> url2 -> ..."
  - Actions: regex for action verbs `(?:clicked|typed|navigated|searched|scrolled|selected|pressed)[^.]{0,80}` (case-insensitive), deduplicate, keep last 10, format as "Actions taken:" bullet list
  - Errors: regex for error patterns `(?:error|failed|not found|timeout)[^.]{0,60}` (case-insensitive), deduplicate, keep last 5, format as "Errors encountered:" bullet list
  - For each message, extract content as string (handle both string content and object content via JSON.stringify)
  - Prefix with "Session progress (auto-extracted):"
  - Join all parts with newlines

  The function is pure JavaScript -- no API call, no async, no external dependencies. It is a synchronous fallback.
  </action>
  <verify>
  1. Search for `_localExtractiveFallback` in ai-integration.js -- should exist as a method.
  2. Verify `_localExtractiveFallback` returns a string (not a promise) -- it must be synchronous.
  3. Verify it handles both string and object message content gracefully.
  </verify>
  <done>_localExtractiveFallback method exists on AIIntegration class and produces a structured extractive summary from raw conversation messages without any API call.</done>
</task>

<task type="auto">
  <name>Task 1b: Harden triggerCompaction with retry and fallback calls</name>
  <files>ai/ai-integration.js</files>
  <action>
  Harden `triggerCompaction()` (~line 757-824) to use the _localExtractiveFallback from Task 1a:

  Inside the async IIFE (the `this.pendingCompaction = (async () => { ... })()` block):
  - After the provider parses the response and produces `summary` (line 801-803), add a length check:
    - If `summary.length < 500`, log a warning and retry ONCE with a stronger prompt:
      ```
      "Your summary was too short ({N} chars). Produce at least 500 characters covering: 1) actions taken with element details, 2) selectors used, 3) pages visited, 4) errors encountered, 5) current progress toward the task. Be specific -- include element names, URLs, and outcomes."
      ```
    - Use the same provider to send the retry request. If the retry also produces < 500 chars, fall through to the local fallback.
  - In the catch block (line 814-819), instead of returning null, call `this._localExtractiveFallback(messagesToCompact)` and store the result in `this.compactedSummary`.
  - Also add a final safety net: if after the API call (including retry) the summary is still < 500 chars, call `this._localExtractiveFallback(messagesToCompact)` and use its result instead.
  - Keep the `this.compactedSummary = summary.substring(0, 1500)` truncation -- the 1500-char cap is fine.

  Important: `messagesToCompact` is already defined in the outer scope of `triggerCompaction()` (line 767). The async IIFE has closure access to it. No need to pass it as a parameter to the IIFE.

  Important: The retry should NOT block the automation loop. It runs inside the existing fire-and-forget async IIFE. The only change is that where it previously gave up on failure, it now retries once and then falls back to local extraction.
  </action>
  <verify>
  1. Search for `500` or `min.*length` in triggerCompaction -- should show the length validation.
  2. Search for `too short` or `retry` in triggerCompaction -- should show the retry logic.
  3. Verify the catch block in triggerCompaction calls `_localExtractiveFallback` instead of returning null.
  4. Verify the retry path still runs inside the async IIFE (non-blocking).
  </verify>
  <done>Compaction validates summary length (min 500 chars), retries once with stronger prompt on short output, and falls back to local extractive summary on total failure. The automation loop is never left with a null or 27-character summary.</done>
</task>

<task type="auto">
  <name>Task 2: Add hard facts section and critical action tracking (depends on 04-01 for field paths)</name>
  <files>ai/ai-integration.js</files>
  <action>
  Three changes in ai-integration.js:

  1. **Initialize hardFacts in session reset:** In `clearConversationHistory()` or wherever `sessionMemory` is initialized, also initialize `this.hardFacts`:
     ```javascript
     this.hardFacts = {
       taskGoal: '',
       criticalActions: [],   // { description, selector, verified, iteration }
       workingSelectors: {}   // { label: selector } -- max 10 entries
     };
     ```
     Also set `this.hardFacts.taskGoal` in `updateSessionMemory()` when the task goal is first captured (from `this._currentTask` per 04-01 changes). Guard: only set once when empty.

  2. **Detect and record critical actions in updateSessionMemory():** After the existing step recording logic (modified by 04-01), add critical action detection.

     **Field path context (from 04-01):** `context.lastActionResult` is an actionHistory ENTRY with shape `{ tool, params, result: {slim fields}, iteration }`. Access element text as `lastActionResult.result.elementText`, selector as `lastActionResult.result.selectorUsed`, success as `lastActionResult.result.success`, had-effect as `lastActionResult.result.hadEffect`. The tool name is at `lastActionResult.tool`.

     - When a successful action is recorded and `lastActionResult.tool === 'click'`, check if `lastActionResult.result.elementText` matches the irrevocable verb pattern: `/send|submit|purchase|order|delete|publish|post/i`
     - If it matches, add to `this.hardFacts.criticalActions`:
       ```javascript
       { description: stepDesc, selector: lastActionResult.result.selectorUsed || lastActionResult.result.clicked || '', verified: lastActionResult.result.hadEffect !== false, iteration: this.currentIteration }
       ```
     - Cap `criticalActions` at 10 entries. When at capacity, remove the oldest (shift).
     - Also track working selectors: when any action has `lastActionResult.result.success === true` AND `lastActionResult.result.hadEffect === true` (both conditions required -- success alone may mean the selector was found but the action had no visible effect) and has a `lastActionResult.result.selectorUsed`, increment a usage counter. After 2+ successful uses of the same selector meeting BOTH criteria, add it to `this.hardFacts.workingSelectors` with a label derived from `lastActionResult.result.elementText` or `lastActionResult.tool`. Cap at 10 entries (evict least-used).
     - For working selectors, use a helper map `this._selectorUsageCount = {}` (initialized alongside hardFacts) to track usage before promoting to hardFacts.

  3. **Restructure buildMemoryContext() (~line 829):** Add a HARD FACTS section at the TOP of the output, BEFORE the existing Layer 1 (SESSION MEMORY):
     ```
     === HARD FACTS (verified, do not repeat these actions) ===
     Original task: {taskGoal}
     Critical actions completed:
       - [iter N] {description} (VERIFIED)
     Working selectors:
       - {label}: {selector}
     === END HARD FACTS ===
     ```
     - Only include sections that have content (skip empty criticalActions, skip empty workingSelectors).
     - Cap hard facts output at 800 characters total. If it exceeds, truncate working selectors first (they are least critical), then truncate critical actions to most recent 5.
     - The hard facts section is NEVER compacted -- it is always rebuilt from `this.hardFacts` on every call to `buildMemoryContext()`.
  </action>
  <verify>
  1. Search for `hardFacts` in ai-integration.js -- should appear in initialization, updateSessionMemory, and buildMemoryContext.
  2. Search for `HARD FACTS` in buildMemoryContext -- should appear as a section header.
  3. Search for `criticalActions` -- should show the irrevocable verb pattern matching.
  4. Search for `_selectorUsageCount` -- should show the working selector promotion logic.
  5. Verify hardFacts section appears BEFORE "SESSION MEMORY" in buildMemoryContext output.
  6. Verify criticalActions is capped at 10 entries and workingSelectors at 10 entries.
  </verify>
  <done>Hard facts section with task goal, critical actions, and working selectors is always present in memory context. Critical actions are detected from irrevocable verb patterns in lastActionResult.result.elementText. Working selectors are promoted after 2+ uses where BOTH success===true AND hadEffect===true. Hard facts survive all compaction cycles.</done>
</task>

<task type="auto">
  <name>Task 3: Inject long-term memories into first-iteration prompt</name>
  <files>ai/ai-integration.js</files>
  <action>
  One change in ai-integration.js buildPrompt() method:

  In `buildPrompt()` (~line 1362), after the existing prompt assembly but BEFORE the final return, check if long-term memories are available and inject them into the user prompt for the first iteration:

  - Condition: `isFirstIteration && this._longTermMemories && this._longTermMemories.length > 0`
  - Find where the user prompt string is assembled (look for the section that builds the user message content with task description and DOM state). After the automation context section and before the DOM elements section, insert a "SITE KNOWLEDGE" block:
    ```
    === SITE KNOWLEDGE (from previous sessions on this domain) ===
    {formatted memories}
    === END SITE KNOWLEDGE ===
    ```
  - Format each memory based on its type:
    - **Procedural** (has `steps` or type === 'procedural'): Format as "How to: {text}" with steps if available
    - **Semantic** (has `domain` or type === 'semantic'): Format as "Known: {text}"
    - **Episodic** (type === 'episodic'): Format as "Past: {text}"
    - Default: Just include `m.text`
  - Cap the total SITE KNOWLEDGE section at 500 characters (to stay within the ~5% budget alongside hard facts 800 chars = ~8.7% of 15K total, well within the 10% memory budget from Phase 2).
  - Truncate individual memories to 100 chars each if needed to fit the cap.

  Important: `_fetchLongTermMemories()` is already called at session start (line 1033) as a non-blocking async call. By the time `buildPrompt()` runs for the first iteration, the memories may or may not have resolved. If `this._longTermMemories` is empty (not yet resolved or no matches), skip the section gracefully -- no error, no empty headers.

  Important: This injection is ONLY for the first iteration. Subsequent iterations get long-term memories via `buildMemoryContext()` Layer 3 (which already works). This avoids duplicating the memories in every prompt.
  </action>
  <verify>
  1. Search for `SITE KNOWLEDGE` in buildPrompt -- should appear in the first-iteration injection block.
  2. Search for `_longTermMemories` in buildPrompt -- should appear in the condition check.
  3. Verify the injection only happens when `isFirstIteration` is true.
  4. Verify the section is capped at 500 characters.
  5. Verify graceful handling when `_longTermMemories` is empty or undefined.
  </verify>
  <done>Long-term memories from MemoryManager are injected into the first-iteration prompt as a SITE KNOWLEDGE section. The AI has domain-specific knowledge from past sessions starting at iteration 1, not iteration 5+ when compaction triggers.</done>
</task>

</tasks>

<verification>
1. Trace compaction failure path: API timeout -> catch block -> _localExtractiveFallback() called -> compactedSummary set to extracted content (not null)
2. Trace short compaction path: API returns 100-char summary -> length check fails -> retry with stronger prompt -> if still short -> _localExtractiveFallback()
3. Trace hard facts path: updateSessionMemory records critical click on "Send" element -> hardFacts.criticalActions updated -> buildMemoryContext outputs HARD FACTS section at top -> section survives trimConversationHistory compaction
4. Trace long-term memory path: _fetchLongTermMemories called at session start -> memories cached -> buildPrompt (first iteration) injects SITE KNOWLEDGE section -> subsequent iterations get memories via buildMemoryContext Layer 3
5. Verify prompt budget: hard facts (800 chars) + site knowledge (500 chars) = 1300 chars = ~8.7% of 15K cap, well within 10% memory budget
</verification>

<success_criteria>
- Compaction never produces a summary shorter than 500 characters (either retry succeeds or local fallback provides it)
- Compaction failure (API error) always falls back to local extraction instead of null
- Hard facts section (task goal + critical actions + working selectors) appears in every buildMemoryContext output regardless of compaction state
- Critical actions are identified by irrevocable verb pattern in element text, capped at 10
- Working selectors are promoted after 2+ uses with BOTH success===true AND hadEffect===true, capped at 10
- Long-term memories appear in first-iteration prompt when available
- Total memory overhead stays within ~8.7% of 15K prompt budget
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversation-memory/04-02-SUMMARY.md`
</output>
