---
phase: 04-conversation-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - background.js
  - ai/ai-integration.js
autonomous: true

must_haves:
  truths:
    - "After a successful click action, sessionMemory.stepsCompleted contains the clicked element's text and selector -- not just 'clicked element'"
    - "After typing text, sessionMemory.stepsCompleted contains what was typed and which input received it"
    - "Each completed action is recorded exactly once in stepsCompleted -- not duplicated per proposed action"
    - "Failed actions are recorded in failedApproaches with the element text and error -- not just the tool name"
  artifacts:
    - path: "background.js"
      provides: "Enriched slimActionResult with tool, elementText, and selectorUsed; lastActionResult in context object"
      contains: "slim.tool"
    - path: "ai/ai-integration.js"
      provides: "Rewritten updateSessionMemory using actionHistory; rich describeAction with element text and selector"
      contains: "describeAction"
  key_links:
    - from: "background.js slimActionResult()"
      to: "ai-integration.js describeAction()"
      via: "elementText and selectorUsed fields preserved through actionHistory -> context -> updateSessionMemory"
      pattern: "elementText|selectorUsed"
    - from: "background.js context assembly"
      to: "ai-integration.js _lastActionResult"
      via: "lastActionResult field added to context object"
      pattern: "lastActionResult"
---

<objective>
Fix the broken data flow that prevents session memory from recording meaningful action descriptions. Enrich slimActionResult to preserve element text and selector, pass lastActionResult in the context object, rewrite updateSessionMemory to iterate completed actions (not proposed actions), and make describeAction produce rich descriptions like "clicked 'Send' button (#send-btn)" instead of "clicked element".

Purpose: MEM-02 is the data foundation for all other memory improvements. Without rich action data flowing from background.js to ai-integration.js, compaction (MEM-01), hard facts (MEM-03), and long-term memory (MEM-04) have nothing meaningful to preserve or reference.

Output: Enriched action pipeline where every completed action is recorded with element text, selector, success/failure, and verification outcome.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-conversation-memory/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich slimActionResult and pass lastActionResult in context</name>
  <files>background.js</files>
  <action>
  Two changes in background.js:

  1. **Enrich slimActionResult() (~line 1430):** Add three fields to the slim result object:
     - `tool`: Copy from `result.tool`. Only add if present. This is needed so downstream code (describeAction, critical action detection) can identify the action type from the slim result alone, without relying on the outer actionHistory entry wrapper.
     - `elementText`: Extract from `result.elementInfo?.text`, truncate to 50 chars. Only add if present.
     - `selectorUsed`: Copy from `result.selectorUsed`. Only add if present.
     These are small additions (~120 bytes per action) that enable rich descriptions downstream.
     Add these AFTER the existing `result.retryable` block, BEFORE the `return slim;` line.

     **Important field path note:** The actionHistory entry structure is `{ tool, params, result: slimActionResult(...), iteration }`. The `tool` field exists at BOTH the entry level (`entry.tool`) and now inside the slim result (`entry.result.tool`). Downstream code should prefer `entry.tool` but can fall back to `entry.result.tool`. The `elementText` and `selectorUsed` fields live INSIDE the slim result: `entry.result.elementText`, `entry.result.selectorUsed`.

  2. **Add lastActionResult to context assembly (~line 5573):** In the context object constructed for `callAIAPI()`, add a `lastActionResult` field pointing to the most recent entry in `session.actionHistory`:
     ```
     lastActionResult: session.actionHistory.length > 0
       ? session.actionHistory[session.actionHistory.length - 1]
       : null,
     ```
     Add this right after the `actionHistory` line (line 5575).

     **Important:** The `lastActionResult` value is an actionHistory ENTRY object with shape `{ tool, params, result: {slim fields}, iteration }`. Downstream consumers (updateSessionMemory, describeAction, critical action detection) must access fields as: `lastActionResult.tool` for action type, `lastActionResult.result.elementText` for element text, `lastActionResult.result.selectorUsed` for selector, `lastActionResult.result.success` for outcome.

  Do NOT modify any other fields in slimActionResult or the context object. The function exists for memory efficiency -- only add the minimum needed.
  </action>
  <verify>
  1. Search for `slim.tool` in background.js slimActionResult -- should appear as a preserved field from `result.tool`.
  2. Search for `elementText` in background.js -- should appear in slimActionResult.
  3. Search for `selectorUsed` in slimActionResult -- should appear as a preserved field.
  4. Search for `lastActionResult` in the context object near line 5573 -- should reference the last actionHistory entry.
  5. Verify slimActionResult still returns a lean object (no full elementInfo, no verification details).
  </verify>
  <done>slimActionResult preserves tool, elementText (50 chars), and selectorUsed. The context object includes lastActionResult pointing to the most recent action history entry (shape: { tool, params, result: {slim fields}, iteration }).</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite updateSessionMemory and describeAction for rich action recording</name>
  <files>ai/ai-integration.js</files>
  <action>
  Two changes in ai-integration.js:

  1. **Rewrite updateSessionMemory() (~line 671):** Fix the logic bug where the function iterates over `aiResponse.actions` (proposed) but checks `context.lastActionResult` (completed).

     Replace the "Track completed steps" block (lines 692-705) with logic that:
     - Uses `context.lastActionResult` (now available from Task 1) as the single most-recently-completed action.
     - **Field paths:** `lastActionResult` is an actionHistory ENTRY with shape `{ tool, params, result: {slim fields}, iteration }`. Access fields as:
       - Action type: `lastActionResult.tool`
       - Success: `lastActionResult.result.success`
       - Element text: `lastActionResult.result.elementText`
       - Selector: `lastActionResult.result.selectorUsed`
       - Had effect: `lastActionResult.result.hadEffect`
     - If `lastActionResult` exists and `lastActionResult.result.success` is true, calls `this.describeAction()` with `lastActionResult.tool` and `lastActionResult.result` (the slim result object).
     - Records one step per call (not N steps per N proposed actions).
     - The `tool` field: use `lastActionResult.tool` (always available at entry level). Fallback (should not be needed): infer from result fields: if `result.clicked` -> 'click', if `result.typed` -> 'type', if `result.navigatingTo` -> 'navigate', else 'action'.

     Also fix the "Track failed approaches" block (lines 707-717):
     - Use `lastActionResult.tool` for the tool name.
     - Include `lastActionResult.result.elementText` in the failure description if available: `"click on 'Send': Element not found"` instead of `"click: Element not found"`.

     Also fix the "Extract task goal" block (lines 685-689):
     - Instead of the fragile regex extraction from `aiResponse.reasoning`, check if `this._currentTask` is set (stashed by `getAutomationActions` at line 1023 as `this._currentTask = task || '';`). Use it directly as the task goal: `mem.taskGoal = this._currentTask;`
     - Only set once (keep the `!mem.taskGoal` guard).
     - **Verified:** `this._currentTask` IS populated at line 1023 of ai-integration.js: `this._currentTask = task || '';`. This runs at the start of every `getAutomationActions()` call, which is invoked before `updateSessionMemory()` is ever called. The field will always be set by the time updateSessionMemory runs.
     - **Guard against empty:** If `this._currentTask` is an empty string (edge case: task parameter was falsy), fall back to the existing regex extraction from `aiResponse.reasoning` as a secondary source. Priority: `this._currentTask` > regex extraction > leave taskGoal unset.

  2. **Enrich describeAction() (~line 739):** Replace the current switch statement with a version that includes element text and selector:
     - Extract `text` from `result?.elementText` (the 50-char field from enriched slimActionResult).
     - Extract `sel` from `result?.selectorUsed || result?.clicked || ''`.
     - Format selector as `(selector)` suffix, truncated to 40 chars.
     - Format element text as `'text'` prefix where meaningful.

     Target output examples:
     - click: `"clicked 'Send' button (#send-btn)"` or `"clicked element (#send-btn)"` or `"clicked 'Send' button"` depending on available data
     - type: `"typed 'Hello there' in (.msg-form__contenteditable)"` -- use `result.typed` truncated to 30 chars
     - navigate: `"navigated to linkedin.com/messaging"` -- use `result.navigatingTo` or `result.result`
     - searchGoogle: `"searched Google for 'wireless mouse'"` -- use result
     - selectOption: `"selected option in dropdown (selector)"`
     - scroll: `"scrolled page down"` -- include direction if available
     - pressEnter: `"pressed Enter (selector)"`
     - clickSearchResult: `"clicked search result #N"`
     - default: `"tool 'text' (selector)"` -- generic fallback with available info

     Keep the function simple -- no more than 15 lines for the switch statement. The enrichment comes from using the new data fields, not from complex logic.
  </action>
  <verify>
  1. Search for `aiResponse.actions` inside `updateSessionMemory` -- should NOT appear (removed the broken iteration).
  2. Search for `lastActionResult` inside `updateSessionMemory` -- should appear using context.lastActionResult.
  3. Search for `_currentTask` inside `updateSessionMemory` -- should appear for task goal extraction.
  4. Verify `this._currentTask = task` exists in `getAutomationActions()` (~line 1023) -- confirms the field is populated before updateSessionMemory runs.
  5. Search for `elementText` inside `describeAction` -- should appear for rich descriptions.
  6. Verify describeAction still handles all original tool types (navigate, click, type, searchGoogle, selectOption, scroll, pressEnter, clickSearchResult).
  </verify>
  <done>updateSessionMemory records one rich description per completed action using actionHistory data. describeAction produces descriptions with element text and selectors. Task goal uses _currentTask directly instead of regex extraction from AI reasoning.</done>
</task>

</tasks>

<verification>
1. Trace the full data path: content.js action result -> slimActionResult (preserves elementText + selectorUsed) -> actionHistory -> context.lastActionResult -> updateSessionMemory -> describeAction -> stepsCompleted entry
2. Confirm no regression: slimActionResult still strips large fields (full elementInfo object, verification details, result strings beyond what was already kept)
3. Confirm updateSessionMemory does NOT iterate over aiResponse.actions (the logic bug that duplicated entries)
4. Confirm describeAction handles missing data gracefully (no crashes when elementText or selectorUsed are undefined)
</verification>

<success_criteria>
- slimActionResult output includes tool, elementText, and selectorUsed when present in the original result
- context object includes lastActionResult pointing to most recent actionHistory entry
- updateSessionMemory records exactly one step per completed action, not one per proposed action
- describeAction produces human-readable descriptions with element text and selector context
- Task goal extracted from _currentTask (user's original input), not regex on AI reasoning
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversation-memory/04-01-SUMMARY.md`
</output>
