---
plan: 252-01
phase: 252-repo-integration-doc-updates
title: Wire FSB OpenClaw skill into root README + mcp README + install.ts + llms.txt
wave: 1
depends_on: []
files_modified:
  - README.md
  - mcp/README.md
  - mcp/src/install.ts
  - showcase/angular/public/llms.txt
  - showcase/angular/public/llms-full.txt
autonomous: true
requirements: [INTEG-01, INTEG-02, INTEG-03, INTEG-04]
status: in_progress
---

<objective>
Make the FSB OpenClaw skill discoverable from every existing entry point. Four mechanical edits across four files; all surface-level documentation, no logic changes.
</objective>

<must_haves>
- README.md Quick Start TL;DR mentions the FSB skill (path + role).
- README.md Repository Layout table has a `skills/` row pointing at `skills/FSB Skill/SKILL.md`.
- mcp/README.md has an OpenClaw paragraph naming the skill as the canonical install path; manual stdio fallback preserved.
- mcp/src/install.ts:413-420 OpenClaw block points at the skill; STDIO_COMMAND fallback line preserved.
- showcase llms.txt and llms-full.txt mention the FSB skill for AI-search discoverability.
- ZERO emojis; ASCII only.
- Existing `npm test` chain still passes.
</must_haves>

<task n="1" id="252-01-1">
<files>- README.md (MODIFY)</files>
<read_first>- README.md (Quick Start TL;DR + Repository Layout)</read_first>
<action>
Edit README.md to:
1. Quick Start TL;DR: add a 1-2 sentence callout mentioning the FSB OpenClaw skill at `skills/FSB Skill/` for users on OpenClaw. Position: append after the "Restart the client" line (around line 62) or as a small note below the platform install table.
2. Repository Layout table: add a new row above the showcase row: `| [`skills/FSB Skill/`](./skills/FSB%20Skill/SKILL.md) | OpenClaw skill: doctor + stdio printer + consent-gated multi-host install. |`
</action>
<acceptance_criteria>
- README.md contains the literal substring `skills/FSB Skill/`
- README.md contains a Repository Layout row that mentions the skill
- README.md still parses (no broken markdown table)
- No emojis added
</acceptance_criteria>
<verify>
<automated>
grep -q 'skills/FSB Skill/' README.md && echo "OK_README_MENTION"
grep -qE 'OpenClaw skill|FSB skill' README.md && echo "OK_README_LAYOUT"
perl -ne 'print if /[^\x00-\x7F]/' README.md | wc -c | grep -qx 0 && echo "OK_README_ASCII"
</automated>
</verify>
<done>README.md updated.</done>
</task>

<task n="2" id="252-01-2">
<files>- mcp/README.md (MODIFY)</files>
<read_first>- mcp/README.md (One Command Install section)</read_first>
<action>
Add a 2-3 sentence "OpenClaw" paragraph in the platform/install section pointing OpenClaw users at `skills/FSB Skill/`. Note `--openclaw` install flag stays manual/unsupported because OpenClaw MCP config schema is unstable; the skill prints + the user pastes.
</action>
<acceptance_criteria>
- mcp/README.md contains the literal `skills/FSB Skill/`
- mcp/README.md mentions OpenClaw in a paragraph (not just a label list)
- No emojis
</acceptance_criteria>
<verify>
<automated>
grep -q 'skills/FSB Skill/' mcp/README.md && echo "OK_MCP_README_MENTION"
perl -ne 'print if /[^\x00-\x7F]/' mcp/README.md | wc -c | grep -qx 0 && echo "OK_MCP_README_ASCII"
</automated>
</verify>
<done>mcp/README.md updated.</done>
</task>

<task n="3" id="252-01-3">
<files>- mcp/src/install.ts (MODIFY)</files>
<read_first>- mcp/src/install.ts:413-420 (OpenClaw block in getSetupSections())</read_first>
<action>
Replace the existing OpenClaw block lines (preserving block frame and STDIO_COMMAND constant reference) with new content pointing at the skill while keeping the manual stdio fallback line. New lines array:
```
'Canonical install: load the FSB skill from skills/FSB Skill/ in this repo.',
'  The skill runs the doctor flow, prints the OpenClaw stdio block, and offers',
'  consent-gated install for other detected MCP hosts.',
'Status of the --openclaw install flag: still manual / unsupported.',
'Why:',
'  OpenClaw MCP config schema is unstable across builds; the skill prints + asks',
'  the user to paste, never auto-writes the OpenClaw config.',
'Manual stdio fallback (if you cannot use the skill):',
'  ' + STDIO_COMMAND,
```
</action>
<acceptance_criteria>
- mcp/src/install.ts compiles (TypeScript, no syntax errors)
- File contains literal `skills/FSB Skill/`
- File still references STDIO_COMMAND constant
- npm test still passes (existing tests don't depend on these specific lines)
</acceptance_criteria>
<verify>
<automated>
grep -q 'skills/FSB Skill/' mcp/src/install.ts && echo "OK_INSTALL_TS_MENTION"
grep -q 'STDIO_COMMAND' mcp/src/install.ts && echo "OK_STDIO_PRESERVED"
</automated>
</verify>
<done>install.ts OpenClaw block updated.</done>
</task>

<task n="4" id="252-01-4">
<files>
- showcase/angular/public/llms.txt (MODIFY)
- showcase/angular/public/llms-full.txt (MODIFY)
</files>
<read_first>
- showcase/angular/public/llms.txt
- showcase/angular/public/llms-full.txt
</read_first>
<action>
Append a short FSB OpenClaw skill paragraph to both files at an appropriate position (after MCP server / install section). 1-2 sentences in llms.txt; 3-4 sentences in llms-full.txt with more context.
</action>
<acceptance_criteria>
- Both files contain literal `skills/FSB Skill/`
- Both files mention OpenClaw
- Both files remain ASCII-only
</acceptance_criteria>
<verify>
<automated>
grep -q 'skills/FSB Skill/' showcase/angular/public/llms.txt && echo "OK_LLMS_TXT"
grep -q 'skills/FSB Skill/' showcase/angular/public/llms-full.txt && echo "OK_LLMS_FULL_TXT"
</automated>
</verify>
<done>Both llms files updated.</done>
</task>
