---
phase: 114-natural-language-diagrams
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site-guides/design/excalidraw.js
autonomous: true
requirements:
  - NL-01
  - NL-02
  - NL-03
  - NL-04
  - NL-05

must_haves:
  truths:
    - "Site guide contains a NATURAL LANGUAGE DIAGRAM GENERATION section with layout planning instructions"
    - "Flowchart coordinate template exists with top-to-bottom positions and diamond decision branching"
    - "Architecture diagram coordinate template exists with left-to-right tiers"
    - "Mind map coordinate template exists with center node and radial branches"
    - "Step-by-step sequence documented: plan layout then draw shapes then add labels then draw connectors"
    - "Grid convention uses 150px horizontal and 120px vertical spacing consistently"
    - "All shapes and connectors in templates include text labels"
  artifacts:
    - path: "site-guides/design/excalidraw.js"
      provides: "NATURAL LANGUAGE DIAGRAM GENERATION section in guidance string"
      contains: "NATURAL LANGUAGE DIAGRAM GENERATION"
  key_links:
    - from: "NATURAL LANGUAGE DIAGRAM GENERATION section"
      to: "DRAWING PRIMITIVES, TEXT ENTRY, CONNECTORS sections"
      via: "cross-references in guidance text"
      pattern: "DRAWING PRIMITIVES|TEXT ENTRY|CONNECTORS"
---

<objective>
Add a NATURAL LANGUAGE DIAGRAM GENERATION section to the Excalidraw site guide's guidance string with coordinate templates and step-by-step sequences for flowcharts, architecture diagrams, and mind maps.

Purpose: Give the AI a layout planning framework so it can autonomously position shapes on a grid when a user describes a diagram in plain English, rather than guessing coordinates.
Output: Updated site-guides/design/excalidraw.js with the new guidance section appended before the closing backtick of the guidance string.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@site-guides/design/excalidraw.js
@.planning/phases/114-natural-language-diagrams/114-CONTEXT.md

<interfaces>
The guidance string in excalidraw.js is a template literal (backtick-delimited) assigned to the `guidance` property of the registerSiteGuide call. It currently ends with the EXPORT section (line ~421) followed by the closing backtick. The new section must be inserted BEFORE the closing backtick on line 421.

Existing sections in the guidance string (in order):
- AUTOPILOT STRATEGY HINTS
- SESSION SETUP
- EXCALIDRAW-SPECIFIC INTELLIGENCE (KEYBOARD SHORTCUTS)
- CANVAS OPERATIONS
- ELEMENT EDITING
- CONNECTORS AND ARROWS
- STYLING
- DRAWING PRIMITIVES
- TEXT ENTRY
- EXPORT

The workflows object (line 476+) contains workflow arrays. New workflow arrays for diagram generation should be added there too.

Coordinate convention already established: 150px horizontal spacing, 120px vertical spacing, 150x80px default shape size.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NATURAL LANGUAGE DIAGRAM GENERATION section to Excalidraw site guide</name>
  <files>site-guides/design/excalidraw.js</files>
  <read_first>site-guides/design/excalidraw.js (full file -- read in chunks if needed)</read_first>
  <action>
Insert a new section titled "NATURAL LANGUAGE DIAGRAM GENERATION (NL-01 through NL-05):" into the guidance string, placed AFTER the EXPORT section and BEFORE the closing backtick. This section provides the AI with layout planning intelligence so it can autonomously generate diagrams from natural language descriptions.

The section MUST contain these subsections:

1. LAYOUT PLANNING RULES (per NL-04, NL-05):
   - Grid convention: 150px horizontal spacing, 120px vertical spacing, default shape size 150x80
   - Planning step: AI MUST use # comment lines in its first iteration to plan all coordinates before drawing anything
   - Execution sequence: plan layout -> draw shapes -> add text labels -> draw connectors -> (optional) align/style
   - Every shape MUST get a text label (per NL-05) -- use in-shape text (double-click) for rectangles/diamonds/ellipses
   - Every connector MUST get a text label (per NL-05) -- use arrow label workflow (double-click arrow midpoint)

2. FLOWCHART TEMPLATE (per NL-01):
   - Layout: top-to-bottom, single column for linear flow
   - Coordinate template for a 4-step flow:
     - Step 1 (rectangle): top-left (300, 200), drag to (450, 280)
     - Step 2 (diamond decision): top-left (300, 320), drag to (450, 400)
     - Step 3a (rectangle, yes branch): top-left (300, 440), drag to (450, 520)
     - Step 3b (rectangle, no branch): top-left (550, 320), drag to (700, 400)
     - Step 4 (rectangle): top-left (300, 560), drag to (450, 640)
   - Connectors: arrows from bottom-edge of each shape to top-edge of next shape
   - Diamond branches: yes arrow goes down from bottom-edge, no arrow goes right from right-edge to Step 3b left-edge
   - Formula: for shape at (x,y) size (w,h) -- bottom-edge midpoint is (x+w/2, y+h), top-edge midpoint is (x+w/2, y)
   - All shapes labeled, decision diamond labeled with question text, branch arrows labeled "Yes" / "No"

3. ARCHITECTURE DIAGRAM TEMPLATE (per NL-02):
   - Layout: left-to-right tiers, components stacked vertically within each tier
   - Coordinate template for a 3-tier architecture:
     - Tier 1 (Frontend) label: standalone text at (275, 150)
     - Tier 1 components: rectangles at (200, 200) to (350, 280), (200, 320) to (350, 400)
     - Tier 2 (API/Backend) label: standalone text at (525, 150)
     - Tier 2 components: rectangles at (450, 200) to (600, 280), (450, 320) to (600, 400)
     - Tier 3 (Database) label: standalone text at (775, 150)
     - Tier 3 components: rectangles at (700, 200) to (850, 280), (700, 320) to (850, 400)
   - Horizontal spacing between tiers: 250px (column centers at x=275, 525, 775)
   - Vertical spacing within tier: 120px between component tops
   - Connectors: horizontal arrows from right-edge of Tier 1 components to left-edge of Tier 2 components, same for Tier 2 to Tier 3
   - All components labeled with their names, tier labels as standalone text above

4. MIND MAP TEMPLATE (per NL-03):
   - Layout: center node with radial branches extending outward
   - Coordinate template for a center + 4 branches:
     - Center node (ellipse): (525, 340) to (675, 420) -- center at (600, 380)
     - Right branch node (rectangle): (800, 340) to (950, 420) -- arrow from center right-edge (675, 380) to node left-edge (800, 380)
     - Top branch node (rectangle): (525, 180) to (675, 260) -- arrow from center top-edge (600, 340) to node bottom-edge (600, 260)
     - Left branch node (rectangle): (250, 340) to (400, 420) -- arrow from center left-edge (525, 380) to node right-edge (400, 380)
     - Bottom branch node (rectangle): (525, 500) to (675, 580) -- arrow from center bottom-edge (600, 420) to node top-edge (600, 500)
   - Sub-branches: extend 200px further in same direction from each branch node
   - Center node uses ellipse (O key), branch nodes use rectangles (R key)
   - All nodes labeled, center with main topic, branches with subtopics

5. GENERAL SCALING RULES:
   - For more than 4 steps in a flowchart, continue adding 120px vertical spacing per row
   - For more tiers or components, add 250px per tier column, 120px per component row
   - For more mind map branches, use diagonal positions (e.g., top-right at (800, 180))
   - If diagram would exceed ~1200px in any direction, the AI should note that zoom-to-fit (Shift+1) is needed after drawing

Also add THREE new workflow arrays to the workflows object:

A. generateFlowchart workflow array (8 steps):
   1. "Plan layout: use # comments to list all steps, decisions, and branches with (x,y) coordinates using 150px horizontal / 120px vertical grid"
   2. "Run session setup: Escape, Ctrl+A, Delete, Ctrl+0"
   3. "Draw all shapes: press R for rectangles, D for diamonds -- cdpDrag each shape at planned coordinates -- re-press tool key before each shape"
   4. "Add text labels to each shape: double-click shape center via cdpClickAt, wait 300ms, cdpInsertText label, press Escape"
   5. "Draw arrows between shapes: press A, cdpDrag from source bottom-edge to target top-edge (20 steps, 20ms delay) -- re-press A before each arrow"
   6. "Label decision arrows: double-click arrow midpoint, wait 300ms, cdpInsertText Yes or No, press Escape"
   7. "Zoom to fit: press_key(1, shift=true) to show entire diagram"
   8. "Verify: count shapes and arrows drawn match the plan -- if missing, draw remaining elements"

B. generateArchitectureDiagram workflow array (8 steps):
   1. "Plan layout: use # comments to list all tiers and components with (x,y) coordinates -- tiers left-to-right at 250px spacing, components top-to-bottom at 120px spacing"
   2. "Run session setup: Escape, Ctrl+A, Delete, Ctrl+0"
   3. "Add tier labels: press T, cdpClickAt tier label position, wait 300ms, cdpInsertText tier name, press Escape -- re-press T before each label"
   4. "Draw all component shapes: press R, cdpDrag each rectangle at planned coordinates -- re-press R before each shape"
   5. "Add text labels to each component: double-click shape center via cdpClickAt, wait 300ms, cdpInsertText component name, press Escape"
   6. "Draw arrows between tiers: press A, cdpDrag from source right-edge to target left-edge (20 steps, 20ms delay) -- re-press A before each arrow"
   7. "Zoom to fit: press_key(1, shift=true) to show entire diagram"
   8. "Verify: count shapes, labels, and arrows drawn match the plan -- if missing, draw remaining elements"

C. generateMindMap workflow array (8 steps):
   1. "Plan layout: use # comments to list center topic and all branches with (x,y) coordinates -- center at (600,380), branches extending 200px in cardinal directions"
   2. "Run session setup: Escape, Ctrl+A, Delete, Ctrl+0"
   3. "Draw center node: press O (ellipse), cdpDrag at center coordinates"
   4. "Label center node: double-click center via cdpClickAt, wait 300ms, cdpInsertText main topic, press Escape"
   5. "Draw branch nodes: press R, cdpDrag each rectangle at planned branch coordinates -- re-press R before each shape"
   6. "Label branch nodes: double-click each branch shape center, wait 300ms, cdpInsertText branch topic, press Escape"
   7. "Draw arrows from center to branches: press A, cdpDrag from center edge to branch edge (20 steps, 20ms delay) -- re-press A before each arrow"
   8. "Zoom to fit: press_key(1, shift=true) to show entire diagram"

Add a new warning to the warnings array:
   "Natural language diagrams require planning coordinates BEFORE drawing -- use # comment lines to lay out all positions first, then draw shapes, then label, then connect. Skipping the planning step causes overlapping shapes and missed connections."
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Extensions/FSB && node -e "const fs = require('fs'); const src = fs.readFileSync('site-guides/design/excalidraw.js', 'utf8'); const checks = ['NATURAL LANGUAGE DIAGRAM GENERATION', 'FLOWCHART TEMPLATE', 'ARCHITECTURE DIAGRAM TEMPLATE', 'MIND MAP TEMPLATE', 'LAYOUT PLANNING RULES', 'GENERAL SCALING RULES', 'generateFlowchart', 'generateArchitectureDiagram', 'generateMindMap', 'NL-01', 'NL-02', 'NL-03', 'NL-04', 'NL-05']; const missing = checks.filter(c => !src.includes(c)); if (missing.length) { console.error('MISSING:', missing); process.exit(1); } console.log('All checks passed'); process.exit(0);"</automated>
  </verify>
  <acceptance_criteria>
    - grep "NATURAL LANGUAGE DIAGRAM GENERATION" site-guides/design/excalidraw.js returns a match
    - grep "FLOWCHART TEMPLATE" site-guides/design/excalidraw.js returns a match
    - grep "ARCHITECTURE DIAGRAM TEMPLATE" site-guides/design/excalidraw.js returns a match
    - grep "MIND MAP TEMPLATE" site-guides/design/excalidraw.js returns a match
    - grep "LAYOUT PLANNING RULES" site-guides/design/excalidraw.js returns a match
    - grep "generateFlowchart" site-guides/design/excalidraw.js returns a match
    - grep "generateArchitectureDiagram" site-guides/design/excalidraw.js returns a match
    - grep "generateMindMap" site-guides/design/excalidraw.js returns a match
    - grep "NL-01" site-guides/design/excalidraw.js returns a match
    - grep "NL-02" site-guides/design/excalidraw.js returns a match
    - grep "NL-03" site-guides/design/excalidraw.js returns a match
    - grep "NL-04" site-guides/design/excalidraw.js returns a match
    - grep "NL-05" site-guides/design/excalidraw.js returns a match
    - grep "150px horizontal" site-guides/design/excalidraw.js returns a match in the NL section
    - grep "120px vertical" site-guides/design/excalidraw.js returns a match in the NL section
    - node -e "require('./site-guides/design/excalidraw.js')" does NOT throw (file is valid JS -- requires registerSiteGuide to be defined first, but syntax should parse)
  </acceptance_criteria>
  <done>The Excalidraw site guide guidance string contains a complete NATURAL LANGUAGE DIAGRAM GENERATION section with layout planning rules (grid convention, planning-first approach), coordinate templates for flowcharts (top-to-bottom with diamond branching), architecture diagrams (left-to-right tiers), and mind maps (radial branches). Three new workflow arrays (generateFlowchart, generateArchitectureDiagram, generateMindMap) exist in the workflows object. A warning about planning before drawing is added. All shapes and connectors in templates specify text labels.</done>
</task>

</tasks>

<verification>
1. The file site-guides/design/excalidraw.js parses without syntax errors
2. The guidance string contains all five subsections: LAYOUT PLANNING RULES, FLOWCHART TEMPLATE, ARCHITECTURE DIAGRAM TEMPLATE, MIND MAP TEMPLATE, GENERAL SCALING RULES
3. All five requirement IDs (NL-01 through NL-05) are referenced in the guidance text
4. Three workflow arrays (generateFlowchart, generateArchitectureDiagram, generateMindMap) exist in the workflows object
5. Coordinate templates use 150px horizontal / 120px vertical spacing consistently
</verification>

<success_criteria>
The Excalidraw site guide contains a complete natural language diagram generation section that gives the AI concrete coordinate templates and step-by-step sequences for generating flowcharts, architecture diagrams, and mind maps from plain English descriptions. The AI can follow these templates to plan layouts on a grid before drawing.
</success_criteria>

<output>
After completion, create `.planning/phases/114-natural-language-diagrams/114-01-SUMMARY.md`
</output>
