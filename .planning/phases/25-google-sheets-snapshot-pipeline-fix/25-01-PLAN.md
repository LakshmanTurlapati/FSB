---
phase: 25-google-sheets-snapshot-pipeline-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [content/dom-analysis.js]
autonomous: true
gap_closure: true
requirements: [P25-WALKER-FIX]

must_haves:
  truths:
    - "Formula bar element with fsbRole appears in markdown snapshot output on Google Sheets pages"
    - "Name Box element with fsbRole appears in markdown snapshot output on Google Sheets pages"
    - "Parent containers with aria-hidden=true do NOT prevent fsbRole children from appearing in snapshot"
    - "Interactive parent elements do NOT swallow fsbRole children — each emitted as its own ref"
    - "No duplicate emission of fsbRole elements (emitted once, not twice)"
  artifacts:
    - path: "content/dom-analysis.js"
      provides: "walkDOMToMarkdown fsbRole post-injection and debug logging"
      contains: "fsbRole"
  key_links:
    - from: "walkDOMToMarkdown"
      to: "formatInlineRef"
      via: "post-walk fsbRole injection loop"
      pattern: "fsbRole.*formatInlineRef"
    - from: "buildMarkdownSnapshot"
      to: "sheets_snapshot_summary log"
      via: "post-walk debug logging"
      pattern: "sheets_snapshot_summary"
---

<objective>
Fix the markdown snapshot walker so fsbRole elements (Google Sheets formula bar and name box) appear in the AI's snapshot regardless of parent container visibility state.

Purpose: Phase 24 added Stage 1b injection and formatInlineRef content reading for Sheets elements, but the walkDOMToMarkdown function never reaches these elements because their parent containers have aria-hidden="true". The walker calls isVisibleForSnapshot on the PARENT (which has no fsbRole), fails, and skips the entire subtree. This is the single remaining gap causing "AI blind to sheet content."

Output: Modified walkDOMToMarkdown that post-injects missed fsbRole elements after the main walk completes.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-google-sheets-snapshot-pipeline-fix/25-RESEARCH.md
@content/dom-analysis.js (lines 2187-2370: walkDOMToMarkdown, lines 2382-2442: buildMarkdownSnapshot)

<interfaces>
<!-- Key functions and data structures the executor needs -->

From content/dom-analysis.js:

walkDOMToMarkdown(root, interactiveSet, refMap, guideAnnotations)
- Returns: Array<{region: string, text: string}>
- interactiveSet: Set of DOM elements that have been registered in refMap
- refMap: { map: Map<string, {element: WeakRef}>, reset(), register() }
- The walk starts at document.body, visits all nodes recursively
- Line 2227: `if (!isVisibleForSnapshot(node)) return;` -- THE PROBLEM: skips entire subtree
- Line 2242-2250: `if (interactiveSet.has(node))` -- emits ref and returns (skips children)

formatInlineRef(node, refMap, guideAnnotations)
- Returns: string like `` `e5: div "Formula bar" [hint:formulaBar] = "cell content"` ``
- Lines 2112-2148: Special formula bar content reading (3 fallback sources)
- Lines 2150-2156: Special name box content reading
- Already works correctly -- just never called because walker never reaches fsbRole nodes

buildMarkdownSnapshot(options)
- Lines 2424: calls walkDOMToMarkdown, gets walkedLines
- Lines 2427-2442: existing sheets_snapshot_summary logging (checks walkedLines for formula bar presence)

Stage 1b injection (lines 1782-1803):
- Injects formula bar (#t-formula-bar-input, .cell-input) with fsbRole='formula-bar'
- Injects name box (#t-name-box, .waffle-name-box) with fsbRole='name-box'
- Elements get registered in interactiveSet at line 2416

Stage 2 visibility (line 1809):
- `if (el.dataset.fsbRole) return true;` -- fsbRole elements PASS Stage 2
- So they ARE in interactiveSet. They just never get visited by the walker.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Post-walk fsbRole injection in walkDOMToMarkdown</name>
  <files>content/dom-analysis.js</files>
  <action>
Modify the `walkDOMToMarkdown` function (starting at line 2187) to guarantee fsbRole elements appear in the snapshot output even when their parent containers are hidden.

After the main walk completes (after line 2368 `flushLine()`) and before `return lines;` (line 2369), add a post-injection block:

1. Track which fsbRole elements were already emitted during the walk by checking if any line in `lines` contains their ref string.
2. For each element in `interactiveSet` that has `dataset.fsbRole`:
   a. Call `formatInlineRef(el, refMap, guideAnnotations)` to get the ref string
   b. Check if this ref string already appears in any line's text (to avoid double-emission per Pitfall 4)
   c. If NOT already emitted, prepend it to the lines array as `{ region: '@main', text: refStr }` so it appears near the top of the snapshot (within char budget, per Pitfall 3)
3. Log the injection with `logger.logDOMOperation` using key `'sheets_walker_postinject'` including: how many fsbRole elements exist in interactiveSet, how many were already emitted by the walk, how many were post-injected.

Implementation pattern (insert between line 2368 and 2369):
```javascript
    // Post-walk: guarantee fsbRole elements appear even if parent was aria-hidden
    const emittedText = lines.map(l => l.text).join('\n');
    let postInjected = 0;
    let alreadyEmitted = 0;
    const fsbRoleElements = [];
    for (const el of interactiveSet) {
      if (el.dataset && el.dataset.fsbRole) {
        fsbRoleElements.push(el);
        const refStr = formatInlineRef(el, refMap, guideAnnotations);
        if (refStr) {
          if (emittedText.includes(refStr)) {
            alreadyEmitted++;
          } else {
            // Insert after metadata header (index 0 is usually first content line)
            lines.splice(postInjected, 0, { region: '@main', text: refStr });
            postInjected++;
          }
        }
      }
    }
    if (fsbRoleElements.length > 0) {
      logger.logDOMOperation(FSB.sessionId, 'sheets_walker_postinject', {
        fsbRoleCount: fsbRoleElements.length,
        alreadyEmitted,
        postInjected,
        roles: fsbRoleElements.map(el => el.dataset.fsbRole)
      });
    }
```

Do NOT modify `isVisibleForSnapshot` or the `visit()` function's visibility check. The fix is purely additive -- post-injection after the walk, not changing the walk itself. This avoids exposing other hidden elements and is structurally immune to any future parent filtering changes (per Research Option B recommendation).
  </action>
  <verify>
    <automated>grep -n "sheets_walker_postinject\|fsbRoleElements\|postInjected" content/dom-analysis.js | head -20</automated>
  </verify>
  <done>walkDOMToMarkdown contains post-injection loop that finds missed fsbRole elements from interactiveSet and prepends them to lines array. Debug logging confirms injection count.</done>
</task>

<task type="auto">
  <name>Task 2: Verify no syntax errors and snapshot summary alignment</name>
  <files>content/dom-analysis.js</files>
  <action>
After Task 1's changes, verify the file parses correctly and the existing sheets_snapshot_summary logging (lines 2427-2442 in buildMarkdownSnapshot) will now correctly detect the post-injected formula bar and name box lines.

1. Run a syntax check on the modified file to catch any unclosed braces, missing semicolons, or other JS errors introduced by the insertion.

2. Verify that the existing `sheets_snapshot_summary` logging block (which checks `walkedLines` for formula bar and name box presence) will work with the post-injected lines. The logging uses:
   - `snapshotText.includes('formula-bar')` -- will match because formatInlineRef includes `[hint:formulaBar]` or the fsbRole in the ref output
   - `snapshotText.includes('Formula bar')` -- will match the fsbLabel
   - Similarly for name-box/Name Box

   If the existing patterns would NOT match the post-injected ref format, update the regex patterns in the summary logging to match. Specifically check: does formatInlineRef for a formula-bar fsbRole element include the text "formula" or "Formula bar" in its output? It should via the fsbLabel or guide annotation. If not, add a fallback check that looks for the ref of any element with `dataset.fsbRole === 'formula-bar'` in the refMap.

3. Ensure the `logger` object is accessible at the point of the post-injection code (it should be -- it's used in the same scope at lines 2433-2441).
  </action>
  <verify>
    <automated>node -c content/dom-analysis.js 2>&1 && echo "SYNTAX OK" || echo "SYNTAX ERROR"</automated>
  </verify>
  <done>dom-analysis.js parses without syntax errors. The sheets_snapshot_summary logging correctly detects post-injected fsbRole elements. The complete pipeline: Stage 1b injects -> Stage 2 passes fsbRole -> interactiveSet contains them -> walker misses them -> post-injection adds them -> snapshot summary confirms presence.</done>
</task>

</tasks>

<verification>
1. `node -c content/dom-analysis.js` exits 0 (no syntax errors)
2. `grep -c "sheets_walker_postinject" content/dom-analysis.js` returns 1 (logging exists)
3. `grep -c "fsbRoleElements" content/dom-analysis.js` returns at least 2 (declaration + usage)
4. The post-injection code is between `flushLine()` and `return lines;` in walkDOMToMarkdown
5. No changes to `isVisibleForSnapshot` or the `visit()` function's visibility check line
</verification>

<success_criteria>
- walkDOMToMarkdown post-injects fsbRole elements that were missed by the main walk
- No double-emission of elements already captured during the walk
- Debug log `sheets_walker_postinject` confirms injection count
- `sheets_snapshot_summary` will show hasFormulaBar=true and hasNameBox=true on Sheets pages
- File parses without syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-google-sheets-snapshot-pipeline-fix/25-01-SUMMARY.md`
</output>
