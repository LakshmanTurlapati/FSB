---
phase: 85-crm-vs-hr-portal-cross-reference
plan: 02
type: execute
wave: 2
depends_on:
  - 85-01
files_modified:
  - .planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md
autonomous: false
requirements:
  - CONTEXT-09

must_haves:
  truths:
    - "Live MCP test attempted against demo CRM (demoqa.com/webtables or fallback) and HR portal (the-internet.herokuapp.com/tables or fallback) using navigate + read_page + open_tab + switch_tab + list_tabs to extract employee names from CRM and cross-reference in HR portal, with documented outcome (pass/partial/fail/skip-auth)"
    - "Diagnostic report exists with step-by-step log, what worked, what failed, tool gaps, and autopilot recommendations specific to multi-tab cross-reference of 50 employee names between two separate web applications"
    - "Diagnostic report contains a Context Bloat Analysis section assessing context consumption for 50-name batch cross-reference workflow (per-batch context, compact result tracking, multi-tab switching overhead)"
    - "Human verified the diagnostic report accuracy and outcome classification"
  artifacts:
    - path: ".planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md"
      provides: "CONTEXT-09 autopilot diagnostic report"
      contains: "CONTEXT-09"
  key_links:
    - from: "85-DIAGNOSTIC.md"
      to: "site-guides/productivity/crm-hr-cross-ref.js"
      via: "selector accuracy table references crm-hr-cross-ref.js selectors for demoqa webtables and herokuapp sortable tables"
      pattern: "rt-table|rt-tr-group|searchBox|table1|table2"
    - from: "85-DIAGNOSTIC.md"
      to: "mcp-server/src/tools/manual.ts"
      via: "tool usage section references navigate, click, type_text, read_page, scroll, open_tab, switch_tab, list_tabs tools"
      pattern: "navigate|click|type_text|read_page|scroll|open_tab|switch_tab|list_tabs"
---

<objective>
Execute live MCP test for CONTEXT-09 (cross-reference 50 employee names in web CRM against separate HR portal) and generate the autopilot diagnostic report.

Purpose: Validate whether the MCP navigate + read_page + open_tab + switch_tab + list_tabs chain can extract employee names from a demo CRM, open a separate HR portal tab, and cross-reference the names between the two systems. This is a "Context Bloat & Long-Running Workflows" edge case -- the challenge is efficiently managing context across 50 name lookups spanning two browser tabs without bloating the AI's working memory. The batch-processing strategy (10 names per batch) and compact result tracking (under 3000 chars) are tested. Expected outcome is PARTIAL (HTTP validation of both sites + selectors, live multi-tab workflow blocked by WebSocket bridge disconnect) or PASS (if MCP bridge is operational). Produces a structured diagnostic report for the autopilot enhancement milestone.

Output: 85-DIAGNOSTIC.md with CONTEXT-09 test results
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-crm-vs-hr-portal-cross-reference/85-CONTEXT.md
@.planning/phases/85-crm-vs-hr-portal-cross-reference/85-01-SUMMARY.md
@site-guides/productivity/crm-hr-cross-ref.js
@.planning/phases/84-google-doc-word-replace/84-DIAGNOSTIC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute live MCP CRM-HR cross-reference test, generate CONTEXT-09 diagnostic report</name>
  <files>.planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md</files>
  <read_first>
    - site-guides/productivity/crm-hr-cross-ref.js (full file -- site guide with crossReferenceEmployees workflow, CONTEXT-09 guidance, selectors for demoqa and herokuapp)
    - .planning/phases/84-google-doc-word-replace/84-DIAGNOSTIC.md lines 1-15 (diagnostic report template structure -- use exact same format with Context Bloat Analysis section)
    - .planning/phases/85-crm-vs-hr-portal-cross-reference/85-01-SUMMARY.md (Plan 01 outcome -- what was added to crm-hr-cross-ref.js)
    - .planning/phases/80-multi-tab-flight-price-compare/80-DIAGNOSTIC.md lines 1-15 (prior multi-tab diagnostic for comparison reference -- Context Bloat Analysis pattern)
  </read_first>
  <action>
Execute the CONTEXT-09 edge case test using MCP manual tools following the crossReferenceEmployees workflow from the crm-hr-cross-ref.js site guide. Test steps:

1. VALIDATE CRM TARGET (demoqa.com/webtables):
   (a) Navigate to https://demoqa.com/webtables -- check if the page loads
   (b) HTTP fetch as fallback: check server response, page size, presence of employee table HTML
   (c) Look for .rt-table, .rt-tr-group selectors in the HTML
   (d) Extract employee names visible in the table (firstName + lastName from .rt-td columns)
   (e) Check for pagination (.-pagination, .-next button)
   (f) Count total employee records available
   Record: URL loaded (yes/no), HTTP status, page size, employee count, selectors found, sample names

2. VALIDATE HR PORTAL TARGET (the-internet.herokuapp.com/tables):
   (a) Navigate to https://the-internet.herokuapp.com/tables -- check if the page loads
   (b) HTTP fetch as fallback: check server response, page size, presence of sortable tables
   (c) Look for #table1, #table2, tbody tr selectors in the HTML
   (d) Extract employee names from table rows (lastName + firstName columns)
   (e) Count total records across both tables
   Record: URL loaded (yes/no), HTTP status, page size, employee count per table, selectors found, sample names

3. VALIDATE FALLBACK TARGETS (if primaries fail):
   (a) Fetch https://reqres.in/api/users?page=1 -- check JSON response for user data
   (b) Fetch https://dummyjson.com/users -- check JSON response for user data
   (c) Record: status, user count, sample names from each

4. ATTEMPT MULTI-TAB WORKFLOW (if MCP bridge available):
   (a) Open CRM tab, record tabId
   (b) Extract first batch of 10 names from CRM
   (c) Open HR portal tab via open_tab
   (d) Cross-reference first batch against HR portal
   (e) Switch back to CRM tab via switch_tab
   (f) If MCP bridge unavailable: document workflow as designed but not executed

5. CROSS-REFERENCE ANALYSIS (even without live MCP):
   (a) Compare names extracted from CRM (HTTP) against names extracted from HR portal (HTTP)
   (b) Identify matches, CRM-only names, HR-only names
   (c) Document the cross-reference results as proof-of-concept
   Record: matched names, mismatched names, CRM-only, HR-only

6. OUTCOME CLASSIFICATION:
   - PASS = 50 names extracted from CRM, cross-referenced against HR portal via multi-tab workflow, report generated
   - PARTIAL = HTTP validation confirms both sites accessible with employee data and selectors match, but multi-tab workflow blocked by WebSocket bridge disconnect
   - FAIL = could not validate either CRM or HR portal target
   - SKIP-AUTH = both primary targets require authentication

Record every step outcome for the diagnostic report.

After test execution (regardless of outcome), generate 85-DIAGNOSTIC.md with ALL of these sections:

## Metadata
- Phase: 85, Requirement: CONTEXT-09, Date: today, Outcome: (PASS/PARTIAL/FAIL/SKIP-AUTH), Live MCP Testing: YES/NO

## Prompt Executed
The exact prompt: "Open a web CRM containing employee records, extract 50 employee names. Open a separate HR portal in a new tab. Cross-reference each name from the CRM against the HR portal. Report which names appear in both systems, which are CRM-only, and which are HR-only."

## Result Summary
2-3 sentence summary of what happened.

## Step-by-Step Log
Table with columns: Step | MCP Tool Used | Target | Result | Notes
One row per tool invocation or HTTP fetch attempted.

## What Worked
Bullet list of everything that succeeded.

## What Failed
Bullet list of everything that failed or was not executed, with reasons.

## Tool Gaps Identified
New tool gaps discovered plus reference to known gaps. Consider:
- Did navigate successfully load demoqa.com/webtables?
- Were employee table selectors (.rt-table, .rt-tr-group) found in the DOM?
- Could read_page extract structured employee names from table cells?
- Did navigate load the-internet.herokuapp.com/tables?
- Were sortable table selectors (#table1, #table2) found in the DOM?
- Could open_tab create a second browser tab for the HR portal?
- Could switch_tab alternate between CRM and HR portal tabs?
- Could list_tabs track tab IDs for reliable switching?
- Is there a tool gap for structured table data extraction (extracting specific columns)?
- WebSocket bridge availability (persistent gap from phases 55-84)

## Context Bloat Analysis (CONTEXT CATEGORY SECTION)
Specific analysis of context management during 50-name cross-reference:
- Per-batch context: read_page on 10 table rows = estimated bytes
- Compact result tracking: {name, match} per name = estimated 50 chars each, 50 names = ~2500 chars total
- Multi-tab switching overhead: switch_tab + read_page per tab switch
- Full table DOM read vs targeted column extraction comparison
- Total context across 5 batches of 10 names with compact tracking vs reading full tables each time
- Comparison to Phase 80 (multi-tab flight comparison) -- both are multi-tab cross-reference workflows
- Comparison to Phase 79 (50-page PDF form fill) -- both involve extracting data from one source and using it in another
- Recommendations for context-efficient multi-tab cross-reference workflows

## Bugs Fixed In-Phase
Any bugs found and fixed during testing.

## Autopilot Recommendations
8-10 specific recommendations for how autopilot should handle CRM-HR cross-reference tasks. Cover:
- Batch processing: extract 10 names at a time, not all 50 at once
- Compact result tracking: {name, crmFound, hrFound, match} per name, never full DOM snapshots
- Tab lifecycle: use list_tabs to confirm tab IDs before switch_tab
- Sequential open-and-read: extract from CRM tab, switch to HR tab, cross-reference, switch back
- Pagination handling: use pagination controls on CRM (.-next button) to access all 50 names
- Deduplication: track processed names to avoid re-checking on subsequent batches
- Search vs scan: if HR portal has search, use it; if static table, scan all rows once and cache
- Error recovery: if tab becomes unresponsive, re-navigate rather than failing
- Result compilation: build final report incrementally, not at the end from memory
- Context budget: under 3000 chars total for all 50 name cross-reference results

## Selector Accuracy
Table: Selector | Expected | Actual | Match -- for each selector from crm-hr-cross-ref.js tested against live DOM or HTTP. Test at minimum: demoqa employeeTable (.rt-table), tableRow (.rt-tr-group), firstName (.rt-td:nth-child(1)), lastName (.rt-td:nth-child(2)), searchBox (#searchBox), herokuapp table1 (#table1), table2 (#table2), tableRow (#table1 tbody tr).

## New Tools Added This Phase
Table: Tool Name | File | Purpose | Parameters
Note: No new MCP tools added this phase. List the crm-hr-cross-ref.js site guide crossReferenceEmployees workflow addition as the new content.

Use the exact same structure and level of detail as 84-DIAGNOSTIC.md, with the Context Bloat Analysis section. Do NOT leave any section as placeholder text like "TBD" or "TODO".
  </action>
  <verify>
    <automated>test -f .planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md && grep -q "CONTEXT-09" .planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md && grep -q "Outcome:" .planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md && grep -q "Autopilot Recommendations" .planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md && grep -q "Step-by-Step Log" .planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md && grep -q "Context Bloat" .planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md && grep -q "Selector Accuracy" .planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md && grep -q "New Tools Added" .planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md && grep -q "demoqa\|webtables" .planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md && grep -q "herokuapp\|sortable" .planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md && grep -q "cross-reference\|cross.reference" .planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md && grep -q "open_tab\|switch_tab" .planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md && grep -q "batch" .planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md exists
    - grep "CONTEXT-09" 85-DIAGNOSTIC.md returns at least 1 match
    - grep "Outcome:" 85-DIAGNOSTIC.md returns at least 1 match (PASS, PARTIAL, FAIL, or SKIP-AUTH)
    - grep "Autopilot Recommendations" 85-DIAGNOSTIC.md returns at least 1 match
    - grep "Step-by-Step Log" 85-DIAGNOSTIC.md returns at least 1 match
    - grep "Context Bloat" 85-DIAGNOSTIC.md returns at least 1 match (CONTEXT category section)
    - grep "Selector Accuracy" 85-DIAGNOSTIC.md returns at least 1 match
    - grep "What Worked" 85-DIAGNOSTIC.md returns at least 1 match
    - grep "What Failed" 85-DIAGNOSTIC.md returns at least 1 match
    - grep "demoqa\|webtables" 85-DIAGNOSTIC.md returns at least 2 matches (CRM target referenced)
    - grep "herokuapp" 85-DIAGNOSTIC.md returns at least 2 matches (HR portal target referenced)
    - grep "cross-reference" 85-DIAGNOSTIC.md returns at least 3 matches (core operation documented)
    - grep "open_tab\|switch_tab" 85-DIAGNOSTIC.md returns at least 2 matches (multi-tab tools documented)
    - grep "batch" 85-DIAGNOSTIC.md returns at least 2 matches (batch processing documented)
    - grep "New Tools Added" 85-DIAGNOSTIC.md returns at least 1 match
    - No section contains only placeholder text like "TBD" or "TODO"
  </acceptance_criteria>
  <done>CONTEXT-09 diagnostic report generated with real test data covering all required sections: metadata, prompt, result summary, step-by-step log (CRM navigation and employee extraction from demoqa webtables, HR portal navigation and employee extraction from herokuapp sortable tables, fallback API validation, multi-tab workflow attempt, cross-reference analysis), what worked, what failed, tool gaps (table data extraction, multi-tab switching, structured column reads, WebSocket bridge), context bloat analysis (per-batch context, compact result tracking savings, multi-tab switching overhead, comparison to Phase 80 and Phase 79), bugs fixed, autopilot recommendations (batch processing, compact tracking, tab lifecycle, sequential open-and-read, pagination handling, deduplication, search vs scan, error recovery, incremental reporting, context budget), selector accuracy (crm-hr-cross-ref.js selectors vs live DOM), new tools (crossReferenceEmployees workflow addition)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify CONTEXT-09 diagnostic report accuracy</name>
  <files>.planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md</files>
  <action>
Human verifies the CONTEXT-09 diagnostic report accuracy.

What was built: CONTEXT-09 CRM vs HR portal cross-reference diagnostic report with MCP test results. The report documents the outcome (PASS/PARTIAL/FAIL/SKIP-AUTH) of attempting to extract 50 employee names from a demo CRM (demoqa.com/webtables) and cross-reference them against a separate HR portal (the-internet.herokuapp.com/tables) using multi-tab MCP tools. The batch-processing strategy (10 names per batch) and compact result tracking (under 3000 chars) were designed to mitigate context bloat across 50 name lookups. This phase also created a crm-hr-cross-ref.js site guide with a crossReferenceEmployees workflow (12 steps), selectors for both demo sites, and context bloat mitigation guidance. This is a CONTEXT-category edge case -- the diagnostic includes a "Context Bloat Analysis" section assessing context consumption for multi-tab cross-reference of large employee lists.

How to verify:
1. Open .planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md
2. Verify the outcome classification is accurate (PASS/PARTIAL/FAIL/SKIP-AUTH matches what actually happened)
3. Verify step-by-step log has real data (no placeholder rows)
4. Verify CRM target validation is documented (demoqa.com/webtables access, employee table selectors, sample names)
5. Verify HR portal target validation is documented (the-internet.herokuapp.com/tables access, sortable table selectors, sample names)
6. Verify cross-reference analysis is documented (name comparison between CRM and HR data)
7. Verify multi-tab workflow attempt is documented (open_tab, switch_tab, list_tabs usage or blocked reason)
8. Verify Context Bloat Analysis section has specific findings about 50-name batch cross-reference context management
9. Verify autopilot recommendations are specific to multi-tab cross-reference workflows (not generic)
10. Verify selector accuracy table has entries from crm-hr-cross-ref.js tested against live DOM or HTTP
11. Confirm the report is complete and ready for the autopilot enhancement milestone

Resume: Type "approved" to accept the diagnostic report, or describe any issues to fix.
  </action>
  <verify>Human confirms by typing "approved" or describes issues</verify>
  <done>Human has reviewed and approved the CONTEXT-09 edge case results and diagnostic report.</done>
</task>

</tasks>

<verification>
1. 85-DIAGNOSTIC.md exists with CONTEXT-09 reference and all required sections
2. Outcome is one of: PASS, PARTIAL, FAIL, SKIP-AUTH
3. Step-by-step log contains at least 8 rows with real tool invocations or HTTP fetch results
4. CRM target (demoqa.com/webtables) validation documented with selectors and sample data
5. HR portal target (the-internet.herokuapp.com/tables) validation documented with selectors and sample data
6. Cross-reference analysis documented with match/mismatch counts
7. Multi-tab workflow documented (open_tab, switch_tab usage or blocked reason)
8. Context Bloat Analysis section has specific findings (per-batch context, compact tracking savings)
9. Autopilot recommendations section has at least 8 specific items about multi-tab cross-reference
10. Selector accuracy table has entries from crm-hr-cross-ref.js tested against live DOM or HTTP
11. New Tools Added section notes crossReferenceEmployees workflow addition to crm-hr-cross-ref.js
12. Human approved the diagnostic report
</verification>

<success_criteria>
- CONTEXT-09 edge case attempted via MCP manual tools with documented outcome
- Diagnostic report generated with all required sections filled with real data
- Multi-tab CRM-HR cross-reference workflow documented (or reason for incomplete execution)
- Demo CRM and HR portal targets validated with selector accuracy
- Batch processing strategy for 50 names documented
- Context Bloat Analysis section provides insights for multi-tab cross-reference workflows
- Human verified and approved the report accuracy
- Phase 85 complete, ready to proceed to Phase 86 (CONTEXT-10)
</success_criteria>

<output>
After completion, create `.planning/phases/85-crm-vs-hr-portal-cross-reference/85-02-SUMMARY.md`
</output>
