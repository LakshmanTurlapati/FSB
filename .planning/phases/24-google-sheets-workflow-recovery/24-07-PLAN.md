---
phase: 24-google-sheets-workflow-recovery
plan: 07
type: execute
wave: 2
depends_on: [24-06]
files_modified:
  - site-guides/productivity/google-sheets.js
autonomous: true
requirements: [P24-06]
gap_closure: true

must_haves:
  truths:
    - "Site guide accurately describes what the AI will see in the snapshot (formula bar value as = \"value\")"
    - "Guide does not promise snapshot features the pipeline cannot deliver"
    - "Formula bar verification workflow uses both passive snapshot reading and active getText as fallback"
  artifacts:
    - path: "site-guides/productivity/google-sheets.js"
      provides: "Updated guidance reflecting fixed pipeline behavior"
      contains: "formula bar ref in the snapshot"
  key_links:
    - from: "site-guides/productivity/google-sheets.js"
      to: "content/dom-analysis.js"
      via: "formula bar fsbRole injection"
      pattern: "formula.*bar.*snapshot"
---

<objective>
Update the Google Sheets site guide to accurately reflect the fixed snapshot pipeline behavior and add a passive-then-active verification strategy.

Purpose: After plan 24-06 fixes the pipeline, the formula bar value WILL appear as `= "value"` in the snapshot. The guide should reinforce this as the primary verification method, with getText as a fallback for edge cases where the display layer is empty.

Output: Updated google-sheets.js with accurate formula bar verification guidance.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-google-sheets-workflow-recovery/24-06-SUMMARY.md
@site-guides/productivity/google-sheets.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update FEEDBACK LOOP AWARENESS and FORMULA BAR VERIFICATION sections</name>
  <files>site-guides/productivity/google-sheets.js</files>
  <action>
  Update two sections in the `guidance` string of google-sheets.js:

  **Change 1: Update FEEDBACK LOOP AWARENESS section (currently lines 36-50 of the guidance string)**

  Replace the current text about formula bar from:
  ```
  The formula bar element shows the content of the currently selected cell.
  After navigating to a cell, check the formula bar ref in the snapshot for its value
  (shown as = "value" in the element ref).
  ```

  With:
  ```
  The formula bar element shows the content of the currently selected cell.
  After navigating to a cell, the formula bar ref in the snapshot shows its value
  as = "value" (e.g., `e8: toolbar-input "Formula bar" [hint:formulaBar] = "Revenue"`).
  Check this FIRST before using getText. If the snapshot shows = "" or no value,
  use getText on the formula bar ref as a fallback.
  ```

  **Change 2: Update FORMULA BAR VERIFICATION section (currently lines 110-114 of the file)**

  Replace:
  ```
  FORMULA BAR VERIFICATION:
  - The formula bar (#t-formula-bar-input or .cell-input) shows the content of the currently selected cell
  - Navigate to a cell via Name Box, then use getText on the formula bar to read the cell's value
  - For HYPERLINK cells, the formula bar shows the full formula (e.g., =HYPERLINK("url","Apply"))
  - This is the ONLY reliable way to verify cell contents -- do NOT try to read from the canvas grid
  ```

  With:
  ```
  FORMULA BAR VERIFICATION:
  - The formula bar appears in the snapshot as a toolbar-input ref with the cell's value (= "value")
  - FIRST check the formula bar ref in the snapshot for the cell value (passive -- no action needed)
  - If the snapshot shows no value or = "", use getText on the formula bar ref as a fallback
  - Navigate to a cell via Name Box, then check the snapshot or use getText on the formula bar
  - For HYPERLINK cells, the formula bar shows the full formula (e.g., =HYPERLINK("url","Apply"))
  - This is the ONLY reliable way to verify cell contents -- do NOT try to read from the canvas grid
  ```

  Do NOT change any other sections of the file.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/laksh/OneDrive/Documents/Codes/FSB && grep -A2 "toolbar-input" site-guides/productivity/google-sheets.js | head -10</automated>
  </verify>
  <done>FEEDBACK LOOP AWARENESS section accurately describes that the formula bar ref shows = "value" in the snapshot with a getText fallback. FORMULA BAR VERIFICATION section prioritizes passive snapshot reading over active getText, with getText as explicit fallback for edge cases.</done>
</task>

</tasks>

<verification>
1. `grep "toolbar-input" site-guides/productivity/google-sheets.js` shows the new guidance referencing the actual role name from the pipeline
2. `grep "getText.*fallback" site-guides/productivity/google-sheets.js` shows getText positioned as fallback, not primary method
3. No broken string literals — the file parses without syntax errors
</verification>

<success_criteria>
- Guide accurately describes formula bar appearing as toolbar-input ref with = "value" in snapshot
- getText is positioned as fallback, not primary verification method
- No false promises about pipeline capabilities
- File has no syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/24-google-sheets-workflow-recovery/24-07-SUMMARY.md`
</output>
