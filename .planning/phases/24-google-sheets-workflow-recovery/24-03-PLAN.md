---
phase: 24-google-sheets-workflow-recovery
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - content/actions.js
  - background.js
autonomous: true
requirements: [P24-01, P24-02, P24-03, P24-04, P24-05, P24-06]

must_haves:
  truths:
    - "Click actions on Google Sheets toolbar elements (Name Box, formula bar, menus) succeed without timeout"
    - "Batch execution preserves Tab/Enter keystrokes between type actions on Sheets"
    - "Non-toolbar elements on canvas editor pages still go through normal readiness pipeline"
  artifacts:
    - path: "content/actions.js"
      provides: "Canvas editor toolbar click bypass before readiness check"
      contains: "canvas_editor_toolbar_bypass"
    - path: "background.js"
      provides: "Inter-action delay replacing batch suppression for Sheets"
      contains: "sheetsInterActionDelay"
  key_links:
    - from: "content/actions.js"
      to: "FSB.isCanvasBasedEditor()"
      via: "Canvas detection gate before toolbar bypass"
      pattern: "isCanvasBasedEditor.*isToolbarElement"
    - from: "background.js"
      to: "executeBatchActions action loop"
      via: "Delay inserted after each action result on Sheets URLs"
      pattern: "sheetsInterActionDelay.*setTimeout"
---

<objective>
Fix the two interaction-layer blockers that prevent Google Sheets automation: (1) click actions timing out on toolbar elements due to readiness pipeline false failures, and (2) batch suppression dropping Tab/Enter keystrokes between type actions.

Purpose: The detection layer (plans 24-01/02) correctly loads the Sheets guide, but the AI cannot actually interact with Sheets because click hangs and batch execution drops navigation keystrokes.
Output: Click works on Sheets toolbar elements, batch type+Tab+type sequences fill multiple cells.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-google-sheets-workflow-recovery/24-RESEARCH.md

@content/actions.js (click action at line 1033, readiness check at line 1102)
@background.js (executeBatchActions at line 6104, batch suppression at lines 6112-6140, action loop at line 6157)
@content/messaging.js (isCanvasBasedEditor at line 217)
</context>

<interfaces>
<!-- Key functions and patterns the executor needs -->

From content/actions.js (click action, lines 1033-1110):
- Element lookup at lines 1044-1050: `element = FSB.querySelectorWithShadow(sel)`
- Readiness check at line 1102: `const readiness = await FSB.smartEnsureReady(element, 'click')`
- Action recorder at line 1066: `actionRecorder.record(null, 'click', params, {...})`
- `captureElementDetails(element)` helper for recording
- `isElementInViewport(element)` helper for viewport check

From content/messaging.js (line 217-225):
- `FSB.isCanvasBasedEditor()` returns boolean, checks URL pattern for docs.google.com/(spreadsheets|document|presentation)/d/

From background.js (executeBatchActions, lines 6104-6250):
- Batch suppression block: lines 6112-6140 (the code to REPLACE)
- Action loop: lines 6157-6250 with `results.push()` at line 6194
- Stop on failure: line 6222
- Stop on navigation: line 6238
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add canvas editor toolbar click bypass in actions.js</name>
  <files>content/actions.js</files>
  <action>
In the `tools.click` function (line 1033), insert a canvas editor toolbar bypass AFTER the element lookup loop (line 1050) and BEFORE the readiness check (line 1102). The bypass must:

1. Check `FSB.isCanvasBasedEditor()` first (cheap boolean check).
2. If true, check if the found element matches known Sheets toolbar containers using `element.matches(selectors) || element.closest(selectors)` where selectors = `'#docs-chrome, .docs-titlebar-container, #docs-toolbar, .waffle-name-box, #t-name-box, .cell-input, #t-formula-bar-input, [id^="docs-"], .docs-menubar, .goog-toolbar, .docs-sheet-tab-container'`.
3. If toolbar element detected:
   a. Log via `actionRecorder.record` or the existing logger pattern with method `'canvas_toolbar_bypass'`.
   b. Scroll into view if not in viewport using `element.scrollIntoView({ behavior: 'instant', block: 'center' })` + 50ms wait.
   c. Dispatch full mouse event sequence: mousedown, mouseup, click with coordinates from `getBoundingClientRect()` center. Include `screenX`/`screenY` offsets.
   d. Call `element.click()` and `element.focus()`.
   e. Wait 100ms for Sheets to process.
   f. Record the action via `actionRecorder.record()` matching the existing pattern (see line 1066 for format).
   g. Return `{ success: true, clicked: params.selector, hadEffect: true, method: 'canvas_editor_toolbar_bypass', scrolled: false }`.
4. If NOT a toolbar element, fall through to normal readiness pipeline (no change).

Do NOT modify any code below the insertion point. The bypass is an early return that skips the readiness check only for toolbar elements.

Reference the exact code example in RESEARCH.md "Canvas Editor Click Bypass (actions.js)" section for the implementation pattern.
  </action>
  <verify>
    <automated>grep -n "canvas_editor_toolbar_bypass" content/actions.js | head -5</automated>
  </verify>
  <done>Click action on canvas editor toolbar elements bypasses readiness pipeline and returns success via direct DOM click. Non-toolbar elements still go through smartEnsureReady.</done>
</task>

<task type="auto">
  <name>Task 2: Replace batch suppression with inter-action delay in background.js</name>
  <files>background.js</files>
  <action>
In `executeBatchActions` (line 6104), replace the batch suppression block (lines 6112-6140) with a Sheets inter-action delay mechanism:

1. REMOVE lines 6112-6140 entirely (the `if (typeActionCount >= 2) { return { suppressed: true ... } }` block).
2. KEEP the `try { const tab = await chrome.tabs.get(tabId)` and URL check structure, but change the behavior:
   a. Instead of returning early with `suppressed: true`, set a variable `sheetsInterActionDelay = 200` when the URL matches `/docs\.google\.com\/spreadsheets\/d\//i`.
   b. Log at info level: `'Google Sheets batch: applying 200ms inter-action delay'` with sessionId, actionCount, and tools array.
3. In the action loop (after `results.push()` at line 6200 and session history push at line 6213), ADD before the stop-on-failure check:
   ```javascript
   // Google Sheets inter-action delay for canvas grid processing
   if (sheetsInterActionDelay > 0 && i < actions.length - 1) {
     await new Promise(resolve => setTimeout(resolve, sheetsInterActionDelay));
   }
   ```
4. Also remove the batch suppression fallback in `startAutomationLoop` at lines 9257-9266 (the `if (batchResult.suppressed)` block). Since we no longer return `suppressed: true`, this dead code should be cleaned up. Replace with a comment noting batches now execute with inter-action delay on Sheets.

The delay variable must be declared before the action loop (after line 6110, alongside the existing variable declarations).
  </action>
  <verify>
    <automated>grep -n "sheetsInterActionDelay\|suppressed.*true\|batch suppressed" background.js | head -10</automated>
  </verify>
  <done>Batch execution on Sheets URLs applies 200ms inter-action delay instead of suppressing the entire batch. Tab/Enter keystrokes between type actions are preserved. No "suppressed: true" return path exists.</done>
</task>

</tasks>

<verification>
1. `grep -n "canvas_editor_toolbar_bypass" content/actions.js` shows the bypass in the click action
2. `grep -n "sheetsInterActionDelay" background.js` shows the delay variable and its usage
3. `grep -n "suppressed.*true" background.js` returns NO results (suppression removed)
4. `grep -n "batch suppressed" background.js` returns NO results or only in comments
</verification>

<success_criteria>
- Click action on canvas editor toolbar elements bypasses readiness and succeeds via direct DOM events
- Batch execution on Sheets URLs adds 200ms delay between actions instead of suppressing
- Tab/Enter keystrokes in batch sequences are preserved (not dropped)
- Non-toolbar elements on canvas pages still go through normal readiness checks
- No "suppressed: true" return path remains in executeBatchActions
</success_criteria>

<output>
After completion, create `.planning/phases/24-google-sheets-workflow-recovery/24-03-SUMMARY.md`
</output>
