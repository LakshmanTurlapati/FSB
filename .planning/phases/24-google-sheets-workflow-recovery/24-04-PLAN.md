---
phase: 24-google-sheets-workflow-recovery
plan: 04
type: execute
wave: 2
depends_on: [24-03]
files_modified:
  - site-guides/productivity/google-sheets.js
autonomous: true
requirements:
  - P24-03
  - P24-04
  - P24-05
  - P24-06

must_haves:
  truths:
    - "Site guide emphasizes keyboard-first navigation patterns as the most reliable approach"
    - "Name Box click pattern is still documented but keyboard-only fallback is prominent"
    - "Sequential data entry pattern (type + Tab + type + Tab + Enter) is explicitly documented"
  artifacts:
    - path: "site-guides/productivity/google-sheets.js"
      provides: "Updated guidance with keyboard-first patterns and reliability notes"
      contains: "KEYBOARD-FIRST"
  key_links:
    - from: "site-guides/productivity/google-sheets.js"
      to: "ai/ai-integration.js prompt builder"
      via: "Site guide guidance string injected into AI system prompt"
      pattern: "guidance.*KEYBOARD-FIRST"
---

<objective>
Update the Google Sheets site guide to emphasize keyboard-first interaction patterns that bypass readiness checks entirely, while keeping the Name Box click pattern (now working with the toolbar bypass from plan 24-03).

Purpose: Even with the click bypass fix, keyboard-only patterns (keyPress/typeWithKeys via CDP debugger) are inherently more reliable on canvas editors because they bypass ALL DOM readiness checks. The guide should steer the AI toward these patterns as the primary approach.
Output: Updated site guide with keyboard-first navigation patterns prominently documented.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-google-sheets-workflow-recovery/24-RESEARCH.md
@.planning/phases/24-google-sheets-workflow-recovery/24-03-SUMMARY.md

@site-guides/productivity/google-sheets.js (full 256-line guide)

<interfaces>
<!-- The site guide structure -->

From site-guides/productivity/google-sheets.js:
```javascript
// registerSiteGuide({ site, category, patterns, guidance, selectors, workflows, warnings, toolPreferences })
// The `guidance` field is a template literal string injected into the AI system prompt.
// The `warnings` field is an array of strings also injected into the AI prompt.

// Current guidance structure (line 13):
// guidance: `GOOGLE SHEETS-SPECIFIC INTELLIGENCE:
//
// COMMON PATTERNS:              <- line 15, first section
//   # navigate to cell...
//   key "Escape"
//   click e5              # Name Box
//   ...
//
// CRITICAL WARNING...            <- line 29
// CANVAS-BASED GRID:            <- line 39
// NAME BOX NAVIGATION:          <- line 44
// DATA ENTRY:                   <- line 53
// KEYBOARD SHORTCUTS:           <- line 59
// FORMULA BAR VERIFICATION:     <- line 71
// DATA ENTRY BEST PRACTICES:    <- line 76
// `  <- guidance string ends line 81

// warnings array starts at line 232, contains 20 warning strings
// Last warning at line 253: 'CRITICAL: Do NOT type cell references...'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add keyboard-first navigation section to Sheets site guide</name>
  <files>site-guides/productivity/google-sheets.js</files>
  <action>
Update the `guidance` template literal string in site-guides/productivity/google-sheets.js. Make these changes:

1. Add a new section "KEYBOARD-FIRST NAVIGATION (MOST RELIABLE):" BEFORE the existing "COMMON PATTERNS:" section (currently at line 15 in the guidance string). This makes it the first thing the AI reads after the "GOOGLE SHEETS-SPECIFIC INTELLIGENCE:" heading. Content:

```
KEYBOARD-FIRST NAVIGATION (MOST RELIABLE):
  # For sequential data entry, keyboard navigation is more reliable than clicking:
  key "Escape"              # exit any edit mode
  click e5                  # click Name Box (reliable with toolbar bypass)
  type e5 "A1"              # type starting cell reference
  key "Enter"               # navigate to cell A1
  type "Header 1"           # type into active cell (no ref needed)
  key "Tab"                 # move to next column (B1)
  type "Header 2"           # type into B1
  key "Tab"                 # move to C1
  type "Header 3"           # type into C1
  key "Enter"               # move to next row (A2)
  type "Data 1"             # type into A2
  key "Tab"                 # move to B2
  type "Data 2"             # type into B2

  IMPORTANT: After navigating to a cell via Name Box, you do NOT need to click
  the cell. Just type -- keystrokes go to the active cell automatically.
  For sequential data entry: type value, Tab (next column), type value, Tab, ..., Enter (next row).
  The key and type commands use the CDP keyboard API which bypasses all DOM readiness checks.
```

2. In the existing "COMMON PATTERNS:" section, add a comment line as the first line of that section:
```
  # Name Box click pattern (also reliable with toolbar bypass):
```

3. In the "NAME BOX NAVIGATION (PRIMARY METHOD):" section, add after item 6 (the last step "Type the cell value..."):
```
  KEYBOARD ALTERNATIVE: If click on Name Box fails, use Ctrl+Home to go to A1 as a starting point,
  then use Tab/Enter to navigate to the target cell sequentially.
```

4. Do NOT remove any existing content -- only add the new keyboard-first section and the inline notes. The existing patterns remain valid (especially now that click bypass works from plan 24-03).
  </action>
  <verify>
    <automated>grep -n "KEYBOARD-FIRST" site-guides/productivity/google-sheets.js | head -3</automated>
  </verify>
  <done>Site guide has a prominent KEYBOARD-FIRST NAVIGATION section as the first pattern block in the guidance string, with keyboard-only examples that bypass readiness checks. Name Box click pattern preserved with toolbar bypass note. Keyboard alternative added to Name Box Navigation section.</done>
</task>

<task type="auto">
  <name>Task 2: Add keyboard reliability warning to site guide warnings array</name>
  <files>site-guides/productivity/google-sheets.js</files>
  <action>
Add one new warning string to the `warnings` array in the Google Sheets site guide. Insert it as the FIRST element of the array (before the existing "Google Sheets cells are rendered on a CANVAS" warning at the current first position), so the AI reads it first:

```javascript
'KEYBOARD NAVIGATION IS MOST RELIABLE: The key and type (without ref) commands use the Chrome DevTools Protocol keyboard API, bypassing all DOM readiness checks. When click actions fail or time out, switch to keyboard-only navigation: Escape (exit edit mode), Ctrl+Home (go to A1), Tab (next column), Enter (next row). For sequential data entry, prefer type + Tab + type + Tab + Enter over clicking individual cells.',
```

This reinforces the keyboard-first approach in the warnings section, which the AI also reads as part of the site guide injection.

Do NOT remove any existing warnings. Only add the one new warning at the start of the array.
  </action>
  <verify>
    <automated>grep -n "KEYBOARD NAVIGATION IS MOST RELIABLE" site-guides/productivity/google-sheets.js</automated>
  </verify>
  <done>Warnings array includes keyboard reliability guidance as the first warning. AI receives this as part of the site guide injection into prompts.</done>
</task>

</tasks>

<verification>
1. `grep -n "KEYBOARD-FIRST" site-guides/productivity/google-sheets.js` shows the new section in the guidance string
2. `grep -n "KEYBOARD NAVIGATION IS MOST RELIABLE" site-guides/productivity/google-sheets.js` shows the new warning
3. `grep -c "COMMON PATTERNS" site-guides/productivity/google-sheets.js` still returns 1 (existing section preserved)
4. `grep -c "NAME BOX NAVIGATION" site-guides/productivity/google-sheets.js` still returns 1 (existing section preserved)
5. `grep -c "KEYBOARD ALTERNATIVE" site-guides/productivity/google-sheets.js` returns 1 (new fallback note added)
</verification>

<success_criteria>
- Site guide has KEYBOARD-FIRST NAVIGATION section as the first pattern block in the guidance string
- Keyboard-only examples demonstrate type + Tab + Enter workflow without click
- Name Box click pattern preserved with note about toolbar bypass reliability
- Keyboard alternative fallback added to Name Box Navigation section
- New warning about keyboard reliability added as first element of warnings array
- No existing content removed -- only additions
</success_criteria>

<output>
After completion, create `.planning/phases/24-google-sheets-workflow-recovery/24-04-SUMMARY.md`
</output>
