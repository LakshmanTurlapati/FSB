---
phase: 27-site-explorer-markdown-snapshot-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/site-explorer.js
  - ui/options.js
autonomous: true
requirements:
  - P27-01
  - P27-02
  - P27-03
  - P27-04

must_haves:
  truths:
    - "Crawling any page fetches and stores a markdown snapshot alongside DOM data"
    - "Research detail view shows per-page markdown snapshot in a collapsible pre block with stats"
    - "Pages without a snapshot show a Snapshot unavailable warning"
    - "Matched guide name appears next to each page URL"
    - "Downloaded research JSON includes markdownSnapshot field per page"
  artifacts:
    - path: "utils/site-explorer.js"
      provides: "getMarkdownSnapshot call in collectPageData with guide selector threading"
      contains: "getMarkdownSnapshot"
    - path: "ui/options.js"
      provides: "Per-page collapsible snapshot pre block in research detail view"
      contains: "markdownSnapshot"
  key_links:
    - from: "utils/site-explorer.js"
      to: "content/messaging.js"
      via: "sendTabMessage with action getMarkdownSnapshot"
      pattern: "action.*getMarkdownSnapshot"
    - from: "utils/site-explorer.js"
      to: "site-guides/index.js"
      via: "getGuideForTask for URL-based guide resolution"
      pattern: "getGuideForTask"
    - from: "ui/options.js"
      to: "pageData.markdownSnapshot"
      via: "research detail HTML template rendering"
      pattern: "markdownSnapshot"
---

<objective>
Add markdown snapshot capture to the Site Explorer crawler and display it in the research detail view, so developers can see exactly what the AI sees when automating any site.

Purpose: Diagnostic visibility -- crawl a Google Sheets URL and confirm formula bar, name box, toolbar elements appear in the AI-visible snapshot with their values.
Output: Modified site-explorer.js (data collection) and options.js (display rendering).
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-site-explorer-markdown-snapshot-integration/27-CONTEXT.md
@.planning/phases/27-site-explorer-markdown-snapshot-integration/27-RESEARCH.md

@utils/site-explorer.js
@ui/options.js

<interfaces>
<!-- Key contracts the executor needs -->

From content/messaging.js (getMarkdownSnapshot handler, lines 749-767):
- Request: `{ action: 'getMarkdownSnapshot', options: { charBudget: 12000, maxElements: 80, guideSelectors } }`
- Response: `{ success: true, markdownSnapshot: string, refGeneration: number, elementCount: number }`

From site-guides/index.js:
- `getGuideForTask(task, url)` -- task FIRST, url SECOND
- Returns guide object with `.site` (per-site name) or `.name` (category name), `.selectors`, `.fsbElements`
- For URL-only lookup: `getGuideForTask('', url)` (empty string for task)

From background.js (guide selector construction pattern, line 8340):
- `iterationGuideSelectors = { ...guide.selectors, fsbElements: guide.fsbElements }`

From site-explorer.js:
- `sendTabMessage(tabId, message, timeout = 10000)` -- wraps chrome.tabs.sendMessage, does NOT pass frameId
- IMPORTANT: background.js uses `chrome.tabs.sendMessage(tabId, msg, { frameId: 0 })` for getMarkdownSnapshot
- sendTabMessage needs frameId:0 passed for this call -- modify the call to use chrome.tabs.sendMessage directly with { frameId: 0 }, or pass frameId through sendTabMessage

From site-explorer.js (pageData object, lines 287-299):
- Object with fields: url, depth, title, timestamp, interactiveElements, forms, navigation, headings, layout, internalLinks, keySelectors
- Stored in `this.pagesCollected[]`, serialized via `saveResearch()` which references `this.pagesCollected` directly

From ui/options.js (research detail view, lines 3181-3188):
- Per-page list uses: `data.pages.map(p => ...)`
- Existing per-page display: `<li>${escapeHtml(p.url)} (${p.interactiveElements?.length || 0} elements)</li>`
- Uses `escapeHtml()` for safe rendering (available globally in options.js)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add markdown snapshot capture to collectPageData</name>
  <files>utils/site-explorer.js</files>
  <action>
In `collectPageData()`, after the getDOM call (line 266) and before the getExplorerData call (line 268), add the markdown snapshot fetch:

1. Resolve the site guide for the current URL using `getGuideForTask('', url)` -- note argument order: empty task string FIRST, url SECOND.
2. Construct guideSelectors from the guide: `{ ...guide.selectors, fsbElements: guide.fsbElements }` (same pattern as background.js:8340).
3. Fetch the markdown snapshot. IMPORTANT: Do NOT use `this.sendTabMessage` because it does not pass `{ frameId: 0 }`. Instead, use `chrome.tabs.sendMessage` directly with `{ frameId: 0 }` as the third argument, wrapped in a Promise with timeout (same 10000ms pattern). This ensures the message targets the main frame, matching how background.js calls getMarkdownSnapshot.
4. Store the guide name as `guide.site || guide.name` for display.

After the pageData object definition (line 299), add three new fields:
- `markdownSnapshot: null` -- will hold the full snapshot text
- `markdownElementCount: 0` -- element count from response
- `guideName: null` -- matched guide name for display

After the "Merge explorer data" block (around line 335), add a "Merge markdown snapshot" block:
- If markdownData is truthy and markdownData.success is true, set `pageData.markdownSnapshot = markdownData.markdownSnapshot`, `pageData.markdownElementCount = markdownData.elementCount || 0`, and `pageData.guideName = guideName` (from the guide resolution step above).
- If markdownData is falsy but a guide was found, still set `pageData.guideName = guideName` so guide detection is visible even when snapshot fails.

Log at info level: `[SiteExplorer] Markdown snapshot: ${markdownData?.markdownSnapshot?.length || 0} chars, ${markdownData?.elementCount || 0} elements${guideName ? ', guide: ' + guideName : ''}`.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/laksh/OneDrive/Documents/Codes/FSB && grep -n "getMarkdownSnapshot" utils/site-explorer.js && grep -n "markdownSnapshot" utils/site-explorer.js && grep -n "getGuideForTask" utils/site-explorer.js && grep -n "guideName" utils/site-explorer.js && grep -n "frameId" utils/site-explorer.js</automated>
  </verify>
  <done>collectPageData fetches getMarkdownSnapshot after getDOM with guideSelectors threaded via getGuideForTask, stores markdownSnapshot/markdownElementCount/guideName on pageData, uses frameId:0 for main frame targeting</done>
</task>

<task type="auto">
  <name>Task 2: Render per-page markdown snapshot in research detail view</name>
  <files>ui/options.js</files>
  <action>
In `viewResearchDetail()` (lines 3181-3188), replace the per-page list item template. Currently each page is:
```
<li>${escapeHtml(p.url)} (${p.interactiveElements?.length || 0} elements)</li>
```

Replace with an expanded per-page item that includes:

1. **Guide name badge** next to URL: If `p.guideName` exists, show `<span style="color: #4fc3f7; margin-left: 0.5rem; font-size: 0.6875rem;">Guide: ${escapeHtml(p.guideName)}</span>`.

2. **Stats line** below URL: Show markdown stats or unavailable warning.
   - If `p.markdownSnapshot` exists: `Markdown: ${p.markdownSnapshot.length.toLocaleString()} chars, ${p.markdownElementCount || 0} elements`
   - If no snapshot: `<span style="color: #ffb74d;">Snapshot unavailable</span>` -- makes failures visible per user decision.

3. **Collapsible pre block** for the snapshot text (only if snapshot exists):
   - Use `<details>` / `<summary>` pattern matching the existing pages-crawled details.
   - Summary text: "View Markdown Snapshot"
   - Pre block with dark background styling: `background: #1a1a2e; color: #e0e0e0; padding: 0.75rem; border-radius: 4px; font-size: 0.6875rem; line-height: 1.4; max-height: 400px; overflow: auto; white-space: pre-wrap; word-break: break-word;`
   - Content: `escapeHtml(p.markdownSnapshot)` -- always escape for security.

4. **Increase max-height** of the pages list container from 200px to 500px since each page item is now taller with the snapshot collapsible.

The `<li>` elements should use `list-style: none; margin-bottom: 0.5rem;` for clean spacing since items now have sub-content. Add `padding-left: 0;` to the `<ul>` to compensate for removing list bullets.

No changes needed to `downloadResearch()` -- it serializes `data` as JSON directly, so `markdownSnapshot` on each page object is automatically included in downloads.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/laksh/OneDrive/Documents/Codes/FSB && grep -n "markdownSnapshot" ui/options.js && grep -n "guideName" ui/options.js && grep -n "Snapshot unavailable" ui/options.js && grep -n "View Markdown Snapshot" ui/options.js</automated>
  </verify>
  <done>Research detail view renders per-page guide name, markdown stats line (or "Snapshot unavailable" warning), and collapsible pre block with dark-themed snapshot text. Downloaded JSON automatically includes markdownSnapshot.</done>
</task>

</tasks>

<verification>
1. Load extension in Chrome
2. Open options page, navigate to Site Explorer
3. Crawl any website (e.g., a simple page like google.com)
4. Open research detail -- verify per-page snapshot collapsible appears with stats
5. Crawl a Google Sheets URL -- verify snapshot shows formula bar, name box, toolbar elements
6. Download research JSON -- verify markdownSnapshot field present in each page object
7. Check console for `[SiteExplorer] Markdown snapshot:` log lines confirming capture
</verification>

<success_criteria>
- collectPageData() fetches getMarkdownSnapshot after getDOM with guide selector threading via getGuideForTask
- Research detail view shows per-page collapsible pre blocks with markdown snapshot text and stats
- Pages without a snapshot display "Snapshot unavailable" warning
- Matched guide name shown next to each page URL
- Downloaded JSON includes markdownSnapshot field per page automatically
</success_criteria>

<output>
After completion, create `.planning/phases/27-site-explorer-markdown-snapshot-integration/27-01-SUMMARY.md`
</output>
