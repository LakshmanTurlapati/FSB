---
phase: 248-openclaw-spec-verification-gate-repo-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v0.9.61-OPENCLAW-SPEC.md
autonomous: true
requirements:
  - SCAFFOLD-01
  - SCAFFOLD-02
  - SCAFFOLD-03
  - SCAFFOLD-04

must_haves:
  truths:
    - "The findings doc at .planning/v0.9.61-OPENCLAW-SPEC.md answers all four spec questions (install[] schema, requires.bins enum, install hook timing, ClawHub FSB name decision) with explicit ASCII confidence markers."
    - "Every finding distinguishes web-research evidence from items that need live OpenClaw validation; the validation list is preserved verbatim in section 'Items requiring user validation against live OpenClaw'."
    - "The ClawHub name decision is recorded with a yes/no availability outcome and the namespaced fallback (fsb-browser + displayName: FSB) is documented as the Phase 253 escape hatch."
    - "Every confidence marker uses ASCII tags only ([OK], [WARN], [ASSUMED], [UNVERIFIED], plus HIGH/MEDIUM/LOW band words); no emojis anywhere in the document."
    - "A 'schema pinned' sign-off block is present at the bottom of the findings doc so Phase 249 has an explicit go/no-go anchor."
  artifacts:
    - path: ".planning/v0.9.61-OPENCLAW-SPEC.md"
      provides: "OpenClaw spec verification findings doc covering install[] schema, requires.bins enum, install timing, ClawHub name decision, plus bonus findings on command-arg-mode and publish QA gates."
      contains: "## 1. metadata.openclaw.install[] schema"
      contains_also:
        - "## 2. requires.bins[] accepted enum"
        - "## 3. install hook timing"
        - "## 4. ClawHub name collision for `FSB`"
        - "## Items requiring user validation against live OpenClaw"
        - "## Schema-pinned sign-off"
  key_links:
    - from: ".planning/v0.9.61-OPENCLAW-SPEC.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Each section heading references its REQ-ID (SCAFFOLD-01..04)"
      pattern: "SCAFFOLD-0[1-4]"
    - from: ".planning/v0.9.61-OPENCLAW-SPEC.md"
      to: ".planning/phases/248-openclaw-spec-verification-gate-repo-scaffolding/248-CONTEXT.md"
      via: "Schema findings summary in CONTEXT.md decisions block matches the findings doc verbatim shape (kind values, requires.bins guidance, install timing posture)."
      pattern: "kind:\\s*(brew|node|go|uv|download)"
---

<objective>
Refine and finalize the OpenClaw spec verification findings document at `.planning/v0.9.61-OPENCLAW-SPEC.md` so it answers the four spec questions with explicit confidence markers, records the ClawHub name-availability decision, preserves the live-validation deferral list, and ends with an explicit "schema pinned" sign-off block that gates Phase 249.

Purpose: Phases 249-253 author SKILL.md frontmatter, scripts, references, and publish QA against this document. A pinned, self-contained findings doc with traceable confidence markers and a deferred-validation list is the only Phase 248 deliverable that downstream phases can lock against without re-researching.

Output: An updated `.planning/v0.9.61-OPENCLAW-SPEC.md` that preserves the existing research subagent draft, adds any missing required structure (REQ-ID anchors, sign-off block), and removes any emoji or non-ASCII status marker if present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/248-openclaw-spec-verification-gate-repo-scaffolding/248-CONTEXT.md
@.planning/v0.9.61-OPENCLAW-SPEC.md

<interfaces>
<!-- Required ASCII status markers (project rule: zero emojis, ASCII only) -->
<!-- Confidence-band words: HIGH, MEDIUM-HIGH, MEDIUM, MEDIUM-LOW, LOW -->
<!-- Per-finding tags: [OK] verified-from-authoritative-source -->
<!--                   [WARN] partial-evidence -->
<!--                   [ASSUMED] reasonable-inference-from-prior-research -->
<!--                   [UNVERIFIED] not-confirmable-from-public-docs -->

<!-- REQ-ID coverage required (each MUST appear in a section heading): -->
<!-- SCAFFOLD-01: metadata.openclaw.install[] schema -->
<!-- SCAFFOLD-02: requires.bins[] accepted enum -->
<!-- SCAFFOLD-03: install hook timing -->
<!-- SCAFFOLD-04: ClawHub name collision for `FSB` -->

<!-- Locked decisions from .planning/STATE.md and 248-CONTEXT.md (do NOT relitigate): -->
<!-- - name: FSB (bare); fallback fsb-browser + displayName: FSB stays in reserve for Phase 253. -->
<!-- - requires.env: [] mandatory. -->
<!-- - npx -y fsb-mcp-server invocations stay unpinned. -->
<!-- - ASCII status markers only ([OK]/[FAIL]/[WARN]); zero emojis. -->
<!-- - version: 0.9.61 (milestone-aligned, NOT fsb-mcp-server@0.8.0). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and refine the existing spec findings document</name>
  <files>.planning/v0.9.61-OPENCLAW-SPEC.md</files>
  <read_first>
    - .planning/v0.9.61-OPENCLAW-SPEC.md (the existing research-subagent draft -- treat as starting artifact, not regenerate)
    - .planning/phases/248-openclaw-spec-verification-gate-repo-scaffolding/248-CONTEXT.md (locked decisions; verify findings align)
    - .planning/REQUIREMENTS.md (SCAFFOLD-01..04 wording -- match the question framing)
  </read_first>
  <action>
Read the existing `.planning/v0.9.61-OPENCLAW-SPEC.md` end-to-end. The research subagent has already written the substantive findings; this task LIGHTLY REFINES that draft (preserve content, fix gaps).

Perform these specific edits in order:

1. **Confirm REQ-ID anchors are present in section headings.** Each of these four sections MUST have its REQ-ID in parentheses on the heading line:
   - `## 1. metadata.openclaw.install[] schema (SCAFFOLD-01)`
   - `## 2. requires.bins[] accepted enum (SCAFFOLD-02)`
   - `## 3. install hook timing (SCAFFOLD-03)`
   - `## 4. ClawHub name collision for \`FSB\` (SCAFFOLD-04)`
   If any anchor is missing, add it. Do not change the section bodies otherwise.

2. **Verify each finding has an explicit confidence band.** Each of sections 1-4 MUST contain one of these confidence-band lines near the top: `**Confidence:** HIGH | MEDIUM-HIGH | MEDIUM | MEDIUM-LOW | LOW`. The current draft already has these; verify and leave alone if present.

3. **Verify ASCII markers throughout.** Run a grep-like scan in your head: any of the markers `[OK]`, `[WARN]`, `[ASSUMED]`, `[UNVERIFIED]`, `[FAIL]` may appear in body text -- this is correct. NO emoji glyphs (no checkmarks, no warning triangles, no party hats, no rockets, no anything outside ASCII). If you find any non-ASCII status glyph, replace with the corresponding ASCII marker. Project rule: ASCII status markers only.

4. **Confirm the "Items requiring user validation against live OpenClaw" section is preserved verbatim.** It currently lists three required validation items (install timing on Linux/Windows, definitive `clawhub search "FSB"` collision check, `requires.bins` warn-vs-block behavior) and one optional item (single-line metadata YAML constraint). Do not remove or rewrite. If the section is missing, restore it from `248-CONTEXT.md` lines 104-108.

5. **Confirm the `## Summary table` row count is 7** with the columns `Item | Status | Confidence`. Current rows: install[] schema, requires.bins, install timing, FSB name available, command-arg-mode default, ClawHub publish gates, priority/must-use absent. Do not edit unless a row is missing.

6. **Add (or preserve if present) a `## Schema-pinned sign-off` section as the final section** (after the validation list). Section body is exactly:

```
[OK] Phase 248 schema gate -- pinned 2026-05-08.

The four spec questions (SCAFFOLD-01..04) are answered above with explicit confidence markers. Items needing live OpenClaw validation are listed in "Items requiring user validation against live OpenClaw" and are USER-GATED, not blocking for Phase 248 closeout.

Downstream phases (249-253) MAY proceed using these findings as the canonical schema reference. If a live-validation item later contradicts a finding here, this document is the source of truth that gets edited; downstream artifacts get rebuilt against the corrected schema.

ClawHub name decision: name: FSB (bare). Fallback name: fsb-browser + displayName: FSB stays in reserve for Phase 253 publish QA only.
```

Do not paraphrase this block; copy the wording exactly.

7. **Do NOT re-research.** Do not invoke WebFetch or WebSearch. The existing findings stand. If you spot a gap that genuinely cannot be resolved without new research, leave a `[UNVERIFIED]` marker and add the gap to the validation list.

8. **Do NOT author SKILL.md frontmatter or any code.** This task ends at the findings doc.

Use the Edit or Write tool. If using Write to rewrite the whole file, preserve every existing finding verbatim except for the targeted changes above.
  </action>
  <verify>
    <automated>grep -E '^## (1\. metadata\.openclaw\.install\[\] schema \(SCAFFOLD-01\)|2\. requires\.bins\[\] accepted enum \(SCAFFOLD-02\)|3\. install hook timing \(SCAFFOLD-03\)|4\. ClawHub name collision for `FSB` \(SCAFFOLD-04\)|Items requiring user validation against live OpenClaw|Schema-pinned sign-off)' .planning/v0.9.61-OPENCLAW-SPEC.md | wc -l | tr -d ' ' | grep -qx 6 && echo OK_HEADINGS</automated>
    <automated>grep -c '\*\*Confidence:\*\* ' .planning/v0.9.61-OPENCLAW-SPEC.md | grep -E '^[4-9]$|^[0-9]{2,}$' && echo OK_CONFIDENCE_BANDS</automated>
    <automated>grep -q 'name: FSB' .planning/v0.9.61-OPENCLAW-SPEC.md && grep -q 'fsb-browser' .planning/v0.9.61-OPENCLAW-SPEC.md && echo OK_NAME_DECISION</automated>
    <automated>perl -ne 'print if /[^\x00-\x7F]/' .planning/v0.9.61-OPENCLAW-SPEC.md | wc -l | tr -d ' ' | grep -qx 0 && echo OK_ASCII_ONLY</automated>
  </verify>
  <acceptance_criteria>
    - The file `.planning/v0.9.61-OPENCLAW-SPEC.md` exists.
    - Six required headings are present (verbatim): the four SCAFFOLD-01..04 anchored section headings, plus `## Items requiring user validation against live OpenClaw`, plus `## Schema-pinned sign-off`. Verifiable via the OK_HEADINGS grep.
    - At least four `**Confidence:**` band lines are present (one per spec section). Verifiable via the OK_CONFIDENCE_BANDS grep.
    - Both `name: FSB` and `fsb-browser` strings appear in the document (current decision and fallback). Verifiable via OK_NAME_DECISION grep.
    - Zero non-ASCII bytes in the file. Verifiable via the OK_ASCII_ONLY perl one-liner -- if it prints any line, the test fails.
    - The literal string `[OK] Phase 248 schema gate -- pinned 2026-05-08.` appears under the sign-off section.
  </acceptance_criteria>
  <done>
    All four verify commands print their `OK_*` token. The findings document is committed-ready, preserves the existing research, and has an explicit sign-off block that Phase 249 can point at.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify ClawHub name decision is recorded with explicit yes/no outcome and fallback</name>
  <files>.planning/v0.9.61-OPENCLAW-SPEC.md</files>
  <read_first>
    - .planning/v0.9.61-OPENCLAW-SPEC.md (post Task 1)
    - .planning/phases/248-openclaw-spec-verification-gate-repo-scaffolding/248-CONTEXT.md (locked decision: name: FSB; fallback fsb-browser + displayName: FSB)
  </read_first>
  <action>
Open `.planning/v0.9.61-OPENCLAW-SPEC.md` section `## 4. ClawHub name collision for `FSB` (SCAFFOLD-04)`. Confirm the section contains all of:

1. **An explicit availability outcome line.** Either currently reads (and must continue to read): `**Result:** available (no collision found)`. If missing, add immediately after `**Confidence:**` line.

2. **A `### Method` subsection** describing the four research probes (direct WebFetch of clawhub.ai/skills/fsb, two WebSearches, listicle cross-reference). Already present per draft -- leave alone.

3. **A `### Finding` subsection** with the `[OK] No skill named ...` line and the `[WARN] Caveat: ClawHub's catalog ... ~13,000+ skills` caveat. Already present.

4. **A `### Recommendation` subsection** with two paragraphs:
   - Paragraph 1: "[OK] Lock `name: FSB` in SKILL.md frontmatter for the local skill (current milestone). Do **not** preemptively switch to `fsb-browser` -- the name appears available."
   - Paragraph 2: "For ClawHub publish (Phase 253), if `clawhub skill publish` rejects the slug or the user runs `clawhub search "FSB"` and finds a hit, fall back to the pre-decided pattern: `name: fsb-browser` + `displayName: FSB` (matches roadmap-locked decision)."

   These paragraphs are already present in the draft -- verify only. Do not paraphrase.

5. **The validation list section MUST include item 2: "`clawhub search "FSB"` definitive collision check"** as a deferred user task. Already present per draft -- verify only.

If any of items 1-5 are missing, add them. If all present, this task is a no-op verification pass and the file is unchanged.

Do NOT invoke a live ClawHub registry probe. Do NOT add new web research. The autonomous environment has no live ClawHub access; the user runs `clawhub search "FSB"` between Phase 248 and Phase 249.
  </action>
  <verify>
    <automated>grep -E '^\*\*Result:\*\* available \(no collision found\)' .planning/v0.9.61-OPENCLAW-SPEC.md && echo OK_OUTCOME</automated>
    <automated>grep -q 'Lock `name: FSB` in SKILL.md frontmatter' .planning/v0.9.61-OPENCLAW-SPEC.md && echo OK_LOCK_LOCAL</automated>
    <automated>grep -q '`name: fsb-browser` + `displayName: FSB`' .planning/v0.9.61-OPENCLAW-SPEC.md && echo OK_FALLBACK_DOCUMENTED</automated>
    <automated>grep -q '`clawhub search "FSB"` definitive collision check' .planning/v0.9.61-OPENCLAW-SPEC.md && echo OK_VALIDATION_DEFERRED</automated>
  </verify>
  <acceptance_criteria>
    - The `## 4. ClawHub name collision for `FSB` (SCAFFOLD-04)` section contains an explicit `**Result:** available (no collision found)` line.
    - The recommendation locks the local-milestone name as `FSB` and documents `fsb-browser` + `displayName: FSB` as the Phase 253 fallback.
    - The deferred-validation list still includes the live `clawhub search "FSB"` collision recheck as a user-gated item.
    - All four verify commands print their `OK_*` token.
  </acceptance_criteria>
  <done>
    Section 4 of the findings doc records the name decision (yes available, lock FSB) with the fallback documented, and the validation list still defers the definitive registry recheck to the user. Phase 253 can re-read this and act.
  </done>
</task>

</tasks>

<verification>
1. File `.planning/v0.9.61-OPENCLAW-SPEC.md` exists.
2. The six required section headings are present (four SCAFFOLD anchors + validation list + sign-off).
3. Zero non-ASCII characters in the file.
4. Both `name: FSB` (current) and `fsb-browser` (fallback) strings appear.
5. `[OK] Phase 248 schema gate -- pinned 2026-05-08.` appears in the sign-off section.

Run all verify commands from both tasks; every one must print its `OK_*` token.
</verification>

<success_criteria>
The findings document is the canonical schema reference for Phases 249-253:
- Four SCAFFOLD-01..04 questions answered with explicit confidence bands and ASCII markers.
- ClawHub name decision recorded: bare `FSB` is available; `fsb-browser` + `displayName: FSB` is the documented fallback for Phase 253.
- Live-validation items preserved as a user task list (not a Phase 248 blocker).
- A "Schema-pinned sign-off" block ends the doc so Phase 249 has an explicit anchor.
- Zero emojis; ASCII status markers only.
</success_criteria>

<output>
After completion, create `.planning/phases/248-openclaw-spec-verification-gate-repo-scaffolding/248-01-SUMMARY.md` documenting which sections were touched (or confirmed unchanged), the grep-verifiable acceptance evidence, and a one-line "schema pinned" handoff for Plan 248-02 / Phase 249.
</output>
