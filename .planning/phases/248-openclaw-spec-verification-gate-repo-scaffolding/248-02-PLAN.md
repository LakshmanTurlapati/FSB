---
phase: 248-openclaw-spec-verification-gate-repo-scaffolding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/FSB Skill/SKILL.md
  - skills/FSB Skill/USAGE.md
  - skills/FSB Skill/references/README.md
  - skills/FSB Skill/scripts/README.md
autonomous: true
requirements:
  - SCAFFOLD-05

must_haves:
  truths:
    - "The directory `skills/FSB Skill/` exists at the repo top level (sibling to `extension/`, `mcp/`, `showcase/`)."
    - "Four placeholder files exist inside the skeleton: `SKILL.md`, `USAGE.md`, `references/README.md`, `scripts/README.md`."
    - "Every placeholder file contains a single ASCII heading + a one-line `filled in Phase NNN` marker so a reader scanning the tree mid-milestone understands the staging."
    - "No SKILL.md frontmatter has been authored (Phase 249 owns frontmatter); the SKILL.md placeholder is intentionally NOT a YAML frontmatter document."
    - "Zero emojis anywhere in the new files; ASCII only."
    - "The `git status -s` output lists every new file under `skills/FSB Skill/`."
    - "Nothing in the existing CI chain breaks (no `package.json` / `plugin.json` is created inside the skill folder; no test files added; no script wired into root `npm test`)."
  artifacts:
    - path: "skills/FSB Skill/SKILL.md"
      provides: "Placeholder for the SKILL.md that Phase 249 will replace with verified frontmatter."
      contains: "filled in Phase 249"
    - path: "skills/FSB Skill/USAGE.md"
      provides: "Placeholder for the human-facing 3-step install + try-it + recovery doc that Phase 250 owns."
      contains: "filled in Phase 250"
    - path: "skills/FSB Skill/references/README.md"
      provides: "One-line directory marker for `references/`; Phase 250 fills in the five reference files (tool-decision-tree, multi-agent-contract, restricted-tab-recovery, default-to-fsb, vault-boundary, plus visual-session lifecycle)."
      contains: "filled in Phase 250"
    - path: "skills/FSB Skill/scripts/README.md"
      provides: "One-line directory marker for `scripts/`; Phase 249 fills in `doctor.mjs`, `print-stdio.mjs`, `install-host.mjs`."
      contains: "filled in Phase 249"
  key_links:
    - from: "skills/FSB Skill/"
      to: "git status"
      via: "all four placeholder files appear as untracked or staged in `git status -s`"
      pattern: "\\?\\? skills/FSB Skill/|A  skills/FSB Skill/"
    - from: "skills/FSB Skill/SKILL.md"
      to: ".planning/v0.9.61-OPENCLAW-SPEC.md"
      via: "placeholder body explicitly defers frontmatter authoring to Phase 249 and points readers at the schema findings doc"
      pattern: "Phase 249|v0\\.9\\.61-OPENCLAW-SPEC"
---

<objective>
Stand up the empty-but-committed `skills/FSB Skill/` skeleton at the repo root with placeholder content for `SKILL.md`, `USAGE.md`, `references/README.md`, and `scripts/README.md`. Each placeholder includes a one-line "filled in Phase NNN" marker so the staging is self-documenting mid-milestone.

Purpose: Phases 249-252 author against this directory shape. Without the skeleton committed, Phase 249 cannot write SKILL.md frontmatter (no parent directory) and Phase 250 cannot drop reference files (no `references/` subdir). The skeleton is the structural lock that lets parallel future work proceed.

Output: Four new files under `skills/FSB Skill/` (the directory contains a literal space). No frontmatter authored; no scripts authored; no tests added; no CI changes; no `package.json` / `plugin.json` inside the skill folder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/248-openclaw-spec-verification-gate-repo-scaffolding/248-CONTEXT.md

<interfaces>
<!-- Hard constraints from ROADMAP.md (enforced project-wide for v0.9.61): -->
<!-- - Repo path: `skills/FSB Skill/` (LITERAL space; quote all shell paths). -->
<!-- - SKILL.md `name:` field stays unauthored in this phase. Phase 249 owns it. -->
<!-- - All scripts are Node `.mjs` only (no `.sh`/`.cmd`). Phase 249 owns scripts. -->
<!-- - ASCII status markers only ([OK]/[FAIL]/[WARN]). Zero emojis. -->
<!-- - Use one-line README placeholders (NOT `.gitkeep`) per `248-CONTEXT.md` decisions. -->
<!--   Rationale: discoverable, conveys intent, sortable next to real Markdown content. -->

<!-- Anti-scope (do not add to this plan, per ROADMAP.md "Anti-Scope" list): -->
<!-- - NO `package.json` inside `skills/FSB Skill/`. -->
<!-- - NO `plugin.json` / `openclaw.plugin.json` inside `skills/FSB Skill/`. -->
<!-- - NO actual SKILL.md frontmatter. -->
<!-- - NO `.mjs` scripts authored (Phase 249). -->
<!-- - NO reference files authored (Phase 250). -->
<!-- - NO emojis. -->
<!-- - NO i18n / localised content. -->
<!-- - NO writing into `extension/`, `mcp/`, `showcase/`, or root README.md (Phase 252). -->

<!-- Repo top-level peers (ls /Users/lakshmanturlapati/Desktop/FSB shows these): -->
<!-- extension/, mcp/, showcase/, server/, server-py/, mcp-server/, scripts/, dist/, ... -->
<!-- The new `skills/` directory sits alongside these. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the skills/FSB Skill/ skeleton with placeholder SKILL.md and USAGE.md</name>
  <files>
    - skills/FSB Skill/SKILL.md
    - skills/FSB Skill/USAGE.md
  </files>
  <read_first>
    - .planning/phases/248-openclaw-spec-verification-gate-repo-scaffolding/248-CONTEXT.md (scaffold shape decisions; anti-scope list)
    - .planning/ROADMAP.md (Phase 249 / Phase 250 ownership of SKILL.md and USAGE.md respectively)
  </read_first>
  <action>
Create the parent directory and the two top-level placeholder files. The directory name contains a literal space; ALWAYS quote it in shell commands.

Step 1. Create the directory tree using the Bash tool:

```
mkdir -p "skills/FSB Skill/references" && mkdir -p "skills/FSB Skill/scripts"
```

(Both subdirectories are created here so Task 2 can drop README placeholders into them.)

Step 2. Create `skills/FSB Skill/SKILL.md` using the Write tool with EXACTLY this content (no leading blank line, no trailing whitespace beyond the final newline):

```
# FSB Skill -- placeholder

Status: scaffolded by Phase 248. SKILL.md frontmatter is filled in Phase 249.

This file is a placeholder. The verified OpenClaw frontmatter (name, description, version, requires.bins, requires.env, homepage, metadata.openclaw{...}) is authored in Phase 249 against the schema findings at `.planning/v0.9.61-OPENCLAW-SPEC.md`.

Do NOT load this skill into OpenClaw yet -- it has no frontmatter and no body content.

ASCII status markers only ([OK] / [FAIL] / [WARN]); zero emojis. See repo CLAUDE.md.
```

Step 3. Create `skills/FSB Skill/USAGE.md` using the Write tool with EXACTLY this content:

```
# FSB Skill USAGE -- placeholder

Status: scaffolded by Phase 248. USAGE.md is filled in Phase 250.

This file is a placeholder. The 3-step install (Chrome extension via `https://chromewebstore.google.com/detail/badgafnfchcihdfnjneklogedcdkmjfk` plus a GitHub Releases fallback link, then MCP server, then verify), the try-it prompts, and the doctor-failure recovery recipe are authored in Phase 250.

ASCII status markers only ([OK] / [FAIL] / [WARN]); zero emojis. See repo CLAUDE.md.
```

Hard constraints:
- Do NOT add YAML frontmatter to SKILL.md (no `---` block; Phase 249 owns this).
- Do NOT include any non-ASCII characters in either file.
- Do NOT include emojis. Markers are bracketed ASCII only.
- Do NOT write a `package.json`, `plugin.json`, `openclaw.plugin.json`, or any code file inside `skills/FSB Skill/`.
- The literal space in the directory name MUST be preserved -- the path is `skills/FSB Skill/`, not `skills/FSB-Skill/` or `skills/fsb-skill/`.
  </action>
  <verify>
    <automated>test -d "skills/FSB Skill" && test -d "skills/FSB Skill/references" && test -d "skills/FSB Skill/scripts" && echo OK_DIRS</automated>
    <automated>test -f "skills/FSB Skill/SKILL.md" && test -f "skills/FSB Skill/USAGE.md" && echo OK_FILES</automated>
    <automated>grep -q 'filled in Phase 249' "skills/FSB Skill/SKILL.md" && grep -q 'filled in Phase 250' "skills/FSB Skill/USAGE.md" && echo OK_PHASE_MARKERS</automated>
    <automated>head -1 "skills/FSB Skill/SKILL.md" | grep -qx '# FSB Skill -- placeholder' && head -1 "skills/FSB Skill/USAGE.md" | grep -qx '# FSB Skill USAGE -- placeholder' && echo OK_HEADINGS</automated>
    <automated>perl -ne 'print if /[^\x00-\x7F]/' "skills/FSB Skill/SKILL.md" "skills/FSB Skill/USAGE.md" | wc -l | tr -d ' ' | grep -qx 0 && echo OK_ASCII_ONLY</automated>
    <automated>! head -1 "skills/FSB Skill/SKILL.md" | grep -q '^---' && echo OK_NO_FRONTMATTER</automated>
    <automated>! test -f "skills/FSB Skill/package.json" && ! test -f "skills/FSB Skill/plugin.json" && ! test -f "skills/FSB Skill/openclaw.plugin.json" && echo OK_NO_FORBIDDEN_FILES</automated>
  </verify>
  <acceptance_criteria>
    - Directories `skills/FSB Skill/`, `skills/FSB Skill/references/`, `skills/FSB Skill/scripts/` all exist (verifiable with `test -d`).
    - Files `skills/FSB Skill/SKILL.md` and `skills/FSB Skill/USAGE.md` exist.
    - SKILL.md first line is exactly `# FSB Skill -- placeholder` (no leading whitespace, no surrounding blank lines).
    - USAGE.md first line is exactly `# FSB Skill USAGE -- placeholder`.
    - SKILL.md body contains the literal substring `filled in Phase 249`.
    - USAGE.md body contains the literal substring `filled in Phase 250`.
    - SKILL.md does NOT begin with a YAML frontmatter delimiter (`---`); the OK_NO_FRONTMATTER check confirms this.
    - Neither file contains any non-ASCII byte.
    - No `package.json`, `plugin.json`, or `openclaw.plugin.json` exists inside `skills/FSB Skill/`.
    - All seven verify commands print their `OK_*` token.
  </acceptance_criteria>
  <done>
    The skill skeleton root + two top-level placeholders exist with the exact content specified. No frontmatter, no code, no forbidden files. Phase 249 can later overwrite `SKILL.md` with verified frontmatter; Phase 250 can later overwrite `USAGE.md`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add one-line README placeholders to references/ and scripts/ subdirectories</name>
  <files>
    - skills/FSB Skill/references/README.md
    - skills/FSB Skill/scripts/README.md
  </files>
  <read_first>
    - .planning/phases/248-openclaw-spec-verification-gate-repo-scaffolding/248-CONTEXT.md (decision: prefer one-line README placeholders over `.gitkeep` for empty subdirs)
    - .planning/ROADMAP.md Phase 249 (scripts: doctor.mjs, print-stdio.mjs, install-host.mjs) and Phase 250 (references: tool-decision-tree.md, multi-agent-contract.md, restricted-tab-recovery.md, default-to-fsb.md, vault-boundary.md, visual-session lifecycle)
  </read_first>
  <action>
Create two README.md placeholder files inside the subdirectories. Quote the directory paths because of the literal space.

Step 1. Create `skills/FSB Skill/references/README.md` using the Write tool with EXACTLY this content:

```
# references/ -- placeholder

Status: scaffolded by Phase 248. Reference files are filled in Phase 250.

Phase 250 adds: tool-decision-tree.md, multi-agent-contract.md, restricted-tab-recovery.md, default-to-fsb.md, vault-boundary.md, plus a visual-session lifecycle reference. SKILL.md (authored Phase 249) points into this directory using progressive disclosure.

ASCII only; no emojis. See repo CLAUDE.md.
```

Step 2. Create `skills/FSB Skill/scripts/README.md` using the Write tool with EXACTLY this content:

```
# scripts/ -- placeholder

Status: scaffolded by Phase 248. Script files are filled in Phase 249.

Phase 249 adds three Node `.mjs` scripts: doctor.mjs (wraps `npx -y fsb-mcp-server doctor`), print-stdio.mjs (sources canonical OpenClaw stdio block from `mcp/src/install.ts` `getSetupSections()`), install-host.mjs (detect-list-confirm flow over `npx -y fsb-mcp-server install --list`).

No `.sh` / `.cmd` siblings; cross-platform Node only. ASCII only; no emojis. See repo CLAUDE.md.
```

Hard constraints:
- Do NOT create `.gitkeep` files; the README placeholders replace `.gitkeep` per `248-CONTEXT.md` Scaffold Shape decision (more discoverable than empty marker files).
- Do NOT author actual scripts (Phase 249) or reference files (Phase 250).
- Do NOT include any non-ASCII characters or emojis.
- Do NOT add a top-level `skills/FSB Skill/README.md` (`248-CONTEXT.md` Claude's Discretion section: "decision: skip; SKILL.md placeholder serves this role").
- Do NOT add a `package.json` or any code file in either subdirectory.
  </action>
  <verify>
    <automated>test -f "skills/FSB Skill/references/README.md" && test -f "skills/FSB Skill/scripts/README.md" && echo OK_README_FILES</automated>
    <automated>head -1 "skills/FSB Skill/references/README.md" | grep -qx '# references/ -- placeholder' && head -1 "skills/FSB Skill/scripts/README.md" | grep -qx '# scripts/ -- placeholder' && echo OK_README_HEADINGS</automated>
    <automated>grep -q 'filled in Phase 250' "skills/FSB Skill/references/README.md" && grep -q 'filled in Phase 249' "skills/FSB Skill/scripts/README.md" && echo OK_README_PHASE_MARKERS</automated>
    <automated>perl -ne 'print if /[^\x00-\x7F]/' "skills/FSB Skill/references/README.md" "skills/FSB Skill/scripts/README.md" | wc -l | tr -d ' ' | grep -qx 0 && echo OK_README_ASCII_ONLY</automated>
    <automated>! test -f "skills/FSB Skill/references/.gitkeep" && ! test -f "skills/FSB Skill/scripts/.gitkeep" && ! test -f "skills/FSB Skill/README.md" && echo OK_NO_GITKEEP_NO_TOPLEVEL_README</automated>
    <automated>git status -s 2>/dev/null | grep -E '\?\? skills/FSB Skill/|A  skills/FSB Skill/' | wc -l | tr -d ' ' | awk '{ if ($1 + 0 >= 4) print "OK_GIT_STATUS_TRACKS_TREE"; else print "FAIL_GIT_STATUS_count="$1 }' | grep -q OK_GIT_STATUS_TRACKS_TREE</automated>
    <automated>npm test --prefix . --silent 2>&1 | tail -20 || true; echo OK_CI_NOT_BROKEN_BY_SCAFFOLD</automated>
  </verify>
  <acceptance_criteria>
    - Files `skills/FSB Skill/references/README.md` and `skills/FSB Skill/scripts/README.md` both exist.
    - First line of `references/README.md` is exactly `# references/ -- placeholder`.
    - First line of `scripts/README.md` is exactly `# scripts/ -- placeholder`.
    - `references/README.md` body contains literal substring `filled in Phase 250`.
    - `scripts/README.md` body contains literal substring `filled in Phase 249`.
    - Neither README contains any non-ASCII byte.
    - No `.gitkeep` files were created in either subdirectory.
    - No top-level `skills/FSB Skill/README.md` was created (decision: SKILL.md placeholder serves this role).
    - `git status -s` shows at least 4 entries under `skills/FSB Skill/` (the four placeholders); the OK_GIT_STATUS_TRACKS_TREE check confirms the tree is visible to git.
    - The existing CI chain is not broken: the OK_CI_NOT_BROKEN_BY_SCAFFOLD check runs `npm test` and does not fail because of the new tree (no new test files added by this plan; if `npm test` was failing for unrelated reasons before this plan, that is not a regression introduced here).
  </acceptance_criteria>
  <done>
    Both subdirectory READMEs exist with the exact content specified, no `.gitkeep` files were created, no top-level skill README was created, and `git status -s` shows the four-file tree. The skeleton is committable.
  </done>
</task>

</tasks>

<verification>
1. `test -d "skills/FSB Skill"` succeeds.
2. All four placeholder files exist: `SKILL.md`, `USAGE.md`, `references/README.md`, `scripts/README.md`.
3. Each placeholder begins with the exact heading specified and contains its `filled in Phase NNN` marker.
4. SKILL.md does NOT have YAML frontmatter (no leading `---`); Phase 249 owns frontmatter.
5. Zero non-ASCII bytes across all four files.
6. No `package.json`, `plugin.json`, or `openclaw.plugin.json` inside `skills/FSB Skill/`.
7. No `.gitkeep` files anywhere in the new tree.
8. No top-level `skills/FSB Skill/README.md` (only the placeholders listed above).
9. `git status -s` shows the four new files under `skills/FSB Skill/`.
10. `npm test` from repo root continues to behave the same as before this plan ran (this plan adds no test files and no `package.json` inside the skill folder, so there is no path by which `npm test` could newly break).

Run all verify commands from both tasks; every one must print its `OK_*` token.
</verification>

<success_criteria>
The repo has a committable `skills/FSB Skill/` skeleton:
- Top level has `SKILL.md` and `USAGE.md` placeholders.
- `references/` and `scripts/` subdirs each have a one-line README placeholder.
- Every placeholder names its owning future phase (249 or 250) so mid-milestone readers understand staging.
- No frontmatter, no scripts, no reference content, no `package.json`, no `plugin.json` -- the skeleton is purely structural.
- ASCII only; zero emojis.
- Existing CI chain unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/248-openclaw-spec-verification-gate-repo-scaffolding/248-02-SUMMARY.md` documenting the four files created, the directory tree as captured by `git status -s`, the grep-verifiable acceptance evidence, and a one-line "skeleton committable" handoff for Phase 249 (scripts) and Phase 250 (references + USAGE.md).
</output>
