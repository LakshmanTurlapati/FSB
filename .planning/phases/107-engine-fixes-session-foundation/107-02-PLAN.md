---
phase: 107-engine-fixes-session-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - site-guides/design/excalidraw.js
autonomous: true
requirements: [ENGINE-03]

must_haves:
  truths:
    - "Every new Excalidraw session starts with modals dismissed, canvas cleared, and zoom reset to default"
    - "The site guide documents session setup as a mandatory first step before any drawing"
  artifacts:
    - path: "site-guides/design/excalidraw.js"
      provides: "Session setup workflow and mandatory initialization sequence"
      contains: "SESSION SETUP"
  key_links:
    - from: "site-guides/design/excalidraw.js guidance"
      to: "ai-integration.js site guide injection"
      via: "registerSiteGuide guidance string injected into AI system prompt"
      pattern: "SESSION SETUP"
---

<objective>
Add a mandatory session setup sequence to the Excalidraw site guide so the AI always initializes a clean canvas state before drawing.

Purpose: Without session setup, Excalidraw loads leftover content from localStorage, shows welcome modals that block input, and has unpredictable zoom/pan state. All three cause automation failures.

Output: Updated excalidraw.js site guide with a SESSION SETUP section documenting the Escape -> Ctrl+A -> Delete -> Ctrl+0 sequence, plus an updated fullFrameAlignmentWorkflow that starts with session setup.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-engine-fixes-session-foundation/107-CONTEXT.md
@.planning/research/PITFALLS-EXCALIDRAW.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mandatory SESSION SETUP section and text entry workflow to Excalidraw site guide</name>
  <files>site-guides/design/excalidraw.js</files>
  <read_first>
    - site-guides/design/excalidraw.js (full file -- the guide being modified)
    - .planning/research/PITFALLS-EXCALIDRAW.md (pitfalls 1, 5, 11, 12 for session setup rationale)
    - .planning/research/ARCHITECTURE.md (interaction pattern categories and site guide structure)
  </read_first>
  <action>
1. In the `guidance` string, add a new section titled "SESSION SETUP (MANDATORY -- run before ANY drawing)" immediately after the "AUTOPILOT STRATEGY HINTS" section and before "EXCALIDRAW-SPECIFIC INTELLIGENCE". The section content must be:

SESSION SETUP (MANDATORY -- run before ANY drawing):
  Step 1: press_key Escape                   -- dismiss any welcome modal or dialog
  Step 2: press_key a ctrl=true              -- select all existing elements (Ctrl+A)
  Step 3: press_key Delete                   -- delete selected elements (clear canvas)
  Step 4: press_key 0 ctrl=true              -- reset zoom to 100% (Ctrl+0)
  WHY: Excalidraw auto-saves to localStorage. Without clearing, previous session content contaminates new diagrams. Modals from first visit block all keyboard shortcuts. Zoom/pan state affects all coordinate calculations.

2. In the `guidance` string, add a new section titled "TEXT ENTRY WORKFLOW" after the "DRAWING SHAPES ON CANVAS" section. The content must be:

TEXT ENTRY WORKFLOW (for typing text on Excalidraw canvas):
  Step 1: Activate text mode -- either press T (standalone text) or double-click a shape via cdpClickAt (in-shape text)
  Step 2: Wait 300ms for the transient textarea to mount (Excalidraw creates textarea.excalidraw-wysiwyg dynamically)
  Step 3: Use cdpInsertText to type the text (NOT the type tool -- the textarea is ephemeral and not in DOM snapshots)
  Step 4: Press Escape via press_key to commit the text and close the textarea
  IMPORTANT: The textarea auto-focuses on creation. Do NOT try to find or click it. Just wait and use cdpInsertText.
  IMPORTANT: Never use the type tool for Excalidraw text -- it cannot find the transient textarea reliably.

3. Add a new autopilot hint line in the "AUTOPILOT STRATEGY HINTS" section:
- [canvas] ALWAYS run session setup first: Escape, Ctrl+A, Delete, Ctrl+0 -- clears modals, old content, and zoom

4. In the `workflows` object, add a new `sessionSetup` workflow array:
   sessionSetup: [
     'Press Escape via press_key to dismiss any welcome modal or dialog',
     'Press Ctrl+A via press_key(a, ctrl=true) to select all existing elements',
     'Press Delete via press_key to delete all selected elements (clear canvas)',
     'Press Ctrl+0 via press_key(0, ctrl=true) to reset zoom to 100%',
     'Canvas is now clean and ready for drawing'
   ]

5. In the `workflows` object, update `fullFrameAlignmentWorkflow` to start with session setup. Replace the first two steps:
   OLD: 'Navigate to excalidraw.com and wait for canvas to load', 'Dismiss any welcome dialogs (Escape key or click outside)'
   NEW: 'Navigate to excalidraw.com and wait for canvas to load', 'Run session setup: press Escape (dismiss modals), Ctrl+A then Delete (clear canvas), Ctrl+0 (reset zoom)'

6. Add a new workflow `textEntry` to the workflows object:
   textEntry: [
     'Activate text mode: press T for standalone text, or double-click shape center via cdpClickAt for in-shape text',
     'Wait 300ms for transient textarea.excalidraw-wysiwyg to mount and auto-focus',
     'Type text using cdpInsertText (NOT the type tool)',
     'Press Escape via press_key to commit text and close textarea',
     'Text is now rendered on the canvas'
   ]

7. Add a new warning to the `warnings` array:
   'ALWAYS run session setup (Escape, Ctrl+A, Delete, Ctrl+0) before any Excalidraw automation -- skipping causes stale content, blocked shortcuts, and coordinate errors'

8. Add 'cdpInsertText' to the `toolPreferences` array if not already present.
  </action>
  <verify>
    <automated>grep -c 'SESSION SETUP' site-guides/design/excalidraw.js && grep -c 'TEXT ENTRY WORKFLOW' site-guides/design/excalidraw.js && grep -c 'sessionSetup' site-guides/design/excalidraw.js && grep -c 'textEntry' site-guides/design/excalidraw.js</automated>
  </verify>
  <acceptance_criteria>
    - site-guides/design/excalidraw.js guidance string contains "SESSION SETUP (MANDATORY"
    - site-guides/design/excalidraw.js guidance string contains "press_key Escape" in session setup
    - site-guides/design/excalidraw.js guidance string contains "press_key a ctrl=true" in session setup
    - site-guides/design/excalidraw.js guidance string contains "press_key Delete" in session setup
    - site-guides/design/excalidraw.js guidance string contains "press_key 0 ctrl=true" in session setup
    - site-guides/design/excalidraw.js guidance string contains "TEXT ENTRY WORKFLOW"
    - site-guides/design/excalidraw.js guidance string contains "cdpInsertText" in text entry workflow
    - site-guides/design/excalidraw.js guidance string contains "textarea.excalidraw-wysiwyg"
    - site-guides/design/excalidraw.js workflows object contains sessionSetup array
    - site-guides/design/excalidraw.js workflows object contains textEntry array
    - site-guides/design/excalidraw.js warnings array contains "ALWAYS run session setup"
    - site-guides/design/excalidraw.js toolPreferences array contains "cdpInsertText"
    - No JavaScript syntax errors: node -c site-guides/design/excalidraw.js exits 0
  </acceptance_criteria>
  <done>Excalidraw site guide documents the mandatory session setup sequence (Escape, Ctrl+A, Delete, Ctrl+0) and text entry workflow (cdpInsertText into transient textarea), ensuring the AI always initializes a clean canvas state and uses the correct text entry path</done>
</task>

</tasks>

<verification>
1. grep 'SESSION SETUP' site-guides/design/excalidraw.js -- must match at least 2 times (guidance + workflow reference)
2. grep 'cdpInsertText' site-guides/design/excalidraw.js -- must match at least 2 times (text workflow + toolPreferences)
3. grep 'sessionSetup' site-guides/design/excalidraw.js -- must match at least 1 time (workflow key)
4. node -c site-guides/design/excalidraw.js -- must exit 0 (no syntax errors)
</verification>

<success_criteria>
- The guidance string contains a SESSION SETUP section with the 4-step sequence
- The guidance string contains a TEXT ENTRY WORKFLOW section with cdpInsertText instructions
- The workflows object has sessionSetup and textEntry entries
- The warnings array warns about always running session setup
- No syntax errors in the file
</success_criteria>

<output>
After completion, create `.planning/phases/107-engine-fixes-session-foundation/107-02-SUMMARY.md`
</output>
