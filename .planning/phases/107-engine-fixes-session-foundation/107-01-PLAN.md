---
phase: 107-engine-fixes-session-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - background.js
  - content/messaging.js
autonomous: true
requirements: [ENGINE-01, ENGINE-02]

must_haves:
  truths:
    - "FSB runs 10+ iteration Excalidraw sessions without the progress detector aborting for no progress"
    - "Text typed during an Excalidraw session reaches the canvas via CDP direct path, not the broken type tool round-trip"
  artifacts:
    - path: "background.js"
      provides: "isCanvasEditorUrl includes excalidraw.com"
      contains: "excalidraw"
    - path: "content/messaging.js"
      provides: "isCanvasBasedEditor detects excalidraw hostname"
      contains: "excalidraw"
  key_links:
    - from: "background.js isCanvasEditorUrl"
      to: "progress tracking madeProgress check"
      via: "isCanvasEditorUrl(currentUrl) branch in madeProgress calculation"
      pattern: "isCanvasEditorUrl.*excalidraw"
    - from: "content/messaging.js isCanvasBasedEditor"
      to: "content/actions.js type tool CDP bypass"
      via: "FSB.isCanvasBasedEditor() check at line 2132"
      pattern: "excalidraw\\.com"
---

<objective>
Fix the two gating engine bugs that prevent multi-step Excalidraw automation from working.

Purpose: Without these fixes, (1) the progress detector aborts every Excalidraw session after 6 iterations because canvas drawing produces no DOM changes, and (2) the type tool fails on Excalidraw because isCanvasBasedEditor does not recognize excalidraw.com, so CDP direct text entry is never triggered.

Output: background.js with expanded isCanvasEditorUrl (including unified canvasUrl regex), and content/messaging.js with Excalidraw hostname detection in isCanvasBasedEditor.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-engine-fixes-session-foundation/107-CONTEXT.md
@.planning/research/PITFALLS-EXCALIDRAW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand isCanvasEditorUrl to include Excalidraw and add CDP tools as progress signals</name>
  <files>background.js</files>
  <read_first>
    - background.js (lines 11070-11130 for isCanvasEditorUrl and madeProgress logic)
    - background.js (lines 4900-4915 for canvasUrl regex in validateCompletion)
  </read_first>
  <action>
1. At line 11074, update isCanvasEditorUrl to include excalidraw.com. Change the regex from:
   /docs\.google\.com\/(spreadsheets|document|presentation)\/d\//i
   to:
   /docs\.google\.com\/(spreadsheets|document|presentation)\/d\/|excalidraw\.com/i

2. At line 11101-11105, expand the canvas editor progress signal to also count CDP tools as progress (not just type/keyPress). The current code only counts type and keyPress as progress on canvas editors, but Excalidraw automation uses cdpDrag, cdpClickAt, cdpInsertText, and press_key. Change the action filter from:
   iterationActions.some(a => ['type', 'keyPress'].includes(a.tool) && a.result?.success)
   to:
   iterationActions.some(a => ['type', 'keyPress', 'press_key', 'cdpDrag', 'cdpClickAt', 'cdpInsertText', 'cdpDragVariableSpeed'].includes(a.tool) && a.result?.success)

3. Update the comment above (line 11101) from "Canvas-based editors (Sheets, Docs)" to "Canvas-based editors (Sheets, Docs, Excalidraw)" to reflect the expanded scope.

4. At line 4908, unify the canvasUrl regex with isCanvasEditorUrl by adding a comment referencing isCanvasEditorUrl to prevent future drift:
   Add comment: // NOTE: Keep in sync with isCanvasEditorUrl() around line 11074
  </action>
  <verify>
    <automated>grep -n 'excalidraw' background.js | grep -i 'isCanvasEditorUrl\|canvasUrl\|canvas.*editor' | head -5</automated>
  </verify>
  <acceptance_criteria>
    - background.js line ~11076 regex contains |excalidraw\.com
    - background.js line ~11104 array contains 'press_key', 'cdpDrag', 'cdpClickAt', 'cdpInsertText'
    - background.js line ~11101 comment contains "Excalidraw"
    - background.js line ~4908 has a sync comment referencing isCanvasEditorUrl
  </acceptance_criteria>
  <done>isCanvasEditorUrl returns true for excalidraw.com URLs, and successful CDP tool actions on Excalidraw count as progress in the madeProgress calculation</done>
</task>

<task type="auto">
  <name>Task 2: Expand isCanvasBasedEditor to detect Excalidraw hostname</name>
  <files>content/messaging.js</files>
  <read_first>
    - content/messaging.js (lines 214-225 for isCanvasBasedEditor function)
    - content/actions.js (lines 2129-2135 for the canvasEditor bypass in the type tool)
  </read_first>
  <action>
1. In the isCanvasBasedEditor function (line 217-225 of content/messaging.js), add Excalidraw detection after the Google Docs hostname check (line 219). Add:

   // Check for Excalidraw canvas editor
   if (host === 'excalidraw.com' || host.endsWith('.excalidraw.com')) return true;
   // Check for Excalidraw DOM markers (self-hosted instances)
   if (document.querySelector('.excalidraw, canvas.interactive')) return true;

   Insert these lines after line 219 (the docs.google.com check) and before the Google Docs kix class check (line 221).

2. Update the JSDoc comment (line 214-216) from "Google Docs, Sheets, Slides, etc." to "Google Docs, Sheets, Slides, Excalidraw, etc." to reflect the expanded scope.
  </action>
  <verify>
    <automated>grep -n 'excalidraw' content/messaging.js | head -5</automated>
  </verify>
  <acceptance_criteria>
    - content/messaging.js isCanvasBasedEditor function contains host === 'excalidraw.com'
    - content/messaging.js isCanvasBasedEditor function contains host.endsWith('.excalidraw.com')
    - content/messaging.js isCanvasBasedEditor function contains document.querySelector('.excalidraw, canvas.interactive')
    - content/messaging.js JSDoc comment contains "Excalidraw"
  </acceptance_criteria>
  <done>isCanvasBasedEditor returns true on excalidraw.com (and self-hosted Excalidraw instances), enabling the CDP direct text entry path in the type tool</done>
</task>

</tasks>

<verification>
1. grep -c 'excalidraw' background.js -- should return at least 3 matches (canvasUrl regex, isCanvasEditorUrl regex, sync comment)
2. grep -c 'excalidraw' content/messaging.js -- should return at least 3 matches (hostname, endsWith, querySelector, JSDoc)
3. Verify no syntax errors: node -c background.js 2>&1 | head -5
4. Verify no syntax errors: node -c content/messaging.js 2>&1 | head -5
</verification>

<success_criteria>
- isCanvasEditorUrl('https://excalidraw.com/') returns true (regex match)
- isCanvasBasedEditor() returns true when hostname is excalidraw.com
- CDP tools (cdpDrag, cdpClickAt, press_key, cdpInsertText) count as progress signals on canvas editor URLs
- No syntax errors in either modified file
</success_criteria>

<output>
After completion, create `.planning/phases/107-engine-fixes-session-foundation/107-01-SUMMARY.md`
</output>
