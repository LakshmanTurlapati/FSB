---
phase: 181-agent-loop-repair
plan: 03
type: execute
wave: 3
depends_on:
  - 181-02
files_modified:
  - background.js
autonomous: false
requirements:
  - LOOP-01
  - LOOP-02
  - LOOP-03
  - LOOP-04
  - LOOP-05
  - LOOP-06

must_haves:
  truths:
    - "Extension loads without errors in the service worker console"
    - "Starting automation from popup triggers the modular agent loop (not legacy)"
    - "CDP mouse tools respond with success objects"
    - "Safety breaker hooks are registered in the pipeline"
  artifacts:
    - path: "background.js"
      provides: "Fully wired agent loop with CDP handlers and hook pipeline"
      contains: "runAgentLoop(sessionId"
  key_links:
    - from: "popup.js/sidepanel.js"
      to: "background.js handleStartAutomation"
      via: "chrome.runtime.sendMessage startAutomation"
      pattern: "startAutomation"
    - from: "background.js handleStartAutomation"
      to: "ai/agent-loop.js runAgentLoop"
      via: "direct function call"
      pattern: "runAgentLoop"
---

<objective>
Verify the full agent loop repair works end-to-end: extension loads cleanly, automation starts via the modular loop, CDP tools respond, and safety limits are active.

Purpose: This is the verification checkpoint that confirms Plans 01 and 02 achieved their goals. It catches integration issues before the phase is marked complete.

Output: Verified working state of the agent loop repair.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/181-agent-loop-repair/181-CONTEXT.md
@.planning/phases/181-agent-loop-repair/181-01-SUMMARY.md
@.planning/phases/181-agent-loop-repair/181-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke-test the service worker for load errors and correct wiring</name>
  <files>background.js</files>
  <read_first>
    - background.js (lines 1-170 to verify all importScripts are present)
    - background.js (search for "runAgentLoop(sessionId" to verify both call sites)
    - background.js (search for "case 'cdpMouseClick'" to verify CDP handlers)
    - background.js (search for "function createSessionHooks" to verify hook factory)
  </read_first>
  <action>
Perform a static analysis of background.js to verify all Phase 181 changes are correctly integrated. Check the following:

1. **importScripts completeness:** Verify all 14 agent-loop ecosystem imports are present (engine-config.js, cost-tracker.js, transcript-store.js, hook-pipeline.js, turn-result.js, action-history.js, session-schema.js, permission-context.js, safety-hooks.js, permission-hook.js, progress-hook.js, tool-use-adapter.js, tool-executor.js, agent-loop.js).

2. **CDP handler completeness:** Verify all 5 case handlers exist in the message listener switch AND all 5 handler functions exist (handleCDPMouseClick, handleCDPMouseClickAndHold, handleCDPMouseDrag, handleCDPMouseDragVariableSpeed, handleCDPMouseWheel).

3. **createSessionHooks existence:** Verify the function exists and creates a HookPipeline with at least the safety breaker hook registered on BEFORE_ITERATION.

4. **runAgentLoop wiring:** Verify both call sites in handleStartAutomation use runAgentLoop (not startAutomationLoop). Verify the options objects include activeSessions, persistSession, sendSessionStatus, hooks.

5. **startAutomationLoop status:** Verify the function still exists (not deleted) and has a @deprecated comment. Verify zero active call sites (grep for `startAutomationLoop(sessionId)` should return 0 non-comment matches).

6. **No syntax errors:** Look for obvious issues -- unclosed braces, missing semicolons near the edit points, mismatched parentheses.

If any check fails, fix the issue in background.js before proceeding to the checkpoint.

Specific fixes to apply if found:
- If any importScripts is missing, add it in the correct position
- If any CDP handler case is missing, add it before the default: case
- If runAgentLoop options object is missing a required field, add it
- If startAutomationLoop still has active call sites, replace them
  </action>
  <verify>
    <automated>grep -c "importScripts('ai/agent-loop.js')" background.js && grep -c "importScripts('ai/engine-config.js')" background.js && grep -c "case 'cdpMouseClick'" background.js && grep -c "case 'cdpMouseWheel'" background.js && grep -c "function createSessionHooks" background.js && grep -c "runAgentLoop(sessionId" background.js && echo "All 6 integration checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - All 14 importScripts for agent-loop ecosystem are present in background.js
    - All 5 CDP case handlers present in message listener
    - All 5 CDP handler functions defined
    - createSessionHooks function defined with HookPipeline
    - runAgentLoop called at 2+ sites in handleStartAutomation
    - Zero non-comment calls to startAutomationLoop(sessionId)
    - No obvious syntax errors at edit boundaries
  </acceptance_criteria>
  <done>Static analysis confirms all Phase 181 changes are correctly integrated in background.js. All importScripts present, CDP handlers wired, hook pipeline factory exists, runAgentLoop is the canonical path, legacy loop deprecated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of agent loop repair</name>
  <files>background.js</files>
  <action>
Present the user with verification steps for the complete agent loop repair. The user must manually load the extension in Chrome and verify the service worker initializes correctly with the modular agent loop.

What was built across Plans 01-03:
(1) 14 importScripts for modular agent-loop ecosystem
(2) 5 CDP mouse message handlers restored
(3) createSessionHooks with HookPipeline
(4) handleStartAutomation rewired to call runAgentLoop

This means:
- The modular agent loop (tool_use protocol) is now the canonical automation path
- CDP tools (canvas click, drag-and-drop, coordinate scroll, click-and-hold) are functional
- Safety limits ($2 cost, 10 min time, 500 iterations) are enforced via checkSafetyBreakers
- Hook pipeline provides lifecycle events for safety, progress, and permissions
- startAutomationLoop is deprecated but preserved as unreachable fallback

Verification steps:
1. Open Chrome and navigate to chrome://extensions
2. Click "Load unpacked" and select the FSB extension directory (or click the reload button if already loaded)
3. Check the service worker link -- click it to open the console
4. Verify NO errors in the console related to importScripts failures
5. In the service worker console, type and verify each:
   - typeof runAgentLoop -- should return "function"
   - typeof SESSION_DEFAULTS -- should return "object"
   - typeof HookPipeline -- should return "function"
   - typeof createSessionHooks -- should return "function"
   - typeof handleCDPMouseClick -- should return "function"
6. Open a regular webpage (e.g., google.com)
7. Click the FSB extension icon to open the popup
8. Type a simple task like "click the search box" and click Start
9. Check the service worker console for [AgentLoop] log prefixes (confirms runAgentLoop is executing, not startAutomationLoop)
10. If the automation runs, check that it does not crash with errors about missing functions
  </action>
  <verify>
    <automated>grep -c "runAgentLoop(sessionId" background.js</automated>
  </verify>
  <done>User confirms: extension loads without errors, typeof checks pass, automation triggers [AgentLoop] logs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none new) | -- | -- | -- | All threats addressed in Plans 01 and 02 |
</threat_model>

<verification>
All checks from Plans 01 and 02 pass, plus the human verification confirms the extension loads and the agent loop executes.
</verification>

<success_criteria>
- Extension loads without service worker errors
- typeof runAgentLoop === 'function' in service worker console
- typeof createSessionHooks === 'function' in service worker console
- typeof handleCDPMouseClick === 'function' in service worker console
- Automation start triggers [AgentLoop] log output (not legacy loop)
- Human approves the verification checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/181-agent-loop-repair/181-03-SUMMARY.md`
</output>
