---
phase: 23-markdown-snapshot-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/dom-analysis.js
  - content/messaging.js
  - background.js
  - site-guides/index.js
autonomous: true
requirements: [P23-01, P23-02, P23-06]

must_haves:
  truths:
    - "No function with 'YAML' or 'compact' in its name exists in dom-analysis.js (except shared helpers getRegion, buildGuideAnnotations, inferActionForElement)"
    - "No message handler for getYAMLSnapshot or getCompactDOM exists in messaging.js"
    - "No includeCompactSnapshot property is sent from background.js"
    - "No comments reference 'YAML snapshot' or 'compact snapshot' as current/active features in modified files"
  artifacts:
    - path: "content/dom-analysis.js"
      provides: "Cleaned DOM analysis module without YAML/compact snapshot code"
      contains: "buildMarkdownSnapshot"
    - path: "content/messaging.js"
      provides: "Cleaned messaging without YAML/compact handlers"
    - path: "background.js"
      provides: "Background script without includeCompactSnapshot"
  key_links:
    - from: "content/messaging.js"
      to: "dom-analysis.js"
      via: "getMarkdownSnapshot handler remains, YAML/compact handlers removed"
      pattern: "getMarkdownSnapshot"
    - from: "background.js"
      to: "content/messaging.js"
      via: "getDOM calls no longer include includeCompactSnapshot"
      pattern: "getDOM"
---

<objective>
Remove all legacy YAML and compact snapshot code from dom-analysis.js, messaging.js, and background.js.

Purpose: Eliminate ~600 lines of dead code that maintained the pre-markdown snapshot formats. Phase 22 replaced these with markdown snapshots; this plan removes the legacy code paths.
Output: Four cleaned source files with YAML/compact code fully excised.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-page-text-extraction-for-reading-tasks/22-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove YAML and compact snapshot functions from dom-analysis.js</name>
  <files>content/dom-analysis.js</files>
  <action>
Delete the following functions and exports from content/dom-analysis.js:

**Functions to delete (by name -- locate them, delete entire function body):**
1. `generateCompactSnapshot` -- starts around line 1858, ~164 lines. Contains the "COMPACT SNAPSHOT GENERATION" comment block.
2. `buildMetadataHeader` -- starts around line 2033, ~69 lines.
3. `buildElementLine` -- starts around line 2130, ~99 lines.
4. `getElementFingerprint` -- starts around line 2236, ~10 lines.
5. `buildFilterFooter` -- starts around line 2256, ~19 lines.
6. `buildSelectOptions` -- starts around line 2282, ~17 lines.
7. `buildYAMLSnapshot` -- starts around line 2363, ~191 lines. This is the main YAML function.
8. `_runYAMLSnapshotSelfTest` -- starts around line 3242, ~110 lines. Contains "YAML Snapshot Self-Test" comment.

**Exports to delete (near end of file, ~line 3818-3819):**
- `FSB.generateCompactSnapshot = generateCompactSnapshot;`
- `FSB.buildYAMLSnapshot = buildYAMLSnapshot;`
- `FSB._runYAMLSnapshotSelfTest` related lines (around lines 3348, 3351)

**DO NOT delete:**
- `getRegion()` -- shared with markdown snapshot
- `inferActionForElement()` -- shared with markdown snapshot
- `buildGuideAnnotations()` -- shared with markdown snapshot
- `buildMarkdownSnapshot` -- the current active engine
- `extractPageText` -- the current active engine

**Comment cleanup in this file:**
- Line 2122 comment "Format a single element as a compact annotated line for the YAML snapshot" -- will be deleted with buildElementLine
- Lines 3229-3233 "YAML Snapshot Self-Test" comments -- will be deleted with the function
- Lines 3308, 3324-3326 references to compact/YAML -- will be deleted with _runYAMLSnapshotSelfTest
- Line 1852 "COMPACT SNAPSHOT GENERATION" section comment -- delete this comment block

After deletion, verify no dangling references remain. Search for "buildYAMLSnapshot", "generateCompactSnapshot", "buildMetadataHeader", "buildElementLine", "getElementFingerprint", "buildFilterFooter", "buildSelectOptions", "_runYAMLSnapshotSelfTest" -- zero hits expected.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/laksh/OneDrive/Documents/Codes/FSB && node -c content/dom-analysis.js && echo "SYNTAX OK" && ! grep -n "buildYAMLSnapshot\|generateCompactSnapshot\|buildMetadataHeader\|buildElementLine\|getElementFingerprint\|buildFilterFooter\|buildSelectOptions\|_runYAMLSnapshotSelfTest" content/dom-analysis.js && echo "NO LEGACY REFS" && grep -c "buildMarkdownSnapshot\|extractPageText\|getRegion\|buildGuideAnnotations" content/dom-analysis.js && echo "SHARED HELPERS PRESERVED"</automated>
  </verify>
  <done>All 8 YAML/compact functions deleted from dom-analysis.js. Exports removed. File passes syntax check. Shared helpers (getRegion, buildGuideAnnotations, inferActionForElement, buildMarkdownSnapshot, extractPageText) preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Remove YAML/compact handlers from messaging.js, background.js, and fix outdated comments</name>
  <files>content/messaging.js, background.js, site-guides/index.js</files>
  <action>
**messaging.js changes:**

1. Delete the `getCompactDOM` case block (around line 759-761, 3 lines). This is a `case 'getCompactDOM':` in a switch statement.

2. Delete the `getYAMLSnapshot` case block (around lines 764-783, ~20 lines). This is a `case 'getYAMLSnapshot':` in a switch statement.

3. Delete the `includeCompactSnapshot` path inside the getDOM handler (around lines 747-755, ~9 lines). This is an `if (domOptions.includeCompactSnapshot)` block that calls generateCompactSnapshot.

4. Remove `getYAMLSnapshot` from the async filter condition at line 1017. The line reads:
   `if (request.action === 'executeAction' || request.action === 'getDOM' || request.action === 'getYAMLSnapshot' || request.action === 'getMarkdownSnapshot' || request.action === 'readPage') {`
   Change to:
   `if (request.action === 'executeAction' || request.action === 'getDOM' || request.action === 'getMarkdownSnapshot' || request.action === 'readPage') {`

**background.js changes:**

1. Remove all 5 `includeCompactSnapshot: true` properties from getDOM message payloads at lines 8427, 8511, 8530, 9161, 9368. Just delete the line `includeCompactSnapshot: true` (and any trailing comma adjustment needed) from each object literal.

2. Update the outdated comment at line 3914 that reads "Covers 'check the price of X' where AI reads from YAML snapshot directly" -- change "YAML snapshot" to "page snapshot" to reflect current reality.

**site-guides/index.js comment fix:**

At line 420, change "Used by the YAML snapshot engine for [hint:key:action] annotation matching." to "Used by the markdown snapshot engine for [hint:key:action] annotation matching."
  </action>
  <verify>
    <automated>cd /mnt/c/Users/laksh/OneDrive/Documents/Codes/FSB && node -c content/messaging.js && echo "MESSAGING SYNTAX OK" && node -c background.js && echo "BACKGROUND SYNTAX OK" && ! grep -n "getYAMLSnapshot\|getCompactDOM\|includeCompactSnapshot" content/messaging.js background.js && echo "NO LEGACY REFS" && ! grep -in "YAML snapshot" background.js site-guides/index.js && echo "NO OUTDATED YAML COMMENTS"</automated>
  </verify>
  <done>getYAMLSnapshot and getCompactDOM handlers removed from messaging.js. includeCompactSnapshot removed from all 5 background.js locations. Async filter updated. Outdated YAML comments fixed in background.js and site-guides/index.js. All files pass syntax check with zero legacy references.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `node -c content/dom-analysis.js` -- syntax valid
2. `node -c content/messaging.js` -- syntax valid
3. `node -c background.js` -- syntax valid
4. Zero grep hits for: buildYAMLSnapshot, generateCompactSnapshot, getYAMLSnapshot, getCompactDOM, includeCompactSnapshot across all three files
5. buildMarkdownSnapshot, extractPageText, getRegion still present in dom-analysis.js
6. getMarkdownSnapshot handler still present in messaging.js
7. No "YAML snapshot" comments remain as current/active references
</verification>

<success_criteria>
~600 lines of legacy YAML/compact snapshot code removed across 3 files. All files pass syntax check. No callers of removed functions remain. Outdated comments updated. Markdown snapshot path completely unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/23-markdown-snapshot-cleanup/23-01-SUMMARY.md`
</output>
