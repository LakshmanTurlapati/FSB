---
plan: 251-01
phase: 251-tests-ci-integration
title: Author tests/skill-fsb-spec.test.js + wire into npm test
wave: 1
depends_on: []
files_modified:
  - tests/skill-fsb-spec.test.js
  - package.json
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05]
status: in_progress
---

<objective>
Ship a single static-content test (`tests/skill-fsb-spec.test.js`) that locks the FSB OpenClaw skill against silent regression, then wire it into root `npm test` so the existing `ci / all-green` PR gate covers the skill. Five assertions cover frontmatter, stdio block parity, references completeness, multi-agent typed errors, and USAGE.md links.
</objective>

<must_haves>
- `tests/skill-fsb-spec.test.js` exists, is plain Node CommonJS (matching `tests/tool-definitions-parity.test.js` style), exits 0 on green and non-zero on first failure.
- Frontmatter check (TEST-02): `name: FSB`, `version: 0.9.61`, `requires.bins` contains `node` and `npx`, `requires.env` empty array, `metadata.openclaw` parses as JSON with `kind: "node"` and `package: "fsb-mcp-server"`. No `priority`, no `must-use` keys.
- Stdio parity (TEST-03): `print-stdio.mjs` JSON shape deep-equals canonical from `mcp/src/install.ts`.
- References completeness (TEST-04): all 6 `references/*.md` exist; `multi-agent-contract.md` mentions all 4 typed errors + `back`; `tool-decision-tree.md` only references tool names that exist in `mcp/ai/tool-definitions.cjs`.
- USAGE links (TEST-05): exact Chrome Web Store URL + GitHub Releases fallback present.
- CI integration (TEST-01): root `package.json` `"test"` script chain ends with `&& node tests/skill-fsb-spec.test.js`.
- ZERO emojis. ASCII only.
</must_haves>

<task n="1" id="251-01-1">

<files>
- tests/skill-fsb-spec.test.js (NEW)
</files>

<read_first>
- skills/FSB Skill/SKILL.md
- skills/FSB Skill/USAGE.md
- skills/FSB Skill/scripts/print-stdio.mjs
- skills/FSB Skill/references/{tool-decision-tree,multi-agent-contract,restricted-tab-recovery,default-to-fsb,vault-boundary,visual-session-lifecycle}.md
- mcp/src/install.ts (lines 28, 173-174)
- mcp/ai/tool-definitions.cjs (canonical tool name registry)
- tests/tool-definitions-parity.test.js (style reference)
</read_first>

<action>
Create `tests/skill-fsb-spec.test.js` with all five static-content checks. Use CommonJS (`require`) to match dominant project test style. Use the `passed`/`failed` counter pattern from tool-definitions-parity.test.js. Inline minimal YAML/JSON parsing (no new dev deps).
</action>

<acceptance_criteria>
- File exists at `tests/skill-fsb-spec.test.js`
- `node tests/skill-fsb-spec.test.js` exits 0
- Output contains `[OK]` or `PASS:` markers (no emojis)
- File is ASCII-only (perl `[^\x00-\x7F]` returns 0)
- File covers all 5 TEST-01..05 IDs in code comments / sections
</acceptance_criteria>

<verify>
<automated>
node "tests/skill-fsb-spec.test.js" && echo "OK_TEST_PASSES"
test -f tests/skill-fsb-spec.test.js && echo "OK_FILE_EXISTS"
perl -ne 'print if /[^\x00-\x7F]/' tests/skill-fsb-spec.test.js | wc -c | grep -qx 0 && echo "OK_ASCII_ONLY"
</automated>
</verify>

<done>Test file authored, runs green standalone.</done>

</task>

<task n="2" id="251-01-2">

<files>
- package.json (MODIFY)
</files>

<read_first>
- package.json (existing test script)
</read_first>

<action>
Edit root `package.json` `"test"` script: append ` && node tests/skill-fsb-spec.test.js` to the END of the existing chain. Do not modify any other field. Preserve formatting.
</action>

<acceptance_criteria>
- `package.json` `"test"` script ends with `&& node tests/skill-fsb-spec.test.js`
- `node -e "JSON.parse(require('fs').readFileSync('package.json'))"` succeeds (file is still valid JSON)
- No other fields changed
</acceptance_criteria>

<verify>
<automated>
node -e "const t=require('./package.json').scripts.test; if(!t.endsWith('node tests/skill-fsb-spec.test.js')) throw new Error('test script does not end with new test'); console.log('OK_CI_WIRED');"
</automated>
</verify>

<done>npm test chain extended with the new test.</done>

</task>
