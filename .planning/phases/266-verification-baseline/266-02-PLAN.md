---
phase: 266-verification-baseline
plan: 02
gap_closure: true
parent_plan: 266-01
subsystem: showcase/i18n
tags: [i18n, gap-closure, extract-i18n-drift]
one_liner: "Refresh messages.xlf to absorb post-Phase-264 line-number drift; no translation content change."
requires:
  - "266-01-PLAN.md Task 4 ran and exposed drift"
provides:
  - "messages.xlf byte-equal to fresh ng extract-i18n"
  - "T4 of 266-01 re-runs green"
affects:
  - "showcase/angular/src/locale/messages.xlf (only <context-type=\"linenumber\"> values updated)"
requirements_addressed: [VERIFY-01]
---

## Background

`266-01-PLAN.md` Task 4 ran `ng extract-i18n` + `diff -u` against the committed `messages.xlf`. Diff is non-empty BUT:

- Trans-unit count: 420 == 420 (unchanged)
- All trans-unit IDs unchanged
- All `<source>` content unchanged
- Only `<context-type="linenumber">` values drifted (shifted by 2-3 lines)

Root cause: Phase 264 commit `7a5b6b0` (per-locale canonical + hreflang on marketing pages) added a handful of lines to `*-page.component.ts` files after Phase 262 had captured the original `$localize` line numbers. The translations themselves are unaffected.

## Task

### Task 1 -- Refresh committed messages.xlf
**File:** `showcase/angular/src/locale/messages.xlf`
**Action:**
```bash
cp /tmp/extract-check-266/messages.xlf showcase/angular/src/locale/messages.xlf
```
(Fresh extract was already produced in 266-01 Task 4.)

**Verification:**
- `grep -c "<trans-unit " showcase/angular/src/locale/messages.xlf` returns 420.
- Re-run `diff -u showcase/angular/src/locale/messages.xlf /tmp/extract-check-266/messages.xlf` -> empty.
- `git diff --stat showcase/angular/src/locale/messages.xlf` shows changes only on `<context context-type="linenumber">` lines (`git diff showcase/angular/src/locale/messages.xlf | grep -E "^[-+]" | grep -v "^[-+]{3}" | grep -v linenumber` empty).
- Target XLIFFs untouched (`git diff --stat showcase/angular/src/locale/messages.{es,de,ja,zh-CN,zh-TW}.xlf` empty).

**Commit:** `fix(266-02): refresh messages.xlf linenumber metadata after 264 TS edits`

## Acceptance

- T4 of `266-01-PLAN.md` re-runs and exits 0 with empty diff.
- No target XLIFF touched.
- No translation content changed.
