---
plan: 253-01
phase: 253-clawhub-pre-publish-qa-user-gated-publish
title: ClawHub Pre-Publish QA + package:skill build script + user-gated publish docs
wave: 1
depends_on: []
files_modified:
  - .planning/v0.9.61-CLAWHUB-PUBLISH-QA.md
  - scripts/package-skill.mjs
  - package.json
  - mcp/README.md
autonomous: true
requirements: [PUB-01, PUB-02, PUB-03]
status: complete
---

<objective>
Ship the pre-publish QA pass + reproducible build artifact + documented user-gated publish flow. Actual `clawhub publish` is NOT run by autonomous mode.
</objective>

<must_haves>
- `.planning/v0.9.61-CLAWHUB-PUBLISH-QA.md` records all 7 QA gates with [OK] / [USER-GATED] markers.
- `scripts/package-skill.mjs` produces `dist/skill/FSB-Skill-<version>.zip` reproducibly.
- `package.json` has `"package:skill"` script wired.
- `mcp/README.md` documents `npm run package:skill` and the publish one-liner.
- Phase 251 test still passes.
- ZERO emojis; ASCII only.
</must_haves>

<task n="1" id="253-01-1">
<files>
- .planning/v0.9.61-CLAWHUB-PUBLISH-QA.md (NEW)
</files>
<read_first>
- .planning/REQUIREMENTS.md (PUB-01 details)
- skills/FSB Skill/ (whole tree -- subject of QA)
</read_first>
<action>
Run local QA gates (clawhub inspect, secret grep, invisible unicode scan), record results plus user-gated server-side gates in a new file at `.planning/v0.9.61-CLAWHUB-PUBLISH-QA.md`. Include recovery section.
</action>
<acceptance_criteria>
- File exists at `.planning/v0.9.61-CLAWHUB-PUBLISH-QA.md`
- Contains all 7 gate sections (name collision, secret grep, invisible unicode, account age, VirusTotal, ClawScan, reproducible build, publish sequence)
- Each gate has explicit [OK] / [USER-GATED] / [DOCUMENTED] marker
- ASCII-only
</acceptance_criteria>
<verify>
<automated>
test -f .planning/v0.9.61-CLAWHUB-PUBLISH-QA.md && echo OK_QA_DOC_EXISTS
grep -q "Schema-pinned\|name: FSB\|fsb-browser" .planning/v0.9.61-CLAWHUB-PUBLISH-QA.md || grep -q "available" .planning/v0.9.61-CLAWHUB-PUBLISH-QA.md && echo OK_NAME_LOCK_RECORDED
perl -ne 'print if /[^\x00-\x7F]/' .planning/v0.9.61-CLAWHUB-PUBLISH-QA.md | wc -c | grep -qx 0 && echo OK_QA_ASCII
</automated>
</verify>
<done>QA doc written with all gate results.</done>
</task>

<task n="2" id="253-01-2">
<files>
- scripts/package-skill.mjs (NEW)
- package.json (MODIFY -- add scripts.package:skill)
</files>
<read_first>
- skills/FSB Skill/SKILL.md (version stamping)
- scripts/package-extension.mjs (sibling pattern)
- package.json (existing scripts)
</read_first>
<action>
Create `scripts/package-skill.mjs`. Read version from SKILL.md frontmatter; zip `skills/FSB Skill/` into `dist/skill/FSB-Skill-<version>.zip` using system `zip -X -r`. Wire into `package.json` `"scripts.package:skill"`.
</action>
<acceptance_criteria>
- `scripts/package-skill.mjs` exists, runs without error
- `npm run package:skill` produces `dist/skill/FSB-Skill-0.9.61.zip`
- `package.json` `"scripts.package:skill"` field exists
- Zip artifact contains `skills/FSB Skill/SKILL.md` and at least 4 reference files
</acceptance_criteria>
<verify>
<automated>
test -f scripts/package-skill.mjs && echo OK_SCRIPT_EXISTS
node -e "const t=require('./package.json').scripts['package:skill']; if(!t||!t.includes('package-skill')) throw new Error('not wired'); console.log('OK_PACKAGE_SCRIPT_WIRED');"
test -f dist/skill/FSB-Skill-0.9.61.zip && echo OK_ARTIFACT_BUILT
</automated>
</verify>
<done>Build script + npm wiring complete.</done>
</task>

<task n="3" id="253-01-3">
<files>
- mcp/README.md (MODIFY -- extend OpenClaw paragraph with package:skill docs)
</files>
<read_first>
- mcp/README.md (### OpenClaw section added in Phase 252)
</read_first>
<action>
Append a 2-3 sentence paragraph to the existing `### OpenClaw` section explaining `npm run package:skill` (reproducible artifact) and the user-gated `clawhub login` + `clawhub publish "skills/FSB Skill"` sequence. Reference the QA doc.
</action>
<acceptance_criteria>
- `mcp/README.md` mentions `npm run package:skill`
- `mcp/README.md` mentions `clawhub publish`
- `mcp/README.md` references `.planning/v0.9.61-CLAWHUB-PUBLISH-QA.md`
- ASCII-only
</acceptance_criteria>
<verify>
<automated>
grep -q "package:skill" mcp/README.md && echo OK_MCP_README_PACKAGE_SCRIPT
grep -q "clawhub publish" mcp/README.md && echo OK_MCP_README_PUBLISH
</automated>
</verify>
<done>mcp/README.md PUB-02 documentation added.</done>
</task>
