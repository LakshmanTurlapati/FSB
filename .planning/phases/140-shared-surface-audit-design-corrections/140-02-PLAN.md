---
phase: 140-shared-surface-audit-design-corrections
plan: 02
type: execute
wave: 2
depends_on: [140-01]
files_modified:
  - ui/sidepanel.css
  - ui/popup.css
  - ui/options.css
  - showcase/css/main.css
  - showcase/css/dashboard.css
autonomous: true
requirements: [COH-01, COH-02, COH-03]

must_haves:
  truths:
    - "Repeated shared primitives on audited surfaces consume the new shared baseline and no longer show obvious spacing, border, or state drift"
    - "Sidepanel and popup retain the same product feel while aligning their headers, message chrome, icon buttons, and composer surfaces"
    - "Control panel and dashboard shared controls, cards, badges, and input chrome visually align with the same shared baseline"
  artifacts:
    - path: "ui/sidepanel.css"
      provides: "Retouched sidepanel primitives aligned with the shared baseline"
      contains: "var(--fsb-primary"
    - path: "ui/popup.css"
      provides: "Retouched popup primitives aligned with the shared baseline"
      contains: "var(--fsb-primary"
    - path: "ui/options.css"
      provides: "Control panel primitives aligned with the shared baseline"
      contains: "var(--fsb-surface"
    - path: "showcase/css/dashboard.css"
      provides: "Dashboard primitives aligned with the shared baseline"
      contains: "var(--fsb-surface"
  key_links:
    - from: "ui/sidepanel.css"
      to: "shared/fsb-ui-core.css"
      via: "Selectors consume shared token variables"
      pattern: "var\\(--fsb-"
    - from: "ui/popup.css"
      to: "shared/fsb-ui-core.css"
      via: "Selectors consume shared token variables"
      pattern: "var\\(--fsb-"
    - from: "ui/options.css"
      to: "shared/fsb-ui-core.css"
      via: "Control-panel chrome consumes shared token variables"
      pattern: "var\\(--fsb-"
    - from: "showcase/css/dashboard.css"
      to: "shared/fsb-ui-core.css"
      via: "Dashboard chrome consumes shared token variables"
      pattern: "var\\(--fsb-"
---

<objective>
Apply the shared FSB UI baseline to the repeated primitives already present on the popup, sidepanel, control panel, and dashboard. Remove obvious drift in headers, controls, cards, badges, status treatments, and input chrome while keeping layout-level redesign work out of this phase.

Purpose: make the product feel like one system before later phases refine individual surfaces more deeply.

Output: updated popup, sidepanel, control panel, and dashboard CSS consuming the shared baseline.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-shared-surface-audit-design-corrections/140-CONTEXT.md
@.planning/phases/140-shared-surface-audit-design-corrections/140-UI-SPEC.md
@.planning/phases/140-shared-surface-audit-design-corrections/140-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize popup and sidepanel shared primitives</name>
  <files>ui/sidepanel.css, ui/popup.css</files>
  <read_first>ui/sidepanel.css, ui/popup.css, shared/fsb-ui-core.css</read_first>
  <action>
Retouch the sidepanel and popup shared primitives to consume the shared baseline. Focus on header chrome, icon buttons, status indicator, message bubble family, composer/input chrome, footer treatment, and other obviously shared UI pieces. Keep layout changes conservative; do not absorb the dedicated Phase 141 surface pass.
  </action>
  <acceptance_criteria>
- Sidepanel and popup use shared token variables for repeated primitives
- Shared primitives no longer diverge in obvious borders, shadows, spacing, or state styling
- The pages still look like FSB, not like a different product
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Normalize control panel and dashboard shared primitives</name>
  <files>ui/options.css, showcase/css/main.css, showcase/css/dashboard.css</files>
  <read_first>ui/options.css, showcase/css/main.css, showcase/css/dashboard.css, shared/fsb-ui-core.css</read_first>
  <action>
Retouch the control panel and showcase dashboard repeated primitives to consume the shared baseline. Focus on cards, buttons, header controls, chips/badges, input/select chrome, and repeated panel surfaces. Avoid heavier layout composition changes that belong to Phase 142.
  </action>
  <acceptance_criteria>
- Control panel and dashboard repeated primitives use the shared token layer
- Cards, badges, buttons, and inputs feel consistent with the popup/sidepanel baseline
- Obvious cross-surface visual drift is reduced without changing page information architecture
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Read modified CSS and confirm shared token usage (`var(--fsb-...)`) appears on repeated primitives
- Spot-check that popup/sidepanel and control-panel/dashboard selectors now align in button, card, badge, input, and status styling
</verification>

<success_criteria>
- Repeated audited primitives now feel like one product system
- Shared primitives no longer show obvious drift in default states
- The milestone's later phases can focus on surface-specific polish instead of baseline inconsistencies
</success_criteria>
