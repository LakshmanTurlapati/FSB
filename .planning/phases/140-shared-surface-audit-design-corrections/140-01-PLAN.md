---
phase: 140-shared-surface-audit-design-corrections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/fsb-ui-core.css
  - ui/sidepanel.html
  - ui/popup.html
  - ui/control_panel.html
  - showcase/dashboard.html
autonomous: true
requirements: [COH-01, COH-02, COH-03]

must_haves:
  truths:
    - "Popup, sidepanel, control panel, and showcase dashboard all load a shared FSB UI baseline stylesheet"
    - "The shared baseline defines one token set for FSB orange accents, neutrals, radii, spacing, and focus states without changing the product's aesthetic direction"
    - "Shared button, card, input, badge, and status treatments can be consumed by audited surfaces from the same baseline file"
  artifacts:
    - path: "shared/fsb-ui-core.css"
      provides: "Shared FSB UI tokens and baseline treatments for repeated primitives"
      contains: "--fsb-primary"
    - path: "ui/sidepanel.html"
      provides: "Shared baseline wiring for the sidepanel surface"
      contains: "shared/fsb-ui-core.css"
    - path: "ui/popup.html"
      provides: "Shared baseline wiring for the popup surface"
      contains: "shared/fsb-ui-core.css"
    - path: "ui/control_panel.html"
      provides: "Shared baseline wiring for the control panel surface"
      contains: "shared/fsb-ui-core.css"
    - path: "showcase/dashboard.html"
      provides: "Shared baseline wiring for the showcase dashboard surface"
      contains: "../shared/fsb-ui-core.css"
  key_links:
    - from: "ui/sidepanel.html"
      to: "shared/fsb-ui-core.css"
      via: "Shared stylesheet link in document head"
      pattern: "fsb-ui-core.css"
    - from: "ui/popup.html"
      to: "shared/fsb-ui-core.css"
      via: "Shared stylesheet link in document head"
      pattern: "fsb-ui-core.css"
    - from: "ui/control_panel.html"
      to: "shared/fsb-ui-core.css"
      via: "Shared stylesheet link in document head"
      pattern: "fsb-ui-core.css"
    - from: "showcase/dashboard.html"
      to: "shared/fsb-ui-core.css"
      via: "Shared stylesheet link in document head"
      pattern: "fsb-ui-core.css"
---

<objective>
Create the shared FSB UI baseline used by the v0.9.21 retouch milestone. Land one shared stylesheet that captures the common token system and common primitive treatments, then wire all audited UI entrypoints to load it.

Purpose: remove token drift and make later surface-specific phases build on the same visual foundation instead of duplicating fixes.

Output: a new shared stylesheet plus audited HTML surfaces loading it.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-shared-surface-audit-design-corrections/140-CONTEXT.md
@.planning/phases/140-shared-surface-audit-design-corrections/140-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared FSB UI core stylesheet</name>
  <files>shared/fsb-ui-core.css</files>
  <read_first>ui/sidepanel.css, ui/popup.css, ui/options.css, showcase/css/main.css, showcase/css/dashboard.css</read_first>
  <action>
Create `shared/fsb-ui-core.css` as the milestone baseline. It must:
- Define a shared FSB token layer for orange accent, neutrals, surface elevation, radii, spacing, focus ring, and semantic status colors
- Preserve the current FSB aesthetic instead of introducing a new visual language
- Provide reusable baseline treatments for repeated primitives across audited surfaces: icon buttons, header buttons, primary/outline buttons, cards, chips/badges, inputs/selects, and status indicators
- Be safe to load in both extension HTML pages and showcase pages
  </action>
  <acceptance_criteria>
- `shared/fsb-ui-core.css` exists
- The file defines shared tokens including `--fsb-primary`, surface colors, radii, and focus-ring values
- The file includes reusable treatments for buttons, cards, inputs, chips, and status indicators
- The file does not replace page-specific layout rules
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire audited UI entrypoints to the shared baseline</name>
  <files>ui/sidepanel.html, ui/popup.html, ui/control_panel.html, showcase/dashboard.html</files>
  <read_first>ui/sidepanel.html, ui/popup.html, ui/control_panel.html, showcase/dashboard.html, shared/fsb-ui-core.css</read_first>
  <action>
Update each audited HTML entrypoint to load the shared stylesheet from the correct relative path without disturbing existing page-specific CSS. Keep existing page assets intact; only add the shared baseline import in the right place so later per-surface work inherits it consistently.
  </action>
  <acceptance_criteria>
- `ui/sidepanel.html` loads `../shared/fsb-ui-core.css`
- `ui/popup.html` loads `../shared/fsb-ui-core.css`
- `ui/control_panel.html` loads `../shared/fsb-ui-core.css`
- `showcase/dashboard.html` loads `../shared/fsb-ui-core.css`
- Existing page-specific CSS files remain loaded after the shared baseline
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Read each updated HTML file and confirm the shared stylesheet link is present with the correct relative path
- Read `shared/fsb-ui-core.css` and confirm token and primitive sections exist
</verification>

<success_criteria>
- All audited surfaces now share one baseline stylesheet
- The baseline clearly encodes the phase's shared retouch direction
- Later phases can build on the shared token/primitive layer instead of reintroducing drift
</success_criteria>
