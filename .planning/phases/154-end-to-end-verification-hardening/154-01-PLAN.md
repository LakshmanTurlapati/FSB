---
phase: 154-end-to-end-verification-hardening
plan: 01
type: execute
wave: 1
depends_on: [151-01, 151-02, 152-01, 152-02, 153-01, 153-02]
files_modified:
  - .planning/phases/154-end-to-end-verification-hardening/154-LIVE-CHECKLIST.md
  - background.js
  - ws/ws-client.js
  - showcase/js/dashboard.js
  - content/dom-stream.js
autonomous: true
requirements: [VER-02]

must_haves:
  truths:
    - "The complete dashboard path is verified end to end against a live extension, relay, website dashboard, and browser tab."
    - "Any gap found during live verification is either fixed immediately in code or documented as a residual risk with reproduction steps."
    - "The final milestone claim is based on observed behavior, not only syntax checks or code inspection."
  artifacts:
    - path: ".planning/phases/154-end-to-end-verification-hardening/154-LIVE-CHECKLIST.md"
      provides: "Executable live verification matrix and evidence log"
      contains: "Stream start"
    - path: "background.js"
      provides: "Potential final hardening fixes from live verification"
      contains: "taskRunId"
    - path: "showcase/js/dashboard.js"
      provides: "Potential final hardening fixes from live verification"
      contains: "requestPreviewResync"
  key_links:
    - from: "showcase/js/dashboard.js"
      to: "ws/ws-client.js"
      via: "dash:* control and recovery messages"
      pattern: "dash:request-status|dash:dom-stream-start|dash:remote-click|dash:task-submit"
    - from: "ws/ws-client.js"
      to: "background.js"
      via: "reconnect snapshot, remote control, and task relay"
      pattern: "ext:snapshot|handleRemoteClick|startDashboardTask"
    - from: "background.js"
      to: "content/dom-stream.js"
      via: "DOM stream start/ready/mutation overlay flow"
      pattern: "domStreamStart|domStreamReady|domStreamRequestOverlay"
---

<objective>
Run the final end-to-end verification pass for the dashboard sync path and close any last reliability gaps found in real browser usage.

Purpose: Phases 150-153 hardened transport, DOM streaming, remote control, and task relay at the code level. Phase 154 is where those claims must be proven against a live extension, relay, and browser session.
Output: A completed live verification checklist, any final hardening fixes required by that pass, and a verification report grounded in observed behavior.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/150-dashboard-transport-baseline-recovery/150-VERIFICATION.md
@.planning/phases/151-dom-stream-consistency-and-state-sync/151-VERIFICATION.md
@.planning/phases/152-remote-control-reliability/152-VERIFICATION.md
@.planning/phases/153-dashboard-task-relay-correctness/153-VERIFICATION.md
@.planning/phases/154-end-to-end-verification-hardening/154-LIVE-CHECKLIST.md
@background.js
@ws/ws-client.js
@showcase/js/dashboard.js
@content/dom-stream.js
@server/src/ws/handler.js

<interfaces>
From `showcase/js/dashboard.js`:
```javascript
sendDashboardWSMessage(type, payload);
setRemoteControl(on);
submitTask(text);
```

From `ws/ws-client.js`:
```javascript
_sendStateSnapshot(snapshotSource);
_handleDashboardStreamStart(payload);
_handleDashboardTask(payload);
_handleStopTask();
```

From `background.js`:
```javascript
_sendStreamState(status, reason, details);
broadcastDashboardProgress(session);
broadcastDashboardComplete(result);
handleRemoteClick(payload);
handleRemoteKey(payload);
handleRemoteScroll(payload);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture automated/static baseline evidence before live verification</name>
  <files>.planning/phases/154-end-to-end-verification-hardening/154-LIVE-CHECKLIST.md</files>
  <read_first>package.json, tests/, background.js, ws/ws-client.js, showcase/js/dashboard.js, content/dom-stream.js</read_first>
  <action>
Record the automated verification evidence that can be produced in the repo-local environment before touching the live browser stack.

1. Run the repo test suite (`npm test`) and syntax checks for the key dashboard sync files.
2. Record those results in the live checklist as baseline evidence rather than pretending they are full end-to-end coverage.
3. Confirm that the latest phase verification artifacts for 150-153 exist and point to the code-level guarantees being relied upon.
4. Keep the distinction explicit: automated/static evidence is supporting evidence, not substitute evidence for live dashboard verification.
  </action>
  <acceptance_criteria>
- `154-LIVE-CHECKLIST.md` records the automated checks run and their pass/fail status.
- `npm test` and the key `node --check` commands have been executed in this environment.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    - Automated baseline evidence is captured.
    - The live checklist clearly distinguishes automated checks from live browser validation.
  </done>
</task>

<task type="manual">
  <name>Task 2: Execute live dashboard verification matrix</name>
  <files>.planning/phases/154-end-to-end-verification-hardening/154-LIVE-CHECKLIST.md</files>
  <read_first>.planning/phases/154-end-to-end-verification-hardening/154-LIVE-CHECKLIST.md</read_first>
  <action>
Run the live end-to-end verification matrix against a real extension + relay + browser setup.

1. Verify stream start, reconnect, tab switch, and explicit not-ready transitions.
2. Verify remote click, type, scroll, repeated remote toggle, and debugger retargeting across tab changes.
3. Verify task submit, initial running state, throttled progress updates, stop, success, failure, and reconnect recovery for the same `taskRunId`.
4. Inspect dashboard, extension, and relay diagnostics during at least one reconnect and one failure scenario.
5. Record observed results and evidence directly in the checklist.
  </action>
  <acceptance_criteria>
- Every checklist item is marked PASS, FAIL, or BLOCKED with notes.
- At least one reconnect and one terminal task outcome have concrete runtime evidence captured.
  </acceptance_criteria>
  <verify>
    <manual>Requires a live website dashboard, relay server, loaded extension, and streamable browser tab. Cannot be completed from static repo analysis alone.</manual>
  </verify>
  <done>
    - Live dashboard behavior is observed and recorded rather than assumed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply final hardening fixes or document residual risks</name>
  <files>background.js, ws/ws-client.js, showcase/js/dashboard.js, content/dom-stream.js, .planning/phases/154-end-to-end-verification-hardening/154-LIVE-CHECKLIST.md</files>
  <read_first>Live verification notes from Task 2</read_first>
  <action>
If the live pass reveals gaps, fix them immediately when they are in scope. If they cannot be fixed in the current pass, document them precisely.

1. Patch any last-mile issues found during live verification.
2. Re-run only the affected verification cases.
3. If a gap remains, record exact repro conditions, affected requirement(s), and residual risk.
4. Only after the live matrix is complete should the phase summary and verification report be created.
  </action>
  <acceptance_criteria>
- Any discovered gap has either a code fix or a documented residual-risk note.
- No final "phase complete" claim is made without the live matrix being executed.
  </acceptance_criteria>
  <verify>
    <manual>Depends on outcomes of Task 2.</manual>
  </verify>
  <done>
    - Final hardening is grounded in live verification findings.
  </done>
</task>

</tasks>

<verification>
- `npm test`
- `node --check content/dom-stream.js`
- `node --check background.js`
- `node --check ws/ws-client.js`
- `node --check showcase/js/dashboard.js`
- `node --check server/src/ws/handler.js`
- Live checklist execution in `.planning/phases/154-end-to-end-verification-hardening/154-LIVE-CHECKLIST.md`
</verification>

<success_criteria>
- The full dashboard sync path is verified against a live browser session.
- Any final gaps are fixed or documented with concrete repro steps.
- The milestone is not claimed complete on code inspection alone.
</success_criteria>

<output>
After live verification completes, create `.planning/phases/154-end-to-end-verification-hardening/154-01-SUMMARY.md` and `.planning/phases/154-end-to-end-verification-hardening/154-VERIFICATION.md`.
</output>
