---
phase: 205-validation-mcp-usage-docs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "tests/mcp-tool-smoke.test.js"
  - "tests/mcp-lifecycle-smoke.test.js"
  - "tests/mcp-tool-routing-contract.test.js"
  - "tests/mcp-visual-session-contract.test.js"
  - "tests/test-overlay-state.js"
  - "tests/dashboard-runtime-state.test.js"
autonomous: true
requirements:
  - VALID-01
  - VALID-02
must_haves:
  truths:
    - "Automated MCP tests prove explicit visual-session start/progress/end flows, trusted allowlist enforcement, and idempotent cleanup behavior."
    - "Release smoke covers the explicit visual-session tool surface so packaged MCP drift is caught before shipping."
    - "Overlay and preview regressions prove badge rendering, final freeze handling, watchdog degradation/orphan behavior, and stale-message suppression for client-owned sessions."
  artifacts:
    - path: "tests/mcp-tool-smoke.test.js"
      provides: "Packaged MCP smoke coverage for explicit visual-session tools"
      contains: "start_visual_session"
    - path: "tests/mcp-visual-session-contract.test.js"
      provides: "Lifecycle, allowlist, replay, and cleanup contract coverage"
      contains: "end_visual_session"
    - path: "tests/test-overlay-state.js"
      provides: "Overlay normalization and stale-message suppression coverage"
      contains: "sessionToken"
    - path: "tests/dashboard-runtime-state.test.js"
      provides: "Preview badge and frozen-state source/runtime assertions"
      contains: "renderPreviewClientBadge"
  key_links:
    - from: "tests/mcp-tool-smoke.test.js"
      to: "tests/mcp-tool-routing-contract.test.js"
      via: "the packaged smoke surface and the route contract should expose the same public lifecycle tool names"
      pattern: "start_visual_session|end_visual_session|report_progress"
    - from: "tests/mcp-visual-session-contract.test.js"
      to: "tests/test-overlay-state.js"
      via: "token-aware lifecycle behavior and stale-message suppression must agree on `sessionToken` and `version` semantics"
      pattern: "sessionToken|version|clientLabel|lifecycle"
    - from: "tests/test-overlay-state.js"
      to: "tests/dashboard-runtime-state.test.js"
      via: "canonical badge and lifecycle fields should stay intact from overlay state to preview rendering"
      pattern: "clientLabel|result|frozen"
---

<objective>
Lock the final regression and smoke surface around the MCP visual-session lifecycle so the contract is provably stable before the milestone ships documentation.

Purpose: Phase 205 first needs durable automated evidence that the new lifecycle tools, trusted client identity, and live/preview overlay semantics behave exactly as intended. This plan should expand confidence, not product scope.
Output: Stronger route/smoke coverage for explicit lifecycle tools plus final overlay/UI regressions for badge, freeze, watchdog, and stale-state behavior.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/205-validation-mcp-usage-docs/205-CONTEXT.md
@.planning/phases/204-overlay-badge-session-persistence/204-CONTEXT.md
@.planning/phases/203-mcp-visual-session-contract/203-CONTEXT.md
@tests/mcp-tool-smoke.test.js
@tests/mcp-lifecycle-smoke.test.js
@tests/mcp-tool-routing-contract.test.js
@tests/mcp-visual-session-contract.test.js
@tests/test-overlay-state.js
@tests/dashboard-runtime-state.test.js

<interfaces>
The explicit client-owned lifecycle that must be covered:
```javascript
start_visual_session({ client_label, task, tab_id? })
report_progress({ session_token, task?, detail?, progress_percent? })
complete_task({ session_token, summary?, detail? })
partial_task({ session_token, summary, blocker, next_step, reason? })
fail_task({ session_token, reason, detail? })
end_visual_session({ session_token })
```

Key contract truths to lock:
```javascript
// trusted identity
client_label must normalize to the approved allowlist

// stale safety
older { sessionToken, version } updates cannot overwrite a newer owner

// cleanup safety
repeated end/final clear calls must be safe no-ops once the session is gone
```
</interfaces>
</context>

<source_audit>
GOAL: Covered by strengthening the existing MCP lifecycle and overlay/UI regression surfaces instead of introducing a new harness family.
REQ VALID-01: Covered directly by smoke plus contract tests for lifecycle routing, allowlist enforcement, and idempotent cleanup.
REQ VALID-02: Covered directly by overlay-state and preview/runtime tests that assert badge, final freeze, degrade/orphan posture, and stale suppression.
CONTEXT D-01 through D-04: Covered by lifecycle smoke, contract assertions, and canonical overlay/preview tests keyed on structured fields.
</source_audit>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expand packaged MCP smoke and route coverage for the explicit lifecycle tools</name>
  <files>tests/mcp-tool-smoke.test.js, tests/mcp-lifecycle-smoke.test.js, tests/mcp-tool-routing-contract.test.js, tests/mcp-visual-session-contract.test.js</files>
  <read_first>
    - tests/mcp-tool-smoke.test.js
    - tests/mcp-lifecycle-smoke.test.js
    - tests/mcp-tool-routing-contract.test.js
    - tests/mcp-visual-session-contract.test.js
    - .planning/phases/205-validation-mcp-usage-docs/205-CONTEXT.md
  </read_first>
  <behavior>
    - The packaged smoke suite explicitly covers the public visual-session tool surface instead of smoke-testing only `run_task`.
    - Route-contract tests still prove the public lifecycle tool names map to the intended bridge message families.
    - Contract tests prove trusted allowlist enforcement and repeated cleanup/end calls stay idempotent for the same token.
  </behavior>
  <action>Extend `tests/mcp-tool-smoke.test.js` so the packaged MCP smoke surface includes the explicit lifecycle tools and their expected bridge messages. Keep the smoke path compact: it should prove the packaged server exposes and routes the lifecycle surface correctly without turning smoke into a full end-to-end browser simulation. Adjust `tests/mcp-lifecycle-smoke.test.js` only where useful to keep the release smoke flow representative of the lifecycle-enabled runtime surface. In `tests/mcp-tool-routing-contract.test.js`, preserve and tighten the route assertions so the public lifecycle names, aliases, and server-only routing posture remain explicit. In `tests/mcp-visual-session-contract.test.js`, add or refine cases that prove repeated `end_visual_session` and related final-clear behavior are safe no-ops after cleanup while keeping allowlist validation strict.</action>
  <verify>
    <automated>node tests/mcp-tool-smoke.test.js && node tests/mcp-lifecycle-smoke.test.js && node tests/mcp-tool-routing-contract.test.js && node tests/mcp-visual-session-contract.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `rg "start_visual_session|end_visual_session|report_progress|complete_task|partial_task|fail_task" tests/mcp-tool-smoke.test.js tests/mcp-tool-routing-contract.test.js` returns explicit smoke and route-contract coverage for the lifecycle surface.
    - `rg "allowlist|Gemini|idempotent|end_visual_session|clearsAfterMs" tests/mcp-visual-session-contract.test.js` returns trusted-client and cleanup assertions.
    - `node tests/mcp-tool-smoke.test.js && node tests/mcp-lifecycle-smoke.test.js && node tests/mcp-tool-routing-contract.test.js && node tests/mcp-visual-session-contract.test.js` exits 0.
  </acceptance_criteria>
  <done>The packaged MCP smoke and contract suites explicitly prove the visual-session tool surface, allowlist, and cleanup behavior.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tighten overlay and preview regressions for badge, freeze, watchdog, and stale-state behavior</name>
  <files>tests/mcp-visual-session-contract.test.js, tests/test-overlay-state.js, tests/dashboard-runtime-state.test.js</files>
  <read_first>
    - tests/mcp-visual-session-contract.test.js
    - tests/test-overlay-state.js
    - tests/dashboard-runtime-state.test.js
    - .planning/phases/205-validation-mcp-usage-docs/205-CONTEXT.md
    - .planning/phases/204-overlay-badge-session-persistence/204-CONTEXT.md
  </read_first>
  <behavior>
    - Overlay tests prove canonical badge/lifecycle metadata survives running, degraded, final, and cleared transitions for client-owned sessions.
    - Preview/runtime tests prove live and frozen badge rendering stays anchored to structured overlay identity instead of plain text inference.
    - Regression coverage proves stale or older token/version updates cannot clear or overwrite a newer client-owned session.
  </behavior>
  <action>Extend `tests/test-overlay-state.js` with explicit client-owned session cases that cover final/frozen badge state, degradation/recovery posture, and stale clear suppression against a newer token/version owner. Use `tests/dashboard-runtime-state.test.js` to lock the preview badge and frozen-overlay behavior around the structured `ext:dom-overlay` payload fields delivered in Phase 204. Where needed, add or refine `tests/mcp-visual-session-contract.test.js` assertions so the 3200ms final-freeze semantics and the existing 60s degrade / 120s orphan cleanup posture remain intentional, token-scoped, and non-destructive to newer owners.</action>
  <verify>
    <automated>node tests/mcp-visual-session-contract.test.js && node tests/test-overlay-state.js && node tests/dashboard-runtime-state.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `rg "clientLabel|sessionToken|version|result|final" tests/test-overlay-state.js tests/mcp-visual-session-contract.test.js` returns lifecycle and stale-suppression assertions.
    - `rg "renderPreviewClientBadge|dash-preview-progress-badge|dash-preview-frozen-badge|clientLabel" tests/dashboard-runtime-state.test.js` returns preview badge and frozen-state coverage.
    - `node tests/mcp-visual-session-contract.test.js && node tests/test-overlay-state.js && node tests/dashboard-runtime-state.test.js` exits 0.
  </acceptance_criteria>
  <done>The overlay and preview regression suites explicitly prove badge, freeze, watchdog, and stale-message behavior for client-owned visual sessions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public MCP tool names -> bridge message families | Smoke and route tests must prove the explicit lifecycle tools still hit the intended bridge surface. |
| Older token/version updates -> current overlay owner | Regression coverage must prevent stale session updates from clearing or overwriting a newer owner. |
| Packaged release smoke -> operator trust | Smoke should catch lifecycle-surface drift before release rather than leaving it to live debugging. |
| Preview rendering -> operator interpretation | Preview/runtime tests must ensure badge and frozen-state semantics stay aligned with live overlay state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-205-01-01 | Spoofing | Public lifecycle routing | mitigate | Lock explicit tool names and bridge routes in smoke plus route-contract tests. |
| T-205-01-02 | Tampering | Stale cleanup behavior | mitigate | Add token/version-scoped stale suppression and idempotent cleanup assertions. |
| T-205-01-03 | Denial of Service | Release smoke | mitigate | Ensure the smoke suite exercises the lifecycle surface so drift is caught pre-release. |
| T-205-01-04 | Information confusion | Preview and frozen overlay state | mitigate | Keep preview/runtime assertions anchored to structured lifecycle fields and badge markup. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `node tests/mcp-tool-smoke.test.js`
- [ ] `node tests/mcp-lifecycle-smoke.test.js`
- [ ] `node tests/mcp-tool-routing-contract.test.js`
- [ ] `node tests/mcp-visual-session-contract.test.js`
- [ ] `node tests/test-overlay-state.js`
- [ ] `node tests/dashboard-runtime-state.test.js`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- The packaged MCP smoke surface explicitly covers the visual-session lifecycle tools
- Regression suites explicitly prove badge, final freeze, watchdog, and stale-message behavior for client-owned sessions
</success_criteria>

<output>
After completion, create `.planning/phases/205-validation-mcp-usage-docs/205-01-SUMMARY.md`
</output>
