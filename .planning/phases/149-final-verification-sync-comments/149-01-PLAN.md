---
phase: 149-final-verification-sync-comments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - showcase/about.html
autonomous: true
requirements: [AUD-02, AUD-03]

must_haves:
  truths:
    - "Every replica section in about.html has a version-stamped sync comment identifying what it replicates and when it was last synced"
    - "Every replica container has role=img, aria-label describing what it depicts, and aria-hidden=true on internal decorative elements"
    - "No obvious visual discrepancies exist between replicas and the real extension in dark or light theme"
  artifacts:
    - path: "showcase/about.html"
      provides: "All 4 recreation sections with sync comments and accessibility attributes"
      contains: "Replica of:"
  key_links:
    - from: "showcase/about.html"
      to: "ui/sidepanel.html"
      via: "sync comment referencing source file and version"
      pattern: "Replica of: ui/sidepanel.html"
    - from: "showcase/about.html"
      to: "ui/control_panel.html"
      via: "sync comment referencing source file and version"
      pattern: "Replica of: ui/control_panel.html"
---

<objective>
Add version-stamped sync comments and accessibility attributes to all replica sections in about.html, and fix any visual discrepancies found during side-by-side comparison against the real extension.

Purpose: Close out the v0.9.22 Showcase High-Fidelity Replicas milestone by ensuring replicas are stamped for drift detection and meet accessibility standards.
Output: Updated showcase/about.html with sync comments and ARIA attributes on all 4 recreation sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-final-verification-sync-comments/149-CONTEXT.md
@.planning/phases/146-sidepanel-replica/146-01-SUMMARY.md
@.planning/phases/147-control-panel-replica/147-01-SUMMARY.md
@.planning/phases/148-mcp-terminal-examples/148-01-SUMMARY.md

Source files for comparison:
@showcase/about.html
@ui/sidepanel.html
@ui/control_panel.html
@showcase/css/recreations.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sync comments and accessibility attributes to all replica sections</name>
  <files>showcase/about.html</files>
  <read_first>
    - showcase/about.html (full file -- all 4 recreation sections)
    - ui/sidepanel.html (real sidepanel for comparison)
    - ui/control_panel.html (real control panel for comparison)
    - showcase/css/recreations.css (CSS variable definitions for visual comparison)
  </read_first>
  <action>
Edit showcase/about.html to add sync comments and ARIA attributes to all 4 recreation sections. The sidepanel replicas (Recreation 1 line ~159, Recreation 3 line ~444) already have sync comments -- leave those as-is. Add missing sync comments and accessibility attributes as follows:

**Sync comments to ADD (format: &lt;!-- Replica of: {source} | Last synced: v0.9.22 -->):**

1. Recreation 1 (Google Search, line ~83): Add above the rec-google-page div:
   `&lt;!-- Replica of: Google Search SERP (visual approximation) | Last synced: v0.9.22 -->`
   (The sidepanel sync comment at line ~159 already exists -- keep it)

2. Recreation 2 (Dashboard Analytics, line ~235): Add above the rec-dashboard div:
   `&lt;!-- Replica of: ui/control_panel.html (Dashboard tab) | Last synced: v0.9.22 -->`

3. Recreation 3 (Form, line ~396 area): Add above the rec-form-page div (or equivalent form container):
   `&lt;!-- Replica of: Generic form interaction (visual approximation) | Last synced: v0.9.22 -->`
   (The sidepanel sync comment at line ~444 already exists -- keep it)

4. Recreation 4 (MCP Terminals, line ~508): Add above the rec-mcp-grid div:
   `&lt;!-- Replica of: Claude Code terminal (MCP tool_use session) | Last synced: v0.9.22 -->`

**Accessibility attributes to ADD on each recreation's outermost visual container:**

1. Recreation 1: On the `.browser-frame` div (line ~69):
   Add `role="img" aria-label="Recreation of FSB automating a Google search with the sidepanel overlay"`

2. Recreation 2: On the `.browser-frame` div (line ~222):
   Add `role="img" aria-label="Recreation of the FSB control panel dashboard with analytics and session history"`

3. Recreation 3: On the `.browser-frame` div (line ~383):
   Add `role="img" aria-label="Recreation of FSB automating a web form with the sidepanel overlay"`

4. Recreation 4 -- Autopilot terminal: On the first `.rec-mcp-terminal` div (line ~510):
   Add `role="img" aria-label="Recreation of a Claude Code terminal session using FSB autopilot mode"`

5. Recreation 4 -- Manual terminal: On the second `.rec-mcp-terminal` div (line ~538):
   Add `role="img" aria-label="Recreation of a Claude Code terminal session using FSB manual orchestration mode"`

**Add aria-hidden="true" on ALL decorative child elements inside each replica container:**
- All `.browser-dots` containers (4 instances): add `aria-hidden="true"`
- All `.rec-mcp-topbar` containers that hold only visual chrome: add `aria-hidden="true"` to the `.browser-dots` inside them (2 instances in MCP section)

Do NOT change any existing structure, CSS classes, or content. This is metadata-only.
  </action>
  <verify>
    <automated>grep -c 'Replica of:' showcase/about.html | grep -q '^[6-9]' && grep -c 'role="img"' showcase/about.html | grep -q '^[5-9]' && grep -c 'aria-label=' showcase/about.html | grep -q '^[6-9]' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'Replica of:' showcase/about.html returns at least 6 matches (2 existing sidepanel + 4 new)
    - grep 'role="img"' showcase/about.html returns at least 5 matches (one per visual container)
    - grep 'aria-label=' showcase/about.html returns at least 6 matches (5 replica containers + existing nav toggle)
    - grep 'aria-hidden="true"' showcase/about.html returns at least 4 matches (browser-dots decorative elements)
    - No HTML structure or CSS class changes -- diff shows only attribute additions and comment insertions
  </acceptance_criteria>
  <done>All 4 recreation sections have version-stamped sync comments. All 5 visual replica containers have role="img" and aria-label. Decorative browser chrome elements have aria-hidden="true". No structural or visual changes.</done>
</task>

<task type="auto">
  <name>Task 2: Side-by-side visual fidelity comparison and fix discrepancies</name>
  <files>showcase/about.html</files>
  <read_first>
    - ui/sidepanel.html (real sidepanel HTML structure)
    - ui/control_panel.html (real control panel HTML -- read lines 1-100 for dashboard tab structure)
    - showcase/about.html (lines 62-577 -- all replica sections)
    - showcase/css/recreations.css (lines 1-100 -- CSS variable block for dark/light themes)
    - ui/sidepanel.css (lines 1-50 -- header and layout tokens)
    - ui/options.css (lines 1-50 -- dashboard layout tokens)
  </read_first>
  <action>
Compare each replica section in about.html against the real extension HTML and CSS. Look for these specific discrepancy categories:

1. **Missing elements**: Any visible element in the real extension that has no counterpart in the replica
2. **Wrong icons**: Font Awesome class mismatches between replica and real HTML
3. **Wrong text content**: Labels, placeholders, or static text that differs
4. **Structural order**: Elements appearing in wrong order vs the real extension

For each discrepancy found, fix it directly in showcase/about.html. If NO discrepancies are found, document that in the commit message.

Known correct elements (from Phase 146-148 summaries -- do NOT re-verify these):
- Sidepanel: brand-row, pill status, 5 message types, mic/send, model badge, footer
- Dashboard: 8 sidebar items with correct icons, 4 metric cards, cost-breakdown, SVG chart, 3 session cards
- MCP terminals: autopilot run_task and manual multi-tool sequences with correct line structure

Focus comparison on:
- Icon classes (e.g., fa-solid vs fa-regular, fa-circle-nodes vs fa-network-wired)
- Text label accuracy (sidebar nav labels, metric card labels)
- Element count parity (same number of items in sidebar, same number of message types)

If everything checks out with no discrepancies, this task results in zero file changes (metadata-only phase). That is a valid outcome.
  </action>
  <verify>
    <automated>node -e "const h=require('fs').readFileSync('showcase/about.html','utf8'); const checks = [h.includes('rec-sp-header'), h.includes('rec-dashboard'), h.includes('rec-mcp-terminal'), h.includes('role=\"img\"'), h.includes('Replica of:')]; console.log(checks.every(Boolean) ? 'PASS' : 'FAIL: ' + checks.join(','))"</automated>
  </verify>
  <acceptance_criteria>
    - showcase/about.html contains rec-sp-header (sidepanel replica present)
    - showcase/about.html contains rec-dashboard (dashboard replica present)
    - showcase/about.html contains rec-mcp-terminal (MCP terminal replica present)
    - All role="img" and aria-label attributes from Task 1 are still present
    - All sync comments from Task 1 are still present
    - If any fixes were made, they are limited to icon classes, text content, or element additions -- no CSS changes
  </acceptance_criteria>
  <done>Side-by-side comparison completed for all 4 recreation sections. Any discrepancies are fixed or documented as intentional (e.g., proportional scaling). All replicas visually match the real extension structure.</done>
</task>

</tasks>

<verification>
1. `grep -c 'Replica of:' showcase/about.html` returns 6+ (sync comments on all sections)
2. `grep -c 'role="img"' showcase/about.html` returns 5+ (accessibility on all replica containers)
3. `grep 'aria-hidden="true"' showcase/about.html` returns matches for decorative elements
4. `grep 'Last synced: v0.9.22' showcase/about.html` returns 6+ matches (version stamp on all comments)
5. All replica class structures (rec-sp-header, rec-dashboard, rec-mcp-terminal) still present and unchanged
</verification>

<success_criteria>
- Every replica section has a version-stamped sync comment identifying source and version
- Every visual replica container has role="img" and a descriptive aria-label
- Decorative browser chrome has aria-hidden="true"
- No obvious structural discrepancies between replicas and real extension
- Milestone v0.9.22 requirements AUD-02 and AUD-03 are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/149-final-verification-sync-comments/149-01-SUMMARY.md`
</output>
