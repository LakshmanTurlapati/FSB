---
phase: 58-click-and-hold-record
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp-server/src/tools/manual.ts
  - content/actions.js
  - background.js
  - site-guides/media/voice-recorder.js
autonomous: true
requirements:
  - MICRO-02

must_haves:
  truths:
    - "A click_and_hold MCP tool exists that dispatches CDP mousePressed, waits holdMs milliseconds, then dispatches mouseReleased at the same coordinates"
    - "The tool is accessible via MCP manual tools and routes through the full chain: manual.ts -> content/actions.js -> background.js CDP handler"
    - "Voice recorder site guide exists with record button selectors and click-and-hold workflow for 5-second recording"
    - "Site guide registered in background.js under the Media section"
  artifacts:
    - path: "mcp-server/src/tools/manual.ts"
      provides: "click_and_hold MCP tool definition"
      contains: "click_and_hold"
    - path: "content/actions.js"
      provides: "cdpClickAndHold content script tool routing to background.js"
      contains: "cdpClickAndHold"
    - path: "background.js"
      provides: "handleCDPMouseClickAndHold CDP handler and case routing plus voice-recorder.js registration"
      contains: "cdpMouseClickAndHold"
    - path: "site-guides/media/voice-recorder.js"
      provides: "Voice recorder site guide with click-and-hold recording workflow"
      contains: "registerSiteGuide"
  key_links:
    - from: "mcp-server/src/tools/manual.ts"
      to: "content/actions.js"
      via: "execAction maps click_and_hold to cdpClickAndHold FSB verb"
      pattern: "execAction.*click_and_hold.*cdpClickAndHold"
    - from: "content/actions.js"
      to: "background.js"
      via: "chrome.runtime.sendMessage with action cdpMouseClickAndHold"
      pattern: "action.*cdpMouseClickAndHold"
    - from: "background.js"
      to: "background.js"
      via: "case cdpMouseClickAndHold routes to handleCDPMouseClickAndHold"
      pattern: "case 'cdpMouseClickAndHold'"
    - from: "background.js"
      to: "site-guides/media/voice-recorder.js"
      via: "importScripts"
      pattern: "importScripts.*site-guides/media/voice-recorder\\.js"
---

<objective>
Create the click_and_hold CDP tool across the full MCP tool chain (manual.ts, content/actions.js, background.js) and a voice recorder site guide for MICRO-02 edge case validation.

Purpose: MICRO-02 requires pressing and holding a record button for 5 seconds then releasing. No existing tool supports timed click-and-hold at fixed coordinates -- click_at releases immediately and drag moves the cursor. A dedicated click_and_hold tool dispatches CDP mousePressed, waits holdMs, then dispatches mouseReleased at the same position.

Output: click_and_hold tool wired through all three layers, voice-recorder.js site guide registered in background.js
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-click-and-hold-record/58-CONTEXT.md
@.planning/phases/57-volume-slider-precision/57-01-SUMMARY.md
@site-guides/media/video-player.js
@site-guides/music/virtual-piano.js
</context>

<interfaces>
<!-- MCP manual tool registration pattern (manual.ts lines 269-285) -->
server.tool(
  'click_at',
  'Click at specific viewport coordinates using CDP trusted events...',
  {
    x: z.number().describe('X coordinate in viewport CSS pixels'),
    y: z.number().describe('Y coordinate in viewport CSS pixels'),
    shift: z.boolean().optional().describe('Hold Shift key during click (for multi-select)'),
    ctrl: z.boolean().optional().describe('Hold Ctrl key during click'),
    alt: z.boolean().optional().describe('Hold Alt key during click'),
  },
  async ({ x, y, shift, ctrl, alt }) => execAction(bridge, queue, 'click_at', 'cdpClickAt', {
    x, y,
    shiftKey: shift ?? false,
    ctrlKey: ctrl ?? false,
    altKey: alt ?? false,
  }),
);

<!-- Content script CDP tool pattern (content/actions.js lines 5081-5095) -->
tools.cdpClickAt = async (params) => {
  const { x, y, shiftKey, ctrlKey, altKey } = params;
  if (typeof x !== 'number' || typeof y !== 'number') {
    return { success: false, error: 'x and y coordinates required (viewport-relative numbers)' };
  }
  try {
    const result = await chrome.runtime.sendMessage({
      action: 'cdpMouseClick', x, y,
      shiftKey: !!shiftKey, ctrlKey: !!ctrlKey, altKey: !!altKey
    });
    return result || { success: false, error: 'No response from CDP click handler' };
  } catch (e) {
    return { success: false, error: `CDP click failed: ${e.message}` };
  }
};

<!-- Background.js CDP handler pattern (lines 11984-12012) -->
async function handleCDPMouseClick(request, sender, sendResponse) {
  const tabId = sender.tab?.id;
  const { x, y, shiftKey, ctrlKey, altKey } = request;
  // ... validation, modifiers bitmask ...
  await chrome.debugger.attach({ tabId }, '1.3');
  await chrome.debugger.sendCommand({ tabId }, 'Input.dispatchMouseEvent', {
    type: 'mousePressed', x, y, button: 'left', clickCount: 1, modifiers
  });
  await chrome.debugger.sendCommand({ tabId }, 'Input.dispatchMouseEvent', {
    type: 'mouseReleased', x, y, button: 'left', clickCount: 1, modifiers
  });
  await chrome.debugger.detach({ tabId });
}

<!-- Background.js message routing (lines 4938-4940) -->
case 'cdpMouseClick':
  handleCDPMouseClick(request, sender, sendResponse);
  return true;

<!-- Background.js site guide registration (Media section, after Music) -->
// Per-site guides: Media
importScripts('site-guides/media/video-player.js');

<!-- registerSiteGuide pattern -->
registerSiteGuide({
  site: 'SiteName',
  category: 'Category',
  patterns: [/regex/i],
  guidance: `string`,
  selectors: { key: 'css-selector' },
  workflows: { name: ['step1', 'step2'] },
  warnings: ['warning1'],
  toolPreferences: ['tool1', 'tool2']
});
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add click_and_hold CDP tool across MCP chain (manual.ts, actions.js, background.js)</name>
  <files>mcp-server/src/tools/manual.ts, content/actions.js, background.js</files>
  <read_first>
    - mcp-server/src/tools/manual.ts lines 267-322 (CDP coordinate tools section -- click_at, drag, scroll_at patterns)
    - content/actions.js lines 5081-5130 (cdpClickAt, cdpDrag, cdpScrollAt tool definitions)
    - background.js lines 4933-4952 (case routing for cdpMouseClick, cdpMouseDrag, cdpMouseWheel)
    - background.js lines 11984-12063 (handleCDPMouseClick and handleCDPMouseDrag implementations)
  </read_first>
  <action>
Add a `click_and_hold` tool to all three layers following the exact patterns of the existing `click_at` tool. The key difference: instead of immediate mouseReleased after mousePressed, insert a delay of `holdMs` milliseconds between them.

LAYER 1 -- mcp-server/src/tools/manual.ts:
Insert a new server.tool block between the click_at tool (ends at line 285) and the drag tool (starts at line 287). The new tool:

```
server.tool(
  'click_and_hold',
  'Click and hold at specific viewport coordinates for a specified duration using CDP trusted events. Dispatches mousePressed, waits holdMs milliseconds, then dispatches mouseReleased at the same position. Use for record buttons, long-press menus, or any UI that requires sustained mouse press. Coordinates are CSS pixels relative to the browser viewport.',
  {
    x: z.number().describe('X coordinate in viewport CSS pixels'),
    y: z.number().describe('Y coordinate in viewport CSS pixels'),
    holdMs: z.number().default(5000).describe('Duration to hold the mouse button in milliseconds (default 5000 = 5 seconds)'),
  },
  async ({ x, y, holdMs }) => execAction(bridge, queue, 'click_and_hold', 'cdpClickAndHold', { x, y, holdMs }),
);
```

LAYER 2 -- content/actions.js:
Insert a new tools.cdpClickAndHold block after cdpClickAt (after line 5095) and before cdpDrag (line 5097). Follow the exact cdpClickAt pattern:

```
tools.cdpClickAndHold = async (params) => {
  const { x, y, holdMs } = params;
  if (typeof x !== 'number' || typeof y !== 'number') {
    return { success: false, error: 'x and y coordinates required (viewport-relative numbers)' };
  }
  try {
    const result = await chrome.runtime.sendMessage({
      action: 'cdpMouseClickAndHold', x, y,
      holdMs: holdMs || 5000
    });
    return result || { success: false, error: 'No response from CDP click-and-hold handler' };
  } catch (e) {
    return { success: false, error: `CDP click-and-hold failed: ${e.message}` };
  }
};
```

LAYER 3 -- background.js:
Part A: Add case routing after the existing cdpMouseClick case (after line 4940, before cdpMouseDrag on line 4942):

```
case 'cdpMouseClickAndHold':
  handleCDPMouseClickAndHold(request, sender, sendResponse);
  return true; // Will respond asynchronously
```

Part B: Add the handler function after handleCDPMouseClick (after line 12012, before handleCDPMouseDrag on line 12014). Follow handleCDPMouseClick pattern exactly, adding a delay between mousePressed and mouseReleased:

```
/**
 * Handle CDP mouse click-and-hold at specific page coordinates.
 * Uses Input.dispatchMouseEvent for browser-level press-hold-release simulation.
 * mousePressed -> wait holdMs -> mouseReleased at same coordinates.
 * Essential for record buttons, long-press menus, and timed press interactions.
 */
async function handleCDPMouseClickAndHold(request, sender, sendResponse) {
  const tabId = sender.tab?.id;
  const { x, y, holdMs = 5000 } = request;
  if (!tabId || typeof x !== 'number' || typeof y !== 'number') {
    sendResponse({ success: false, error: 'Missing tabId or coordinates' });
    return;
  }
  try {
    await chrome.debugger.attach({ tabId }, '1.3');
    // mousePressed
    await chrome.debugger.sendCommand({ tabId }, 'Input.dispatchMouseEvent', {
      type: 'mousePressed', x, y, button: 'left', clickCount: 1
    });
    // Hold for specified duration
    await new Promise(r => setTimeout(r, holdMs));
    // mouseReleased at same position
    await chrome.debugger.sendCommand({ tabId }, 'Input.dispatchMouseEvent', {
      type: 'mouseReleased', x, y, button: 'left', clickCount: 1
    });
    await chrome.debugger.detach({ tabId });
    sendResponse({ success: true, x, y, holdMs, method: 'cdp_click_and_hold' });
  } catch (e) {
    try { await chrome.debugger.detach({ tabId }); } catch (_) {}
    sendResponse({ success: false, error: e.message });
  }
}
```

After all three files are modified, rebuild the MCP server:
```
cd mcp-server && npm run build
```
  </action>
  <verify>
    <automated>grep -q "click_and_hold" mcp-server/src/tools/manual.ts && grep -q "cdpClickAndHold" content/actions.js && grep -q "cdpMouseClickAndHold" background.js && grep -q "handleCDPMouseClickAndHold" background.js && cd mcp-server && npm run build 2>&1 | tail -1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "click_and_hold" mcp-server/src/tools/manual.ts returns at least 1 match
    - grep "cdpClickAndHold" mcp-server/src/tools/manual.ts returns at least 1 match
    - grep "holdMs" mcp-server/src/tools/manual.ts returns at least 1 match
    - grep "cdpClickAndHold" content/actions.js returns at least 1 match
    - grep "cdpMouseClickAndHold" content/actions.js returns at least 1 match
    - grep "case 'cdpMouseClickAndHold'" background.js returns exactly 1 match
    - grep "handleCDPMouseClickAndHold" background.js returns at least 2 matches (case routing + function definition)
    - grep "mousePressed" in handleCDPMouseClickAndHold confirms press event
    - grep "mouseReleased" in handleCDPMouseClickAndHold confirms release event
    - grep "setTimeout.*holdMs" background.js returns at least 1 match (delay between press and release)
    - MCP server builds successfully (npm run build exits 0)
  </acceptance_criteria>
  <done>click_and_hold MCP tool wired through all three layers: manual.ts defines the tool with x/y/holdMs params, content/actions.js routes cdpClickAndHold to background via cdpMouseClickAndHold message, background.js has case routing and handleCDPMouseClickAndHold handler that does mousePressed -> holdMs delay -> mouseReleased. MCP server builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create voice recorder site guide and register in background.js</name>
  <files>site-guides/media/voice-recorder.js, background.js</files>
  <read_first>
    - site-guides/media/video-player.js (sibling site guide in same directory -- follow identical registerSiteGuide structure)
    - site-guides/music/virtual-piano.js lines 1-30 (file header comment pattern)
    - background.js (Media section importScripts -- add voice-recorder.js after video-player.js)
    - .planning/phases/58-click-and-hold-record/58-CONTEXT.md (target site decisions)
  </read_first>
  <action>
Create site-guides/media/voice-recorder.js following the exact registerSiteGuide pattern from video-player.js.

FILE HEADER COMMENT:
Document that online voice recorders typically have a large record button (often circular, red) that requires click-and-hold or click-to-start/click-to-stop interaction. Two interaction types exist: (1) hold-to-record (mousedown starts, mouseup stops -- use click_and_hold tool), (2) toggle-to-record (click starts, click again stops -- use click_at twice with delay). Primary targets: onlinevoicerecorder.com, vocaroo.com. Free, no-auth required.

SITE CONFIG:
- site: 'Online Voice Recorder'
- category: 'Media'
- patterns: [/online-voice-recorder\.com/i, /onlinevoicerecorder\.com/i, /vocaroo\.com/i, /rev\.com\/voice-recorder/i, /voca\.ro/i]

GUIDANCE string covering:

1. RECORD BUTTON ANATOMY: Online voice recorders typically have a prominent circular record button, often red or with a microphone icon. The button may be an HTML button element, a div with click handler, or a canvas-rendered element. Key attributes: aria-label containing "record", data-action containing "record", class containing "record" or "mic". Some sites use a single button that toggles between record/stop states.

2. SELECTORS BY PLATFORM:
   - onlinevoicerecorder.com: #recordButton, .record-button, button[aria-label*="Record" i], [class*="record"][class*="button"], [class*="rec-btn"], svg[class*="mic"]. The main record interface is typically a large circular button in the center of the page.
   - vocaroo.com: #record-button, button[aria-label*="Record" i], [class*="record"], [data-action="record"], .rec-button. Vocaroo shows a large red button to start recording.
   - Generic fallback: button[aria-label*="Record" i], button[aria-label*="record" i], [role="button"][aria-label*="record" i], [class*="record"][class*="btn"], [class*="record-button"], [class*="rec-btn"], [class*="microphone"], [id*="record"]

3. MICROPHONE PERMISSION: The browser will prompt for microphone access on first recording attempt. The extension cannot auto-grant this permission. If a permissions dialog appears, it must be accepted manually before the recording can start. Some sites show their own permission prompt UI before the browser prompt.

4. INTERACTION TYPE DETECTION: Check the record button behavior:
   - If the button text/aria-label changes to "Stop" or "Recording" after mousedown, it is a hold-to-record button -- use click_and_hold(x, y, 5000) for 5 seconds.
   - If the button text/aria-label changes to "Stop" after a click and stays in "recording" state, it is a toggle-to-record button -- use click_at to start, wait 5 seconds, click_at again to stop.
   - Default assumption: toggle-to-record (more common on web-based recorders).

5. WORKFLOW -- HOLD-TO-RECORD (5 seconds):
   - Navigate to voice recorder page
   - Dismiss cookie/consent popups
   - Locate the record button via get_dom_snapshot
   - Get the record button bounding rect (center coordinates)
   - Use click_and_hold(centerX, centerY, 5000) to press for 5 seconds then release
   - Verify recording occurred: check for playback controls, timer display, or download button appearing

6. WORKFLOW -- TOGGLE-TO-RECORD (5 seconds):
   - Navigate to voice recorder page
   - Dismiss cookie/consent popups
   - Locate the record button via get_dom_snapshot
   - Get the record button bounding rect (center coordinates)
   - Use click_at(centerX, centerY) to start recording
   - Wait 5 seconds (the MCP caller should wait between tool invocations)
   - Use click_at(centerX, centerY) again to stop recording (button may now show "Stop")
   - Alternatively, look for a separate stop button and click_at that
   - Verify recording: check for playback controls or audio waveform

7. VERIFICATION AFTER RECORDING:
   - Look for playback button (play/pause), audio waveform display, timer showing elapsed time
   - Check for download/save button appearing after recording stops
   - Selectors: [class*="play"], [aria-label*="Play" i], [class*="waveform"], [class*="timer"], [class*="download"], [class*="save"], audio[src]
   - If timer shows >= 4 seconds (allowing for startup delay), recording succeeded

8. COOKIE/CONSENT HANDLING: Dismiss cookie banners before interacting. Common selectors: #onetrust-accept-btn-handler, [class*="cookie"] button[class*="accept"], [class*="consent"] button, button[class*="agree"].

SELECTORS object:
- recordButton: '#recordButton, #record-button, .record-button, .rec-btn, button[aria-label*="Record" i], [role="button"][aria-label*="record" i], [class*="record"][class*="btn"], [class*="record-button"], [data-action="record"], [id*="record"]'
- stopButton: '#stopButton, #stop-button, .stop-button, button[aria-label*="Stop" i], [class*="stop"][class*="btn"], [data-action="stop"]'
- playbackButton: '.play-button, button[aria-label*="Play" i], [class*="play"][class*="btn"], [data-action="play"]'
- timer: '.timer, .recording-timer, [class*="timer"], [class*="duration"], [class*="elapsed"]'
- waveform: '.waveform, [class*="waveform"], [class*="audio-wave"], canvas[class*="wave"]'
- downloadButton: '.download-button, button[aria-label*="Download" i], [class*="download"], a[download], [class*="save"]'
- cookieConsent: '#onetrust-accept-btn-handler, [class*="cookie"] button[class*="accept"], [class*="consent"] button'

WORKFLOWS object:
- holdToRecord: 6 steps -- navigate to voice recorder page, dismiss cookie/consent popups, locate record button via get_dom_snapshot, calculate button center (centerX = left + width/2, centerY = top + height/2), click_and_hold(centerX, centerY, 5000) for 5-second recording, verify recording via playback controls or timer >= 4 seconds
- toggleToRecord: 8 steps -- navigate to voice recorder page, dismiss cookie/consent popups, locate record button via get_dom_snapshot, calculate button center, click_at(centerX, centerY) to start recording, wait 5 seconds, click_at(centerX, centerY) or click stop button to end recording, verify recording via playback controls or timer
- fullRecordTest: 10 steps -- navigate to target voice recorder, dismiss popups, use get_dom_snapshot to identify record button and its type (hold vs toggle), read button bounding rect, attempt holdToRecord with click_and_hold(centerX, centerY, 5000), verify recording by checking for playback controls or timer, if hold method failed try toggleToRecord method, verify again, check for download/save option, report outcome

WARNINGS array (7 items):
1. Microphone permission must be granted before recording can start -- browser will show a permission dialog that cannot be automated
2. Most web voice recorders use toggle-to-record (click start, click stop), not hold-to-record -- try toggle method first if hold method fails
3. Some sites require HTTPS for microphone access -- ensure the URL uses https://
4. The record button may change appearance/label between record and stop states -- re-snapshot DOM after starting recording to find the stop button
5. Audio recording may have a 0.5-1 second startup delay -- a 5-second hold may produce ~4-4.5 seconds of audio
6. Cookie/consent popups can block the record button -- always dismiss popups first
7. Some sites show an interstitial "allow microphone" UI before the browser's native permission dialog -- dismiss this first

TOOL PREFERENCES: ['click_and_hold', 'click_at', 'click', 'navigate', 'get_dom_snapshot', 'read_page', 'waitForDOMStable', 'hover']

Then register the site guide in background.js by adding an importScripts entry after the existing video-player.js line in the Media section:

importScripts('site-guides/media/voice-recorder.js');
  </action>
  <verify>
    <automated>grep -c "registerSiteGuide" site-guides/media/voice-recorder.js | grep -q "1" && grep -q "vocaroo\\.com" site-guides/media/voice-recorder.js && grep -q "holdToRecord" site-guides/media/voice-recorder.js && grep -q "toggleToRecord" site-guides/media/voice-recorder.js && grep -q "click_and_hold" site-guides/media/voice-recorder.js && grep -q "site-guides/media/voice-recorder.js" background.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "registerSiteGuide" site-guides/media/voice-recorder.js returns exactly 1 match
    - grep "vocaroo\\.com" site-guides/media/voice-recorder.js returns at least 1 match
    - grep "onlinevoicerecorder\\.com\\|online-voice-recorder\\.com" site-guides/media/voice-recorder.js returns at least 1 match
    - grep "holdToRecord" site-guides/media/voice-recorder.js returns at least 1 match
    - grep "toggleToRecord" site-guides/media/voice-recorder.js returns at least 1 match
    - grep "fullRecordTest" site-guides/media/voice-recorder.js returns at least 1 match
    - grep "click_and_hold" site-guides/media/voice-recorder.js returns at least 1 match
    - grep "5000" site-guides/media/voice-recorder.js returns at least 1 match
    - grep "recordButton" site-guides/media/voice-recorder.js returns at least 1 match
    - grep "warnings" site-guides/media/voice-recorder.js returns at least 1 match
    - grep "site-guides/media/voice-recorder.js" background.js returns exactly 1 match
  </acceptance_criteria>
  <done>Voice recorder site guide created with registerSiteGuide call containing vocaroo.com/onlinevoicerecorder.com patterns, record button selectors, holdToRecord (click_and_hold 5s) and toggleToRecord (click_at start/stop) workflows, microphone permission warnings, playback verification selectors, and tool preferences including click_and_hold. Site guide registered in background.js Media section after video-player.js.</done>
</task>

</tasks>

<verification>
1. mcp-server/src/tools/manual.ts contains click_and_hold tool with x, y, holdMs parameters
2. content/actions.js contains cdpClickAndHold tool that sends cdpMouseClickAndHold to background.js
3. background.js contains case 'cdpMouseClickAndHold' routing and handleCDPMouseClickAndHold function
4. handleCDPMouseClickAndHold does mousePressed -> holdMs delay -> mouseReleased at same coordinates
5. MCP server builds successfully (npm run build)
6. site-guides/media/voice-recorder.js exists with registerSiteGuide, two workflows, selectors
7. background.js contains importScripts('site-guides/media/voice-recorder.js') in Media section
8. No syntax errors in JS files (node -c checks pass)
</verification>

<success_criteria>
- click_and_hold MCP tool available with CDP mousePressed -> holdMs delay -> mouseReleased pattern
- Tool chain complete: manual.ts -> content/actions.js -> background.js handler
- Voice recorder site guide created with holdToRecord and toggleToRecord workflows
- Site guide registered in background.js Media section
- MCP server builds cleanly
- Ready for Plan 02 live MCP testing
</success_criteria>

<output>
After completion, create `.planning/phases/58-click-and-hold-record/58-01-SUMMARY.md`
</output>
