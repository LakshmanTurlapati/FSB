---
phase: 04-content-script-modularization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/init.js
  - content/utils.js
  - content/dom-state.js
  - content/selectors.js
  - content/visual-feedback.js
  - content/accessibility.js
  - content/actions.js
  - content/dom-analysis.js
  - content/messaging.js
  - content/lifecycle.js
  - background.js
autonomous: true

must_haves:
  truths:
    - "Every module registers itself on FSB._modules with name and loaded timestamp"
    - "FSB._modules is initialized in init.js and accessible from any module"
    - "Module load failures set a red badge on the extension icon via chrome.runtime.sendMessage"
    - "background.js listens for contentScriptError messages and sets badge accordingly"
  artifacts:
    - path: "content/init.js"
      provides: "FSB._modules initialization"
      contains: "FSB._modules"
    - path: "content/lifecycle.js"
      provides: "Module load status reporting"
      contains: "FSB._modules"
    - path: "background.js"
      provides: "Badge error indicator on module load failure"
      contains: "contentScriptError"
  key_links:
    - from: "content/init.js"
      to: "content/*.js (all modules)"
      via: "FSB._modules object shared across all modules"
      pattern: "FSB\\._modules"
    - from: "content/*.js"
      to: "background.js"
      via: "chrome.runtime.sendMessage contentScriptError"
      pattern: "contentScriptError"
---

<objective>
Add FSB._modules tracking to all 10 content modules and extension badge error indicator.

Purpose: The modularization is complete (10 files in content/, CONTENT_SCRIPT_FILES constant in background.js, old content.js deleted). This plan adds the debugging infrastructure that was part of the user's decisions: FSB._modules property to track loaded modules and their status, plus badge indicator for load failures. These are the only missing implementation items before verification.

Output: All 10 module files register on FSB._modules. background.js sets a red badge when contentScriptError is received.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-script-modularization/04-CONTEXT.md
@.planning/phases/04-content-script-modularization/04-RESEARCH.md
@content/init.js
@content/utils.js
@content/dom-state.js
@content/selectors.js
@content/visual-feedback.js
@content/accessibility.js
@content/actions.js
@content/dom-analysis.js
@content/messaging.js
@content/lifecycle.js
@background.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FSB._modules tracking to init.js and register in all 10 modules</name>
  <files>
    content/init.js
    content/utils.js
    content/dom-state.js
    content/selectors.js
    content/visual-feedback.js
    content/accessibility.js
    content/actions.js
    content/dom-analysis.js
    content/messaging.js
    content/lifecycle.js
  </files>
  <action>
1. In content/init.js, inside the `else` block (the "first load" branch), add after `window.FSB = {};`:
   ```javascript
   // Module tracking for debugging injection issues
   window.FSB._modules = {};
   ```

2. For each of the remaining 9 module files, add a registration line at the END of the IIFE (just before the closing `})();`), INSIDE the skip guard check (so it only runs on first load). The registration line is:
   ```javascript
   window.FSB._modules['MODULE_NAME'] = { loaded: true, timestamp: Date.now() };
   ```
   Where MODULE_NAME matches the file name without .js:
   - content/utils.js -> `'utils'`
   - content/dom-state.js -> `'dom-state'`
   - content/selectors.js -> `'selectors'`
   - content/visual-feedback.js -> `'visual-feedback'`
   - content/accessibility.js -> `'accessibility'`
   - content/actions.js -> `'actions'`
   - content/dom-analysis.js -> `'dom-analysis'`
   - content/messaging.js -> `'messaging'`
   - content/lifecycle.js -> `'lifecycle'`

3. Also register init.js itself. At the very end of init.js's else block (just before the closing brace of the else), add:
   ```javascript
   window.FSB._modules['init'] = { loaded: true, timestamp: Date.now() };
   ```

4. Do NOT change any existing functionality. Only add the registration lines. Keep them as the last statement inside each module's IIFE body.
  </action>
  <verify>
Run: `grep -c "FSB._modules" content/*.js` -- each of the 10 files should show at least 1 match. Run: `grep "FSB._modules\[" content/*.js` to verify the exact registration line format in each file. Verify init.js initializes: `grep "FSB._modules = {}" content/init.js` should return 1 match. Run `node --check` on all 10 files to confirm no syntax errors introduced.
  </verify>
  <done>All 10 content module files register on FSB._modules with name and timestamp. FSB._modules is initialized in init.js. No existing functionality changed.</done>
</task>

<task type="auto">
  <name>Task 2: Add extension badge error indicator in background.js for module load failures</name>
  <files>background.js</files>
  <action>
1. Find the existing `case 'contentScriptError':` handler in background.js (around line 3908). It already contains `automationLogger.logInit(...)` and `sendResponse({ success: true })`. Do NOT remove or replace any existing lines in this handler.

2. Add two badge-setting lines BEFORE the existing `sendResponse({ success: true });` call inside the `contentScriptError` case. Insert these two lines only:
   ```javascript
   chrome.action.setBadgeBackgroundColor({ color: '#FF0000' });
   chrome.action.setBadgeText({ text: '!' });
   ```
   Do NOT add an `automationLogger.error(...)` call -- the existing `automationLogger.logInit('content_script', 'failed', ...)` call already captures the error details. Adding another logger call would create duplicate logging.

3. Find the existing `case 'contentScriptReady':` handler in background.js (around line 3841). Inside the `if (frameId === 0)` block, add one line to clear the error badge when content scripts load successfully:
   ```javascript
   chrome.action.setBadgeText({ text: '' });
   ```
   Add this line AFTER the existing `contentScriptReadyStatus.set(...)` call. This ensures the badge self-heals on successful reload.

4. Do NOT modify any other behavior in background.js. Only add badge set/clear logic. Preserve all existing lines including logInit, sendResponse, debugLog, and contentScriptReadyStatus calls.
  </action>
  <verify>
Search: `grep -n "setBadgeText" background.js` should show at least 2 matches (one for error '!', one for clear ''). Search: `grep -n "contentScriptError" background.js` should show the handler. Search: `grep -n "setBadgeBackgroundColor" background.js` should show the red color setting. Run `node --check background.js` to verify no syntax errors.
  </verify>
  <done>background.js sets a red "!" badge when contentScriptError is received and clears it when contentScriptReady is received. Module load failures are visually indicated on the extension icon.</done>
</task>

</tasks>

<verification>
1. `grep -c "FSB._modules" content/*.js` -- all 10 files show 1+ matches
2. `grep "FSB._modules = {}" content/init.js` -- initialization exists
3. `node --check content/*.js background.js` -- all pass syntax check
4. `grep "setBadgeText" background.js` -- badge set and clear present
5. No existing functionality changed in any file
</verification>

<success_criteria>
- FSB._modules is initialized in init.js and every module registers itself
- background.js sets red badge on contentScriptError, clears on contentScriptReady
- All files pass syntax validation
- No existing behavior altered
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-script-modularization/04-01-SUMMARY.md`
</output>
