---
phase: 04-content-script-modularization
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false

must_haves:
  truths:
    - "Loading the extension on any page produces zero console errors from content script files"
    - "Running a full automation task (search, click, type, navigate) works identically to before the split"
    - "Re-injecting content scripts on the same tab does not create duplicate message handlers, observers, or overlays"
    - "All injection points in background.js use the CONTENT_SCRIPT_FILES constant"
    - "FSB._modules shows all 10 modules as loaded in the page console"
    - "content.js monolith file no longer exists"
  artifacts:
    - path: "content/init.js"
      provides: "FSB namespace bootstrap with _modules tracking"
      contains: "FSB._modules"
    - path: "content/actions.js"
      provides: "All 25+ browser action tool functions"
      contains: "FSB.tools"
    - path: "content/messaging.js"
      provides: "Message handler registration"
      contains: "onMessage.addListener"
    - path: "background.js"
      provides: "CONTENT_SCRIPT_FILES constant used at all injection points"
      contains: "CONTENT_SCRIPT_FILES"
  key_links:
    - from: "background.js CONTENT_SCRIPT_FILES"
      to: "content/init.js through content/lifecycle.js"
      via: "chrome.scripting.executeScript files array"
      pattern: "files: CONTENT_SCRIPT_FILES"
---

<objective>
Verify the complete modularization works end-to-end and update project documentation.

Purpose: All code changes are done (10 modules extracted, background.js wired, old content.js deleted, FSB._modules tracking added). This plan runs the final verification checkpoint and updates the roadmap/requirements to reflect Phase 4 completion.

Output: Human-verified working extension. Updated ROADMAP.md and REQUIREMENTS.md.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-content-script-modularization/04-01-SUMMARY.md
@background.js (lines 110-130 for CONTENT_SCRIPT_FILES)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete content.js modularization into 10 modules under content/ directory:
    - init.js (namespace, re-injection guard, error handlers)
    - utils.js (shared helpers)
    - dom-state.js (DOMStateManager, ElementCache, RefMap)
    - selectors.js (generateSelector, selector generation)
    - visual-feedback.js (HighlightManager, overlays, Shadow DOM)
    - accessibility.js (ARIA roles, element readiness checks)
    - actions.js (25+ browser action tools)
    - dom-analysis.js (DOM serialization, page context detection)
    - messaging.js (message handlers, chrome.runtime.onMessage)
    - lifecycle.js (MutationObserver, SPA detection, background port)

    background.js uses CONTENT_SCRIPT_FILES constant at all injection points.
    FSB._modules tracks loaded modules for debugging.
    Extension badge shows red "!" on module load failure.
  </what-built>
  <how-to-verify>
    1. Open chrome://extensions and reload the FSB extension
    2. Click "service worker" link -- verify no errors in the service worker console
    3. Open any webpage (e.g., google.com)
    4. Open DevTools console on that page
    5. Type `window.FSB._modules` and press Enter -- should show an object with 10 modules (init, utils, dom-state, selectors, visual-feedback, accessibility, actions, dom-analysis, messaging, lifecycle), each with `loaded: true` and a timestamp
    6. Verify no FSB-related errors in the page console
    7. Open the FSB side panel (or popup)
    8. Run a simple automation task (e.g., "search for wireless mouse on Amazon")
    9. Verify: elements get highlighted, actions execute, pages navigate correctly
    10. Navigate to a different page on the same tab -- verify no duplicate overlay or observer errors
    11. Check that the extension icon does NOT show a red "!" badge (indicating no module load errors)
    12. If everything works, type "approved"
    13. If something is broken, describe the error
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe any issues you see</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md and REQUIREMENTS.md to reflect Phase 4 completion</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
1. In .planning/ROADMAP.md:
   - Change `- [ ] **Phase 4: Content Script Modularization**` to `- [x] **Phase 4: Content Script Modularization**`
   - Mark all 5 plan checkboxes as complete: `- [x] 04-01-PLAN.md` through `- [x] 04-02-PLAN.md` (update to reflect the NEW plan structure -- 2 plans, not the old 5)
   - Update the progress table: Phase 4 row should show `2/2` plans complete, status `Complete`, and today's date

2. In .planning/REQUIREMENTS.md:
   - Change MOD-01 status from `[ ]` to `[x]`
   - Change MOD-02 status from `[ ]` to `[x]`
   - Change MOD-03 to `[x]` and note that modules are loaded via programmatic injection (chrome.scripting.executeScript) per user decision, not manifest content_scripts. The requirement text says "manifest.json content_scripts array" but the user explicitly decided on programmatic injection in CONTEXT.md. Update the requirement text to match: "Modules are loaded via chrome.scripting.executeScript in correct dependency order"
   - Update the traceability table: MOD-01, MOD-02, MOD-03 status from `Pending` to `Complete`
  </action>
  <verify>
Verify: `grep "\[x\].*Phase 4" .planning/ROADMAP.md` returns a match. Verify: `grep "\[x\].*MOD-0" .planning/REQUIREMENTS.md` returns 3 matches. Verify: `grep "Complete.*Phase 4\|4.*Complete" .planning/ROADMAP.md` appears in the progress table.
  </verify>
  <done>ROADMAP.md marks Phase 4 as complete with correct plan count. REQUIREMENTS.md marks MOD-01, MOD-02, MOD-03 as complete. Traceability table updated.</done>
</task>

</tasks>

<verification>
1. Human verified: extension loads without errors, FSB._modules shows 10 modules, automation works
2. ROADMAP.md shows Phase 4 complete
3. REQUIREMENTS.md shows MOD-01, MOD-02, MOD-03 complete
4. Phase 4 success criteria all met:
   - content.js no longer exists as a monolith (code in content/ directory)
   - Zero console errors from content script files
   - Automation tasks work identically
   - Re-injection does not create duplicates
   - All injection points use CONTENT_SCRIPT_FILES constant
</verification>

<success_criteria>
- User has verified the extension works end-to-end (checkpoint approved)
- ROADMAP.md marks Phase 4 as complete
- REQUIREMENTS.md marks all MOD requirements as complete
- All 5 Phase 4 success criteria are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-script-modularization/04-02-SUMMARY.md`
</output>
