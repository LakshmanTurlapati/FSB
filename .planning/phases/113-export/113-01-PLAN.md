---
phase: 113-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site-guides/design/excalidraw.js
autonomous: true
requirements:
  - EXPORT-01
  - EXPORT-02
  - EXPORT-03

must_haves:
  truths:
    - "Excalidraw site guide contains an EXPORT section in the guidance string with PNG clipboard, SVG export, and clipboard copy workflows"
    - "Workflow arrays exportPngToClipboard, exportSvg, and copyToClipboard exist in the workflows object"
    - "Export-related warnings are included in the warnings array"
  artifacts:
    - path: "site-guides/design/excalidraw.js"
      provides: "EXPORT section in guidance string and three export workflow arrays"
      contains: "EXPORT"
  key_links:
    - from: "guidance string EXPORT section"
      to: "workflows.exportPngToClipboard"
      via: "documented keyboard shortcut Shift+Alt+C"
      pattern: "exportPngToClipboard"
    - from: "guidance string EXPORT section"
      to: "workflows.exportSvg"
      via: "menu-based SVG export workflow"
      pattern: "exportSvg"
    - from: "guidance string EXPORT section"
      to: "workflows.copyToClipboard"
      via: "Ctrl+C copy shortcut"
      pattern: "copyToClipboard"
---

<objective>
Add an EXPORT section to the Excalidraw site guide covering PNG-to-clipboard, SVG export, and clipboard copy workflows.

Purpose: Completes the export capabilities for Excalidraw mastery so FSB can export user diagrams on request.
Output: Updated excalidraw.js with EXPORT guidance block, three workflow arrays, and export warnings.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-export/113-CONTEXT.md
@site-guides/design/excalidraw.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EXPORT section to Excalidraw site guide</name>
  <files>site-guides/design/excalidraw.js</files>
  <read_first>
    - site-guides/design/excalidraw.js (full file -- understand guidance string structure, workflows object, warnings array, and selectors object)
    - .planning/phases/113-export/113-CONTEXT.md (user decisions on shortcuts vs menu navigation)
  </read_first>
  <action>
    Edit site-guides/design/excalidraw.js to add export capabilities in three places:

    1. GUIDANCE STRING -- Add an EXPORT section after the existing PROPERTY PANELS block (before the closing backtick of the guidance template literal). Follow the exact formatting pattern of existing sections (e.g., CANVAS OPERATIONS, STYLING). Content:

    ```
    EXPORT (EXPORT-01, EXPORT-02, EXPORT-03):

      PNG TO CLIPBOARD (EXPORT-01):
        press_key c shift=true alt=true (Shift+Alt+C) -- copies entire canvas as PNG image to clipboard
        NOTE: This is the fastest and most reliable export method -- zero DOM interaction, no dialogs.
        NOTE: Works on entire canvas content (all elements). Select specific elements first if you want partial export.
        NOTE: After copying, the user can paste (Ctrl+V) into any application that accepts images.

      SVG EXPORT (EXPORT-02):
        Step 1: Open export dialog via hamburger menu -- click the menu button (top-left, [class*="menu"], [aria-label="Menu"]) or find the export option
        Step 2: Click "Export image" or similar option in the dropdown menu
        Step 3: In the export dialog, find format selection -- look for SVG radio/button ([data-testid*="svg"], [aria-label*="SVG"])
        Step 4: Click the SVG format option to switch from PNG to SVG
        Step 5: Click the "Export" or "Download" button to download the SVG file
        Step 6: Press Escape to close the export dialog
        NOTE: SVG export requires menu navigation -- no keyboard shortcut exists for direct SVG download.
        NOTE: The export dialog may also offer options for background color and dark mode.

      CLIPBOARD COPY (EXPORT-03):
        Step 1: Select elements to copy -- press_key(a, ctrl=true) for all, or cdpClickAt on a specific element
        Step 2: press_key c ctrl=true (Ctrl+C) -- copies selected elements to clipboard in Excalidraw format
        NOTE: Ctrl+C copies the Excalidraw element data, not a rendered image. Pasting (Ctrl+V) works within Excalidraw or another Excalidraw instance.
        NOTE: For image clipboard (PNG), use Shift+Alt+C instead (EXPORT-01 above).
    ```

    2. WORKFLOWS OBJECT -- Add three new workflow arrays inside the workflows object, after the existing changeLayerOrder array:

    exportPngToClipboard: [
      'Select elements to export (Ctrl+A for all, or click specific elements) -- unselected elements are still included in full canvas export',
      'Press Shift+Alt+C via press_key(c, shift=true, alt=true) to copy canvas as PNG to clipboard',
      'PNG image is now on clipboard -- user can paste into any image-accepting application'
    ]

    exportSvg: [
      'Click hamburger menu button (top-left area) -- look for [class*="menu"], [aria-label="Menu"], or the three-line icon',
      'Click "Export image" option in dropdown menu',
      'In export dialog, find and click SVG format button -- [data-testid*="svg"], [aria-label*="SVG"], or button with "SVG" text',
      'Click "Export" or "Download" button to save SVG file',
      'Press Escape via press_key to close export dialog'
    ]

    copyToClipboard: [
      'Select elements to copy: press_key(a, ctrl=true) for all, or cdpClickAt on specific elements',
      'Press Ctrl+C via press_key(c, ctrl=true) to copy selected elements in Excalidraw format',
      'Elements are on clipboard -- paste with Ctrl+V in same or different Excalidraw instance'
    ]

    3. SELECTORS OBJECT -- Add export-related selectors after the existing textAlignRight selector:

    exportMenuButton: '[class*="menu"], [aria-label="Menu"], button[class*="hamburger"]'
    exportDialog: '[class*="ExportDialog"], [data-testid*="export"]'
    exportSvgOption: '[data-testid*="svg"], [aria-label*="SVG"]'
    exportDownload: '[data-testid*="export-button"], button[aria-label*="Export"]'

    4. WARNINGS ARRAY -- Add these export-specific warnings to the end of the warnings array:

    'Shift+Alt+C (PNG to clipboard) exports the entire visible canvas -- not just selected elements'
    'SVG export requires navigating the hamburger menu and export dialog -- there is no keyboard-only shortcut for SVG download'
    'Ctrl+C copies Excalidraw element data (not a rendered image) -- use Shift+Alt+C for a PNG image on clipboard'

    5. KEYBOARD SHORTCUTS block in guidance -- Add these lines to the existing shortcuts list (after the Shift+1 line):

    Shift+Alt+C = Copy canvas as PNG to clipboard
    Ctrl+C = Copy selected elements (Excalidraw format)
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Extensions/FSB && node -e "const fs = require('fs'); const c = fs.readFileSync('site-guides/design/excalidraw.js','utf8'); const checks = ['EXPORT (EXPORT-01', 'PNG TO CLIPBOARD', 'SVG EXPORT', 'CLIPBOARD COPY', 'exportPngToClipboard', 'exportSvg', 'copyToClipboard', 'Shift+Alt+C', 'exportMenuButton', 'exportDialog']; const missing = checks.filter(k => !c.includes(k)); if(missing.length){console.error('MISSING:',missing);process.exit(1)}else{console.log('All export content present');}" && node -c site-guides/design/excalidraw.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "EXPORT (EXPORT-01" site-guides/design/excalidraw.js returns a match
    - grep "PNG TO CLIPBOARD" site-guides/design/excalidraw.js returns a match
    - grep "SVG EXPORT" site-guides/design/excalidraw.js returns a match
    - grep "CLIPBOARD COPY" site-guides/design/excalidraw.js returns a match
    - grep "exportPngToClipboard" site-guides/design/excalidraw.js returns a match
    - grep "exportSvg:" site-guides/design/excalidraw.js returns a match
    - grep "copyToClipboard" site-guides/design/excalidraw.js returns a match
    - grep "Shift+Alt+C" site-guides/design/excalidraw.js returns a match
    - grep "exportMenuButton" site-guides/design/excalidraw.js returns a match
    - node -c site-guides/design/excalidraw.js succeeds (valid JavaScript syntax)
  </acceptance_criteria>
  <done>The Excalidraw site guide contains an EXPORT section in the guidance string documenting PNG clipboard (Shift+Alt+C shortcut), SVG export (menu workflow), and clipboard copy (Ctrl+C). Three corresponding workflow arrays exist. Export selectors and warnings are added. File passes JavaScript syntax check.</done>
</task>

</tasks>

<verification>
1. node -c site-guides/design/excalidraw.js -- file is valid JavaScript
2. All three requirement IDs (EXPORT-01, EXPORT-02, EXPORT-03) are referenced in the guidance text
3. Three workflow arrays (exportPngToClipboard, exportSvg, copyToClipboard) exist in workflows object
4. Export selectors added to selectors object
5. Export warnings added to warnings array
6. Keyboard shortcuts section updated with Shift+Alt+C and Ctrl+C
</verification>

<success_criteria>
- Excalidraw site guide has a complete EXPORT section with all three export methods documented
- Keyboard shortcut Shift+Alt+C is the primary PNG clipboard path (no dialog needed)
- SVG export uses menu navigation as documented in CONTEXT.md research
- Ctrl+C clipboard copy for Excalidraw-format element data is documented
- File syntax is valid JavaScript
</success_criteria>

<output>
After completion, create `.planning/phases/113-export/113-01-SUMMARY.md`
</output>
