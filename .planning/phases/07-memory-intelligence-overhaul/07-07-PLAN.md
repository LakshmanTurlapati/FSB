---
phase: 07-memory-intelligence-overhaul
plan: 07
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05", "07-06"]
files_modified:
  - ui/options.html
  - ui/options.js
  - manifest.json
autonomous: false

must_haves:
  truths:
    - "Loading the extension produces zero console errors from memory or options page scripts"
    - "End-to-end flow works: automation session completes -> AI extracts memories -> enrichment runs -> memories appear in tab with detail viewers -> costs tracked -> no manual refresh needed"
    - "The Memory tab is visually clean with no redundant controls, proper alignment, and consistent styling"
    - "All 7 success criteria from the Phase 7 roadmap are satisfied"
  artifacts:
    - path: "manifest.json"
      provides: "cross-site-patterns.js included in background service worker scripts"
      contains: "cross-site-patterns.js"
    - path: "ui/options.js"
      provides: "All memory functions work together without errors"
    - path: "ui/options.html"
      provides: "Memory section properly structured with all Phase 7 elements, includes cross-site-patterns.js script tag"
      contains: "cross-site-patterns.js"
  key_links:
    - from: "background.js"
      to: "lib/memory/memory-manager.js"
      via: "extractAndStoreMemories -> memoryManager.add -> enrichment pipeline"
      pattern: "memoryManager\\.add"
    - from: "lib/memory/memory-manager.js"
      to: "lib/memory/memory-extractor.js"
      via: "enrich() called asynchronously after storage"
      pattern: "_enrichAsync"
    - from: "ui/options.js"
      to: "chrome.storage.onChanged"
      via: "Auto-refresh triggers when memories are modified"
      pattern: "_memoryRefreshTimer"
---

<objective>
Integration testing and cleanup: verify all Phase 7 features work together end-to-end, fix any issues, and ensure production readiness.

Purpose: Plans 07-01 through 07-06 built individual features. This plan verifies they integrate correctly: the AI enrichment pipeline from 07-01 feeds data that the detail panels from 07-03 display, the auto-refresh from 07-05 shows updates live, the cost tracking from 07-06 captures enrichment costs, and the UI from 07-04 presents everything cleanly.

Output: A verified, production-ready Memory Intelligence system. All scripts load without errors, the end-to-end flow works, and the UI is polished.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-memory-intelligence-overhaul/07-RESEARCH.md
@.planning/phases/07-memory-intelligence-overhaul/07-01-PLAN.md
@.planning/phases/07-memory-intelligence-overhaul/07-02-PLAN.md
@.planning/phases/07-memory-intelligence-overhaul/07-03-PLAN.md
@.planning/phases/07-memory-intelligence-overhaul/07-04-PLAN.md
@.planning/phases/07-memory-intelligence-overhaul/07-05-PLAN.md
@.planning/phases/07-memory-intelligence-overhaul/07-06-PLAN.md
@manifest.json
@background.js
@ui/options.js
@ui/options.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify script loading and fix integration issues</name>
  <files>manifest.json, ui/options.html, ui/options.js</files>
  <action>
    1. Verify that `lib/memory/cross-site-patterns.js` is included in the background service worker script loading. Check manifest.json for the background scripts list and add it if missing.

    2. Verify that the options page loads all required memory scripts. Check options.html for script tags. The options page needs access to:
       - memory-schemas.js (for MEMORY_STORAGE_KEY, createBaseMemory, etc.)
       - memory-storage.js (for memoryStorage)
       - memory-retriever.js (for memoryRetriever)
       - memory-manager.js (for memoryManager)
       - cross-site-patterns.js (for groupSitemapsByDomain -- used by UI rendering)
       If any are missing, add the appropriate script tags.
       CRITICAL: cross-site-patterns.js MUST be added as a `<script src="lib/memory/cross-site-patterns.js"></script>` tag in options.html. No plan before 07-07 adds this script tag, so it will be missing. Add it after the other memory script tags.

    3. Check for any undefined reference errors by scanning the code paths:
       - Does `toggleMemoryDetail` reference functions that exist?
       - Does `_smartMemoryRefresh` correctly call `loadMemoryDashboard`?
       - Does `loadMemoryCostPanel` correctly call `analytics.getStatsBySource`?
       - Does `initializeMemorySection` wire up the overflow menu and auto-analyze toggle?
       - Does the auto-analyze toggle correctly read/write `autoAnalyzeMemories`?

    4. Fix any integration issues found:
       - Missing function references
       - Incorrect element IDs
       - Wrong function signatures
       - Missing script imports
       - Race conditions in initialization order

    5. Verify the initialization order in options.js:
       - `initializeMemorySection()` (called on DOMContentLoaded with 400ms delay)
       - This should set up: overflow menu, auto-analyze toggle, search, type filter
       - `loadMemoryDashboard()` (called inside initializeMemorySection)
       - This should: load stats, render list, load cost panel
       - Auto-refresh listener should be active after initialization

    6. Ensure no duplicate event listeners:
       - The onChanged listener should not be attached multiple times
       - The overflow menu click handler should handle re-attachment gracefully
       - IMPORTANT: Verify that the `chrome.storage.onChanged` listener for memory auto-refresh (from plan 07-05) is at MODULE level (top-level scope of options.js), NOT inside `initializeMemorySection()`. Plans 07-04 and 07-05 both modify options.js in Wave 3, so confirm the listener is not accidentally nested inside the initialization function, which would cause it to be re-attached on every call.
  </action>
  <verify>
    - manifest.json includes cross-site-patterns.js in background scripts
    - options.html includes cross-site-patterns.js script tag (added by this task)
    - options.html includes all other necessary memory script tags
    - No undefined reference errors in the integration paths
    - Initialization order is correct
    - chrome.storage.onChanged listener for memory auto-refresh is at module level, not inside initializeMemorySection()
  </verify>
  <done>All scripts load correctly, integration paths are verified, no undefined references, initialization order is correct.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Memory Intelligence Overhaul (Phase 7):
    1. AI enrichment pipeline -- every memory type gets AI analysis on creation (when auto-analyze is enabled)
    2. Cross-site pattern analysis -- consolidation generates common patterns across domains
    3. Read-only detail panels -- clicking any memory shows structured type-specific data
    4. Memory tab UI redesign -- auto-analyze toggle in header, overflow menu for actions, no Refresh button
    5. Auto-refresh -- Memory tab updates live when background processes add/modify memories
    6. Memory cost tracking -- Dashboard shows cost breakdown, Memory tab shows dedicated cost card
  </what-built>
  <how-to-verify>
    1. Load the extension in Chrome (chrome://extensions -> Load unpacked)
    2. Open the options page (click extension icon -> settings, or right-click -> Options)
    3. Navigate to the Memory tab:
       a. Verify the auto-analyze toggle is in the section header (not in Advanced Settings)
       b. Verify NO Refresh button exists
       c. Verify a three-dot overflow menu shows Consolidate, Export, Clear All
       d. Verify a "Memory AI Costs" card appears above the memory list
    4. If there are existing memories, click on one:
       a. Verify a detail panel expands below it with type-specific structured data
       b. Click it again to collapse
       c. Click a different memory to verify accordion behavior (only one open at a time)
    5. Navigate to Dashboard:
       a. Verify the hero section shows a cost breakdown row (Automation / Memory AI / Sitemap)
    6. Check browser console for errors -- should be zero memory-related errors
    7. Toggle the auto-analyze switch off and on -- verify it persists on page reload
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Zero console errors from memory or options page scripts
2. All 7 Phase 7 success criteria verified:
   - SC1: AI analysis for all memory types (check memory.metadata.aiEnriched field)
   - SC2: Domain-grouped sitemaps + cross-site patterns (run Consolidate and check)
   - SC3: Read-only detail panels (click any memory)
   - SC4: Auto-analyze toggle in Memory tab header
   - SC5: Auto-refresh (modify fsb_memories from devtools and check)
   - SC6: Cost panels in Dashboard and Memory tab
   - SC7: Clean layout, no redundant controls
</verification>

<success_criteria>
- Extension loads without errors
- All Phase 7 features work together end-to-end
- Memory tab is visually clean and functional
- User confirms visual verification passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-memory-intelligence-overhaul/07-07-SUMMARY.md`
</output>
