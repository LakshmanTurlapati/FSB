---
phase: 249-skill-md-scripts
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - "skills/FSB Skill/scripts/print-stdio.mjs"
autonomous: true
requirements:
  - SKILL-04
  - SKILL-06
tags: [skill, stdio, openclaw-config, parity, mjs]

must_haves:
  truths:
    - "Running `node scripts/print-stdio.mjs` prints the canonical OpenClaw stdio JSON block to stdout"
    - "The printed block matches the canonical shape from `mcp/src/install.ts` getSetupSections() (Claude Desktop section, lines 170-176): {\"mcpServers\":{\"fsb\":{\"command\":\"npx\",\"args\":[\"-y\",\"fsb-mcp-server\"]}}}"
    - "The script prints a header line BEFORE the JSON block: 'Paste this into your OpenClaw MCP config:'"
    - "The script prints a footer line AFTER the JSON block referencing the install.ts source-of-truth"
    - "Script runs unchanged on macOS, Linux, and Windows (pure stdout writes)"
    - "ZERO emojis; ASCII-only"
  artifacts:
    - path: "skills/FSB Skill/scripts/print-stdio.mjs"
      provides: "Canonical OpenClaw stdio config block printer (parity-locked with mcp/src/install.ts)"
      contains: "mcpServers"
      contains_also:
        - "fsb"
        - "command"
        - "args"
        - "npx"
        - "-y"
        - "fsb-mcp-server"
        - "Paste this"
        - "install.ts"
  key_links:
    - from: "skills/FSB Skill/scripts/print-stdio.mjs"
      to: "mcp/src/install.ts (STDIO_COMMAND constant + Claude Desktop section)"
      via: "parity contract enforced by Phase 251 TEST-03"
      pattern: "install\\.ts"
---

<objective>
Author `skills/FSB Skill/scripts/print-stdio.mjs`: a Node ESM script that prints the canonical OpenClaw stdio config block (the JSON the user pastes into OpenClaw's MCP config). The block content mirrors `mcp/src/install.ts` constants (`STDIO_COMMAND = 'npx -y fsb-mcp-server'` at line 28; the JSON shape at lines 170-176 in the Claude Desktop section).

Purpose: OpenClaw's MCP config schema is unstable across builds (per Phase 248), so the skill PRINTS rather than WRITES the config. This script is the print surface. It also enforces the "single source of truth" pattern: drift between this script and `mcp/src/install.ts` is caught by Phase 251 TEST-03 at CI time.

Output: A new file at `skills/FSB Skill/scripts/print-stdio.mjs`. No build step (no transpilation, no codegen).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/249-skill-md-scripts/249-CONTEXT.md
@./CLAUDE.md
@mcp/src/install.ts

<interfaces>
<!-- Source of truth: mcp/src/install.ts -->

Line 28:
  const STDIO_COMMAND = 'npx -y fsb-mcp-server';

Lines 170-176 (Claude Desktop section in getSetupSections() -- the canonical JSON shape FSB uses for any host that takes a JSON config):
  '  {',
  '    "mcpServers": {',
  '      "fsb": {',
  '        "command": "npx",',
  '        "args": ["-y", "fsb-mcp-server"]',
  '      }',
  '    }',
  '  }',

The CANONICAL block this script prints (must match the parsed JSON exactly):

  {
    "mcpServers": {
      "fsb": {
        "command": "npx",
        "args": ["-y", "fsb-mcp-server"]
      }
    }
  }

Indentation: 2 spaces per level (matches install.ts after stripping the 2-space line prefix used for terminal indentation).
Line endings: \n (LF) only -- Phase 251 TEST-03 reads bytes; CRLF would fail parity.

Header (printed BEFORE the block, on its own line):
  Paste this into your OpenClaw MCP config:

Footer (printed AFTER the block, on its own line):
  If your OpenClaw build documents a different shape, see mcp/src/install.ts getSetupSections() (canonical source of truth).

Top-of-file parity comment (per CONTEXT.md > Specific Ideas):
  // Block parity with mcp/src/install.ts STDIO_COMMAND (line 28) and Claude Desktop section (~lines 170-176)
  // -- enforced at CI time by tests/skill-fsb-spec.test.js (Phase 251 TEST-03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement print-stdio.mjs canonical block printer (SKILL-04)</name>
  <files>skills/FSB Skill/scripts/print-stdio.mjs</files>
  <read_first>
    - .planning/phases/249-skill-md-scripts/249-CONTEXT.md (Implementation Decisions > scripts/print-stdio.mjs)
    - mcp/src/install.ts lines 1-50 (STDIO_COMMAND constant)
    - mcp/src/install.ts lines 165-180 (Claude Desktop section -- canonical JSON shape)
    - skills/FSB Skill/scripts/README.md (placeholder; do NOT modify)
    - ./CLAUDE.md (NO EMOJIS rule)
  </read_first>
  <action>
    Create `skills/FSB Skill/scripts/print-stdio.mjs` using the Write tool. ESM only. The file MUST start with these EXACT lines (4 lines, no blank line between them):

      #!/usr/bin/env node
      // print-stdio.mjs -- prints the canonical OpenClaw stdio MCP config block (SKILL-04 / SKILL-06)
      // Block parity with mcp/src/install.ts STDIO_COMMAND (line 28) and Claude Desktop section (~lines 170-176)
      // -- enforced at CI time by tests/skill-fsb-spec.test.js (Phase 251 TEST-03).

    No imports required (the script does pure stdout writes). If you find any reason to import, restrict to `node:process` only.

    Define a single constant `BLOCK` as a template literal containing EXACTLY these 9 physical lines (each ending with \n, no trailing whitespace, 2-space indentation per level):

      {
        "mcpServers": {
          "fsb": {
            "command": "npx",
            "args": ["-y", "fsb-mcp-server"]
          }
        }
      }

    The first character is `{`, the last character before the trailing newline is `}`. JSON.parse(BLOCK) MUST succeed and round-trip to the equivalent object structure.

    Print sequence (use `process.stdout.write`, NOT `console.log`, to avoid adding spurious trailing whitespace or stripping the parity-critical newlines):

      1. process.stdout.write('Paste this into your OpenClaw MCP config:\n');
      2. process.stdout.write('\n');
      3. process.stdout.write(BLOCK);
      4. process.stdout.write('\n');
      5. process.stdout.write('If your OpenClaw build documents a different shape, see mcp/src/install.ts getSetupSections() (canonical source of truth).\n');

    Then `process.exit(0);` explicitly.

    Self-validation BEFORE the print sequence: round-trip the BLOCK through JSON.parse and assert the resulting object equals
      { mcpServers: { fsb: { command: 'npx', args: ['-y', 'fsb-mcp-server'] } } }
    If the parse or assertion fails, print `[FAIL] BLOCK constant is not valid JSON or has drifted` to stderr and exit 1. (This is a defense-in-depth check; the literal is hard-coded so the assertion should always pass.)

    Forbidden:
    - emojis / non-ASCII bytes
    - `require(...)`
    - any logic that builds the JSON dynamically (must be a literal template -- so byte-for-byte parity with install.ts is verifiable)
    - line endings other than `\n` (NO CRLF; the editor must save the file with LF endings)
    - reading or importing from `mcp/src/install.ts` at runtime (the script ships inside the skill folder which is published independently to ClawHub; it cannot depend on `mcp/src/*.ts` existing on disk at runtime)
  </action>
  <verify>
    <automated>
      bash -c 'f="skills/FSB Skill/scripts/print-stdio.mjs"; test -f "$f"; head -1 "$f" | grep -qx "#!/usr/bin/env node"; grep -q "Block parity with mcp/src/install.ts" "$f"; grep -q "\"mcpServers\"" "$f"; grep -q "\"fsb\"" "$f"; grep -q "\"command\": \"npx\"" "$f"; grep -q "\"args\": \\[\"-y\", \"fsb-mcp-server\"\\]" "$f"; grep -q "Paste this into your OpenClaw MCP config:" "$f"; grep -q "install.ts getSetupSections" "$f"; ! grep -q "require(" "$f"; ! LC_ALL=C grep -P "[^\x00-\x7F]" "$f"; node --check "$f"; out=$(node "$f"); echo "$out" | grep -q "Paste this into your OpenClaw MCP config:"; echo "$out" | python3 -c "import sys,json,re; t=sys.stdin.read(); m=re.search(r\"\\{[^\\0]*\\}\", t); o=json.loads(m.group(0)); assert o==={\"mcpServers\":{\"fsb\":{\"command\":\"npx\",\"args\":[\"-y\",\"fsb-mcp-server\"]}}}, o; print(\"[OK] runtime print parses to canonical JSON\")"; test ! "$(file \"$f\" | grep -c CRLF)" = "1" || { echo "[FAIL] CRLF line endings detected"; exit 1; }; echo "[OK] print-stdio.mjs structure + parity pass"'
    </automated>
  </verify>
  <acceptance_criteria>
    - `node --check "skills/FSB Skill/scripts/print-stdio.mjs"` exits 0.
    - First line is exactly `#!/usr/bin/env node`.
    - File contains the parity comment `Block parity with mcp/src/install.ts`.
    - File contains the literal `"command": "npx"` and the literal `"args": ["-y", "fsb-mcp-server"]` (verbatim shape from install.ts:173-174).
    - Header line `Paste this into your OpenClaw MCP config:` and footer reference `install.ts getSetupSections` both present.
    - `require(` does NOT appear.
    - No non-ASCII bytes (`LC_ALL=C grep -P '[^\x00-\x7F]'` returns nothing).
    - File has LF line endings (NOT CRLF).
    - Running `node "skills/FSB Skill/scripts/print-stdio.mjs"` exits 0 and stdout includes a JSON object that `json.loads` parses to the canonical structure {"mcpServers":{"fsb":{"command":"npx","args":["-y","fsb-mcp-server"]}}}.
  </acceptance_criteria>
  <done>
    print-stdio.mjs is a valid ESM file. Running it prints the header, the canonical JSON block (parses byte-for-byte to the canonical structure), and the footer pointing at `mcp/src/install.ts` as the source of truth. Phase 251 TEST-03 will assert byte-level parity against install.ts at CI time.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-platform self-test for print-stdio.mjs (SKILL-06)</name>
  <files>skills/FSB Skill/scripts/print-stdio.mjs</files>
  <read_first>
    - skills/FSB Skill/scripts/print-stdio.mjs (post-Task-1)
    - .planning/phases/249-skill-md-scripts/249-CONTEXT.md (Cross-platform discipline)
  </read_first>
  <action>
    No file mutation. Run cross-platform discipline checks:

    1. ESM parse: `node --check "skills/FSB Skill/scripts/print-stdio.mjs"` -- exits 0.
    2. No shell-specific tokens grep: `\\bcmd\\.exe\\b`, `\\bpowershell\\b`, `\\bbash\\b`, `\\bsh -c\\b` -- all return zero matches.
    3. No hard-coded OS paths: `/usr/`, `/bin/`, `C:\\`, `D:\\` (in string literals) -- zero matches.
    4. No filesystem reads: grep for `readFile`, `readFileSync`, `import.*node:fs` -- zero matches (the script must be self-contained; it cannot read mcp/src/install.ts at runtime because the skill ships separately to ClawHub).
    5. No dynamic JSON building: grep for `JSON.stringify` -- zero matches (the BLOCK must be a literal so parity is byte-verifiable).
    6. Live run: `node "skills/FSB Skill/scripts/print-stdio.mjs" > /tmp/fsb-stdio.txt 2>&1`. Exit code 0. The captured output:
       - Begins with the literal header `Paste this into your OpenClaw MCP config:`.
       - Contains a JSON block whose parse equals `{"mcpServers":{"fsb":{"command":"npx","args":["-y","fsb-mcp-server"]}}}`.
       - Ends with the footer line referencing `install.ts getSetupSections`.

    If any of 1-6 fail, return to Task 1 to patch.
  </action>
  <verify>
    <automated>
      bash -c 'set -e; f="skills/FSB Skill/scripts/print-stdio.mjs"; node --check "$f"; ! grep -E "\\bcmd\\.exe\\b|\\bpowershell\\b|^[^#/]*\\bbash\\b|\\bsh -c\\b" "$f"; ! grep -E "\"/usr/|\"/bin/|\"C:\\\\\\\\|\"D:\\\\\\\\" "$f"; ! grep -E "readFile|node:fs" "$f"; ! grep "JSON.stringify" "$f"; node "$f" > /tmp/fsb-stdio.txt; head -1 /tmp/fsb-stdio.txt | grep -qx "Paste this into your OpenClaw MCP config:"; tail -1 /tmp/fsb-stdio.txt | grep -q "install.ts getSetupSections"; python3 -c "import json,re; t=open(\"/tmp/fsb-stdio.txt\").read(); m=re.search(r\"\\{[\\s\\S]*\\}\", t); o=json.loads(m.group(0)); assert o==={\"mcpServers\":{\"fsb\":{\"command\":\"npx\",\"args\":[\"-y\",\"fsb-mcp-server\"]}}}, o; print(\"[OK] live run round-trips to canonical JSON\")"'
    </automated>
  </verify>
  <acceptance_criteria>
    - All six checks pass: ESM parse, no shell-specific tokens, no OS paths, no filesystem reads, no JSON.stringify (BLOCK is literal), live run prints header + canonical JSON + footer.
    - The script's printed JSON deep-equals `{"mcpServers":{"fsb":{"command":"npx","args":["-y","fsb-mcp-server"]}}}`.
  </acceptance_criteria>
  <done>
    print-stdio.mjs satisfies SKILL-06 cross-platform discipline AND its runtime output round-trips to the canonical OpenClaw stdio JSON. Ready for Phase 251 TEST-03 to lock byte-level parity at CI time.
  </done>
</task>

</tasks>

<verification>
1. ESM parses (`node --check`) and runs cleanly (Tasks 1 + 2 automated).
2. Output begins with the header, contains a JSON block whose parse equals the canonical structure, ends with the install.ts footer (Tasks 1 + 2 automated).
3. No emojis, no non-ASCII, no CRLF, no shell-specific tokens, no filesystem reads, no JSON.stringify (Tasks 1 + 2 automated).
4. Block is a literal template (so Phase 251 TEST-03 can assert byte-for-byte parity against install.ts).
</verification>

<success_criteria>
- `skills/FSB Skill/scripts/print-stdio.mjs` runs as `node "skills/FSB Skill/scripts/print-stdio.mjs"` on macOS / Linux / Windows.
- Stdout: header line + canonical JSON block + footer line.
- Parsed JSON: `{"mcpServers":{"fsb":{"command":"npx","args":["-y","fsb-mcp-server"]}}}`.
- Exit code 0 always (the script never fails at runtime because the BLOCK is a hard-coded literal).
- ASCII-only, no emojis, LF line endings.
</success_criteria>

<output>
After completion, create `.planning/phases/249-skill-md-scripts/249-03-SUMMARY.md`.
</output>
