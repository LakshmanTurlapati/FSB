---
phase: 249-skill-md-scripts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "skills/FSB Skill/SKILL.md"
autonomous: true
requirements:
  - SKILL-01
  - SKILL-02
tags: [skill, openclaw, frontmatter, progressive-disclosure]

must_haves:
  truths:
    - "SKILL.md begins with a YAML frontmatter block delimited by --- lines"
    - "Frontmatter contains exactly the keys verified by Phase 248 (no priority, no must-use)"
    - "metadata.openclaw is a single-line valid JSON object inside YAML"
    - "SKILL.md body fits the ~600 token budget and points at references/*.md instead of inlining"
    - "ZERO emojis, ZERO non-ASCII bytes anywhere in the file"
  artifacts:
    - path: "skills/FSB Skill/SKILL.md"
      provides: "OpenClaw-loadable skill metadata + concise body"
      contains: "name: FSB"
      contains_also:
        - "version: 0.9.61"
        - "user-invocable: true"
        - "requires:"
        - "metadata:"
        - "openclaw:"
  key_links:
    - from: "skills/FSB Skill/SKILL.md"
      to: ".planning/v0.9.61-OPENCLAW-SPEC.md"
      via: "schema fields verified there"
      pattern: "name:\\s*FSB"
    - from: "skills/FSB Skill/SKILL.md body"
      to: "references/tool-decision-tree.md, references/multi-agent-contract.md, references/restricted-tab-recovery.md, references/vault-boundary.md, references/default-to-fsb.md"
      via: "progressive-disclosure pointers (NOT inlined)"
      pattern: "references/"
---

<objective>
Author the verified OpenClaw frontmatter and the concise body of `skills/FSB Skill/SKILL.md`, overwriting the Phase 248 placeholder.

Purpose: Give OpenClaw the exact metadata it needs to load the FSB skill (name, version, requires.bins, single-line metadata.openclaw JSON) plus a body short enough that OpenClaw will not blow context loading the skill, but specific enough that the four core priors (default-to-FSB, doctor-first, visual-session wrapping, progressive disclosure into references/) are present.

Output: A single rewritten file at `skills/FSB Skill/SKILL.md` with valid YAML frontmatter and a body under approximately 600 tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/249-skill-md-scripts/249-CONTEXT.md
@.planning/v0.9.61-OPENCLAW-SPEC.md
@.planning/phases/248-openclaw-spec-verification-gate-repo-scaffolding/248-02-SUMMARY.md
@skills/FSB Skill/SKILL.md
@./CLAUDE.md

<interfaces>
<!-- Verified frontmatter shape from .planning/v0.9.61-OPENCLAW-SPEC.md -->
<!-- All values below are LOCKED by CONTEXT.md; do not re-debate. -->

Required frontmatter keys (per Phase 248 finding):
  name: FSB                              # bare name, no namespace (Phase 248 SCAFFOLD-04 verified available)
  description: <one-line, <=120 chars>
  version: 0.9.61                        # milestone version, NOT fsb-mcp-server@0.8.0
  user-invocable: true
  requires:
    bins: [node, npx]                    # open string array, HIGH confidence
    env: []                              # MANDATORY empty -- secrets resolve inside extension
  homepage: https://github.com/LakshmanTurlapati/FSB
  metadata:
    openclaw: {"install":[{"kind":"node","package":"fsb-mcp-server","bins":["fsb-mcp-server"],"label":"Install FSB MCP server (npm)"}]}
    # ^ MUST be a single line of valid JSON inside YAML (Phase 248 finding -- multi-line YAML parser unreliable)

Forbidden frontmatter keys (per Phase 248 finding):
  priority           # NOT in OpenClaw schema
  must-use           # NOT in OpenClaw schema
  always: true       # decided to omit
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author SKILL.md frontmatter (SKILL-01)</name>
  <files>skills/FSB Skill/SKILL.md</files>
  <read_first>
    - .planning/v0.9.61-OPENCLAW-SPEC.md (sections 1, 2, 4, and 5 for verified shape)
    - .planning/phases/249-skill-md-scripts/249-CONTEXT.md (Implementation Decisions > Frontmatter)
    - skills/FSB Skill/SKILL.md (current placeholder; will be overwritten)
    - ./CLAUDE.md (NO EMOJIS rule)
  </read_first>
  <action>
    Use the Write tool to OVERWRITE `skills/FSB Skill/SKILL.md`. Begin with a YAML frontmatter block delimited by `---` lines. Set EXACTLY these keys (no others, no aliases, no comments inside the YAML):

      name: FSB
      description: FSB drives the user's Chrome via the FSB extension and an MCP bridge for live web tasks.
      version: 0.9.61
      user-invocable: true
      requires:
        bins: [node, npx]
        env: []
      homepage: https://github.com/LakshmanTurlapati/FSB
      metadata:
        openclaw: {"install":[{"kind":"node","package":"fsb-mcp-server","bins":["fsb-mcp-server"],"label":"Install FSB MCP server (npm)"}]}

    The `metadata.openclaw` value MUST be on a SINGLE physical line of valid JSON (per Phase 248 finding: OpenClaw's YAML parser does not reliably handle multi-line `metadata:` blocks). Do NOT split it across lines, do NOT use YAML flow style, do NOT add YAML anchors.

    Forbidden keys (do not add): `priority`, `must-use`, `always`, `displayName`, `tags`, `category`. The fallback `displayName: FSB` is reserved for Phase 253 publish-time only if a ClawHub collision appears; do NOT add it now.

    Description text must be ASCII-only, single sentence, under 120 characters.

    The body content (under the closing `---`) is authored in Task 2 of this plan; for now leave a single blank line after the closing `---` so YAML parses cleanly even if Task 2 is interrupted.

    Per ./CLAUDE.md and CONTEXT.md: ZERO emojis. Plain ASCII only.
  </action>
  <verify>
    <automated>
      python3 -c "import yaml,sys,json,re; t=open('skills/FSB Skill/SKILL.md').read(); assert t.startswith('---\n'), 'no frontmatter open'; end=t.index('\n---', 4); fm=yaml.safe_load(t[4:end]); assert fm['name']=='FSB', fm; assert fm['version']==0.9610 or str(fm['version'])=='0.9.61', fm['version']; assert fm['user-invocable'] is True; assert fm['requires']['bins']==['node','npx']; assert fm['requires']['env']==[]; oc=fm['metadata']['openclaw']; assert isinstance(oc, dict) and 'install' in oc and oc['install'][0]['kind']=='node' and oc['install'][0]['package']=='fsb-mcp-server'; line=[ln for ln in t.splitlines() if 'openclaw:' in ln][0]; assert '{' in line and '}' in line, 'metadata.openclaw must be inline single-line JSON'; assert all(ord(c)<128 for c in t), 'non-ASCII byte found'; assert 'priority' not in fm and 'must-use' not in fm; print('[OK] frontmatter valid')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `head -1 "skills/FSB Skill/SKILL.md"` is exactly `---`.
    - `grep -c '^name: FSB$' "skills/FSB Skill/SKILL.md"` returns 1.
    - `grep -c '^version: 0.9.61$' "skills/FSB Skill/SKILL.md"` returns 1.
    - `grep -c '^user-invocable: true$' "skills/FSB Skill/SKILL.md"` returns 1.
    - `grep -c '^  bins: \[node, npx\]$' "skills/FSB Skill/SKILL.md"` returns 1.
    - `grep -c '^  env: \[\]$' "skills/FSB Skill/SKILL.md"` returns 1.
    - `grep -c '^  openclaw: {"install":\[{"kind":"node","package":"fsb-mcp-server"' "skills/FSB Skill/SKILL.md"` returns 1.
    - `grep -E 'priority|must-use' "skills/FSB Skill/SKILL.md"` returns nothing (exit 1).
    - `LC_ALL=C grep -P '[^\x00-\x7F]' "skills/FSB Skill/SKILL.md"` returns nothing (no non-ASCII bytes).
    - YAML parses cleanly (verified by the python3 automated command).
  </acceptance_criteria>
  <done>
    SKILL.md frontmatter parses as valid YAML with the exact keys and values from the verified-shape interfaces block. metadata.openclaw is on a single line and round-trips through json.loads. No forbidden keys present. No non-ASCII bytes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author SKILL.md body with progressive-disclosure pointers (SKILL-02)</name>
  <files>skills/FSB Skill/SKILL.md</files>
  <read_first>
    - skills/FSB Skill/SKILL.md (post-Task-1 state with frontmatter)
    - .planning/phases/249-skill-md-scripts/249-CONTEXT.md (Implementation Decisions > SKILL.md body)
    - ./CLAUDE.md (NO EMOJIS rule)
  </read_first>
  <action>
    Use the Edit tool to APPEND the body content immediately after the closing `---` line that Task 1 wrote. Do NOT modify the frontmatter. The body MUST stay under approximately 600 tokens (target ~450-550 tokens; hard cap ~600 = roughly 4500 chars of plain prose).

    Body structure (in this exact order, plain Markdown, ASCII only):

    1. `# FSB` heading.

    2. One-paragraph framing (3-4 sentences): "FSB lets you drive the user's real Chrome via the FSB extension and a local MCP bridge. Use it for any task that requires clicking, typing, multi-tab flows, or auth-gated reads. Public docs / JSON / RSS reads stay on WebFetch (see references/default-to-fsb.md)."

    3. `## When to use FSB` -- 4-6 bullets restating the soft-preference / hard-escalation rule INLINE (one-liner each). Soft: "If a request mentions a real website, prefer FSB tools when one fits." Hard: "ALWAYS escalate to FSB for click, type, auth, multi-tab, or anything chrome-state-dependent." Carve-out: "Stay on WebFetch for public-doc / JSON / RSS reads."

    4. `## Doctor-first protocol` -- 2-3 sentences: "If anything looks off (no page response, errors, stale state) run `node scripts/doctor.mjs` (which wraps `npx -y fsb-mcp-server doctor`) BEFORE retrying. Fix the failing layer; do not loop on the same call."

    5. `## Visual session wrapping` -- 2-3 sentences: "Any external-AI-driven sequence opens with `start_visual_session(client=\"OpenClaw\", ...)` and closes with `end_visual_session(session_token=..., reason=...)`. The wrap MUST close on every error path so the orange glow does not get stuck. Lifecycle details live in `references/`."

    6. `## Multi-agent contract` -- 1 sentence + pointer: "Never pass `agent_id` (server mints). Use `back` instead of `execute_js(\"history.back()\")`. Typed errors and recovery: see `references/multi-agent-contract.md`."

    7. `## Vault and credentials` -- 1 sentence + pointer: "Passwords and CVV resolve INSIDE the extension via `fill_credential` / `use_payment_method`. Never put secrets in chat. See `references/vault-boundary.md`."

    8. `## References (load on demand)` -- bulleted list of FIVE pointers (path + one-line summary, NOT inlined content):
       - `references/tool-decision-tree.md` -- read_page vs get_dom_snapshot vs get_page_snapshot vs get_site_guide; typed events over .value
       - `references/multi-agent-contract.md` -- typed errors (TAB_NOT_OWNED, AGENT_CAP_REACHED, TAB_INCOGNITO_NOT_SUPPORTED, TAB_OUT_OF_SCOPE), back tool
       - `references/restricted-tab-recovery.md` -- chrome:// / edge:// / web-store recovery tools
       - `references/vault-boundary.md` -- credential routing rules
       - `references/default-to-fsb.md` -- soft preference + hard escalation rule

    9. `## Scripts (run as needed)` -- bulleted list of THREE pointers:
       - `scripts/doctor.mjs` -- diagnose the failing layer; print [OK]/[FAIL]/[WARN] markers
       - `scripts/print-stdio.mjs` -- print the OpenClaw stdio config block to paste into your MCP config
       - `scripts/install-host.mjs` -- detect other MCP hosts; consent-gated per-host install

    Body MUST NOT contain:
    - Inlined tool catalogs (those live in references/tool-decision-tree.md per Phase 250)
    - The full typed-error catalog (that lives in references/multi-agent-contract.md)
    - The full visual-session lifecycle (lives in references/ per Phase 250)
    - Any code blocks longer than 2 lines
    - Any link to chromewebstore.google.com (Phase 250 USAGE.md owns the install URL)
    - Emojis, em-dashes that look like emojis, box-drawing characters, or any non-ASCII

    The references/*.md files do NOT YET EXIST -- they ship in Phase 250. Pointers are forward references; this is the progressive-disclosure pattern OpenClaw expects.
  </action>
  <verify>
    <automated>
      bash -c 'f="skills/FSB Skill/SKILL.md"; sz=$(wc -c < "$f"); test "$sz" -lt 5500 || { echo "[FAIL] body exceeds ~600 token budget ($sz bytes > 5500)"; exit 1; }; grep -q "^# FSB$" "$f" && grep -q "^## When to use FSB$" "$f" && grep -q "^## Doctor-first protocol$" "$f" && grep -q "^## Visual session wrapping$" "$f" && grep -q "^## Multi-agent contract$" "$f" && grep -q "^## Vault and credentials$" "$f" && grep -q "^## References (load on demand)$" "$f" && grep -q "^## Scripts (run as needed)$" "$f" && grep -q "references/tool-decision-tree.md" "$f" && grep -q "references/multi-agent-contract.md" "$f" && grep -q "references/restricted-tab-recovery.md" "$f" && grep -q "references/vault-boundary.md" "$f" && grep -q "references/default-to-fsb.md" "$f" && grep -q "scripts/doctor.mjs" "$f" && grep -q "scripts/print-stdio.mjs" "$f" && grep -q "scripts/install-host.mjs" "$f" && grep -q "start_visual_session" "$f" && grep -q "end_visual_session" "$f" && grep -q "agent_id" "$f" && grep -q "fill_credential" "$f" && ! LC_ALL=C grep -P "[^\x00-\x7F]" "$f" && echo "[OK] body structure + budget + ASCII pass"'
    </automated>
  </verify>
  <acceptance_criteria>
    - `wc -c < "skills/FSB Skill/SKILL.md"` is less than 5500 bytes (approx 600 token cap with frontmatter overhead).
    - `grep -c '^# FSB$' "skills/FSB Skill/SKILL.md"` returns 1.
    - All eight section headings present (verified by automated command).
    - All five `references/*.md` paths appear as text in the body.
    - All three `scripts/*.mjs` paths appear as text in the body.
    - `start_visual_session` and `end_visual_session` appear at least once.
    - `agent_id` and `fill_credential` appear at least once.
    - `LC_ALL=C grep -P '[^\x00-\x7F]' "skills/FSB Skill/SKILL.md"` returns nothing.
    - `grep -c 'chromewebstore' "skills/FSB Skill/SKILL.md"` returns 0 (URL belongs to Phase 250 USAGE.md).
    - The frontmatter from Task 1 is unchanged: `head -1` is `---`, `name: FSB` is still on its own line, etc.
  </acceptance_criteria>
  <done>
    SKILL.md has frontmatter (Task 1) AND a body (Task 2) under ~600 tokens with all eight required headings, five references/ pointers, three scripts/ pointers, the visual-session wrapping rule, the agent_id rule, and the fill_credential rule. No inlined tool catalog. No non-ASCII. No chromewebstore URL.
  </done>
</task>

</tasks>

<verification>
1. Frontmatter parses as YAML with the exact verified shape (Task 1 automated).
2. metadata.openclaw is a single physical line of valid JSON (Task 1 automated).
3. Body has all eight section headings and stays under the ~600 token budget (Task 2 automated).
4. ZERO non-ASCII bytes anywhere (both tasks automated).
5. ZERO forbidden keys (`priority`, `must-use`) in the frontmatter (Task 1 automated).
</verification>

<success_criteria>
- `skills/FSB Skill/SKILL.md` parses as a valid skill: YAML frontmatter + Markdown body.
- Frontmatter matches `.planning/v0.9.61-OPENCLAW-SPEC.md` exactly (name, version, requires.bins, requires.env empty, single-line metadata.openclaw JSON, no forbidden keys).
- Body uses progressive disclosure: pointers to references/*.md and scripts/*.mjs, NOT inlined content.
- Body fits the ~600 token budget; ASCII-only; no emojis.
</success_criteria>

<output>
After completion, create `.planning/phases/249-skill-md-scripts/249-01-SUMMARY.md`.
</output>
