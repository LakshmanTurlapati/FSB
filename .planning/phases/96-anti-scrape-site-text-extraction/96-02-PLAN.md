---
phase: 96-anti-scrape-site-text-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 96-01
files_modified:
  - .planning/phases/96-anti-scrape-site-text-extraction/96-DIAGNOSTIC.md
autonomous: false
requirements:
  - DARK-10

must_haves:
  truths:
    - "Live MCP test attempted against a site with anti-scrape protections (right-click disabled, text selection disabled, obfuscated CSS classes) using get_dom_snapshot to detect protection indicators, read_page or get_text to extract text content despite protections, and verification that extracted text is non-empty and contains real article/page content, with documented outcome (pass/partial/fail/skip-auth)"
    - "Diagnostic report exists with step-by-step log, what worked, what failed, tool gaps, and autopilot recommendations specific to anti-scrape text extraction dark pattern navigation"
    - "Diagnostic report contains a Dark Pattern Analysis section assessing each anti-scrape protection type encountered (right-click block, selection block, copy block, CSS obfuscation, overlay div), whether DOM-level tools bypassed them, and structural selector effectiveness for obfuscated class names"
    - "Human verified the diagnostic report accuracy and outcome classification"
  artifacts:
    - path: ".planning/phases/96-anti-scrape-site-text-extraction/96-DIAGNOSTIC.md"
      provides: "DARK-10 autopilot diagnostic report"
      contains: "DARK-10"
  key_links:
    - from: "96-DIAGNOSTIC.md"
      to: "site-guides/utilities/anti-scrape-text-extraction.js"
      via: "selector accuracy table references anti-scrape-text-extraction.js selectors for content containers, protection indicators, data attributes"
      pattern: "extractProtectedText|contentContainers|protectionIndicators|dataAttributes"
    - from: "96-DIAGNOSTIC.md"
      to: "mcp-server/src/tools/manual.ts"
      via: "tool usage section references get_dom_snapshot, read_page, get_text, execute_js, navigate tools"
      pattern: "get_dom_snapshot|read_page|get_text|execute_js|navigate"
---

<objective>
Execute live MCP test for DARK-10 (scrape text from site blocking right-clicks, disabling selection, masking CSS classes) and generate the autopilot diagnostic report.

Purpose: Validate whether the MCP DOM-level text extraction tools (get_dom_snapshot, read_page, get_text) can bypass JavaScript/CSS anti-scrape protections to extract text content. The key test: given a page where right-click is disabled, text selection is blocked by CSS user-select:none, and CSS class names are obfuscated, can the AI extract the full article text using DOM-level tools that bypass the UI layer? Expected outcome is PARTIAL (HTTP validation of page DOM structure, anti-scrape protection indicators in server HTML/JavaScript, content container identification via structural selectors, but live MCP text extraction blocked by WebSocket bridge disconnect) or PASS (if MCP bridge is operational). Produces a structured diagnostic report for the autopilot enhancement milestone.

Output: 96-DIAGNOSTIC.md with DARK-10 test results
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-anti-scrape-site-text-extraction/96-CONTEXT.md
@.planning/phases/96-anti-scrape-site-text-extraction/96-01-SUMMARY.md
@site-guides/utilities/anti-scrape-text-extraction.js
@.planning/phases/95-skip-ad-countdown/95-DIAGNOSTIC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute live MCP anti-scrape text extraction test, generate DARK-10 diagnostic report</name>
  <files>.planning/phases/96-anti-scrape-site-text-extraction/96-DIAGNOSTIC.md</files>
  <read_first>
    - site-guides/utilities/anti-scrape-text-extraction.js (full file -- site guide with extractProtectedText workflow, DARK-10 guidance, protection types, site-specific selectors, structural selector strategies)
    - .planning/phases/95-skip-ad-countdown/95-DIAGNOSTIC.md lines 1-15 (diagnostic report template structure -- use exact same format with Dark Pattern Analysis section for anti-scrape text extraction)
    - .planning/phases/96-anti-scrape-site-text-extraction/96-01-SUMMARY.md (Plan 01 outcome -- what was added to anti-scrape-text-extraction.js)
  </read_first>
  <action>
Execute the DARK-10 edge case test using MCP manual tools following the extractProtectedText workflow from the anti-scrape-text-extraction.js site guide. Test steps:

1. IDENTIFY TEST TARGETS FOR ANTI-SCRAPE TEXT EXTRACTION:
   Use HTTP-based validation to analyze page structures for anti-scrape protection indicators:
   (a) medium.com -- primary target. Metered paywall with selection restrictions. Fetch a public article page and analyze HTML for: user-select CSS rules, oncontextmenu handlers, obfuscated class names (CSS Module hashes like pw-*), article/section/p content structure, [data-selectable-paragraph] attributes.
   (b) genius.com -- secondary target. Lyrics site with right-click and selection blocking. Fetch a popular song lyrics page and analyze HTML for: oncontextmenu handlers, user-select restrictions, [data-lyrics-container] selectors, lyrics text in DOM.
   (c) bloomberg.com -- third target. Premium content with copy/selection prevention. Fetch a public article and analyze for: JS event handlers, styled-components class obfuscation, [data-component="article-body"] selectors.
   (d) allrecipes.com -- fourth target. Recipe site with ad protection overlays. Fetch a recipe page and analyze for: overlay divs, ad containers wrapping content, recipe structured data (schema.org), .recipe-body selectors.
   (e) wsj.com or nytimes.com -- fifth target. News paywall with class obfuscation and content truncation. Fetch a public article and analyze for: CSS-in-JS obfuscated classes, article body structure, paywall truncation indicators.
   (f) HTTP fetch each candidate page to analyze their HTML/JS for anti-scrape protection indicators.

2. VALIDATE MEDIUM ARTICLE PAGE (primary target):
   (a) Fetch a public Medium article (e.g., https://medium.com/tag/programming or a well-known article URL) to analyze page structure
   (b) Search the HTML source for anti-scrape protection indicators: user-select, -webkit-user-select, oncontextmenu, onselectstart, oncopy
   (c) Search for content container elements: article, section, p tags, [data-selectable-paragraph], pw-post-body-paragraph
   (d) Analyze CSS class name patterns: are classes readable or obfuscated (hash-like)?
   (e) Check if article text content is present in server HTML or requires client-side rendering
   (f) Record: URL tested, HTTP status, page size, protections found, content structure, class name pattern

3. VALIDATE GENIUS LYRICS PAGE (secondary target):
   (a) Fetch a popular song lyrics page (e.g., https://genius.com/Queen-bohemian-rhapsody-lyrics or similar)
   (b) Search for anti-scrape indicators: oncontextmenu, user-select:none, onselectstart, oncopy
   (c) Search for lyrics container: [data-lyrics-container], .lyrics, Lyrics__Container
   (d) Check if lyrics text is in server-rendered HTML or client-rendered
   (e) Record: URL tested, HTTP status, protections found, lyrics container elements, text availability

4. VALIDATE BLOOMBERG ARTICLE PAGE (third target):
   (a) Fetch a public Bloomberg article or the homepage
   (b) Search for protection indicators: event handlers, CSS restrictions, overlay elements
   (c) Search for content selectors: [data-component="article-body"], article, .body-content
   (d) Analyze class name obfuscation pattern (styled-components generate sc-* or random hashes)
   (e) Record: URL tested, HTTP status, protections found, content structure

5. VALIDATE ALLRECIPES RECIPE PAGE (fourth target):
   (a) Fetch a popular recipe page (e.g., https://www.allrecipes.com/recipe/popular)
   (b) Search for overlay divs, ad container wrappers, z-index positioned elements
   (c) Search for recipe content: [itemtype*="Recipe"], .recipe-body, .ingredients-section, schema.org structured data
   (d) Check if recipe text is accessible despite ad overlay elements
   (e) Record: URL tested, HTTP status, overlay elements found, recipe content structure

6. VALIDATE NEWS PAYWALL SITE (fifth target):
   (a) Fetch a public article from wsj.com or nytimes.com
   (b) Search for CSS-in-JS class name patterns, selection restrictions, copy handlers
   (c) Search for article content: [data-testid="article-body"], article, .article-body
   (d) Check for paywall truncation indicators (content cutoff, subscribe prompts)
   (e) Record: URL tested, HTTP status, protections found, content availability

7. ANTI-SCRAPE PROTECTION ANALYSIS:
   (a) For every site tested, compile which protections are present:
       - Right-click block: oncontextmenu handler found in HTML/JS?
       - Selection block: user-select:none in CSS? onselectstart handler?
       - Copy block: oncopy handler?
       - Class obfuscation: random/hashed class names?
       - Overlay div: transparent positioned div over content?
       - JS-only content: text absent from server HTML, client-rendered only?
   (b) For each protection found, assess:
       - Would get_dom_snapshot bypass it? (Yes for all except image-based text)
       - Would read_page bypass it? (Yes for all except image-based text)
       - Would get_text bypass it? (Yes for all except CSS content property text)
       - Would structural selectors work despite class obfuscation? (Yes -- article, p, data-* attributes)
   (c) Record: protection matrix per site, bypass feasibility per tool

8. OUTCOME CLASSIFICATION:
   - PASS = text extracted from at least 2 anti-scrape protected sites via MCP DOM tools, protections confirmed and bypassed, extracted text verified as real content
   - PARTIAL = HTTP validation confirms page structures with anti-scrape indicators (JS handlers, CSS restrictions, obfuscated classes) and text content identifiable in server HTML via structural selectors, but live MCP DOM extraction blocked by WebSocket bridge disconnect
   - FAIL = could not identify anti-scrape protections on any target site, or text content is entirely image-based and not extractable via DOM
   - SKIP-AUTH = not applicable for public articles (reading does not require auth on most targets)

Record every step outcome for the diagnostic report.

After test execution (regardless of outcome), generate 96-DIAGNOSTIC.md with ALL of these sections:

## Metadata
- Phase: 96, Requirement: DARK-10, Date: today, Outcome: (PASS/PARTIAL/FAIL/SKIP-AUTH), Live MCP Testing: YES/NO

## Prompt Executed
The exact prompt: "Navigate to a site that blocks right-clicks, disables text selection, and uses obfuscated CSS class names, then extract the full article text using DOM-level tools (get_dom_snapshot, read_page, get_text) that bypass these UI-layer anti-scrape protections."

## Result Summary
2-3 sentence summary of what happened.

## Step-by-Step Log
Table with columns: Step | MCP Tool Used | Target | Result | Notes
One row per tool invocation or HTTP fetch attempted.

## What Worked
Bullet list of everything that succeeded.

## What Failed
Bullet list of everything that failed or was not executed, with reasons.

## Tool Gaps Identified
New tool gaps discovered plus reference to known gaps. Consider:
- Did navigate successfully load anti-scrape protected pages?
- Could get_dom_snapshot detect protection indicators (oncontextmenu, user-select)?
- Could read_page extract text despite CSS user-select:none?
- Could get_text extract text from specific elements behind overlay divs?
- Is there a tool gap for CSS pseudo-element text extraction (::before/::after content)?
- Is there a tool gap for detecting image-based text (text rendered as canvas/SVG)?
- Are structural selectors (article, p, data-*) sufficient for obfuscated class sites?
- WebSocket bridge availability (persistent gap from phases 55-95)

## Dark Pattern Analysis (DARK PATTERN CATEGORY SECTION)
Specific analysis of anti-scrape text extraction dark patterns:
- Protection types encountered per site: right-click block, selection block, copy block, class obfuscation, overlay div, JS-only rendering
- Server-rendered vs client-rendered content per site
- CSS class name obfuscation patterns: CSS Modules (pw-*), styled-components (sc-*, css-*), Emotion, random hashes
- Structural selector effectiveness: which selectors (article, [data-*], semantic HTML) worked on which sites
- DOM text extraction feasibility: textContent/innerText availability despite user-select:none
- Overlay div prevalence: which sites use transparent overlays and can DOM tools read through them
- Content truncation by paywall: how much text is available in DOM before paywall cutoff
- Recommendations for bypassing anti-scrape protections without vision

## Bugs Fixed In-Phase
Any bugs found and fixed during testing.

## Autopilot Recommendations
8-10 specific recommendations for how autopilot should handle anti-scrape protected sites. Cover:
- Always use get_dom_snapshot or read_page for text extraction -- these bypass ALL JS/CSS UI-layer protections
- When class names are obfuscated, use structural selectors: article, p, [data-*], [role="article"], [itemtype*="Article"], semantic HTML tags
- Do NOT attempt to "disable" protections (remove event handlers, modify CSS) as first strategy -- direct DOM text extraction is simpler and more reliable
- If text appears empty in extracted content, check for: (a) client-rendered content requiring live browser, (b) CSS ::before/::after pseudo-element text, (c) iframe-isolated content, (d) image-based text
- Use execute_js to read CSS pseudo-element content only as a fallback when standard text extraction returns empty for visible text
- For sites with transparent overlay divs, get_text reads through overlays by default -- no overlay removal needed for text extraction
- Document encountered protection types for future reference: same site may use different protections on different pages
- Text extraction from paywalled content: extract whatever is available in DOM (often first few paragraphs), do not attempt paywall bypass
- Verify extracted text by checking: non-empty, contains headline, reasonable length (500+ chars for articles), not just navigation/boilerplate
- If DOM text extraction fails completely on a site, it likely uses image-based text rendering (canvas/SVG) which requires vision -- document as limitation

## Selector Accuracy
Table: Selector | Expected | Actual | Match -- for each selector from anti-scrape-text-extraction.js tested against live DOM or HTTP. Test at minimum: contentContainers.article, contentContainers.lyrics (genius), contentContainers.recipe (allrecipes), contentContainers.generic, protectionIndicators.rightClickBlock, protectionIndicators.selectionBlock, dataAttributes.medium, dataAttributes.genius.

## New Tools Added This Phase
Table: Tool Name | File | Purpose | Parameters
Note: No new MCP tools added this phase. List the anti-scrape-text-extraction.js site guide extractProtectedText workflow addition as the new content.

Use the exact same structure and level of detail as 95-DIAGNOSTIC.md, with the Dark Pattern Analysis section covering anti-scrape text extraction specifics. Do NOT leave any section as placeholder text like "TBD" or "TODO".
  </action>
  <verify>
    <automated>test -f .planning/phases/96-anti-scrape-site-text-extraction/96-DIAGNOSTIC.md && grep -q "DARK-10" .planning/phases/96-anti-scrape-site-text-extraction/96-DIAGNOSTIC.md && grep -q "Outcome:" .planning/phases/96-anti-scrape-site-text-extraction/96-DIAGNOSTIC.md && grep -q "Autopilot Recommendations" .planning/phases/96-anti-scrape-site-text-extraction/96-DIAGNOSTIC.md && grep -q "Step-by-Step Log" .planning/phases/96-anti-scrape-site-text-extraction/96-DIAGNOSTIC.md && grep -q "Dark Pattern" .planning/phases/96-anti-scrape-site-text-extraction/96-DIAGNOSTIC.md && grep -q "Selector Accuracy" .planning/phases/96-anti-scrape-site-text-extraction/96-DIAGNOSTIC.md && grep -q "New Tools Added" .planning/phases/96-anti-scrape-site-text-extraction/96-DIAGNOSTIC.md && grep -q "user-select\|oncontextmenu\|right-click" .planning/phases/96-anti-scrape-site-text-extraction/96-DIAGNOSTIC.md && grep -q "obfuscat\|class.*name\|structural" .planning/phases/96-anti-scrape-site-text-extraction/96-DIAGNOSTIC.md && grep -q "get_dom_snapshot\|read_page\|get_text" .planning/phases/96-anti-scrape-site-text-extraction/96-DIAGNOSTIC.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/96-anti-scrape-site-text-extraction/96-DIAGNOSTIC.md exists
    - grep "DARK-10" 96-DIAGNOSTIC.md returns at least 1 match
    - grep "Outcome:" 96-DIAGNOSTIC.md returns at least 1 match (PASS, PARTIAL, FAIL, or SKIP-AUTH)
    - grep "Autopilot Recommendations" 96-DIAGNOSTIC.md returns at least 1 match
    - grep "Step-by-Step Log" 96-DIAGNOSTIC.md returns at least 1 match
    - grep "Dark Pattern" 96-DIAGNOSTIC.md returns at least 1 match (category-specific section)
    - grep "Selector Accuracy" 96-DIAGNOSTIC.md returns at least 1 match
    - grep "What Worked" 96-DIAGNOSTIC.md returns at least 1 match
    - grep "What Failed" 96-DIAGNOSTIC.md returns at least 1 match
    - grep "user-select\|oncontextmenu\|right-click\|onselectstart" 96-DIAGNOSTIC.md returns at least 5 matches (anti-scrape protection references)
    - grep "obfuscat\|class.*obfus\|hashed\|randomiz\|structural.*select" 96-DIAGNOSTIC.md returns at least 3 matches (class obfuscation analysis)
    - grep "get_dom_snapshot\|read_page\|get_text" 96-DIAGNOSTIC.md returns at least 5 matches (DOM extraction tool references)
    - grep "textContent\|innerText\|DOM.*text\|DOM.*extract" 96-DIAGNOSTIC.md returns at least 2 matches (DOM text property analysis)
    - grep "medium\|bloomberg\|genius\|allrecipes\|wsj\|nytimes" 96-DIAGNOSTIC.md returns at least 3 matches (test target sites)
    - grep "overlay\|transparent.*div\|z-index" 96-DIAGNOSTIC.md returns at least 1 match (overlay analysis)
    - grep "New Tools Added" 96-DIAGNOSTIC.md returns at least 1 match
    - No section contains only placeholder text like "TBD" or "TODO"
  </acceptance_criteria>
  <done>DARK-10 diagnostic report generated with real test data covering all required sections: metadata, prompt, result summary, step-by-step log (Medium/Genius/Bloomberg/Allrecipes/news paywall page analysis, protection indicator scanning, content container identification, structural selector testing, text availability analysis), what worked, what failed, tool gaps (page loading, protection detection, text extraction despite CSS/JS blocks, pseudo-element text, image-based text detection, structural selector sufficiency, WebSocket bridge), dark pattern analysis (protection types per site, server vs client rendering, class obfuscation patterns, structural selector effectiveness, DOM text extraction feasibility, overlay div prevalence, paywall truncation, bypass recommendations), bugs fixed, autopilot recommendations (use DOM tools for extraction, structural selectors for obfuscated classes, avoid disabling protections, check for empty text causes, execute_js fallback for pseudo-elements, overlays do not block DOM reads, document protection types, paywall content limits, verify extraction quality, image-based text limitation), selector accuracy (anti-scrape-text-extraction.js selectors vs live DOM/HTTP), new tools (extractProtectedText workflow addition)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify DARK-10 diagnostic report accuracy</name>
  <files>.planning/phases/96-anti-scrape-site-text-extraction/96-DIAGNOSTIC.md</files>
  <action>
Human verifies the DARK-10 diagnostic report accuracy.

What was built: DARK-10 anti-scrape text extraction diagnostic report with MCP test results. The report documents the outcome (PASS/PARTIAL/FAIL/SKIP-AUTH) of attempting to extract text from sites that block right-clicks, disable text selection, and use obfuscated CSS class names. The workflow tests: (a) navigating to anti-scrape protected pages (Medium, Genius, Bloomberg, Allrecipes, news paywall), (b) using get_dom_snapshot to detect protection indicators (oncontextmenu, user-select:none, oncopy handlers, overlay divs), (c) identifying content containers via structural selectors (article, [data-*], semantic HTML) instead of obfuscated class names, (d) extracting text content using read_page/get_text that bypass CSS/JS UI-layer protections, (e) verifying the extracted text is real article content. This phase also created an anti-scrape-text-extraction.js site guide with an extractProtectedText workflow (6 steps), 6+ anti-scrape protection types with bypass methods, 4+ target site patterns, and structural selector strategies for obfuscated classes. The diagnostic includes a "Dark Pattern Analysis" section assessing protection types, class obfuscation patterns, and DOM extraction effectiveness.

How to verify:
1. Open .planning/phases/96-anti-scrape-site-text-extraction/96-DIAGNOSTIC.md
2. Verify the outcome classification is accurate (PASS/PARTIAL/FAIL/SKIP-AUTH matches what actually happened)
3. Verify step-by-step log has real data (no placeholder rows)
4. Verify anti-scrape protection analysis is documented (which protections found on which sites)
5. Verify content container identification is documented (structural selectors used, what worked)
6. Verify class name obfuscation analysis is present (CSS Module hashes, styled-components, etc.)
7. Verify Dark Pattern Analysis section has specific findings about each protection type and bypass effectiveness
8. Verify DOM text extraction feasibility analysis is present (textContent/innerText vs user-select:none)
9. Verify autopilot recommendations are specific to anti-scrape text extraction (not generic browsing advice)
10. Verify selector accuracy table has entries from anti-scrape-text-extraction.js tested against live DOM or HTTP
11. Confirm the report is complete and ready for the autopilot enhancement milestone

Resume: Type "approved" to accept the diagnostic report, or describe any issues to fix.
  </action>
  <verify>Human confirms by typing "approved" or describes issues</verify>
  <done>Human has reviewed and approved the DARK-10 edge case results and diagnostic report.</done>
</task>

</tasks>

<verification>
1. 96-DIAGNOSTIC.md exists with DARK-10 reference and all required sections
2. Outcome is one of: PASS, PARTIAL, FAIL, SKIP-AUTH
3. Step-by-step log contains at least 8 rows with real tool invocations or HTTP fetch results
4. Anti-scrape protection analysis documented (protections found per site)
5. Content container structural selectors documented (what worked despite obfuscated classes)
6. Class name obfuscation patterns documented (CSS Modules, styled-components, random hashes)
7. DOM text extraction feasibility documented (textContent/innerText availability vs user-select:none)
8. Overlay div analysis documented (transparent divs, DOM read-through capability)
9. Dark Pattern Analysis section has specific findings per protection type and per site
10. Autopilot recommendations section has at least 8 specific items about anti-scrape text extraction
11. Selector accuracy table has entries from anti-scrape-text-extraction.js tested against live DOM or HTTP
12. New Tools Added section notes extractProtectedText workflow addition to anti-scrape-text-extraction.js
13. Human approved the diagnostic report
</verification>

<success_criteria>
- DARK-10 edge case attempted via MCP manual tools with documented outcome
- Diagnostic report generated with all required sections filled with real data
- Anti-scrape protection detection and DOM bypass strategy validated (or reason for incomplete execution)
- Site-specific protection types identified and bypass feasibility assessed
- Structural selector effectiveness validated against obfuscated class names
- Dark Pattern Analysis section provides insights for anti-scrape text extraction automation
- Human verified and approved the report accuracy
- Phase 96 complete, DARK-10 is the last requirement in v0.9.7 milestone
</success_criteria>

<output>
After completion, create `.planning/phases/96-anti-scrape-site-text-extraction/96-02-SUMMARY.md`
</output>
