---
phase: 153-dashboard-task-relay-correctness
plan: 02
type: execute
wave: 2
depends_on: [153-01]
files_modified:
  - background.js
  - ws/ws-client.js
  - showcase/js/dashboard.js
autonomous: true
requirements: [RLY-03]

must_haves:
  truths:
    - "Stop, success, and failure outcomes for a dashboard task run are delivered or recovered once per run, not replayed ambiguously across duplicate sources."
    - "Fallback completion logic is real and bounded instead of dead code that never runs."
    - "The dashboard ignores duplicate or stale terminal events for a previous task run."
  artifacts:
    - path: "background.js"
      provides: "Normalized terminal payload builder and bounded completion fallback"
      contains: "taskStatus"
    - path: "ws/ws-client.js"
      provides: "Run-aware stop completion payload"
      contains: "taskRunId"
    - path: "showcase/js/dashboard.js"
      provides: "Run-aware terminal-event dedupe"
      contains: "lastCompletedTaskRunId"
  key_links:
    - from: "background.js"
      to: "showcase/js/dashboard.js"
      via: "ext:task-complete terminal payload contract"
      pattern: "taskRunId|taskStatus|updatedAt"
    - from: "ws/ws-client.js"
      to: "background.js"
      via: "stop-task completion path and dedupe flags"
      pattern: "_dashStopSent|taskRunId"
---

<objective>
Make dashboard task terminal outcomes exact and recoverable per run by replacing the current ambiguous completion paths with a normalized, run-aware terminal contract.

Purpose: The task relay still relies on timestamp freshness and a dead fallback timer. Phase 153 needs explicit run-aware terminal dedupe so stop/success/failure outcomes do not conflict across live sends, fallback sends, and reconnect recovery.
Output: Terminal payloads carry `taskRunId`, fallback completion is bounded and meaningful, and the dashboard ignores duplicate terminal events from an older or already-completed run.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@background.js
@ws/ws-client.js
@showcase/js/dashboard.js

<interfaces>
From `background.js`:
```javascript
broadcastDashboardComplete(result);
startDashboardTask(tabId, task);
handleStopAutomation(request, sender, sendResponse);
```

From `ws/ws-client.js`:
```javascript
_handleStopTask();
send(type, payload);
```

From `showcase/js/dashboard.js`:
```javascript
handleTaskComplete(payload);
applyRecoveredTaskState(snapshot);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize and bound terminal outcome delivery per task run</name>
  <files>background.js, ws/ws-client.js</files>
  <read_first>background.js (broadcastDashboardComplete, startDashboardTask, _rememberDashboardTaskSnapshot), ws/ws-client.js (_handleStopTask)</read_first>
  <action>
Unify success, failure, stop, and fallback terminal delivery around one per-run contract.

1. Build a normalized terminal payload shape that always includes `taskRunId`, `taskStatus`, `task`, `progress`, `phase`, `elapsed`, `lastAction`, `summary`/`error`, and `updatedAt`.
2. Use that shape from normal completion, thrown-error completion, and stop completion paths.
3. Replace the current dead fallback timer logic with a bounded fallback that only exists when a real terminal message still needs to be emitted.
4. Keep `_dashStopSent`-style dedupe, but make the terminal payload itself explicit enough that reconnect recovery and live delivery agree on the last truth.
5. Do not emit multiple different terminal payloads for the same run unless the later one is objectively newer and the dashboard can identify it as such.
  </action>
  <acceptance_criteria>
- `rg -n "taskRunId|taskStatus|summary|error|updatedAt" background.js ws/ws-client.js` returns matches in terminal paths.
- `rg -n "_completionSent|fallback|broadcastDashboardComplete|_dashStopSent" background.js ws/ws-client.js` shows fallback logic is bounded and intentional.
- `rg -n "stopped|success|failed" background.js ws/ws-client.js` shows all three terminal states use the normalized payload shape.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const bg=fs.readFileSync('background.js','utf8'); const ws=fs.readFileSync('ws/ws-client.js','utf8'); const checks=['taskRunId','taskStatus','updatedAt']; if(!(checks.every(s=>bg.includes(s)) && checks.every(s=>ws.includes(s)))) process.exit(1);"</automated>
  </verify>
  <done>
    - Terminal outcomes are normalized per run.
    - The fallback path is real and bounded.
    - Stop, success, and failure payloads no longer disagree structurally.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deduplicate stale or repeated terminal events on the dashboard</name>
  <files>showcase/js/dashboard.js</files>
  <read_first>showcase/js/dashboard.js (handleTaskComplete, applyRecoveredTaskState, setTaskState)</read_first>
  <action>
Make the website dashboard consume terminal events exactly once per task run.

1. Track the current active task run and the most recently completed task run.
2. Ignore terminal events whose `taskRunId` belongs to an older run or whose freshness is older than the last accepted terminal state for the current run.
3. Keep reconnect recovery compatible by allowing a recovered final state for the current run when it is newer than the local state.
4. Reset terminal dedupe state when a new task run becomes active so the next run can complete normally.
5. Preserve the current user-visible success/failed/stopped rendering while making the event acceptance rules run-aware.
  </action>
  <acceptance_criteria>
- `rg -n "lastCompletedTaskRunId|activeTaskRunId|taskRunId" showcase/js/dashboard.js` returns matches.
- `rg -n "handleTaskComplete|applyRecoveredTaskState" showcase/js/dashboard.js` shows run-aware stale/duplicate rejection.
- `rg -n "updatedAt" showcase/js/dashboard.js` still shows freshness checks layered with run identity.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const dash=fs.readFileSync('showcase/js/dashboard.js','utf8'); const checks=['lastCompletedTaskRunId','activeTaskRunId','taskRunId']; if(!checks.every(s=>dash.includes(s))) process.exit(1);"</automated>
  </verify>
  <done>
    - The dashboard renders terminal state once per task run.
    - Duplicate or stale final outcomes from previous runs are ignored.
    - Reconnect recovery still restores the correct final state for the active run.
  </done>
</task>

</tasks>

<verification>
- `node --check background.js`
- `node --check ws/ws-client.js`
- `node --check showcase/js/dashboard.js`
- `rg -n "taskRunId|lastCompletedTaskRunId|taskStatus|updatedAt|_dashStopSent|fallback" background.js ws/ws-client.js showcase/js/dashboard.js`
</verification>

<success_criteria>
- Stop, success, and failure outcomes are delivered or recovered once per task run.
- Terminal payloads are structurally consistent across live, stop, error, and reconnect paths.
- Dead or ambiguous fallback completion behavior is removed.
</success_criteria>

<output>
After completion, create `.planning/phases/153-dashboard-task-relay-correctness/153-02-SUMMARY.md`.
</output>
