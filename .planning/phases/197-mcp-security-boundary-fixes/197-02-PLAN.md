---
phase: 197-mcp-security-boundary-fixes
plan: 02
type: execute
wave: 1
gap_closure: true
depends_on: [197-01]
files_modified:
  - mcp-server/src/tools/vault.ts
autonomous: true
requirements: [MCP-04]

must_haves:
  truths:
    - "MCP use_payment_method bridge request remains pending for the full extension-side payment confirmation window"
    - "MCP server timeout for use_payment_method exceeds ws/mcp-bridge-client.js 120_000ms sidepanel confirmation timeout"
    - "Approval after 30 seconds but before 120 seconds can still return the final MCP tool result instead of timing out early"
  artifacts:
    - path: "mcp-server/src/tools/vault.ts"
      provides: "MCP payment confirmation timeout aligned with extension confirmation window"
      contains: "PAYMENT_CONFIRMATION_TIMEOUT_MS"
  key_links:
    - from: "mcp-server/src/tools/vault.ts (use_payment_method)"
      to: "ws/mcp-bridge-client.js (_handleUsePaymentMethod)"
      via: "bridge.sendAndWait timeout is greater than extension confirmation timeout"
      pattern: "PAYMENT_CONFIRMATION_TIMEOUT_MS\\s*=\\s*125_000"
---

<objective>
Close the remaining Phase 197 verification gap: MCP server `use_payment_method` times out after 30 seconds while the extension-side sidepanel payment confirmation waits 120 seconds.

Purpose: Make MCP payment confirmation reliable end-to-end by keeping the MCP bridge request alive for the full browser confirmation window plus a small response buffer.
Output: Patched `mcp-server/src/tools/vault.ts` with a named 125-second payment confirmation timeout used by `use_payment_method`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/197-mcp-security-boundary-fixes/197-VERIFICATION.md
@.planning/phases/197-mcp-security-boundary-fixes/197-01-SUMMARY.md

<verification_gap>
`197-VERIFICATION.md` reports `status: gaps_found` because `mcp-server/src/tools/vault.ts` sends:

```typescript
{ timeout: 30_000 }
```

for `use_payment_method`, while `ws/mcp-bridge-client.js` waits:

```javascript
}, 120_000);
```

inside `_handleUsePaymentMethod` before returning `timeout`. This means an MCP caller can receive a timeout after 30 seconds while the extension confirmation listener remains active and can still fill the active tab later.
</verification_gap>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align MCP payment bridge timeout with sidepanel confirmation window</name>
  <files>mcp-server/src/tools/vault.ts</files>
  <read_first>
    - mcp-server/src/tools/vault.ts (full file -- contains use_payment_method bridge.sendAndWait timeout)
    - ws/mcp-bridge-client.js (lines 757-763 -- extension-side 120_000ms confirmation timeout)
    - .planning/phases/197-mcp-security-boundary-fixes/197-VERIFICATION.md (gaps section -- source of truth for this closure)
  </read_first>
  <action>
Make one focused change in `mcp-server/src/tools/vault.ts`:

1. Add this named constant after the imports and before `registerVaultTools`:

```typescript
const PAYMENT_CONFIRMATION_TIMEOUT_MS = 125_000;
```

2. Add this comment immediately above the constant:

```typescript
// Must exceed the extension-side 120_000ms payment confirmation gate.
```

3. In the `use_payment_method` handler, replace the current bridge timeout:

```typescript
{ timeout: 30_000 },
```

with:

```typescript
{ timeout: PAYMENT_CONFIRMATION_TIMEOUT_MS },
```

Do not change the shorter read/list timeouts for `list_credentials`, `fill_credential`, or `list_payment_methods`. Do not change `ws/mcp-bridge-client.js`; the extension-side confirmation gate is already wired and verified.
  </action>
  <verify>
    <automated>
grep -n "PAYMENT_CONFIRMATION_TIMEOUT_MS = 125_000" mcp-server/src/tools/vault.ts
grep -n "timeout: PAYMENT_CONFIRMATION_TIMEOUT_MS" mcp-server/src/tools/vault.ts
! grep -n "timeout: 30_000" mcp-server/src/tools/vault.ts
npm --prefix mcp-server run build
    </automated>
  </verify>
  <acceptance_criteria>
    - `mcp-server/src/tools/vault.ts` contains `const PAYMENT_CONFIRMATION_TIMEOUT_MS = 125_000;`
    - `mcp-server/src/tools/vault.ts` contains `// Must exceed the extension-side 120_000ms payment confirmation gate.`
    - `mcp-server/src/tools/vault.ts` `use_payment_method` passes `{ timeout: PAYMENT_CONFIRMATION_TIMEOUT_MS }` to `bridge.sendAndWait`
    - `mcp-server/src/tools/vault.ts` does not contain `timeout: 30_000`
    - `npm --prefix mcp-server run build` exits 0
  </acceptance_criteria>
  <done>
    MCP server `use_payment_method` now waits long enough for the full extension-side sidepanel confirmation window, closing the MCP-04 timeout mismatch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> MCP server | Client waits for final payment fill result from local MCP server |
| MCP server -> extension bridge | Bridge request must stay alive while user makes sidepanel payment decision |
| Extension bridge -> sidepanel | Sidepanel confirmation remains the authority for approval/denial |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-06 | Denial of Service / Reliability | mcp-server/src/tools/vault.ts use_payment_method timeout | mitigate | Increase MCP bridge timeout to 125_000ms, exceeding the extension's 120_000ms confirmation gate so the MCP caller remains pending for the full approval window |
| T-197-07 | Elevation of Privilege | Extension confirmation listener after MCP timeout | mitigate | By preventing early MCP timeout, the extension no longer continues a confirmation flow after the MCP caller has already received a timeout |
</threat_model>

<verification>
1. `grep -n "PAYMENT_CONFIRMATION_TIMEOUT_MS = 125_000" mcp-server/src/tools/vault.ts` shows the named timeout constant.
2. `grep -n "timeout: PAYMENT_CONFIRMATION_TIMEOUT_MS" mcp-server/src/tools/vault.ts` shows `use_payment_method` uses the named timeout.
3. `grep -n "timeout: 30_000" mcp-server/src/tools/vault.ts` returns no matches.
4. `npm --prefix mcp-server run build` passes.
5. Re-run Phase 197 verification and confirm MCP-04 no longer reports the 30-second timeout mismatch.
</verification>

<success_criteria>
- MCP server `use_payment_method` timeout is greater than the extension-side `120_000ms` confirmation timeout.
- MCP payment confirmation can remain pending past 30 seconds without the MCP tool returning early.
- The existing sidepanel approval/denial gate remains unchanged.
- Phase 197 verifier can mark MCP-04 satisfied after re-verification.
</success_criteria>

<output>
After completion, create `.planning/phases/197-mcp-security-boundary-fixes/197-02-SUMMARY.md`
</output>
