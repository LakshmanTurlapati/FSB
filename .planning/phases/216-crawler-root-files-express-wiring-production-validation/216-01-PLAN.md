---
phase: 216
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - showcase/angular/public/robots.txt
  - showcase/angular/public/llms.txt
  - showcase/angular/scripts/llms-full.source.md
  - .planning/phases/216-crawler-root-files-express-wiring-production-validation/verify-static.sh
autonomous: true
requirements: [CRAWL-01, CRAWL-03]
must_haves:
  truths:
    - "robots.txt exists at showcase/angular/public/robots.txt with the verbatim 15-bot allowlist + wildcard + Sitemap line"
    - "llms.txt exists at showcase/angular/public/llms.txt with the verbatim D-01 H1 + opening paragraph + Docs section"
    - "llms-full.source.md exists at showcase/angular/scripts/llms-full.source.md following D-04 section structure with neutral D-03 comparison tone"
    - "All three static files are emoji-free per project CLAUDE.md"
  artifacts:
    - path: "showcase/angular/public/robots.txt"
      provides: "Crawler allowlist for the 15 verified 2026 AI bots + wildcard + Sitemap pointer"
      contains: "User-agent: GPTBot"
    - path: "showcase/angular/public/llms.txt"
      provides: "llmstxt.org-format short overview pointing crawlers at /about, /support, /privacy, GitHub, llms-full.txt"
      contains: "# FSB (Full Self-Browsing)"
    - path: "showcase/angular/scripts/llms-full.source.md"
      provides: "Hand-curated long-form source markdown that Plan 02's prebuild copies to public/llms-full.txt"
      contains: "## Comparison"
    - path: ".planning/phases/216-crawler-root-files-express-wiring-production-validation/verify-static.sh"
      provides: "Plan-01 verifier: greps the literal bot list, llms.txt opening, llms-full.source.md sections"
  key_links:
    - from: "showcase/angular/public/robots.txt"
      to: "showcase/angular/public/sitemap.xml"
      via: "Sitemap: directive"
      pattern: "Sitemap: https://full-selfbrowsing\\.com/sitemap\\.xml"
    - from: "showcase/angular/scripts/llms-full.source.md"
      to: "showcase/angular/public/llms-full.txt"
      via: "Plan 02 prebuild script copy + size assert"
      pattern: "build-crawler-files\\.mjs.*llms-full\\.source\\.md"
---

<objective>
Hand-author the three static crawler-facing source files: robots.txt (CRAWL-01) with the verbatim 15-bot 2026 allowlist, llms.txt (CRAWL-03) with the verbatim D-01 opening paragraph and Docs section, and llms-full.source.md (per D-05) as the long-form source markdown that Plan 02 will copy to public/llms-full.txt at build time.

Wave 1 placement: this plan writes to public/robots.txt, public/llms.txt, scripts/llms-full.source.md and a per-plan verify script -- all file-disjoint from Plan 02 (which owns scripts/build-crawler-files.mjs + package.json + GENERATED public/sitemap.xml and public/llms-full.txt) and Plan 03 (which owns server/server.js). Parallel-safe.

Purpose: ship the editorial content that AI crawlers ingest. The bot list and llms.txt voice are locked verbatim in REQUIREMENTS.md CRAWL-01 and CONTEXT.md D-01 -- do not paraphrase.

Output: three source files committed to the repo, plus verify-static.sh that asserts their content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/216-crawler-root-files-express-wiring-production-validation/216-CONTEXT.md
@.planning/phases/215-prerender-foundation-per-route-metadata-structured-data/verify.sh
@CLAUDE.md
</context>

<interfaces>
<!-- Existing asset wiring (no change needed; just consumed) -->
showcase/angular/angular.json already globs `public/**` to dist root via the assets config, so any file dropped into showcase/angular/public/ ships to dist/showcase-angular/browser/ on next `npm --prefix showcase/angular run build`. No angular.json edits in this plan.

CONTEXT.md D-01 verbatim opening paragraph (DO NOT paraphrase):
  "FSB (Full Self-Browsing) is an open-source Chrome extension that automates the browser through natural language. You describe a task; FSB plans the clicks, types, and navigation to complete it. Multi-model AI (xAI, OpenAI, Anthropic, Gemini, local), 50+ browser actions, 142+ site guides. MIT-licensed, BYO API key."

CONTEXT.md D-04 llms-full.txt section structure (top-down):
  1. Project description (long-form expansion of llms.txt paragraph)
  2. Capabilities list (action categories, supported AI providers, install posture)
  3. Install instructions (chrome://extensions -> developer mode -> load unpacked, BYO API key)
  4. Key concepts (uniqueness-scored selectors, visual feedback / orange glow, action verification, MCP server surface)
  5. Comparison framing (Browser Use, Project Mariner, Operator -- neutral tone per D-03)
  6. Links section mirroring llms.txt Docs entries

REQUIREMENTS.md CRAWL-01 verbatim bot list (15 names, in this order):
  GPTBot, ChatGPT-User, OAI-SearchBot, ClaudeBot, Claude-User, Claude-SearchBot, PerplexityBot, Perplexity-User, Google-Extended, Applebot-Extended, Amazonbot, Bytespider, CCBot, Meta-ExternalAgent, DuckAssistBot
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Hand-author showcase/angular/public/robots.txt with the verbatim 15-bot allowlist</name>
  <read_first>
    - .planning/REQUIREMENTS.md (CRAWL-01 acceptance criteria with verbatim bot list)
    - .planning/phases/216-crawler-root-files-express-wiring-production-validation/216-CONTEXT.md (D-08 -- robots.txt is hand-authored, prebuild does NOT regenerate it)
    - showcase/angular/angular.json (confirm public/** glob -- no edit needed)
  </read_first>
  <files>showcase/angular/public/robots.txt</files>
  <action>
Create showcase/angular/public/robots.txt with the EXACT body below. Each AI bot gets its own User-agent + Allow: / pair separated by a blank line, then a final wildcard, then the Sitemap line. No emojis, no comments above the directives, ASCII only.

Required exact content (line-by-line):

```
User-agent: GPTBot
Allow: /

User-agent: ChatGPT-User
Allow: /

User-agent: OAI-SearchBot
Allow: /

User-agent: ClaudeBot
Allow: /

User-agent: Claude-User
Allow: /

User-agent: Claude-SearchBot
Allow: /

User-agent: PerplexityBot
Allow: /

User-agent: Perplexity-User
Allow: /

User-agent: Google-Extended
Allow: /

User-agent: Applebot-Extended
Allow: /

User-agent: Amazonbot
Allow: /

User-agent: Bytespider
Allow: /

User-agent: CCBot
Allow: /

User-agent: Meta-ExternalAgent
Allow: /

User-agent: DuckAssistBot
Allow: /

User-agent: *
Allow: /

Sitemap: https://full-selfbrowsing.com/sitemap.xml
```

End with a trailing newline. The order matches REQUIREMENTS.md CRAWL-01. The wildcard `User-agent: *` block is the 16th block; the Sitemap line is the closing line per CONTEXT.md D-08.
  </action>
  <verify>
    <automated>bash -c 'F=showcase/angular/public/robots.txt; for ua in GPTBot ChatGPT-User OAI-SearchBot ClaudeBot Claude-User Claude-SearchBot PerplexityBot Perplexity-User Google-Extended Applebot-Extended Amazonbot Bytespider CCBot Meta-ExternalAgent DuckAssistBot; do grep -q "^User-agent: $ua$" "$F" || { echo "MISSING $ua"; exit 1; }; done; grep -q "^User-agent: \*$" "$F" && grep -q "^Sitemap: https://full-selfbrowsing\.com/sitemap\.xml$" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^User-agent:" showcase/angular/public/robots.txt` returns 16 (15 named bots + wildcard)
    - `grep -q "^User-agent: GPTBot$" showcase/angular/public/robots.txt` exits 0
    - `grep -q "^User-agent: DuckAssistBot$" showcase/angular/public/robots.txt` exits 0
    - `grep -q "^Sitemap: https://full-selfbrowsing\.com/sitemap\.xml$" showcase/angular/public/robots.txt` exits 0
    - `grep -P "[^\x00-\x7F]" showcase/angular/public/robots.txt` returns no matches (ASCII-only)
  </acceptance_criteria>
  <done>robots.txt with the 15-bot verbatim allowlist + wildcard + Sitemap line committed to showcase/angular/public/</done>
</task>

<task type="auto">
  <name>Task 2: Hand-author showcase/angular/public/llms.txt with the verbatim D-01 opening + Docs section</name>
  <read_first>
    - .planning/phases/216-crawler-root-files-express-wiring-production-validation/216-CONTEXT.md (D-01 verbatim paragraph, D-02 count drift note)
    - .planning/REQUIREMENTS.md (CRAWL-03 -- llmstxt.org markdown convention)
    - CLAUDE.md (no emojis policy)
  </read_first>
  <files>showcase/angular/public/llms.txt</files>
  <action>
Create showcase/angular/public/llms.txt following the llmstxt.org convention. Use the EXACT D-01 opening paragraph verbatim -- do not rewrite for "tighter copy" (CONTEXT.md Specifics section explicitly forbids paraphrasing).

Required exact content:

```
# FSB (Full Self-Browsing)

FSB (Full Self-Browsing) is an open-source Chrome extension that automates the browser through natural language. You describe a task; FSB plans the clicks, types, and navigation to complete it. Multi-model AI (xAI, OpenAI, Anthropic, Gemini, local), 50+ browser actions, 142+ site guides. MIT-licensed, BYO API key.

## Docs

- [About](https://full-selfbrowsing.com/about): Watch FSB drive Google, search Amazon, and book travel autonomously.
- [Support](https://full-selfbrowsing.com/support): Setup guides, troubleshooting, GitHub issues, and direct contact.
- [Privacy](https://full-selfbrowsing.com/privacy): How FSB handles your data -- API keys encrypted in Chrome local storage, no telemetry.
- [GitHub repository](https://github.com/lakshmanturlapati/FSB): MIT-licensed source code, install instructions, issue tracker.
- [llms-full.txt](https://full-selfbrowsing.com/llms-full.txt): Long-form project dump for LLM ingestion -- capabilities, install, comparison framing.
```

End with a trailing newline. ASCII only (use `--` for em-dash equivalent, not the Unicode em-dash, to keep the file ASCII-clean and grep-stable). Per D-02, the action/guide counts in the opening paragraph are content claims that must match reality at build time -- if you find these have drifted, leave the literal numbers and surface a follow-up todo (do NOT silently change the verbatim D-01 paragraph).

No emojis anywhere per project CLAUDE.md.
  </action>
  <verify>
    <automated>bash -c 'F=showcase/angular/public/llms.txt; grep -q "^# FSB (Full Self-Browsing)$" "$F" && grep -q "^FSB (Full Self-Browsing) is an open-source Chrome extension that automates the browser through natural language\." "$F" && grep -q "^## Docs$" "$F" && grep -q "https://github.com/lakshmanturlapati/FSB" "$F" && grep -q "/llms-full\.txt" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 showcase/angular/public/llms.txt` equals `# FSB (Full Self-Browsing)`
    - `grep -q "MIT-licensed, BYO API key\." showcase/angular/public/llms.txt` exits 0 (verbatim D-01 closing)
    - `grep -q "^## Docs$" showcase/angular/public/llms.txt` exits 0
    - `grep -c "^- \[" showcase/angular/public/llms.txt` returns 5 (one per Docs link)
    - `grep -P "[^\x00-\x7F]" showcase/angular/public/llms.txt` returns no matches (ASCII-only)
  </acceptance_criteria>
  <done>llms.txt with verbatim D-01 paragraph + Docs section linking 5 destinations committed to showcase/angular/public/</done>
</task>

<task type="auto">
  <name>Task 3: Hand-author showcase/angular/scripts/llms-full.source.md (long-form source per D-04)</name>
  <read_first>
    - .planning/phases/216-crawler-root-files-express-wiring-production-validation/216-CONTEXT.md (D-03 neutral comparison tone, D-04 section structure, D-05 source location)
    - .planning/REQUIREMENTS.md (CRAWL-04 -- llms-full.txt scope and 256KB ceiling)
    - CLAUDE.md (no emojis)
    - showcase/angular/public/llms.txt (Task 2 output -- the Docs links must match the Links section in this source)
  </read_first>
  <files>showcase/angular/scripts/llms-full.source.md</files>
  <action>
Create showcase/angular/scripts/llms-full.source.md as the hand-curated long-form markdown source that Plan 02's prebuild script will copy verbatim (with a generated-at header prepended) to showcase/angular/public/llms-full.txt.

Use EXACTLY the D-04 section structure (six sections, in order):

```
# FSB (Full Self-Browsing)

## 1. Project Description

FSB (Full Self-Browsing) is an open-source Chrome extension that automates the browser through natural language. You describe a task in plain English; FSB plans the clicks, types, and navigation to complete it. The extension runs entirely in your browser with your own API keys -- no backend, no telemetry, no data collection. Multi-model AI (xAI Grok, OpenAI, Anthropic Claude, Google Gemini, local providers), 50+ browser actions, 142+ site-specific guides. MIT-licensed.

[expand to 2-4 paragraphs covering: what FSB does, who it is for (developers, power users, researchers), what makes it open and local-first, the BYO-key model]

## 2. Capabilities

### Action categories
- Navigation: navigate, searchGoogle, refresh, goBack, goForward
- Element interaction: click, rightClick, doubleClick, hover, focus, blur
- Text and input: type, clearInput, selectText, pressEnter, keyPress
- Form controls: selectOption, toggleCheckbox
- Information gathering: getText, getAttribute, setAttribute
- Advanced: waitForElement, moveMouse, scroll, waitForDOMStable, detectLoadingState

### Supported AI providers
- xAI: grok-4-1-fast (recommended), grok-4-1, grok-4, grok-code-fast-1, grok-3, grok-3-mini, grok-2-vision
- OpenAI: GPT-4o, ChatGPT-4o Latest, GPT-4o Mini, GPT-4 Turbo
- Anthropic: Claude Sonnet 4.5, Claude Haiku 4.5, Claude Opus 4.1
- Google: Gemini 2.5 Flash, Gemini 2.5 Pro, Gemini 2.0 Flash (free experimental)
- Custom: any OpenAI-compatible endpoint

### Install posture
- Chrome 88+ (Manifest V3); also runs on Chromium-based browsers (Edge, Brave)
- Single-user, single-machine: extension is local; no shared infrastructure
- BYO API key: stored encrypted in Chrome local storage; never leaves the browser

## 3. Install Instructions

1. Clone the repository: `git clone https://github.com/lakshmanturlapati/FSB.git`
2. Open `chrome://extensions/` in Chrome.
3. Enable Developer Mode (top-right toggle).
4. Click "Load unpacked" and select the cloned FSB directory.
5. Click the FSB icon in the toolbar; open Settings; paste an API key for at least one supported provider.
6. Pin the extension and use the popup or side panel to issue natural-language tasks.

No build step is required for end users -- the extension ships as plain JavaScript per Manifest V3 conventions. Developers contributing to FSB do not need npm to install or run the extension itself; npm is only used by the showcase site under `showcase/angular/`.

## 4. Key Concepts

### Uniqueness-scored selectors
FSB generates multiple CSS-selector candidates per element and ranks them by uniqueness (data-testid > aria-label > unique class > nth-child fallback). The top-ranked selector is tried first; alternates are used if the DOM has shifted between plan and execution. This is the load-bearing mechanism for reliability across DOM mutations.

### Visual feedback (orange glow)
When FSB acts on an element, the page briefly outlines that element in orange. This gives the user immediate visibility into what FSB is doing and lets them intervene by clicking pause if a mistake is in progress.

### Action verification
After every action, FSB checks the DOM for expected post-conditions (URL change, element disappearance, text appearance). If verification fails, FSB does not silently continue -- it surfaces the failure and lets the planner choose between retry, alternate selector, or stop.

### MCP server surface
FSB exposes its tooling through a Model Context Protocol (MCP) server at `server/`. External AI clients (Claude Desktop, custom MCP clients) can drive FSB by speaking MCP, which lets you orchestrate browser tasks from a chat client without writing extension code.

## 5. Comparison

These are factual capability summaries, not endorsements. FSB has overlap and differentiation with each project; the right tool depends on what you need.

### Browser Use
Browser Use is an open-source library and CLI for headless browser automation driven by LLMs. It runs in Node.js / Python contexts and targets server-side automation pipelines. Overlap with FSB: natural-language task execution against a browser, action-verification loops. Difference from FSB: Browser Use is a server-side library, not a Chrome extension; it operates a headless browser the user does not see, while FSB drives the user's actual browser session with the user's logged-in cookies.

### Project Mariner
Project Mariner is Google's research agent for autonomous browser tasks (announced late 2024). Overlap with FSB: same problem statement -- "AI plans clicks and types to complete a goal in a real browser." Difference from FSB: Mariner is a Google research preview tied to Google infrastructure and Gemini, not generally available, not open source. FSB is open source, BYO key, runs locally, and supports multiple model providers.

### Operator
Operator is OpenAI's agentic browser product (2025). Overlap with FSB: end-user-facing browser automation driven by an LLM. Difference from FSB: Operator runs in OpenAI's hosted environment with its own browser instance; the user does not control which model, browser, or storage is used. FSB runs in the user's own browser with the user's own keys and storage; the user owns the session and the data.

## 6. Links

- Home: https://full-selfbrowsing.com
- About: https://full-selfbrowsing.com/about
- Support: https://full-selfbrowsing.com/support
- Privacy: https://full-selfbrowsing.com/privacy
- GitHub: https://github.com/lakshmanturlapati/FSB
- llms.txt: https://full-selfbrowsing.com/llms.txt
```

Expand sections 1, 2, and 4 enough to give an AI summarizer real substance, but keep total file size well under the 256KB ceiling (target 8-30KB; the prebuild script will assert <256000 bytes per D-05). ASCII only (use `--` for em-dash). No emojis per project CLAUDE.md.

Comparison tone (D-03): neutral capability-matrix language. NO "better than" claims. The summary above is the locked tone -- expand within that frame.
  </action>
  <verify>
    <automated>bash -c 'F=showcase/angular/scripts/llms-full.source.md; grep -q "^# FSB (Full Self-Browsing)$" "$F" && grep -q "^## 1\. Project Description$" "$F" && grep -q "^## 2\. Capabilities$" "$F" && grep -q "^## 3\. Install Instructions$" "$F" && grep -q "^## 4\. Key Concepts$" "$F" && grep -q "^## 5\. Comparison$" "$F" && grep -q "^## 6\. Links$" "$F" && grep -q "Browser Use" "$F" && grep -q "Project Mariner" "$F" && grep -q "Operator" "$F" && [ "$(wc -c < "$F")" -lt 256000 ]'</automated>
  </verify>
  <acceptance_criteria>
    - `wc -c < showcase/angular/scripts/llms-full.source.md` returns a value less than 256000
    - All six section headings (`## 1.` through `## 6.`) appear in order
    - Comparison section names exactly: Browser Use, Project Mariner, Operator
    - `grep -P "[^\x00-\x7F]" showcase/angular/scripts/llms-full.source.md` returns no matches (ASCII-only)
    - File contains no emoji characters (covered by ASCII-only check)
    - `grep -qE "^- (Home|About|Support|Privacy|GitHub|llms\.txt):" showcase/angular/scripts/llms-full.source.md` returns matches for at least the 6 links
  </acceptance_criteria>
  <done>llms-full.source.md with six D-04 sections + neutral comparison tone committed to showcase/angular/scripts/</done>
</task>

<task type="auto">
  <name>Task 4: Add verify-static.sh asserting Plan 01 outputs</name>
  <read_first>
    - .planning/phases/215-prerender-foundation-per-route-metadata-structured-data/verify-pre.sh (proven pattern from Phase 215 Plan 01)
    - .planning/phases/215-prerender-foundation-per-route-metadata-structured-data/verify.sh (umbrella shape)
    - showcase/angular/public/robots.txt (Task 1 output)
    - showcase/angular/public/llms.txt (Task 2 output)
    - showcase/angular/scripts/llms-full.source.md (Task 3 output)
  </read_first>
  <files>.planning/phases/216-crawler-root-files-express-wiring-production-validation/verify-static.sh</files>
  <action>
Create verify-static.sh in the phase directory, chmod +x, that asserts CRAWL-01 and CRAWL-03 source-file content. Use the same shape as Phase 215's verify-pre.sh: bash with `set -euo pipefail`, echo `[216] CHECK: ...` lines per assertion, exit 0 with `[216] verify-static.sh: ALL PASSED` on success.

Required assertions:

1. CRAWL-01-A: robots.txt has all 15 named User-agent blocks. For each of the 15 names (GPTBot, ChatGPT-User, OAI-SearchBot, ClaudeBot, Claude-User, Claude-SearchBot, PerplexityBot, Perplexity-User, Google-Extended, Applebot-Extended, Amazonbot, Bytespider, CCBot, Meta-ExternalAgent, DuckAssistBot), `grep -q "^User-agent: <name>$" showcase/angular/public/robots.txt` must exit 0.

2. CRAWL-01-B: robots.txt has the wildcard block `^User-agent: \*$` and the Sitemap line `^Sitemap: https://full-selfbrowsing\.com/sitemap\.xml$`.

3. CRAWL-01-C: robots.txt User-agent block count equals 16: `[ "$(grep -c '^User-agent:' showcase/angular/public/robots.txt)" -eq 16 ]`.

4. CRAWL-03-A: llms.txt opens with the verbatim H1 -- `head -1 showcase/angular/public/llms.txt` equals `# FSB (Full Self-Browsing)`.

5. CRAWL-03-B: llms.txt contains the verbatim D-01 closing fragment -- `grep -q "MIT-licensed, BYO API key\." showcase/angular/public/llms.txt`.

6. CRAWL-03-C: llms.txt has the Docs section and 5 link entries -- `grep -q "^## Docs$" showcase/angular/public/llms.txt` and `[ "$(grep -c '^- \[' showcase/angular/public/llms.txt)" -eq 5 ]`.

7. CRAWL-04-source-A: llms-full.source.md exists and has all six numbered sections in order. Use awk or six grep -q calls.

8. CRAWL-04-source-B: llms-full.source.md size is under 256000 bytes -- `[ "$(wc -c < showcase/angular/scripts/llms-full.source.md)" -lt 256000 ]`.

9. CRAWL-04-source-C: comparison section names all three competitors -- grep -q for each of "Browser Use", "Project Mariner", "Operator".

10. ASCII-only on all three files -- `! grep -qP "[^\x00-\x7F]" <file>` per file. (No emojis, no Unicode em-dash; matches CLAUDE.md emoji policy and keeps grep stable.)

End with `echo "[216] verify-static.sh: CRAWL-01, CRAWL-03 source ALL PASSED"`.

Also update the phase umbrella verify.sh (create it if absent) following the Phase 215 pattern: source verify-static.sh unconditionally; conditionally source verify-prebuild.sh, verify-server.sh, verify-smoke.sh, verify-uat.sh if present (using `[ -f path ] && bash path` semantics so partial Wave 1 runs do not break). DO NOT create those four downstream scripts in this plan -- they belong to Plans 02-05.
  </action>
  <verify>
    <automated>bash .planning/phases/216-crawler-root-files-express-wiring-production-validation/verify-static.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash .planning/phases/216-crawler-root-files-express-wiring-production-validation/verify-static.sh` exits 0
    - The script's final stdout line contains `ALL PASSED`
    - Removing any one of the 15 bot User-agent lines causes the script to exit non-zero (proves the assertion is real, not vacuous)
    - `[ -x .planning/phases/216-crawler-root-files-express-wiring-production-validation/verify-static.sh ]` succeeds (executable bit set)
    - Umbrella verify.sh exists, is executable, and exits 0 when only verify-static.sh is present
  </acceptance_criteria>
  <done>verify-static.sh + umbrella verify.sh committed and exit 0 against Tasks 1-3 outputs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo author -> committed source files | The three files in this plan are static editorial content. No untrusted input. |
| Plan 02 prebuild script -> public/llms-full.txt | Plan 02 reads scripts/llms-full.source.md as a trusted source and copies it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-216-S1 | Spoofing | robots.txt allowlist drift | accept | Hand-authored per CONTEXT.md D-08; bot list is verified 2026 universe; drift is a deliberate edit, caught by code review and verify-static.sh CRAWL-01-A grep |
| T-216-T1 | Tampering | llms.txt verbatim opening | mitigate | verify-static.sh CRAWL-03-B greps for the verbatim D-01 closing fragment; any paraphrase fails the assertion |
| T-LD-01 | (carried from Phase 215) | JSON-LD payload escaping | (no new code in this plan emits JSON-LD; threat surface unchanged from Phase 215 mitigations) | n/a here -- validated by Phase 216 Plan 04 (smoke) and Plan 05 (Rich Results manual UAT) |

T-216-01 (crawler file shadowing by SPA fallback) is addressed in Plan 03's verify-server.sh, not here -- this plan only creates source files; serving behavior is Plan 03's threat surface.
</threat_model>

<verification>
Run `bash .planning/phases/216-crawler-root-files-express-wiring-production-validation/verify-static.sh`. Exit 0 means CRAWL-01 source + CRAWL-03 source + CRAWL-04 source assertions all pass. After Plans 02-05 land, the umbrella verify.sh chains all five sub-verifiers in order.
</verification>

<success_criteria>
- showcase/angular/public/robots.txt exists with the verbatim 15-bot allowlist + wildcard + Sitemap line
- showcase/angular/public/llms.txt exists with the verbatim D-01 opening + Docs section linking to /about, /support, /privacy, GitHub, llms-full.txt
- showcase/angular/scripts/llms-full.source.md exists with the six D-04 sections + neutral D-03 comparison tone, under 256KB
- verify-static.sh exits 0 and has real assertions (not vacuous)
- All three files are ASCII-only and emoji-free
</success_criteria>

<output>
After completion, create `.planning/phases/216-crawler-root-files-express-wiring-production-validation/216-01-SUMMARY.md` documenting the three files shipped, byte counts, and any drift notes (e.g., if the 50+/142+ counts in D-01 turned out to be inaccurate at build time, surface a follow-up todo per D-02).
</output>
