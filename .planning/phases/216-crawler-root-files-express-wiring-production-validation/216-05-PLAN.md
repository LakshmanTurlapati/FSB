---
phase: 216
plan: 05
type: execute
wave: 3
depends_on: [216-04]
files_modified:
  - .planning/phases/216-crawler-root-files-express-wiring-production-validation/216-HUMAN-UAT.md
  - .planning/phases/216-crawler-root-files-express-wiring-production-validation/verify-uat.sh
autonomous: true
requirements: [LD-03, SMOKE-04]
must_haves:
  truths:
    - "216-HUMAN-UAT.md exists in the phase directory with one entry per Search Console live-URL test (/, /about, /privacy, /support) and one entry for Rich Results Test on the home page"
    - "Each entry has: title, URL to test, tool URL (search.google.com/test/rich-results or Search Console), expected outcome, and a results-capture stub (status, evidence link, date)"
    - "verify-uat.sh asserts the UAT file exists with the 5 expected entries scaffolded (4 Search Console + 1 Rich Results)"
    - "Plan does not perform the UAT itself -- it scaffolds the artifact for the human operator to fill in post-deploy"
  artifacts:
    - path: ".planning/phases/216-crawler-root-files-express-wiring-production-validation/216-HUMAN-UAT.md"
      provides: "Manual UAT scaffold with 5 entries (4 Search Console live-URL tests + 1 Rich Results Test) for the human operator to execute and record results in"
      contains: "Rich Results Test"
    - path: ".planning/phases/216-crawler-root-files-express-wiring-production-validation/verify-uat.sh"
      provides: "Plan-05 verifier: asserts the UAT scaffold file exists with the 5 entries"
  key_links:
    - from: ".planning/phases/216-crawler-root-files-express-wiring-production-validation/216-HUMAN-UAT.md"
      to: "https://search.google.com/test/rich-results"
      via: "Tool URL referenced in the Rich Results entry"
      pattern: "search\\.google\\.com/test/rich-results"
    - from: ".planning/phases/216-crawler-root-files-express-wiring-production-validation/216-HUMAN-UAT.md"
      to: "Google Search Console live-URL test"
      via: "Tool URL referenced in each Search Console entry"
      pattern: "search\\.google\\.com/search-console"
---

<objective>
Scaffold the human-UAT artifact for SMOKE-04 (Google Search Console "Test live URL" passes for /, /about, /privacy, /support) and LD-03 (Rich Results Test on the home page passes with zero errors and zero warnings). These checks require a Google account, browser session, and the production deployment to be live -- they cannot be scripted (CONTEXT.md D-13). This plan does not run the UAT; it commits the scaffold so when the human operator runs the checks post-deploy, results are recorded in a structured place.

Wave 3 placement: depends on Plan 04 only because SMOKE-04 / LD-03 are conceptually the last verification gates -- they run against the deployed production site, after the local smoke (Plan 04) has signed off the build. No file overlap with any earlier plan; the plan only writes to the phase directory.

Purpose: per CONTEXT.md D-13, manual UAT items go into a 216-HUMAN-UAT.md file after execution. This plan creates the scaffold ahead of execution so the operator has a defined place to capture: status (pass/fail), evidence link (screenshot URL, archived page), and date. Without the scaffold, results are scattered in chat or commit messages and the milestone gate is fuzzy.

Output: 216-HUMAN-UAT.md scaffold with 5 entries + verify-uat.sh that asserts the scaffold's existence and structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/216-crawler-root-files-express-wiring-production-validation/216-CONTEXT.md
@CLAUDE.md
</context>

<interfaces>
<!-- REQUIREMENTS.md SMOKE-04 + LD-03 acceptance criteria -->

LD-03: All emitted JSON-LD passes Google's Rich Results Test (https://search.google.com/test/rich-results) on the deployed URL with zero errors and zero warnings.

SMOKE-04: Google Search Console "Test live URL" passes for /, /about, /privacy, /support (rendered HTML matches prerendered HTML; no `noindex` leaks; no JavaScript-only content); Rich Results Test passes for the home page's Organization and SoftwareApplication JSON-LD with zero errors.

<!-- ROADMAP.md Phase 216 success criterion 5 -->
Google's Rich Results Test on the deployed home URL detects exactly one Organization and exactly one SoftwareApplication block with zero errors and zero warnings; Search Console "Test live URL" passes for /, /about, /privacy, /support.

<!-- D-13: SMOKE-04 + LD-03 are MANUAL UAT items -->
"They require browser sessions and Google account auth and cannot be scripted."

<!-- Phase 215 Plan 03 SUMMARY confirms which JSON-LD blocks Rich Results Test will encounter on the home page -->
- Organization JSON-LD: in src/index.html, inherited by every prerendered route (1 block on every route)
- SoftwareApplication JSON-LD: in home component, injected via Renderer2 (1 block, home only)
- Home page therefore has 2 JSON-LD blocks; about/privacy/support have 1 each (Organization only)
- Rich Results Test on the home URL should detect exactly: 1 Organization + 1 SoftwareApplication, 0 errors, 0 warnings
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 216-HUMAN-UAT.md with 5 entries (4 Search Console + 1 Rich Results)</name>
  <read_first>
    - .planning/REQUIREMENTS.md (LD-03 + SMOKE-04 verbatim acceptance criteria)
    - .planning/phases/216-crawler-root-files-express-wiring-production-validation/216-CONTEXT.md (D-13 -- this is a manual UAT item)
    - .planning/ROADMAP.md (Phase 216 success criterion 5 -- "exactly one Organization and exactly one SoftwareApplication block")
    - .planning/phases/215-prerender-foundation-per-route-metadata-structured-data/215-03-SUMMARY.md (which JSON-LD blocks live where)
    - CLAUDE.md (no emojis)
  </read_first>
  <files>.planning/phases/216-crawler-root-files-express-wiring-production-validation/216-HUMAN-UAT.md</files>
  <action>
Create the phase HUMAN-UAT.md scaffold. Five entries: four Search Console live-URL tests (one per marketing route) plus one Rich Results Test for the home page. Each entry has the same structure: title, what to test, tool URL, expected outcome, results-capture block (status / evidence / date) with placeholder values for the operator to fill in.

Required exact content:

```markdown
# Phase 216 Human UAT

This file tracks manual validation checks that require a Google account, browser session, and a live production deploy. Per CONTEXT.md D-13 these cannot be scripted. The automated smoke (Plan 04 verify-smoke.sh + smoke-crawler.mjs) is the file-level gate; this UAT covers the rendering-engine and structured-data validators that only Google itself can run.

Run order: deploy first; verify automated smoke against production with `BASE_URL=https://full-selfbrowsing.com npm --prefix showcase/angular run smoke:crawler`; THEN run the five UAT entries below. Update each entry's Status / Evidence / Date fields when complete.

<!-- UAT entry separator -->

## UAT-216-01: Search Console "Test live URL" -- Home (/)

- **Requirement:** SMOKE-04
- **URL to test:** https://full-selfbrowsing.com/
- **Tool:** Google Search Console -> Inspect URL -> Test Live URL (https://search.google.com/search-console)
- **Expected outcome:** "URL is available to Google" with rendered HTML matching the prerendered HTML; no `noindex` warnings; no "JavaScript-only content" notes; the rendered DOM contains the route-specific title `FSB -- Full Self-Browsing` and the JSON-LD blocks (Organization + SoftwareApplication).
- **Status:** [ ] not yet run
- **Evidence:** _(operator: paste Search Console screenshot URL or archive link here)_
- **Date:** _(YYYY-MM-DD when run)_

<!-- UAT entry separator -->

## UAT-216-02: Search Console "Test live URL" -- About (/about)

- **Requirement:** SMOKE-04
- **URL to test:** https://full-selfbrowsing.com/about
- **Tool:** Google Search Console -> Inspect URL -> Test Live URL (https://search.google.com/search-console)
- **Expected outcome:** "URL is available to Google"; rendered HTML matches prerendered HTML; route-specific title `FSB -- About` and route-specific canonical (`https://full-selfbrowsing.com/about`); no `noindex`; no JavaScript-only-content note.
- **Status:** [ ] not yet run
- **Evidence:** _(operator: paste screenshot or archive link)_
- **Date:** _(YYYY-MM-DD when run)_

<!-- UAT entry separator -->

## UAT-216-03: Search Console "Test live URL" -- Privacy (/privacy)

- **Requirement:** SMOKE-04
- **URL to test:** https://full-selfbrowsing.com/privacy
- **Tool:** Google Search Console -> Inspect URL -> Test Live URL (https://search.google.com/search-console)
- **Expected outcome:** "URL is available to Google"; rendered HTML matches prerendered HTML; route-specific title `FSB -- Privacy` and canonical `https://full-selfbrowsing.com/privacy`; no `noindex`; no JavaScript-only-content note.
- **Status:** [ ] not yet run
- **Evidence:** _(operator: paste screenshot or archive link)_
- **Date:** _(YYYY-MM-DD when run)_

<!-- UAT entry separator -->

## UAT-216-04: Search Console "Test live URL" -- Support (/support)

- **Requirement:** SMOKE-04
- **URL to test:** https://full-selfbrowsing.com/support
- **Tool:** Google Search Console -> Inspect URL -> Test Live URL (https://search.google.com/search-console)
- **Expected outcome:** "URL is available to Google"; rendered HTML matches prerendered HTML; route-specific title `FSB -- Support` and canonical `https://full-selfbrowsing.com/support`; no `noindex`; no JavaScript-only-content note.
- **Status:** [ ] not yet run
- **Evidence:** _(operator: paste screenshot or archive link)_
- **Date:** _(YYYY-MM-DD when run)_

<!-- UAT entry separator -->

## UAT-216-05: Rich Results Test -- Home page JSON-LD

- **Requirement:** LD-03 + SMOKE-04 (Rich Results clause)
- **URL to test:** https://full-selfbrowsing.com/
- **Tool:** Rich Results Test (https://search.google.com/test/rich-results)
- **Expected outcome:** Detected items list contains EXACTLY one `Organization` block and EXACTLY one `SoftwareApplication` block (Phase 215 LD-01 + LD-02). Zero errors. Zero warnings. The Organization block carries `name: "FSB"`, `alternateName: "Full Self-Browsing"`, `url: "https://full-selfbrowsing.com"`, `logo: "https://full-selfbrowsing.com/assets/fsb_logo_dark.png"`, `sameAs: ["https://github.com/lakshmanturlapati/FSB"]`. The SoftwareApplication block carries `applicationCategory: "BrowserApplication"`, `operatingSystem: "Chrome"`, `offers.price: "0"`, `softwareVersion` matching manifest.json's version field, and `publisher.@id` cross-referencing the Organization's @id.
- **Status:** [ ] not yet run
- **Evidence:** _(operator: paste Rich Results Test screenshot URL or archive link; include the "View tested page" rendered HTML if there are any warnings)_
- **Date:** _(YYYY-MM-DD when run)_

<!-- UAT entry separator -->

## Sign-off

Once all five entries above are Status: PASSED, Phase 216 is complete and the v0.9.46 milestone is signed off. If any entry fails, follow the gap-closure path: open a follow-up phase (216.1 or 217) to address the specific finding, do NOT silently update the entry's expected outcome.
```

Notes:
- ASCII-only (use `--` for em-dash equivalents in title fragments per Phase 215 D-01 ASCII-clean source convention; the actual prerendered HTML has the UTF-8 em-dash, but this UAT scaffold is a markdown checklist where ASCII is fine and grep-friendly).
- No emojis per project CLAUDE.md.
- The "Status: [ ] not yet run" checkbox uses standard markdown task-list syntax so the operator can switch to `[x]` when complete.
- The scaffold deliberately includes prefilled "Expected outcome" text so the operator does not need to re-derive what to look for from REQUIREMENTS.md.
- The "Sign-off" footer is the milestone gate -- referenced by ROADMAP.md when Phase 216 is marked complete.
  </action>
  <verify>
    <automated>bash -c 'F=.planning/phases/216-crawler-root-files-express-wiring-production-validation/216-HUMAN-UAT.md; test -f "$F" && grep -q "^## UAT-216-01:" "$F" && grep -q "^## UAT-216-02:" "$F" && grep -q "^## UAT-216-03:" "$F" && grep -q "^## UAT-216-04:" "$F" && grep -q "^## UAT-216-05:" "$F" && grep -q "search\.google\.com/test/rich-results" "$F" && grep -q "search\.google\.com/search-console" "$F" && grep -q "Organization" "$F" && grep -q "SoftwareApplication" "$F" && [ "$(grep -c "^## UAT-216-" "$F")" -eq 5 ]'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/216-crawler-root-files-express-wiring-production-validation/216-HUMAN-UAT.md` exists
    - File contains exactly 5 second-level UAT headings (`^## UAT-216-` count equals 5)
    - All four Search Console entries reference https://search.google.com/search-console
    - The Rich Results entry references https://search.google.com/test/rich-results AND mentions both Organization and SoftwareApplication
    - Each entry has the four expected fields: Requirement, URL to test, Tool, Expected outcome, Status, Evidence, Date
    - File contains no emojis (ASCII-only check via `! grep -qP "[^\x00-\x7F]" $F`)
    - Sign-off footer is present
  </acceptance_criteria>
  <done>216-HUMAN-UAT.md scaffold committed in phase directory with 5 entries</done>
</task>

<task type="auto">
  <name>Task 2: Add verify-uat.sh and finalize umbrella verify.sh</name>
  <read_first>
    - .planning/phases/216-crawler-root-files-express-wiring-production-validation/216-HUMAN-UAT.md (Task 1 output)
    - .planning/phases/215-prerender-foundation-per-route-metadata-structured-data/verify.sh (umbrella pattern)
    - All four prior verify-*.sh in the phase directory (verify-static, verify-prebuild, verify-server, verify-smoke -- to confirm umbrella sourcing order)
  </read_first>
  <files>.planning/phases/216-crawler-root-files-express-wiring-production-validation/verify-uat.sh</files>
  <action>
Create verify-uat.sh in the phase directory, chmod +x. Set `set -euo pipefail`. Echo `[216] CHECK: ...`.

Required assertions:

1. UAT-FILE-EXISTS: `[ -f .planning/phases/216-crawler-root-files-express-wiring-production-validation/216-HUMAN-UAT.md ]`.

2. UAT-COUNT: file has exactly 5 second-level UAT headings -- `[ "$(grep -c '^## UAT-216-' <file>)" -eq 5 ]`.

3. UAT-RICH-RESULTS: file references the Rich Results Test URL -- `grep -q "search\.google\.com/test/rich-results" <file>`.

4. UAT-SEARCH-CONSOLE: file references the Search Console URL -- `grep -q "search\.google\.com/search-console" <file>`.

5. UAT-LD-COVERAGE: Rich Results entry mentions both `Organization` and `SoftwareApplication` -- two grep -q calls.

6. UAT-ASCII: file is ASCII-only -- `! grep -qP "[^\x00-\x7F]" <file>` (no emojis per CLAUDE.md).

7. UAT-SIGNOFF: file has the Sign-off footer -- `grep -q "^## Sign-off" <file>`.

End with `echo "[216] verify-uat.sh: LD-03, SMOKE-04 scaffold ALL PRESENT"`.

Note: this verifier asserts the SCAFFOLD exists, NOT that the UAT has been run. The status checkboxes (`[ ] not yet run`) remain unchecked until the operator runs the live tests post-deploy. After UAT execution, the operator updates the file in-place (changing `[ ]` to `[x]` and filling Evidence + Date); a future verify pass could optionally assert all checkboxes are `[x]`, but that is the operator's milestone gate, not Plan 05's deliverable.

Update the umbrella verify.sh in the phase directory to source verify-uat.sh after verify-smoke.sh. Final umbrella sourcing order:
   verify-static.sh -> verify-prebuild.sh -> verify-server.sh -> verify-smoke.sh -> verify-uat.sh

Use the Phase 215 pattern: each sub-script sourced via `[ -f path ] && bash path` (so partial runs do not break, and missing scripts surface as "informationally skipped" lines).

Final umbrella stdout shape on a full pass:
```
[216 umbrella] running available sub-verifiers
[216] verify-static.sh: ... ALL PASSED
[216] verify-prebuild.sh: ... ALL PASSED
[216] verify-server.sh: ... ALL PASSED
[216] verify-smoke.sh: ... ALL PASSED
[216] verify-uat.sh: LD-03, SMOKE-04 scaffold ALL PRESENT
[216 umbrella] ALL AVAILABLE ASSERTIONS PASSED
```
  </action>
  <verify>
    <automated>bash .planning/phases/216-crawler-root-files-express-wiring-production-validation/verify-uat.sh && bash .planning/phases/216-crawler-root-files-express-wiring-production-validation/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash .planning/phases/216-crawler-root-files-express-wiring-production-validation/verify-uat.sh` exits 0
    - The script's final stdout line contains `ALL PRESENT`
    - Umbrella verify.sh exits 0 with all five sub-verifiers chained
    - Removing the Rich Results entry from 216-HUMAN-UAT.md causes verify-uat.sh to exit non-zero (proves the assertion is real)
    - Adding an emoji to the UAT file causes the ASCII assertion to fail (proves CLAUDE.md compliance is enforced)
    - `[ -x verify-uat.sh ]` succeeds
  </acceptance_criteria>
  <done>verify-uat.sh + final umbrella verify.sh committed; full Plan-01-through-Plan-05 chain exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> committed UAT scaffold | Editorial document; no untrusted input. |
| Operator's manual verdict -> phase sign-off | The human operator's judgment on Search Console + Rich Results outcomes is the milestone gate; falsifying these would be intentional repo tampering, out of scope for an automated threat model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-216-T5 | Tampering | UAT entries silently weakened or removed before sign-off | mitigate | verify-uat.sh asserts the 5 expected entries exist by heading count and by tool-URL grep; removing or renaming an entry causes the verifier to fail. Combined with the umbrella verify.sh chaining, any pre-deploy CI run catches this. |
| T-216-R1 | Repudiation | Operator marks UAT [x] without actually running the test | accept | This is a process-level concern (trust the operator); the Evidence field requires a screenshot URL or archive link, which provides audit trail. Future enhancement: require Evidence to be a non-empty value before sign-off (not enforced in v0.9.46). |

T-LD-01 (Phase 215 carry-forward): the Rich Results Test (UAT-216-05) IS the external-validation gate for T-LD-01. The Phase 215 verify-ld.sh asserted source-level escaping (`</` -> `\u003c/`) and grep-level absence of `</script>` injection on disk; this UAT closes the loop by having Google's parser confirm the JSON-LD payload semantically validates with zero errors and zero warnings.

T-216-01 (crawler file shadowing): not in scope here -- enforced by Plan 03 verify-server.sh and Plan 04 smoke-crawler.mjs.
</threat_model>

<verification>
Run `bash .planning/phases/216-crawler-root-files-express-wiring-production-validation/verify.sh`. Final umbrella sources all five sub-verifiers. Exit 0 means every Plan 01-05 file-level and runtime-level assertion passes; the human-UAT scaffold is in place and ready for the operator to fill in post-deploy.

Phase 216 is "code complete" when this umbrella exits 0. The milestone is "shipped" when 216-HUMAN-UAT.md has all five Status fields set to `[x]` with Evidence links, per the Sign-off footer.
</verification>

<success_criteria>
- 216-HUMAN-UAT.md exists with 5 entries (4 Search Console live-URL tests + 1 Rich Results Test on home)
- Each entry references the correct tool URL and lists expected outcomes verbatim from REQUIREMENTS.md SMOKE-04 + LD-03
- verify-uat.sh asserts the scaffold structure and exits 0
- Final umbrella verify.sh chains all five sub-verifiers and exits 0
- File is ASCII-only and emoji-free per CLAUDE.md
</success_criteria>

<output>
After completion, create `.planning/phases/216-crawler-root-files-express-wiring-production-validation/216-05-SUMMARY.md` documenting: scaffold structure, links to the deployment that the operator should test against once it ships, and a brief note that the UAT entries themselves remain `[ ] not yet run` until the operator executes them post-deploy.
</output>
