---
phase: 103-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/103-validation/extract-test-cases.js
  - .planning/phases/103-validation/VALIDATION-RUNNER.md
autonomous: true
requirements: [VALID-01, VALID-02, VALID-03, VALID-04]

must_haves:
  truths:
    - "All 50 v0.9.7 edge case prompts are listed in VALIDATION-RUNNER.md with autopilot-adapted wording"
    - "Each test case has tracking columns for pass/fail, CLI parse failures, and completion validation accuracy"
    - "Metrics formulas calculate pass rate, parse failure rate, and completion accuracy from filled-in results"
    - "User can fill in results row-by-row as they manually test each prompt in the browser"
  artifacts:
    - path: ".planning/phases/103-validation/extract-test-cases.js"
      provides: "Node.js script that reads 50 DIAGNOSTIC.md files and generates VALIDATION-RUNNER.md"
      min_lines: 80
    - path: ".planning/phases/103-validation/VALIDATION-RUNNER.md"
      provides: "Complete test harness with 50 test cases, tracking columns, and metrics section"
      contains: "CANVAS-01"
  key_links:
    - from: "extract-test-cases.js"
      to: ".planning/phases/{47-96}-*/*-DIAGNOSTIC.md"
      via: "fs.readFileSync parsing Prompt Executed and Requirement fields"
      pattern: "## Prompt Executed"
    - from: "extract-test-cases.js"
      to: "VALIDATION-RUNNER.md"
      via: "fs.writeFileSync generating markdown table"
      pattern: "writeFileSync"
---

<objective>
Build the Phase 103 validation test harness: a Node.js script that extracts all 50 edge case prompts from v0.9.7 diagnostic reports (phases 47-96), adapts them for autopilot mode, and generates a structured VALIDATION-RUNNER.md with test case listings, result tracking columns, and milestone gate metrics.

Purpose: Enable the user to systematically run all 50 edge cases through autopilot mode, record pass/fail outcomes, and measure whether the v0.9.8 milestone gates are met (90%+ pass rate, <5% parse failures, 90%+ completion accuracy).

Output: extract-test-cases.js (generator script) + VALIDATION-RUNNER.md (the test harness document)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-validation/103-CONTEXT.md
@.planning/phases/47-tradingview-fibonacci/47-DIAGNOSTIC.md
@.planning/phases/87-freeware-download-ad-avoidance/87-DIAGNOSTIC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build extraction script that parses 50 diagnostics and generates VALIDATION-RUNNER.md</name>
  <files>
    .planning/phases/103-validation/extract-test-cases.js
  </files>
  <read_first>
    .planning/phases/47-tradingview-fibonacci/47-DIAGNOSTIC.md
    .planning/phases/87-freeware-download-ad-avoidance/87-DIAGNOSTIC.md
    .planning/phases/67-twitter-150th-post-extraction/67-DIAGNOSTIC.md
  </read_first>
  <action>
Create a Node.js script at .planning/phases/103-validation/extract-test-cases.js that:

1. Scans .planning/phases/ for directories matching pattern {47-96}-* (the 50 v0.9.7 diagnostic phases)
2. For each directory, reads the {phase}-DIAGNOSTIC.md file
3. Extracts these fields from each diagnostic:
   - Phase number (from directory name, e.g., "47")
   - Phase description (from directory name after the number, e.g., "tradingview-fibonacci")
   - Requirement ID (from "- Requirement: CANVAS-01" line under ## Metadata)
   - MCP Outcome (from "- Outcome: PASS" or "- Outcome: PARTIAL" line under ## Metadata -- take only the first word after "Outcome: ")
   - Prompt text (the quoted string on the line immediately after "## Prompt Executed")
4. Adapts each prompt for autopilot mode by applying these regex replacements:
   - Remove " using MCP manual tools" (7 prompts have this exact suffix before the closing quote)
   - Remove " via MCP manual tools" (3 prompts have this exact suffix before the closing quote)
   - Remove surrounding double quotes from the prompt string
5. Sorts test cases by category then number: CANVAS-01 through CANVAS-10, MICRO-01 through MICRO-10, SCROLL-01 through SCROLL-10, CONTEXT-01 through CONTEXT-10, DARK-01 through DARK-10
6. Generates VALIDATION-RUNNER.md in the same directory with this structure:

```
# Phase 103: Autopilot Validation Runner

**Generated:** {ISO date}
**Test Cases:** 50
**Milestone Gate:** 90%+ pass rate (45/50 minimum)

## How To Use

1. Open the FSB extension in Chrome with autopilot mode enabled
2. For each test case below, paste the Autopilot Prompt into the FSB chat input
3. Let autopilot execute the task
4. Record results in the tracking columns:
   - **Result**: PASS, FAIL, or SKIP (with reason)
   - **CLI Parse Failures**: Count of CLI parse errors during the run (check console/logs)
   - **Completion Correct**: YES if autopilot's done/not-done determination matched reality, NO if it declared done when not done or vice versa
   - **Notes**: Any observations about the run
5. After all 50 tests, scroll to the Metrics section for automatic tallies

## Test Cases

### CANVAS (01-10)

| # | Req ID | Phase | Autopilot Prompt | MCP Baseline | Result | CLI Parse Failures | Completion Correct | Notes |
|---|--------|-------|------------------|--------------|--------|-------------------|-------------------|-------|
| 1 | CANVAS-01 | 47 | {adapted prompt} | PASS | | | | |
... (all 10 CANVAS rows)

### MICRO-INTERACTION (01-10)

| # | Req ID | Phase | Autopilot Prompt | MCP Baseline | Result | CLI Parse Failures | Completion Correct | Notes |
... (all 10 MICRO rows)

### SCROLL (01-10)
... (all 10 SCROLL rows)

### CONTEXT (01-10)
... (all 10 CONTEXT rows)

### DARK (01-10)
... (all 10 DARK rows)

## Metrics

### VALID-02: Pass Rate
- Total tests: 50
- Passed: ___ / 50
- Pass rate: ___% (gate: 90% / 45 minimum)
- Status: [ ] GATE MET / [ ] GATE NOT MET

### VALID-03: CLI Parse Failure Rate
- Total CLI actions across all 50 runs: ___
- Total CLI parse failures: ___
- Parse failure rate: ___% (gate: <5%)
- Status: [ ] GATE MET / [ ] GATE NOT MET

### VALID-04: Completion Accuracy
- Total completion judgments: 50
- Correct judgments: ___ / 50
- Accuracy: ___% (gate: 90%+)
- Status: [ ] GATE MET / [ ] GATE NOT MET

### Overall Milestone Gate
- [ ] VALID-02: Pass rate >= 90%
- [ ] VALID-03: Parse failure rate < 5%
- [ ] VALID-04: Completion accuracy >= 90%
- [ ] ALL GATES MET -- v0.9.8 milestone complete
```

The script must use only Node.js built-in modules (fs, path). No npm dependencies.
The script must handle edge cases: missing DIAGNOSTIC.md files (skip with warning), prompts without MCP phrasing (use as-is), and Outcome fields with parenthetical details after the first word (extract only first word: PASS, PARTIAL, FAIL, SKIP-AUTH).

Run the script with: node .planning/phases/103-validation/extract-test-cases.js
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Extensions/FSB && node .planning/phases/103-validation/extract-test-cases.js && grep -c "CANVAS-01" .planning/phases/103-validation/VALIDATION-RUNNER.md && grep -c "DARK-10" .planning/phases/103-validation/VALIDATION-RUNNER.md && grep -c "| Result |" .planning/phases/103-validation/VALIDATION-RUNNER.md</automated>
  </verify>
  <acceptance_criteria>
    - extract-test-cases.js exists and runs without errors via `node extract-test-cases.js`
    - VALIDATION-RUNNER.md is generated in .planning/phases/103-validation/
    - grep "CANVAS-01" VALIDATION-RUNNER.md returns at least 1 match
    - grep "DARK-10" VALIDATION-RUNNER.md returns at least 1 match
    - grep -c "| CANVAS-" VALIDATION-RUNNER.md returns 10
    - grep -c "| MICRO-" VALIDATION-RUNNER.md returns 10
    - grep -c "| SCROLL-" VALIDATION-RUNNER.md returns 10
    - grep -c "| CONTEXT-" VALIDATION-RUNNER.md returns 10
    - grep -c "| DARK-" VALIDATION-RUNNER.md returns 10
    - No prompts in VALIDATION-RUNNER.md contain "using MCP manual tools" or "via MCP manual tools"
    - grep "VALID-02" VALIDATION-RUNNER.md returns matches (metrics section present)
    - grep "VALID-03" VALIDATION-RUNNER.md returns matches
    - grep "VALID-04" VALIDATION-RUNNER.md returns matches
    - grep "90%" VALIDATION-RUNNER.md returns matches (gate thresholds documented)
  </acceptance_criteria>
  <done>
    Script executes successfully and generates VALIDATION-RUNNER.md containing all 50 test cases (10 per category), with autopilot-adapted prompts (no MCP references), MCP baseline outcomes, empty result tracking columns, and a metrics section with VALID-02/03/04 gate formulas.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate generated runner has correct prompt adaptation and complete coverage</name>
  <files>
    .planning/phases/103-validation/VALIDATION-RUNNER.md
  </files>
  <read_first>
    .planning/phases/103-validation/VALIDATION-RUNNER.md
    .planning/phases/103-validation/extract-test-cases.js
  </read_first>
  <action>
After Task 1 generates VALIDATION-RUNNER.md, validate and fix if needed:

1. Verify all 50 test cases are present (10 CANVAS, 10 MICRO, 10 SCROLL, 10 CONTEXT, 10 DARK)
2. Verify NO prompt contains "using MCP manual tools" or "via MCP manual tools" -- these 10 prompts must have been cleaned:
   - CANVAS-01 (phase 47): had "using MCP manual tools"
   - CANVAS-02 (phase 48): had "via MCP manual tools"
   - CANVAS-03 (phase 49): had "using MCP manual tools"
   - CANVAS-04 (phase 50): had "using MCP manual tools"
   - CANVAS-05 (phase 51): had "using MCP manual tools"
   - CANVAS-06 (phase 52): had "using MCP manual tools"
   - CANVAS-07 (phase 53): had "via MCP manual tools"
   - CANVAS-08 (phase 54): had "via MCP manual tools"
   - CANVAS-09 (phase 55): had "using MCP manual tools"
   - MICRO-01 (phase 57): had "using MCP manual tools"
3. Verify MCP Baseline column shows the correct outcome for each (PASS, PARTIAL, FAIL, or SKIP-AUTH)
4. Verify the metrics section has all three gates: VALID-02 (pass rate 90%), VALID-03 (parse failure <5%), VALID-04 (completion accuracy 90%)
5. Verify table formatting is valid markdown (consistent column counts per row)
6. If any issues found, fix them directly in VALIDATION-RUNNER.md

Additionally, add a "Quick Reference" section at the top of VALIDATION-RUNNER.md (after the How To Use section) with:
- Category descriptions (what CANVAS tests exercise, what MICRO tests exercise, etc.)
- Expected difficulty ranking by category based on MCP baseline results
- Count of PASS vs PARTIAL vs SKIP-AUTH baselines per category
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Extensions/FSB && grep -c "MCP manual tools" .planning/phases/103-validation/VALIDATION-RUNNER.md | grep "^0$" && grep -c "| CANVAS-" .planning/phases/103-validation/VALIDATION-RUNNER.md | grep "^10$" && grep -c "| DARK-" .planning/phases/103-validation/VALIDATION-RUNNER.md | grep "^10$" && grep "Quick Reference" .planning/phases/103-validation/VALIDATION-RUNNER.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "MCP manual tools" VALIDATION-RUNNER.md returns exactly 0
    - grep -c "| CANVAS-" VALIDATION-RUNNER.md returns exactly 10
    - grep -c "| MICRO-" VALIDATION-RUNNER.md returns exactly 10
    - grep -c "| SCROLL-" VALIDATION-RUNNER.md returns exactly 10
    - grep -c "| CONTEXT-" VALIDATION-RUNNER.md returns exactly 10
    - grep -c "| DARK-" VALIDATION-RUNNER.md returns exactly 10
    - grep "Quick Reference" VALIDATION-RUNNER.md returns a match
    - grep "VALID-02" VALIDATION-RUNNER.md returns matches
    - grep "VALID-03" VALIDATION-RUNNER.md returns matches
    - grep "VALID-04" VALIDATION-RUNNER.md returns matches
    - grep "PASS" VALIDATION-RUNNER.md returns matches in MCP Baseline column
    - grep "PARTIAL" VALIDATION-RUNNER.md returns matches in MCP Baseline column
  </acceptance_criteria>
  <done>
    VALIDATION-RUNNER.md contains exactly 50 test cases with zero MCP manual tool references, correct MCP baseline outcomes, valid markdown tables, a Quick Reference section with category descriptions and difficulty rankings, and a complete metrics section with all three milestone gate formulas (VALID-02, VALID-03, VALID-04).
  </done>
</task>

</tasks>

<verification>
1. Run: `node .planning/phases/103-validation/extract-test-cases.js` -- exits 0
2. Run: `grep -c "MCP manual tools" .planning/phases/103-validation/VALIDATION-RUNNER.md` -- returns 0
3. Run: `grep -c "^|" .planning/phases/103-validation/VALIDATION-RUNNER.md` -- returns 50+ (50 data rows + headers)
4. Run: `grep "VALID-02\|VALID-03\|VALID-04" .planning/phases/103-validation/VALIDATION-RUNNER.md` -- returns 3+ matches
5. Verify CANVAS-01 through DARK-10 all present in sequence
</verification>

<success_criteria>
- extract-test-cases.js runs cleanly and generates VALIDATION-RUNNER.md
- VALIDATION-RUNNER.md has exactly 50 test cases (10 per category)
- Zero prompts contain MCP manual tool references
- All three milestone gates (VALID-02, VALID-03, VALID-04) have tracking sections
- User can open VALIDATION-RUNNER.md, run each prompt through autopilot, and fill in results
</success_criteria>

<output>
After completion, create `.planning/phases/103-validation/103-01-SUMMARY.md`
</output>
