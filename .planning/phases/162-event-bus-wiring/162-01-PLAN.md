---
phase: 162-event-bus-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/popup.js
  - ui/sidepanel.js
autonomous: true
requirements:
  - WIRE-01
  - WIRE-02

must_haves:
  truths:
    - "Popup processes sessionStateEvent messages and updates progress display from iteration_complete events"
    - "Sidepanel processes sessionStateEvent messages and updates progress display from iteration_complete events"
    - "Sidepanel shows tool execution actions via addActionMessage when showSidepanelProgressEnabled is true"
    - "Duplicate state transitions from session_ended do not interfere with automationComplete handling"
  artifacts:
    - path: "ui/popup.js"
      provides: "sessionStateEvent case in onMessage handler"
      contains: "case 'sessionStateEvent'"
    - path: "ui/sidepanel.js"
      provides: "sessionStateEvent case in onMessage handler"
      contains: "case 'sessionStateEvent'"
  key_links:
    - from: "ai/state-emitter.js"
      to: "ui/popup.js"
      via: "chrome.runtime.sendMessage -> onMessage handler"
      pattern: "case 'sessionStateEvent'"
    - from: "ai/state-emitter.js"
      to: "ui/sidepanel.js"
      via: "chrome.runtime.sendMessage -> onMessage handler"
      pattern: "case 'sessionStateEvent'"
---

<objective>
Wire SessionStateEmitter events to popup and sidepanel UI consumers by adding `case 'sessionStateEvent'` handlers that route 4 event types (tool_executed, iteration_complete, session_ended, error_occurred) to existing UI update functions.

Purpose: Close the last mile of the event bus created in Phase 156. The emitter already broadcasts via chrome.runtime.sendMessage from progress hooks but no UI surface listens. This plan adds the receivers.
Output: Both popup.js and sidepanel.js process emitter events, with iteration_complete visually updating the progress display (satisfying WIRE-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-event-bus-wiring/162-CONTEXT.md
@.planning/phases/162-event-bus-wiring/162-RESEARCH.md

@ai/state-emitter.js
@ai/hooks/progress-hook.js
@ui/popup.js
@ui/sidepanel.js

<interfaces>
<!-- Event payload shape from ai/state-emitter.js emit() (line 118): -->
<!-- { action: 'sessionStateEvent', eventType: string, ...data } -->

<!-- Event types from ai/state-emitter.js STATE_EVENTS: -->
<!-- tool_executed, iteration_complete, session_ended, error_occurred -->
<!-- (also status_changed, cost_updated, session_started -- but not emitted by progress hooks) -->

<!-- Payload shapes from ai/hooks/progress-hook.js: -->

<!-- tool_executed (from createToolProgressHook): -->
<!-- { sessionId, toolName, success: boolean, iteration, timestamp } -->

<!-- iteration_complete (from createIterationProgressHook): -->
<!-- { sessionId, iteration, cost, inputTokens, outputTokens, timestamp } -->

<!-- session_ended (from createCompletionProgressHook): -->
<!-- { sessionId, result, totalCost, iterations, timestamp } -->

<!-- error_occurred (from createErrorProgressHook): -->
<!-- { sessionId, error: string, iteration, timestamp } -->

<!-- Existing UI functions available in popup.js: -->
<!-- updateStatusMessage(text, progressData) -- line 578 -->
<!-- setIdleState() -- line 414 -->
<!-- setErrorState() -- line 438 -->
<!-- addActionMessage(text) -- line 456 -->
<!-- Variables: currentSessionId (line 3), isRunning (line 5) -->

<!-- Existing UI functions available in sidepanel.js: -->
<!-- updateStatusMessage(text, progressData) -- line 838 -->
<!-- setIdleState() -- line 678 -->
<!-- setErrorState() -- line 704 -->
<!-- addActionMessage(text) -- line 755 -->
<!-- Variables: currentSessionId (line 3), isRunning (line 7), -->
<!--   showSidepanelProgressEnabled (line 10), isHistoryViewActive (line 9) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sessionStateEvent handler to popup.js</name>
  <files>ui/popup.js</files>
  <read_first>ui/popup.js (lines 810-876), ai/state-emitter.js (lines 116-131), ai/hooks/progress-hook.js</read_first>
  <action>
Add a `case 'sessionStateEvent':` block inside the existing `chrome.runtime.onMessage.addListener` switch statement at line 814 of popup.js. Insert it after the `automationError` case (after line 873, before the closing `}` of the switch at line 875).

The handler must:

1. Guard with `if (request.sessionId !== currentSessionId) break;` -- same session-filtering pattern used by automationComplete and statusUpdate cases.

2. Switch on `request.eventType` (NOT `request.event` -- verified from state-emitter.js line 118) with 4 sub-cases:

   - `iteration_complete` (WIRE-02 visual): Call `updateStatusMessage('Step ' + request.iteration + ' complete', { iteration: request.iteration, maxIterations: 20, progressPercent: Math.min(100, Math.round((request.iteration / 20) * 100)) })` -- but ONLY if `currentStatusMessage && isRunning` to avoid updating after automation ends.

   - `session_ended`: Guard with `if (!isRunning) break;` to avoid duplicate state transition (automationComplete already calls setIdleState which sets isRunning = false). If still running, call `setIdleState()`.

   - `tool_executed`: Console-only -- `console.debug('[FSB] tool:', request.toolName, request.success ? 'ok' : 'fail');` -- popup is too small for action log DOM updates.

   - `error_occurred`: Console-only -- `console.warn('[FSB] emitter error:', request.error);` -- the existing automationError handler already manages visual error display.

3. End the case with `break;`.

Do NOT create new DOM elements, new CSS, or new UI components (per D-02). Do NOT modify any existing cases. Do NOT add a separate onMessage listener (keep inside the existing one to avoid DOM-before-ready issues per Pitfall 3).
  </action>
  <verify>
    <automated>grep -n "case 'sessionStateEvent'" ui/popup.js | head -1</automated>
  </verify>
  <acceptance_criteria>
    - popup.js contains exactly one `case 'sessionStateEvent':` block inside the line-813 onMessage listener
    - The handler switches on `request.eventType` with cases for iteration_complete, session_ended, tool_executed, error_occurred
    - iteration_complete calls updateStatusMessage with progress data (WIRE-02)
    - session_ended guards with `if (!isRunning) break;` before calling setIdleState (no duplicate transition)
    - tool_executed and error_occurred use console.debug/console.warn only (no DOM changes)
    - No new DOM elements, no new functions, no new CSS
  </acceptance_criteria>
  <done>popup.js onMessage handler processes all 4 sessionStateEvent types with iteration_complete visually updating progress</done>
</task>

<task type="auto">
  <name>Task 2: Add sessionStateEvent handler to sidepanel.js</name>
  <files>ui/sidepanel.js</files>
  <read_first>ui/sidepanel.js (lines 1012-1147), ai/state-emitter.js (lines 116-131), ai/hooks/progress-hook.js</read_first>
  <action>
Add a `case 'sessionStateEvent':` block inside the existing `chrome.runtime.onMessage.addListener` switch statement at line 1014 of sidepanel.js. Insert it after the `loginDetected` case (after line 1145, before the closing `}` of the switch at line 1146).

The handler must:

1. Guard with `if (request.sessionId !== currentSessionId) break;` -- same session-filtering pattern used by existing cases.

2. Switch on `request.eventType` (NOT `request.event` -- verified from state-emitter.js line 118) with 4 sub-cases:

   - `iteration_complete` (WIRE-02 visual): Call `updateStatusMessage('Step ' + request.iteration + ' complete', { iteration: request.iteration, maxIterations: 20, progressPercent: Math.min(100, Math.round((request.iteration / 20) * 100)) })` -- but ONLY if `currentStatusMessage && isRunning`.

   - `session_ended`: Guard with `if (!isRunning) break;` to avoid duplicate state transition. If still running, call `setIdleState()`. Then check `if (isHistoryViewActive) { loadHistoryList(); }` to refresh history (matching the pattern in the automationComplete case).

   - `tool_executed`: Gate behind `showSidepanelProgressEnabled` flag AND `isRunning`. If both true, call `addActionMessage(request.toolName + (request.success ? '' : ' [failed]'));`. This uses the existing debug-gated action message system, preventing event flood in normal mode (Pitfall 2).

   - `error_occurred`: Console-only -- `console.warn('[FSB] emitter error:', request.error);` -- the existing automationError handler manages visual error display.

3. End the case with `break;`.

NOTE: The `loginDetected` case at line 1136-1145 uses `return;` (not `break;`) because it calls `sendResponse`. The new sessionStateEvent case must use `break;` (it does not need sendResponse). Insert the new case AFTER the loginDetected case closing -- just before the `}` that closes the switch.

Do NOT create new DOM elements, new CSS, or new UI components (per D-02). Do NOT modify any existing cases. Do NOT add a separate onMessage listener.
  </action>
  <verify>
    <automated>grep -n "case 'sessionStateEvent'" ui/sidepanel.js | head -1</automated>
  </verify>
  <acceptance_criteria>
    - sidepanel.js contains exactly one `case 'sessionStateEvent':` block inside the line-1013 onMessage listener
    - The handler switches on `request.eventType` with cases for iteration_complete, session_ended, tool_executed, error_occurred
    - iteration_complete calls updateStatusMessage with progress data (WIRE-02)
    - session_ended guards with `if (!isRunning) break;` and refreshes history if isHistoryViewActive
    - tool_executed gates behind showSidepanelProgressEnabled and isRunning, calls addActionMessage (not raw DOM)
    - error_occurred uses console.warn only
    - No new DOM elements, no new functions, no new CSS
  </acceptance_criteria>
  <done>sidepanel.js onMessage handler processes all 4 sessionStateEvent types with iteration_complete visually updating progress and tool_executed showing debug action messages</done>
</task>

</tasks>

<verification>
Both WIRE-01 and WIRE-02 requirements are satisfied:

WIRE-01: Both popup.js and sidepanel.js have `case 'sessionStateEvent'` handling all 4 event types.
WIRE-02: `iteration_complete` calls `updateStatusMessage()` with progress data in both surfaces, providing visual feedback.

Verify no regressions:
- Existing automationComplete, statusUpdate, automationError handlers are unmodified
- No duplicate state transitions (session_ended guarded by isRunning check)
- No event flood (tool_executed gated by showSidepanelProgressEnabled in sidepanel, console-only in popup)
</verification>

<success_criteria>
1. `grep "case 'sessionStateEvent'" ui/popup.js` returns exactly 1 match
2. `grep "case 'sessionStateEvent'" ui/sidepanel.js` returns exactly 1 match
3. `grep "request.eventType" ui/popup.js` returns matches showing the inner switch
4. `grep "request.eventType" ui/sidepanel.js` returns matches showing the inner switch
5. `grep "updateStatusMessage" ui/popup.js` includes the iteration_complete call
6. `grep "addActionMessage" ui/sidepanel.js` includes the tool_executed call
7. No syntax errors: extension loads without errors in chrome://extensions
</success_criteria>

<output>
After completion, create `.planning/phases/162-event-bus-wiring/162-01-SUMMARY.md`
</output>
