---
phase: 17-prompt-architecture-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai/ai-integration.js
  - ai/cli-parser.js
autonomous: true
requirements:
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03
  - PROMPT-05
  - PROMPT-06
  - PROMPT-07

must_haves:
  truths:
    - "The system prompt contains a CLI command reference table (verb + args + description + example) instead of JSON tool documentation"
    - "Continuation prompts (iteration 2+) include a CLI syntax reminder line and explicitly say NOT JSON"
    - "Stuck recovery prompts suggest alternative CLI commands and list anti-patterns to avoid"
    - "Task-type prompts (career, email, gaming, sheets, etc.) use CLI command examples instead of JSON examples"
    - "The done command is documented as the sole task completion mechanism, replacing taskComplete JSON field"
    - "Batch action instructions describe multi-line CLI commands instead of batchActions JSON array"
    - "The help command is registered in COMMAND_REGISTRY as a signal command"
    - "Zero JSON format examples remain in any prompt string in ai-integration.js"
  artifacts:
    - path: "ai/ai-integration.js"
      provides: "All prompt strings rewritten for CLI format"
      contains: "CLI COMMAND REFERENCE"
    - path: "ai/cli-parser.js"
      provides: "help command registry entry"
      contains: "help"
  key_links:
    - from: "ai/ai-integration.js"
      to: "ai/cli-parser.js"
      via: "COMMAND_REGISTRY help entry referenced in stuck recovery prompts"
      pattern: "help"
---

<objective>
Rewrite every prompt string in ai-integration.js from JSON tool-call format to CLI command grammar (verb + ref + args), and register the help command in cli-parser.js.

Purpose: Replace ~35 discrete JSON format locations in the AI prompt system with CLI equivalents so the AI outputs line-based commands instead of JSON objects. This is the core format swap for v10.0.

Output: Modified ai-integration.js with all prompt strings speaking CLI, modified cli-parser.js with help command in COMMAND_REGISTRY.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-prompt-architecture-rewrite/17-RESEARCH.md
@.planning/phases/15-cli-parser-module/15-01-SUMMARY.md
@.planning/phases/15-cli-parser-module/15-02-SUMMARY.md
@ai/ai-integration.js
@ai/cli-parser.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite prompt constants and system prompt builder for CLI format</name>
  <files>ai/ai-integration.js, ai/cli-parser.js</files>
  <action>
    This task rewrites all top-level prompt constants and the main system prompt construction in ai-integration.js, plus adds the help command to cli-parser.js.

    **In ai/cli-parser.js -- Add help command to COMMAND_REGISTRY:**
    Add entry: `help: { tool: '__help', args: [{ name: 'verb', type: 'string', optional: true }], signal: 'help' }`
    This makes help a signal command (like done/fail) -- the parser returns {signal: 'help', message: verbArg} instead of dispatching to content script. The automation loop in background.js will handle this signal in Phase 18.

    **In ai/ai-integration.js -- Replace TOOL_DOCUMENTATION constant (lines ~15-116):**
    Replace the entire TOOL_DOCUMENTATION constant with a CLI_COMMAND_TABLE constant using compact markdown table format per user decision (compact table format, grouped by category, per-command example column). Group commands by category:
    - Navigation: navigate, search, back, forward, refresh
    - Element Interaction: click, rightclick, doubleclick, hover, focus, blur
    - Text Input: type, clear, enter, key (with --ctrl --shift --alt --meta flags)
    - Form Controls: select (with --by-index), check
    - Scrolling: scroll (up/down/left/right + amount), scrolltotop, scrolltobottom
    - Information: gettext, getattr
    - Waiting: wait (CSS selector), waitstable
    - Tab Management: opentab, switchtab, tabs
    - Data: storejobdata (inline JSON), fillsheetdata (inline JSON)
    - Completion: done "summary", fail "reason"
    - Help: help [verb]

    Derive the verb list from COMMAND_REGISTRY in cli-parser.js (source of truth). Each row: | verb | args | description | example |

    **Replace TASK_PROMPTS object (lines ~186-366):**
    Convert all 11 task types. Specific JSON examples to convert:
    - email (line ~206): keyPress JSON fallback -> `key "Enter" --meta`
    - gaming (line ~250): JSON tool examples -> CLI examples like `key "ArrowUp"`, `click e5`
    - career (lines ~288-364): JSON storeJobData format at line ~439 -> `storejobdata {"company":"...","role":"...",...}`
    - All other types (search, form, extraction, navigation, shopping, multitab, general): reference tool names are already format-agnostic, but audit each for any JSON format remnants and convert
    For the career task type: convert the storeJobData JSON example to `storejobdata {"company":"X","role":"Y","location":"Z","type":"FT","salary":"$X","link":"url","source":"site"}`. Preserve ALL workflow text, verification rules, and error reporting instructions -- only change the format examples.

    **Replace MINIMAL_CONTINUATION_PROMPT constant (lines ~370-396):**
    Rewrite to:
    ```
    You are a browser automation agent. Continue the task based on the current page state.

    Respond with CLI commands only (verb ref args). Do NOT output JSON. Use # for reasoning. Use done "summary" to complete.

    RULES:
    1. If search results are shown, click a result -- do not search again
    2. Only use done when task is ACTUALLY complete
    3. If a previous type action SUCCEEDED, do not re-type -- just submit (enter or click submit)
    4. Check hasMoreBelow and scroll down if looking for content
    5. For extraction tasks, extract visible items, scroll down, repeat until atBottom
    6. Do NOT retry actions that already showed SUCCESS in action history
    7. Use refs from the latest snapshot -- stale refs mean the page changed
    ```
    Include the current iteration number: `Iteration {N}:` at the beginning per user decision.

    **Replace BATCH_ACTION_INSTRUCTIONS constant (lines ~400-435):**
    Replace with a brief note:
    ```
    MULTI-COMMAND BATCHING:
    You may output multiple commands on consecutive lines. Each line is one command.
    Commands execute sequentially with DOM stability checks between each.
    Maximum 8 commands per response.
    Do NOT batch on Google Sheets canvas -- one type command at a time.
    ```
    Remove the JSON batchActions example entirely. In CLI, multi-line commands ARE the batch naturally.

    **Rewrite the system prompt construction in _buildSystemPrompt / getSystemPrompt method (lines ~2206-2418):**
    Replace the entire JSON-oriented system prompt with CLI-oriented version:
    - Replace "RESPOND WITH ONLY VALID JSON" (line ~2224) with "Output CLI commands, one per line. Use # for reasoning comments."
    - Replace the clickSearchResult JSON example (line ~2261) with `clicksearchresult e3`
    - Replace the REQUIRED RESPONSE FORMAT block (lines ~2359-2402) with CLI format instructions:
      ```
      RESPONSE FORMAT:
      # your analysis of the current page state
      # your plan and why you chose this approach
      click e5
      type e12 "search query"
      done "task completed successfully with these results: ..."

      RULES:
      - One command per line
      - Use element refs from the snapshot: e1, e2, etc.
      - Quote strings with double quotes: type e12 "hello world"
      - Use # comments for reasoning (REQUIRED before actions)
      - End with done "summary" when task is complete
      - Refs are only valid for current snapshot -- if action fails with "stale", use latest refs
      ```
    - Add a dedicated TASK COMPLETION section per user decision:
      ```
      TASK COMPLETION:
      Output: done "detailed summary of what was accomplished"
      - ONLY use done after verifying the task is actually complete
      - Include specific data found (exact values, not "found it")
      - If critical actions failed, retry before using done
      ```
    - Replace the JSON refs instruction (line ~2220: "refs in actions: JSON") with `Use element refs: click e1, type e5 "text"`
    - Embed the CLI_COMMAND_TABLE in place of getToolsDocumentation() output
    - Preserve all non-format-related system prompt content: task description injection, element context injection, page context, user locale, site knowledge, etc.

    **Rewrite getToolsDocumentation() method (lines ~4523-4661):**
    Replace the method body to return the CLI_COMMAND_TABLE string instead of JSON-formatted tool descriptions. Convert all remaining JSON examples in this method:
    - type tool JSON example (line ~4540) -> `type e12 "hello"`
    - scroll tool JSON example (line ~4552) -> `scroll down`
    - keyPress JSON examples (lines ~4566-4567) -> `key "Tab"`, `key "a" --ctrl`
    - pressKeySequence JSON example (line ~4572) -> removed (not a CLI verb)
    - typeWithKeys JSON example (line ~4577) -> removed (not a CLI verb)
    - sendSpecialKey JSON example (line ~4582) -> `key "Escape"`
    - arrow/game JSON examples (lines ~4587-4607) -> `key "ArrowUp"`, `key "ArrowDown"`
    - getEditorContent JSON example (line ~4612) -> `gettext e5`
  </action>
  <verify>
    1. Run `grep -c '"tool":' ai/ai-integration.js` -- count of JSON tool references should be zero or near-zero in prompt string areas (grep for "tool": inside template literals and string constants)
    2. Run `grep -c 'batchActions' ai/ai-integration.js` -- should show only the brief CLI note, not JSON examples
    3. Run `grep -c 'taskComplete' ai/ai-integration.js` -- should show zero in prompt strings (may remain in response parsing code which is OK for now)
    4. Run `grep 'COMMAND_TABLE\|CLI COMMAND' ai/ai-integration.js` -- should find the CLI command reference
    5. Run `grep 'help' ai/cli-parser.js` -- should find the help entry in COMMAND_REGISTRY
    6. Run `node -c ai/ai-integration.js` -- syntax check passes
    7. Run `node -c ai/cli-parser.js` -- syntax check passes
  </verify>
  <done>
    All prompt constants (TOOL_DOCUMENTATION -> CLI_COMMAND_TABLE, TASK_PROMPTS, MINIMAL_CONTINUATION_PROMPT, BATCH_ACTION_INSTRUCTIONS) and the system prompt builder are rewritten for CLI format. getToolsDocumentation() returns CLI command table instead of JSON tool docs. Help command is registered in COMMAND_REGISTRY as a signal command.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite stuck recovery prompts, conditional directive injections, and remaining JSON locations</name>
  <files>ai/ai-integration.js</files>
  <action>
    This task converts all remaining JSON format locations in ai-integration.js that were NOT covered in Task 1: stuck recovery prompts, conditional directive injections, and the _buildTaskGuidance method.

    **Rewrite stuck recovery prompts (lines ~2551-2803):**
    Convert the clickSearchResult JSON example at line ~2565 to CLI:
    ```
    clicksearchresult e3
    ```
    Implement contextual stuck recovery per user decision:
    - When on search page (isOnSearchPage): suggest `clicksearchresult eN` with anti-patterns ("Do not search again when results are visible")
    - When on form page: suggest `scroll down` to find submit button, `clear eN` then retype
    - Generic recovery: suggest `scroll down`, `back`, `help` as alternatives
    - Include anti-patterns section per user decision:
      ```
      DO NOT:
      - Repeat the same click that already failed
      - Search again when results are visible
      - Type into the same field without clearing first
      ```
    - Progressive escalation (Claude's discretion resolution):
      Level 1 (stuckCounter=1): suggest alternatives including `help`
      Level 2 (stuckCounter=2): add explicit anti-patterns ("DO NOT repeat...")
      Level 3 (stuckCounter>=3): force a specific alternative command

    **Rewrite multiSiteDirective (line ~2439):**
    Convert the storeJobData JSON format example to CLI:
    `storejobdata {"company":"...", "role":"...", "location":"...", "type":"...", "salary":"...", "link":"...", "source":"..."}`
    Preserve all multi-site workflow instructions.

    **Rewrite sheetsDataDirective (line ~2469) and buildSheetsFormattingDirective:**
    Convert any JSON format references in the sheets data entry directive to CLI format.
    Convert the fillsheetdata JSON example (if any) to CLI format.
    Preserve all sheets-specific workflow instructions (Name Box navigation, Tab/Enter pattern, etc.).

    **Rewrite _buildTaskGuidance method (line ~4251):**
    Convert the `{"ref": "e5"}` reference to CLI format: `click e5`
    Update the guidance note about using element refs to reference CLI syntax: "Use the ref identifier from the snapshot to target elements (e.g., click e5, type e12 'text')."

    **Rewrite completionCandidate prompt injection (line ~2861):**
    If there is a JSON-format completion signal, convert to: "Output `done 'summary of results'` if the task is complete."

    **Convert Sheets-aware continuation prompt injection in buildMinimalUpdate:**
    The URL-conditional Sheets reminder (added in Phase 14.2) needs to reference CLI commands instead of JSON tool format. Convert to CLI: "Navigate via Name Box: click eN (Name Box ref), type eN 'A1', enter"

    **Final JSON sweep -- search for ALL remaining format references:**
    After all rewrites, systematically search for these patterns in ALL string literals and template literals throughout ai-integration.js:
    - `"tool":` -- JSON tool field
    - `"params":` -- JSON params field
    - `"actions":` -- JSON actions array
    - `"batchActions":` -- JSON batch array (except the brief CLI note mentioning the old format is replaced)
    - `"taskComplete":` -- JSON completion field (in prompt strings only; response parsing code keeps it)
    - `"ref":` -- JSON ref field (in prompt strings only; response parsing code keeps it)
    - `VALID JSON` or `valid JSON` -- old format instructions
    Convert any remaining matches found. Response parsing code (normalizeResponse, parseActionResponse, etc.) should NOT be modified -- those are Phase 18 scope.

    **IMPORTANT: Do NOT modify response parsing functions** (normalizeResponse, parseActionResponse, extractActions, etc.). Those functions handle runtime AI responses and will be replaced by parseCliResponse in Phase 18. Only modify PROMPT STRINGS -- what we TELL the AI, not how we PARSE its output.
  </action>
  <verify>
    1. Run comprehensive grep across ai-integration.js for JSON format remnants in string literals:
       - `grep -n '"tool":' ai/ai-integration.js` -- check each match is in parsing code not prompt strings
       - `grep -n '"params":' ai/ai-integration.js` -- same check
       - `grep -n 'VALID JSON' ai/ai-integration.js` -- should be zero
       - `grep -n '"batchActions"' ai/ai-integration.js` -- should only appear in the brief historical note
       - `grep -n '"taskComplete"' ai/ai-integration.js` -- should not appear in prompt strings
    2. Run `grep -n 'clicksearchresult\|clickSearchResult' ai/ai-integration.js` -- stuck recovery should use CLI format
    3. Run `grep -n 'storejobdata\|storeJobData' ai/ai-integration.js` -- multi-site directive should use CLI format example
    4. Run `grep -n 'DO NOT' ai/ai-integration.js` -- anti-patterns section should exist in stuck recovery
    5. Run `node -c ai/ai-integration.js` -- syntax check passes
  </verify>
  <done>
    All ~35 JSON format locations in ai-integration.js are converted to CLI format. Stuck recovery prompts use contextual CLI suggestions with progressive escalation and anti-patterns. Conditional directives (multiSite, sheetsData, completionCandidate) use CLI format. The _buildTaskGuidance method references CLI syntax. A comprehensive grep confirms zero JSON format remnants in any prompt string.
  </done>
</task>

</tasks>

<verification>
1. Syntax validation: `node -c ai/ai-integration.js && node -c ai/cli-parser.js` both pass
2. JSON remnant sweep: grep for "tool":", "params":", "VALID JSON", "batchActions" in prompt strings returns zero matches
3. CLI presence check: grep for "CLI COMMAND", "verb ref args", "done.*summary" confirms CLI format is present in system prompt, continuation prompt, and stuck recovery
4. help command: grep for "help" in cli-parser.js COMMAND_REGISTRY confirms signal entry exists
5. Task completion: grep for "done" in system prompt confirms dedicated TASK COMPLETION section with rules
</verification>

<success_criteria>
- All prompt constants in ai-integration.js speak CLI command grammar exclusively
- System prompt contains a compact CLI command reference table replacing JSON tool documentation
- Continuation prompt reinforces CLI format with explicit "NOT JSON" negation
- Stuck recovery uses contextual CLI suggestions with anti-patterns
- Task-type prompts use CLI examples (career storeJobData, gaming key commands, email key commands)
- done command is documented as sole completion mechanism
- help command is registered in COMMAND_REGISTRY
- Zero JSON format examples remain in any prompt string in ai-integration.js
</success_criteria>

<output>
After completion, create `.planning/phases/17-prompt-architecture-rewrite/17-01-SUMMARY.md`
</output>
