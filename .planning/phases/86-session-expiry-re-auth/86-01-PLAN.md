---
phase: 86-session-expiry-re-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site-guides/utilities/session-expiry.js
  - background.js
autonomous: true
requirements:
  - CONTEXT-10

must_haves:
  truths:
    - "The session-expiry.js site guide contains a handleSessionExpiry workflow documenting the full sequence: detect session expiration modal/redirect, capture pre-expiry task state, re-authenticate with credentials, dismiss modal or navigate back, resume original task from saved state"
    - "The session-expiry.js site guide contains a CONTEXT-10 section explaining context bloat mitigation for session re-auth interruptions: compact task state snapshot under 500 characters preserving only current URL, last action, and task goal"
    - "The session-expiry.js site guide documents modal detection patterns for at least 3 common session expiry presentations: dialog/modal overlay, redirect to login page, inline banner/toast"
    - "The session-expiry.js site guide documents at least 2 demo targets with session timeout behavior and selectors for login forms and session expiry indicators"
  artifacts:
    - path: "site-guides/utilities/session-expiry.js"
      provides: "handleSessionExpiry workflow and CONTEXT-10 guidance for session expiration modal detection, re-authentication, and task resumption"
      contains: "handleSessionExpiry"
    - path: "background.js"
      provides: "importScripts entry for session-expiry.js in Utilities section"
      contains: "session-expiry.js"
  key_links:
    - from: "site-guides/utilities/session-expiry.js"
      to: "mcp-server/src/tools/manual.ts"
      via: "toolPreferences referencing navigate, click, type_text, read_page, get_dom_snapshot, wait_for_element"
      pattern: "navigate|click|type_text|read_page|get_dom_snapshot|wait_for_element"
    - from: "background.js"
      to: "site-guides/utilities/session-expiry.js"
      via: "importScripts registration in Utilities section"
      pattern: "importScripts.*session-expiry"
---

<objective>
Create a session-expiry.js site guide with a handleSessionExpiry workflow and CONTEXT-10 guidance for detecting session expiration modals, re-authenticating, and resuming the interrupted task.

Purpose: CONTEXT-10 requires handling session expiration mid-task: detect the session expiry modal or login redirect, re-authenticate, and resume the original task without losing progress. This is a "Context Bloat & Long-Running Workflows" edge case -- the challenge is preserving task state across an authentication interruption without bloating context. The site guide must document: (a) target demo sites with session timeout behavior (auth-free or with test credentials), (b) session expiry detection patterns (modal/dialog, login redirect, inline banner), (c) the re-auth and task resumption workflow, (d) compact task state preservation under 500 characters.

Output: site-guides/utilities/session-expiry.js and background.js updated with importScripts entry
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-session-expiry-re-auth/86-CONTEXT.md
@.planning/phases/83-2fa-multi-tab-auth-flow/83-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create session-expiry.js site guide with handleSessionExpiry workflow and CONTEXT-10 guidance</name>
  <files>site-guides/utilities/session-expiry.js, background.js</files>
  <read_first>
    - site-guides/utilities/two-factor-auth.js lines 1-60 (reference for Utilities site guide structure: registerSiteGuide call with site, category, patterns, guidance, selectors, workflows, warnings, toolPreferences -- closest auth-related analog)
    - site-guides/utilities/support-chatbot.js lines 1-40 (reference for site guide structure in Utilities category)
    - background.js lines 179-186 (Utilities section importScripts order -- new guide goes after two-factor-auth.js as last entry before agent modules)
    - .planning/phases/86-session-expiry-re-auth/86-CONTEXT.md (phase context -- session expiry modal detection, re-auth, task resumption)
    - mcp-server/src/tools/manual.ts lines 1-30 (tool signatures for navigate, click, type_text, read_page, get_dom_snapshot, wait_for_element)
  </read_first>
  <action>
Create a new file site-guides/utilities/session-expiry.js. This site guide targets demo sites with session timeout behavior for the CONTEXT-10 session expiry re-auth edge case.

**Demo site targets (test credentials or auth-free session expiry):**

Primary target: the-internet.herokuapp.com/login -- Heroku test app with known test credentials (tomsmith / SuperSecretPassword!). After login, navigate to /secure page. Simulate session expiry by navigating to /logout which redirects to /login with a "You logged out of the secure area!" flash message. Re-authenticate and verify /secure page is accessible again. This demonstrates the full login -> session loss -> re-auth -> resume cycle.

Secondary target: the-internet.herokuapp.com/basic_auth -- Basic HTTP auth page with user:admin / password:admin credentials. Auth state is per-request (session-less), demonstrating a different re-auth pattern.

Fallback 1: practiceautomation.com -- test site with various form types including login forms.

Fallback 2: uitestingplayground.com/sampleapp -- simple login form with test credentials for practice.

Fallback 3: demoqa.com/login -- BookStore login form (requires registration but demonstrates login form interaction).

Simulation approach: Since real session timeout is time-dependent (typically 15-30 minutes), the workflow simulates session expiry by: (a) Logging in to the target site, (b) Performing a task (e.g., reading secure page content), (c) Navigating to /logout to force session end, (d) Attempting to access the secure page (which triggers redirect to login), (e) Detecting the login page redirect as "session expiry", (f) Re-authenticating, (g) Resuming the original task (re-reading secure page content).

**The registerSiteGuide call must include:**

1. `site`: 'Session Expiry Re-Authentication'
2. `category`: 'Utilities'
3. `patterns`: Array of RegExp matching session-related URLs and patterns: /session.expir/i, /session.timeout/i, /login.*redirect/i, /re.?auth/i, /the-internet\.herokuapp\.com/i, /practiceautomation\.com/i, /uitestingplayground\.com/i, /demoqa\.com\/login/i
4. `guidance`: Multi-line template string with these sections:
   - SESSION EXPIRY RE-AUTHENTICATION INTELLIGENCE (CONTEXT-10)
   - TARGET SITES section listing primary and fallback targets with URLs and test credentials
   - SESSION EXPIRY DETECTION PATTERNS section documenting 4 common patterns:
     Pattern A: Modal/Dialog overlay (aria-modal="true", role="dialog", .modal, .overlay elements appearing over page content)
     Pattern B: Login page redirect (URL changes to /login, /signin, /auth; current page URL differs from expected task URL)
     Pattern C: Inline banner/toast (flash messages like "Session expired", "Please log in again" appearing as div.flash, .alert, .notification)
     Pattern D: HTTP 401/403 response (page content replaced with "Unauthorized" or "Forbidden" text; read_page returns auth error content)
   - RE-AUTH AND RESUME STRATEGY: 3-phase workflow overview (detect, re-auth, resume)
   - CONTEXT BLOAT MITIGATION FOR SESSION RE-AUTH: compact task state snapshot under 500 chars total, preserving only {taskGoal, lastUrl, lastAction, lastResult} -- discard all DOM snapshots before re-auth
   - TASK STATE PRESERVATION: before re-authenticating, capture minimal state: current URL, the goal being pursued, the last successful action and its result -- this is all that is needed to resume after re-auth
   - SKIP-AUTH EXPECTATION: if no demo site with session behavior is available, document as skip-auth
5. `selectors`: Object with selectors for each target site:
   - herokuapp: { loginForm: '#login', usernameField: '#username', passwordField: '#password', loginButton: 'button[type="submit"], .radius', flashMessage: '#flash', secureHeading: '.example h2', secureParagraph: '.example p', logoutButton: '.button.secondary, a[href="/logout"]' }
   - sessionExpiryIndicators: { modal: '[aria-modal="true"], [role="dialog"], .modal, .modal-dialog', overlay: '.overlay, .modal-overlay, .modal-backdrop', loginRedirect: 'input[name="username"], input[type="email"][name*="user"], #username, #email', flashBanner: '.flash, .alert, .notification, .toast, [role="alert"]', expiredText: 'Session expired, session has expired, please log in, please sign in, login required, authentication required' }
   - uitestingplayground: { loginForm: '#login', userField: 'input[name="UserName"]', passField: 'input[name="Password"]', loginBtn: '#login', statusText: '#loginstatus' }
   - demoqa: { loginForm: '#login', usernameField: '#userName', passwordField: '#password', loginButton: '#login', logoutButton: '#submit' }
6. `workflows`: Object with handleSessionExpiry array of 14 steps:
   Step 1: INITIAL LOGIN -- Navigate to target site login page (the-internet.herokuapp.com/login). Enter test credentials (tomsmith / SuperSecretPassword!). Click login button. Verify successful login (flash message contains "You logged into a secure area!" or URL contains /secure).
   Step 2: CAPTURE TASK STATE -- Record pre-task state as compact snapshot: { taskGoal: "read secure page content", currentUrl: "/secure", loginCredentials: "tomsmith", loggedIn: true }. This snapshot is under 200 characters.
   Step 3: PERFORM ORIGINAL TASK -- Use read_page on .example to extract the secure page heading and paragraph text. Record the content as lastResult. This is the "task in progress" before session expiry.
   Step 4: SIMULATE SESSION EXPIRY -- Navigate to /logout to force session end. This simulates what happens when a session cookie expires mid-task. The page redirects to /login with a flash message "You logged out of the secure area!".
   Step 5: DETECT SESSION EXPIRY -- After simulated expiry, attempt to navigate back to /secure. The site redirects to /login (Pattern B: login page redirect). Detection criteria: (a) URL does NOT contain /secure, (b) URL contains /login, (c) get_dom_snapshot finds #login form, (d) flash message may contain expiry text.
   Step 6: CONFIRM EXPIRY TYPE -- Use get_dom_snapshot to identify which session expiry pattern is present: check for aria-modal/dialog (Pattern A), check URL for login redirect (Pattern B), check for .flash/.alert elements (Pattern C), check page content for "Unauthorized" (Pattern D). Record the detected pattern.
   Step 7: PRESERVE PRE-REAUTH STATE -- Before re-authenticating, update task state: { taskGoal: "read secure page content", lastUrl: "/secure", lastAction: "read_page on .example", lastResult: "(first 100 chars of previously read content)", sessionExpired: true, expiryPattern: "login-redirect" }. Total under 500 characters.
   Step 8: RE-AUTHENTICATE -- Enter credentials again: type_text on #username with "tomsmith", type_text on #password with "SuperSecretPassword!", click login button. Verify re-auth success (flash message contains "You logged into a secure area!" or URL contains /secure).
   Step 9: VERIFY RE-AUTH SUCCESS -- Confirm the session is restored: URL should contain /secure, read_page should show the secure page content, not a login form. If re-auth fails, retry once with same credentials.
   Step 10: RESUME ORIGINAL TASK -- Navigate back to the URL from saved task state (/secure). Use read_page on .example to re-extract the secure page content. Compare with lastResult from Step 3 to verify the same content is accessible.
   Step 11: VERIFY TASK CONTINUITY -- Confirm the resumed task produces the same result as before session expiry. The secure page content after re-auth should match the content captured before logout. This proves task resumption works.
   Step 12: CONTEXT TRACKING -- Throughout the workflow, maintain only: { phase: "login|task|expired|reauth|resumed", taskGoal: "...", url: "...", step: N }. Total tracking under 150 characters at any point.
   Step 13: EDGE CASES -- Document handling for: (a) re-auth fails (credentials rejected after expiry -- retry or escalate), (b) session expires during re-auth (double expiry -- start from Step 1), (c) original page changed after re-auth (stale content -- re-read and proceed), (d) modal blocks page interaction (dismiss modal first before re-entering credentials).
   Step 14: REPORT -- State final outcome: session expiry detected (pattern type), re-auth completed (success/fail), original task resumed (content matched/differed).
7. `warnings`: Array of 5 strings:
   - 'CONTEXT-10: Before re-authenticating, capture ONLY minimal task state: {taskGoal, lastUrl, lastAction, lastResult (truncated to 100 chars)}. Total under 500 characters. Do NOT retain DOM snapshots from before the session expiry.'
   - 'Session expiry detection priority: check URL first (fastest -- login redirect pattern), then check DOM for modal/dialog elements, then check flash/alert messages, then check page content for auth error text.'
   - 'The-Internet herokuapp uses flash messages for login status -- #flash element with class "success" or "error". Check this element after every login attempt.'
   - 'After re-authentication, always verify the session is truly restored by navigating to the protected page and reading content -- do NOT assume re-auth succeeded just because no error appeared.'
   - 'If the target site requires real session timeout (15+ minutes), use the simulate approach: login, logout, re-login. This demonstrates the same re-auth pattern without waiting for a real timeout.'
8. `toolPreferences`: Array including navigate, click, type_text, read_page, get_dom_snapshot, wait_for_element

Then update background.js: Add `importScripts('site-guides/utilities/session-expiry.js');` after the `importScripts('site-guides/utilities/two-factor-auth.js');` line (line ~185) in the Utilities section, making it the last Utilities import before the agent modules section.
  </action>
  <verify>
    <automated>grep -q "handleSessionExpiry" site-guides/utilities/session-expiry.js && grep -q "CONTEXT-10" site-guides/utilities/session-expiry.js && grep -q "registerSiteGuide" site-guides/utilities/session-expiry.js && grep -q "herokuapp" site-guides/utilities/session-expiry.js && grep -q "re-auth\|reauth\|re.auth" site-guides/utilities/session-expiry.js && grep -q "modal\|dialog\|aria-modal" site-guides/utilities/session-expiry.js && grep -q "session.expir\|Session Expir\|SESSION EXPIR" site-guides/utilities/session-expiry.js && grep -q "taskGoal\|task.goal\|taskState" site-guides/utilities/session-expiry.js && grep -q "context bloat\|Context Bloat\|CONTEXT BLOAT" site-guides/utilities/session-expiry.js && grep -q "session-expiry" background.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "handleSessionExpiry" session-expiry.js returns at least 1 match (workflow name)
    - grep "CONTEXT-10" session-expiry.js returns at least 2 matches (guidance section and warning)
    - grep "registerSiteGuide" session-expiry.js returns exactly 1 match (registration call)
    - grep "herokuapp" session-expiry.js returns at least 2 matches (primary target URL and selectors)
    - grep "re-auth\|reauth" session-expiry.js returns at least 3 matches (re-authentication workflow steps)
    - grep "modal\|dialog" session-expiry.js returns at least 2 matches (session expiry detection patterns)
    - grep "aria-modal" session-expiry.js returns at least 1 match (modal detection selector)
    - grep "session.expir\|Session Expir" session-expiry.js returns at least 3 matches (core concept documented)
    - grep "taskGoal\|taskState" session-expiry.js returns at least 2 matches (task state preservation)
    - grep "context bloat\|Context Bloat\|CONTEXT BLOAT" session-expiry.js returns at least 1 match (mitigation section)
    - grep "tomsmith\|SuperSecretPassword" session-expiry.js returns at least 2 matches (test credentials for herokuapp)
    - grep "#username" session-expiry.js returns at least 1 match (login form selector)
    - grep "#password" session-expiry.js returns at least 1 match (login form selector)
    - grep "resume\|Resume\|RESUME" session-expiry.js returns at least 2 matches (task resumption documented)
    - grep "session-expiry" background.js returns at least 1 match (importScripts entry)
    - grep "Utilities" session-expiry.js returns at least 1 match (category)
    - The background.js importScripts entry for session-expiry.js appears after two-factor-auth.js in the Utilities section
  </acceptance_criteria>
  <done>session-expiry.js created with: registerSiteGuide call (site, category, patterns for session expiry and 4 demo targets), CONTEXT-10 guidance section (session expiry detection patterns: modal/dialog, login redirect, inline banner, HTTP 401/403; re-auth and resume strategy; context bloat mitigation with compact task state under 500 chars; task state preservation), selectors for herokuapp login form and session expiry indicators and uitestingplayground and demoqa, handleSessionExpiry workflow (14 steps: initial login with test credentials, capture task state, perform original task, simulate session expiry via logout, detect expiry pattern, confirm expiry type, preserve pre-reauth state, re-authenticate, verify re-auth success, resume original task, verify task continuity, context tracking, edge cases, report), 5 warnings (compact state capture, detection priority, flash messages, post-reauth verification, simulate approach), toolPreferences (6 MCP tools). background.js updated with importScripts entry in Utilities section.</done>
</task>

</tasks>

<verification>
1. site-guides/utilities/session-expiry.js exists with registerSiteGuide call
2. Site guide contains handleSessionExpiry workflow with 14 steps
3. Site guide contains CONTEXT-10 guidance section with session expiry detection patterns
4. Site guide documents at least 2 demo targets (herokuapp primary, uitestingplayground/demoqa fallbacks)
5. Site guide documents 4 session expiry detection patterns (modal, redirect, banner, HTTP error)
6. Site guide documents re-auth workflow using type_text for credentials + click for submit
7. Site guide documents task state preservation under 500 characters
8. Site guide documents task resumption after re-auth with content verification
9. Site guide contains 5 warnings about state capture, detection priority, verification
10. background.js contains importScripts entry for session-expiry.js in Utilities section
</verification>

<success_criteria>
- session-expiry.js created with CONTEXT-10 session expiry re-auth workflow and guidance
- handleSessionExpiry workflow documents full detect-reauth-resume cycle
- Demo targets documented with test credentials and selectors
- Session expiry detection patterns documented (modal, redirect, banner, HTTP error)
- Context bloat mitigation for re-auth interruptions documented
- background.js updated with importScripts registration
- Ready for Plan 02 live MCP test
</success_criteria>

<output>
After completion, create `.planning/phases/86-session-expiry-re-auth/86-01-SUMMARY.md`
</output>
