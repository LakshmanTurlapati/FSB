---
phase: 86-session-expiry-re-auth
plan: 02
type: execute
wave: 2
depends_on:
  - 86-01
files_modified:
  - .planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md
autonomous: false
requirements:
  - CONTEXT-10

must_haves:
  truths:
    - "Live MCP test attempted against the-internet.herokuapp.com/login (or fallback) using navigate + type_text + click + read_page + get_dom_snapshot to login, simulate session expiry via logout, detect expiry, re-authenticate, and resume original task, with documented outcome (pass/partial/fail/skip-auth)"
    - "Diagnostic report exists with step-by-step log, what worked, what failed, tool gaps, and autopilot recommendations specific to session expiry detection, re-authentication, and task resumption"
    - "Diagnostic report contains a Context Bloat Analysis section assessing context consumption for session re-auth interruptions (task state snapshot size, DOM reads before vs after re-auth, compact state tracking overhead)"
    - "Human verified the diagnostic report accuracy and outcome classification"
  artifacts:
    - path: ".planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md"
      provides: "CONTEXT-10 autopilot diagnostic report"
      contains: "CONTEXT-10"
  key_links:
    - from: "86-DIAGNOSTIC.md"
      to: "site-guides/utilities/session-expiry.js"
      via: "selector accuracy table references session-expiry.js selectors for herokuapp login form, flash messages, and session expiry indicators"
      pattern: "#username|#password|#flash|loginButton|secureHeading"
    - from: "86-DIAGNOSTIC.md"
      to: "mcp-server/src/tools/manual.ts"
      via: "tool usage section references navigate, click, type_text, read_page, get_dom_snapshot, wait_for_element tools"
      pattern: "navigate|click|type_text|read_page|get_dom_snapshot|wait_for_element"
---

<objective>
Execute live MCP test for CONTEXT-10 (handle session expiration modal, re-authenticate, and resume previous task) and generate the autopilot diagnostic report.

Purpose: Validate whether the MCP navigate + type_text + click + read_page + get_dom_snapshot chain can detect session expiry (via login redirect after forced logout), re-authenticate with test credentials, and resume the original task (reading secure page content). This is a "Context Bloat & Long-Running Workflows" edge case -- the challenge is preserving minimal task state across an authentication interruption without bloating context. The compact task state snapshot ({taskGoal, lastUrl, lastAction, lastResult} under 500 chars) is tested. Expected outcome is PARTIAL (HTTP validation of herokuapp login + secure pages, live login/logout/re-login workflow blocked by WebSocket bridge disconnect) or PASS (if MCP bridge is operational). Produces a structured diagnostic report for the autopilot enhancement milestone.

Output: 86-DIAGNOSTIC.md with CONTEXT-10 test results
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-session-expiry-re-auth/86-CONTEXT.md
@.planning/phases/86-session-expiry-re-auth/86-01-SUMMARY.md
@site-guides/utilities/session-expiry.js
@.planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute live MCP session expiry re-auth test, generate CONTEXT-10 diagnostic report</name>
  <files>.planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md</files>
  <read_first>
    - site-guides/utilities/session-expiry.js (full file -- site guide with handleSessionExpiry workflow, CONTEXT-10 guidance, selectors for herokuapp and session expiry indicators)
    - .planning/phases/85-crm-vs-hr-portal-cross-reference/85-DIAGNOSTIC.md lines 1-15 (diagnostic report template structure -- use exact same format with Context Bloat Analysis section)
    - .planning/phases/86-session-expiry-re-auth/86-01-SUMMARY.md (Plan 01 outcome -- what was added to session-expiry.js)
    - .planning/phases/83-2fa-multi-tab-auth-flow/83-DIAGNOSTIC.md lines 1-15 (prior auth-related diagnostic for comparison reference -- authentication flow patterns)
  </read_first>
  <action>
Execute the CONTEXT-10 edge case test using MCP manual tools following the handleSessionExpiry workflow from the session-expiry.js site guide. Test steps:

1. VALIDATE PRIMARY TARGET LOGIN PAGE (the-internet.herokuapp.com/login):
   (a) Navigate to https://the-internet.herokuapp.com/login -- check if the page loads
   (b) HTTP fetch as fallback: check server response, page size, presence of login form HTML
   (c) Look for #login form, #username, #password, button[type="submit"] selectors in the HTML
   (d) Verify the flash message container (#flash) is present
   (e) Check for the "Login Page" heading (h2)
   Record: URL loaded (yes/no), HTTP status, page size, login form selectors found, page title

2. VALIDATE SECURE PAGE (the-internet.herokuapp.com/secure):
   (a) Attempt to navigate to https://the-internet.herokuapp.com/secure without login
   (b) HTTP fetch: check if it redirects to /login (expected -- session not established)
   (c) Document the redirect behavior (this IS the session expiry detection pattern)
   Record: URL loaded (yes/no), HTTP status, redirect behavior, flash message content

3. VALIDATE LOGOUT ENDPOINT (the-internet.herokuapp.com/logout):
   (a) HTTP fetch https://the-internet.herokuapp.com/logout
   (b) Check redirect behavior (should redirect to /login)
   (c) Check for flash message "You logged out of the secure area!"
   Record: HTTP status, redirect URL, flash message content

4. ATTEMPT FULL SESSION EXPIRY WORKFLOW (if MCP bridge available):
   (a) Navigate to /login, enter "tomsmith" in #username, "SuperSecretPassword!" in #password, click login button
   (b) Verify login success (URL contains /secure, flash message contains "You logged into a secure area!")
   (c) Capture task state: { taskGoal: "read secure page content", currentUrl: "/secure", loggedIn: true }
   (d) Read secure page content via read_page on .example (capture heading and paragraph text)
   (e) Navigate to /logout to simulate session expiry
   (f) Attempt to navigate to /secure (expect redirect to /login -- session expiry detected)
   (g) Use get_dom_snapshot to confirm login form is present (session expired)
   (h) Re-authenticate: type_text on #username with "tomsmith", type_text on #password with "SuperSecretPassword!", click login button
   (i) Verify re-auth success (URL contains /secure, flash contains success message)
   (j) Resume task: read_page on .example again, compare with content from step (d)
   (k) If MCP bridge unavailable: document workflow as designed but not executed

5. VALIDATE FALLBACK TARGETS (if primary fails):
   (a) Fetch https://uitestingplayground.com/sampleapp -- check for login form HTML
   (b) Fetch https://demoqa.com/login -- check for login form HTML
   (c) Record: status, form selectors found, page size for each

6. SESSION EXPIRY DETECTION ANALYSIS:
   (a) Document which of the 4 detection patterns apply to the-internet.herokuapp.com:
       - Pattern A (Modal/Dialog): does the site use modals for session expiry? (likely no -- uses redirect)
       - Pattern B (Login Redirect): does /secure redirect to /login when not authenticated? (expected yes)
       - Pattern C (Inline Banner): does the flash message indicate session state? (expected yes -- #flash element)
       - Pattern D (HTTP 401/403): does the server return auth error codes? (check HTTP status)
   (b) Document which detection method would work without vision (DOM-only)

7. OUTCOME CLASSIFICATION:
   - PASS = full login -> task -> logout -> detect expiry -> re-auth -> resume task cycle completed via MCP tools
   - PARTIAL = HTTP validation confirms login page accessible with selectors matching, session expiry detection pattern documented, but live login/logout/re-login cycle blocked by WebSocket bridge disconnect
   - FAIL = could not validate login page or session behavior on any target
   - SKIP-AUTH = no demo site with session expiry behavior available

Record every step outcome for the diagnostic report.

After test execution (regardless of outcome), generate 86-DIAGNOSTIC.md with ALL of these sections:

## Metadata
- Phase: 86, Requirement: CONTEXT-10, Date: today, Outcome: (PASS/PARTIAL/FAIL/SKIP-AUTH), Live MCP Testing: YES/NO

## Prompt Executed
The exact prompt: "Navigate to a web application, log in, and begin reading a page. The session expires mid-task (simulated via logout). Detect the session expiration, re-authenticate with the same credentials, and resume reading the same page to verify the content matches what was seen before the session expired."

## Result Summary
2-3 sentence summary of what happened.

## Step-by-Step Log
Table with columns: Step | MCP Tool Used | Target | Result | Notes
One row per tool invocation or HTTP fetch attempted.

## What Worked
Bullet list of everything that succeeded.

## What Failed
Bullet list of everything that failed or was not executed, with reasons.

## Tool Gaps Identified
New tool gaps discovered plus reference to known gaps. Consider:
- Did navigate successfully load the-internet.herokuapp.com/login?
- Were login form selectors (#username, #password, button) found in the DOM?
- Could type_text enter test credentials in the login form?
- Could click submit the login form?
- Could read_page extract content from the secure page?
- Could navigate to /logout force a session end?
- Could get_dom_snapshot detect the session expiry (login form present after redirect)?
- Is there a tool gap for session state detection (checking cookies, auth headers)?
- Is there a tool gap for automatic modal detection (aria-modal, role="dialog")?
- WebSocket bridge availability (persistent gap from phases 55-85)

## Context Bloat Analysis (CONTEXT CATEGORY SECTION)
Specific analysis of context management during session re-auth interruptions:
- Task state snapshot size: {taskGoal, lastUrl, lastAction, lastResult} = estimated bytes
- DOM reads per phase: login page read + secure page read (pre-expiry) + login page read (post-expiry) + secure page read (post-reauth) = 4 read_page calls
- Compact state tracking: under 500 chars for entire re-auth cycle vs full DOM retention
- Comparison: DOM size of login page vs secure page vs task state snapshot
- Full workflow context: login form (selectors + credential entry + button click) + secure page (read_page) + logout (navigate) + re-auth (same as login) + resume (read_page again)
- Comparison to Phase 83 (2FA multi-tab auth flow) -- both involve authentication interruption and resumption
- Comparison to Phase 82 (support chatbot 15-turn) -- both are long-running workflows with state tracking
- Recommendations for context-efficient session re-auth handling

## Bugs Fixed In-Phase
Any bugs found and fixed during testing.

## Autopilot Recommendations
8-10 specific recommendations for how autopilot should handle session expiry scenarios. Cover:
- Detection priority: check URL change first (fastest), then DOM for modal/dialog, then flash/alert text, then page content
- Pre-expiry state capture: save {taskGoal, lastUrl, lastAction, lastResult} BEFORE attempting re-auth -- max 500 chars
- Credential management: reuse credentials from initial login, do not ask user again
- Re-auth verification: always navigate to the protected page AFTER re-auth to confirm session is truly restored
- Task resumption: re-execute the last action (e.g., read_page on same selector) and compare results
- Double expiry handling: if session expires again during re-auth, restart from login (max 2 retry cycles)
- Modal dismissal: if session expiry appears as modal overlay, dismiss/interact with modal before re-entering credentials
- Flash message parsing: check #flash or .alert elements for "expired", "logged out", "session" keywords
- Avoid context bloat: discard all DOM snapshots from before the expiry -- only retain the compact task state
- Polling approach: for real session timeouts, periodically check if current page still shows expected content (every 5 read_page calls, verify URL has not changed to /login)

## Selector Accuracy
Table: Selector | Expected | Actual | Match -- for each selector from session-expiry.js tested against live DOM or HTTP. Test at minimum: herokuapp loginForm (#login), usernameField (#username), passwordField (#password), loginButton (button[type="submit"]), flashMessage (#flash), secureHeading (.example h2), logoutButton (a[href="/logout"]).

## New Tools Added This Phase
Table: Tool Name | File | Purpose | Parameters
Note: No new MCP tools added this phase. List the session-expiry.js site guide handleSessionExpiry workflow addition as the new content.

Use the exact same structure and level of detail as 85-DIAGNOSTIC.md, with the Context Bloat Analysis section. Do NOT leave any section as placeholder text like "TBD" or "TODO".
  </action>
  <verify>
    <automated>test -f .planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md && grep -q "CONTEXT-10" .planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md && grep -q "Outcome:" .planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md && grep -q "Autopilot Recommendations" .planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md && grep -q "Step-by-Step Log" .planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md && grep -q "Context Bloat" .planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md && grep -q "Selector Accuracy" .planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md && grep -q "New Tools Added" .planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md && grep -q "herokuapp" .planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md && grep -q "session.expir\|Session Expir\|session expir" .planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md && grep -q "re-auth\|reauth\|re.auth" .planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md && grep -q "resume\|Resume" .planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md && grep -q "#username\|#password" .planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md exists
    - grep "CONTEXT-10" 86-DIAGNOSTIC.md returns at least 1 match
    - grep "Outcome:" 86-DIAGNOSTIC.md returns at least 1 match (PASS, PARTIAL, FAIL, or SKIP-AUTH)
    - grep "Autopilot Recommendations" 86-DIAGNOSTIC.md returns at least 1 match
    - grep "Step-by-Step Log" 86-DIAGNOSTIC.md returns at least 1 match
    - grep "Context Bloat" 86-DIAGNOSTIC.md returns at least 1 match (CONTEXT category section)
    - grep "Selector Accuracy" 86-DIAGNOSTIC.md returns at least 1 match
    - grep "What Worked" 86-DIAGNOSTIC.md returns at least 1 match
    - grep "What Failed" 86-DIAGNOSTIC.md returns at least 1 match
    - grep "herokuapp" 86-DIAGNOSTIC.md returns at least 2 matches (primary target referenced)
    - grep "session.expir\|Session Expir" 86-DIAGNOSTIC.md returns at least 3 matches (core concept documented)
    - grep "re-auth\|reauth" 86-DIAGNOSTIC.md returns at least 3 matches (re-authentication documented)
    - grep "resume\|Resume" 86-DIAGNOSTIC.md returns at least 2 matches (task resumption documented)
    - grep "#username\|#password" 86-DIAGNOSTIC.md returns at least 1 match (login selectors referenced)
    - grep "#flash" 86-DIAGNOSTIC.md returns at least 1 match (flash message selector referenced)
    - grep "New Tools Added" 86-DIAGNOSTIC.md returns at least 1 match
    - No section contains only placeholder text like "TBD" or "TODO"
  </acceptance_criteria>
  <done>CONTEXT-10 diagnostic report generated with real test data covering all required sections: metadata, prompt, result summary, step-by-step log (login page validation with form selectors, secure page redirect behavior, logout endpoint redirect, full login/logout/re-login workflow attempt, fallback target validation, session expiry detection analysis across 4 patterns), what worked, what failed, tool gaps (credential entry, session state detection, modal detection, WebSocket bridge), context bloat analysis (task state snapshot size, DOM reads per phase, compact tracking savings, comparison to Phase 83 2FA and Phase 82 chatbot), bugs fixed, autopilot recommendations (detection priority, pre-expiry state capture, credential reuse, re-auth verification, task resumption comparison, double expiry handling, modal dismissal, flash parsing, context discard, polling approach), selector accuracy (session-expiry.js selectors vs live DOM), new tools (handleSessionExpiry workflow addition)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify CONTEXT-10 diagnostic report accuracy</name>
  <files>.planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md</files>
  <action>
Human verifies the CONTEXT-10 diagnostic report accuracy.

What was built: CONTEXT-10 session expiry re-authentication diagnostic report with MCP test results. The report documents the outcome (PASS/PARTIAL/FAIL/SKIP-AUTH) of attempting to detect session expiration, re-authenticate, and resume the original task on the-internet.herokuapp.com. The workflow tests: (a) login with test credentials (tomsmith/SuperSecretPassword!), (b) reading secure page content, (c) forcing session expiry via logout, (d) detecting the login redirect as session expiry, (e) re-authenticating with same credentials, (f) verifying the secure page content matches pre-expiry. Task state preservation uses a compact snapshot under 500 characters to mitigate context bloat. This phase also created a session-expiry.js site guide with a handleSessionExpiry workflow (14 steps), 4 session expiry detection patterns, and context bloat mitigation guidance. This is a CONTEXT-category edge case -- the diagnostic includes a "Context Bloat Analysis" section assessing context consumption during authentication interruptions.

How to verify:
1. Open .planning/phases/86-session-expiry-re-auth/86-DIAGNOSTIC.md
2. Verify the outcome classification is accurate (PASS/PARTIAL/FAIL/SKIP-AUTH matches what actually happened)
3. Verify step-by-step log has real data (no placeholder rows)
4. Verify login page validation is documented (the-internet.herokuapp.com/login access, form selectors, page structure)
5. Verify secure page redirect behavior is documented (redirect to /login when not authenticated)
6. Verify logout endpoint behavior is documented (redirect to /login with flash message)
7. Verify session expiry detection analysis covers the 4 patterns (modal, redirect, banner, HTTP error)
8. Verify re-authentication workflow is documented (credential entry, login button click, success verification)
9. Verify task resumption is documented (navigate to /secure, read content, compare with pre-expiry capture)
10. Verify Context Bloat Analysis section has specific findings about task state preservation during re-auth
11. Verify autopilot recommendations are specific to session expiry handling (not generic auth recommendations)
12. Verify selector accuracy table has entries from session-expiry.js tested against live DOM or HTTP
13. Confirm the report is complete and ready for the autopilot enhancement milestone

Resume: Type "approved" to accept the diagnostic report, or describe any issues to fix.
  </action>
  <verify>Human confirms by typing "approved" or describes issues</verify>
  <done>Human has reviewed and approved the CONTEXT-10 edge case results and diagnostic report.</done>
</task>

</tasks>

<verification>
1. 86-DIAGNOSTIC.md exists with CONTEXT-10 reference and all required sections
2. Outcome is one of: PASS, PARTIAL, FAIL, SKIP-AUTH
3. Step-by-step log contains at least 8 rows with real tool invocations or HTTP fetch results
4. Login page (the-internet.herokuapp.com/login) validation documented with selectors and page structure
5. Secure page redirect behavior documented (session expiry detection pattern)
6. Logout endpoint redirect behavior documented
7. Session expiry detection analysis covers 4 patterns with applicability to herokuapp
8. Re-authentication workflow documented (credential entry, success verification)
9. Task resumption documented (content comparison before and after re-auth)
10. Context Bloat Analysis section has specific findings (task state snapshot size, DOM reads, compact tracking)
11. Autopilot recommendations section has at least 8 specific items about session expiry handling
12. Selector accuracy table has entries from session-expiry.js tested against live DOM or HTTP
13. New Tools Added section notes handleSessionExpiry workflow addition to session-expiry.js
14. Human approved the diagnostic report
</verification>

<success_criteria>
- CONTEXT-10 edge case attempted via MCP manual tools with documented outcome
- Diagnostic report generated with all required sections filled with real data
- Session expiry detection, re-authentication, and task resumption documented (or reason for incomplete execution)
- Login page and secure page validated with selector accuracy
- 4 session expiry detection patterns analyzed for applicability
- Context Bloat Analysis section provides insights for session re-auth interruptions
- Human verified and approved the report accuracy
- Phase 86 complete, ready to proceed to Phase 87 (DARK-01)
</success_criteria>

<output>
After completion, create `.planning/phases/86-session-expiry-re-auth/86-02-SUMMARY.md`
</output>
