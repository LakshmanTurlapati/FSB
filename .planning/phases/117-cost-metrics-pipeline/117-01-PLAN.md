---
phase: 117-cost-metrics-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - background.js
autonomous: true
requirements:
  - COST-01
  - COST-02
  - COST-03

must_haves:
  truths:
    - "executeAutomationTask resolve object includes real tokensUsed from session accumulator"
    - "executeAutomationTask resolve object includes real costUsd from session accumulator"
    - "Agent recordedScript.estimatedCostPerRun reflects actual cost of the recording run"
    - "Agent stats dashboard shows accurate cumulative cost and tokens"
  artifacts:
    - path: "background.js"
      provides: "executeAutomationTask with real cost/token data in all resolve paths"
      contains: "session.totalCost"
  key_links:
    - from: "ai/ai-integration.js"
      to: "background.js:accumulateSessionCost"
      via: "trackTokenUsage calls accumulateSessionCost per iteration"
      pattern: "accumulateSessionCost"
    - from: "background.js:executeAutomationTask"
      to: "agents/agent-executor.js"
      via: "resolve object with tokensUsed and costUsd"
      pattern: "costUsd.*totalCost"
    - from: "agents/agent-executor.js"
      to: "agents/agent-manager.js:recordRunResult"
      via: "runResult with costUsd propagated to agent.totalCostUsd"
      pattern: "totalCostUsd.*costUsd"
---

<objective>
Wire real token count and cost data from the session accumulator into executeAutomationTask's resolve object, replacing hardcoded zeros.

Purpose: The AI provider already returns usage data per call, and accumulateSessionCost already sums it on the session object. But executeAutomationTask's completion listener ignores these totals and returns hardcoded zeros. This single disconnect causes all downstream consumers (agent run history, estimatedCostPerRun, stats dashboard) to show zero/placeholder costs.

Output: background.js with all 4 resolve paths in executeAutomationTask reading real session cost data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire session cost data into executeAutomationTask resolve paths</name>
  <files>background.js</files>
  <read_first>background.js (lines 6370-6524 for executeAutomationTask, lines 3759-3767 for accumulateSessionCost, lines 6418-6421 for session cost fields)</read_first>
  <action>
In background.js function `executeAutomationTask` (starts at line 6381), there are 4 resolve paths that return hardcoded zero for tokensUsed and costUsd. Fix all 4:

**Path 1 - automationComplete listener (line 6459):**
Current:
```
tokensUsed: metrics?.totalActions || 0,
costUsd: 0,
```
Replace with:
```
tokensUsed: (session.totalInputTokens || 0) + (session.totalOutputTokens || 0),
costUsd: session.totalCost || 0,
```

**Path 2 - automationError listener (line 6481):**
Current:
```
tokensUsed: 0,
costUsd: 0,
```
Replace with:
```
tokensUsed: (sessionData.totalInputTokens || 0) + (sessionData.totalOutputTokens || 0),
costUsd: sessionData.totalCost || 0,
```

**Path 3 - safety timeout (line 6499):**
Current:
```
tokensUsed: 0,
costUsd: 0,
```
Replace with:
```
tokensUsed: (sessionData.totalInputTokens || 0) + (sessionData.totalOutputTokens || 0),
costUsd: sessionData.totalCost || 0,
```

**Path 4 - catch block (line 6518):**
Leave as `tokensUsed: 0, costUsd: 0` -- this fires before any AI calls happen so there is nothing to accumulate.

Why this works: `accumulateSessionCost` (line 3759) is already called by `ai-integration.js:trackTokenUsage` after every AI API call. It sums `totalCost`, `totalInputTokens`, and `totalOutputTokens` on the session object. The session fields are initialized at line 6419-6421. The downstream chain (agent-executor reads `result.costUsd`, agent-manager sums into `agent.totalCostUsd`, options page reads `stats.totalCost`) all already work -- they just receive zero from the source.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/laksh/OneDrive/Documents/Codes/FSB && grep -n "session\.totalCost\|session\.totalInputTokens\|sessionData\.totalCost\|sessionData\.totalInputTokens" background.js | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Line ~6465: `tokensUsed:` references `session.totalInputTokens` and `session.totalOutputTokens` (not `metrics?.totalActions`)
    - Line ~6466: `costUsd:` references `session.totalCost` (not hardcoded `0`)
    - Line ~6481: `tokensUsed:` references `sessionData.totalInputTokens` and `sessionData.totalOutputTokens`
    - Line ~6482: `costUsd:` references `sessionData.totalCost`
    - Line ~6499: `tokensUsed:` references `sessionData.totalInputTokens` and `sessionData.totalOutputTokens`
    - Line ~6500: `costUsd:` references `sessionData.totalCost`
    - Line ~6518: `tokensUsed: 0` unchanged (catch block before any AI calls)
    - grep for `costUsd: 0` inside executeAutomationTask returns only the catch block (line ~6519)
  </acceptance_criteria>
  <done>All 3 active resolve paths in executeAutomationTask return real session-accumulated tokensUsed and costUsd. The catch block (path 4) correctly stays at zero since no AI calls have been made when it fires.</done>
</task>

<task type="auto">
  <name>Task 2: Verify end-to-end cost data flow</name>
  <files>agents/agent-executor.js, agents/agent-manager.js, ui/options.js</files>
  <read_first>agents/agent-executor.js (lines 186-187, 232-233, 310-320), agents/agent-manager.js (lines 240-254), ui/options.js (lines 3892-3911)</read_first>
  <action>
Verify (read-only) that the downstream consumers already correctly propagate cost data. No code changes expected -- this task confirms the chain is intact:

1. **agent-executor.js line 186-187**: AI fallback path reads `aiResult.tokensUsed` and `aiResult.costUsd` -- these come from executeAutomationTask resolve. Correct.

2. **agent-executor.js line 232-233**: AI initial path reads `result.tokensUsed` and `result.costUsd` -- same source. Correct.

3. **agent-executor.js line 310**: `estimatedCostPerRun = result.costUsd || 0.002` -- will now get real cost instead of always falling back to 0.002. Correct.

4. **agent-manager.js line 240-241**: `recordRunResult` stores `runResult.tokensUsed` and `runResult.costUsd` in run entry. Correct.

5. **agent-manager.js line 253-254**: Accumulates `runEntry.tokensUsed` and `runEntry.costUsd` into agent totals. Correct.

6. **ui/options.js line 3906**: Displays `stats.totalCost.toFixed(4)`. Correct.

If any link is broken (e.g., field name mismatch), fix it. Otherwise, confirm the chain is intact by running the grep verification.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/laksh/OneDrive/Documents/Codes/FSB && echo "=== executor reads costUsd ===" && grep -n "costUsd" agents/agent-executor.js | head -10 && echo "=== manager stores costUsd ===" && grep -n "costUsd" agents/agent-manager.js | head -10 && echo "=== options displays totalCost ===" && grep -n "totalCost" ui/options.js | head -5</automated>
  </verify>
  <acceptance_criteria>
    - agent-executor.js contains `result.costUsd` or `aiResult.costUsd` in return objects
    - agent-executor.js line ~310 contains `result.costUsd || 0.002` for estimatedCostPerRun
    - agent-manager.js contains `runResult.costUsd` being stored and accumulated into `totalCostUsd`
    - ui/options.js contains `stats.totalCost.toFixed(4)` display
    - No field name mismatches between producer and consumer
  </acceptance_criteria>
  <done>End-to-end cost data flow verified: executeAutomationTask -> agent-executor -> agent-manager -> options page stats. All field names match and data propagates correctly.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "costUsd: 0" background.js` inside executeAutomationTask should show only 1 occurrence (the catch block)
2. `grep "session.totalCost\|sessionData.totalCost" background.js` should show 3 occurrences (the 3 active resolve paths)
3. The chain from AI provider -> session accumulator -> executeAutomationTask resolve -> agent-executor -> agent-manager -> options page is unbroken
</verification>

<success_criteria>
- COST-01: executeAutomationTask resolve includes real tokensUsed and costUsd from session accumulator
- COST-02: Agent recordedScript.estimatedCostPerRun reflects actual cost (reads from result.costUsd which is now real)
- COST-03: Agent stats dashboard shows accurate cumulative cost (totalCostUsd accumulates real values)
</success_criteria>

<output>
After completion, create `.planning/phases/117-cost-metrics-pipeline/117-01-SUMMARY.md`
</output>
</output>
