---
phase: 94-buried-login-link
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site-guides/utilities/buried-login-link.js
  - background.js
autonomous: true
requirements:
  - DARK-08

must_haves:
  truths:
    - "The buried-login-link.js site guide contains a findBuriedLoginLink workflow documenting the full sequence: navigate to a homepage or landing page dominated by Sign Up CTAs, use get_dom_snapshot to find ALL anchor and button elements, classify each by text content and href attribute into login vs signup categories, identify the login link by matching login-intent keywords (log in, sign in, already have an account, existing user, member login) while excluding signup-intent keywords (sign up, create account, get started, register, join, try free), click the login link, and verify the login page or form loads"
    - "The buried-login-link.js site guide contains a DARK-08 section explaining the dark pattern: SaaS and freemium sites deliberately make Sign Up/Get Started CTAs visually dominant (large, colorful, prominent placement) while burying the Login link in small text, footer, or behind a hamburger menu to maximize new signups over returning user convenience"
    - "The buried-login-link.js site guide documents login link as the primary identification strategy: semantic text analysis of all clickable elements on the page to find login-intent text, with href pattern matching as secondary confirmation (href containing /login, /signin, /auth)"
    - "The buried-login-link.js site guide documents at least 6 hiding techniques used to bury login links and how to counter each through DOM-only text and attribute analysis"
  artifacts:
    - path: "site-guides/utilities/buried-login-link.js"
      provides: "findBuriedLoginLink workflow and DARK-08 guidance for finding login links buried among dominant signup CTAs"
      contains: "findBuriedLoginLink"
    - path: "background.js"
      provides: "importScripts entry for buried-login-link.js in Utilities section"
      contains: "buried-login-link.js"
  key_links:
    - from: "site-guides/utilities/buried-login-link.js"
      to: "mcp-server/src/tools/manual.ts"
      via: "toolPreferences referencing get_dom_snapshot, get_text, get_attribute, click, scroll, navigate"
      pattern: "get_dom_snapshot|get_text|get_attribute|click|scroll|navigate"
    - from: "background.js"
      to: "site-guides/utilities/buried-login-link.js"
      via: "importScripts registration in Utilities section"
      pattern: "importScripts.*buried-login-link"
---

<objective>
Create a buried-login-link.js site guide with a findBuriedLoginLink workflow and DARK-08 guidance for finding and clicking the actual login link on homepages that visually prioritize Sign Up CTAs over returning-user login access.

Purpose: DARK-08 requires finding the actual login link on a homepage that prioritizes Sign Up CTAs. SaaS, freemium, and subscription sites deliberately make their Sign Up / Get Started / Create Account buttons visually dominant (large, colorful, above-fold, centered) while burying the Login / Sign In link in small text, footer, navigation overflow, or behind a hamburger menu. The dark pattern exploits the visual hierarchy to funnel users toward new signups rather than returning-user login. The AI must use DOM text analysis to find ALL clickable elements, classify each as login-intent vs signup-intent by analyzing text content and href attributes, and click the login link regardless of its visual prominence. The site guide must document: common hiding techniques (small font, footer placement, hamburger menu, "Already have an account?" text, color de-emphasis, conditional rendering after scroll), identification strategies (text keyword matching, href pattern matching, ARIA attribute analysis), and a multi-step workflow immune to visual misdirection.

Output: site-guides/utilities/buried-login-link.js and background.js updated with importScripts entry
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-buried-login-link/94-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create buried-login-link.js site guide with findBuriedLoginLink workflow and DARK-08 guidance</name>
  <files>site-guides/utilities/buried-login-link.js, background.js</files>
  <read_first>
    - site-guides/utilities/freeware-download.js lines 1-60 (reference for DARK-01 elimination-based detection pattern with DOM attribute analysis to distinguish real from fake)
    - site-guides/utilities/camouflaged-close.js lines 1-60 (reference for DARK-04 3-tier DOM attribute detection ranking: attributes > content > fallback)
    - site-guides/utilities/shuffled-cancel.js lines 1-60 (reference for DARK-03 text-based button identification using semantic text analysis rather than position/style)
    - site-guides/utilities/cookie-opt-out.js lines 1-60 (reference for DARK-02 hidden element detection in DOM with keyword-based classification)
    - background.js lines 191-195 (Utilities section importScripts order -- new guide goes after newsletter-uncheck.js as last Utilities entry)
    - .planning/phases/94-buried-login-link/94-CONTEXT.md (phase context -- buried login link discovery via DOM text analysis)
  </read_first>
  <action>
Create a new file site-guides/utilities/buried-login-link.js. This site guide targets SaaS homepages, freemium landing pages, and subscription sites that visually prioritize Sign Up CTAs while burying the Login link. This is the DARK-08 dark pattern avoidance edge case.

**Distinction from other DARK patterns:** DARK-08 is NOT about hidden buttons (DARK-04) or fake buttons (DARK-01) or shuffled buttons (DARK-03). It is about visual hierarchy manipulation: both Login and Sign Up links exist in the DOM and are technically accessible, but the site's design makes Sign Up visually dominant (large, colorful, centered, above-fold) while Login is de-emphasized (small text, muted color, footer, or behind a menu). The counter-strategy is semantic text classification of all clickable elements to find login-intent text regardless of visual prominence.

**Login link hiding techniques documented in the guide:**

1. **Small font / muted color:** Login link rendered in 12-13px font with gray or light color while Sign Up button uses 16-18px bold with high-contrast primary color. The login link is technically visible but easily overlooked.
2. **Footer burial:** Login link placed in the page footer among other utility links (privacy policy, terms, about), far below the dominant hero section with Sign Up CTA. Users who focus on the main content area never scroll down to find it.
3. **Hamburger menu hiding:** On both mobile and some desktop layouts, the Login link is placed inside a hamburger menu or dropdown navigation while the Sign Up button remains as a persistent, always-visible CTA in the header.
4. **"Already have an account?" de-emphasis:** Login access provided as a small text link below or beside the Sign Up form with phrasing like "Already have an account? Log in" in small, muted text. The visual weight is entirely on the Sign Up form above.
5. **CTA button vs text link asymmetry:** Sign Up is a styled button (background color, padding, border-radius, shadow) while Login is a plain text link with no button styling. The visual affordance strongly suggests Sign Up is the primary action.
6. **Conditional rendering / scroll-gated:** Login link only appears after scrolling past the hero section, or is hidden until the user interacts with a "More options" link. Above-fold content shows only Sign Up.
7. **Tab or toggle hiding:** Login and Sign Up are on separate tabs within the same form area, with the Sign Up tab selected by default. Login requires clicking a tab label that may be styled to look inactive.
8. **Redirect-first pattern:** Clicking any CTA (including ambiguous ones like "Get Started") redirects to the signup flow, with a small "Already have an account? Log in here" link on the signup page itself, not on the original homepage.

**The registerSiteGuide call must include:**

1. `site`: 'Buried Login Link'
2. `category`: 'Utilities'
3. `patterns`: Array of RegExp matching login-finding contexts: /buried.*login/i, /login.*link/i, /find.*login/i, /sign.*in/i, /log.*in/i, /already.*account/i, /existing.*user/i, /returning.*user/i, /member.*login/i, /login.*hidden/i, /login.*buried/i, /signup.*dominant/i, /dropbox\.com/i, /slack\.com/i, /notion\.so/i, /canva\.com/i, /figma\.com/i, /hubspot\.com/i, /mailchimp\.com/i, /trello\.com/i, /asana\.com/i, /zoom\.us/i, /atlassian\.com/i
4. `guidance`: Multi-line template string with these sections:
   - BURIED LOGIN LINK INTELLIGENCE (DARK-08)
   - DARK PATTERN CONTEXT section explaining: SaaS, freemium, and subscription sites deliberately manipulate visual hierarchy to funnel visitors toward new signups. The Sign Up / Get Started / Create Account CTA is made visually dominant -- large button, primary color, centered, above the fold, repeated multiple times on the page. The Login / Sign In link for existing users is intentionally de-emphasized: placed in small text, muted color, footer, hamburger menu, or behind "Already have an account?" phrasing. The AI has no vision and cannot see visual weight differences -- it must SCAN ALL CLICKABLE ELEMENTS (links and buttons) on the page, CLASSIFY each by text content as login-intent or signup-intent, and CLICK the login-intent element regardless of its visual prominence or position.
   - LOGIN vs SIGNUP CLASSIFICATION STRATEGY section: The core identification approach is text content and href attribute analysis:
     Step A: SCAN ALL CLICKABLE ELEMENTS -- Use get_dom_snapshot to find every a[href], button, and [role="button"] element on the page. Include elements inside nav, header, footer, and any hamburger/dropdown menus.
     Step B: EXTRACT TEXT AND HREF -- For each clickable element, extract: (1) text content (innerText), (2) href attribute, (3) aria-label, (4) title attribute, (5) class name (may contain "login" or "signin" keywords).
     Step C: CLASSIFY EACH ELEMENT -- Based on text and href, classify into:
       LOGIN-INTENT indicators (target for clicking): "log in", "login", "sign in", "signin", "already have an account", "existing user", "returning user", "member login", "my account", "account login" -- OR href containing /login, /signin, /auth/login, /accounts/login, /session/new
       SIGNUP-INTENT indicators (skip these): "sign up", "signup", "create account", "get started", "register", "join", "try free", "start free trial", "start now", "join free" -- OR href containing /signup, /register, /create-account, /trial, /pricing
       AMBIGUOUS indicators (examine href for confirmation): "get started" (could be login if href points to /login), "continue" (check href), icon-only buttons (check aria-label)
     Step D: CHECK HEADER AND FOOTER -- Login links are commonly placed in the top-right of the header (nav > a) or in the footer. Scan both areas specifically. If the header has only a Sign Up button, check the footer and hamburger menu.
     Step E: EXPAND MENUS -- If the page has a hamburger menu (common on mobile-responsive layouts), click it to reveal hidden navigation items including the login link. Look for: button[aria-label*="menu"], .hamburger, .nav-toggle, [data-toggle="collapse"].
     Step F: CHECK SIGNUP PAGE FALLBACK -- If no login link is found on the homepage, the login link may be on the signup page itself (redirect-first pattern). Navigate to the signup page and look for "Already have an account? Log in" text.
   - TARGET SITES section documenting SaaS sites known for buried login links:
     Dropbox (dropbox.com): Sign Up button dominant, "Sign in" link in small text in header
     Slack (slack.com): "Try for Free" and "Get Started" CTAs dominant, "Sign in" in top-right corner small text
     Notion (notion.so): "Get Notion Free" primary CTA, "Log in" link in header navigation
     Canva (canva.com): "Sign up" prominent, "Log in" as secondary text link
     Figma (figma.com): "Get started for free" dominant CTA, "Log in" as small header link
     HubSpot (hubspot.com): Multiple "Get started free" CTAs, "Log in" in header top-right small text
     Mailchimp (mailchimp.com): "Sign Up Free" dominant, "Log In" in header
     Generic SaaS pattern: Primary CTA is always signup-oriented, login is always secondary text
   - VERIFICATION section: After clicking the login link, verify the login page loaded: look for email/username input field, password input field, "Forgot password?" link, or "Sign in" form heading. If the page still shows the signup form, the wrong link was clicked.
5. `selectors`: Object with selectors for login link detection:
   - loginLink: { byText: 'a:has-text("Log in"), a:has-text("Sign in"), a:has-text("Login"), a:has-text("Signin"), a:has-text("Already have an account")', byHref: 'a[href*="/login"], a[href*="/signin"], a[href*="/auth"], a[href*="/session"], a[href*="/accounts/login"]', byAria: '[aria-label*="log in" i], [aria-label*="sign in" i], [aria-label*="login" i]', byClass: '[class*="login"], [class*="signin"], [class*="sign-in"], [class*="log-in"]', inHeader: 'header a[href*="/login"], nav a[href*="/login"], header a[href*="/signin"], nav a[href*="/signin"]', inFooter: 'footer a[href*="/login"], footer a[href*="/signin"]' }
   - signupButton: { byText: 'a:has-text("Sign up"), a:has-text("Get started"), a:has-text("Create account"), button:has-text("Sign up"), button:has-text("Get started")', byHref: 'a[href*="/signup"], a[href*="/register"], a[href*="/create-account"], a[href*="/trial"]' }
   - hamburgerMenu: { trigger: 'button[aria-label*="menu" i], .hamburger, .nav-toggle, [data-toggle="collapse"], button[aria-expanded]', content: '.nav-menu, .dropdown-menu, [aria-expanded="true"] + *, nav[class*="mobile"]' }
   - loginForm: { emailInput: 'input[type="email"], input[name="email"], input[name="username"], input[id*="email"], input[id*="username"]', passwordInput: 'input[type="password"]', forgotPassword: 'a[href*="forgot"], a[href*="reset"], a:has-text("Forgot")' }
6. `workflows`: Object with findBuriedLoginLink array of 8 steps:
   Step 1: NAVIGATE TO HOMEPAGE -- Use navigate to load the site homepage or landing page. Wait for the page to fully render (use get_dom_snapshot to confirm navigation elements are present).
   Step 2: SCAN ALL CLICKABLE ELEMENTS -- Use get_dom_snapshot to find every a[href], button, and [role="button"] on the page. Record each element's text content, href, class, id, and aria-label.
   Step 3: CLASSIFY ELEMENTS -- For each clickable element, classify as LOGIN-INTENT, SIGNUP-INTENT, or OTHER using the text keyword lists and href patterns from the classification strategy. Count how many login-intent vs signup-intent elements exist.
   Step 4: CHECK HEADER AND FOOTER SPECIFICALLY -- Scan the header (nav, top bar) and footer areas for login links that may be visually de-emphasized but present in the DOM. Many sites place "Sign in" in the top-right nav with no button styling.
   Step 5: EXPAND HAMBURGER MENU -- If no login link found in visible navigation, check for hamburger/mobile menu triggers. Click to expand and rescan for login links inside the revealed menu.
   Step 6: CLICK LOGIN LINK -- Click the login-intent element identified in Steps 2-5. Prefer: (a) links with explicit "Log in" or "Sign in" text, (b) links with /login or /signin in href, (c) header-positioned login links over footer ones.
   Step 7: VERIFY LOGIN PAGE -- After clicking, verify the login page loaded: use get_dom_snapshot to confirm presence of email/username input, password input, and/or "Forgot password" link. If the signup page loaded instead, go back and try the next login-intent element.
   Step 8: FALLBACK TO SIGNUP PAGE -- If no login link found on homepage, navigate to the signup page and look for "Already have an account? Log in" text link. This covers the redirect-first dark pattern where login access is only available from the signup page.
7. `warnings`: Array of 5 strings:
   - 'DARK-08: Sites bury the Login link among dominant Sign Up CTAs to maximize new signups. SCAN ALL clickable elements by text content, CLASSIFY as login-intent vs signup-intent, and CLICK the login link regardless of its visual prominence or position.'
   - 'NEVER click the first prominent CTA button -- it is almost always Sign Up or Get Started, not Login. Always classify ALL clickable elements before choosing which to click.'
   - 'Login links are commonly found as: small text in the header top-right, plain text link (not button) near the Sign Up button, "Already have an account?" text below signup forms, or inside hamburger menus.'
   - 'If the page has multiple Sign Up buttons and no visible Login link, check the footer, hamburger menu, and any expandable navigation sections. The login link is almost always present somewhere in the DOM.'
   - 'After clicking what you believe is the login link, VERIFY the destination has a password input field. If you landed on a signup form, you clicked the wrong link -- go back and find the actual login link.'
8. `toolPreferences`: Array including get_dom_snapshot, get_text, get_attribute, click, scroll, navigate

Then update background.js: Add `importScripts('site-guides/utilities/buried-login-link.js');` after the `importScripts('site-guides/utilities/newsletter-uncheck.js');` line (line ~193) in the Utilities section, making it the last Utilities import before the agent modules section.
  </action>
  <verify>
    <automated>grep -q "findBuriedLoginLink" site-guides/utilities/buried-login-link.js && grep -q "DARK-08" site-guides/utilities/buried-login-link.js && grep -q "registerSiteGuide" site-guides/utilities/buried-login-link.js && grep -q "login\|signin\|sign.in\|log.in" site-guides/utilities/buried-login-link.js && grep -q "signup\|sign.up\|create.account\|get.started" site-guides/utilities/buried-login-link.js && grep -q "get_dom_snapshot\|get_text\|get_attribute" site-guides/utilities/buried-login-link.js && grep -q "header\|footer\|hamburger\|nav" site-guides/utilities/buried-login-link.js && grep -q "buried-login-link" background.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "findBuriedLoginLink" buried-login-link.js returns at least 1 match (workflow name)
    - grep "DARK-08" buried-login-link.js returns at least 2 matches (guidance section and warning)
    - grep "registerSiteGuide" buried-login-link.js returns exactly 1 match (registration call)
    - grep "login\|signin\|sign.in\|log.in" buried-login-link.js returns at least 15 matches (login-intent keywords and selectors)
    - grep "signup\|sign.up\|create.account\|get.started\|register" buried-login-link.js returns at least 8 matches (signup-intent classification)
    - grep "get_dom_snapshot\|get_text\|get_attribute\|click" buried-login-link.js returns at least 5 matches (MCP tools for DOM analysis)
    - grep "header\|footer\|hamburger\|nav\|menu" buried-login-link.js returns at least 6 matches (location-based scanning documentation)
    - grep "small.*text\|muted\|de-emphasis\|font.*size\|color.*gray\|buried\|hidden" buried-login-link.js returns at least 3 matches (hiding technique documentation)
    - grep "Already have an account\|existing user\|returning user" buried-login-link.js returns at least 2 matches (fallback phrasing patterns)
    - grep "password\|email.*input\|username" buried-login-link.js returns at least 2 matches (login page verification)
    - grep "dropbox\|slack\|notion\|canva\|figma\|hubspot\|mailchimp" buried-login-link.js returns at least 3 matches (target site documentation)
    - grep "buried-login-link" background.js returns at least 1 match (importScripts entry)
    - The background.js importScripts entry for buried-login-link.js appears after newsletter-uncheck.js in the Utilities section
  </acceptance_criteria>
  <done>buried-login-link.js created with: registerSiteGuide call (site, category, patterns for login/signin/SaaS site contexts), DARK-08 guidance section (dark pattern context explaining visual hierarchy manipulation with 8 hiding techniques -- small font/muted color, footer burial, hamburger menu, already-have-an-account de-emphasis, CTA vs text link asymmetry, conditional rendering, tab/toggle hiding, redirect-first pattern; login vs signup classification strategy with 6 steps -- scan all clickable elements, extract text and href, classify each element, check header and footer, expand menus, check signup page fallback; target sites for testing; login page verification), selectors for login links by text/href/aria/class/header/footer, signup buttons, hamburger menus, and login form elements, findBuriedLoginLink workflow (8 steps: navigate to homepage, scan all clickable elements, classify elements, check header and footer, expand hamburger menu, click login link, verify login page, fallback to signup page), 5 warnings (scan all elements, never click first CTA, common login locations, check footer and menus, verify destination), toolPreferences (6 MCP tools). background.js updated with importScripts entry in Utilities section.</done>
</task>

</tasks>

<verification>
1. site-guides/utilities/buried-login-link.js exists with registerSiteGuide call
2. Site guide contains findBuriedLoginLink workflow with 8 steps
3. Site guide contains DARK-08 guidance section with dark pattern context
4. Site guide documents 8 hiding techniques for login links
5. Site guide documents login vs signup classification strategy (scan, extract, classify, check locations, expand menus, fallback)
6. Site guide documents target SaaS sites for testing (Dropbox, Slack, Notion, Canva, Figma, HubSpot, Mailchimp)
7. Site guide documents login page verification criteria (email input, password input, forgot password link)
8. Site guide contains selectors for login links by text, href, aria, class, header placement, footer placement
9. Site guide contains 5 warnings about text-based identification and avoiding signup CTAs
10. background.js contains importScripts entry for buried-login-link.js in Utilities section
</verification>

<success_criteria>
- buried-login-link.js created with DARK-08 buried login link dark pattern avoidance workflow and guidance
- findBuriedLoginLink workflow documents full navigate-scan-classify-click-verify cycle using DOM text analysis
- Login link identification strategy explicitly documented (scan all clickable elements, classify by text and href, distinguish login from signup)
- Eight hiding techniques documented with counter-strategies for each
- Login vs signup classification criteria clearly specified with keyword lists
- background.js updated with importScripts registration
- Ready for Plan 02 live MCP test
</success_criteria>

<output>
After completion, create `.planning/phases/94-buried-login-link/94-01-SUMMARY.md`
</output>
