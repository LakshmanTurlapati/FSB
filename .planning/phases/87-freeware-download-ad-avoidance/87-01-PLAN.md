---
phase: 87-freeware-download-ad-avoidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site-guides/utilities/freeware-download.js
  - background.js
autonomous: true
requirements:
  - DARK-01

must_haves:
  truths:
    - "The freeware-download.js site guide contains a downloadRealFile workflow documenting the full sequence: navigate to freeware site, analyze all download links on the page, distinguish real download links from fake ad download buttons using DOM attribute analysis (href domain, class names, iframe/ad indicators, data-* attributes, sponsored markers), click only the real download link"
    - "The freeware-download.js site guide contains a DARK-01 section explaining dark pattern avoidance for freeware download pages: fake download button identification via ad network domains in href/src, iframe wrappers, ad-related class names (ad, sponsored, promoted, banner), and mismatch between visible text and actual link destination"
    - "The freeware-download.js site guide documents ad detection heuristics for at least 5 fake download button indicators: ad network domains (googleads, doubleclick, adsense, taboola, outbrain), iframe wrappers, ad-related CSS class patterns, data-ad-* attributes, and external tracking redirect URLs"
    - "The freeware-download.js site guide documents at least 2 freeware download site targets with selectors for real download links and known fake ad button patterns"
  artifacts:
    - path: "site-guides/utilities/freeware-download.js"
      provides: "downloadRealFile workflow and DARK-01 guidance for distinguishing real download links from fake ad download buttons on freeware sites"
      contains: "downloadRealFile"
    - path: "background.js"
      provides: "importScripts entry for freeware-download.js in Utilities section"
      contains: "freeware-download.js"
  key_links:
    - from: "site-guides/utilities/freeware-download.js"
      to: "mcp-server/src/tools/manual.ts"
      via: "toolPreferences referencing navigate, click, read_page, get_dom_snapshot, get_attribute"
      pattern: "navigate|click|read_page|get_dom_snapshot|get_attribute"
    - from: "background.js"
      to: "site-guides/utilities/freeware-download.js"
      via: "importScripts registration in Utilities section"
      pattern: "importScripts.*freeware-download"
---

<objective>
Create a freeware-download.js site guide with a downloadRealFile workflow and DARK-01 guidance for navigating freeware download sites while distinguishing real download links from fake ad "Download Now" buttons.

Purpose: DARK-01 requires downloading a file from a freeware site while ignoring fake "Download Now" ad buttons. This is a "Visual Ambiguity & Dark Patterns" edge case -- the challenge is that freeware sites (SourceForge, Softonic, CNET, FileHippo) display multiple prominent "Download" buttons, most of which are advertisements that link to external ad network domains or redirect through tracking URLs. The AI must use DOM analysis (href attributes, iframe wrappers, ad-related class names, data attributes) to identify which download link is the real one. The site guide must document: (a) target freeware sites with known fake download ad patterns, (b) ad detection heuristics (5+ indicators), (c) the real download link identification workflow, (d) selectors for real vs fake buttons on target sites.

Output: site-guides/utilities/freeware-download.js and background.js updated with importScripts entry
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-freeware-download-ad-avoidance/87-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create freeware-download.js site guide with downloadRealFile workflow and DARK-01 guidance</name>
  <files>site-guides/utilities/freeware-download.js, background.js</files>
  <read_first>
    - site-guides/utilities/session-expiry.js lines 1-60 (reference for Utilities site guide structure: registerSiteGuide call with site, category, patterns, guidance, selectors, workflows, warnings, toolPreferences -- closest recently-created analog)
    - site-guides/utilities/slider-captcha.js lines 1-40 (reference for site guide structure in Utilities category -- another edge case guide)
    - background.js lines 180-190 (Utilities section importScripts order -- new guide goes after session-expiry.js as last entry before agent modules)
    - .planning/phases/87-freeware-download-ad-avoidance/87-CONTEXT.md (phase context -- freeware download ad avoidance, DOM-based ad detection)
    - mcp-server/src/tools/manual.ts lines 1-30 (tool signatures for navigate, click, read_page, get_dom_snapshot, get_attribute)
  </read_first>
  <action>
Create a new file site-guides/utilities/freeware-download.js. This site guide targets freeware download sites with fake "Download Now" ad buttons for the DARK-01 dark pattern avoidance edge case.

**Freeware download site targets:**

Primary target: sourceforge.net -- widely used open-source hosting site. Download pages (e.g., sourceforge.net/projects/vlc/files/ or sourceforge.net/projects/7zip/) contain a prominent green "Download" button for the real file, surrounded by ad-served banners and buttons. The real download link has href pointing to sourceforge.net/projects/*/files/*/download or downloads.sourceforge.net domain. Fake ad buttons are served inside iframes or have href pointing to ad network domains (googleadservices.com, doubleclick.net, adsrvr.org, etc.).

Secondary target: filehippo.com -- freeware aggregator with download pages containing both a real download button and multiple ad-served download banners. The real download button typically has an href containing filehippo.com/download_* pattern. Ad buttons often appear in div elements with class names containing "ad", "sponsored", or "banner".

Fallback 1: fosshub.com -- open-source software hosting. Cleaner than SourceForge but still has ad banners near download buttons. Real download links point to fosshub.com/*/download or directly to the software file.

Fallback 2: majorgeeks.com -- freeware download site with multiple mirror links. Real download links point to majorgeeks.com/mg/getmirror/* pattern. Ad download buttons link to external domains.

Fallback 3: ninite.com -- bulk installer site. Simpler layout but useful as a contrast example where the download button is unambiguous (single "Get Your Ninite" button).

**The registerSiteGuide call must include:**

1. `site`: 'Freeware Download Ad Avoidance'
2. `category`: 'Utilities'
3. `patterns`: Array of RegExp matching freeware download sites and dark pattern contexts: /freeware.download/i, /fake.download/i, /ad.avoidance/i, /sourceforge\.net/i, /filehippo\.com/i, /fosshub\.com/i, /majorgeeks\.com/i, /ninite\.com/i, /download.*button/i
4. `guidance`: Multi-line template string with these sections:
   - FREEWARE DOWNLOAD AD AVOIDANCE INTELLIGENCE (DARK-01)
   - DARK PATTERN CONTEXT section explaining: Freeware download sites monetize through ad networks that serve fake "Download" buttons designed to look like the real download link. These ads are intentionally styled to match the site's theme and are positioned near the real download button to trick users into clicking them. The AI must use DOM analysis, NOT visual similarity, to distinguish real from fake.
   - TARGET SITES section listing primary and fallback targets with URLs and expected download page structures
   - AD DETECTION HEURISTICS section documenting 8 indicators of fake download buttons:
     Indicator 1: Ad network domains in href -- links pointing to googleadservices.com, doubleclick.net, googleads.g.doubleclick.net, adsrvr.org, taboola.com, outbrain.com, revcontent.com, media.net, amazon-adsystem.com are ALWAYS ads
     Indicator 2: Iframe wrapper -- download buttons inside iframe elements are almost always ad-served; real download buttons are in the page's own DOM, not inside iframes
     Indicator 3: Ad-related CSS classes -- class names containing: ad, ads, advert, advertisement, sponsored, promoted, banner, promo, dfp, gpt-ad, taboola, outbrain
     Indicator 4: data-ad-* attributes -- elements with data-ad-slot, data-ad-client, data-ad-format, data-google-query-id, data-ad-unit are ad containers
     Indicator 5: Tracking redirect URLs -- hrefs containing /aclk?, /pagead/, /redirect?, utm_source=ad, click.php, /track/ are ad redirect chains, not direct downloads
     Indicator 6: External domain mismatch -- if the page is sourceforge.net but the download button href points to a completely different domain (not downloads.sourceforge.net or master.dl.sourceforge.net), it is likely an ad
     Indicator 7: aria-label or title containing "advertisement", "sponsored", "ad" -- accessibility labels sometimes expose ad nature
     Indicator 8: Parent container markers -- the download link's parent div/section having id or class containing "ad", "sponsor", "promo", "leaderboard", "skyscraper", "mpu" (medium placement unit)
   - REAL DOWNLOAD LINK IDENTIFICATION section: Strategy is ELIMINATION -- first identify and exclude all fake ad buttons using the 8 indicators above, then among remaining download-like links, find the one whose href points to the same domain or a known CDN/mirror of the site. For SourceForge: href matches sourceforge.net/projects/*/files/*/download or downloads.sourceforge.net/*. For FileHippo: href matches filehippo.com/download_*.
   - VERIFICATION BEFORE CLICK section: Before clicking any download link, use get_attribute to extract the href and verify it is NOT an ad domain. Use get_dom_snapshot to check if the element is inside an iframe. Read the parent element's class/id for ad markers. Only click after positive verification.
5. `selectors`: Object with selectors for each target site:
   - sourceforge: { realDownloadButton: 'a.button.green[href*="/download"], a[href*="downloads.sourceforge.net"], a[href*="/files/latest/download"]', projectTitle: '.project-info h1, #project-title', fileList: '#files_list a, .files a[href*="/download"]', downloadCount: '.download-count, .stats .count', adIframes: 'iframe[src*="googleads"], iframe[src*="doubleclick"], iframe[src*="adsrvr"], iframe[id*="google_ads"]', adContainers: 'div[id*="ad"], div[class*="ad-"], div[class*="advert"], div[data-ad-slot], ins.adsbygoogle, div[id*="gpt-ad"]' }
   - filehippo: { realDownloadButton: 'a[href*="filehippo.com/download_"], a.download-link, a.program-header-download-link', programTitle: 'h1.program-header-title, h1.program-title', adContainers: 'div[class*="ad"], div[id*="ad-"], div[class*="banner"], iframe[src*="doubleclick"]' }
   - fosshub: { realDownloadButton: 'a[href*="fosshub.com"][href*="download"], a.download-link', programTitle: 'h1, .program-name', adContainers: 'div[class*="ad"], iframe[src*="google"]' }
   - adNetworkDomains: { patterns: 'googleadservices.com, doubleclick.net, googleads.g.doubleclick.net, adsrvr.org, taboola.com, outbrain.com, revcontent.com, media.net, amazon-adsystem.com, pagead2.googlesyndication.com' }
   - fakeButtonIndicators: { iframeAds: 'iframe[src*="ad"], iframe[src*="doubleclick"], iframe[src*="google_ads"]', dataAdAttributes: '[data-ad-slot], [data-ad-client], [data-ad-format], [data-google-query-id]', adClasses: '[class*="advert"], [class*="sponsored"], [class*="promoted"], [class*="banner-ad"], [id*="gpt-ad"], ins.adsbygoogle', trackingRedirects: 'a[href*="/aclk?"], a[href*="/pagead/"], a[href*="click.php"], a[href*="/redirect?"]' }
6. `workflows`: Object with downloadRealFile array of 12 steps:
   Step 1: NAVIGATE TO DOWNLOAD PAGE -- Navigate to the freeware download site page (e.g., sourceforge.net/projects/vlc/files/latest/download). Use read_page to capture the initial page content and verify the page loaded correctly with a project/program title visible.
   Step 2: SNAPSHOT ALL DOWNLOAD-LIKE ELEMENTS -- Use get_dom_snapshot to capture all elements that look like download buttons: links containing text "download", "get", "install", or similar. Also capture all iframe elements on the page. Record the total count of download-like elements found.
   Step 3: IDENTIFY AD IFRAMES -- From the snapshot, list all iframe elements. Check each iframe src attribute for ad network domains (googleadservices, doubleclick, adsrvr, taboola, outbrain). Mark all elements INSIDE iframes as fake ads. Elements in the main document DOM are candidates for the real button.
   Step 4: CHECK HREF DOMAINS -- For each download-like link in the main DOM (not in iframes), use get_attribute to extract the href. Classify each href: (a) same-site domain (sourceforge.net, downloads.sourceforge.net) = CANDIDATE for real, (b) known ad domain from adNetworkDomains list = FAKE AD, (c) tracking redirect URL pattern = FAKE AD, (d) unknown external domain = SUSPICIOUS (investigate further).
   Step 5: CHECK PARENT CONTAINERS -- For each remaining candidate link, examine the parent elements (div, section) for ad-related class names or IDs (ad, advert, sponsored, promoted, banner, gpt-ad, dfp). If the parent has ad markers, classify the child link as FAKE AD even if its href looks legitimate.
   Step 6: CHECK DATA ATTRIBUTES -- For each remaining candidate, check for data-ad-slot, data-ad-client, data-ad-format, data-google-query-id attributes on the element or its ancestors. Presence of these = FAKE AD.
   Step 7: CHECK ARIA/TITLE LABELS -- For each remaining candidate, check aria-label and title attributes for "advertisement", "sponsored", "ad" keywords. Presence = FAKE AD.
   Step 8: IDENTIFY THE REAL DOWNLOAD LINK -- After eliminating all fake ad buttons (steps 3-7), the remaining download-like link(s) should be the real download. For SourceForge: the real button typically has class "button green" or href containing "/files/latest/download" or "downloads.sourceforge.net". If multiple candidates remain, prefer the one with (a) same-site href, (b) most specific download path, (c) largest visual size (likely the main call-to-action).
   Step 9: VERIFY BEFORE CLICK -- Before clicking the identified real download link, perform final verification: (a) get_attribute to confirm href points to expected download domain, (b) confirm the element is NOT inside an iframe, (c) confirm no ad-related parent containers, (d) confirm no tracking redirect patterns in href. Log the full href for the diagnostic report.
   Step 10: CLICK REAL DOWNLOAD LINK -- Click the verified real download link. Note: the browser may navigate to an intermediate download page (e.g., SourceForge shows a "your download will begin shortly" page before starting the actual file download). This is normal behavior.
   Step 11: VERIFY DOWNLOAD INITIATED -- After clicking, check the page state: (a) SourceForge shows a countdown page with "Your download will begin shortly" text, (b) the URL may change to a /download path, (c) the browser may show a download prompt (not detectable via DOM, but the page transition confirms the click target was correct). Use read_page to confirm the post-click page is a download confirmation, not an ad landing page.
   Step 12: REPORT -- State final outcome: number of fake ad buttons identified and skipped (with reasons), the real download link that was clicked (full href), whether the download confirmation page appeared (success) or an ad page appeared (failure -- wrong button clicked).
7. `warnings`: Array of 5 strings:
   - 'DARK-01: NEVER click a download button without first verifying its href via get_attribute. Fake ad buttons are intentionally designed to look identical to real download buttons.'
   - 'Ad detection priority: check for iframes FIRST (fastest elimination), then href domain, then parent container classes, then data-ad-* attributes, then aria-label/title.'
   - 'SourceForge real download buttons typically have class "button green" or href containing "downloads.sourceforge.net". Any button inside an iframe is ALWAYS an ad on SourceForge.'
   - 'Some freeware sites have MULTIPLE pages before the actual download starts (e.g., SourceForge: project page -> files page -> download countdown page). Each page may have fresh ad buttons -- re-run ad detection on every page transition.'
   - 'If ALL download-like links are eliminated as ads, the page may be entirely ad-driven with no real download. Navigate directly to the known download URL pattern (e.g., sourceforge.net/projects/PROJECT/files/latest/download) to bypass the ad-heavy page.'
8. `toolPreferences`: Array including navigate, click, read_page, get_dom_snapshot, get_attribute

Then update background.js: Add `importScripts('site-guides/utilities/freeware-download.js');` after the `importScripts('site-guides/utilities/session-expiry.js');` line (line ~186) in the Utilities section, making it the last Utilities import before the agent modules section.
  </action>
  <verify>
    <automated>grep -q "downloadRealFile" site-guides/utilities/freeware-download.js && grep -q "DARK-01" site-guides/utilities/freeware-download.js && grep -q "registerSiteGuide" site-guides/utilities/freeware-download.js && grep -q "sourceforge" site-guides/utilities/freeware-download.js && grep -q "doubleclick\|googleads\|googleadservices" site-guides/utilities/freeware-download.js && grep -q "iframe" site-guides/utilities/freeware-download.js && grep -q "ad.network\|adNetworkDomains\|ad network" site-guides/utilities/freeware-download.js && grep -q "get_attribute" site-guides/utilities/freeware-download.js && grep -q "fakeButtonIndicators\|fake.*button\|Fake.*Button\|FAKE AD" site-guides/utilities/freeware-download.js && grep -q "freeware-download" background.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "downloadRealFile" freeware-download.js returns at least 1 match (workflow name)
    - grep "DARK-01" freeware-download.js returns at least 2 matches (guidance section and warning)
    - grep "registerSiteGuide" freeware-download.js returns exactly 1 match (registration call)
    - grep "sourceforge" freeware-download.js returns at least 3 matches (primary target URL, selectors, and workflow reference)
    - grep "doubleclick\|googleads\|googleadservices" freeware-download.js returns at least 3 matches (ad network domain indicators)
    - grep "iframe" freeware-download.js returns at least 3 matches (iframe ad detection documented)
    - grep "ad.network\|adNetworkDomains\|ad network" freeware-download.js returns at least 2 matches (ad network identification)
    - grep "get_attribute" freeware-download.js returns at least 2 matches (href verification tool usage)
    - grep "filehippo\|fosshub\|majorgeeks" freeware-download.js returns at least 2 matches (fallback targets)
    - grep "data-ad-slot\|data-ad-client\|data-ad-format" freeware-download.js returns at least 1 match (data attribute detection)
    - grep "sponsored\|promoted\|advertisement" freeware-download.js returns at least 2 matches (CSS class detection)
    - grep "FAKE AD\|fake ad\|Fake Ad" freeware-download.js returns at least 3 matches (classification labels in workflow)
    - grep "freeware-download" background.js returns at least 1 match (importScripts entry)
    - grep "Utilities" freeware-download.js returns at least 1 match (category)
    - The background.js importScripts entry for freeware-download.js appears after session-expiry.js in the Utilities section
  </acceptance_criteria>
  <done>freeware-download.js created with: registerSiteGuide call (site, category, patterns for freeware download and 5 target sites), DARK-01 guidance section (dark pattern context explaining fake download buttons, 8 ad detection heuristics: ad network domains, iframe wrappers, ad-related CSS classes, data-ad-* attributes, tracking redirect URLs, external domain mismatch, aria/title labels, parent container markers), real download link identification strategy (elimination-based), verification before click section, selectors for sourceforge (real download button, ad iframes, ad containers) and filehippo and fosshub and ad network domain patterns and fake button indicators, downloadRealFile workflow (12 steps: navigate, snapshot download elements, identify ad iframes, check href domains, check parent containers, check data attributes, check aria/title, identify real link, verify before click, click real link, verify download initiated, report), 5 warnings (never click without href verification, detection priority, SourceForge patterns, multi-page ad detection, direct URL fallback), toolPreferences (5 MCP tools). background.js updated with importScripts entry in Utilities section.</done>
</task>

</tasks>

<verification>
1. site-guides/utilities/freeware-download.js exists with registerSiteGuide call
2. Site guide contains downloadRealFile workflow with 12 steps
3. Site guide contains DARK-01 guidance section with dark pattern context
4. Site guide documents at least 2 freeware download site targets (SourceForge primary, FileHippo/FossHub fallbacks)
5. Site guide documents 8 ad detection heuristics (ad domains, iframes, CSS classes, data attributes, tracking URLs, domain mismatch, aria/title, parent containers)
6. Site guide documents elimination-based real download link identification strategy
7. Site guide documents verification-before-click workflow using get_attribute
8. Site guide contains selectors for real download buttons and fake ad indicators
9. Site guide contains 5 warnings about ad detection priority and verification
10. background.js contains importScripts entry for freeware-download.js in Utilities section
</verification>

<success_criteria>
- freeware-download.js created with DARK-01 freeware download ad avoidance workflow and guidance
- downloadRealFile workflow documents full detection-elimination-verification-click cycle
- Freeware site targets documented with selectors for real and fake download buttons
- 8 ad detection heuristics documented for DOM-based fake button identification
- background.js updated with importScripts registration
- Ready for Plan 02 live MCP test
</success_criteria>

<output>
After completion, create `.planning/phases/87-freeware-download-ad-avoidance/87-01-SUMMARY.md`
</output>
