---
phase: 141-sidepanel-popup-retouch
plan: 01
type: execute
wave: 1
depends_on: [140-02]
files_modified:
  - ui/sidepanel.html
  - ui/sidepanel.css
  - ui/sidepanel.js
autonomous: true
requirements: [SID-01, SID-02]

must_haves:
  truths:
    - "The sidepanel presents a clearer persistent-workspace hierarchy in the header, history view, composer, and footer"
    - "Running, error, and history-active states are visually explicit rather than relying only on incidental class flips"
    - "Sidepanel history items and action affordances feel intentional and polished without changing behavior"
  artifacts:
    - path: "ui/sidepanel.html"
      provides: "Sidepanel markup hooks for richer hierarchy and footer metadata"
      contains: "surface-label"
    - path: "ui/sidepanel.css"
      provides: "Retouched sidepanel header, history, composer, footer, and state styling"
      contains: "data-ui-state"
    - path: "ui/sidepanel.js"
      provides: "State/view attributes that drive polished sidepanel visual states"
      contains: "dataset.uiState"
  key_links:
    - from: "ui/sidepanel.js"
      to: "ui/sidepanel.css"
      via: "Body data attributes for running/error/history-active styling"
      pattern: "data-ui-state"
    - from: "ui/sidepanel.html"
      to: "ui/sidepanel.css"
      via: "Surface label and footer metadata hooks"
      pattern: "surface-label"
---

<objective>
Retouch the sidepanel into a cleaner persistent workspace surface. Improve the header hierarchy, history subview, composer chrome, footer metadata, and high-frequency state feedback while preserving existing sidepanel behavior.

Output: updated sidepanel markup, CSS, and lightweight state/view hooks.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/141-sidepanel-popup-retouch/141-CONTEXT.md
@.planning/phases/141-sidepanel-popup-retouch/141-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sidepanel hierarchy and state hooks</name>
  <files>ui/sidepanel.html, ui/sidepanel.js</files>
  <read_first>ui/sidepanel.html, ui/sidepanel.js</read_first>
  <action>
Add minimal markup hooks for clearer surface hierarchy and footer metadata, then expose body-level state/view data attributes from the existing sidepanel JS so CSS can style running, error, and history-active states without changing behavior.
  </action>
  <acceptance_criteria>
- Sidepanel markup contains surface-label and footer metadata hooks
- Sidepanel JS sets body data attributes for UI state and active view
- No existing history, send, stop, replay, or navigation behavior changes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Retouch sidepanel header, history, composer, and footer</name>
  <files>ui/sidepanel.css</files>
  <read_first>ui/sidepanel.css, ui/sidepanel.html, ui/sidepanel.js, shared/fsb-ui-core.css</read_first>
  <action>
Use the new hierarchy and state hooks to retouch the sidepanel header, status pill, history subview, message rail, composer shell, action summary, and footer. Preserve the current aesthetic and functionality while making the persistent workspace feel cleaner and more deliberate.
  </action>
  <acceptance_criteria>
- Header and footer read as a coherent persistent-workspace shell
- History view feels like a first-class subview with clearer chrome and item affordances
- Running/error/history-active states are visually distinct and polished
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Read updated sidepanel HTML/JS and confirm state/view hooks exist
- Read updated sidepanel CSS and confirm header, history, composer, and state styling consume those hooks
</verification>

<success_criteria>
- The sidepanel feels like a polished persistent workspace variant of FSB
- Sidepanel controls stay behaviorally identical while reading as more intentional
- History and busy states no longer feel like incidental styling leftovers
</success_criteria>
