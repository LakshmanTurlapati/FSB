---
phase: 105-package-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp-server/package.json
  - mcp-server/.npmignore
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
  - PKG-04
  - DIST-02

must_haves:
  truths:
    - "package.json version is 0.1.0, not 1.0.0"
    - "npm pack --dry-run lists only build/ files and README.md"
    - "npm run prepublishOnly compiles TypeScript without errors"
    - ".npmignore excludes src/, tsconfig.json, and dev artifacts"
  artifacts:
    - path: "mcp-server/package.json"
      provides: "npm-publishable package metadata"
      contains: "\"version\": \"0.1.0\""
    - path: "mcp-server/.npmignore"
      provides: "npm publish exclusion rules"
      min_lines: 5
  key_links:
    - from: "mcp-server/package.json"
      to: "mcp-server/build/index.js"
      via: "main and bin fields"
      pattern: "\"main\": \"build/index.js\""
    - from: "mcp-server/package.json"
      to: "mcp-server/build/"
      via: "files field restricting published content"
      pattern: "\"files\":"
---

<objective>
Prepare the FSB MCP server package.json and .npmignore for npm publication.

Purpose: The existing package.json lacks npm registry metadata (version is 1.0.0 instead of 0.1.0, no license/keywords/repository/engines/prepublishOnly). The .npmignore does not exist, so npm would publish source files and dev artifacts. This plan adds all required metadata and exclusions so `npm pack` produces a clean, minimal tarball.

Output: Updated mcp-server/package.json with full npm metadata, new mcp-server/.npmignore
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-package-distribution/105-CONTEXT.md

@mcp-server/package.json
@mcp-server/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add npm registry metadata to package.json</name>
  <files>mcp-server/package.json</files>
  <read_first>
    - mcp-server/package.json (current state -- preserve existing name, bin, main, type, scripts, dependencies, devDependencies)
    - mcp-server/tsconfig.json (confirm declaration and sourceMap are already true)
  </read_first>
  <action>
Update mcp-server/package.json with the following changes. Preserve ALL existing fields (name, bin, main, type, scripts, dependencies, devDependencies) exactly as they are.

**Changes to make:**

1. Change `"version"` from `"1.0.0"` to `"0.1.0"` (per D-05, D-06: start at 0.1.0 to signal early/beta status)

2. Add these NEW fields after `"description"`:
   - `"license": "MIT"` (per D-11)
   - `"author": "Lakshman Turlapati"`
   - `"repository": { "type": "git", "url": "https://github.com/lakshmanturlapati/FSB.git", "directory": "mcp-server" }`
   - `"keywords": ["mcp", "browser-automation", "chrome", "fsb", "model-context-protocol", "ai-agent"]`
   - `"engines": { "node": ">=18.0.0" }`

3. Add `"files"` field: `["build/", "README.md"]` (per D-07, D-08, D-09: include compiled JS, .d.ts, .js.map, and README)

4. Add `"prepublishOnly"` script: `"prepublishOnly": "tsc"` (ensures fresh build before every publish)

**Do NOT change:**
- `"name": "fsb-mcp-server"` (per D-10, already correct)
- `"bin": { "fsb-mcp": "build/index.js" }` (per D-12, already correct)
- `"main": "build/index.js"` (already correct)
- `"type": "module"` (already correct)
- `"build": "tsc"` script (already correct)
- `"dev": "tsx src/index.ts"` script (already correct)
- Any dependency or devDependency entries
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Extensions/FSB/mcp-server && node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); const checks = [p.version==='0.1.0', p.license==='MIT', p.files?.includes('build/'), p.files?.includes('README.md'), p.scripts?.prepublishOnly==='tsc', p.engines?.node==='>=18.0.0', p.repository?.directory==='mcp-server', p.bin?.['fsb-mcp']==='build/index.js', p.name==='fsb-mcp-server']; console.log(checks.every(Boolean) ? 'PASS' : 'FAIL: ' + JSON.stringify(checks))"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"version": "0.1.0"`
    - package.json contains `"license": "MIT"`
    - package.json contains `"files": ["build/", "README.md"]`
    - package.json contains `"prepublishOnly": "tsc"`
    - package.json contains `"engines": { "node": ">=18.0.0" }`
    - package.json contains `"repository"` with `"directory": "mcp-server"`
    - package.json contains `"keywords"` array with at least "mcp" and "browser-automation"
    - package.json still contains `"name": "fsb-mcp-server"` (unchanged)
    - package.json still contains `"bin": { "fsb-mcp": "build/index.js" }` (unchanged)
  </acceptance_criteria>
  <done>package.json has all npm registry metadata: version 0.1.0, MIT license, repository with monorepo directory, keywords, node engine constraint, files whitelist, and prepublishOnly build script</done>
</task>

<task type="auto">
  <name>Task 2: Create .npmignore and verify pack output</name>
  <files>mcp-server/.npmignore</files>
  <read_first>
    - mcp-server/package.json (confirm files field is set -- this is the primary filter, .npmignore is the safety net)
    - /Users/lakshmanturlapati/Documents/Codes/Extensions/FSB/.gitignore (see existing patterns for reference)
  </read_first>
  <action>
Create mcp-server/.npmignore with the following exact contents. This file acts as a safety net alongside the `files` field in package.json. Even though `files` whitelists only `build/` and `README.md`, the .npmignore provides defense-in-depth against accidental inclusion.

```
# Source files (compiled output is in build/)
src/

# TypeScript config
tsconfig.json

# Development tools
.eslintrc*
.prettierrc*
.editorconfig

# Test files
__tests__/
*.test.ts
*.spec.ts
coverage/

# IDE and OS
.vscode/
.idea/
.DS_Store

# Dev artifacts
*.tsbuildinfo
.env
.env.*
```

After creating the file, run `npm pack --dry-run` from the mcp-server directory and verify the output:
- MUST list files under `build/` (index.js, server.js, bridge.js, etc. plus .d.ts and .js.map variants)
- MUST list README.md (even if it doesn't exist yet -- npm includes it by default when files field lists it)
- MUST NOT list any `.ts` source files from `src/`
- MUST NOT list `tsconfig.json`
- MUST NOT list `node_modules/`
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Extensions/FSB/mcp-server && npm pack --dry-run 2>&1 | grep -c "src/" | xargs -I{} test {} -eq 0 && echo "PASS: no src/ files in pack" || echo "FAIL: src/ files found in pack"</automated>
  </verify>
  <acceptance_criteria>
    - mcp-server/.npmignore file exists with at least 10 lines
    - .npmignore contains the line `src/`
    - .npmignore contains the line `tsconfig.json`
    - `npm pack --dry-run` output contains `build/index.js`
    - `npm pack --dry-run` output does NOT contain any file matching `src/*.ts`
    - `npm pack --dry-run` output does NOT contain `tsconfig.json`
  </acceptance_criteria>
  <done>.npmignore exists in mcp-server/ excluding source and dev artifacts. npm pack --dry-run shows only build/ output files and package.json (no source, no tsconfig, no dev files)</done>
</task>

</tasks>

<verification>
1. `cd mcp-server && node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('version:', p.version, 'license:', p.license, 'files:', p.files, 'prepublishOnly:', p.scripts?.prepublishOnly)"` -- shows version 0.1.0, MIT, files array, tsc
2. `cd mcp-server && npm pack --dry-run 2>&1 | tail -20` -- shows only build/ files and metadata
3. `cd mcp-server && npm run prepublishOnly` -- compiles TypeScript without errors
4. `head -1 mcp-server/build/index.js` -- shows `#!/usr/bin/env node` (shebang survives compilation)
</verification>

<success_criteria>
- package.json version is 0.1.0 with MIT license, repository, keywords, engines, files, prepublishOnly
- .npmignore excludes src/, tsconfig.json, and dev artifacts
- npm pack --dry-run produces a tarball containing only build/ files and package.json
- npm run prepublishOnly (tsc) completes without errors
- build/index.js line 1 is #!/usr/bin/env node
</success_criteria>

<output>
After completion, create `.planning/phases/105-package-distribution/105-01-SUMMARY.md`
</output>
