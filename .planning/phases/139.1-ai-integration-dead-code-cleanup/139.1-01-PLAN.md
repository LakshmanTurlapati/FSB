---
phase: 139.1-ai-integration-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai/ai-integration.js
  - ui/options.js
autonomous: true
requirements: [CLN-01, CLN-02]
gap_closure: true

must_haves:
  truths:
    - "grep for CLI_COMMAND_TABLE in ai/ai-integration.js returns zero matches"
    - "grep for TASK_PROMPTS in ai/ai-integration.js returns zero matches"
    - "grep for buildPrompt in ai/ai-integration.js returns zero matches"
    - "grep for buildMinimalUpdate in ai/ai-integration.js returns zero matches"
    - "grep for parseCliResponse in ai/ai-integration.js returns zero matches"
    - "grep for getAutomationActions in ai/ai-integration.js returns zero matches"
    - "grep for CLIValidator in ui/options.js returns zero matches"
    - "grep for parseCliResponse in ui/options.js returns zero matches"
    - "callAPI method still present and functional in ai/ai-integration.js"
    - "getGuideForTask and loadSiteGuide still present in ai/ai-integration.js"
    - "AIIntegration constructor still present in ai/ai-integration.js"
  artifacts:
    - path: "ai/ai-integration.js"
      provides: "AIIntegration class with dead code removed (~760 lines deleted)"
      contains: "callAPI"
    - path: "ui/options.js"
      provides: "Options dashboard with CLI validation section removed (~430 lines deleted)"
  key_links:
    - from: "ai/agent-loop.js"
      to: "ai/ai-integration.js"
      via: "callAPI for memory extraction"
      pattern: "callAPI"
    - from: "ai/agent-loop.js"
      to: "ai/ai-integration.js"
      via: "getGuideForTask for site guides"
      pattern: "getGuideForTask"
---

<objective>
Remove all dead code from ai-integration.js and options.js identified by the v0.9.20 milestone audit.

Purpose: Close CLN-01 (remove cli-parser references) and CLN-02 (remove TASK_PROMPTS and buildPrompt templates) gaps that remain after Phase 139 deleted the callers but left the callees.

Output: Two cleaner source files with ~1190 fewer lines of unreachable code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139.1-ai-integration-dead-code-cleanup/139.1-CONTEXT.md
@.planning/v0.9.20-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead code from ai/ai-integration.js</name>
  <files>ai/ai-integration.js</files>
  <action>
    Read ai/ai-integration.js fully before making changes. Delete the following dead code blocks IN ORDER from bottom-to-top to preserve line numbers during deletion:

    CRITICAL SAFETY RULE: Do NOT delete callAPI(), extractMemory(), enrichMemory(), getGuideForTask(), loadSiteGuide(), the AIIntegration constructor, or migrateSettings(). These are ALIVE. Only delete what is listed below.

    Dead code to delete (per CLN-01, CLN-02):

    1. **getToolsDocumentation method** (~lines 4898-4936) -- returns CLI_COMMAND_TABLE, only called by buildPrompt which is dead. Delete entire method.

    2. **_buildTaskGuidance method** (~lines 4490-4555) -- only called by buildPrompt (line 2733). Delete entire method. Note: the TASK_PROMPTS reference at line 4517 is inside this method.

    3. **getToolHints method** (~lines 4843-4896) -- only called by buildPrompt via getToolsDocumentation. Delete entire method.

    4. **createFallbackResponse method** (~lines 5010-5033) -- output shape matches parseCliResponse. Only called within processQueue recovery path (dead). Delete entire method.

    5. **enhancePromptForRetry method** (~lines 4992-5008) -- CLI format reinforcement, only called within getAutomationActions retry path (dead). Delete entire method.

    6. **buildPrompt method** (~lines 2496-3558) -- ~1060 lines. Only called by getAutomationActions (line 2075). Delete entire method, including the progressive trimming logic that references PROMPT_CHAR_LIMIT.

    7. **processQueue method** (~lines 2245-2445) -- ~200 lines. Contains the 3 parseCliResponse calls (lines 2275, 2310, 2356). Only called by getAutomationActions (line 2127). Delete entire method.

    8. **getAutomationActions method** (~lines 1943-2242) -- ~300 lines. Has NO callers in the active codebase (was called by startAutomationLoop which was deleted in Phase 139). Delete entire method.

    9. **decomposeTask method** (~lines 2454-2494) -- only called by buildPrompt (line 2508). Delete entire method.

    10. **buildMinimalUpdate method** (~lines 822-~line before decomposeTask) -- only called by getAutomationActions (line 2060). Find exact end boundary and delete. Also delete formatChangeInfo if it is only used by buildMinimalUpdate.

    11. **buildSheetsFormattingDirective function** (~lines 522-~line before class) -- standalone function only called by buildPrompt (line 2803). Delete entire function.

    12. **Top-level constants** (delete these last):
        - PROMPT_CHAR_LIMIT (line 13) -- only used by buildPrompt progressive trimming
        - CLI_COMMAND_TABLE (lines 18-255) -- ~238 lines, only used by getToolsDocumentation
        - TASK_PROMPTS (lines 257-471) -- ~215 lines, only used by _buildTaskGuidance
        - HYBRID_CONTINUATION_PROMPT (lines 476-503) -- only used by buildPrompt
        - BATCH_ACTION_INSTRUCTIONS (lines 507-512) -- only used by buildPrompt

    13. **Dead comments** -- remove the comment at line 4428 ("JSON parsing pipeline deleted -- parseCliResponse is the sole parser") and the comment at line 4481 ("Uses site guide when available, falls back to TASK_PROMPTS") since the referenced code no longer exists.

    14. **Dead comment at line 692** -- "SM-22: Site map knowledge cache for synchronous injection in buildPrompt" -- update to remove the "in buildPrompt" reference since buildPrompt is being deleted. The cache fields themselves (_cachedSiteMap etc) may still be used by site guide loading -- keep the fields, just fix the comment.

    15. **Dead comment at line 4890** -- "Return all common tools (CLI_COMMAND_TABLE covers all verbs)" -- this is inside getToolHints which is being deleted entirely.

    After deletion, verify the file still has valid JavaScript syntax by checking bracket/brace balance. The sanitizePageContent method (line 3560 area) should become the next method after whatever precedes the deleted buildMinimalUpdate block.

    IMPORTANT: The sanitizeActions() standalone function (~lines 215-254) is NOT dead -- it is called by processQueue BUT also potentially by other code. Check before deleting. If only called within processQueue, delete it too.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Extensions/FSB && grep -c "CLI_COMMAND_TABLE\|TASK_PROMPTS\|buildPrompt\|buildMinimalUpdate\|parseCliResponse\|getAutomationActions\|HYBRID_CONTINUATION_PROMPT\|BATCH_ACTION_INSTRUCTIONS\|PROMPT_CHAR_LIMIT\|processQueue\|getToolsDocumentation\|buildSheetsFormattingDirective\|getToolHints\|enhancePromptForRetry\|createFallbackResponse\|decomposeTask" ai/ai-integration.js</automated>
  </verify>
  <done>
    All dead code deleted from ai-integration.js:
    - CLI_COMMAND_TABLE, TASK_PROMPTS, HYBRID_CONTINUATION_PROMPT, BATCH_ACTION_INSTRUCTIONS, PROMPT_CHAR_LIMIT constants removed
    - buildPrompt, buildMinimalUpdate, getAutomationActions, processQueue, decomposeTask, getToolsDocumentation, _buildTaskGuidance, getToolHints, buildSheetsFormattingDirective, enhancePromptForRetry, createFallbackResponse methods/functions removed
    - All 3 parseCliResponse call sites removed (they were inside processQueue)
    - grep returns 0 matches for all deleted symbols
    - Living code (callAPI, memory extraction, site guides, constructor) untouched
    - File has valid JS syntax
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove CLI validation dead code from ui/options.js</name>
  <files>ui/options.js</files>
  <action>
    Read ui/options.js fully before making changes. Delete the entire CLI Validation section and its references:

    CRITICAL SAFETY RULE: Do NOT delete anything outside the CLI Validation section. The rest of options.js (settings, analytics, session management, agent section, memory section) is all ALIVE.

    Dead code to delete (per CLN-01):

    1. **Entire CLI Validation section** (lines 5371-5802) -- ~430 lines. This includes:
       - updateCLIValidationVisibility function (lines 5379-5384)
       - initCLIValidation function (lines 5389-5433)
       - runCLIValidationTests function (lines 5439-5519)
       - renderProviderResults function (lines 5526-5565)
       - renderDetailedResults function (lines 5572-5608)
       - escapeHtml function (lines 5615-5619) -- check if used elsewhere first; if ONLY used by CLI validation functions, delete it
       - runTokenComparison function (lines 5624-5694)
       - runEdgeCaseTests function (lines 5699-5802)
       All of these reference CLIValidator, parseCliResponse, or cli-validator.js which no longer exist.

    2. **initCLIValidation call** (line 5367) -- remove `setTimeout(initCLIValidation, 500);` from the DOMContentLoaded handler.

    3. **updateCLIValidationVisibility calls**:
       - Line 352: `updateCLIValidationVisibility(e.target.checked);` -- remove this line from the debug mode toggle handler
       - Line 659: `updateCLIValidationVisibility(settings.debugMode);` -- remove this line from settings loading

    4. **Comment cleanup** -- remove the "CLI Validation Section" banner comment at lines 5371-5373.

    After deletion, verify the DOMContentLoaded handler still has its other setTimeout calls (initializeAgentSection, initializeMemorySection) intact. The module.exports block at the end should remain.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Extensions/FSB && grep -c "CLIValidator\|parseCliResponse\|runEdgeCaseTests\|cli-validator\|initCLIValidation\|updateCLIValidationVisibility\|runCLIValidationTests\|runTokenComparison\|renderProviderResults\|renderDetailedResults" ui/options.js</automated>
  </verify>
  <done>
    All CLI validation dead code removed from options.js:
    - CLIValidator references removed (was from deleted cli-validator.js)
    - parseCliResponse references removed (was from deleted cli-parser.js)
    - Entire CLI Validation section (~430 lines) deleted
    - initCLIValidation and updateCLIValidationVisibility call sites removed
    - grep returns 0 for all deleted symbols
    - Rest of options.js (settings, analytics, agent, memory) untouched
    - module.exports block at end preserved
  </done>
</task>

</tasks>

<verification>
Final cross-file verification that all dead code targets are gone:

```bash
# Must all return 0
grep -rc "CLI_COMMAND_TABLE" ai/ai-integration.js ui/options.js
grep -rc "TASK_PROMPTS" ai/ai-integration.js ui/options.js
grep -rc "buildPrompt" ai/ai-integration.js
grep -rc "parseCliResponse" ai/ai-integration.js ui/options.js
grep -rc "CLIValidator" ui/options.js
grep -rc "getAutomationActions" ai/ai-integration.js

# Must still find these (living code):
grep -c "callAPI" ai/ai-integration.js    # should be > 0
grep -c "getGuideForTask" ai/ai-integration.js  # should be > 0
grep -c "extractMemory" ai/ai-integration.js  # should be > 0
grep -c "class AIIntegration" ai/ai-integration.js  # should be 1
```
</verification>

<success_criteria>
1. Zero grep matches for CLI_COMMAND_TABLE, TASK_PROMPTS, buildPrompt, parseCliResponse, getAutomationActions, CLIValidator across active codebase (ai/ai-integration.js, ui/options.js)
2. callAPI, getGuideForTask, extractMemory, loadSiteGuide, AIIntegration constructor all still present
3. ai-integration.js reduced by ~760 lines (from ~5253 to ~4493)
4. options.js reduced by ~430 lines
5. No JavaScript syntax errors in either file
6. CLN-01 and CLN-02 fully satisfied per REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/139.1-ai-integration-dead-code-cleanup/139.1-01-SUMMARY.md`
</output>
