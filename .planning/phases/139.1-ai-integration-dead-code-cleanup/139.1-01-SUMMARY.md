---
phase: "139.1"
plan: "01"
subsystem: ai-integration
tags: [dead-code-removal, refactoring, ai-integration, options-ui]
dependency_graph:
  requires: []
  provides: [clean-ai-integration]
  affects: [ai/ai-integration.js, ui/options.js]
tech_stack:
  added: []
  patterns: [dead-code-cleanup]
key_files:
  modified:
    - ai/ai-integration.js
    - ui/options.js
decisions:
  - "Preserved callAPI as orphan method (callable by background.js directly)"
  - "Replaced CLIValidator functions with no-op stub instead of removing call site to prevent ReferenceError"
  - "Left replacement comments at each deletion site for traceability"
metrics:
  duration: 9min
  completed: "2026-04-01T11:45:34Z"
---

# Phase 139.1 Plan 01: AI Integration Dead Code Cleanup Summary

Deleted 2836 lines of dead autopilot prompt-building pipeline from ai-integration.js and dead CLIValidator section from options.js, reducing ai-integration.js by 50.6% (5253 to 2595 lines).

## What Changed

### ai-integration.js (2658 lines removed)

**Top-level constants deleted:**
- `PROMPT_CHAR_LIMIT` (200K char threshold for progressive trimming)
- `CLI_COMMAND_TABLE` (~140-line CLI command reference table)
- `TASK_PROMPTS` (~215-line object with per-task-type prompt templates: search, email, form, extraction, navigation, multitab, gaming, shopping, career, general, canvas)
- `HYBRID_CONTINUATION_PROMPT` (~30-line minimal continuation prompt)
- `BATCH_ACTION_INSTRUCTIONS` (~5-line multi-command batching text)

**Top-level function deleted:**
- `buildSheetsFormattingDirective()` (~100-line function generating Google Sheets formatting instructions)

**Class methods deleted:**
- `buildMinimalUpdate()` (~260 lines) - built delta prompt for continuation iterations
- `getAutomationActions()` (~300 lines) - main entry point for autopilot AI calls with retry/cache/queue logic
- `processQueue()` (~200 lines) - queue processor that called callAPI and parseCliResponse
- `decomposeTask()` (~40 lines) - split multi-step tasks on sequential separators
- `buildPrompt()` (~1060 lines) - massive full prompt builder with system/user prompt construction
- `_buildTaskGuidance()` (~190 lines) - task-type-specific guidance text builder
- `getToolsDocumentation()` (~40 lines) - CLI command table injection with priority blocks
- `enhancePromptForRetry()` (~20 lines) - CLI format reinforcement for retry attempts
- `createFallbackResponse()` (~25 lines) - error fallback response generator

**Living code preserved:**
- `callAPI()` and `legacyCallAPI()` - raw API call methods
- `constructor`, `migrateSettings`, `createProvider` - initialization
- `clearConversationHistory`, `updateConversationHistory`, `trimConversationHistory`, `triggerCompaction` - conversation management
- `_fetchLongTermMemories`, `_fetchSiteMap`, `buildMemoryContext` - memory and site intelligence
- `detectTaskType`, `getRelevantTools`, `isInformationGatheringTask` - task analysis
- `formatElements`, `formatSemanticContext`, `formatPageStructureSummary` - DOM formatting
- `isValidResponse`, `isValidTool` - validation
- `testConnection`, `trackTokenUsage` - connection testing and analytics
- `sanitizePageContent`, `sanitizeActions` - security
- All cache methods (`generateCacheKey`, `getCachedResponse`, `setCachedResponse`)

### ui/options.js (178 lines removed)

- `runCLIValidationTests()` - replaced with no-op stub
- `renderProviderResults()` - deleted entirely
- `renderDetailedResults()` - deleted entirely
- Duplicate `escapeHtml()` at line 5615 - deleted (primary definition at line 2335 preserved)

## Verification Results

All deleted identifiers have 0 non-comment references:
- Constants: CLI_COMMAND_TABLE, TASK_PROMPTS, HYBRID_CONTINUATION_PROMPT, BATCH_ACTION_INSTRUCTIONS, PROMPT_CHAR_LIMIT, buildSheetsFormattingDirective
- Methods: buildMinimalUpdate, getAutomationActions, processQueue, decomposeTask, buildPrompt, _buildTaskGuidance, getToolsDocumentation, enhancePromptForRetry, createFallbackResponse
- CLIValidator section: renderProviderResults, renderDetailedResults removed; runCLIValidationTests stubbed
- Both files pass JavaScript syntax check (`node -c`)

## Commits

| Task | Description | Commit |
|------|-------------|--------|
| 1 | Delete dead autopilot prompt pipeline from ai-integration.js | 72bdc89 |
| 2 | Remove dead CLIValidator section from options.js | b9b5f64 |
| 3 | Update stale parseCliResponse comment | 21473da |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing] Added no-op stub for runCLIValidationTests**
- **Found during:** Task 2 (options.js cleanup)
- **Issue:** Removing `runCLIValidationTests` entirely would cause a `ReferenceError` at runtime because the button event listener at line 5408 calls it
- **Fix:** Replaced with a no-op stub that shows an info toast instead of deleting entirely
- **Files modified:** ui/options.js
- **Commit:** b9b5f64

**2. [Rule 1 - Bug] Cleaned up stale comments referencing deleted code**
- **Found during:** Task 3 (verification)
- **Issue:** Multiple comments in living code referenced now-deleted methods (buildPrompt, processQueue, _buildTaskGuidance, CLI_COMMAND_TABLE)
- **Fix:** Updated 6 stale comments to reference current architecture instead
- **Files modified:** ai/ai-integration.js
- **Commit:** 72bdc89, 21473da

## Known Stubs

None. All deletion was of dead code; no stubs were created that need wiring.

## Self-Check: PASSED
