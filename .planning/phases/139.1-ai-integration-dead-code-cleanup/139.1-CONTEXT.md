# Phase 139.1: ai-integration.js Dead Code Cleanup - Context

**Gathered:** 2026-04-01
**Status:** Ready for planning
**Mode:** Gap closure (from v0.9.20 milestone audit)

<domain>
## Phase Boundary

Remove all remaining dead code from ai-integration.js left after Phase 139. Also clean up orphaned CLIValidator references in options.js. Pure deletion -- no new functionality.

</domain>

<decisions>
## Implementation Decisions

### Claude's Discretion
All implementation choices are at Claude's discretion -- pure deletion phase. The audit identified exactly what to delete.

### Deletion targets (from MILESTONE-AUDIT.md)
- **ai/ai-integration.js**: Delete CLI_COMMAND_TABLE (~140 lines), TASK_PROMPTS (~220 lines), buildPrompt method, buildMinimalUpdate method, HYBRID_CONTINUATION_PROMPT, BATCH_ACTION_INSTRUCTIONS, PROMPT_CHAR_LIMIT, buildSheetsFormattingDirective, parseCliResponse references (3 calls), getAutomationActions method (the entire old autopilot AI call path)
- **ui/options.js**: Remove CLIValidator, parseCliResponse, runEdgeCaseTests references (from deleted cli-validator.js)
- **background.js**: Remove importScripts('utils/cli-validator.js') if present

### What to keep in ai-integration.js
- AIIntegration class constructor and settings management
- Memory extraction functions (extractMemory, enrichMemory)
- Site guide loading (getGuideForTask, loadSiteGuide) -- used by agent-loop.js get_site_guide tool
- callAPI method -- still used for memory extraction AI calls
- Any utility functions that memory or site guide code depends on

</decisions>

<canonical_refs>
## Canonical References

- `.planning/v0.9.20-MILESTONE-AUDIT.md` -- Gap details and tech debt inventory
- `ai/ai-integration.js` -- File to clean up (5253 lines currently)
- `ui/options.js` -- CLIValidator references to remove
- `ai/agent-loop.js` -- The replacement (must still work after cleanup)

</canonical_refs>

<code_context>
## Existing Code Insights

### What's dead (no callers after Phase 139)
- getAutomationActions() -- was called by startAutomationLoop (deleted)
- buildPrompt() -- called only by getAutomationActions
- buildMinimalUpdate() -- called only by getAutomationActions
- parseCliResponse -- called only by getAutomationActions (and function deleted with cli-parser.js)
- CLI_COMMAND_TABLE -- used only by buildPrompt
- TASK_PROMPTS -- used only by buildPrompt

### What's alive (still has callers)
- callAPI() -- called by memory extraction (extractMemory)
- getGuideForTask() -- called by agent-loop.js get_site_guide interception
- AIIntegration constructor -- instantiated in background.js for memory

</code_context>

<specifics>
## Specific Ideas

No specific requirements -- pure deletion per audit findings.

</specifics>

<deferred>
## Deferred Ideas

None.

</deferred>

---

*Phase: 139.1-ai-integration-dead-code-cleanup*
*Context gathered: 2026-04-01*
