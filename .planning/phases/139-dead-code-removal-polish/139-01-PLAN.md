---
phase: 139-dead-code-removal-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai/cli-parser.js (DELETE)
  - utils/cli-validator.js (DELETE)
  - ai/ai-integration.js
  - background.js
  - ui/options.js
autonomous: true
requirements: [CLN-01, CLN-02]

must_haves:
  truths:
    - "cli-parser.js file no longer exists on disk"
    - "cli-validator.js file no longer exists on disk"
    - "No code references parseCliResponse, tokenizeLine, mapCommand, or COMMAND_REGISTRY"
    - "No code references CLI_COMMAND_TABLE or TASK_PROMPTS"
    - "No code references buildPrompt or buildMinimalUpdate"
    - "No code references HYBRID_CONTINUATION_PROMPT or BATCH_ACTION_INSTRUCTIONS"
    - "AIIntegration class still instantiates and its callAPI/testConnection/getAutomationActions methods still work"
  artifacts:
    - path: "ai/ai-integration.js"
      provides: "AIIntegration class with legacy prompt infrastructure removed"
    - path: "background.js"
      provides: "No importScripts for cli-parser.js, no CLI_VALIDATION message handlers"
    - path: "ui/options.js"
      provides: "No CLI parser or validator references"
  key_links:
    - from: "background.js"
      to: "ai/ai-integration.js"
      via: "new AIIntegration(settings) for testConnection and callAPI"
      pattern: "new AIIntegration"
    - from: "ai/agent-loop.js"
      to: "ai/tool-definitions.js"
      via: "TOOL_REGISTRY for tool_use protocol (NOT ai-integration.js)"
      pattern: "_TOOL_REGISTRY"
---

<objective>
Delete cli-parser.js, cli-validator.js, CLI_COMMAND_TABLE, TASK_PROMPTS, buildPrompt, buildMinimalUpdate, and all prompt template constants from ai-integration.js. Remove all references to these deleted entities from background.js and ui/options.js.

Purpose: CLN-01 and CLN-02 -- remove the entire CLI text parsing layer and old prompt template system that are dead code after the Phase 135-138 tool_use agent loop replacement.
Output: ~2,400 fewer lines across deleted files and gutted constants/methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-dead-code-removal-polish/139-CONTEXT.md
@.planning/phases/137-agent-loop-core-safety-mechanisms/137-01-SUMMARY.md
@.planning/phases/138-context-management-on-demand-tools/138-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete cli-parser.js, cli-validator.js, and strip CLI_COMMAND_TABLE + TASK_PROMPTS + prompt templates from ai-integration.js</name>
  <files>ai/cli-parser.js (DELETE), utils/cli-validator.js (DELETE), ai/ai-integration.js</files>
  <action>
    read_first: ai/cli-parser.js, utils/cli-validator.js, ai/ai-integration.js

    1. DELETE ai/cli-parser.js entirely (953 lines). Use `rm ai/cli-parser.js`.

    2. DELETE utils/cli-validator.js entirely (1233 lines). Use `rm utils/cli-validator.js`.

    3. In ai/ai-integration.js, DELETE these top-level constants and functions (they exist BEFORE the AIIntegration class definition at line 631):
       - `PROMPT_CHAR_LIMIT` constant (line 13) -- only used by buildPrompt
       - `CLI_COMMAND_TABLE` template literal (lines 18-156) -- entire multi-line string
       - `TASK_PROMPTS` object (lines 257-475) -- all task type prompt templates
       - `HYBRID_CONTINUATION_PROMPT` template literal (lines 476-506)
       - `BATCH_ACTION_INSTRUCTIONS` template literal (lines 507-521)
       - `buildSheetsFormattingDirective` function (lines 522-624)
       Keep: `formatSiteKnowledge` (line 157-227) -- still used by getAutomationActions prompt building
       Keep: `sanitizeActions` (line 228-256) -- still used by getAutomationActions response processing

    4. In AIIntegration class (inside ai-integration.js), DELETE these methods:
       - `buildPrompt` method (starts around line 2496) -- the old system prompt builder
       - `buildMinimalUpdate` method (starts around line 822) -- the old continuation prompt builder
       - `getToolDocumentation` method (look for function that returns CLI_COMMAND_TABLE) -- the old tool docs method
       - `getGuidance` method if it references TASK_PROMPTS
       Keep: constructor, migrateSettings, createProvider, callAPI, getAutomationActions, testConnection, clearConversationHistory, conversationHistory management, sessionMemory, compactedSummary, extractMemory, trackUsage, storePrompt, and ALL provider/API methods

    5. In AIIntegration class, clean up constructor:
       - Remove `this._lastSiteKnowledgeDomain`, `this._cachedSiteMap`, `this._cachedSiteMapDomain`, `this._cachedSiteMapSource` if ONLY used by buildPrompt (check first).
       - Remove `this._stashedTask` if ONLY used by buildMinimalUpdate.

    6. In AIIntegration's `getAutomationActions` method, find and remove the code path that calls buildPrompt/buildMinimalUpdate (the old "first iteration uses buildPrompt, continuation uses buildMinimalUpdate" logic). This is the CLI text response path. The new agent-loop.js handles prompt building separately.
       CAUTION: getAutomationActions is still called by callAIAPI in background.js (lines 11861-11917). Verify that getAutomationActions still works for callers that pass (task, structuredDOM, context, options) -- the method should still call this.provider and return a response. Only remove the buildPrompt/buildMinimalUpdate code paths INSIDE it.

    acceptance_criteria:
    - `ls ai/cli-parser.js` returns "No such file"
    - `ls utils/cli-validator.js` returns "No such file"
    - `grep -r 'CLI_COMMAND_TABLE' ai/ background.js` returns no matches
    - `grep -r 'TASK_PROMPTS' ai/ background.js` returns no matches
    - `grep -r 'buildPrompt\|buildMinimalUpdate' ai/ai-integration.js` returns no matches
    - `grep -r 'HYBRID_CONTINUATION_PROMPT\|BATCH_ACTION_INSTRUCTIONS\|PROMPT_CHAR_LIMIT' ai/ai-integration.js` returns no matches
    - `grep -r 'buildSheetsFormattingDirective' ai/` returns no matches
    - `node -e "const AI = require('./ai/ai-integration.js'); const ai = new AI({modelProvider:'xai',modelName:'test',apiKey:'test'}); console.log(typeof ai.callAPI)"` prints "function"
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Extensions/FSB && ! test -f ai/cli-parser.js && ! test -f utils/cli-validator.js && ! grep -rq 'CLI_COMMAND_TABLE\|TASK_PROMPTS\|buildPrompt\|buildMinimalUpdate\|HYBRID_CONTINUATION_PROMPT\|BATCH_ACTION_INSTRUCTIONS\|PROMPT_CHAR_LIMIT\|buildSheetsFormattingDirective' ai/ai-integration.js && echo "PASS: CLN-01 + CLN-02 dead code removed"</automated>
  </verify>
  <done>cli-parser.js and cli-validator.js deleted. CLI_COMMAND_TABLE, TASK_PROMPTS, HYBRID_CONTINUATION_PROMPT, BATCH_ACTION_INSTRUCTIONS, buildSheetsFormattingDirective, buildPrompt, buildMinimalUpdate all removed from ai-integration.js. AIIntegration class still instantiates and exports correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Remove CLI references from background.js and ui/options.js</name>
  <files>background.js, ui/options.js</files>
  <action>
    read_first: background.js (lines 1-20, 5809-5862), ui/options.js (lines 5695-5780, 5440-5450)

    1. In background.js, remove the importScripts line for cli-parser.js:
       - DELETE line 7: `importScripts('ai/cli-parser.js');`

    2. In background.js, remove the CLI_VALIDATION message handlers. In the main message listener switch statement, DELETE these two cases entirely:
       - case 'CLI_VALIDATION_LIVE_TEST' (lines ~5809-5843) -- the live test handler that creates AIIntegration and calls buildPrompt
       - case 'CLI_VALIDATION_GET_PROMPT' (lines ~5845-5862) -- the prompt getter that calls ai.buildPrompt
       Also remove `let _cliValidationLastCall` variable if it exists (rate limit tracker for CLI validation).

    3. In background.js, remove the comment at line 5850 referencing buildPrompt ("Build a representative system prompt by calling buildPrompt").

    4. In ui/options.js, remove CLI parser/validator dead code:
       - Remove `runEdgeCaseTests` function (line ~5699-5780+) -- calls parseCliResponse
       - Remove any reference to `parseCliResponse` (line 5702, 5773)
       - Remove the toast message "CLI parser not loaded" (line 5703)
       - Remove the toast message "CLI Validator not loaded" (line 5446)
       - Remove the `runCliValidation` or similar function that references CLIValidator (around line 5446)
       - Search for and remove any DOM element setup for cli-validation UI (edge case test buttons, results containers)

    5. Verify no orphaned references remain:
       - `grep -rn 'cli-parser\|cli.parser\|cliParser\|parseCliResponse\|CLIValidator\|cli.validator\|CLI_VALIDATION' background.js ui/ --include='*.js'` should return zero matches (excluding .claude/worktrees)

    acceptance_criteria:
    - `grep 'cli-parser' background.js` returns no matches
    - `grep 'CLI_VALIDATION' background.js` returns no matches
    - `grep -r 'parseCliResponse\|CLIValidator\|cli-validator\|runEdgeCaseTests' ui/options.js` returns no matches
    - `grep '_cliValidationLastCall' background.js` returns no matches
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Extensions/FSB && ! grep -q 'cli-parser' background.js && ! grep -q 'CLI_VALIDATION' background.js && ! grep -q 'parseCliResponse' ui/options.js && ! grep -q 'CLIValidator' ui/options.js && echo "PASS: CLI references removed from background.js and options.js"</automated>
  </verify>
  <done>background.js no longer imports cli-parser.js or handles CLI_VALIDATION messages. ui/options.js no longer references parseCliResponse, CLIValidator, or cli-validator.js. Zero orphaned CLI references remain.</done>
</task>

</tasks>

<verification>
Full sweep for any remaining CLI/prompt template references across the entire codebase (excluding .claude/worktrees):

```bash
grep -rn 'cli-parser\|CLI_COMMAND_TABLE\|TASK_PROMPTS\|buildPrompt\|buildMinimalUpdate\|parseCliResponse\|COMMAND_REGISTRY\|tokenizeLine\|mapCommand\|HYBRID_CONTINUATION\|BATCH_ACTION_INSTRUCTIONS\|CLI_VALIDATION\|CLIValidator\|cli-validator\|buildSheetsFormattingDirective\|PROMPT_CHAR_LIMIT' --include='*.js' --include='*.html' --include='*.json' . | grep -v '.claude/worktrees' | grep -v 'node_modules'
```

Expected: Zero matches. Any match is an orphaned reference that must be cleaned.
</verification>

<success_criteria>
1. cli-parser.js does not exist
2. cli-validator.js does not exist
3. ai-integration.js has no CLI_COMMAND_TABLE, TASK_PROMPTS, buildPrompt, buildMinimalUpdate, HYBRID_CONTINUATION_PROMPT, BATCH_ACTION_INSTRUCTIONS, PROMPT_CHAR_LIMIT, or buildSheetsFormattingDirective
4. background.js does not import cli-parser.js and has no CLI_VALIDATION handlers
5. ui/options.js has no parseCliResponse or CLIValidator references
6. AIIntegration class still instantiates and exports callAPI, testConnection, getAutomationActions
7. Zero orphaned references to deleted entities across the codebase
</success_criteria>

<output>
After completion, create `.planning/phases/139-dead-code-removal-polish/139-01-SUMMARY.md`
</output>
