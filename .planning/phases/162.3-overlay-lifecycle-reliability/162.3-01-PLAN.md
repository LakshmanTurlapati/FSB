---
phase: 162.3-overlay-lifecycle-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - background.js
  - ai/agent-loop.js
  - content/lifecycle.js
autonomous: true
gap_closure: true
requirements:
  - OVERLAY-01
must_haves:
  truths:
    - "An active session re-applies its latest overlay state after content reconnect or navigation without waiting for startup-only transitions."
    - "Normal agent-loop progress and report_progress refresh the canonical overlay state during the run."
    - "The canonical overlay source remains sendSessionStatus/overlayState rather than popup or dashboard-only events."
  artifacts:
    - path: "background.js"
      provides: "Cached canonical overlay snapshot plus replay on content ready"
      contains: "sendSessionStatus"
    - path: "ai/agent-loop.js"
      provides: "Routine overlay refresh calls during iteration flow"
      contains: "report_progress"
    - path: "content/lifecycle.js"
      provides: "Reconnect path that no longer permanently drops canonical overlay state"
      contains: "backgroundPort.onDisconnect"
  key_links:
    - from: "background.js"
      to: "content/lifecycle.js"
      via: "content-script port ready -> cached overlay replay"
      pattern: "msg.type === 'ready'"
    - from: "ai/agent-loop.js"
      to: "background.js"
      via: "sendStatus callback at iteration/report_progress refresh points"
      pattern: "sendStatus"
---

<objective>
Establish a canonical overlay replay path that survives content reconnects and navigation, then make ordinary agent-loop progress refresh that canonical state throughout the run.

Purpose: Close the primary disappearance path from the gap notes. Content overlays currently die on disconnect/navigation and routine agent-loop progress does not republish canonical overlay state, so the overlay never reliably comes back.
Output: Background-owned cached overlay status/replay logic, reconnect-safe content lifecycle handling, and agent-loop refresh points tied to normal progress.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.9.24-MILESTONE-AUDIT.md
@.planning/phases/162.3-overlay-lifecycle-reliability/162.3-CONTEXT.md
@.planning/debug/overlay-lifecycle-rehydration-gap.md
@.planning/debug/e2e-career-stall.md
@.planning/phases/151-dom-stream-consistency-and-state-sync/151-01-SUMMARY.md
@.planning/phases/162-event-bus-wiring/162-01-SUMMARY.md

@utils/overlay-state.js
@background.js
@ai/agent-loop.js
@content/lifecycle.js
@content/messaging.js

<interfaces>
From utils/overlay-state.js:
```javascript
buildOverlayState(statusData, session) => {
  lifecycle,
  result,
  phase,
  display: { title, subtitle, detail },
  progress: { mode, percent, label, eta },
  highlight: { animated }
}
```

From background.js:
```javascript
async function sendSessionStatus(tabId, statusData) {
  const payload = {
    action: 'sessionStatus',
    ...statusData,
    overlayState: buildOverlayPayload(tabId, statusData)
  };
}
```

From background.js onConnect handling:
```javascript
if (msg.type === 'ready') {
  contentScriptReadyStatus.set(tabId, { ready: true, timestamp: msg.timestamp, url: msg.url, method: 'port' });
}
```

From ai/agent-loop.js:
```javascript
const { sendSessionStatus: sendStatus } = options;

else if (call.name === 'report_progress') {
  session.lastAiReasoning = msg;
}
```

From content/messaging.js:
```javascript
case 'sessionStatus':
  FSB.overlayState = overlayState;
  FSB.progressOverlay.update(overlayState);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cache and replay canonical overlay state on reconnect</name>
  <files>
background.js
content/lifecycle.js
  </files>
  <read_first>
background.js (findOverlaySession, buildOverlayPayload, sendSessionStatus, chrome.runtime.onConnect ready handling)
content/lifecycle.js (establishBackgroundConnection, backgroundPort.onDisconnect, pageshow reconnect path)
utils/overlay-state.js
.planning/phases/162.3-overlay-lifecycle-reliability/162.3-CONTEXT.md
  </read_first>
  <action>
Implement the reconnect rehydration contract described by D-01 and D-02.

In `background.js`, upgrade `sendSessionStatus` into the canonical overlay publisher by caching the latest status snapshot on the matched active session before sending. Store enough data to replay the same state after reconnect, for example `session._lastOverlayStatusData`, `session._lastOverlaySentAt`, and the overlay identity token/version currently built in `buildOverlayPayload`. Keep `sendSessionStatus` as the only canonical overlay builder; do not introduce a second dashboard-only model.

Still in `background.js`, when the content-script port sends `msg.type === 'ready'`, find the active session for that tab (including `tabId`, `originalTabId`, or `previousTabId` matches consistent with `findOverlaySession`). If the session is running and has a cached overlay snapshot, immediately resend it through `sendSessionStatus` so a new content script or BF-cache restore rehydrates the overlay without waiting for a session start or tab-switch special case. If dashboard streaming is currently active for the same tab, follow the replay with `domStreamRequestOverlay` so the debugger preview can pull the same canonical overlay state.

In `content/lifecycle.js`, keep the disconnect cleanup limited to DOM overlay instances and action text. Do not permanently wipe canonical overlay memory on a transient port disconnect; preserve `FSB.overlayState` (or mark it as reconnecting) so short-lived port churn does not erase the last known state before background replay lands. Keep the existing exponential backoff and BF-cache reconnect behavior intact.

Avoid any visual redesign per D-06. Do not route content overlay recovery through `sessionStateEvent` or popup/sidepanel-only emitters.
  </action>
  <verify>
    <automated>node --check background.js && node --check content/lifecycle.js && rg -n "_lastOverlayStatusData|msg.type === 'ready'|domStreamRequestOverlay" background.js content/lifecycle.js</automated>
  </verify>
  <acceptance_criteria>
    - `background.js` caches the latest canonical overlay status data on the active session inside `sendSessionStatus`
    - The `msg.type === 'ready'` port path in `background.js` replays cached overlay state for an active running session on that tab
    - Replay still uses `sendSessionStatus` / `buildOverlayPayload` as the source of truth per D-01
    - `content/lifecycle.js` no longer unconditionally nulls `FSB.overlayState` on transient port disconnect
    - No new popup/sidepanel-only overlay recovery path is introduced
  </acceptance_criteria>
  <done>Reconnect or navigation-created content scripts can rehydrate the active overlay from background-owned canonical state without waiting for startup-only transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh canonical overlay state from normal agent-loop progress</name>
  <files>
ai/agent-loop.js
  </files>
  <read_first>
ai/agent-loop.js (runAgentLoop options, finalizeSession helper, before-provider broadcastDashboardProgress call, report_progress handling, after-iteration persistence path)
utils/overlay-state.js
.planning/debug/overlay-lifecycle-rehydration-gap.md
  </read_first>
  <action>
Implement D-03 by wiring routine agent-loop activity into `sendStatus`, not just startup and rare transitions.

In `ai/agent-loop.js`, add a small local helper inside `runAgentLoop`/`runAgentIteration` that refreshes canonical overlay status with the current session data. Use it at the minimum refresh points that map to real progress without flooding:

1. Right before the provider call starts for an iteration, emit an indeterminate planning/thinking state using the current iteration number, max iterations, task/taskSummary, and the latest `session.lastAiReasoning` if available.
2. In the `report_progress` tool path, keep updating `session.lastAiReasoning` but also immediately call `sendStatus(session.tabId, ...)` so the content overlay detail updates mid-iteration instead of waiting for the next startup-only or completion message.
3. After tool execution / afterIteration, emit a refreshed acting or planning state that reflects the new iteration count and most recent action context, while preserving the existing dashboard broadcast path.

Do not emit from popup/sidepanel event hooks and do not convert the setTimeout-chained loop into a synchronous loop. Keep the existing completion/error cleanup behavior intact.
  </action>
  <verify>
    <automated>node --check ai/agent-loop.js && rg -n "report_progress|sendStatus\\(|lastAiReasoning" ai/agent-loop.js</automated>
  </verify>
  <acceptance_criteria>
    - `ai/agent-loop.js` calls `sendStatus(` from normal iteration flow before provider waits begin
    - The `report_progress` branch both updates `session.lastAiReasoning` and triggers an overlay refresh
    - Post-tool or post-iteration progress refreshes exist without relying on popup/sidepanel `sessionStateEvent`
    - The existing setTimeout-chained execution model is preserved
    - No new dashboard-only overlay model or visual redesign is introduced
  </acceptance_criteria>
  <done>Ordinary iteration flow and `report_progress` keep canonical overlay state fresh throughout the run instead of leaving it tied to startup-only transitions.</done>
</task>

</tasks>

<verification>
Plan 01 closes the reconnect and sparse-refresh half of the gap:

1. Background owns the latest overlay snapshot and replays it when content reconnects or a new page instance reports ready.
2. Agent-loop progress now republishes canonical overlay state during real execution, including `report_progress`.
3. The canonical content overlay source remains `sendSessionStatus` / `overlayState` per D-01.
</verification>

<success_criteria>
1. `background.js` contains a cached overlay snapshot path tied to `sendSessionStatus`.
2. `background.js` replays the cached overlay from the content-script `ready` message path.
3. `content/lifecycle.js` no longer permanently clears canonical overlay state on transient port disconnect.
4. `ai/agent-loop.js` triggers `sendStatus` from routine iteration flow and the `report_progress` tool path.
</success_criteria>

<output>
After completion, create `.planning/phases/162.3-overlay-lifecycle-reliability/162.3-01-SUMMARY.md`
</output>
