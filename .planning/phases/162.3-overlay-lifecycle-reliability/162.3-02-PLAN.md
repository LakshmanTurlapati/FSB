---
phase: 162.3-overlay-lifecycle-reliability
plan: 02
type: execute
wave: 2
depends_on:
  - 162.3-01
files_modified:
  - background.js
  - content/messaging.js
  - content/dom-stream.js
autonomous: true
gap_closure: true
requirements:
  - OVERLAY-02
  - OVERLAY-03
must_haves:
  truths:
    - "Long provider waits or retries keep a visible overlay or degraded waiting state instead of disappearing at the 60-second watchdog."
    - "Dashboard/debugger overlay updates come from the same canonical content overlay state during reconnects, stream restarts, and long waits."
    - "Terminal cleanup still removes overlays, but active-running sessions no longer look dead during provider stalls."
  artifacts:
    - path: "background.js"
      provides: "Overlay heartbeat/refresh coordination for active running sessions"
      contains: "sendSessionStatus"
    - path: "content/messaging.js"
      provides: "Watchdog that degrades or extends active overlays instead of destroying them at 60s"
      contains: "sessionStatus"
    - path: "content/dom-stream.js"
      provides: "Immediate ext:dom-overlay broadcasts sourced from canonical FSB.overlayState on stream start/resume/request"
      contains: "domStreamRequestOverlay"
  key_links:
    - from: "background.js"
      to: "content/messaging.js"
      via: "overlay heartbeat / replay sendSessionStatus"
      pattern: "sendSessionStatus"
    - from: "content/dom-stream.js"
      to: "showcase/js/dashboard.js"
      via: "domStreamOverlay -> ext:dom-overlay -> handleDOMOverlay"
      pattern: "ext:dom-overlay"
---

<objective>
Keep overlays alive during long provider waits and make the dashboard/debugger consume the same canonical overlay state during reconnect or stream recovery.

Purpose: The gap is not only reconnect-related. Provider waits and retry windows can exceed the existing 60-second watchdog, and the dashboard preview only stays aligned when content actively republishes the canonical state.
Output: Background-driven long-wait overlay refreshes, a softer content watchdog, and DOM-stream overlay broadcasts that keep the dashboard synchronized from the same `FSB.overlayState`.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.9.24-MILESTONE-AUDIT.md
@.planning/phases/162.3-overlay-lifecycle-reliability/162.3-CONTEXT.md
@.planning/debug/e2e-career-stall.md
@.planning/debug/overlay-lifecycle-rehydration-gap.md
@.planning/phases/150-dashboard-transport-baseline-recovery/150-01-SUMMARY.md
@.planning/phases/151-dom-stream-consistency-and-state-sync/151-01-SUMMARY.md
@.planning/phases/162.3-overlay-lifecycle-reliability/162.3-01-PLAN.md

@background.js
@content/messaging.js
@content/dom-stream.js
@showcase/js/dashboard.js
@utils/overlay-state.js

<interfaces>
From content/messaging.js:
```javascript
case 'sessionStatus':
  FSB.overlayState = overlayState;
  FSB.progressOverlay.update(overlayState);
  FSB._overlayWatchdogTimer = setTimeout(..., 60000);
```

From content/dom-stream.js:
```javascript
function broadcastOverlayState() {
  var overlayState = FSB.overlayState;
  chrome.runtime.sendMessage({
    action: 'domStreamOverlay',
    glow,
    progress,
    streamSessionId,
    snapshotId
  });
}
```

From background.js:
```javascript
function broadcastDashboardProgress(session) {
  chrome.tabs.sendMessage(session.tabId, { action: 'domStreamRequestOverlay' }, { frameId: 0 });
}
```

From showcase/js/dashboard.js:
```javascript
if (msg.type === 'ext:dom-overlay') {
  handleDOMOverlay(msg.payload);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keep active overlays alive through long provider waits</name>
  <files>
background.js
content/messaging.js
  </files>
  <read_first>
background.js (sendSessionStatus, activeSessions lifecycle, cleanup/endSessionOverlays paths, broadcastDashboardProgress)
content/messaging.js (sessionStatus handler and 60s overlay watchdog)
.planning/debug/e2e-career-stall.md
.planning/phases/162.3-overlay-lifecycle-reliability/162.3-01-PLAN.md
  </read_first>
  <action>
Implement D-04 using the canonical overlay snapshot from Plan 01 rather than a new retry-specific UI model.

In `background.js`, add a lightweight overlay heartbeat/refresh loop for active running sessions that already have a cached canonical overlay snapshot. The loop should periodically resend the last known overlay state before the content watchdog expires, and it may degrade the detail text to a waiting/retrying message when the session has been idle in the provider call for too long. Keep this tied to active sessions only, stop it on session cleanup/finalization, and reuse the existing `sendSessionStatus` path so overlay identity/versioning stays centralized.

In `content/messaging.js`, replace the current hard 60-second "destroy everything" behavior for running overlays with a softer watchdog. When the timer fires for a still-running lifecycle, keep the progress overlay visible in a waiting/degraded state or arm a second-stage orphan timeout; only fully destroy overlays when lifecycle is explicitly `cleared`/`final` or the content script is clearly orphaned well past the active heartbeat window. Preserve explicit cleanup on ended/error/final states.

Do not redesign the overlay visuals, and do not move this responsibility into popup or sidepanel events.
  </action>
  <verify>
    <automated>node --check background.js && node --check content/messaging.js && rg -n "overlay.*heartbeat|_overlayWatchdogTimer|waiting|retry" background.js content/messaging.js</automated>
  </verify>
  <acceptance_criteria>
    - `background.js` contains an active-session overlay refresh/heartbeat path driven by cached canonical overlay data
    - Heartbeat/refresh logic is stopped or ignored for completed, failed, or cleaned-up sessions
    - `content/messaging.js` no longer unconditionally destroys running overlays at exactly 60 seconds of silence
    - Terminal `cleared` or `final` overlay states still clean up overlays promptly
    - No new visual components or separate retry-only overlay model are introduced
  </acceptance_criteria>
  <done>Long provider waits no longer make an active session overlay vanish after the first 60-second silence window.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize dashboard preview overlays from canonical content state</name>
  <files>
background.js
content/dom-stream.js
  </files>
  <read_first>
content/dom-stream.js (broadcastOverlayState, domStreamStart/domStreamResume/domStreamRequestOverlay cases)
background.js (broadcastDashboardProgress, content-script ready replay path, domStreamOverlay forwarding)
showcase/js/dashboard.js (handleDOMOverlay consumer expectations)
.planning/phases/151-dom-stream-consistency-and-state-sync/151-01-SUMMARY.md
  </read_first>
  <action>
Implement D-05 by making dashboard/debugger overlay updates pull from the same canonical content overlay state used by the page overlay.

In `content/dom-stream.js`, broadcast overlay state immediately when a stream starts, resumes, or receives an explicit overlay request, using the existing `FSB.overlayState` object and current stream identity (`streamSessionId`, `snapshotId`). Do not synthesize independent dashboard progress data.

In `background.js`, whenever canonical overlay replay or heartbeat runs for the actively streamed tab, follow it with `domStreamRequestOverlay` so the dashboard gets a fresh `ext:dom-overlay` update even if no new tool execution has happened. Reuse the Phase 151 stream-generation contract and existing `domStreamOverlay` forwarding code; do not add a second dashboard overlay channel or bypass `ext:dom-overlay`.

Keep `showcase/js/dashboard.js` unchanged unless an exact consumer bug blocks canonical overlay display. The preferred outcome is to feed the existing `handleDOMOverlay` path with more reliable source events, not to create a new dashboard state machine.
  </action>
  <verify>
    <automated>node --check background.js && node --check content/dom-stream.js && rg -n "domStreamStart|domStreamResume|domStreamRequestOverlay|ext:dom-overlay" background.js content/dom-stream.js showcase/js/dashboard.js</automated>
  </verify>
  <acceptance_criteria>
    - `content/dom-stream.js` emits overlay state immediately on stream start/resume/request using `FSB.overlayState`
    - `background.js` requests or forwards overlay refreshes for the actively streamed tab when replay/heartbeat updates occur
    - Dashboard overlay updates still travel through the existing `ext:dom-overlay` consumer path in `showcase/js/dashboard.js`
    - No second overlay schema or dashboard-only progress model is introduced
    - Stream identity metadata remains attached to overlay broadcasts per Phase 151 patterns
  </acceptance_criteria>
  <done>Dashboard/debugger overlay state stays aligned with the same canonical content overlay state across reconnects, stream restarts, and long waits.</done>
</task>

</tasks>

<verification>
Plan 02 closes the long-wait and dashboard-parity gap:

1. Active sessions keep an overlay alive or explicitly degraded during provider stalls and retries.
2. The content watchdog no longer makes a healthy-but-waiting run appear dead after 60 seconds.
3. Dashboard preview overlays continue to come from `FSB.overlayState` through `ext:dom-overlay`, including reconnect and heartbeat refreshes.
</verification>

<success_criteria>
1. `background.js` contains active-session overlay heartbeat/refresh logic tied to cached canonical overlay state.
2. `content/messaging.js` degrades or extends running overlays instead of always destroying them at the first 60-second timeout.
3. `content/dom-stream.js` broadcasts overlay state immediately on stream start/resume/request under the existing stream identity contract.
4. Dashboard overlay updates still flow through `ext:dom-overlay` and `handleDOMOverlay`, with no separate overlay model introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/162.3-overlay-lifecycle-reliability/162.3-02-SUMMARY.md`
</output>
