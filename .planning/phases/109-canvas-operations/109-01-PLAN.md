---
phase: 109-canvas-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site-guides/design/excalidraw.js
autonomous: true
requirements:
  - CANVAS-01
  - CANVAS-02
  - CANVAS-03
  - CANVAS-04
  - CANVAS-05
  - CANVAS-06

must_haves:
  truths:
    - "Guidance string contains CANVAS OPERATIONS section with undo, redo, clear, zoom, pan, select-all, zoom-to-fit instructions"
    - "Workflows object contains undoRedo, clearCanvas, zoomIn, zoomOut, zoomReset, zoomToFit, panCanvas, selectAll entries"
    - "All keyboard shortcuts match CONTEXT.md research: Ctrl+Z, Ctrl+Y, Ctrl+A+Delete, Ctrl+=/-, Ctrl+0, Shift+1, Space+drag"
  artifacts:
    - path: "site-guides/design/excalidraw.js"
      provides: "Canvas operation workflows and guidance"
      contains: "CANVAS OPERATIONS"
  key_links:
    - from: "guidance string CANVAS OPERATIONS section"
      to: "workflows object canvas entries"
      via: "matching operation names"
      pattern: "undoRedo|clearCanvas|zoom|panCanvas|selectAll"
---

<objective>
Add canvas operation workflows to the Excalidraw site guide covering undo/redo, clear canvas, zoom controls, pan, select all, and zoom-to-fit.

Purpose: Enable FSB autopilot to control Excalidraw canvas state via keyboard shortcuts, completing Phase 109 requirements.
Output: Updated site-guides/design/excalidraw.js with CANVAS OPERATIONS guidance section and workflow arrays.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@site-guides/design/excalidraw.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canvas operations guidance and workflows to Excalidraw site guide</name>
  <files>site-guides/design/excalidraw.js</files>
  <read_first>site-guides/design/excalidraw.js</read_first>
  <action>
Edit site-guides/design/excalidraw.js to add canvas operation content in two locations:

1. GUIDANCE STRING -- Add a "CANVAS OPERATIONS" section after the TEXT ENTRY WORKFLOW section (before MULTI-SELECT SHAPES). Include these subsections with exact keyboard shortcuts:

  CANVAS OPERATIONS (keyboard shortcuts):

    UNDO / REDO (CANVAS-01):
      Undo: press_key z ctrl=true (Ctrl+Z)
      Redo: press_key y ctrl=true (Ctrl+Y)
      Alternative redo: press_key z ctrl=true shift=true (Ctrl+Shift+Z)
      NOTE: Undo/redo operate on the Excalidraw internal history stack -- works for draws, deletes, moves, style changes.

    CLEAR CANVAS (CANVAS-02):
      Step 1: press_key a ctrl=true (Ctrl+A) to select all elements
      Step 2: press_key Delete to delete all selected elements
      NOTE: Use Delete not Backspace -- Delete is more reliable on Excalidraw. This is the same sequence as session setup steps 2-3.

    ZOOM IN (CANVAS-03):
      press_key = ctrl=true (Ctrl+=) to zoom in one step
      Repeat for additional zoom levels.

    ZOOM OUT (CANVAS-03):
      press_key - ctrl=true (Ctrl+-) to zoom out one step
      Repeat for additional zoom levels.

    ZOOM RESET (CANVAS-03):
      press_key 0 ctrl=true (Ctrl+0) to reset zoom to 100%.

    ZOOM TO FIT (CANVAS-06):
      press_key 1 shift=true (Shift+1) to zoom and pan so all elements fit in the viewport.
      NOTE: Only useful when elements exist on canvas. On empty canvas this is a no-op.

    PAN CANVAS (CANVAS-04):
      Hold Space then cdpDrag(startX, startY, endX, endY, steps=15, stepDelayMs=15) to pan the viewport.
      Implementation: press_key Space (keyDown only, do not release), then cdpDrag to pan, then release Space.
      Alternative: Use scroll wheel -- scroll(deltaX, deltaY) on the canvas element pans when not over a shape.
      NOTE: Panning shifts the viewport without moving elements. Coordinates in subsequent actions are viewport-relative.

    SELECT ALL (CANVAS-05):
      press_key a ctrl=true (Ctrl+A) to select all elements on the canvas.
      After select-all, shapes show combined selection handles (resize corners around the group).

2. WORKFLOWS OBJECT -- Add these workflow arrays to the existing workflows object (after textEdit, before the closing brace):

    undoRedo: [
      'Undo last action: press_key(z, ctrl=true) -- Ctrl+Z',
      'Redo undone action: press_key(y, ctrl=true) -- Ctrl+Y',
      'Alternative redo: press_key(z, ctrl=true, shift=true) -- Ctrl+Shift+Z',
      'Repeat as needed -- undo/redo operate on Excalidraw internal history stack'
    ],
    clearCanvas: [
      'Select all elements: press_key(a, ctrl=true) -- Ctrl+A',
      'Delete all selected: press_key Delete -- use Delete not Backspace for reliability',
      'Canvas is now empty and ready for new content'
    ],
    zoomIn: [
      'Zoom in one step: press_key(=, ctrl=true) -- Ctrl+=',
      'Repeat press_key(=, ctrl=true) for additional zoom levels'
    ],
    zoomOut: [
      'Zoom out one step: press_key(-, ctrl=true) -- Ctrl+-',
      'Repeat press_key(-, ctrl=true) for additional zoom levels'
    ],
    zoomReset: [
      'Reset zoom to 100%: press_key(0, ctrl=true) -- Ctrl+0'
    ],
    zoomToFit: [
      'Zoom to fit all content: press_key(1, shift=true) -- Shift+1',
      'Viewport adjusts to show all elements -- no-op on empty canvas'
    ],
    panCanvas: [
      'Hold Space key then drag to pan: press_key Space (hold), cdpDrag(startX, startY, endX, endY, steps=15, stepDelayMs=15), release Space',
      'Alternative: scroll on canvas element to pan viewport',
      'Panning shifts viewport without moving elements -- subsequent coordinates are viewport-relative'
    ],
    selectAll: [
      'Select all elements: press_key(a, ctrl=true) -- Ctrl+A',
      'All shapes show combined selection handles (resize corners around group)',
      'Use for bulk operations: delete, move, align, group, duplicate'
    ]

3. KEYBOARD SHORTCUTS section in guidance string -- Add these shortcuts to the existing list (after the Escape entry):
    Ctrl+Z = Undo last action
    Ctrl+Y = Redo (or Ctrl+Shift+Z)
    Ctrl+= = Zoom in
    Ctrl+- = Zoom out
    Ctrl+0 = Reset zoom to 100%
    Shift+1 = Zoom to fit all content

4. WARNINGS array -- Add one entry:
    'Pan (Space+drag) requires holding Space before starting cdpDrag -- releasing Space mid-drag cancels pan mode'
  </action>
  <verify>
    <automated>grep -c "CANVAS OPERATIONS" site-guides/design/excalidraw.js && grep -c "undoRedo" site-guides/design/excalidraw.js && grep -c "clearCanvas" site-guides/design/excalidraw.js && grep -c "zoomIn" site-guides/design/excalidraw.js && grep -c "zoomToFit" site-guides/design/excalidraw.js && grep -c "panCanvas" site-guides/design/excalidraw.js && grep -c "selectAll" site-guides/design/excalidraw.js && grep -c "CANVAS-01" site-guides/design/excalidraw.js && grep -c "CANVAS-06" site-guides/design/excalidraw.js && node -c site-guides/design/excalidraw.js 2>&1 || echo "SYNTAX ERROR"</automated>
  </verify>
  <acceptance_criteria>
    - grep "CANVAS OPERATIONS" site-guides/design/excalidraw.js returns at least 1 match
    - grep "undoRedo:" site-guides/design/excalidraw.js returns 1 match
    - grep "clearCanvas:" site-guides/design/excalidraw.js returns 1 match
    - grep "zoomIn:" site-guides/design/excalidraw.js returns 1 match
    - grep "zoomOut:" site-guides/design/excalidraw.js returns 1 match
    - grep "zoomReset:" site-guides/design/excalidraw.js returns 1 match
    - grep "zoomToFit:" site-guides/design/excalidraw.js returns 1 match
    - grep "panCanvas:" site-guides/design/excalidraw.js returns 1 match
    - grep "selectAll:" site-guides/design/excalidraw.js returns 1 match
    - grep "CANVAS-01" site-guides/design/excalidraw.js returns at least 1 match
    - grep "CANVAS-02" site-guides/design/excalidraw.js returns at least 1 match
    - grep "CANVAS-03" site-guides/design/excalidraw.js returns at least 1 match
    - grep "CANVAS-04" site-guides/design/excalidraw.js returns at least 1 match
    - grep "CANVAS-05" site-guides/design/excalidraw.js returns at least 1 match
    - grep "CANVAS-06" site-guides/design/excalidraw.js returns at least 1 match
    - node -c site-guides/design/excalidraw.js exits 0 (valid JavaScript syntax)
  </acceptance_criteria>
  <done>Excalidraw site guide contains CANVAS OPERATIONS guidance section with all 6 operation types documented, plus matching workflow arrays in the workflows object, and the file passes JavaScript syntax validation.</done>
</task>

</tasks>

<verification>
All canvas operations documented in guidance string and workflow arrays. File passes syntax check.
</verification>

<success_criteria>
- CANVAS OPERATIONS section present in guidance string with undo, redo, clear, zoom in/out/reset/fit, pan, select all
- 8 new workflow arrays added: undoRedo, clearCanvas, zoomIn, zoomOut, zoomReset, zoomToFit, panCanvas, selectAll
- All 6 requirement IDs (CANVAS-01 through CANVAS-06) referenced in guidance text
- File is valid JavaScript (node -c passes)
</success_criteria>

<output>
After completion, create `.planning/phases/109-canvas-operations/109-01-SUMMARY.md`
</output>
