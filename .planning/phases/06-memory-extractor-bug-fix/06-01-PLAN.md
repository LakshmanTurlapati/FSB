---
phase: 06-memory-extractor-bug-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/memory/memory-extractor.js
autonomous: true

must_haves:
  truths:
    - "When an AI provider is configured, memory extraction uses the AI provider (not a local fallback)"
    - "When no API key is configured, extraction does not run and surfaces a clear error"
    - "When AI call fails transiently, it retries 1-2 times before surfacing error with badge indicator"
    - "Auth errors (401, 403) surface immediately with badge indicator, no retry"
    - "Extension badge shows red '!' on any extraction failure so user knows something went wrong"
  artifacts:
    - path: "lib/memory/memory-extractor.js"
      provides: "Fixed MemoryExtractor with correct UniversalProvider instantiation"
      contains: "new UniversalProvider(cfg)"
  key_links:
    - from: "lib/memory/memory-extractor.js"
      to: "ai/universal-provider.js"
      via: "constructor call with single settings object"
      pattern: "new UniversalProvider\\(cfg\\)"
    - from: "lib/memory/memory-extractor.js"
      to: "chrome.storage.local"
      via: "direct chrome.storage.local.get for config loading"
      pattern: "chrome\\.storage\\.local\\.get"
---

<objective>
Fix the UniversalProvider constructor bug in memory-extractor.js and remove local fallback entirely.

Purpose: The constructor at line 273 passes three separate arguments instead of a single settings object, causing AI extraction to silently fail and always fall back to local extraction. Per user decision, local fallback is removed -- memory extraction is AI-only. Failures surface visibly via extension badge and console errors.

Output: A working memory-extractor.js that correctly instantiates UniversalProvider, validates provider config, retries transient failures, and surfaces all errors visibly.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-memory-extractor-bug-fix/06-CONTEXT.md
@.planning/phases/06-memory-extractor-bug-fix/06-RESEARCH.md

Key reference files:
@lib/memory/memory-extractor.js (primary file to modify)
@lib/memory/sitemap-refiner.js (lines 75-94 for loadProviderSettings pattern, lines 112-123 for key validation pattern)
@ai/ai-providers.js (line 19 for correct UniversalProvider instantiation)
@background.js (lines 3920-3921 for badge error pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix _getProvider and remove local fallback from extract()</name>
  <files>lib/memory/memory-extractor.js</files>
  <action>
Rewrite `_getProvider()` (lines 263-288) to:
1. Keep `context.provider` passthrough (line 265) -- if caller already has a provider, use it directly
2. Remove `this._provider` caching check (line 266) and `this._provider = provider` assignment (line 282) -- create fresh provider every call per user decision
3. Use direct `chrome.storage.local.get()` with explicit key list instead of `config.loadFromStorage()` -- match the exact pattern from `sitemap-refiner.js:75-94` (`loadProviderSettings`). Keys: `modelProvider`, `modelName`, `apiKey`, `geminiApiKey`, `openaiApiKey`, `anthropicApiKey`, `customApiKey`, `customEndpoint`
4. Add provider validation matching `sitemap-refiner.js:112-123` pattern: build keyMap object `{ xai: cfg.apiKey, gemini: cfg.geminiApiKey, openai: cfg.openaiApiKey, anthropic: cfg.anthropicApiKey, custom: cfg.customApiKey }`, resolve `activeKey`, throw with clear message if no key found
5. Validate `cfg.modelName` is present, throw if missing
6. Call `new UniversalProvider(cfg)` with single settings object (NOT three separate args)
7. Remove the try/catch that returns null on error -- let errors propagate (caller handles them)

Rewrite `extract()` (lines 18-63) to:
1. Remove the `if (!provider)` block (lines 37-42) that calls `_localFallbackExtract` -- _getProvider now throws instead of returning null
2. Remove the catch block's `_localFallbackExtract` call (line 59) -- replace with error surfacing:
   a. Set badge: `chrome.action.setBadgeBackgroundColor({ color: '#FF0000' })` then `chrome.action.setBadgeText({ text: '!' })`
   b. Log error to console: `console.error('[MemoryExtractor] AI extraction failed:', error.message)`
   c. Implement retry for transient failures: if error message/status indicates timeout or rate limit (status 429, 503, or message contains 'timeout', 'ECONNRESET', 'network'), retry up to 2 times with 1s/2s delay. For auth errors (status 401, 403, or message contains 'unauthorized', 'forbidden', 'invalid.*key'), set badge and throw immediately -- no retry
   d. After retries exhausted, set badge and throw the error (letting memory-manager.js catch handle it)

Update `enrich()` (lines 411-449):
1. The `_getProvider` call already exists at line 418. Since _getProvider now throws instead of returning null, wrap the provider call with error handling
2. On failure: set the extension badge (same red '!' pattern), log to console, return null (per store-first-enrich-second pattern -- enrichment failure should not break storage)

Remove from constructor (lines 8-10):
1. Remove `this._provider = null` -- no longer caching

Delete `_localFallbackExtract` method entirely (lines 297-401).

Remove the catch-block fallback in `_parseExtractedMemories` (lines 233-255):
1. In the catch block, remove the episodic memory creation fallback (lines 236-254)
2. Replace with: log the parse error, return whatever `memories` array has been built so far (could be empty `[]`)

IMPORTANT: Do NOT modify `_buildExtractionPrompt`, `_buildEnrichmentPrompt`, `_parseEnrichmentResponse`, or `_extractDomain` -- they are correct as-is.
  </action>
  <verify>
Run: `grep -n "localFallbackExtract\|this\._provider\|cfg\.modelProvider, cfg\.modelName" lib/memory/memory-extractor.js`
Expected: zero matches (all three patterns removed).

Run: `grep -n "new UniversalProvider(cfg)" lib/memory/memory-extractor.js`
Expected: exactly one match showing the fixed constructor call.

Run: `grep -n "chrome.storage.local.get" lib/memory/memory-extractor.js`
Expected: exactly one match in _getProvider.

Run: `grep -n "setBadgeText" lib/memory/memory-extractor.js`
Expected: matches in extract() catch block and enrich() catch block.

Run: `grep -c "_localFallbackExtract" lib/memory/memory-extractor.js`
Expected: 0 (method and all references deleted).
  </verify>
  <done>
_getProvider uses chrome.storage.local.get directly, validates API key per provider, and calls `new UniversalProvider(cfg)` with a single settings object. Local fallback is completely removed. Extraction failures set extension badge and propagate errors. Transient failures retry 1-2 times. Auth failures surface immediately.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify no regressions in callers and overall coherence</name>
  <files>lib/memory/memory-extractor.js</files>
  <action>
After Task 1 changes, do a final coherence pass on the complete file:

1. Verify the singleton export block at bottom (lines ~518-524) is intact: `const memoryExtractor = new MemoryExtractor()` and `self.MemoryExtractor`/`self.memoryExtractor` assignments
2. Verify `_buildExtractionPrompt` is unchanged and still referenced by `extract()`
3. Verify `_buildEnrichmentPrompt` and `_parseEnrichmentResponse` are unchanged and still referenced by `enrich()`
4. Verify `_extractDomain` is unchanged and still referenced by `_buildExtractionPrompt` and `_parseExtractedMemories`
5. Verify the `createEpisodicMemory`, `createSemanticMemory`, `createProceduralMemory`, `validateMemory` references in `_parseExtractedMemories` are still present (these are globals from memory-types.js loaded via importScripts)
6. Verify `extract()` still sets `session._lastExtractionActionIndex = allActions.length` on BOTH success path and error path (so re-extraction after recovery does not duplicate)
7. Verify `enrich()` still tracks analytics via `globalAnalytics.trackUsage` on the success path
8. Count total lines -- file should be shorter than original 525 lines (removed ~105 lines of _localFallbackExtract, gained ~30 lines of retry/badge logic)

Verify callers are compatible (read-only check, no modifications):
- `lib/memory/memory-manager.js:30` calls `this._extractor.extract(session, context)` in a try/catch that returns `[]` on error -- this is fine, badge is set inside memory-extractor before the error propagates
- `lib/memory/memory-manager.js:71` calls `this._enrichAsync(memory)` which calls `enrich()` -- enrich returns null on failure, which is handled
- `background.js:229` area that calls memory extraction -- any upstream catch is fine since badge is set in memory-extractor itself
  </action>
  <verify>
Run: `node -c lib/memory/memory-extractor.js`
Expected: no syntax errors.

Run: `grep -c "function\|async " lib/memory/memory-extractor.js | head -1`
Sanity check: file has expected number of methods.

Run: `wc -l lib/memory/memory-extractor.js`
Expected: ~420-450 lines (shorter than original 525).
  </verify>
  <done>
File passes syntax check, all method references are intact, singleton export works, callers are compatible, and the file is shorter than the original due to local fallback removal.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "_localFallbackExtract" lib/memory/` returns zero results
2. `grep -n "new UniversalProvider(cfg)" lib/memory/memory-extractor.js` shows single correct constructor call
3. `grep -n "chrome.storage.local.get" lib/memory/memory-extractor.js` shows direct storage access (not config.loadFromStorage)
4. `grep -n "setBadgeText" lib/memory/memory-extractor.js` shows badge error indicators in both extract and enrich error paths
5. `grep -n "this._provider" lib/memory/memory-extractor.js` returns zero results (no provider caching)
6. `node -c lib/memory/memory-extractor.js` passes with no syntax errors
</verification>

<success_criteria>
- UniversalProvider receives a single settings object (matching sitemap-refiner.js and background.js patterns)
- No local fallback code exists anywhere in the file
- Provider config loaded via chrome.storage.local.get (not config.loadFromStorage)
- API key validated per provider before constructing UniversalProvider
- Auth errors surface immediately with badge, no retry
- Transient errors retry 1-2 times, then surface with badge
- Extension badge shows red "!" on any extraction/enrichment failure
- File passes syntax check
</success_criteria>

<output>
After completion, create `.planning/phases/06-memory-extractor-bug-fix/06-01-SUMMARY.md`
</output>
