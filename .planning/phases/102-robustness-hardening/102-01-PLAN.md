---
phase: 102-robustness-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/actions.js
  - background.js
autonomous: true
requirements:
  - ROBUST-01
  - ROBUST-02
must_haves:
  truths:
    - "CDP tool calls with x or y outside viewport bounds are rejected before the chrome.runtime.sendMessage call"
    - "Rejection error message includes actual viewport dimensions (width x height)"
    - "When stuckCounter >= 3 and recent actions used coordinate-based tools, recovery strategies include a DOM fallback suggestion"
    - "When stuckCounter >= 3 and recent actions used DOM-based tools, recovery strategies include a coordinate approach suggestion"
  artifacts:
    - path: "content/actions.js"
      provides: "Viewport bounds validation in all 5 CDP tool functions"
      contains: "window.innerWidth"
    - path: "background.js"
      provides: "Bidirectional tool-type-aware stuck recovery"
      contains: "coordinate_to_dom"
  key_links:
    - from: "content/actions.js:cdpClickAt"
      to: "window.innerWidth/innerHeight"
      via: "bounds check before sendMessage"
      pattern: "x < 0 || y < 0 || x > window\\.innerWidth || y > window\\.innerHeight"
    - from: "background.js:generateRecoveryStrategies"
      to: "session.actionHistory"
      via: "tool type classification from recent actions"
      pattern: "coordinate_to_dom|dom_to_coordinate"
---

<objective>
Add viewport bounds validation to all CDP coordinate tools and bidirectional tool-type-aware stuck recovery.

Purpose: Prevent CDP tools from executing with impossible coordinates (causing silent failures) and make stuck recovery smarter by suggesting the opposite interaction paradigm (coordinate vs DOM).
Output: Modified content/actions.js with bounds checks in 5 CDP functions, modified background.js with tool-type-aware recovery strategies.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@content/actions.js
@background.js

<interfaces>
<!-- CDP tools that need viewport validation (content/actions.js lines 5244-5325) -->
tools.cdpClickAt = async (params) => { const { x, y, shiftKey, ctrlKey, altKey } = params; ... }
tools.cdpClickAndHold = async (params) => { const { x, y, holdMs } = params; ... }
tools.cdpDrag = async (params) => { const { startX, startY, endX, endY, steps, stepDelayMs, ... } = params; ... }
tools.cdpDragVariableSpeed = async (params) => { const { startX, startY, endX, endY, steps, ... } = params; ... }
tools.cdpScrollAt = async (params) => { const { x, y, deltaX, deltaY } = params; ... }

<!-- Existing validateCoordinates at line 25 (DOM-side validation, not used by CDP tools) -->
function validateCoordinates(x, y) {
  if (x < 0 || y < 0 || x > window.innerWidth || y > window.innerHeight) {
    return { valid: false, reason: 'coordinates_outside_viewport' };
  }
  ...
}

<!-- Recovery strategy generation (background.js line 3559) -->
function generateRecoveryStrategies(patterns, session) {
  const strategies = [];
  if (patterns.repetitiveActions) { strategies.push({ type: 'break_repetition', ... }); }
  if (patterns.cyclingBetweenStates) { strategies.push({ type: 'reset_state', ... }); }
  if (patterns.failingOnSameElement) { strategies.push({ type: 'alternative_selectors', ... }); }
  if (patterns.noProgressMade) { strategies.push({ type: 'change_approach', ... }); }
  return strategies.sort(...);
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add viewport bounds validation to all 5 CDP tool functions</name>
  <files>content/actions.js</files>
  <read_first>content/actions.js (lines 5244-5325 for CDP tools, lines 25-46 for existing validateCoordinates pattern)</read_first>
  <action>
Add viewport bounds validation to all 5 CDP tool functions in content/actions.js. Each function already checks `typeof x !== 'number'` -- add a SECOND guard immediately after the type check that validates coordinates are within viewport bounds.

For cdpClickAt (line 5244) and cdpClickAndHold (line 5260) and cdpScrollAt (line 5311) -- these have simple {x, y} params:
After the existing `typeof x !== 'number'` check, add:
```
const vw = window.innerWidth;
const vh = window.innerHeight;
if (x < 0 || y < 0 || x > vw || y > vh) {
  return { success: false, error: `Coordinates (${x}, ${y}) outside viewport bounds (${vw}x${vh}). Use values within 0-${vw} for x and 0-${vh} for y.` };
}
```

For cdpDrag (line 5276) and cdpDragVariableSpeed (line 5294) -- these have {startX, startY, endX, endY} params:
After the existing `typeof startX !== 'number'` check, add:
```
const vw = window.innerWidth;
const vh = window.innerHeight;
if (startX < 0 || startY < 0 || startX > vw || startY > vh ||
    endX < 0 || endY < 0 || endX > vw || endY > vh) {
  return { success: false, error: `Drag coordinates outside viewport bounds (${vw}x${vh}). start=(${startX},${startY}) end=(${endX},${endY}). All values must be within viewport.` };
}
```

Do NOT modify the existing `typeof` checks. Do NOT call the existing validateCoordinates function (it does DOM elementFromPoint checks that are inappropriate for CDP tools which bypass the DOM). This is purely a numeric bounds check.
  </action>
  <verify>
    <automated>grep -c "outside viewport bounds" content/actions.js | grep -q "5" && echo "PASS: 5 viewport checks found" || echo "FAIL: expected 5 viewport bound checks"</automated>
  </verify>
  <acceptance_criteria>
- grep "outside viewport bounds" content/actions.js returns exactly 5 matches (one per CDP tool)
- grep "window.innerWidth" content/actions.js returns matches inside cdpClickAt, cdpClickAndHold, cdpDrag, cdpDragVariableSpeed, cdpScrollAt
- The existing typeof checks remain unchanged
- Each error message includes the actual viewport dimensions and the invalid coordinate values
  </acceptance_criteria>
  <done>All 5 CDP tools reject out-of-viewport coordinates before execution, with descriptive error messages including viewport dimensions</done>
</task>

<task type="auto">
  <name>Task 2: Add bidirectional tool-type-aware stuck recovery strategies</name>
  <files>background.js</files>
  <read_first>background.js (lines 3559-3620 for generateRecoveryStrategies, lines 9747-9835 for stuck detection block, lines 6060-6080 for session state shape)</read_first>
  <action>
Modify the `generateRecoveryStrategies` function in background.js (line 3559) to detect which tool paradigm (coordinate vs DOM) was used in recent actions and suggest the opposite.

Step 1: At the TOP of generateRecoveryStrategies, BEFORE any existing strategy logic, classify recent tool usage from session.actionHistory:
```
// Classify recent tool types for bidirectional recovery
const recentActions = (session.actionHistory || []).slice(-5);
const cdpTools = ['cdpClickAt', 'cdpClickAndHold', 'cdpDrag', 'cdpDragVariableSpeed', 'cdpScrollAt'];
const recentCdpCount = recentActions.filter(a => cdpTools.includes(a.tool)).length;
const recentDomCount = recentActions.filter(a => a.tool && !cdpTools.includes(a.tool) && a.tool !== 'navigate' && a.tool !== 'done' && a.tool !== 'fail').length;
```

Step 2: AFTER the existing `if (patterns.failingOnSameElement)` block (around line 3600), add two new strategy blocks:

Block A -- coordinate-to-DOM fallback:
```
// Bidirectional stuck recovery (ROBUST-02)
if (recentCdpCount > recentDomCount && recentCdpCount >= 2) {
  strategies.push({
    type: 'coordinate_to_dom',
    description: 'Recent CDP coordinate actions are failing. Switch to DOM-based interaction: use element refs (click e5, type e3 "text") instead of coordinate tools (clickat, scrollat). DOM refs are more reliable for standard page elements.',
    priority: 'high'
  });
}
```

Block B -- DOM-to-coordinate fallback:
```
if (recentDomCount > recentCdpCount && recentDomCount >= 2) {
  strategies.push({
    type: 'dom_to_coordinate',
    description: 'Recent DOM-based actions are failing. Switch to CDP coordinate interaction: use clickat x y, scrollat x y instead of element refs. Coordinate tools bypass DOM issues and work on canvas/overlay elements.',
    priority: 'high'
  });
}
```

Do NOT remove any existing strategies. Insert the new blocks after the failingOnSameElement block but before the noProgressMade block.
  </action>
  <verify>
    <automated>grep -c "coordinate_to_dom\|dom_to_coordinate" background.js | grep -q "2" && echo "PASS: both recovery directions found" || echo "FAIL: expected 2 bidirectional recovery types"</automated>
  </verify>
  <acceptance_criteria>
- grep "coordinate_to_dom" background.js returns at least 1 match
- grep "dom_to_coordinate" background.js returns at least 1 match
- grep "recentCdpCount" background.js returns at least 1 match (tool type classification exists)
- The existing strategies (break_repetition, reset_state, alternative_selectors, change_approach, canvas_keyboard) are all still present
  </acceptance_criteria>
  <done>Stuck recovery suggests DOM fallback when coordinate tools fail, and coordinate fallback when DOM tools fail</done>
</task>

</tasks>

<verification>
1. content/actions.js: All 5 CDP tools have viewport bounds checks that fire before chrome.runtime.sendMessage
2. background.js: generateRecoveryStrategies produces coordinate_to_dom or dom_to_coordinate strategies based on recent action history
3. No existing functionality is broken -- all prior strategies and type checks remain intact
</verification>

<success_criteria>
- CDP tools with coordinates (0, -50) or (99999, 500) return error objects with viewport dimensions instead of sending to CDP
- Stuck recovery after 3+ failed cdpClickAt actions includes "Switch to DOM-based interaction" strategy
- Stuck recovery after 3+ failed click/type actions includes "Switch to CDP coordinate interaction" strategy
</success_criteria>

<output>
After completion, create `.planning/phases/102-robustness-hardening/102-01-SUMMARY.md`
</output>
