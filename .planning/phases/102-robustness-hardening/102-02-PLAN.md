---
phase: 102-robustness-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ai/ai-integration.js
autonomous: true
requirements:
  - ROBUST-03
  - ROBUST-04
must_haves:
  truths:
    - "When system prompt + user prompt exceed a character threshold, the prompt is progressively trimmed in stages instead of timing out"
    - "Stage 1 trims task-type examples from getToolsDocumentation output"
    - "Stage 2 reduces element count by applying heavier compression and lower element cap"
    - "Stage 3 strips long-term memory injection from the prompt"
    - "When CLI parser returns zero actions and zero signals, a retry fires with a simplified prompt hint before the existing reformat retry"
    - "The simplified prompt hint includes the phrase 'respond with exactly one CLI command per line'"
  artifacts:
    - path: "ai/ai-integration.js"
      provides: "Progressive prompt trimming and enhanced CLI parse retry"
      contains: "PROMPT_CHAR_LIMIT"
  key_links:
    - from: "ai/ai-integration.js:buildPrompt"
      to: "formatElements"
      via: "reduced charBudget when prompt is over limit"
      pattern: "PROMPT_CHAR_LIMIT|trimStage"
    - from: "ai/ai-integration.js:processQueue"
      to: "parseCliResponse"
      via: "simplified hint retry before reformat retry"
      pattern: "simplified.*hint|one CLI command per line"
---

<objective>
Add progressive prompt trimming for heavy DOM pages and enhanced CLI parse failure retry with simplified prompt hint.

Purpose: Prevent timeouts on content-heavy pages (LinkedIn, large dashboards) by reducing prompt size in stages, and improve parse success rate by giving the AI a clearer format directive before attempting the heavier reformat retry.
Output: Modified ai/ai-integration.js with prompt trimming logic in buildPrompt and simplified-hint retry in processQueue.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@ai/ai-integration.js
@ai/cli-parser.js

<interfaces>
<!-- buildPrompt method (ai/ai-integration.js line 2438) -->
buildPrompt(task, domState, context = null) {
  // Builds systemPrompt (lines 2524-2675) from:
  //   - Core instructions (fixed ~2000 chars)
  //   - BATCH_ACTION_INSTRUCTIONS constant
  //   - getToolsDocumentation(taskType, siteGuide) -- includes CLI_COMMAND_TABLE + examples
  //   - _buildTaskGuidance(taskType, siteGuide, currentUrl, task) -- includes site guide + memory
  // Builds userPrompt from:
  //   - formatSemanticContext(domState)
  //   - formatElements(elements, charBudget, taskType) -- the heaviest section
  //   - Action history
  //   - Recovery strategies
  // Returns { systemPrompt, userPrompt }
}

<!-- formatElements (line 3563) with charBudget parameter -->
formatElements(elements, charBudget = Infinity, taskType = 'general') {
  // compression: none (<= 30 els), moderate (31-60), heavy (61+)
  // Already supports charBudget truncation
}

<!-- CLI reformat retry (lines 2274-2310) -->
// Currently: if parsed.actions.length === 0 && !taskComplete && !taskFailed && !helpRequested
//   -> sends reformat prompt with full rawText echo
//   -> reparses result
// Problem: reformat is heavyweight (echoes 500 chars of raw text back)
// ROBUST-04: Add a lighter "simplified hint" retry FIRST

<!-- parseCliResponse import (used at lines 2269, 2300) -->
// Imported from ai/cli-parser.js, returns { actions, reasoning, errors, taskComplete, taskFailed, ... }

<!-- Long-term memory injection in _buildTaskGuidance (line 4372+) -->
// this._longTermMemories array injected as RECOMMENDED APPROACH block
// this._crossDomainProcedural array injected as cross-domain strategies
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add progressive prompt trimming in buildPrompt</name>
  <files>ai/ai-integration.js</files>
  <read_first>ai/ai-integration.js (lines 2438-2500 for buildPrompt entry, lines 2520-2675 for system prompt assembly, lines 3562-3610 for formatElements, lines 4372-4500 for _buildTaskGuidance)</read_first>
  <action>
Add progressive prompt trimming to the buildPrompt method in ai/ai-integration.js. The trimming happens AFTER the full prompt is assembled, checking total character count and reducing in stages.

Step 1: Add a constant near the top of the file (near other constants, around line 12-20 area):
```
// ROBUST-03: Progressive prompt trimming threshold
// ~200K chars is safe for grok-4-1-fast (2M context / ~4 chars per token = 500K chars, 40% budget = 200K)
const PROMPT_CHAR_LIMIT = 200000;
```

Step 2: At the END of buildPrompt (just before the final return statement that returns { systemPrompt, userPrompt }), add a trimming block. Find where buildPrompt assembles the final return. The method builds systemPrompt (a string) and then builds userPrompt (a string) and returns them. Add this BEFORE the return:

```
    // ROBUST-03: Progressive prompt trimming for heavy pages
    const totalChars = systemPrompt.length + userPrompt.length;
    if (totalChars > PROMPT_CHAR_LIMIT) {
      let trimStage = 0;
      automationLogger.info('Prompt exceeds char limit, starting progressive trim', {
        sessionId: this.currentSessionId,
        totalChars,
        limit: PROMPT_CHAR_LIMIT,
        systemChars: systemPrompt.length,
        userChars: userPrompt.length
      });

      // Stage 1: Strip task-type example blocks from system prompt
      // These are the "Example:" and "PRIORITY TOOLS" sections that add guidance but are not essential
      if (totalChars > PROMPT_CHAR_LIMIT) {
        trimStage = 1;
        // Remove lines between "Example:" or "PRIORITY TOOLS" headers and next "===" section header
        systemPrompt = systemPrompt.replace(/PRIORITY TOOLS[^\n]*\n[\s\S]*?(?=\n===|\n\nPROVIDER NOTE)/g, '');
        // Remove example blocks (multi-line example patterns)
        systemPrompt = systemPrompt.replace(/Example[s]?:[\s\S]*?(?=\n\n[A-Z]|\n===)/g, '');
        automationLogger.debug('Trim stage 1: removed examples and priority blocks', {
          sessionId: this.currentSessionId,
          newSystemChars: systemPrompt.length,
          newTotal: systemPrompt.length + userPrompt.length
        });
      }

      // Stage 2: Reduce element count with tighter budget
      if (systemPrompt.length + userPrompt.length > PROMPT_CHAR_LIMIT) {
        trimStage = 2;
        // Re-format elements with a strict char budget (30K chars max for elements)
        const elementsSource = domState.elements || domState.viewportElements || [];
        if (elementsSource.length > 0) {
          const trimmedElements = this.formatElements(elementsSource, 30000, taskType);
          // Replace the INTERACTIVE ELEMENTS section in userPrompt
          userPrompt = userPrompt.replace(
            /=== INTERACTIVE ELEMENTS[\s\S]*?(?=\n=== (?!INTERACTIVE)|$)/,
            `=== INTERACTIVE ELEMENTS (trimmed: ${elementsSource.length} elements, budget: 30K chars) ===\n${trimmedElements}\n`
          );
          automationLogger.debug('Trim stage 2: reduced element char budget to 30K', {
            sessionId: this.currentSessionId,
            elementCount: elementsSource.length,
            newUserChars: userPrompt.length,
            newTotal: systemPrompt.length + userPrompt.length
          });
        }
      }

      // Stage 3: Strip long-term memory and cross-domain strategy blocks
      if (systemPrompt.length + userPrompt.length > PROMPT_CHAR_LIMIT) {
        trimStage = 3;
        // Remove RECOMMENDED APPROACH block from system prompt
        systemPrompt = systemPrompt.replace(/=== RECOMMENDED APPROACH[\s\S]*?(?=\n===|$)/, '');
        // Remove cross-domain strategy block
        systemPrompt = systemPrompt.replace(/=== CROSS-DOMAIN STRATEGIES[\s\S]*?(?=\n===|$)/, '');
        // Also remove from user prompt if present there
        userPrompt = userPrompt.replace(/=== RECOMMENDED APPROACH[\s\S]*?(?=\n===|$)/, '');
        userPrompt = userPrompt.replace(/=== CROSS-DOMAIN STRATEGIES[\s\S]*?(?=\n===|$)/, '');
        automationLogger.debug('Trim stage 3: stripped memory blocks', {
          sessionId: this.currentSessionId,
          newTotal: systemPrompt.length + userPrompt.length
        });
      }

      automationLogger.info('Progressive trim complete', {
        sessionId: this.currentSessionId,
        trimStage,
        originalChars: totalChars,
        finalChars: systemPrompt.length + userPrompt.length,
        reduction: totalChars - (systemPrompt.length + userPrompt.length)
      });
    }
```

Do NOT modify the existing prompt assembly logic. This is a post-processing step only.
  </action>
  <verify>
    <automated>grep -c "PROMPT_CHAR_LIMIT" ai/ai-integration.js | grep -q "[2-9]" && echo "PASS: PROMPT_CHAR_LIMIT used" || echo "FAIL: PROMPT_CHAR_LIMIT not found enough times"</automated>
  </verify>
  <acceptance_criteria>
- grep "PROMPT_CHAR_LIMIT" ai/ai-integration.js returns at least 2 matches (constant definition + usage in if-check)
- grep "trimStage" ai/ai-integration.js returns at least 3 matches (stages 1, 2, 3)
- grep "Progressive trim" ai/ai-integration.js returns at least 1 match (the logging)
- grep "RECOMMENDED APPROACH" ai/ai-integration.js still returns matches in _buildTaskGuidance (the assembly is unchanged; only the trim removes it when over budget)
- The PROMPT_CHAR_LIMIT constant value is 200000
  </acceptance_criteria>
  <done>buildPrompt progressively trims in 3 stages (examples, element budget, memory) when prompt exceeds 200K chars</done>
</task>

<task type="auto">
  <name>Task 2: Add simplified prompt hint retry on CLI parse failure</name>
  <files>ai/ai-integration.js</files>
  <read_first>ai/ai-integration.js (lines 2260-2310 for the processQueue CLI parse and existing reformat retry, lines 695-772 of ai/cli-parser.js for parseCliResponse shape)</read_first>
  <action>
Enhance the CLI parse failure handling in the processQueue method of ai/ai-integration.js. Currently (lines 2274-2310), when parseCliResponse returns zero actions, it does a heavyweight "reformat retry" that echoes 500 chars of raw text back. Add a LIGHTER retry FIRST with a simplified format hint.

Find the block at line 2274:
```
// CLI reformat retry: if no valid output, ask AI to reformat
if (parsed.actions.length === 0 && !parsed.taskComplete && !parsed.taskFailed && !parsed.helpRequested) {
```

Replace the ENTIRE block (lines 2274-2310) with the following two-stage retry:

```
        // ROBUST-04: Two-stage CLI parse failure recovery
        // Stage 1: Lightweight simplified hint (cheap, fast)
        // Stage 2: Full reformat retry with raw text echo (existing logic, heavier)
        if (parsed.actions.length === 0 && !parsed.taskComplete && !parsed.taskFailed && !parsed.helpRequested) {
          automationLogger.debug('CLI parse failure - starting two-stage recovery', {
            sessionId: this.currentSessionId,
            rawTextPreview: rawText.substring(0, 200),
            errorCount: parsed.errors.length
          });

          // Stage 1: Simplified hint retry
          try {
            const hintPrompt = {
              messages: [
                ...(request.prompt.messages || [
                  { role: 'system', content: request.prompt.systemPrompt || '' },
                  { role: 'user', content: request.prompt.userPrompt || '' }
                ]),
                {
                  role: 'assistant',
                  content: rawText
                },
                {
                  role: 'user',
                  content: 'Format error. Respond with exactly one CLI command per line. Use # for reasoning. Example format:\n# reasoning here\nclick e5\ntype e3 "hello"\ndone "finished"'
                }
              ]
            };
            const hintResponse = await this.callAPI(hintPrompt, { attempt: (request.attempt || 0) + 1 });
            const hintRawText = typeof hintResponse === 'string' ? hintResponse : (hintResponse?.content || String(hintResponse));
            const hintParsed = parseCliResponse(hintRawText);
            hintParsed.actions = sanitizeActions(hintParsed.actions);

            if (hintParsed.actions.length > 0 || hintParsed.taskComplete || hintParsed.taskFailed) {
              automationLogger.debug('CLI simplified hint retry succeeded', {
                sessionId: this.currentSessionId,
                actionCount: hintParsed.actions.length,
                taskComplete: hintParsed.taskComplete
              });
              parsed = hintParsed;
              parsed._rawCliText = hintRawText;
              parsed._recoveryStage = 'simplified_hint';
            }
          } catch (hintError) {
            automationLogger.debug('CLI simplified hint retry failed', {
              sessionId: this.currentSessionId,
              error: hintError.message
            });
          }

          // Stage 2: Full reformat retry (existing logic) -- only if stage 1 did not recover
          if (parsed.actions.length === 0 && !parsed.taskComplete && !parsed.taskFailed && !parsed.helpRequested) {
            automationLogger.debug('CLI reformat retry - stage 2 with raw text echo', {
              sessionId: this.currentSessionId,
              rawTextPreview: rawText.substring(0, 200)
            });

            try {
              const reformatPrompt = {
                messages: [
                  ...(request.prompt.messages || [
                    { role: 'system', content: request.prompt.systemPrompt || '' },
                    { role: 'user', content: request.prompt.userPrompt || '' }
                  ]),
                  {
                    role: 'assistant',
                    content: rawText
                  },
                  {
                    role: 'user',
                    content: 'Your response was not in CLI command format. Reformat as CLI commands (one per line, # for reasoning). Your response was: ' + rawText.substring(0, 500)
                  }
                ]
              };
              const retryResponse = await this.callAPI(reformatPrompt, { attempt: (request.attempt || 0) + 1 });
              const retryRawText = typeof retryResponse === 'string' ? retryResponse : (retryResponse?.content || String(retryResponse));
              parsed = parseCliResponse(retryRawText);
              parsed.actions = sanitizeActions(parsed.actions);
              parsed._rawCliText = retryRawText;
              parsed._recoveryStage = 'full_reformat';
            } catch (retryError) {
              automationLogger.debug('CLI reformat retry failed', {
                sessionId: this.currentSessionId,
                error: retryError.message
              });
              // Keep original parsed result (empty actions)
            }
          }
        }
```

This preserves the existing reformat retry as stage 2 but adds a lighter stage 1 first. The simplified hint is more directive ("respond with exactly one CLI command per line") and includes a concrete example format, which is more likely to succeed than echoing back the raw text.
  </action>
  <verify>
    <automated>grep -c "exactly one CLI command per line" ai/ai-integration.js | grep -q "[1-9]" && echo "PASS: simplified hint found" || echo "FAIL: simplified hint not found"</automated>
  </verify>
  <acceptance_criteria>
- grep "exactly one CLI command per line" ai/ai-integration.js returns at least 1 match
- grep "simplified_hint" ai/ai-integration.js returns at least 1 match (stage 1 recovery tag)
- grep "full_reformat" ai/ai-integration.js returns at least 1 match (stage 2 recovery tag)
- grep "two-stage" ai/ai-integration.js returns at least 1 match (the debug log)
- grep "CLI reformat retry" ai/ai-integration.js still returns matches (existing stage 2 preserved)
- The stage 1 hint includes a concrete example format showing # reasoning, click, type, and done commands
  </acceptance_criteria>
  <done>CLI parse failure triggers lightweight simplified hint retry first, falling back to full reformat retry only if hint fails</done>
</task>

</tasks>

<verification>
1. ai/ai-integration.js: buildPrompt progressively trims prompt when over 200K chars -- examples first, then element budget, then memory
2. ai/ai-integration.js: CLI parse failure recovery has two stages -- lightweight hint first, full reformat second
3. No existing functionality is broken -- the original reformat retry is preserved as stage 2
</verification>

<success_criteria>
- A prompt with 250K+ chars triggers progressive trimming and logs the trim stages and character reduction
- A CLI parse failure first attempts a simplified hint retry with "respond with exactly one CLI command per line"
- If the simplified hint succeeds, the full reformat retry is skipped (visible in _recoveryStage tag)
- If the simplified hint fails, the existing reformat retry fires as fallback
</success_criteria>

<output>
After completion, create `.planning/phases/102-robustness-hardening/102-02-SUMMARY.md`
</output>
