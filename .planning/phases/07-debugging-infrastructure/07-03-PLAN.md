---
phase: 07-debugging-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - automation-logger.js
  - background.js
  - options.js
  - options.html
  - options.css
autonomous: true

must_haves:
  truths:
    - "Sessions can be replayed step-by-step showing action, targeting, and result"
    - "Logs can be exported in human-readable format with step details"
    - "Replay UI shows each step with selector, element found status, coordinates, and outcome"
  artifacts:
    - path: "automation-logger.js"
      provides: "getReplayData and exportHumanReadable methods"
      contains: "getReplayData"
    - path: "background.js"
      provides: "Message handlers for getSessionReplayData and exportSessionHumanReadable"
      contains: "getSessionReplayData"
    - path: "options.js"
      provides: "renderSessionReplay function"
      contains: "renderSessionReplay"
    - path: "options.html"
      provides: "Session replay UI container"
      contains: "session-replay"
  key_links:
    - from: "options.js renderSessionReplay"
      to: "background.js message handler"
      via: "chrome.runtime.sendMessage({ action: 'getSessionReplayData' })"
      pattern: "sendMessage.*getSessionReplayData"
    - from: "background.js getSessionReplayData handler"
      to: "automation-logger.js getReplayData"
      via: "automationLogger.getReplayData(sessionId)"
      pattern: "getReplayData\\(.*sessionId"
    - from: "options.html download button"
      to: "automation-logger.js exportHumanReadable"
      via: "background.js message handler"
      pattern: "exportSessionHumanReadable"
---

<objective>
Create session replay UI and human-readable export

Purpose: Users need to review completed automation sessions step-by-step to understand what happened and debug failures. The export should be human-readable text (not just JSON) showing each step with clear formatting.

Output: getReplayData() and exportHumanReadable() in automation-logger.js, message handlers in background.js, session replay UI in options panel
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-debugging-infrastructure/07-RESEARCH.md
@.planning/phases/07-debugging-infrastructure/07-01-SUMMARY.md (when available)

Prior work (relevant):
- Plan 01 added ActionRecorder and logActionRecord for structured action logging
- automation-logger.js already has formatSessionReport (basic) and exportSession
- options.html has session-detail-panel with session-logs-content pre element
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getReplayData method to AutomationLogger</name>
  <files>automation-logger.js</files>
  <action>
Add getReplayData method to AutomationLogger class, after getSessionActionRecords (added in Plan 01):

async getReplayData(sessionId):
- Load session: const session = await this.loadSession(sessionId)
- If !session, return null
- Get action records: const actionRecords = this.getSessionActionRecords(sessionId)
- Build replay structure:
  {
    version: '1.0',
    id: sessionId,
    metadata: {
      task: session.task,
      startTime: session.startTime,
      endTime: session.endTime,
      status: session.status,
      url: session.logs?.[0]?.data?.url || 'Unknown',
      actionCount: actionRecords.length
    },
    steps: actionRecords.map((record, index) => ({
      stepNumber: index + 1,
      timestamp: record.timestamp,
      action: {
        tool: record.tool,
        params: record.params || {}
      },
      targeting: {
        selectorTried: record.selectorTried,
        selectorUsed: record.selectorUsed,
        elementFound: record.elementFound,
        elementDetails: record.elementDetails,
        coordinatesUsed: record.coordinatesUsed,
        coordinateSource: record.coordinateSource
      },
      result: {
        success: record.success,
        error: record.error,
        hadEffect: record.hadEffect,
        diagnostic: record.diagnostic
      },
      duration: record.duration
    })),
    summary: {
      totalSteps: actionRecords.length,
      successfulSteps: actionRecords.filter(r => r.success).length,
      failedSteps: actionRecords.filter(r => !r.success).length,
      totalDuration: session.endTime - session.startTime
    }
  }
- Return the replay object

This transforms raw action records into a step-by-step replay format.
  </action>
  <verify>grep -n "getReplayData\|steps.*map\|targeting" automation-logger.js | head -15</verify>
  <done>getReplayData returns structured replay object with steps array containing action, targeting, and result</done>
</task>

<task type="auto">
  <name>Task 2: Add exportHumanReadable method to AutomationLogger</name>
  <files>automation-logger.js</files>
  <action>
Add exportHumanReadable method to AutomationLogger class, after getReplayData:

async exportHumanReadable(sessionId):
- Get replay data: const replay = await this.getReplayData(sessionId)
- If !replay, return 'Session not found.'
- Build lines array:

const lines = [];
const divider = '='.repeat(80);

// Header
lines.push(divider);
lines.push('FSB AUTOMATION SESSION REPORT');
lines.push(divider);
lines.push('');
lines.push(`Session ID: ${replay.id}`);
lines.push(`Task: ${replay.metadata.task}`);
lines.push(`Status: ${replay.metadata.status}`);
lines.push(`Duration: ${this.formatDuration(replay.summary.totalDuration)}`);
lines.push(`Steps: ${replay.summary.successfulSteps}/${replay.summary.totalSteps} successful`);
lines.push('');

// Step-by-step
lines.push(divider);
lines.push('STEP-BY-STEP EXECUTION');
lines.push(divider);
lines.push('');

replay.steps.forEach(step => {
  const status = step.result.success ? '[OK]' : '[FAILED]';
  lines.push(`${status} Step ${step.stepNumber}: ${step.action.tool}`);
  lines.push(`    Selector: ${step.targeting.selectorUsed || step.targeting.selectorTried || 'N/A'}`);
  lines.push(`    Element Found: ${step.targeting.elementFound ? 'Yes' : 'No'}`);

  if (step.targeting.elementDetails) {
    const el = step.targeting.elementDetails;
    lines.push(`    Element: <${el.tagName}${el.id ? '#'+el.id : ''}${el.className ? '.'+el.className.split(' ')[0] : ''}>`);
  }

  if (step.targeting.coordinatesUsed) {
    lines.push(`    Coordinates: (${step.targeting.coordinatesUsed.x}, ${step.targeting.coordinatesUsed.y})`);
  }

  if (!step.result.success && step.result.diagnostic) {
    lines.push(`    Error: ${step.result.diagnostic.message}`);
    lines.push(`    Details: ${step.result.diagnostic.details}`);
    if (step.result.diagnostic.suggestions?.length) {
      lines.push('    Suggestions:');
      step.result.diagnostic.suggestions.forEach(s => lines.push(`      - ${s}`));
    }
  }

  lines.push('');
});

// Failure summary if any failures
const failures = replay.steps.filter(s => !s.result.success);
if (failures.length > 0) {
  lines.push(divider);
  lines.push('FAILURE SUMMARY');
  lines.push(divider);
  lines.push('');
  failures.forEach(step => {
    lines.push(`Step ${step.stepNumber}: ${step.result.diagnostic?.message || step.result.error || 'Unknown error'}`);
  });
}

lines.push('');
lines.push(divider);
lines.push(`Report generated: ${new Date().toLocaleString()}`);

return lines.join('\n');
  </action>
  <verify>grep -n "exportHumanReadable\|STEP-BY-STEP\|FAILURE SUMMARY" automation-logger.js | head -10</verify>
  <done>exportHumanReadable returns formatted text report with step-by-step execution and failure summary</done>
</task>

<task type="auto">
  <name>Task 3: Add message handlers and session replay UI</name>
  <files>background.js, options.html, options.css, options.js</files>
  <action>
UPDATE background.js - Add message handlers for replay data retrieval.

In the existing chrome.runtime.onMessage.addListener handler (or create one if needed), add cases for:

```javascript
case 'getSessionReplayData': {
  const replay = await automationLogger.getReplayData(request.sessionId);
  sendResponse({ replay });
  return true; // async response
}

case 'exportSessionHumanReadable': {
  const text = await automationLogger.exportHumanReadable(request.sessionId);
  sendResponse({ text });
  return true; // async response
}
```

These handlers bridge options.js requests to automationLogger methods running in the background script.

UPDATE options.html - Replace the session-detail-panel content (around lines 481-504) with replay-capable structure:

Inside the session-detail-panel div, after session-detail-meta, replace session-logs-container with:

<div class="session-replay-container" id="sessionReplayContainer">
  <div class="replay-controls">
    <button class="replay-btn" id="prevStep" disabled>
      <i class="fas fa-chevron-left"></i> Prev
    </button>
    <span class="replay-step-indicator">
      Step <span id="currentStepNum">0</span> of <span id="totalSteps">0</span>
    </span>
    <button class="replay-btn" id="nextStep" disabled>
      Next <i class="fas fa-chevron-right"></i>
    </button>
  </div>

  <div class="replay-step-content" id="replayStepContent">
    <!-- Step details rendered here -->
    <div class="step-placeholder">Select a session to view replay</div>
  </div>

  <div class="replay-summary" id="replaySummary" style="display: none;">
    <!-- Summary rendered here -->
  </div>
</div>

UPDATE the download button (id="downloadSessionLogs") to also offer human-readable export:
- Change button text to "Export" with dropdown or add second button "Export Text"

UPDATE options.css - Add styles for replay UI:

.session-replay-container { padding: 1rem; }
.replay-controls { display: flex; align-items: center; justify-content: center; gap: 1rem; margin-bottom: 1rem; }
.replay-btn { padding: 0.5rem 1rem; border: 1px solid var(--border-color); border-radius: 4px; background: var(--bg-secondary); cursor: pointer; }
.replay-btn:disabled { opacity: 0.5; cursor: not-allowed; }
.replay-step-indicator { font-weight: 500; }
.replay-step-content { background: var(--bg-secondary); border-radius: 8px; padding: 1rem; min-height: 200px; }
.step-status { display: inline-block; padding: 2px 8px; border-radius: 4px; font-size: 12px; font-weight: 600; }
.step-status.success { background: #d4edda; color: #155724; }
.step-status.failed { background: #f8d7da; color: #721c24; }
.step-section { margin-top: 1rem; }
.step-section h5 { margin: 0 0 0.5rem 0; font-size: 13px; color: var(--text-secondary); }
.step-detail { font-family: monospace; font-size: 12px; margin: 4px 0; }

UPDATE options.js - Add renderSessionReplay function and wire up controls:

let currentReplayData = null;
let currentStepIndex = 0;

async function renderSessionReplay(sessionId) {
  // Get replay data from background (automationLogger runs there)
  const response = await chrome.runtime.sendMessage({
    action: 'getSessionReplayData',
    sessionId
  });

  if (!response?.replay) {
    document.getElementById('replayStepContent').innerHTML = '<div class="step-placeholder">No replay data available</div>';
    return;
  }

  currentReplayData = response.replay;
  currentStepIndex = 0;

  // Update controls
  document.getElementById('totalSteps').textContent = currentReplayData.steps.length;
  document.getElementById('prevStep').disabled = true;
  document.getElementById('nextStep').disabled = currentReplayData.steps.length <= 1;

  // Render first step
  renderStep(0);

  // Show summary
  renderReplaySummary();
}

function renderStep(index) {
  const step = currentReplayData.steps[index];
  if (!step) return;

  currentStepIndex = index;
  document.getElementById('currentStepNum').textContent = index + 1;
  document.getElementById('prevStep').disabled = index === 0;
  document.getElementById('nextStep').disabled = index === currentReplayData.steps.length - 1;

  const statusClass = step.result.success ? 'success' : 'failed';
  const statusText = step.result.success ? 'OK' : 'FAILED';

  let html = `
    <div class="step-header">
      <span class="step-status ${statusClass}">${statusText}</span>
      <strong>${step.action.tool}</strong>
    </div>
    <div class="step-section">
      <h5>Targeting</h5>
      <div class="step-detail">Selector: ${step.targeting.selectorUsed || step.targeting.selectorTried || 'N/A'}</div>
      <div class="step-detail">Element Found: ${step.targeting.elementFound ? 'Yes' : 'No'}</div>
      ${step.targeting.coordinatesUsed ? `<div class="step-detail">Coordinates: (${step.targeting.coordinatesUsed.x}, ${step.targeting.coordinatesUsed.y})</div>` : ''}
    </div>
  `;

  if (step.targeting.elementDetails) {
    const el = step.targeting.elementDetails;
    html += `
      <div class="step-section">
        <h5>Element</h5>
        <div class="step-detail">&lt;${el.tagName}${el.id ? ' id="'+el.id+'"' : ''}${el.className ? ' class="'+el.className.split(' ')[0]+'"' : ''}&gt;</div>
        ${el.text ? `<div class="step-detail">Text: "${el.text.substring(0, 50)}${el.text.length > 50 ? '...' : ''}"</div>` : ''}
      </div>
    `;
  }

  if (!step.result.success && step.result.diagnostic) {
    html += `
      <div class="step-section">
        <h5>Diagnostic</h5>
        <div class="step-detail" style="color: #dc3545;">${step.result.diagnostic.message}</div>
        <div class="step-detail">${step.result.diagnostic.details}</div>
        ${step.result.diagnostic.suggestions ? `<div class="step-detail">Suggestions:<br>${step.result.diagnostic.suggestions.map(s => '  - ' + s).join('<br>')}</div>` : ''}
      </div>
    `;
  }

  document.getElementById('replayStepContent').innerHTML = html;
}

function renderReplaySummary() {
  const summary = currentReplayData.summary;
  document.getElementById('replaySummary').innerHTML = `
    <div style="margin-top: 1rem; padding: 0.75rem; background: var(--bg-primary); border-radius: 4px;">
      <strong>Summary:</strong> ${summary.successfulSteps}/${summary.totalSteps} steps successful
      ${summary.failedSteps > 0 ? ` | <span style="color: #dc3545;">${summary.failedSteps} failed</span>` : ''}
    </div>
  `;
  document.getElementById('replaySummary').style.display = 'block';
}

// Wire up controls
document.getElementById('prevStep')?.addEventListener('click', () => renderStep(currentStepIndex - 1));
document.getElementById('nextStep')?.addEventListener('click', () => renderStep(currentStepIndex + 1));

// Update session click handler to call renderSessionReplay
// In existing showSessionDetail function, add: renderSessionReplay(sessionId);

// Add export text handler
document.getElementById('downloadSessionLogs')?.addEventListener('click', async () => {
  const sessionId = document.getElementById('sessionDetailPanel').dataset.sessionId;
  const response = await chrome.runtime.sendMessage({
    action: 'exportSessionHumanReadable',
    sessionId
  });

  if (response?.text) {
    const blob = new Blob([response.text], { type: 'text/plain' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `fsb-session-${sessionId.substring(0, 8)}.txt`;
    a.click();
    URL.revokeObjectURL(url);
  }
});
  </action>
  <verify>
grep -n "getSessionReplayData\|exportSessionHumanReadable" background.js | head -5
grep -n "session-replay-container\|renderSessionReplay\|replay-controls" options.html options.js options.css 2>/dev/null | head -15
  </verify>
  <done>Background.js has message handlers, session replay UI shows step-by-step with prev/next controls, export downloads human-readable text</done>
</task>

</tasks>

<verification>
Run these commands to verify the implementation:

1. Check getReplayData method:
   grep -A10 "getReplayData" automation-logger.js | head -15

2. Check exportHumanReadable method:
   grep -A5 "exportHumanReadable" automation-logger.js | head -10

3. Check background.js message handlers:
   grep -n "getSessionReplayData\|exportSessionHumanReadable" background.js

4. Check replay UI in HTML:
   grep -n "replay-controls\|session-replay" options.html

5. Check renderSessionReplay in JS:
   grep -n "renderSessionReplay\|renderStep" options.js | head -10

6. Verify no syntax errors:
   node --check automation-logger.js 2>&1 | head -5
   node --check background.js 2>&1 | head -5
</verification>

<success_criteria>
- getReplayData returns structured replay object with steps, targeting, and results
- exportHumanReadable returns formatted text report with step details and failure summary
- background.js has message handlers for 'getSessionReplayData' and 'exportSessionHumanReadable'
- Replay UI shows step content with selector, element found, coordinates, and diagnostic
- Prev/Next buttons navigate between steps
- Export button downloads human-readable .txt file
</success_criteria>

<output>
After completion, create `.planning/phases/07-debugging-infrastructure/07-03-SUMMARY.md`
</output>
