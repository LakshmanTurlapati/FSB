---
phase: 198-mcp-bridge-lifecycle-reconnect-state
plan: "01"
subsystem: testing
tags: [mcp, bridge, lifecycle, topology, websocket, red-tests]

requires: []
provides:
  - Wave 0 RED test contract for extension MCP bridge lifecycle state and wake arming.
  - Wave 0 RED test contract for MCP hub/relay topology and extension reachability.
affects: [198-02, 198-03, BRIDGE-01, BRIDGE-02, BRIDGE-03, BRIDGE-04]

tech-stack:
  added: []
  patterns: [plain-node-assertions, vm-extension-harness, built-js-import, real-ws-topology-test]

key-files:
  created:
    - tests/mcp-bridge-client-lifecycle.test.js
    - tests/mcp-bridge-topology.test.js
  modified: []

key-decisions:
  - "Plan 198-01 remains RED-only; production behavior is intentionally left for Plans 198-02 and 198-03."
  - "Topology coverage imports mcp-server/build/bridge.js after build so later fixes are verified against package output."

patterns-established:
  - "Extension lifecycle tests run ws/mcp-bridge-client.js in a VM with fake Chrome storage, alarms, timers, and WebSocket."
  - "Topology tests allocate a free loopback port and require deterministic WebSocketBridge options for hub/relay behavior."

requirements-completed: [BRIDGE-01, BRIDGE-02, BRIDGE-03, BRIDGE-04]

duration: 5min
completed: 2026-04-22
---

# Phase 198 Plan 01: Wave 0 Bridge Lifecycle RED Tests Summary

**RED bridge lifecycle and hub/relay topology contracts for Phase 198 reconnect repairs**

## Performance

- **Duration:** 5 min
- **Started:** 2026-04-22T16:51:13Z
- **Completed:** 2026-04-22T16:55:31Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added a VM-based client lifecycle test for browser-first reconnect, session-only state, service-worker wake recording, connected transition state, and `armMcpBridge` wake-path wiring.
- Added a built-server topology test for server-first hub/relay mode, relay reachability separation, extension state broadcast, and hub-exit recovery.
- Preserved Wave 0 behavior: both tests fail against current production code and are ready to drive Plans 198-02 and 198-03.

## Task Commits

Each task was committed atomically:

1. **Task 1: Wave 0 client lifecycle test contract** - `94cac7c` (test)
2. **Task 2: Wave 0 hub/relay topology test contract** - `240a649` (test)

## Files Created/Modified

- `tests/mcp-bridge-client-lifecycle.test.js` - Plain Node VM harness with fake `chrome.storage.session`, `chrome.storage.local`, `chrome.alarms`, fake timers, fake WebSocket, and background source assertions.
- `tests/mcp-bridge-topology.test.js` - Plain Node topology harness that builds/imports the MCP bridge, allocates a free port, uses real `ws` extension sockets, and bounds hub/relay waits.

## Decisions Made

- Kept the plan strictly RED-only because the production lifecycle and topology repairs belong to Plans 198-02 and 198-03.
- Used `bash` for the plan verification wrappers because the plan snippet assigns to `status`, which is read-only in the repository's zsh shell.
- Restored tracked build artifacts regenerated by `npm --prefix mcp-server run build`; the plan's deliverables are test contracts only.

## Verification

- `bash -lc 'node tests/mcp-bridge-client-lifecycle.test.js >/tmp/phase198-client-red.log 2>&1; status=$?; test "$status" -ne 0 && rg "FAIL|mcpBridgeState|fsb-mcp-bridge-reconnect|armMcpBridge" /tmp/phase198-client-red.log'` - PASS. The wrapper exits 0 because the new client lifecycle test fails as expected.
- `bash -lc 'npm --prefix mcp-server run build >/tmp/phase198-build.log 2>&1 && node tests/mcp-bridge-topology.test.js >/tmp/phase198-topology-red.log 2>&1; status=$?; test "$status" -ne 0 && rg "FAIL|topology|extensionConnected|hubConnected|hub-exit-promotion" /tmp/phase198-topology-red.log'` - PASS. The build succeeds and the topology test fails as expected.
- `node tests/mcp-bridge-client-lifecycle.test.js` - expected RED failure, exit 1, with 9 passing assertions and 20 failing assertions against missing lifecycle APIs.
- `npm --prefix mcp-server run build && node tests/mcp-bridge-topology.test.js` - expected RED failure, exit 1, with 3 passing assertions and 11 failing assertions against missing topology APIs.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The plan's shell snippets use `status=$?`; zsh treats `status` as read-only. Verification was run through `bash -lc` so the command semantics matched the plan.
- `npm --prefix mcp-server run build` regenerated tracked build artifacts during verification. Those generated changes were restored after each run because Plan 198-01 only commits RED tests.

## User Setup Required

None - no external service configuration required.

## Known Stubs

None. Stub scan only matched normal test harness initializers such as empty arrays/maps and default helper arguments.

## Next Phase Readiness

Plan 198-02 can implement `MCP_BRIDGE_STATE_KEY`, `MCP_RECONNECT_ALARM`, `recordWake()`, `getState()`, session persistence, reconnect alarms, and `armMcpBridge(reason)` wiring until `tests/mcp-bridge-client-lifecycle.test.js` passes.

Plan 198-03 can implement `WebSocketBridge` options, explicit topology state, hub/relay reachability separation, topology broadcasts, and hub-exit recovery until `tests/mcp-bridge-topology.test.js` passes.

## Self-Check: PASSED

- Found `tests/mcp-bridge-client-lifecycle.test.js`
- Found `tests/mcp-bridge-topology.test.js`
- Found `.planning/phases/198-mcp-bridge-lifecycle-reconnect-state/198-01-SUMMARY.md`
- Found task commit `94cac7c`
- Found task commit `240a649`

---
*Phase: 198-mcp-bridge-lifecycle-reconnect-state*
*Completed: 2026-04-22*
