---
phase: 198-mcp-bridge-lifecycle-reconnect-state
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "tests/mcp-bridge-client-lifecycle.test.js"
  - "tests/mcp-bridge-topology.test.js"
autonomous: true
requirements:
  - BRIDGE-01
  - BRIDGE-02
  - BRIDGE-03
  - BRIDGE-04
must_haves:
  truths:
    - "Automated tests exist before implementation for browser-first, server-first, service-worker wake, and hub-exit recovery."
    - "Client lifecycle tests fail before bridge state/reconnect implementation and pass after Plan 02."
    - "Topology tests fail before hub/relay state implementation and pass after Plan 03."
  artifacts:
    - path: "tests/mcp-bridge-client-lifecycle.test.js"
      provides: "Wave 0 client lifecycle coverage for BRIDGE-01 and BRIDGE-03"
      contains: "mcpBridgeState"
    - path: "tests/mcp-bridge-topology.test.js"
      provides: "Wave 0 hub/relay topology coverage for BRIDGE-02 and BRIDGE-04"
      contains: "hub-exit-promotion"
  key_links:
    - from: "tests/mcp-bridge-client-lifecycle.test.js"
      to: "ws/mcp-bridge-client.js"
      via: "vm.runInNewContext lifecycle harness"
      pattern: "vm\\.runInNewContext"
    - from: "tests/mcp-bridge-client-lifecycle.test.js"
      to: "background.js"
      via: "source assertions for armMcpBridge wake paths"
      pattern: "armMcpBridge"
    - from: "tests/mcp-bridge-topology.test.js"
      to: "mcp-server/build/bridge.js"
      via: "dynamic import after npm --prefix mcp-server run build"
      pattern: "WebSocketBridge"
---

<objective>
Create Phase 198 Wave 0 automated tests before production implementation.

Purpose: The phase must repair reconnect lifecycle behavior, not merely add source strings. These tests define the observable browser-first, server-first, service-worker wake, and multi-host hub/relay contracts before Plans 02 and 03 change production code.
Output: Two plain Node.js test scripts that initially fail against the current code and become the focused Phase 198 verification suite after implementation.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/198-mcp-bridge-lifecycle-reconnect-state/198-RESEARCH.md
@.planning/phases/198-mcp-bridge-lifecycle-reconnect-state/198-PATTERNS.md
@.planning/phases/198-mcp-bridge-lifecycle-reconnect-state/198-VALIDATION.md
@ws/mcp-bridge-client.js
@background.js
@mcp-server/src/bridge.ts
@mcp-server/src/types.ts
@tests/secure-config-credential-vault.test.js
@tests/test-agent-scheduler-cron.js

<interfaces>
Current extension lifecycle constants from `ws/mcp-bridge-client.js`:
```javascript
const MCP_BRIDGE_URL = 'ws://localhost:7225';
const MCP_RECONNECT_BASE_MS = 2000;
const MCP_RECONNECT_MAX_MS = 30000;
const MCP_PING_INTERVAL_MS = 25000;
class MCPBridgeClient { connect(); disconnect(); get isConnected(); }
const mcpBridgeClient = new MCPBridgeClient();
```

Required Phase 198 client contract the tests should drive:
```javascript
const MCP_BRIDGE_STATE_KEY = 'mcpBridgeState';
const MCP_RECONNECT_ALARM = 'fsb-mcp-bridge-reconnect';
mcpBridgeClient.recordWake(reason);
mcpBridgeClient.getState();
function armMcpBridge(reason);
```

Current server bridge API from `mcp-server/src/bridge.ts`:
```typescript
export class WebSocketBridge {
  constructor();
  connect(): Promise<void>;
  disconnect(): void;
  sendAndWait(msg, options?): Promise<Record<string, unknown>>;
  get isConnected(): boolean;
  get currentMode(): 'hub' | 'relay' | 'disconnected';
}
```

Required Phase 198 server contract the tests should drive:
```typescript
new WebSocketBridge({ port, host, instanceId, handshakeTimeoutMs, promotionJitterMs, maxReconnectDelayMs });
bridge.topology.extensionConnected;
bridge.topology.hubConnected;
bridge.topology.relayCount;
bridge.topology.activeHubInstanceId;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 client lifecycle test contract</name>
  <files>tests/mcp-bridge-client-lifecycle.test.js</files>
  <read_first>
    - tests/mcp-bridge-client-lifecycle.test.js (create this file)
    - .planning/phases/198-mcp-bridge-lifecycle-reconnect-state/198-VALIDATION.md
    - .planning/phases/198-mcp-bridge-lifecycle-reconnect-state/198-RESEARCH.md
    - .planning/phases/198-mcp-bridge-lifecycle-reconnect-state/198-PATTERNS.md
    - ws/mcp-bridge-client.js
    - background.js
    - tests/secure-config-credential-vault.test.js
    - tests/test-agent-scheduler-cron.js
  </read_first>
  <behavior>
    - Browser-first: with no MCP server available, `mcpBridgeClient.connect()` records a reconnect attempt, persists `mcpBridgeState` to `chrome.storage.session`, and schedules `fsb-mcp-bridge-reconnect` with a delay no greater than 30 seconds.
    - Service-worker wake: `recordWake('runtime.onMessage')` records `lastWakeReason: 'runtime.onMessage'` and updates `wakeCount` without writing to `chrome.storage.local`.
    - Connected transition: a fake WebSocket `onopen` changes state to `status: 'connected'`, resets reconnect delay to `2000`, clears the reconnect alarm, and records `lastConnectedAt`.
    - Background arming: `background.js` contains `function armMcpBridge(reason)` and calls it for `service-worker-evaluated`, `runtime.onInstalled`, `runtime.onStartup`, `runtime.onMessage`, `runtime.onConnect`, `webNavigation.onCommitted`, `action.onClicked`, and `alarm:fsb-mcp-bridge-reconnect`.
  </behavior>
  <action>
    Create a plain Node.js assertion script matching the existing `tests/*.test.js` style: `const fs = require('fs')`, `const path = require('path')`, `const vm = require('vm')`, local `assert()`/`assertEqual()` counters, and `process.exit(failed > 0 ? 1 : 0)`.

    Build a deterministic VM harness for `ws/mcp-bridge-client.js` with:
    - Fake `chrome.storage.session` and `chrome.storage.local` areas using an in-memory `createStorageArea()` helper and `_dump()` method.
    - Fake `chrome.alarms.create`, `chrome.alarms.clear`, and `chrome.alarms.getAll` that record alarm names/options.
    - Fake `WebSocket` with `OPEN = 1`, `CONNECTING = 0`, configurable constructor behavior (`throwOnConstruct`, manual `open()`, `close()`), and an array of sent payloads.
    - Fake timers that capture scheduled delays without waiting.
    - A source footer appended to the VM execution that exposes `MCPBridgeClient`, `mcpBridgeClient`, `MCP_BRIDGE_STATE_KEY`, and `MCP_RECONNECT_ALARM` through `this.__phase198`.

    Add explicit assertions for these exact constants and values:
    - `MCP_BRIDGE_STATE_KEY === 'mcpBridgeState'`
    - `MCP_RECONNECT_ALARM === 'fsb-mcp-bridge-reconnect'`
    - `MCP_RECONNECT_MAX_MS === 30000` or `getState().maxReconnectDelayMs === 30000`
    - persisted state is under `chrome.storage.session._dump().mcpBridgeState`
    - `chrome.storage.local._dump().mcpBridgeState` is `undefined`
    - reconnect alarm options include `{ delayInMinutes: 0.5 }` for the alarm backstop

    Add a background source section that reads `background.js` and asserts exact wake-path strings:
    - `function armMcpBridge(reason)`
    - `armMcpBridge('service-worker-evaluated')`
    - `armMcpBridge('runtime.onInstalled')`
    - `armMcpBridge('runtime.onStartup')`
    - `armMcpBridge('runtime.onMessage')`
    - `armMcpBridge('runtime.onConnect')`
    - `armMcpBridge('webNavigation.onCommitted')`
    - `armMcpBridge('action.onClicked')`
    - `alarm.name === MCP_RECONNECT_ALARM`
    - `armMcpBridge('alarm:' + MCP_RECONNECT_ALARM)`

    Keep this test scoped to bridge lifecycle state and wake arming. Do not add route-contract assertions for `navigate`, `run_task`, `get_logs`, or other Phase 199 tool-routing behavior.
  </action>
  <verify>
    <automated>node tests/mcp-bridge-client-lifecycle.test.js >/tmp/phase198-client-red.log 2>&1; status=$?; test "$status" -ne 0 && rg "FAIL|mcpBridgeState|fsb-mcp-bridge-reconnect|armMcpBridge" /tmp/phase198-client-red.log</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/mcp-bridge-client-lifecycle.test.js` succeeds.
    - `rg "mcpBridgeState" tests/mcp-bridge-client-lifecycle.test.js` returns at least one match.
    - `rg "fsb-mcp-bridge-reconnect" tests/mcp-bridge-client-lifecycle.test.js` returns at least one match.
    - `rg "chrome\\.storage\\.session" tests/mcp-bridge-client-lifecycle.test.js` returns at least one match.
    - `rg "chrome\\.storage\\.local\\._dump\\(\\)\\.mcpBridgeState" tests/mcp-bridge-client-lifecycle.test.js` returns at least one match.
    - `rg "delayInMinutes: 0\\.5" tests/mcp-bridge-client-lifecycle.test.js` returns at least one match.
    - `rg "armMcpBridge\\('runtime\\.onMessage'\\)" tests/mcp-bridge-client-lifecycle.test.js` returns at least one match.
    - The RED verification command exits 0 because the new test currently fails against missing Phase 198 implementation.
  </acceptance_criteria>
  <done>The client lifecycle test file exists, encodes BRIDGE-01/BRIDGE-03 behavior, and fails for the expected missing lifecycle APIs before Plan 02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 hub/relay topology test contract</name>
  <files>tests/mcp-bridge-topology.test.js</files>
  <read_first>
    - tests/mcp-bridge-topology.test.js (create this file)
    - .planning/phases/198-mcp-bridge-lifecycle-reconnect-state/198-VALIDATION.md
    - .planning/phases/198-mcp-bridge-lifecycle-reconnect-state/198-RESEARCH.md
    - .planning/phases/198-mcp-bridge-lifecycle-reconnect-state/198-PATTERNS.md
    - mcp-server/src/bridge.ts
    - mcp-server/src/types.ts
    - mcp-server/src/diagnostics.ts
    - tests/mcp-restricted-tab.test.js
  </read_first>
  <behavior>
    - Server-first: the first `WebSocketBridge` instance binds a test port as `hub`, a second instance becomes `relay`, and the relay reports `hubConnected: true` but `extensionConnected: false` until an extension socket connects.
    - Bounded attach: when a fake extension WebSocket connects to the hub, hub and relay topology both report `extensionConnected: true` within a 30-second bounded window; the test should use short injected timeouts so it finishes quickly.
    - Hub/relay separation: relay `isConnected` is false after `relay:welcome` if the hub has no extension client.
    - Hub-exit recovery: when the hub exits, a relay promotes or reconnects cleanly, reports a stable `currentMode`, and does not permanently orphan pending topology state.
  </behavior>
  <action>
    Create a plain Node.js assertion script matching existing test style. The script must run after `npm --prefix mcp-server run build` and dynamically import `../mcp-server/build/bridge.js`.

    Include helpers:
    - `getFreePort()` using `node:net` to bind port `0` on `127.0.0.1`, read the assigned port, then close the probe server.
    - `sleep(ms)` and `waitFor(predicate, label, timeoutMs = 1000, intervalMs = 10)` for bounded polling.
    - `createExtensionSocket(port)` using the existing `ws` package to connect to `ws://127.0.0.1:${port}` and wait until `open`.
    - Cleanup that calls `disconnect()` on all `WebSocketBridge` instances and `close()` on all extension sockets in `finally`.

    Add named cases printed to stdout:
    - `server-first topology`
    - `relay waits for extension reachability`
    - `extension state broadcasts to relays`
    - `hub-exit-promotion`

    Instantiate bridges with exact deterministic options:
    ```javascript
    const hub = new WebSocketBridge({
      port,
      host: '127.0.0.1',
      instanceId: 'test-hub',
      handshakeTimeoutMs: 25,
      promotionJitterMs: 1,
      maxReconnectDelayMs: 100
    });
    const relay = new WebSocketBridge({
      port,
      host: '127.0.0.1',
      instanceId: 'test-relay',
      handshakeTimeoutMs: 25,
      promotionJitterMs: 1,
      maxReconnectDelayMs: 100
    });
    ```

    Assert these concrete states:
    - `hub.currentMode === 'hub'`
    - `relay.currentMode === 'relay'`
    - `hub.topology.instanceId === 'test-hub'`
    - `relay.topology.instanceId === 'test-relay'`
    - `hub.topology.extensionConnected === false` before the extension socket connects
    - `relay.topology.hubConnected === true` after relay handshake
    - `relay.isConnected === false` after `relay:welcome` and before extension attach
    - `relay.topology.activeHubInstanceId === 'test-hub'`
    - hub and relay both show `extensionConnected === true` after the extension socket connects and the 25ms handshake timeout elapses
    - relay eventually becomes `hub` or reconnects as `relay` after `hub.disconnect()`; the assertion must be bounded and must not wait more than 1000ms.

    Do not add tests for MCP tool route names or background self-dispatch; Phase 199 owns route-contract fixes.
  </action>
  <verify>
    <automated>npm --prefix mcp-server run build >/tmp/phase198-build.log 2>&1 && node tests/mcp-bridge-topology.test.js >/tmp/phase198-topology-red.log 2>&1; status=$?; test "$status" -ne 0 && rg "FAIL|topology|extensionConnected|hubConnected|hub-exit-promotion" /tmp/phase198-topology-red.log</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/mcp-bridge-topology.test.js` succeeds.
    - `rg "getFreePort" tests/mcp-bridge-topology.test.js` returns at least one match.
    - `rg "server-first topology" tests/mcp-bridge-topology.test.js` returns at least one match.
    - `rg "relay waits for extension reachability" tests/mcp-bridge-topology.test.js` returns at least one match.
    - `rg "hub-exit-promotion" tests/mcp-bridge-topology.test.js` returns at least one match.
    - `rg "instanceId: 'test-hub'" tests/mcp-bridge-topology.test.js` returns at least one match.
    - `rg "extensionConnected === false" tests/mcp-bridge-topology.test.js` returns at least one match.
    - `rg "relay\\.isConnected === false" tests/mcp-bridge-topology.test.js` returns at least one match.
    - The RED verification command exits 0 because the new test currently fails against missing Phase 198 topology APIs.
  </acceptance_criteria>
  <done>The topology test file exists, encodes BRIDGE-02/BRIDGE-04 behavior, and fails for the expected missing constructor/topology/state APIs before Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness -> extension VM | Fake Chrome/WebSocket APIs drive extension bridge code without real browser privileges. |
| Local process -> MCP bridge | Tests instantiate local WebSocket clients that may look like relay or extension peers. |
| Test output -> persisted artifacts | Tests must not print or persist credential/payment payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-198-01 | Information Disclosure | `tests/mcp-bridge-client-lifecycle.test.js` | mitigate | Assert bridge lifecycle state is written only to `chrome.storage.session.mcpBridgeState` and never includes payload fields such as `password`, `cardNumber`, `cvv`, or `apiKey`. |
| T-198-02 | Spoofing | `tests/mcp-bridge-topology.test.js` | mitigate | Require relay identity through `instanceId` and verify extension reachability is not inferred from `relay:welcome`. |
| T-198-03 | Denial of Service | fake reconnect timers/alarms | mitigate | Assert reconnect scheduling is bounded by `30000` ms and uses the named alarm `fsb-mcp-bridge-reconnect` rather than unbounded timer creation. |
| T-198-04 | Tampering | topology state contract | mitigate | Assert `hubConnected` and `extensionConnected` are separate fields so relay handshake cannot tamper with extension readiness. |
</threat_model>

<verification>
Plan-local RED checks:
- `node tests/mcp-bridge-client-lifecycle.test.js` must fail before Plan 02 and pass after Plan 02.
- `npm --prefix mcp-server run build && node tests/mcp-bridge-topology.test.js` must fail before Plan 03 and pass after Plan 03.

Final Phase 198 quick verification after Plans 02 and 03:
```bash
npm --prefix mcp-server run build && node tests/mcp-bridge-client-lifecycle.test.js && node tests/mcp-bridge-topology.test.js
```

Do not make `npm test` a Phase 198 gate; unrelated tests are known not clean.
</verification>

<success_criteria>
- `tests/mcp-bridge-client-lifecycle.test.js` exists and covers BRIDGE-01 and BRIDGE-03.
- `tests/mcp-bridge-topology.test.js` exists and covers BRIDGE-02 and BRIDGE-04.
- Both tests are behavioral enough to fail against the current missing lifecycle/topology APIs.
- No Phase 199 tool-routing assertions are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/198-mcp-bridge-lifecycle-reconnect-state/198-01-SUMMARY.md`.
</output>
