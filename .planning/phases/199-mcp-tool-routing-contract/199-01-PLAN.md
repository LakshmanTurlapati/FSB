---
phase: 199-mcp-tool-routing-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "tests/mcp-tool-routing-contract.test.js"
  - "tests/mcp-restricted-tab.test.js"
  - "package.json"
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
  - ROUTE-04
must_haves:
  truths:
    - "MCP route tests execute actual dispatcher code rather than checking source strings."
    - "Tests fail when a public MCP browser, tab, autopilot, observability, session, memory, or read-only route lacks a direct route contract."
    - "Restricted-tab tests prove blank/new-tab and browser-internal pages advertise only navigation/tab recovery tools."
  artifacts:
    - path: "tests/mcp-tool-routing-contract.test.js"
      provides: "Executable route-contract coverage for ROUTE-01, ROUTE-02, and ROUTE-04"
      contains: "requiredPublicRoutes"
    - path: "tests/mcp-restricted-tab.test.js"
      provides: "Executable restricted recovery coverage for ROUTE-03"
      contains: "validRecoveryTools"
    - path: "package.json"
      provides: "Focused route-contract test included in npm test"
      contains: "mcp-tool-routing-contract.test.js"
  key_links:
    - from: "tests/mcp-tool-routing-contract.test.js"
      to: "ws/mcp-tool-dispatcher.js"
      via: "vm.runInNewContext / CommonJS import of dispatcher exports"
      pattern: "mcp-tool-dispatcher\\.js"
    - from: "tests/mcp-tool-routing-contract.test.js"
      to: "ai/tool-definitions.js"
      via: "registry-driven public background tool coverage"
      pattern: "TOOL_REGISTRY"
    - from: "tests/mcp-restricted-tab.test.js"
      to: "mcp-server/build/errors.js"
      via: "dynamic import after npm --prefix mcp-server run build"
      pattern: "mapFSBError"
---

<objective>
Create executable Phase 199 route-contract tests before production routing changes.

Purpose: The phase goal is explicit route mapping, so tests must call real route contracts and restricted-response/error builders. String-presence tests can pass while the bridge still loops through `chrome.runtime.sendMessage`, so they are replaced with behavioral contract checks.
Output: A focused route-contract test, a rewritten restricted-tab test, and `package.json` wiring so the focused contract runs in the project test suite.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/199-mcp-tool-routing-contract/199-CONTEXT.md
@.planning/phases/199-mcp-tool-routing-contract/199-PATTERNS.md
@.planning/phases/198-mcp-bridge-lifecycle-reconnect-state/198-VERIFICATION.md
@.planning/phases/198-mcp-bridge-lifecycle-reconnect-state/198-SECURITY.md
@tests/mcp-restricted-tab.test.js
@tests/mcp-bridge-client-lifecycle.test.js
@tests/mcp-bridge-topology.test.js
@ai/tool-definitions.js
@mcp-server/src/tools/schema-bridge.ts
@mcp-server/src/tools/autopilot.ts
@mcp-server/src/tools/observability.ts
@mcp-server/src/tools/read-only.ts
@mcp-server/src/errors.ts
@package.json

<interfaces>
Current public tool/message sources to verify:
```javascript
// ai/tool-definitions.js
TOOL_REGISTRY.filter(t => t._route === 'background')
// Required browser/tab public tools for this phase:
['navigate', 'go_back', 'go_forward', 'refresh', 'open_tab', 'switch_tab', 'list_tabs']
```

```typescript
// mcp-server/src/tools/autopilot.ts
run_task -> { type: 'mcp:start-automation', payload: { task } }
stop_task -> { type: 'mcp:stop-automation', payload: {} }
get_task_status -> { type: 'mcp:get-status', payload: {} }

// mcp-server/src/tools/observability.ts and read-only.ts
list_sessions -> mcp:list-sessions
get_session_detail -> mcp:get-session
get_logs -> mcp:get-logs
search_memory -> mcp:search-memory
get_memory_stats -> mcp:get-memory
list_tabs -> mcp:get-tabs
```

Required dispatcher exports the tests should drive:
```javascript
getMcpRouteContracts();
hasMcpToolRoute(toolName);
hasMcpMessageRoute(messageType);
buildRestrictedMcpResponse({ currentUrl, pageType, tool, error });
MCP_NAVIGATION_RECOVERY_TOOLS; // ['navigate', 'open_tab', 'switch_tab', 'list_tabs']
```
</interfaces>
</context>

<source_audit>
GOAL: Every MCP tool reaches the intended extension handler through explicit route mapping instead of accidental message-loop behavior. Covered by this plan's executable test contracts and Plans 199-02/199-03 implementation tasks.
REQ ROUTE-01: Covered by browser/tab route assertions for `navigate`, `go_back`, `go_forward`, `refresh`, `open_tab`, `switch_tab`, and `list_tabs`.
REQ ROUTE-02: Covered by autopilot, observability, session, logs, and memory route assertions.
REQ ROUTE-03: Covered by restricted recovery response assertions for blank/new-tab and browser-internal pages.
REQ ROUTE-04: Covered by replacing source-string assertions with executable route-contract tests.
RESEARCH: No RESEARCH.md exists and workflow.research is false; planning uses CONTEXT, PATTERNS, STATE, REQUIREMENTS, and Phase 198 artifacts.
CONTEXT D-01 through D-19: Covered across the plan set; this plan specifically locks D-01, D-04, D-15, D-16, D-17, D-18, and D-19 into failing tests.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add executable MCP route contract test</name>
  <files>tests/mcp-tool-routing-contract.test.js, package.json</files>
  <read_first>
    - tests/mcp-tool-routing-contract.test.js (create this file)
    - package.json
    - tests/mcp-bridge-client-lifecycle.test.js
    - .planning/phases/199-mcp-tool-routing-contract/199-CONTEXT.md
    - .planning/phases/199-mcp-tool-routing-contract/199-PATTERNS.md
    - ai/tool-definitions.js
    - mcp-server/src/tools/autopilot.ts
    - mcp-server/src/tools/observability.ts
    - mcp-server/src/tools/read-only.ts
  </read_first>
  <behavior>
    - Running `node tests/mcp-tool-routing-contract.test.js` before `ws/mcp-tool-dispatcher.js` exists fails with output mentioning `ws/mcp-tool-dispatcher.js` and missing direct route contracts.
    - Running `node tests/mcp-tool-routing-contract.test.js --group=browser` after Plan 199-02 passes for browser/tab routes without requiring autopilot/observability routes.
    - Running `node tests/mcp-tool-routing-contract.test.js` after Plan 199-03 passes only if all public Phase 199 routes have direct route contracts.
  </behavior>
  <action>Create `tests/mcp-tool-routing-contract.test.js` using the plain Node assertion counter style from `tests/mcp-bridge-client-lifecycle.test.js`. Load `ai/tool-definitions.js` and the new `ws/mcp-tool-dispatcher.js` with `require()` or `vm.runInNewContext`; support `--group=browser`, `--group=autopilot`, and no group argument. Define `requiredPublicRoutes` with these exact entries: `navigate`, `go_back`, `go_forward`, `refresh`, `open_tab`, `switch_tab`, `list_tabs`, `run_task`, `stop_task`, `get_task_status`, `get_site_guide`, `list_sessions`, `get_session_detail`, `get_logs`, `search_memory`, `get_memory_stats`, `read_page`, and `get_dom_snapshot`. Assert `getMcpRouteContracts()` returns stable `toolRoutes` and `messageRoutes` objects, `hasMcpToolRoute(tool)` is true for every tool above, and `hasMcpMessageRoute(type)` is true for `mcp:get-tabs`, `mcp:get-dom`, `mcp:read-page`, `mcp:start-automation`, `mcp:stop-automation`, `mcp:get-status`, `mcp:get-site-guides`, `mcp:list-sessions`, `mcp:get-session`, `mcp:get-logs`, `mcp:search-memory`, and `mcp:get-memory`. Assert the background tools from `TOOL_REGISTRY.filter(t => t._route === 'background')` include direct route contracts for at least `navigate`, `go_back`, `go_forward`, `refresh`, `open_tab`, `switch_tab`, `list_tabs`, `get_site_guide`, `execute_js`, `report_progress`, `complete_task`, `partial_task`, and `fail_task`; the test may explicitly mark vault tools out of Phase 199 because D-14 says not to expand vault exposure. Update the root `package.json` test script to run `node tests/mcp-tool-routing-contract.test.js` immediately after `node tests/mcp-bridge-topology.test.js` and before `node tests/mcp-restricted-tab.test.js`.</action>
  <verify>
    <automated>bash -lc 'node tests/mcp-tool-routing-contract.test.js >/tmp/phase199-routing-red.log 2>&1; status=$?; test "$status" -ne 0 && rg "mcp-tool-dispatcher\\.js|Missing direct MCP route|missing direct route" /tmp/phase199-routing-red.log && rg "mcp-tool-routing-contract.test.js" package.json'</automated>
  </verify>
  <acceptance_criteria>
    - `rg "const requiredPublicRoutes" tests/mcp-tool-routing-contract.test.js` returns a match.
    - `rg "navigate|go_back|go_forward|refresh|open_tab|switch_tab|list_tabs|run_task|stop_task|get_task_status|list_sessions|get_session_detail|get_logs|search_memory|get_memory_stats" tests/mcp-tool-routing-contract.test.js` returns matches for every listed public tool.
    - `rg "hasMcpToolRoute|hasMcpMessageRoute|getMcpRouteContracts" tests/mcp-tool-routing-contract.test.js` returns all three exported contract names.
    - `rg "mcp-tool-routing-contract.test.js" package.json` returns a match in the `scripts.test` command.
    - The RED verification command exits 0 by proving the new test fails before implementation for missing direct route contracts.
  </acceptance_criteria>
  <done>The executable route-contract test exists, is wired into `npm test`, and fails for missing direct dispatcher contracts rather than passing on source-string mentions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace restricted-tab source checks with executable recovery contract</name>
  <files>tests/mcp-restricted-tab.test.js</files>
  <read_first>
    - tests/mcp-restricted-tab.test.js
    - tests/mcp-tool-routing-contract.test.js
    - .planning/phases/199-mcp-tool-routing-contract/199-CONTEXT.md
    - .planning/phases/199-mcp-tool-routing-contract/199-PATTERNS.md
    - mcp-server/src/errors.ts
    - background.js
  </read_first>
  <behavior>
    - Restricted recovery assertions call `buildRestrictedMcpResponse()` and `mapFSBError()` instead of scanning `background.js` source strings.
    - Blank/new-tab recovery and `chrome://settings/` recovery both expose exactly `navigate`, `open_tab`, `switch_tab`, and `list_tabs`.
    - No restricted recovery assertion expects or accepts `run_task`.
  </behavior>
  <action>Rewrite `tests/mcp-restricted-tab.test.js` to remove `backgroundSource.includes(...)` route assertions. Keep the existing plain Node assertion counter and dynamic import of `mcp-server/build/errors.js`. Load `buildRestrictedMcpResponse` and `MCP_NAVIGATION_RECOVERY_TOOLS` from `ws/mcp-tool-dispatcher.js`; while this file is still absent, the test should fail loudly with a missing dispatcher/module error. Add cases for `chrome://newtab/`, `about:blank`, `chrome://settings/`, `chrome://extensions/`, `chrome://history/`, and `chrome://downloads/`. For each case, assert `response.errorCode === 'restricted_active_tab'`, `response.validRecoveryTools` deep-equals `['navigate', 'open_tab', 'switch_tab', 'list_tabs']`, and `JSON.stringify(response)` does not contain `run_task`. After `npm --prefix mcp-server run build`, call `mapFSBError({ success: false, errorCode: 'restricted_active_tab', pageType: 'Chrome internal page', currentUrl: 'chrome://newtab/', validRecoveryTools: ['navigate', 'open_tab', 'switch_tab', 'list_tabs'] })` and assert the text includes `navigate`, `open_tab`, `switch_tab`, and `list_tabs`, while omitting `run_task`.</action>
  <verify>
    <automated>bash -lc 'npm --prefix mcp-server run build >/tmp/phase199-build.log 2>&1 && node tests/mcp-restricted-tab.test.js >/tmp/phase199-restricted-red.log 2>&1; status=$?; test "$status" -ne 0 && rg "mcp-tool-dispatcher\\.js|run_task|validRecoveryTools" /tmp/phase199-restricted-red.log tests/mcp-restricted-tab.test.js'</automated>
  </verify>
  <acceptance_criteria>
    - `rg "backgroundSource\\.includes" tests/mcp-restricted-tab.test.js` returns no matches.
    - `rg "buildRestrictedMcpResponse|MCP_NAVIGATION_RECOVERY_TOOLS|validRecoveryTools" tests/mcp-restricted-tab.test.js` returns all three contract names.
    - `rg "chrome://newtab|about:blank|chrome://settings|chrome://extensions|chrome://history|chrome://downloads" tests/mcp-restricted-tab.test.js` returns all six restricted-page cases.
    - `rg "includes\\('run_task'\\).*FAIL|omits run_task|does not contain run_task" tests/mcp-restricted-tab.test.js` returns a negative assertion against `run_task`.
    - The RED verification command exits 0 by proving the rewritten executable test fails until the dispatcher and server error mapper are implemented.
  </acceptance_criteria>
  <done>The restricted-tab regression test executes dispatcher/error code and encodes navigation-only recovery for blank/new-tab and browser-internal pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness -> extension VM/module | Node tests load extension routing code without Chrome privileges and inspect only route metadata and sanitized responses. |
| MCP server error mapper -> MCP client | Structured extension errors are converted into user-facing MCP tool responses. |
| Public tool registry -> route contract | Registered public MCP tools are compared against direct route declarations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-199-01-01 | Spoofing | `tests/mcp-tool-routing-contract.test.js` | mitigate | Assert public MCP route names map to explicit dispatcher entries; unknown or missing routes fail the test instead of falling through. |
| T-199-01-02 | Information Disclosure | `tests/mcp-restricted-tab.test.js` | mitigate | Assert restricted recovery payloads expose only sanitized `validRecoveryTools`, `currentUrl`, `pageType`, and no vault/session/log data. |
| T-199-01-03 | Elevation of Privilege | Route fallback behavior | mitigate | Test for direct route contracts so Phase 199 routes cannot bypass allowlist checks through content-script fallback. |
| T-199-01-04 | Denial of Service | Route queue/progress behavior | mitigate | Include `run_task`, `stop_task`, and `get_task_status` in contract coverage so Plan 199-03 preserves queue/progress routes explicitly. |
</threat_model>

<verification>
Plan 199-01 is complete when both task RED commands exit 0, `package.json` includes `tests/mcp-tool-routing-contract.test.js`, and both tests contain executable imports/calls instead of route source-string assertions.
</verification>

<success_criteria>
- `tests/mcp-tool-routing-contract.test.js` exists and fails for missing direct route contracts before implementation.
- `tests/mcp-restricted-tab.test.js` no longer relies on `background.js` source-string route mentions.
- `package.json` runs the route-contract test as part of `npm test`.
- ROUTE-04 has executable regression coverage that later plans must make green.
</success_criteria>

<output>
After completion, create `.planning/phases/199-mcp-tool-routing-contract/199-01-SUMMARY.md`.
</output>
