---
phase: 200-doctor-status-watch-recovery-messaging
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - "package.json"
  - "mcp-server/src/version.ts"
  - "mcp-server/server.json"
  - "mcp-server/README.md"
  - "README.md"
  - "tests/mcp-version-parity.test.js"
autonomous: true
requirements:
  - DIAG-01
  - DIAG-04
must_haves:
  truths:
    - "The MCP package version surface agrees on one version string across package metadata, runtime constant, registry metadata, CLI output, and docs."
    - "Release docs tell users to run `doctor` and `status --watch` before manual tinkering when the MCP bridge fails."
    - "Version drift fails in automated tests before release."
  artifacts:
    - path: "mcp-server/src/version.ts"
      provides: "Canonical MCP runtime version constant"
      contains: "FSB_MCP_VERSION = '0.6.0'"
    - path: "mcp-server/server.json"
      provides: "Registry metadata aligned to the runtime/package version"
      contains: "\"version\": \"0.6.0\""
    - path: "tests/mcp-version-parity.test.js"
      provides: "Automated parity regression for package/runtime/docs/CLI output"
      contains: "version parity"
  key_links:
    - from: "mcp-server/src/version.ts"
      to: "mcp-server/src/index.ts"
      via: "help and doctor output read the runtime MCP version"
      pattern: "FSB MCP Server"
    - from: "mcp-server/src/version.ts"
      to: "mcp-server/server.json"
      via: "runtime constant and registry metadata must match"
      pattern: "0.6.0"
    - from: "tests/mcp-version-parity.test.js"
      to: "README.md"
      via: "docs parity and troubleshooting flow assertions"
      pattern: "status --watch"
---

<objective>
Make the MCP version surface and troubleshooting docs agree.

Purpose: Phase 200 is the last place users should have to guess whether an MCP failure is caused by drift between package metadata, runtime output, and documentation. The version story and the docs flow need to be explicit and testable.
Output: Canonical `0.6.0` MCP metadata across runtime and registry surfaces, docs that route users through doctor/status before tinkering, and a parity regression test wired into the root test suite.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/200-doctor-status-watch-recovery-messaging/200-CONTEXT.md
@package.json
@README.md
@mcp-server/package.json
@mcp-server/src/version.ts
@mcp-server/server.json
@mcp-server/src/index.ts
@mcp-server/src/install.ts
@mcp-server/README.md
</context>

<source_audit>
GOAL: Covered by aligning runtime/package/docs metadata and documenting the recommended failure flow.
REQ DIAG-04: Covered directly by version alignment and parity tests.
REQ DIAG-01: Reinforced by docs that steer users into `doctor` and `status --watch` before manual tinkering.
CONTEXT D-13 through D-15: Covered directly by the canonical `0.6.0` version target, parity regression, and root/package doc cleanup.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Align the MCP runtime and registry metadata to `0.6.0`</name>
  <files>mcp-server/src/version.ts, mcp-server/server.json, mcp-server/README.md, README.md</files>
  <read_first>
    - mcp-server/package.json
    - mcp-server/src/version.ts
    - mcp-server/server.json
    - mcp-server/README.md
    - README.md
    - .planning/phases/200-doctor-status-watch-recovery-messaging/200-CONTEXT.md
  </read_first>
  <action>Treat `mcp-server/package.json` version `0.6.0` as the canonical MCP package version and align the runtime/registry/docs to that exact string. Change `FSB_MCP_VERSION` in `mcp-server/src/version.ts` from `0.5.2` to `0.6.0`. Change both `version` fields in `mcp-server/server.json` to `0.6.0`. In `mcp-server/README.md`, replace the explicit sentence `The extension pairing contract did not change in 0.5.2.` with wording that uses `0.6.0` or removes the explicit version while still saying the pairing contract did not change. Add a short troubleshooting section that tells users to run `npx -y fsb-mcp-server doctor` first, then `npx -y fsb-mcp-server status --watch`, and only then try manual reinstall/restart steps if the reported layer calls for it. In the root `README.md`, add a short MCP troubleshooting note in the MCP package section that points readers to `doctor` and `status --watch`; do not add a conflicting MCP version banner to the root README. Preserve the extension app version story (`v0.9.x`) in the root README, but keep any explicit `fsb-mcp-server` version callouts aligned to `0.6.0` or absent.</action>
  <verify>
    <automated>rg "0\\.6\\.0|status --watch|doctor" mcp-server/src/version.ts mcp-server/server.json mcp-server/README.md README.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg "FSB_MCP_VERSION = '0.6.0'" mcp-server/src/version.ts` returns a match.
    - `rg "\"version\": \"0.6.0\"" mcp-server/server.json` returns both registry version fields.
    - `rg "status --watch|doctor" mcp-server/README.md README.md` returns the new troubleshooting flow in both docs.
    - `rg "0\\.5\\.2" mcp-server/src/version.ts mcp-server/server.json mcp-server/README.md` returns no matches.
  </acceptance_criteria>
  <done>The MCP runtime constant, registry metadata, and docs all align to `0.6.0`, and the docs now route failures through doctor/status before manual tinkering.</done>
</task>

<task type="auto">
  <name>Task 2: Add a parity regression test and wire it into the root suite</name>
  <files>tests/mcp-version-parity.test.js, package.json, mcp-server/src/version.ts, mcp-server/server.json, mcp-server/README.md, README.md</files>
  <read_first>
    - package.json
    - mcp-server/package.json
    - mcp-server/src/version.ts
    - mcp-server/server.json
    - mcp-server/src/index.ts
    - mcp-server/src/install.ts
    - mcp-server/README.md
    - README.md
  </read_first>
  <action>Create `tests/mcp-version-parity.test.js` using the plain Node assertion style. Read `mcp-server/package.json`, `mcp-server/src/version.ts`, `mcp-server/server.json`, `mcp-server/README.md`, and `README.md`. Extract the canonical version from `mcp-server/package.json` and assert it equals `0.6.0`. Assert `FSB_MCP_VERSION` in `mcp-server/src/version.ts` matches that value. Assert `mcp-server/server.json.version` and `mcp-server/server.json.packages[0].version` match that value. Build the MCP server, then capture `node mcp-server/build/index.js help` and `node mcp-server/build/index.js install` output; assert both print `FSB MCP Server 0.6.0`. For docs, assert `mcp-server/README.md` contains `status --watch` and `doctor`, and if either README contains an explicit `fsb-mcp-server@X.Y.Z` or `FSB MCP Server X.Y.Z` reference, assert that version is `0.6.0`. Update the root `package.json` `test` script to run `node tests/mcp-version-parity.test.js` immediately after the new recovery-messaging test so parity failures stop the suite before later unrelated tests.</action>
  <verify>
    <automated>npm --prefix mcp-server run build && node tests/mcp-version-parity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `rg "0\\.6\\.0" tests/mcp-version-parity.test.js` returns the canonical version target.
    - `rg "mcp-version-parity.test.js" package.json` returns a match in the root `scripts.test` command.
    - `rg "status --watch|doctor" tests/mcp-version-parity.test.js` returns the docs-flow assertions.
    - `npm --prefix mcp-server run build && node tests/mcp-version-parity.test.js` exits 0.
  </acceptance_criteria>
  <done>Version drift and missing doctor/status troubleshooting docs fail in automation before release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime/package metadata -> published docs | Version drift here misleads installation and debugging flows. |
| CLI output -> operator interpretation | Incorrect version output can make users chase the wrong package build or docs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-03-01 | Tampering | Version parity surface | mitigate | Assert `0.6.0` across package metadata, runtime constant, registry metadata, and CLI output in one regression test. |
| T-200-03-02 | Repudiation | Troubleshooting docs | mitigate | Add explicit doctor/status-first troubleshooting flow in both package and root docs. |
| T-200-03-03 | Denial of Service | Release pipeline | mitigate | Fail the root test suite when parity drifts instead of discovering it after publish. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix mcp-server run build`
- [ ] `node tests/mcp-version-parity.test.js`
- [ ] `node mcp-server/build/index.js help` and `node mcp-server/build/index.js install` both print `FSB MCP Server 0.6.0`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- MCP runtime/package/docs agree on `0.6.0`
- Docs route failures through doctor/status before manual tinkering
</success_criteria>

<output>
After completion, create `.planning/phases/200-doctor-status-watch-recovery-messaging/200-03-SUMMARY.md`
</output>
