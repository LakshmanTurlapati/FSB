---
phase: 97-tool-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai/ai-integration.js
autonomous: true
requirements:
  - TOOL-01
  - TOOL-03

must_haves:
  truths:
    - "AI system prompt includes CDP coordinate tools in the CLI command reference"
    - "AI system prompt includes selecttextrange and dropfile in the CLI command reference"
    - "isValidTool returns true for all 7 new tool names"
  artifacts:
    - path: "ai/ai-integration.js"
      provides: "CLI_COMMAND_TABLE with CDP COORDINATE section and 7 new tools"
      contains: "CDP COORDINATE"
    - path: "ai/ai-integration.js"
      provides: "isValidTool accepting 7 new tool names"
      contains: "cdpClickAt"
  key_links:
    - from: "CLI_COMMAND_TABLE"
      to: "system prompt via _buildCoreTools()"
      via: "string constant inclusion"
      pattern: "CLI_COMMAND_TABLE"
    - from: "isValidTool"
      to: "automation loop action gating"
      via: "tool name validation"
      pattern: "isValidTool"
---

<objective>
Add all 7 new CDP/interaction tools to the AI prompt reference table (CLI_COMMAND_TABLE) and the tool validator (isValidTool) in ai-integration.js.

Purpose: The AI needs to know these tools exist (TOOL-01) and the automation loop needs to accept them as valid (TOOL-03). Without both, the AI will never emit these commands and even if it did they would be rejected.

Output: Updated ai/ai-integration.js with new CLI_COMMAND_TABLE entries and expanded isValidTool array.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@ai/ai-integration.js

<interfaces>
<!-- Existing CLI_COMMAND_TABLE format (lines 14-119 of ai/ai-integration.js) -->
<!-- Uses markdown table grouped by category. Each category has a header and table. -->
<!-- Pattern: -->
<!--   CATEGORY_NAME: -->
<!--   | Verb | Args | Description | Example | -->
<!--   |------|------|-------------|---------|  -->
<!--   | verb | args | desc | example | -->

<!-- Existing isValidTool format (lines 4191-4225 of ai/ai-integration.js) -->
<!-- Returns boolean from flat .includes(tool) check on array of camelCase strings -->
<!-- Current tool categories in array: Basic DOM, Multi-tab, Scroll, Advanced DOM, -->
<!--   Code editor, Data persistence, Google Sheets, Mechanical, Content reading -->

<!-- 7 new tool function signatures from content/actions.js: -->
<!--   cdpClickAt({x, y, shiftKey, ctrlKey, altKey}) -->
<!--   cdpClickAndHold({x, y, holdMs}) -->
<!--   cdpDrag({startX, startY, endX, endY, steps, stepDelayMs, shiftKey, ctrlKey, altKey}) -->
<!--   cdpDragVariableSpeed({startX, startY, endX, endY, steps, minDelayMs, maxDelayMs}) -->
<!--   cdpScrollAt({x, y, deltaX, deltaY}) -->
<!--   selectTextRange({selector, startOffset, endOffset}) -->
<!--   dropfile({selector, fileName, fileContent, mimeType}) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 7 new tools to CLI_COMMAND_TABLE constant</name>
  <files>ai/ai-integration.js</files>
  <read_first>
    - ai/ai-integration.js (lines 1-119 for CLI_COMMAND_TABLE)
    - content/actions.js (lines 5244-5325 for CDP tool signatures, lines 4007-4060 for selectTextRange, lines 4901-4970 for dropfile)
  </read_first>
  <action>
In ai/ai-integration.js, insert two new category sections into the CLI_COMMAND_TABLE constant string, placed AFTER the existing "COMPLETION:" section (before the closing backtick on line 119):

**Section 1 -- CDP COORDINATE TOOLS (insert after COMPLETION section):**

```
CDP COORDINATE TOOLS (for canvas, maps, non-DOM elements -- use viewport pixel coordinates):
| Verb | Args | Description | Example |
|------|------|-------------|---------|
| clickat | x y [--shift --ctrl --alt] | CDP click at viewport coordinates | clickat 500 300 |
| clickandhold | x y [--hold ms] | CDP click-and-hold at coordinates | clickandhold 400 250 --hold 3000 |
| drag | startX startY endX endY [--steps n] [--delay ms] | CDP drag between coordinates | drag 100 200 500 200 --steps 15 --delay 30 |
| dragvariablespeed | startX startY endX endY [--steps n] [--mindelay ms] [--maxdelay ms] | CDP drag with speed variation | dragvariablespeed 100 200 500 200 --steps 20 --mindelay 5 --maxdelay 40 |
| scrollat | x y [--dx pixels] [--dy pixels] | CDP scroll at specific coordinates | scrollat 400 300 --dy -120 |
```

**Section 2 -- TEXT SELECTION AND FILE TOOLS (insert after CDP COORDINATE TOOLS section):**

```
TEXT SELECTION & FILE TOOLS:
| Verb | Args | Description | Example |
|------|------|-------------|---------|
| selecttextrange | ref startOffset endOffset | Select text by character offsets within element | selecttextrange e12 5 20 |
| dropfile | ref "fileName" ["content"] ["mimeType"] | Simulate file drop on dropzone element | dropfile e8 "report.pdf" |
```

Do NOT modify any other part of the CLI_COMMAND_TABLE. Preserve the existing sections exactly as they are.
  </action>
  <verify>
    <automated>grep -c "clickat\|clickandhold\|drag \|dragvariablespeed\|scrollat\|selecttextrange\|dropfile" ai/ai-integration.js</automated>
  </verify>
  <acceptance_criteria>
    - ai/ai-integration.js contains the string "CDP COORDINATE TOOLS"
    - ai/ai-integration.js contains "| clickat | x y" in the CLI_COMMAND_TABLE
    - ai/ai-integration.js contains "| clickandhold | x y"
    - ai/ai-integration.js contains "| drag | startX startY endX endY"
    - ai/ai-integration.js contains "| dragvariablespeed | startX startY endX endY"
    - ai/ai-integration.js contains "| scrollat | x y"
    - ai/ai-integration.js contains "| selecttextrange | ref startOffset endOffset"
    - ai/ai-integration.js contains "| dropfile | ref"
    - ai/ai-integration.js contains "TEXT SELECTION & FILE TOOLS"
    - grep count of new verb names in ai-integration.js is >= 7
  </acceptance_criteria>
  <done>CLI_COMMAND_TABLE contains all 7 new tools in two new category sections (CDP COORDINATE TOOLS and TEXT SELECTION & FILE TOOLS) with correct verb names, argument specs, descriptions, and examples</done>
</task>

<task type="auto">
  <name>Task 2: Add 7 new tool names to isValidTool validator</name>
  <files>ai/ai-integration.js</files>
  <read_first>
    - ai/ai-integration.js (lines 4190-4226 for isValidTool method)
  </read_first>
  <action>
In ai/ai-integration.js, in the isValidTool method (around line 4191), add a new comment block and 7 entries to the array inside the .includes() call. Insert AFTER the existing "// Content reading tools" line and its 'readPage' entry, BEFORE the closing bracket of the array:

```javascript
      // CDP coordinate tools (v0.9.8 -- tool parity with MCP)
      'cdpClickAt', 'cdpClickAndHold', 'cdpDrag', 'cdpDragVariableSpeed', 'cdpScrollAt',

      // Text selection and file tools (v0.9.8 -- tool parity with MCP)
      'selectTextRange', 'dropfile'
```

The 7 new entries must be:
1. cdpClickAt
2. cdpClickAndHold
3. cdpDrag
4. cdpDragVariableSpeed
5. cdpScrollAt
6. selectTextRange
7. dropfile

These are the exact camelCase names matching FSB.tools keys in content/actions.js.
  </action>
  <verify>
    <automated>grep -c "'cdpClickAt'\|'cdpClickAndHold'\|'cdpDrag'\|'cdpDragVariableSpeed'\|'cdpScrollAt'\|'selectTextRange'\|'dropfile'" ai/ai-integration.js</automated>
  </verify>
  <acceptance_criteria>
    - ai/ai-integration.js isValidTool method contains 'cdpClickAt' in the includes array
    - ai/ai-integration.js isValidTool method contains 'cdpClickAndHold' in the includes array
    - ai/ai-integration.js isValidTool method contains 'cdpDrag' in the includes array
    - ai/ai-integration.js isValidTool method contains 'cdpDragVariableSpeed' in the includes array
    - ai/ai-integration.js isValidTool method contains 'cdpScrollAt' in the includes array
    - ai/ai-integration.js isValidTool method contains 'selectTextRange' in the includes array
    - ai/ai-integration.js isValidTool method contains 'dropfile' in the includes array
    - grep count of the 7 new tool names in isValidTool section is exactly 7
    - Comment "// CDP coordinate tools (v0.9.8 -- tool parity with MCP)" present
  </acceptance_criteria>
  <done>isValidTool returns true for all 7 new tool names (cdpClickAt, cdpClickAndHold, cdpDrag, cdpDragVariableSpeed, cdpScrollAt, selectTextRange, dropfile)</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep "CDP COORDINATE TOOLS" ai/ai-integration.js` returns a match
2. `grep "'cdpClickAt'" ai/ai-integration.js` returns a match in the isValidTool array
3. All 7 CLI verbs appear in CLI_COMMAND_TABLE
4. All 7 camelCase tool names appear in isValidTool
5. No other parts of ai-integration.js are modified
</verification>

<success_criteria>
- CLI_COMMAND_TABLE has 2 new category sections with all 7 tools documented
- isValidTool accepts all 7 new tool names
- Existing tool entries are unchanged
- File parses without syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/97-tool-parity/97-01-SUMMARY.md`
</output>
