---
phase: 115-canvas-vision
plan: 04
type: execute
wave: 3
depends_on:
  - "115-03"
files_modified:
  - canvas-interceptor.js
  - background.js
  - content/dom-analysis.js
autonomous: false
requirements:
  - VISION-07

must_haves:
  truths:
    - "Canvas vision works on Excalidraw (draw shapes, verify scene shows them)"
    - "Re-render trigger captures existing content on already-loaded pages"
    - "Pixel fallback works on pages without interception"
    - "Canvas vision works on at least 10 of 15 canvas apps in FSB scope"
  artifacts:
    - path: "canvas-interceptor.js"
      provides: "Battle-tested interceptor with any fixes from testing"
    - path: "background.js"
      provides: "Robust canvas scene pipeline"
  key_links:
    - from: "canvas-interceptor.js"
      to: "background.js fetchCanvasScene"
      via: "Runtime.evaluate reading window.__canvasCallLog"
      pattern: "__canvasCallLog"
---

<objective>
Test and validate the canvas vision pipeline end-to-end across canvas-based apps. Fix any issues discovered during testing to ensure VISION-07 coverage (10+ of 15 canvas apps).

Purpose: Validation that the interceptor, pixel fallback, and DOM snapshot integration work together on real-world canvas apps.

Output: Battle-tested canvas vision pipeline, any bugfixes applied
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/115-canvas-vision/115-CONTEXT.md
@.planning/phases/115-canvas-vision/115-01-SUMMARY.md
@.planning/phases/115-canvas-vision/115-02-SUMMARY.md
@.planning/phases/115-canvas-vision/115-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create validation test script</name>
  <files>test-canvas-vision.js</files>
  <read_first>
    - canvas-interceptor.js (the interceptor to validate)
    - background.js (fetchCanvasScene and formatCanvasSceneMarkdown functions)
    - content/dom-analysis.js (getCanvasPixelFallback function)
  </read_first>
  <action>
Create a standalone Node.js test script `test-canvas-vision.js` in the project root that validates the canvas vision code structurally (not in-browser -- that requires the checkpoint).

The script MUST verify:

1. **canvas-interceptor.js integrity:**
   - File exists and is syntactically valid JS (wrap in Function constructor)
   - Contains all 20 tracked method names
   - Contains `__canvasCallLog` initialization
   - Contains `getCanvasScene` function
   - Contains `triggerCanvasRerender` function
   - File size is under 5KB

2. **manifest.json content_scripts:**
   - content_scripts array exists
   - Entry has world: MAIN, run_at: document_start
   - Entry references canvas-interceptor.js

3. **background.js integration:**
   - Contains `fetchCanvasScene` function
   - Contains `formatCanvasSceneMarkdown` function
   - Contains `CANVAS SCENE` string (markdown section header)
   - Contains `__canvasCallLog` reference (reads interceptor data)
   - Contains `pixel-fallback` and `interceptor` source strings

4. **content/dom-analysis.js pixel fallback:**
   - Contains `getCanvasPixelFallback` function
   - Contains `FSB.getCanvasPixelFallback` export

5. **content/messaging.js handler:**
   - Contains `getCanvasPixelFallback` case in message handler

Print a summary table:
```
Canvas Vision Validation
========================
[PASS] canvas-interceptor.js exists (N bytes)
[PASS] Interceptor has all 20 method wrappers
[PASS] manifest.json has MAIN world content_script
[PASS] background.js has fetchCanvasScene
[PASS] background.js has formatCanvasSceneMarkdown
[PASS] dom-analysis.js has getCanvasPixelFallback
[PASS] messaging.js has handler
========================
N/N checks passed
```

Exit with code 0 if all pass, code 1 if any fail.
  </action>
  <verify>
    <automated>node test-canvas-vision.js</automated>
  </verify>
  <acceptance_criteria>
    - test-canvas-vision.js exists and runs without errors
    - All 7+ structural checks pass
    - Script exits with code 0 on success
    - Script provides clear PASS/FAIL output for each check
  </acceptance_criteria>
  <done>Structural validation script passes all checks</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual validation on Excalidraw</name>
  <what-built>
Complete canvas vision pipeline:
1. canvas-interceptor.js wraps Canvas 2D prototype methods and logs draw calls
2. manifest.json injects it at document_start in MAIN world on all pages
3. background.js reads intercepted data via CDP Runtime.evaluate
4. DOM snapshots include CANVAS SCENE section with text labels, rectangles, and paths
5. Pixel fallback works when interception data is unavailable
  </what-built>
  <how-to-verify>
1. Load the extension in Chrome (chrome://extensions -> Load unpacked)
2. Open Excalidraw (https://excalidraw.com)
3. Open Chrome DevTools console on the Excalidraw tab
4. Verify interceptor is installed: type `window.__canvasInterceptorInstalled` -- should return `true`
5. Draw a rectangle and add text "Hello" inside it
6. In the console, run `window.getCanvasScene()` and verify:
   - `texts` array contains an entry with `text: "Hello"`
   - `rects` array or `paths` array contains entries (roughjs generates paths, not rects)
   - `totalCalls` is greater than 0
7. Run the FSB automation: ask FSB to "draw a rectangle and write Test inside it on Excalidraw"
8. After automation, check that the DOM snapshot in the automation log contains `## CANVAS SCENE`
9. Open a non-canvas page (e.g., google.com) and verify no errors in the console (interceptor should be silent no-op)

Expected behavior:
- Interceptor installed on every page load
- Canvas scene data visible in getCanvasScene() after drawing
- CANVAS SCENE section appears in DOM snapshots during automation
- No errors on pages without canvas elements
  </how-to-verify>
  <resume-signal>Type "approved" if canvas vision works, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `node test-canvas-vision.js` -- all structural checks pass
2. Manual verification confirms canvas scene data flows end-to-end on Excalidraw
3. No console errors on non-canvas pages
</verification>

<success_criteria>
- Structural validation script passes all checks
- Canvas vision works on Excalidraw (texts and shapes visible in scene data)
- Re-render trigger works on already-loaded pages
- No errors on non-canvas pages
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/115-canvas-vision/115-04-SUMMARY.md`
</output>
