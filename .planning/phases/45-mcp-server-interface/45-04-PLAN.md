---
phase: 45-mcp-server-interface
plan: 04
type: execute
wave: 3
depends_on: ["45-02", "45-03"]
files_modified:
  - mcp-server/src/index.ts
autonomous: false
requirements:
  - MCP-04
  - MCP-05
  - MCP-08

must_haves:
  truths:
    - "MCP server starts and lists all registered tools, resources, and prompts"
    - "Server responds correctly to MCP protocol initialization"
    - "Build output is complete and executable"
  artifacts:
    - path: "mcp-server/build/index.js"
      provides: "Compiled entry point"
      min_lines: 20
  key_links:
    - from: "mcp-server/build/index.js"
      to: "@modelcontextprotocol/sdk"
      via: "MCP stdio transport"
      pattern: "StdioServerTransport"
---

<objective>
Verify the complete MCP server works end-to-end: builds, starts, responds to MCP protocol, and lists all tools/resources/prompts correctly.

Purpose: This is the integration test -- confirming that all three prior plans produced a working, connected system before handing off to the user.

Output: Verified working MCP server ready for native host installation and use.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/45-mcp-server-interface/45-CONTEXT.md
@.planning/phases/45-mcp-server-interface/45-UI-SPEC.md
@mcp-server/src/index.ts
@mcp-server/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build, fix any compilation issues, and verify MCP protocol response</name>
  <files>mcp-server/src/index.ts</files>
  <read_first>
    mcp-server/src/index.ts
    mcp-server/src/tools/autopilot.ts
    mcp-server/src/tools/manual.ts
    mcp-server/src/tools/read-only.ts
    mcp-server/src/resources/index.ts
    mcp-server/src/prompts/index.ts
    mcp-server/tsconfig.json
  </read_first>
  <action>
    1. Run `cd mcp-server && npx tsc` and fix any compilation errors. Common issues to look for:
       - Import path mismatches (must end in `.js` for ESM)
       - Type incompatibilities between McpServer methods and our call signatures
       - Missing type imports
       - Zod schema compatibility issues between our zod version and SDK's expected zod version

    2. Verify the build output is complete:
       ```bash
       ls -la build/
       ls -la build/tools/
       ls -la build/resources/
       ls -la build/prompts/
       ```

    3. Test the MCP server starts and responds to initialization. Send a JSON-RPC initialize request via stdin and verify the response:
       ```bash
       echo '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"test","version":"1.0.0"}}}' | timeout 5 node build/index.js 2>/dev/null | head -1
       ```
       This should return a JSON-RPC response with server capabilities listing tools, resources, and prompts.

       If the server hangs waiting for the native bridge (which won't be available in this test), add a graceful fallback in index.ts: wrap the `bridge.connect()` call in a try/catch that logs a warning but doesn't prevent the server from starting. The bridge should operate in "disconnected" mode -- all tool calls will return "Extension not connected" errors, but the server itself should still respond to MCP protocol messages.

    4. If the initialize test works, also send a tools/list request:
       ```bash
       printf '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"test","version":"1.0.0"}}}\n{"jsonrpc":"2.0","id":2,"method":"notifications/initialized"}\n{"jsonrpc":"2.0","id":3,"method":"tools/list"}' | timeout 5 node build/index.js 2>/dev/null
       ```
       Verify the tools/list response contains `run_task`, `click`, `navigate`, `read_page`, etc.

    5. Count total tools in the response to verify all 33+ are registered.

    6. If any issues found, fix them in the source files, recompile, and re-test.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/laksh/OneDrive/Documents/Codes/FSB/mcp-server && npx tsc 2>&1 | tail -3 && echo '---BUILD FILES---' && ls build/index.js build/bridge.js build/native-host-shim.js build/queue.js build/errors.js build/types.js build/server.js build/tools/autopilot.js build/tools/manual.js build/tools/read-only.js build/resources/index.js build/prompts/index.js 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc` exits with code 0 (no compilation errors)
    - All 12 expected .js files exist in build/: index.js, bridge.js, native-host-shim.js, queue.js, errors.js, types.js, server.js, tools/autopilot.js, tools/manual.js, tools/read-only.js, resources/index.js, prompts/index.js
    - MCP server starts without crashing when run with `node build/index.js`
    - Server responds to JSON-RPC initialize request with a valid response containing `capabilities`
    - bridge.connect() failure does not prevent server from starting (graceful degradation)
  </acceptance_criteria>
  <done>MCP server builds cleanly, all modules compile, server starts and responds to MCP protocol. Bridge operates in disconnected mode gracefully when extension is not available.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete MCP server</name>
  <files>mcp-server/build/index.js</files>
  <action>
    Present the complete MCP server for human verification. The automated build and protocol tests have passed. The user should now verify:
    1. The MCP server builds with `cd mcp-server && npm run build`
    2. The server starts with `node mcp-server/build/index.js` (logs to stderr, Ctrl+C to stop)
    3. Tool count matches expectations (33+ tools across autopilot, manual, read-only)
    4. The install script at mcp-server/scripts/install-host.js looks correct
    5. The .mcp.json at repo root contains valid fsb-browser server config
    6. manifest.json has the nativeMessaging permission
    7. (Optional) Install native host with `node mcp-server/scripts/install-host.js --extension-id=YOUR_ID`
    8. (Optional) Test with Claude Code: `claude mcp add --transport stdio fsb-browser -- node /path/to/mcp-server/build/index.js`
  </action>
  <verify>User confirms "approved" or reports issues</verify>
  <done>User has verified the MCP server is complete and functional.</done>
</task>

</tasks>

<verification>
1. `cd mcp-server && npx tsc` -- zero errors
2. `node mcp-server/build/index.js` starts without crash
3. MCP initialize handshake returns valid capabilities JSON
4. tools/list returns 33+ tools
5. resources/list returns 5 resources
6. prompts/list returns 4 prompts
7. .mcp.json at repo root is valid JSON
8. manifest.json contains nativeMessaging permission
</verification>

<success_criteria>
- MCP server builds and starts successfully
- Full tool surface (33+ tools) verified via MCP protocol
- All resources and prompts accessible via MCP protocol
- Extension manifest updated with nativeMessaging permission
- Native host install script functional for user's OS
- .mcp.json enables Claude Code integration
</success_criteria>

<output>
After completion, create `.planning/phases/45-mcp-server-interface/45-04-SUMMARY.md`
</output>
